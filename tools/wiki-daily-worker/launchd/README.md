Template: `com.jleechan.wiki-daily-worker.plist.template` — rendered by `../install.sh` into `~/Library/LaunchAgents/com.jleechan.wiki-daily-worker.plist`. `@HOME@` is substituted with the user's home directory at install time. The plist calls the `wiki-daily-worker` binary directly (a `uv`-installed tool at `~/.local/bin/`), with an explicit `PATH` so `claude` and `python3` are findable from the launchd context.
