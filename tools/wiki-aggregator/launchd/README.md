Template: `com.jleechan.wiki-aggregator.plist.template` — rendered by `../install.sh` into `~/Library/LaunchAgents/com.jleechan.wiki-aggregator.plist`. `@HOME@` is substituted with the user's home directory at install time. The plist calls the `wiki-aggregator` binary directly (a `uv`-installed tool at `~/.local/bin/`), with an explicit `PATH` so the script can read other tools if needed.