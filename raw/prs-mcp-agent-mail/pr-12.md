# PR #12: feat: Allow register_agent to accept any string as project_key and au…

**Repo:** jleechanorg/mcp_agent_mail
**Merged:** 2025-11-06
**Author:** jleechan2015
**Stats:** +151/-5 in 3 files

## Summary
(none)

## Raw Body
…to-create projects

Previously, register_agent required calling ensure_project first to create the project. This change makes register_agent more flexible by automatically creating the project if it doesn't exist, similar to how ensure_project works.

Changes:
- Replace _get_project_by_identifier with _ensure_project in register_agent
- Add ensure_archive call to initialize project directory structure
- Update documentation to reflect that ensure_project is no longer required
- Update parameter documentation to clarify project_key accepts any string

This simplifies the agent registration workflow and removes the need for a separate ensure_project call in most cases.

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> `register_agent` now ensures/creates the project (and archive) from any string `project_key`, with updated docs and tests.
> 
> - **Identity/Backend**
>   - `register_agent`: replace `_get_project_by_identifier` with `_ensure_project` and call `ensure_archive`, enabling auto-creation of projects from any `project_key`.
>   - Docstring updates: clarify semantics and parameter details for `project_key`.
> - **Tests**
>   - Add coverage for auto-creation, freeform `project_key` strings, idempotency with `ensure_project`, and same-slug normalization (`tests/test_identity_resources.py`).
>   - Update negative-path expectations: `register_agent` no longer errors on missing project (`tests/test_negative_inputs.py`).
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 892de8f9b31ed01e838841bcd7bce5867d8d4bfc. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->

## Summary by CodeRabbit

* **New Features**
  * Projects are now automatically created when registering agents if they don't already exist.
  * Storage archives are automatically initialized for newly creat
