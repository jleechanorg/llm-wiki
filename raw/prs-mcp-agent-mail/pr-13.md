# PR #13: Reclaim agent names, retire old identities, and harden share wizard

**Repo:** jleechanorg/mcp_agent_mail
**Merged:** 2025-11-07
**Author:** jleechan2015
**Stats:** +2084/-160 in 29 files

## Summary
- add soft-delete tracking so reclaiming an agent name retires the prior identity (releases reservations, updates archives) and scope all discovery/routing queries to active agents only
- document the reclaimable-name flow, migrate DB uniqueness to active rows, and expand tests (including new reuse coverage)
- tighten the share wizard (saved-config fallback, stdout guards) plus polish CLI/test utilities so ruff/ty stay clean

## Raw Body
## Summary
- add soft-delete tracking so reclaiming an agent name retires the prior identity (releases reservations, updates archives) and scope all discovery/routing queries to active agents only
- document the reclaimable-name flow, migrate DB uniqueness to active rows, and expand tests (including new reuse coverage)
- tighten the share wizard (saved-config fallback, stdout guards) plus polish CLI/test utilities so ruff/ty stay clean

## Testing
- uv run ruff check --fix --unsafe-fixes src/mcp_agent_mail/app.py src/mcp_agent_mail/utils.py src/mcp_agent_mail/models.py src/mcp_agent_mail/db.py scripts/share_to_github_pages.py tests/test_global_agent_uniqueness_modes.py tests/test_query_locality.py tests/test_share_export.py
- uv run ruff check --fix --unsafe-fixes src/mcp_agent_mail/cli.py src/mcp_agent_mail/share.py tests/conftest.py tests/test_contact_policy.py tests/test_lazy_loading.py tests/test_performance_benchmarks.py tests/test_viewer_storage.py tests/test_xss_corpus.py
- uvx ty check
- uv run pytest  # currently times out after ~200s with upstream failures; needs follow-up triage

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Implements reclaimable agent names via soft-delete and active-only uniqueness, tightens messaging/routing and share wizard, adds smoke-test CI and extended tests/docs.
> 
> - **Identity & DB**:
>   - Add soft-delete for agents (`is_active`, `deleted_ts`); return `deleted_ts: null` when active.
>   - Enforce global uniqueness only for active agents (partial unique index); auto-retire conflicting identities; reactivate on reuse.
>   - Scope all agent lookups/joins to `is_active = true`; add helpers (`_get_project_by_id`, optional agent fetch).
> - **Messaging & Routing**:
>   - Always write sender outbox (even when all recipients are external); allow empty local recipient set internally.
>   - Cross-project: create sender aliases instead of stealing names; ensure `AgentLink` creation uses correct project ids; active-only filters on links.
> 
