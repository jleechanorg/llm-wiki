# PR #184: feat: Two Sum (LC1) + full pytest CI

**Repo:** jleechanorg/mctrl_test
**Merged:** 2026-04-01
**Author:** jleechan2015
**Stats:** +51/-1 in 3 files

## Summary
- Add `two_sum.py` / `test_two_sum.py` for LeetCode 1 (hash-map O(n)).
- CI runs `pytest -v` so `test_shortest_path_binary_matrix.py` and all modules are exercised (previously only `test_hello.py` ran).

## Raw Body
## Summary
- Add `two_sum.py` / `test_two_sum.py` for LeetCode 1 (hash-map O(n)).
- CI runs `pytest -v` so `test_shortest_path_binary_matrix.py` and all modules are exercised (previously only `test_hello.py` ran).

## Evidence (local)
```
pytest -v  # 26 passed (hello + shortest path + two sum)
```

## Note
Intended as the **Gemini-agent** track PR for mctrl validation; opened from Cursor session because tmux capacity gate blocked new `ao spawn` (21 active sessions > 15).

To reproduce with AO: `ao spawn -p mctrl-test --agent gemini "Implement Two Sum on mctrl_test branch feat/leetcode-two-sum-ci"` (after freeing tmux slots).


Made with [Cursor](https://cursor.com)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Adds isolated algorithm code and tests, plus a small CI command tweak; minimal impact outside test coverage/CI runtime.
> 
> **Overview**
> Adds a new `two_sum.py` implementation for LeetCode 1 and a corresponding `test_two_sum.py` suite covering common cases and the no-solution error path.
> 
> Updates CI to run the full pytest suite (`pytest -v`) instead of targeting a single test file, ensuring all tests execute in GitHub Actions.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 910bf52d5bda0e964ff51a67e6643a69136f88c4. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
