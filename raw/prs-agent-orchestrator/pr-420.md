# PR #420: [agento] fix(scm-github): reset AO-managed files before dirty check

**Repo:** jleechanorg/agent-orchestrator
**Merged:** 2026-04-09
**Author:** jleechan2015
**Stats:** +86/-33 in 2 files

## Summary
- Fix execution order in `checkoutPR`: reset AO-managed files FIRST, then re-check dirty state
- Previously threw "uncommitted changes" BEFORE reaching the reset loop when AGENTS.md (or any non-`AO_MANAGED` file) was dirty
- Also adds missing third git status mock in existing test
- Handles staged-only changes (idx='M', wt=' ') via `git reset HEAD` + `git checkout`

Supersedes #417 for the checkoutPR fix. PR #417 covers reviewDecision normalization (separate area of scm-github/index.ts).

## Test Plan
- [x] `pnpm --filter @jleechanorg/ao-plugin-scm-github test` — 145/145 pass

## Raw Body
## Summary
- Fix execution order in `checkoutPR`: reset AO-managed files FIRST, then re-check dirty state
- Previously threw "uncommitted changes" BEFORE reaching the reset loop when AGENTS.md (or any non-`AO_MANAGED` file) was dirty
- Also adds missing third git status mock in existing test
- Handles staged-only changes (idx='M', wt=' ') via `git reset HEAD` + `git checkout`

Supersedes #417 for the checkoutPR fix. PR #417 covers reviewDecision normalization (separate area of scm-github/index.ts).

## Test plan
- [x] `pnpm --filter @jleechanorg/ao-plugin-scm-github test` — 145/145 pass

## Evidence

**Claim**: checkoutPR now resets AO-managed files (including staged-only changes) before checking dirty state, and correctly throws when non-AO-managed files remain dirty.

**Class**: Functional — Bug Fix

**Verdict**: PASS

**Proof**:
- Unit test `"throws when workspace has uncommitted changes"` passes with new third mock for remainingDirty re-check
- Unit test `"resets AO-managed file and succeeds when only AO-managed file is dirty"` passes after staged-reset fix
- New unit test covers staged-only changes (idx='M', wt=' ') being reset via git reset HEAD + git checkout
- All 145 scm-github tests pass

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Changes `checkoutPR` working-tree cleanup logic before branch switching; mistakes could delete/unstage unexpected files or still block checkouts in edge status cases.
> 
> **Overview**
> Fixes `checkoutPR` to *clean up AO-managed runtime files before* deciding whether a workspace is too dirty to safely switch branches.
> 
> Introduces a shared `CHECKOUT_AO_MANAGED` allowlist and expands the reset logic to handle staged-only changes (via `git reset HEAD` + restore/clean), then re-checks `git status --porcelain` and throws only if non-AO changes remain. Tests are updated to add the required extra status mock and to cover successful AO-managed cleanup (including staged changes).
> 
> <sup>Reviewed by [Cursor Bugbot](
