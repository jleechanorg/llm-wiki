# PR #268: [agento] feat: deterministic gate-closure action plans for AO workers

**Repo:** jleechanorg/agent-orchestrator
**Merged:** 2026-03-29
**Author:** jleechan2015
**Stats:** +502/-3 in 5 files

## Summary
AO workers receive independent reaction messages that don't communicate optimal gate-closure order. A worker with multiple failing gates (merge conflict + CI failing + CR changes requested) gets a disconnected list rather than a prioritized sequence.

## Background
AO workers receive independent reaction messages that don't communicate optimal gate-closure order. A worker with multiple failing gates (merge conflict + CI failing + CR changes requested) gets a disconnected list rather than a prioritized sequence.

## Raw Body
## Background

AO workers receive independent reaction messages that don't communicate optimal gate-closure order. A worker with multiple failing gates (merge conflict + CI failing + CR changes requested) gets a disconnected list rather than a prioritized sequence.

## Goals

- Deterministic, prioritized action plan for workers on changes-requested, agent-stuck, agent-needs-input
- Priority order: conflicts -> CI -> Bugbot -> inline comments -> CR approval -> evidence -> skeptic
- Workers receive readable text, not raw objects

## Changes

### packages/core/src/action-plan.ts
New module with ActionItem/ActionPlan interfaces, GATE constants (matching merge-gate.ts names), buildActionPlan() consuming MergeGateResult, formatActionPlan() rendering numbered checklist.

### packages/core/src/__tests__/action-plan.test.ts
8 tests: all-green empty plan, single failing gate, priority ordering, unknown gates, empty checks, full 7-gate sort, format output, ready-plan empty string.

### packages/core/src/reaction-context.ts
appendActionPlan() helper (checkMergeGate -> buildActionPlan -> formatActionPlan). changes-requested now always computes action plan (even with 0 comments). agent-stuck/agent-needs-input append action plan to PR status summary.

### packages/core/src/__tests__/reaction-context-action-plan.test.ts
4 integration tests: action plan in changes-requested (with/without comments), agent-stuck, agent-needs-input.

### packages/core/src/index.ts
Exports buildActionPlan, formatActionPlan, GATE, ActionPlan, ActionItem.

## Testing

- 8 unit tests pass for action-plan (priority ordering, format output)
- 4 integration tests pass for reaction-context integration
- 7 existing reaction-context tests unchanged (regression-free)
- Core package typecheck passes

## Evidence

**Claim class**: integration

**Proofs**:
- Unit tests: 8 tests, 8 passed - packages/core/src/__tests__/action-plan.test.ts
- Integration tests: 4 tests, 4 passed - packages/core/src/__tests__/reaction-co
