# PR #17: fix(scm-github): use REST API instead of GraphQL in resolvePR

**Repo:** jleechanorg/agent-orchestrator
**Merged:** 2026-03-16
**Author:** jleechan2015
**Stats:** +22/-17 in 3 files

## Summary
(none)

## Raw Body
Replace `gh pr view --json` (GraphQL) with `gh api repos/<owner>/<repo>/pulls/<number>` (REST) to avoid GraphQL rate limits.

## Changes
- `resolvePR()` now uses REST API instead of GraphQL
- Field mapping: head.ref → headRefName, base.ref → baseRefName, draft → isDraft

## Testing
- All 72 tests pass

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Switches `resolvePR` to a different GitHub API surface and response shape, which could break PR resolution if field mapping or permissions differ. Scope is limited to PR lookup plus a small README link update.
> 
> **Overview**
> **GitHub SCM PR resolution now uses REST instead of GraphQL.** `resolvePR()` switches from `gh pr view --json` to `gh api repos/<owner>/<repo>/pulls/<number>` and maps `head.ref`/`base.ref`/`draft` into the existing `PRInfo` shape to reduce GraphQL rate-limit issues.
> 
> Tests were updated to mock the REST response format, and the README’s Discord invite URL was changed.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 8083e1964c2207a47d13226c9c8cae91bfa1eeec. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->

## Summary by CodeRabbit

* **Documentation**
  * Updated Discord community link in README.

* **Refactor**
  * Improved PR resolution mechanism for better reliability.

<!-- end of auto-generated comment: release notes by coderabbit.ai -->
