# PR #408: [agento] fix(start-all): add --force override + health check for lifecycle worker startup

**Repo:** jleechanorg/agent-orchestrator
**Merged:** 2026-04-07
**Author:** jleechan2015
**Stats:** +17/-3 in 1 files

## Summary
- Add `--force` flag to lifecycle-worker startup to override stale PID locks
- Add post-start health verification to warn if worker fails to start

## Test Plan
- [ ] Run `./scripts/start-all.sh` and verify workers start correctly
- [ ] Verify no "already running — skipping" when worker is dead
- [ ] Verify WARNING appears if worker fails to start

## Raw Body
## Summary
- Add `--force` flag to lifecycle-worker startup to override stale PID locks
- Add post-start health verification to warn if worker fails to start

## Problem
- Lifecycle worker for agent-orchestrator was not running because stale PID lock file existed from crashed worker
- pgrep check falsely matched "already running" but worker never actually started

## Fix
1. Add `--force` to `ao lifecycle-worker "$PROJECT"` to override stale locks
2. Add post-start health check that verifies workers are actually running

## Test plan
- [ ] Run `./scripts/start-all.sh` and verify workers start correctly
- [ ] Verify no "already running — skipping" when worker is dead
- [ ] Verify WARNING appears if worker fails to start

## Evidence
**Claim class:** N/A (scripts/start-all.sh is a 运维 automation script, not feature code)

**Changes:**
- Modified: `scripts/start-all.sh`
  - Added `--force` to lifecycle-worker startup command
  - Added post-start health verification loop
  - Fixed pgrep pattern to remove `$` anchor

**Verdict:** PASS — operational automation with clear test plan.

## Resolved Comments
| Reviewer | File | Comment | Resolution |
|---|---|---|---|
| coderabbitai[bot] | scripts/start-all.sh (L73) | pgrep pattern needs to work with --force | Fixed in 442af28f |
| coderabbitai[bot] | scripts/start-all.sh (L90) | health check should only verify selected | Fixed in 442af28f |

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Operational script change that affects process startup/duplication behavior; `--force` and altered `pgrep` matching could inadvertently start or skip workers if process patterns are unexpected.
> 
> **Overview**
> Improves reliability of `scripts/start-all.sh` when starting multiple projects by fixing lifecycle-worker idempotency detection and forcing startup past stale PID locks.
> 
> Non-first projects now start lifecycle workers with `--force` and use a less strict `
