# PR #115: [agento] fix(scm-github): reduce Bugbot severity false positives in merge gate

**Repo:** jleechanorg/agent-orchestrator
**Merged:** 2026-03-23
**Author:** jleechan2015
**Stats:** +107/-6 in 2 files

## Summary
The merge gate Bugbot check (condition #4) uses keyword matching to determine comment severity. Words like "bug" and "error" appearing anywhere in cursor[bot] comment bodies caused false positives, mapping review suggestions to severity=error and blocking genuinely green PRs from auto-merge.

## Background
The merge gate Bugbot check (condition #4) uses keyword matching to determine comment severity. Words like "bug" and "error" appearing anywhere in cursor[bot] comment bodies caused false positives, mapping review suggestions to severity=error and blocking genuinely green PRs from auto-merge.

## Raw Body
## Background
The merge gate Bugbot check (condition #4) uses keyword matching to determine comment severity. Words like "bug" and "error" appearing anywhere in cursor[bot] comment bodies caused false positives, mapping review suggestions to severity=error and blocking genuinely green PRs from auto-merge.

## Goals
- Eliminate false positives in Bugbot severity classification
- Allow auto-merge to fire for PRs where Bugbot check-run is neutral/success

## Testing
- TDD test added: does not false-positive on incidental severity keywords
- All 122 scm-github tests pass
- All 748 core tests pass

## Low-level details
- **packages/plugins/scm-github/src/index.ts**: Changed severity mapping to use regex for structured header detection
- **packages/plugins/scm-github/test/index.test.ts**: Added false-positive test case

Part of bd-ara epic (autonomy blockers). Unblocks auto-merge for PRs with Bugbot review comments.

## Resolved Comments
| Reviewer | File | Comment | Resolution |
|---|---|---|---|
| chatgpt-codex-connector[bot] | packages/plugins/scm-github/src/index.ts | P1: Keep High/Critical Bugbot findings as errors — concerned that downgrading to warning would miss real blockers | Explained design intent: Bugbot check-run conclusion is authoritative signal; body-level severity is secondary filter to avoid false positives |
| copilot-pull-request-reviewer[bot] | packages/plugins/scm-github/src/index.ts | Stale: `\bcritical[:\s]` pattern may match `non-critical:` anywhere | Addressed in commit af2de982 — pattern updated to `^\s*critical[:\s]/m` anchored to line-start; `non-critical:` cannot match |
| cursor[bot] | packages/plugins/scm-github/src/index.ts | Medium: Inconsistent leading whitespace handling — error/warning don't allow `\s*` but critical does | Fixed in commit 3888e3ad — all three patterns now use `^\s*` for consistent whitespace handling |

## Evidence

**Claim class**: Unit test coverage

**Verdict**: PASS

### Artifact 1: Unit test coverage
- **Artifact
