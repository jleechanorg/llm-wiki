# PR #425: [agento] fix(core): address deferred bugs from PR #421 session-prefix work

**Repo:** jleechanorg/agent-orchestrator
**Merged:** 2026-04-11
**Author:** jleechan2015
**Stats:** +4/-2 in 2 files

## Summary
Follow-up to PR #421 addressing the two bugs deferred due to scope constraints.

### Fixes

**1. `isOrchestratorSessionRecord` ignores metadata role when prefix is set** (cursor #3063372527)

When `project.sessionPrefix` is set, `isOrchestratorSessionRecord` only checked session ID, not `raw["role"]`. A session with `role="worker"` but a matching ID pattern would be misclassified as orchestrator. Fix: add early return `if (raw["role"] === "worker") return false` so metadata role takes precedence

## Test Plan
- [x] `pnpm --filter @composio/ao-core test` passes (CI Test check green — run 24272968078)
- [x] Orchestrator sessions for project A are not classified as orchestrators for project B (prefix-aware `isOrchestratorSessionForPrefix` uses exact `sessionId === prefix + "-orchestrator"` match)
- [x] Worker sessions with matching IDs are correctly identified as non-orchestrators (`raw["role"] === "worker"` early-return in session-manager; metadata override in lifecycle-manager prAutoDetect guard)

<!-

## Raw Body
## Summary

Follow-up to PR #421 addressing the two bugs deferred due to scope constraints.

### Fixes

**1. `isOrchestratorSessionRecord` ignores metadata role when prefix is set** (cursor #3063372527)

When `project.sessionPrefix` is set, `isOrchestratorSessionRecord` only checked session ID, not `raw["role"]`. A session with `role="worker"` but a matching ID pattern would be misclassified as orchestrator. Fix: add early return `if (raw["role"] === "worker") return false` so metadata role takes precedence.

**2. prAutoDetect guard uses prefix-unaware dual check** (cursor #3066455088)

The old guard `session.metadata["role"] !== "orchestrator" && !session.id.endsWith("-orchestrator")` fails across projects sharing a suffix. Replaced with `isOrchestratorSessionForPrefix(session, project?.sessionPrefix)`.

### Not included (already fixed in main via #421 force-push 68f6ee6a)
- `normalizeMergePayloadFromRestShape` empty string — already in main
- `getPRStateAndReview` empty string/PENDING — already in main

## Evidence

**Claim class**: code-review

**Verdict**: PASS

Changes are minimal, contained to two files (`session-manager.ts:437`, `lifecycle-manager.ts:699`), each a 1-2 line fix matching the prefix-aware pattern established by commit `d56d4b9c` in PR #421. Both fixes are isolated to logic paths guarded by `project.sessionPrefix` being set, limiting blast radius to multi-project configurations.

CI evidence (all checks green — https://github.com/jleechanorg/agent-orchestrator/actions/runs/24272968078):
- Test: PASS (4m28s)
- Integration Tests: PASS (2m21s)
- Typecheck: PASS (1m53s)
- Lint: PASS (41s)
- Test Fresh Onboarding: PASS (3m41s)
- Test (Web): PASS (1m3s)

## Test plan
- [x] `pnpm --filter @composio/ao-core test` passes (CI Test check green — run 24272968078)
- [x] Orchestrator sessions for project A are not classified as orchestrators for project B (prefix-aware `isOrchestratorSessionForPrefix` uses exact `sessionId === prefix + "-orchestrator"` match)

