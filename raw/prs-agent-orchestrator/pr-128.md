# PR #128: feat(lifecycle): add observer logging to reaction start and send-failed paths

**Repo:** jleechanorg/agent-orchestrator
**Merged:** 2026-03-23
**Author:** jleechan2015
**Stats:** +78/-3 in 2 files

## Summary
(none)

## Raw Body
### Background
While working on jleechan-v7oa (auto-kill on merge, PR #127), local changes were found that added observer logging to reaction paths in lifecycle-manager.ts but were never committed to that PR.

### Goals
- Add structured observer logging when a reaction starts
- Add structured logging when a reaction send fails (including error message)

### Tenets
- Minimal scope, no behavioral changes. Use existing observer.recordOperation pattern.

### High-level description of changes
Adds two observer.recordOperation calls: lifecycle.reaction.start and lifecycle.reaction.send_failed (includes error message).

### Testing
No behavioral change. Existing lifecycle-manager tests still pass.

### Low-level details
- **packages/core/src/lifecycle-manager.ts**: Added observer logging at reaction start and send-failed catch block

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Low risk: changes are limited to observability/metrics and additional logging around reaction execution paths, with no intended behavior changes beyond how operations are recorded/counting.
> 
> **Overview**
> Adds structured `observer.recordOperation` events around lifecycle reactions: records `lifecycle.reaction.start`, logs `lifecycle.reaction.send_failed` (including the error message), and emits `lifecycle.reaction.result` plus `lifecycle.reaction.skipped` when reactions are disabled/misconfigured or lack a reaction key.
> 
> Updates observability typing/aggregation to support an `outcome: "info"` and count it as a non-failure in metric counters.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit aba2f4b9784f9cf40dfe1f5f3f33f7cdb46b159f. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
