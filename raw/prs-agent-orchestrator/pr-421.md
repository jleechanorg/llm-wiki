# PR #421: [agento] fix(core): disambiguate orchestrator session prefixes

**Repo:** jleechanorg/agent-orchestrator
**Merged:** 2026-04-11
**Author:** jleechan2015
**Stats:** +675/-128 in 17 files

## Summary
(none)

## Raw Body
### Background
The current AO worktree was assigned a core/CLI upstream import lane, but the provided `/tmp/ao-upstream-core.txt` handoff file was missing. I reconstructed the intended scope from the repo-local upstream sync inventory and current worktree delta, then finished the in-flight session-prefix manual port instead of fabricating a direct cherry-pick. This change addresses a session-role classification bug where a worker session whose id matched another project's longer prefix could be misread as an orchestrator.

### Goals
Make orchestrator-vs-worker role resolution prefix-aware in the core path.
Prevent cross-project prefix collisions from selecting orchestrator behavior for worker sessions.
Keep the fix isolated to the fork's hot core surfaces with a minimal additive helper.
Keep stale-worktree recovery and PR-claim behavior green after rebasing onto current `main`.

### Tenets
Prefer a fork-aware manual port over a misleading cherry-pick when the upstream change is not standalone for this fork.
Keep upstream-core diffs small by extracting helper logic into a companion module.
Prove the change with regression coverage on the misclassification case and targeted core/plugin suite coverage on the affected call sites.
Treat review and skeptic findings as executable requirements: comments are addressed in code, design docs, or the PR description.

### High-level description of changes
This PR adds a small `session-prefixes` helper that collects configured session prefixes and determines whether a given session id should be treated as an orchestrator for one specific project prefix. The core role-resolution path in agent selection now uses that helper, and the lifecycle manager, recovery validator, and session manager all pass the current project's prefix plus the known project-prefix set into that resolution step. Regression coverage covers the exact collision case where `app-orchestrator-1` must remain a worker for project prefix `app` when another project u
