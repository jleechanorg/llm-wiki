# PR #263: [agento] chore: upstream sync review and P0/P1 cherry-picks

**Repo:** jleechanorg/agent-orchestrator
**Merged:** 2026-03-29
**Author:** jleechan2015
**Stats:** +2192/-67 in 18 files

## Summary
Applies three upstream ComposioHQ P0 security patches to `.github/workflows/security.yml`:

| Upstream PR | What | Status |
|---|---|---|
| #735 `b49c69ba` | Gitleaks scan last 10 commits for PRs (not full history) | Applied |
| #720 `70fe5369` | Correct `--log-opts` syntax with commit range | Applied |
| #721 `f0bcb7b7` + `c36440df` | SHA256 checksum verification on gitleaks binary | Applied |

Adds `roadmap/upstream-sync-review.md` with full P0-P3 ranked analysis of all 346 upstream commits.



## Background
Reviewed all 346 commits from ComposioHQ/agent-orchestrator upstream in the last 30 days (2026-02-26 to 2026-03-28). The fork (jleechanorg) is 12 commits ahead of upstream/main and has diverged significantly — cherry-picks required manual adaptation rather than direct git cherry-pick.

## Raw Body
## Background

Reviewed all 346 commits from ComposioHQ/agent-orchestrator upstream in the last 30 days (2026-02-26 to 2026-03-28). The fork (jleechanorg) is 12 commits ahead of upstream/main and has diverged significantly — cherry-picks required manual adaptation rather than direct git cherry-pick.

## Goals

- Document all upstream changes with P0-P3 priority ranking
- Apply immediately actionable P0 security patches (CI hardening)
- Flag high-value P1 items for future evaluation

## Tenets

- Fork-only artifacts (CLAUDE.md, roadmap/, .beads/, .claude/) are never overwritten from upstream
- Web/mobile packages are out of scope for this fork
- Cherry-pick patterns, not commits, when origin is ahead of upstream

## Summary

Applies three upstream ComposioHQ P0 security patches to `.github/workflows/security.yml`:

| Upstream PR | What | Status |
|---|---|---|
| #735 `b49c69ba` | Gitleaks scan last 10 commits for PRs (not full history) | Applied |
| #720 `70fe5369` | Correct `--log-opts` syntax with commit range | Applied |
| #721 `f0bcb7b7` + `c36440df` | SHA256 checksum verification on gitleaks binary | Applied |

Adds `roadmap/upstream-sync-review.md` with full P0-P3 ranked analysis of all 346 upstream commits.

### Not cherry-picked (already in fork)

- CP-3: CD-prefix hook path stripping (`59633e45`) — already in `packages/plugins/agent-claude-code/src/index.ts`
- CP-5: GH_PATH env var hardening (`b865549e`) — already in `packages/plugins/agent-codex/src/index.ts`

### P1 items flagged for future evaluation

| SHA | Feature | Value |
|---|---|---|
| `4fd8cac7` | SCM webhooks | Replace polling (fixes bd-o4t) |
| `dc85cdbe` | REST pagination | Model for bd-o4t REST fallback |
| `a64c051e` | Stuck detection | Compare with session-reaper |
| `3d518aed` | `ao doctor` | Compare implementations |
| `f48c939d` | Lifecycle backlog/auto-claim | Study patterns only |

## Testing

- `pnpm typecheck` passes
- `pnpm lint` passes (0 errors)
- CI: `.github/workflows/security.ym
