# PR #367: [agento] fix(metadata-updater): env-strip regex, sed escaping, git detection refactor

**Repo:** jleechanorg/agent-orchestrator
**Merged:** 2026-04-04
**Author:** jleechan2015
**Stats:** +26/-60 in 4 files

## Summary
Fix three bugs in `.claude/metadata-updater.sh` (and sync fixes to embedded plugin copies):

1. **Env-stripping regex bug** — Old param expansion `${clean_command#*=* }` stripped from the first `=` to the last space, mangling embedded `=` values (e.g. `FOO=a=b gh pr create` → bare command became `b` instead of `b gh pr create`). Fixed with a proper regex capture group using `[^[:space:]]*` to allow embedded `=`.

2. **Simplified `escaped_value`** — Replaced two-parameter-expansion lines with a s

## Raw Body
## Summary

Fix three bugs in `.claude/metadata-updater.sh` (and sync fixes to embedded plugin copies):

1. **Env-stripping regex bug** — Old param expansion `${clean_command#*=* }` stripped from the first `=` to the last space, mangling embedded `=` values (e.g. `FOO=a=b gh pr create` → bare command became `b` instead of `b gh pr create`). Fixed with a proper regex capture group using `[^[:space:]]*` to allow embedded `=`.

2. **Simplified `escaped_value`** — Replaced two-parameter-expansion lines with a single `sed` call that is clearer and equivalent.

3. **Git checkout/switch detection refactor** — Old code called `git branch --show-current` post-execution for bare `git checkout <branch>`, which is unreliable (detached HEAD, wrong repo). Replaced with command-parsing regex that extracts the branch name directly from the invocation. Also removed bare `git checkout` regex block from plugin copies (it could match file paths like `git checkout src/foo-bar.txt`).

## Testing

- All 1370 core tests pass (`pnpm -C packages/core test`)
- Hook script shellchecked (no errors)

## Resolved Comments
| Reviewer | File | Comment | Resolution |
|---|---|---|---|
| Copilot | .claude/metadata-updater.sh line 59 | `[^= ]*` prevents embedded `=` stripping | Fixed: changed to `[^[:space:]]*` |
| Copilot | packages/plugins/agent-base/src/index.ts | Same env-strip bug in embedded copy | Fixed: applied same regex fix |
| Copilot | packages/plugins/agent-base/src/index.ts line 343 | Bare git checkout regex matches file paths | Fixed: removed block (git switch retained) |
| Copilot | packages/plugins/agent-claude-code/src/index.ts | Same two issues in embedded copy | Fixed: regex fix + removed checkout block |
| chatgpt-codex-connector[bot] | .claude/metadata-updater.sh line 200 | git checkout pathspec ambiguity (files vs branches) | Fixed: removed regex checkout block from plugins |

## Evidence

**Claim**: unit test coverage

**Terminal test output**:
```
Test Files   73 passed (73)
T
