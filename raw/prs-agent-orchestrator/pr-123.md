# PR #123: [agento] bd-jo6: tmux orphan session TTL/sweep in lifecycle-manager

**Repo:** jleechanorg/agent-orchestrator
**Merged:** 2026-03-24
**Author:** jleechan2015
**Stats:** +818/-5 in 5 files

## Summary
The lifecycle-manager runs a polling loop every 30s, but tmux sessions can be orphaned when:
- A lifecycle-manager restart occurs mid-session
- A session is killed outside AO's control
- Metadata files are corrupted or deleted
- Sessions from other AO installations on the same machine accumulate

Over time, these orphans accumulate in tmux and can eventually block the spawn gate (>15 sessions threshold), preventing new PR workers from spawning.

## Background
The lifecycle-manager runs a polling loop every 30s, but tmux sessions can be orphaned when:
- A lifecycle-manager restart occurs mid-session
- A session is killed outside AO's control
- Metadata files are corrupted or deleted
- Sessions from other AO installations on the same machine accumulate

Over time, these orphans accumulate in tmux and can eventually block the spawn gate (>15 sessions threshold), preventing new PR workers from spawning.

## Raw Body
## Background

The lifecycle-manager runs a polling loop every 30s, but tmux sessions can be orphaned when:
- A lifecycle-manager restart occurs mid-session
- A session is killed outside AO's control
- Metadata files are corrupted or deleted
- Sessions from other AO installations on the same machine accumulate

Over time, these orphans accumulate in tmux and can eventually block the spawn gate (>15 sessions threshold), preventing new PR workers from spawning.

## Goals

1. Prevent tmux session accumulation by proactively killing AO-named orphans
2. Avoid killing legitimate active sessions — only sweep truly orphaned tmux sessions
3. Run efficiently within the existing lifecycle-manager poll cycle
4. Provide observable logging of sweep actions

## Tenets

- **Safe by default**: Only kill sessions that are both AO-named AND absent from the AO DB AND past the idle threshold
- **Idempotent**: "session not found" errors during kill are treated as benign (race with concurrent cleanup)
- **Configurable**: idle threshold and max kills per sweep are exposed via config
- **Non-blocking**: sweep failures do not break the main lifecycle poll cycle

## High-level description

### New module: `tmux-session-sweeper.ts`

A new pure-logic module that:
1. Lists all tmux sessions via `listSessions()`
2. Filters for AO-named sessions using `parseTmuxName()` — only sessions matching the `{hash}-(ao|jc|wa|cc|ra|wc)-{num}` pattern are considered
3. Checks AO DB via `sessionManager.list()` — sessions in the DB are exempt
4. Computes session idle time from tmux `session_created_string` (creation timestamp used as idle proxy for detached sessions)
5. Kills orphans past the `orphanIdleThresholdMs` (default: 30 minutes), up to `maxKillsPerSweep` (default: 10)

### Integration in `lifecycle-manager.ts`

`sweepOrphanTmuxSessions()` is called at the end of each `pollAll()` cycle, throttled to every 5 minutes via a `lastSweepTime` guard. Sweep errors are caught and logged but do not propagate — th
