# PR #296: [agento] fix(start): guard main repo branch invariant in start-all.sh and ao start

**Repo:** jleechanorg/agent-orchestrator
**Merged:** 2026-03-30
**Author:** jleechan2015
**Stats:** +201/-7 in 6 files

## Summary
(none)

## Raw Body
"@coderabbitai all good?\n\n<!-- CURSOR_SUMMARY -->\n---\n\n> [!NOTE]\n> **Medium Risk**\n> Adds new guardrails that can now hard-fail `ao start` when paths resolve into the configured main repo, and `start-all.sh` may auto-checkout/pull `main`, so misconfigured env vars or unusual path resolution could block or alter local working state.\n> \n> **Overview**\n> Prevents `ao start` from running inside the main `agent-orchestrator` clone (or any subdirectory) by introducing a canonical-path guard (`getMainRepoPath`/`guardMainRepo`) and applying it across URL clone, local-path add, first-run auto-config, and normal startup flows.\n> \n> Adds unit tests covering the new guard behavior and updates `start-all.sh` to enforce the \"main clone stays on `main`\" invariant (auto-checkout and best-effort `git pull --ff-only`). CI workflows also pin `actions/checkout` to `v4.1.1`.\n> \n> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 5d642a6aafc7b4fb8231e23cead9bca383dd1f59. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>\n<!-- /CURSOR_SUMMARY -->\n\n<!-- This is an auto-generated comment: release notes by coderabbit.ai -->\n## Summary by CodeRabbit\n\n* **Bug Fixes**\n  * Prevent startup when run inside the main repository or its subdirectories; aborts with a clear error.\n\n* **New Features**\n  * Canonical path handling across start flows to reliably detect guarded locations.\n  * Pre-flight script guard that checks the repo branch is on main and halts with guidance if it cannot switch.\n\n* **Tests**\n  * Added tests covering the main-repo guard for startup success and failure scenarios.\n\n* **Chores**\n  * Updated workflow checkout action references.\n<!-- end of auto-generated comment: release notes by coderabbit.ai -->\n\n## Evidence\n\n**Claim class**: unit-test-coverage\n\n**Terminal output**:\n```\n RUN  v3.2.4\n \u2713 __tests__/commands/start.test.ts (26 tests) 72ms\
