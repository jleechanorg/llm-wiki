# PR #206: [agento] feat: add skeptic agent as 7th merge gate condition (bd-qw6)

**Repo:** jleechanorg/agent-orchestrator
**Merged:** 2026-03-26
**Author:** jleechan2015
**Stats:** +1044/-2 in 22 files

## Summary
Adds an independent **Skeptic Agent** as the **7th merge gate condition** (bd-qw6).

### What changed

**packages/core/src/types.ts:**
- `skepticRequired?: boolean` — when true, skeptic verdict is required for merge
- `skepticBypassProjects?: string[]` — projects exempt from skeptic (for bootstrapping)
- `getSkepticVerdict?(pr)` on SCM interface — returns "PASS" | "FAIL" | "SKIPPED"

**packages/core/src/merge-gate.ts:**
- 7th check: "Skeptic approved" — blocks merge when skepticRequired=true and

## Raw Body
## Summary

Adds an independent **Skeptic Agent** as the **7th merge gate condition** (bd-qw6).

### What changed

**packages/core/src/types.ts:**
- `skepticRequired?: boolean` — when true, skeptic verdict is required for merge
- `skepticBypassProjects?: string[]` — projects exempt from skeptic (for bootstrapping)
- `getSkepticVerdict?(pr)` on SCM interface — returns "PASS" | "FAIL" | "SKIPPED"

**packages/core/src/merge-gate.ts:**
- 7th check: "Skeptic approved" — blocks merge when skepticRequired=true and verdict=FAIL
- Passes when verdict=PASS or SKIPPED
- Bypass projects are exempt (skeptic can self-approve its own PRs)

**packages/cli/src/commands/skeptic.ts:**
- New CLI: `ao skeptic --pr <n> [--repo owner/repo] [--dry-run]`
- Fetches CI, CR review, bugbot errors, inline comments via GitHub API
- Runs skeptical LLM evaluation (Claude CLI if available)
- Posts idempotent VERDICT: PASS|FAIL comment on PR

**packages/core/src/__tests__/merge-gate-skeptic.test.ts:**
- 10 TDD tests covering all skeptic scenarios

### Merge gate (7-green)

| # | Condition | Enforcement |
|---|-----------|-------------|
| 1 | CI green | ✅ |
| 2 | No merge conflicts | ✅ |
| 3 | CodeRabbit APPROVED | ✅ |
| 4 | Bugbot clean | ✅ |
| 5 | Inline comments resolved | ✅ |
| 6 | Evidence review PASS | ✅ |
| 7 | **Skeptic VERDICT: PASS** | **✅ NEW** |

### Initial deployment

`skepticRequired: false` (warn mode) — this PR itself can merge without requiring the skeptic to pre-exist.

### Tests

```
✓ 30/30 tests passing (20 original + 10 new skeptic tests)
```

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Changes core merge-gate enforcement by adding an optional skeptic-verdict check that can block merges when enabled, and introduces a new CLI command that reads PR state and posts comments via GitHub CLI. Risk is moderate due to new merge-blocking logic paths and external-command integration (`gh`, `claude`).
> 
> **Overview**
> Adds an optional **7th merge-gate condition** requirin
