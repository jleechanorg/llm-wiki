# PR #415: [agento] fix(config): OpenClaw home path precedence + roadmap (bd-9nvf, bd-qaiz)

**Repo:** jleechanorg/agent-orchestrator
**Merged:** 2026-04-09
**Author:** jleechan2015
**Stats:** +123/-0 in 6 files

## Summary
- **`findConfigFile`**: Prefer `~/.openclaw_prod` / `~/.openclaw` home config paths via `packages/core/src/user-home-config-paths.ts` (keeps `config.ts` free of `openclaw` literals for wholesome fork-isolation).
- **Tests**: `packages/core/src/__tests__/config.find-config.test.ts` (Vitest).
- **Harness / tracking**: `roadmap/README.md`, beads **bd-9nvf**, **bd-qaiz**; default PR template aligns labels with Evidence Gate parsers.

## Raw Body
## Summary

- **`findConfigFile`**: Prefer `~/.openclaw_prod` / `~/.openclaw` home config paths via `packages/core/src/user-home-config-paths.ts` (keeps `config.ts` free of `openclaw` literals for wholesome fork-isolation).
- **Tests**: `packages/core/src/__tests__/config.find-config.test.ts` (Vitest).
- **Harness / tracking**: `roadmap/README.md`, beads **bd-9nvf**, **bd-qaiz**; default PR template aligns labels with Evidence Gate parsers.

## Evidence

**Claim class:** unit

**Claim floor override:** PR touches `.ts` under `packages/core/` for config discovery; OpenClaw path strings are isolated in `user-home-config-paths.ts` with Vitest coverage in `config.find-config.test.ts`—intentional layout behavior, not unrelated product code.

**Repro gist:** N/A — validation is in-repo tests (`config.find-config.test.ts`, `wholesome.test.ts`); no external gist.

**Terminal test output:**

```text
$ pnpm --filter @jleechanorg/ao-core test -- src/__tests__/config.find-config.test.ts

 RUN  v4.0.18 /Users/jleechan/project_agento/agent-orchestrator/packages/core

 ✓ src/__tests__/config.find-config.test.ts (2 tests) 5ms

 Test Files  1 passed (1)
      Tests  2 passed (2)
```

**Self-validation:** Confirmed `findConfigFile` precedence via Vitest above; CI **Test**, **Typecheck**, **Lint**, **Wholesome Checks** on this PR are green.

**Terminal media:** N/A — unit-level config-path change; no new terminal capture required.

**UI media:** N/A - no UI changes

**Verdict:** PASS

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Touches core config discovery/precedence, which can change which config file is loaded in user environments; mitigated by small scope and targeted unit tests.
> 
> **Overview**
> Updates `findConfigFile` to prioritize canonical OpenClaw home config locations (`~/.openclaw_prod/agent-orchestrator.yaml`, then `~/.openclaw/agent-orchestrator.yaml`) ahead of legacy home paths, with the path list extracted into `user-home-config-paths.ts`.
> 
> Adds V
