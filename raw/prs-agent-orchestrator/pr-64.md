# PR #64: [agento][bd-awq] feat: poll GitHub PRs by CodeRabbit changes-requested

**Repo:** jleechanorg/agent-orchestrator
**Merged:** 2026-03-21
**Author:** jleechan2015
**Stats:** +106/-247 in 2 files

## Summary
- update `poller-github-pr` to target only PRs whose latest decisive CodeRabbit review is `CHANGES_REQUESTED`
- keep CI signal in metadata/priority (`ci-failing` -> higher priority)
- include `latestReviews` in `gh pr list` query and derive CodeRabbit state from reviewer login `coderabbitai[bot]`
- rely on poller-manager for active-session dedupe and respawn-cap enforcement (configure `respawnCap: { max: 3, window: "12h" }`)

## Raw Body
## Summary
- update `poller-github-pr` to target only PRs whose latest decisive CodeRabbit review is `CHANGES_REQUESTED`
- keep CI signal in metadata/priority (`ci-failing` -> higher priority)
- include `latestReviews` in `gh pr list` query and derive CodeRabbit state from reviewer login `coderabbitai[bot]`
- rely on poller-manager for active-session dedupe and respawn-cap enforcement (configure `respawnCap: { max: 3, window: "12h" }`)

## Tests
- `pnpm --filter @jleechanorg/ao-plugin-poller-github-pr test`
  - 12 passed

## Notes
- This change is focused to `packages/plugins/poller-github-pr/` only.

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Changes the poller’s selection criteria from general “non-green” PR detection to a narrower CodeRabbit review-based filter, which could affect what work items get created and prioritized. Low security impact, but moderate behavioral risk for automation/workflow expectations.
> 
> **Overview**
> Refocuses the `poller-github-pr` plugin to **only emit work items when the latest decisive review from `coderabbitai[bot]` is `CHANGES_REQUESTED`**, using `latestReviews` from `gh pr list` and ignoring other reviewers’ states.
> 
> Keeps CI signal as a secondary input by adding `ci-failing` to `metadata.reasons` and bumping priority when CI is failing, and updates metadata to include `codeRabbitState` while removing the old review-decision/“green PR” reasoning.
> 
> Updates tests to match the new CodeRabbit-driven behavior, including latest-review ordering, reviewer filtering, and verifying the `gh` JSON query now requests `latestReviews` and that spawned session prompts include the reasons.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit e5d0643a255711ab32b106ee5a4d0a8cf6b821ff. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
