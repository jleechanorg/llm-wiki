# PR #55: fix(scm-github): preserve review body and propagate fetch errors

**Repo:** jleechanorg/agent-orchestrator
**Merged:** 2026-03-21
**Author:** jleechan2015
**Stats:** +52/-42 in 7 files

## Summary
PR #49 (GraphQL-first REST fallback) merged before follow-up CR Major fixes could be included. This PR captures those orphaned fixes.

## Background
PR #49 (GraphQL-first REST fallback) merged before follow-up CR Major fixes could be included. This PR captures those orphaned fixes.

## Raw Body
## Background

PR #49 (GraphQL-first REST fallback) merged before follow-up CR Major fixes could be included. This PR captures those orphaned fixes.

## Goals

- Preserve reviewer message body in REST fallback path for reviews
- Propagate /reviews fetch errors when reviews explicitly requested
- Fix lifecycle-manager terminal session restart edge case
- Worktree-compatible symlink handling in agent-claude-code
- Token-level gitleaks allowlist instead of file-wide suppression

## Tenets

No silent data loss. Idempotent setups. Worktree-compatible.

## High-level description of changes

- scm-github: Add body field to RestReviewRow; use real review body instead of empty string; re-throw fetch error when reviews in jsonFields
- lifecycle-manager: Allow terminal sessions through filter on restart (tracked===undefined) so exit proof emitted
- agent-claude-code: Warn not throw on symlinked .claude; chmod outside content-check block
- gitleaks.toml: Token-level regex patterns replace file-wide path suppressions

## Testing

pnpm typecheck passes. Logic verified against CR Major findings from PR #49 review.

## Low-level details

- packages/plugins/scm-github/src/index.ts: RestReviewRow.body added; body used in mapRestReviewsToPrViewReviewsShape; catch re-throws when reviews in jsonFields
- packages/core/src/lifecycle-manager.ts: Filter allows undefined tracked through
- packages/plugins/agent-claude-code/src/index.ts: Symlink warn not throw; chmod always runs
- .gitleaks.toml: File-wide suppressions replaced with token regex patterns

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Medium risk because it changes session lifecycle terminal-state polling behavior, GitHub REST fallback error propagation, and workspace hook writing semantics (symlink + chmod), which can affect runtime outcomes and file writes across environments.
> 
> **Overview**
> Improves GitHub `gh pr view` REST fallback to **avoid silent data loss** by carrying through review `body` text and re
