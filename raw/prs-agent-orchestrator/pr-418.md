# PR #418: [agento] fix: split managed OpenClaw staging and prod configs

**Repo:** jleechanorg/agent-orchestrator
**Merged:** 2026-04-11
**Author:** jleechan2015
**Stats:** +1737/-625 in 42 files

## Summary
(none)

## Raw Body
### Background
PR [#413](https://github.com/jleechanorg/agent-orchestrator/pull/413) still contains the bulk of the managed OpenClaw staging/production topology work, but only the smaller path-precedence slice landed via [#415](https://github.com/jleechanorg/agent-orchestrator/pull/415). That left the shell helpers, bootstrap/promote flow, `ao start` staging migration, and topology tests unmerged even though they are part of the same intended config model.

This branch ports the remaining `feat/bd-i0w7` changes onto current `main` and keeps one follow-up fix that was needed after replay: first-run staging bootstrap must not require a pre-existing production config.

### Goals
1. Land the still-missing managed staging vs production config topology from the stalled PR [#413](https://github.com/jleechanorg/agent-orchestrator/pull/413).
2. Keep TypeScript and shell config discovery in the same production-first order.
3. Preserve first-run onboarding by allowing staging bootstrap without an existing production file.
4. Add or retain targeted tests around topology discovery, bootstrap/promote scripts, doctor/update flows, and `ao start` URL onboarding.

### Tenets
- Prefer canonical managed config paths over repo-local shadow configs.
- Keep staging editable and promotion to production explicit.
- Centralize topology logic instead of duplicating path heuristics.
- Fix the harness-level regression instead of weakening tests.

### High-level description of changes
This PR replays the substantive config-topology work from PR [#413](https://github.com/jleechanorg/agent-orchestrator/pull/413) onto current `main`: core now exposes managed staging/production topology helpers, config discovery prefers managed paths before local shadow configs, CLI `ao start` migrates/bootstrap configs into the staging canonical path, and shell scripts share centralized topology logic plus explicit bootstrap/promote commands.

On top of that replay, `ensureStagingConfigPath()` in `packages/cli/src
