# PR #196: [agento] docs: close bd-ara.1 — PR description resolved-comments approach implemented

**Repo:** jleechanorg/agent-orchestrator
**Merged:** 2026-03-26
**Author:** jleechan2015
**Stats:** +18/-8 in 3 files

## Summary
bd-ara.1 tracked that AO workers fix code issues but never document which inline comments were addressed. Unresolved thread count stays >0, blocking green condition #5.

## Background
bd-ara.1 tracked that AO workers fix code issues but never document which inline comments were addressed. Unresolved thread count stays >0, blocking green condition #5.

## Raw Body
## Background

bd-ara.1 tracked that AO workers fix code issues but never document which inline comments were addressed. Unresolved thread count stays >0, blocking green condition #5.

## Tenets

- Roadmap docs must accurately reflect implementation state — no ghost tasks
- The PR-description approach avoids GraphQL rate-limit burn from thread-resolution mutations
- **Fork scope**: This PR targets `jleechanorg/agent-orchestrator` (the fork). `roadmap/` and `.beads/` are first-class artifacts in this fork per CLAUDE.md. They are excluded from ComposioHQ upstream PRs.

## High-level description

The agentRules fix was already applied in a prior session: `~/.openclaw/agent-orchestrator.yaml` got a new agentRules section "AFTER FIXING REVIEW COMMENTS — DOCUMENT IN PR DESCRIPTION (bd-ara.1)" instructing workers to append a `## Resolved Comments` table to PR descriptions after fixing review feedback.

**Important clarification**: merge-gate still uses `scm.getPendingComments()` `isResolved` for condition #5. The PR description table is an **audit trail + worker self-certification record** — workers must still fix the underlying code. The table documents what was done.

## Changes

| File | Change |
|---|---|
| `roadmap/autonomy-blockers-v2.md` | Mark bd-ara.1 as ✅ DONE; clarify merge-gate not bypassed |
| `roadmap/green-loop-e2e.md` | Mark bd-ara.1 as ✅ in "Implemented:" sub-section; fix MD058 |
| `.beads/issues.jsonl` | Close bd-ara.1 bead with resolution note |

## Testing

- Core test suite: 957/957 tests pass
- No code changes — documentation only

## Verification

- [x] `pnpm --filter @jleechanorg/ao-core test` passes (957 tests)
- [x] Roadmap docs updated to reflect bd-ara.1 as implemented
- [x] Beads issue closed with resolution note

## Resolved Comments

| Reviewer | File | Comment | Resolution |
|---|---|---|---|
| coderabbitai[bot] | roadmap/zero-touch-6green-rate.md | Remove file as fork-only artifact | Removed entirely from PR |
| Copilot | roadmap/green-loop
