# PR #330: [agento] docs: evolve loop — /antig dispatch when tmux cap blocks

**Repo:** jleechanorg/agent-orchestrator
**Merged:** 2026-04-02
**Author:** jleechan2015
**Stats:** +233/-55 in 8 files

## Summary
Updates `.claude/skills/evolve_loop.md` and `.claude/commands/evolve_loop.md` so the evolution loop treats **`/antig` + `antigravity-computer-use`** as a first-class dispatch path when `/claw` / tmux spawn is blocked or multi-repo IDE work is needed. Hardens CDP target selection, CDP→Peekaboo fallbacks, `managerWindowId` guards, and CI wiring.

## Test Plan
- `pnpm --filter @jleechanorg/ao-plugin-runtime-antigravity test`
- `pnpm --filter @jleechanorg/ao-core test` (with `GITHUB_*` env for PR-title wholesome checks when running locally)

## Raw Body
## Summary

Updates `.claude/skills/evolve_loop.md` and `.claude/commands/evolve_loop.md` so the evolution loop treats **`/antig` + `antigravity-computer-use`** as a first-class dispatch path when `/claw` / tmux spawn is blocked or multi-repo IDE work is needed. Hardens CDP target selection, CDP→Peekaboo fallbacks, `managerWindowId` guards, and CI wiring.

## Changes

- **Purpose**: Document that operators must use `/antig` for IDE-mediated work; `[antig]` vs `[agento]` commit prefix for zero-touch accounting.
- **Phase 6**: Dispatch order `/claw` → `/antig`; checklist for Antigravity prompts; on `/claw` failure, try `/antig` before other fallbacks.
- **Recap / anti-stall / key files**: Count `/antig` dispatches; reference `~/.claude/skills/antigravity-computer-use/SKILL.md` and `~/.claude/commands/antig.md`.
- **Command**: `evolve_loop.md` execution note points to Phase 6 `/antig` when tmux cap blocks.
- **Runtime**: CDP prefers Antigravity-like pages; create/send fall back to Peekaboo on CDP errors; `managerWindowId > 0` for CDP; verification checks progress before sleeping between retries.
- **CI**: `integration-tests.yml` sets `aider_available` and passes `AIDER_PIP_INSTALLED` to the test env.

## Notes

- `packages/cli/vitest.config.ts` sets **`testTimeout: 15000`** (was 10s) to give GHA runners headroom for long-running `session-manager` send tests.

## Test plan

- `pnpm --filter @jleechanorg/ao-plugin-runtime-antigravity test`
- `pnpm --filter @jleechanorg/ao-core test` (with `GITHUB_*` env for PR-title wholesome checks when running locally)

## Evidence

**Claim class**: unit

| Proof | Detail |
|------|--------|
| Primary test scope | `packages/plugins/runtime-antigravity` Vitest suite |
| Commit | `6cdbed37` |
| Test files | 10 passed (10) |
| Tests | 126 passed (126) |
| Coverage | N/A (not collected in this run); structural assertions via targeted tests |

**Verdict**: PASS

**Terminal output** (local, 2026-04-01, `node` 22):

```
pnpm --filter @jleecha
