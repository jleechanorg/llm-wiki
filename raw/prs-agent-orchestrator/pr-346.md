# PR #346: [agento] fix(skeptic-cron): make evidence gate fail-closed (live + template)

**Repo:** jleechanorg/agent-orchestrator
**Merged:** 2026-04-03
**Author:** jleechan2015
**Stats:** +62/-10 in 3 files

## Summary
The skeptic-cron workflow's 6-green gate 6 (Evidence review) was pass-open: it only checked `evidence-review-bot` approval and silently treated missing evidence as N/A. This let PRs merge without any evidence bundle.

## Background
The skeptic-cron workflow's 6-green gate 6 (Evidence review) was pass-open: it only checked `evidence-review-bot` approval and silently treated missing evidence as N/A. This let PRs merge without any evidence bundle.

## Raw Body
## Background

The skeptic-cron workflow's 6-green gate 6 (Evidence review) was pass-open: it only checked `evidence-review-bot` approval and silently treated missing evidence as N/A. This let PRs merge without any evidence bundle.

## Goals

Make gate 6 fail-closed: PRs without evidence (no evidence-review-bot approval AND no passing Evidence Gate CI check) must not merge.

## Changes

### .github/workflows/skeptic-cron.yml (live workflow)

Gate 6 now has three outcomes:
1. PASS — evidence-review-bot approved the current HEAD (commit_id == PR_SHA)
2. PASS — Evidence Gate CI check exists and concluded success
3. FAIL — Evidence check present but not passing, OR evidence entirely missing

Extended ALL_PASS check to include gate 6.

### packages/cli/src/templates/skeptic/skeptic-cron.yml (workflow template)

Template sync, plus three pre-existing bugs caught during review:
- Added checks: read permission (required for gh api .../check-runs)
- Added PR_SHA extraction in merge_prs loop
- Fixed jq select() placement inside comprehension (filtering on individual check-run objects)

## Evidence

| Check | Result |
|---|---|
| Evidence Gate CI | PASS — evidence-gate.yml validates Evidence section |
| jq pagination fix | Verified: [.[].check_runs[]? | select(...)] correctly concatenates all pages |
| PR_SHA extraction | Template extracts .head.sha from PR_DATA in merge_prs loop |
| checks: read permission | Added to template permissions block |

**Terminal output**: Evidence Gate CI passes (conclusion: success) on this PR.

## Testing

- pnpm test — 1321/1323 pass (2 wholesome failures: session/ao-2328 branch name — expected in local context)
- Evidence Gate CI check passes on this PR
- skeptic-gate.yml check passes

## Resolved Comments
| Reviewer | File | Comment | Resolution |
|---|---|---|---|
| Copilot | skeptic-cron.yml | PR_SHA undefined in template merge_prs | Fixed: added PR_SHA extraction |
| Copilot | skeptic-cron.yml | jq -s add drops earlier pages | Fixed: [.[].
