# PR #84: Follow-up: rate-limit pattern docs, tests, poller REST note

**Repo:** jleechanorg/agent-orchestrator
**Merged:** 2026-03-21
**Author:** jleechan2015
**Stats:** +58/-1 in 3 files

## Summary
(none)

## Raw Body
## Context
Small follow-ups from the review on #77 (no functional behavior change intended beyond dropping one redundant pattern string).

## Changes
- **`gh-rate-limit.ts`**: Document why `API error:reth` remains in the pattern list; remove duplicate `rate Limit` entry (matching is already case-insensitive).
- **Tests**: Add `gh-rate-limit.test.ts` for `isGhRateLimitError` (including `error.cause`) and `ghSleep`.
- **poller-github-pr**: Extend module header to explain that REST fallback for `gh pr list` omits `latestReviews` / `statusCheckRollup`, so review-driven items are suppressed until the CLI path recovers.

## Verification
`pnpm --filter @jleechanorg/ao-core test -- --run src/__tests__/gh-rate-limit.test.ts`

Made with [Cursor](https://cursor.com)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Low risk: changes are limited to tests and documentation/comments, plus removing a redundant rate-limit pattern string with no expected behavioral impact due to case-insensitive matching.
> 
> **Overview**
> Adds Vitest coverage for `isGhRateLimitError` (including `error.cause` traversal and non-`Error` inputs) and `ghSleep` timing behavior.
> 
> Clarifies `GH_RATE_LIMIT_ERROR_PATTERNS` documentation (including why `"API error:reth"` is retained) and removes a duplicate-casing pattern entry since matching is case-insensitive.
> 
> Updates `poller-github-pr` module header comments to document that the REST fallback for `gh pr list` lacks `latestReviews`/`statusCheckRollup`, so review-driven work items are intentionally suppressed until the CLI path recovers.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 734a50ee1eed725e95a4ace71a26f3e1d71affd7. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
