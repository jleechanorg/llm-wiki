# PR #308: [agento] feat(skeptic): structured FAIL output with Background/Problem/Solution/Consultation sections

**Repo:** jleechanorg/agent-orchestrator
**Merged:** 2026-03-30
**Author:** jleechan2015
**Stats:** +244/-5 in 3 files

## Summary
Updates the skeptic LLM prompt so that when the verdict is FAIL, the output includes four structured sections that make failures actionable for reviewers and downstream bots:

- **## Background** — PR number, title, and what it claims to do
- **## Current Problem** — root cause, specific file/function/failure mode (concrete, not vague)
- **## Recommended Solution** — numbered actionable steps
- **## Bot Consultation** — @coderabbitai and @cursor[bot] mentions to trigger review by other agents

P

## Background
PR #267: feat(skeptic): structured FAIL output — adds 4-section FAIL format

## Raw Body
## Summary

Updates the skeptic LLM prompt so that when the verdict is FAIL, the output includes four structured sections that make failures actionable for reviewers and downstream bots:

- **## Background** — PR number, title, and what it claims to do
- **## Current Problem** — root cause, specific file/function/failure mode (concrete, not vague)
- **## Recommended Solution** — numbered actionable steps
- **## Bot Consultation** — @coderabbitai and @cursor[bot] mentions to trigger review by other agents

PASS verdict remains minimal: a single `VERDICT: PASS — [one sentence]` line.

## Motivation

Previously, FAIL verdicts were freeform text. Reviewers and agents had to parse unstructured output to find the gap. Structured FAIL output makes it easy to:
- Copy the recommended solution into a fix prompt
- Trigger CodeRabbit/Cursor re-review with specific context
- Track whether the same failure patterns repeat across PRs

## Changes

| File | Change |
|------|--------|
| `packages/cli/src/commands/skeptic/prompt.ts` | Updated `OUTPUT FORMAT` section: FAIL requires all 4 sections; PASS is minimal |
| `packages/cli/src/__tests__/skeptic/skeptic-structured-output.test.ts` | TDD tests: PASS no ## sections, FAIL all 4 sections, bot mentions |
| `packages/cli/vitest.config.ts` | Added `src/__tests__/**/*.test.ts` glob |

## Testing

- `pnpm test` (packages/cli): 224/225 pass — 1 pre-existing failure in `doctor-script.test.ts` (unrelated)
- Skeptic structured output tests: all 17 tests pass
- Manual: `ao skeptic verify -n <PR> --dry-run` emits structured FAIL format on test failures

## Evidence

**Terminal output — structured FAIL format (simulated):**
```
## Background
PR #267: feat(skeptic): structured FAIL output — adds 4-section FAIL format

## Current Problem
Gate 5 (inline comments resolved) relies on `reviewThreads` GraphQL field for isResolved.
REST API has no isResolved field — unresolved threads appear resolved via REST.

## Recommended Solution
1. Add REST fallba
