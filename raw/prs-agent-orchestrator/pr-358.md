# PR #358: [agento] fix: map permissions=auto to --dangerously-skip-permissions

**Repo:** jleechanorg/agent-orchestrator
**Merged:** 2026-04-03
**Author:** jleechan2015
**Stats:** +35/-9 in 5 files

## Summary
- `normalizePermissionMode()` in agent-claude-code returned `undefined` for `permissions: "auto"`, so AO workers configured with `permissions: auto` never received `--dangerously-skip-permissions`
- This caused Claude Code to show approval dialogs even when `bypassPermissions` was intended
- Added `mode === "auto"` → `"permissionless"` mapping, same as `"skip"` already does

## Background
AO worker configs use `permissions: auto` but the launch command builder did not recognize `"auto"` as a bypass mode. It only handled `"permissionless"`, `"skip"`, `"auto-edit"`, `"default"`, `"suggest"`.

## Raw Body
## Summary

- `normalizePermissionMode()` in agent-claude-code returned `undefined` for `permissions: "auto"`, so AO workers configured with `permissions: auto` never received `--dangerously-skip-permissions`
- This caused Claude Code to show approval dialogs even when `bypassPermissions` was intended
- Added `mode === "auto"` → `"permissionless"` mapping, same as `"skip"` already does

## Background

AO worker configs use `permissions: auto` but the launch command builder did not recognize `"auto"` as a bypass mode. It only handled `"permissionless"`, `"skip"`, `"auto-edit"`, `"default"`, `"suggest"`.

## Evidence

| Claim | Artifact | Quality | Result |
|---|---|---|---|
| Fix applied to source | `packages/plugins/agent-claude-code/src/index.ts` diff: `mode === "skip" \|\| mode === "auto" ? "permissionless"` | STRONG | Supported |
| Type definitions updated | `LegacyAgentPermissionMode` in `packages/core/src/types.ts`: `"skip" \| "auto"` | STRONG | Supported |
| Schema validation updated | `AgentPermissionSchema` in `packages/core/src/config.ts`: enum includes `"auto"` | STRONG | Supported |
| AgentSpecificConfig updated | `permissions?: AgentPermissionMode \| LegacyAgentPermissionMode` in `packages/core/src/types.ts` | STRONG | Supported |
| Regression test: launch | `treats permissions=auto as permissionless (bypasses approval dialogs)` in `index.test.ts` | STRONG | Supported |
| Regression test: restore | `treats permissions=auto as permissionless on restore` in `index.test.ts` | STRONG | Supported |
| Build succeeds | CI `npm run build` passes (Skeptic Gate CI run 23950491292: success) | STRONG | Supported |

**Repro gist**: https://gist.github.com/jleechan2015/a4f82da3193c3d1379fe82d7e323fbe2

## Verdict: PASS

All 7 merge-gate criteria met:
1. CI green ✅ (Skeptic Gate CI success)
2. No merge conflicts ✅ (mergeable=true)
3. CR APPROVED ✅ (review 4056020435)
4. Bugbot clean ✅
5. All review comments resolved ✅ (CR APPROVED supersedes previous CHANGES_REQUESTED)
