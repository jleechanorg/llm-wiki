# PR #155: [agento] fix(lifecycle): skip reactions on dead agents (bd-5o1)

**Repo:** jleechanorg/agent-orchestrator
**Merged:** 2026-03-25
**Author:** jleechan2015
**Stats:** +422/-36 in 5 files

## Summary
When a tmux session is killed manually, the AO session metadata persists. The lifecycle-worker keeps polling, detects PR state changes (e.g. CHANGES_REQUESTED), and fires reactions to a dead session.

## Background
When a tmux session is killed manually, the AO session metadata persists. The lifecycle-worker keeps polling, detects PR state changes (e.g. CHANGES_REQUESTED), and fires reactions to a dead session.

## Raw Body
## Background
When a tmux session is killed manually, the AO session metadata persists. The lifecycle-worker keeps polling, detects PR state changes (e.g. CHANGES_REQUESTED), and fires reactions to a dead session.

## Goals
Prevent reactions from firing to confirmed-dead sessions and prevent auto-merge retry against dead agents.

## Changes
1. Refactor determineStatus() to return { status, agentDead } so checkSession() has access to the agentDead flag.
2. Skip all reactions when agentDead=true in the transition block.
3. Skip auto-merge retry when agentDead=true.

## Testing
All 814 core tests pass (814/814).

Closes bd-5o1.

🤖 Generated with [Claude Code](https://claude.ai/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Updates lifecycle polling and reaction dispatch logic, including auto-merge behavior, which could change when/if PRs merge or notifications fire for edge cases around dead agents.
> 
> **Overview**
> Prevents lifecycle polling from dispatching *agent-dependent* reactions to confirmed-dead sessions by threading an `agentDead` flag through `determineStatus()` and into both transition reactions and `maybeDispatchReviewBacklog()` (skipping only `send-to-agent`, while allowing SCM/notification reactions like auto-merge).
> 
> Adjusts dead-agent handling to preserve `agentDead` across PR-status derivation and simplifies the final dead-agent kill guard, while keeping auto-merge retries for `mergeable` PRs even if the agent has exited.
> 
> Adds a dedicated `lifecycle-manager.dead-agent` test suite covering dead-vs-alive reaction behavior, and tightens GitHub SCM auto-merge GraphQL success detection to avoid false positives and correctly fall back to REST.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 694bbad96e2d10b23a6bdc4b7d03a1f541927bf3. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is
