# PR #292: [agento] feat(skeptic): harness-level claim-verifier for skeptic gate assertions

**Repo:** jleechanorg/agent-orchestrator
**Merged:** 2026-03-29
**Author:** jleechan2015
**Stats:** +1050/-3 in 10 files

## Summary
bd-upxh: Deterministic claim verifier enforcing **run-level + comment-level evidence** before agents may report 'working' status.

- **Fail-closed**: ambiguous → INSUFFICIENT/FAIL, never PASS
- **Decision matrix**: PASS only when both `runLevel.result === "pass"` AND `commentLevel.result === "pass"`
- **blocksWorking flag**: any outcome other than PASS blocks 'working' status

## Raw Body
## Summary

bd-upxh: Deterministic claim verifier enforcing **run-level + comment-level evidence** before agents may report 'working' status.

- **Fail-closed**: ambiguous → INSUFFICIENT/FAIL, never PASS
- **Decision matrix**: PASS only when both `runLevel.result === "pass"` AND `commentLevel.result === "pass"`
- **blocksWorking flag**: any outcome other than PASS blocks 'working' status

## Prior Evidence (GHA runs referenced in bd-upxh spec)

- [Run 23708195296 / Job 69063490191](https://github.com/jleechanorg/agent-orchestrator/actions/runs/23708195296/jobs/69063490191) — skeptic-gate precheck passing, demonstrates trigger chain
- [Run 23713926660 / Job 69077909594](https://github.com/jleechanorg/agent-orchestrator/actions/runs/23713926660/jobs/69077909594) — VERDICT: PASS posted by lifecycle-worker via `ao skeptic verify`

## High-level description

### Claim Verifier Decision Matrix

| Run-level | Comment-level | Outcome | blocksWorking |
|-----------|--------------|---------|---------------|
| pass | pass | **PASS** | false |
| fail | pass | **FAIL** | true |
| pass | fail | **FAIL** | true |
| fail | fail | **FAIL** | true |
| skipped | pass | INSUFFICIENT | true |
| pass | skipped | INSUFFICIENT | true |
| skipped | skipped | INSUFFICIENT | true |
| missing | any | INSUFFICIENT | true |

### Files changed

| File | Change |
|------|--------|
| `packages/cli/src/commands/skeptic/claim-verifier.ts` | **NEW** — pure `verifySkepticClaim()` decision matrix |
| `packages/cli/src/commands/skeptic.ts` | Calls `verifySkepticClaim()` after `postVerdict()`, warns when `blocksWorking=true` |
| `packages/cli/__tests__/commands/skeptic/claim-verifier.test.ts` | **NEW** — 32 unit tests covering full decision matrix |
| `packages/core/src/fork-claim-verification.ts` | **NEW** — lifecycle-manager reaction checking GHA skeptic-gate chain |
| `packages/core/src/lifecycle-manager.ts` | Fires `claim-verification` reaction after skeptic-review success and SHA-change retest |
| `p
