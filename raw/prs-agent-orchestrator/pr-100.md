# PR #100: [agento] fix: add AO pre-spawn gate for rate limits and capacity

**Repo:** jleechanorg/agent-orchestrator
**Merged:** 2026-03-22
**Author:** jleechan2015
**Stats:** +43/-0 in 3 files

## Summary
GitHub API limits and session saturation caused deadlocks in AO spawn/claim flows.

## Background
GitHub API limits and session saturation caused deadlocks in AO spawn/claim flows.

## Raw Body
## Background
GitHub API limits and session saturation caused deadlocks in AO spawn/claim flows.

## Goals
- Add explicit pre-spawn rate/capacity checks.
- Document GraphQL→REST fallback thresholds.
- Provide reusable spawn-gate checklist instructions.

## High-level changes
1. Updated `agent-orchestrator.yaml.example` with spawn gate guidance.
2. Added `Pre-spawn capacity check` section to repo `CLAUDE.md`.
3. Added `.claude/skills/ao-spawn-gate.md` with exact commands and thresholds.

## Testing
- Documentation/config guidance update only (no runtime code changed).

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Low risk: documentation-only changes that add guidance for when to block spawning workers based on GitHub API budgets and tmux session count; no runtime behavior is modified.
> 
> **Overview**
> Adds a documented **pre-spawn gate** for AO worker dispatch to avoid GitHub API rate-limit deadlocks and local session saturation.
> 
> Introduces `.claude/skills/ao-spawn-gate.md` with a concrete checklist (commands, thresholds, and a REST fallback example), and updates `CLAUDE.md` plus `agent-orchestrator.yaml.example` comments to require checking `gh api rate_limit` and `tmux` session counts (block spawns at `graphql.remaining < 200` or `tmux sessions > 15`, and prefer REST / defer reads at tighter budgets).
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit fbf5d916af05d8ee0c78814995c2ae2be3a1ca13. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
