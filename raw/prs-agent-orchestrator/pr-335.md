# PR #335: [agento] feat(scripts): curl installer for skeptic CI in consumer repos

**Repo:** jleechanorg/agent-orchestrator
**Merged:** 2026-04-02
**Author:** jleechan2015
**Stats:** +283/-0 in 3 files

## Summary
- Add `scripts/install-skeptic-ci-for-repo.sh` so consumer repos can install Skeptic Gate + cron workflows from raw GitHub without building the AO monorepo.
- Document `ao skeptic install` vs curl path in SETUP.md.
- Vitest harness for the installer script (`packages/cli/__tests__/scripts/install-skeptic-ci-for-repo.integration.test.ts`).

## Test Plan
- From an empty git repo: run the script with `SKEPTIC_CI_REPO` / `SKEPTIC_CI_REF` and confirm YAML files land under `.github/workflows/`.
- `pnpm --filter @jleechanorg/ao-cli test -- __tests__/scripts/install-skeptic-ci-for-repo.integration.test.ts`

## Raw Body
## Summary

- Add `scripts/install-skeptic-ci-for-repo.sh` so consumer repos can install Skeptic Gate + cron workflows from raw GitHub without building the AO monorepo.
- Document `ao skeptic install` vs curl path in SETUP.md.
- Vitest harness for the installer script (`packages/cli/__tests__/scripts/install-skeptic-ci-for-repo.integration.test.ts`).

## Why

`ao skeptic install` is ideal when Node/AO CLI is available; a small shell installer supports one-shot adoption and matches templates under `packages/cli/src/templates/skeptic/`.

## Test plan

- From an empty git repo: run the script with `SKEPTIC_CI_REPO` / `SKEPTIC_CI_REF` and confirm YAML files land under `.github/workflows/`.
- `pnpm --filter @jleechanorg/ao-cli test -- __tests__/scripts/install-skeptic-ci-for-repo.integration.test.ts`

## Evidence

**Claim class**: unit

```text
Smoke test (temp git repo, fetch from main):

FETCH https://raw.githubusercontent.com/jleechanorg/agent-orchestrator/main/packages/cli/src/templates/skeptic/skeptic-gate.yml
OK   skeptic-gate.yml
FETCH https://raw.githubusercontent.com/jleechanorg/agent-orchestrator/main/packages/cli/src/templates/skeptic/skeptic-cron.yml
OK   skeptic-cron.yml

297 skeptic-gate.yml
379 skeptic-cron.yml
```

Related consumer sync PR: https://github.com/jleechanorg/worldarchitect.ai/pull/6069

## Verdict: PASS

## Resolved Comments

| Reviewer | File | Comment | Resolution |
|---|---|---|---|
| coderabbitai[bot] | SETUP.md | Default `raw.githubusercontent.com` URL vs upstream ComposioHQ | Documented that **jleechanorg/agent-orchestrator** hosts fork-maintained templates; `SKEPTIC_CI_REPO` / `SKEPTIC_CI_REF` for other sources |
| Copilot | SETUP.md | Commit paths should include `.github/workflows/` for both YAMLs | Updated to `.github/workflows/skeptic-gate.yml` and `.github/workflows/skeptic-cron.yml` |
| Copilot / Cursor | scripts/install-skeptic-ci-for-repo.sh | Help text vs default when no flags | Usage now states no flags installs both (same as 
