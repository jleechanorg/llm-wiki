# PR #233: [agento] fix(skeptic): remove invalid --no-input flag from Claude CLI invocation

**Repo:** jleechanorg/agent-orchestrator
**Merged:** 2026-03-27
**Author:** jleechan2015
**Stats:** +174/-15 in 2 files

## Summary
`ao skeptic` shells out to `claude --print --no-input` to run LLM evaluation. The `--no-input` flag does not exist in Claude CLI v2.1.x, causing the command to fail silently (stderr suppressed by `2>/dev/null`). The catch block then returns a generic \"CLI not available\" message, making it appear Claude CLI isn't installed.

Discovered when running `ao skeptic --pr 5879 --repo jleechanorg/worldarchitect.ai` from within a Claude Code CLI session — the very tool it was looking for.

## Background
`ao skeptic` shells out to `claude --print --no-input` to run LLM evaluation. The `--no-input` flag does not exist in Claude CLI v2.1.x, causing the command to fail silently (stderr suppressed by `2>/dev/null`). The catch block then returns a generic \"CLI not available\" message, making it appear Claude CLI isn't installed.

Discovered when running `ao skeptic --pr 5879 --repo jleechanorg/worldarchitect.ai` from within a Claude Code CLI session — the very tool it was looking for.

## Raw Body
## Background

`ao skeptic` shells out to `claude --print --no-input` to run LLM evaluation. The `--no-input` flag does not exist in Claude CLI v2.1.x, causing the command to fail silently (stderr suppressed by `2>/dev/null`). The catch block then returns a generic \"CLI not available\" message, making it appear Claude CLI isn't installed.

Discovered when running `ao skeptic --pr 5879 --repo jleechanorg/worldarchitect.ai` from within a Claude Code CLI session — the very tool it was looking for.

## Goals

1. Fix `ao skeptic` to work with Claude CLI v2.1.x
2. Surface actual errors instead of generic fallback messages

## High-level description of changes

- Remove invalid `--no-input` flag, keep `--print` (correct non-interactive mode)
- Increase timeout from 60s to 120s (PR evaluations can be large)
- Catch block now includes the actual error message for debugging
- Metadata updater: add [agento] prefix guardrail on `gh pr create` (PreToolUse hook)

## Testing

- Verified `claude --print --no-input` fails with `error: unknown option '--no-input'`
- Verified `claude --print` works correctly with stdin prompt
- Ran `ao skeptic --dry-run` after fix — produces real LLM verdict

## Evidence

**Claim class**: unit

**Claim**: skeptic fix resolves `--no-input` invalid flag

**Verdict**: PASS

- Evidence type: Code change (before/after)
- Before:
  ```bash
  claude --print --no-input
  # Error: unknown option `--no-input`
  ```
- After: `claude --print` via async spawn with 120s timeout, proper error surfacing
- Skeptic Gate: PASS (verified in CI)
- Actionlint: PASS (verified in CI)

**Claim: metadata-updater.sh [agento] prefix guardrail works**

- PreToolUse hook intercepts `gh pr create` and denies if title lacks `[agento]` prefix
- Exit 0 with `{}` allows the tool to proceed if title is valid
- Evidence: code inspection of metadata-updater.sh guardrail block

---
Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Med
