# PR #232: [agento] feat: periodic MCP mail global inbox poll + heartbeat for AO workers

**Repo:** jleechanorg/agent-orchestrator
**Merged:** 2026-03-27
**Author:** jleechan2015
**Stats:** +582/-308 in 11 files

## Summary
- Add MCP mail HTTP client to core (packages/core/src/mcp-mail.ts) with register_agent, send_message, fetch_inbox, and lifecycle helpers (sendMcpMailHeartbeat, sendMcpMailSessionStart, sendMcpMailSessionEnd)
- Wire MCP mail into lifecycle manager (packages/core/src/lifecycle-manager.ts): inbox polls every 5 minutes, heartbeat sent per-poll with active session count, session start/end messages sent on lifecycle transitions
- Add notifier-mcp-mail plugin (packages/plugins/notifier-mcp-mail/) — Plu

## Test Plan
- [x] Unit tests for notifier-mcp-mail plugin pass (MCP JSON-RPC format, register_agent, send_message, error handling)
- [x] Core package typecheck passes
- [x] Full workspace builds (pnpm --filter @jleechanorg/ao-core build && pnpm --filter @jleechanorg/ao-cli build)
- [x] Live MCP mail integration: heartbeat + session start + session end confirmed in global:jleechanclaw inbox via fetch_inbox

## Raw Body
## Summary
- Add MCP mail HTTP client to core (packages/core/src/mcp-mail.ts) with register_agent, send_message, fetch_inbox, and lifecycle helpers (sendMcpMailHeartbeat, sendMcpMailSessionStart, sendMcpMailSessionEnd)
- Wire MCP mail into lifecycle manager (packages/core/src/lifecycle-manager.ts): inbox polls every 5 minutes, heartbeat sent per-poll with active session count, session start/end messages sent on lifecycle transitions
- Add notifier-mcp-mail plugin (packages/plugins/notifier-mcp-mail/) — PluginModule for notify, notifyWithActions, post — sends AO lifecycle events to global:jleechanclaw inbox
- Register mcp-mail notifier in BUILTIN_PLUGINS (packages/core/src/plugin-registry.ts)

## Test plan
- [x] Unit tests for notifier-mcp-mail plugin pass (MCP JSON-RPC format, register_agent, send_message, error handling)
- [x] Core package typecheck passes
- [x] Full workspace builds (pnpm --filter @jleechanorg/ao-core build && pnpm --filter @jleechanorg/ao-cli build)
- [x] Live MCP mail integration: heartbeat + session start + session end confirmed in global:jleechanclaw inbox via fetch_inbox

## Evidence

**Claim class**: integration

**Evidence**: Core package typecheck passes. Notifier plugin integration tested via live MCP mail inbox verification (gist: https://gist.github.com/jleechan2015/cd52751d08265b44d21ee5d27fc9fbf3).

**Verdict**: PASS

**Test output**: Unit test suite for notifier-mcp-mail plugin (24 tests covering manifest, create, notify, notifyWithActions, post, and register):
- manifest: correct metadata (name, slot, version)
- create: name, endpoint validation, warning on missing config
- notify: JSON-RPC 2.0 envelope, Content-Type header, agentId/projectId params, event body, action links, error handling
- notifyWithActions: appends action links to body_md
- post: free-form text, context channel routing, null when no endpoint
- register: POSTs register_agent on first call, correct params, deduplication on concurrent calls

Full test suite: packag
