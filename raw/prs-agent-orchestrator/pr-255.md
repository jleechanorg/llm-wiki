# PR #255: [agento] fix: replace idx++ pattern with forEach in mergeGate tests

**Repo:** jleechanorg/agent-orchestrator
**Merged:** 2026-03-28
**Author:** jleechan2015
**Stats:** +213/-13 in 5 files

## Summary
Replace `let idx = 0; values[idx++]` pattern with `values.forEach(v => ...)` in two mergeGate test files to fix `no-useless-assignment` ESLint errors.

**Files**:
- `packages/cli/__tests__/commands/skeptic/mergeGate.test.ts`
- `packages/cli/__tests__/commands/skeptic/mergeGate.debug.test.ts`

**Change**: In `setupGhJson()`, replace sequential `mockGhJson.mockResolvedValueOnce(values[idx++] as any)` calls with `values.forEach(v => mockGhJson.mockResolvedValueOnce(v as any))`. Same behavior, elimi

## Raw Body
## Summary

Replace `let idx = 0; values[idx++]` pattern with `values.forEach(v => ...)` in two mergeGate test files to fix `no-useless-assignment` ESLint errors.

**Files**:
- `packages/cli/__tests__/commands/skeptic/mergeGate.test.ts`
- `packages/cli/__tests__/commands/skeptic/mergeGate.debug.test.ts`

**Change**: In `setupGhJson()`, replace sequential `mockGhJson.mockResolvedValueOnce(values[idx++] as any)` calls with `values.forEach(v => mockGhJson.mockResolvedValueOnce(v as any))`. Same behavior, eliminates unused-variable lint error.

## Evidence

**Claim class**: unit-test-coverage

| Evidence | Details |
|---|---|
| Lint | `pnpm lint` → 0 errors, 50 warnings (was 2 no-useless-assignment errors) |

```
=== lint result ===
packages/cli/__tests__/commands/skeptic/mergeGate.test.ts: 0 errors
packages/cli/__tests__/commands/skeptic/mergeGate.debug.test.ts: 0 errors
```

**Verdict**: PASS


<!-- This is an auto-generated comment: release notes by coderabbit.ai -->
## Summary by CodeRabbit

* **Tests**
  * Simplified test helper mock setup to drive queued mock responses from provided arrays, improving clarity and maintainability.
* **Documentation**
  * Added a PR design page and accompanying markdown record; minor template/footer tweak for generated design docs.
<!-- end of auto-generated comment: release notes by coderabbit.ai -->

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Low risk: changes are limited to test helper refactoring and a markdown formatting tweak in a doc-generation script, with no production logic impact.
> 
> **Overview**
> Refactors `setupGhJson()` in `mergeGate.test.ts` and `mergeGate.debug.test.ts` to queue `mockGhJson.mockResolvedValueOnce` responses via `values.forEach(...)`, removing the `idx++` pattern and associated lint noise.
> 
> Fixes markdown output in `scripts/generate-pr-design-docs.mjs` by un-escaping the footer line so it renders as intended.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbo
