# PR #423: [agento] fix(cli): remove duplicate timeout in doctor-script.test.ts (bd-doctest)

**Repo:** jleechanorg/agent-orchestrator
**Merged:** 2026-04-11
**Author:** jleechan2015
**Stats:** +1/-1 in 1 files

## Summary
- Removes duplicate `, 30_000` positional timeout from the first `it()` in `doctor-script.test.ts`
- Vitest 3 does not support the 4-argument overload `it(name, options, fn, timeout)` — the timeout was already set via `{ timeout: 30000 }` in the options object
- Bug introduced in commit `5eeb15c3` after PR #403 merged; tracked as bead `bd-doctest`

## Test Plan
- [ ] `pnpm --filter @composio/ao-cli test` passes with both `doctor-script.test.ts` tests completing within the 30s timeout

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Raw Body
## Summary

- Removes duplicate `, 30_000` positional timeout from the first `it()` in `doctor-script.test.ts`
- Vitest 3 does not support the 4-argument overload `it(name, options, fn, timeout)` — the timeout was already set via `{ timeout: 30000 }` in the options object
- Bug introduced in commit `5eeb15c3` after PR #403 merged; tracked as bead `bd-doctest`

## Evidence

**Claim class:** documentation/test-only (no runtime behavior changed)

**Verdict:** PASS — single-line removal of duplicate positional timeout argument. The `{ timeout: 30000 }` options object at the `it()` declaration already satisfies the timeout requirement; the trailing `, 30_000` was a redundant 4th argument unsupported by Vitest 3.

## Test plan

- [ ] `pnpm --filter @composio/ao-cli test` passes with both `doctor-script.test.ts` tests completing within the 30s timeout

🤖 Generated with [Claude Code](https://claude.com/claude-code)

