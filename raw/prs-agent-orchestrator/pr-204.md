# PR #204: fix(doctor): correct non-canonical lifecycle-worker detection

**Repo:** jleechanorg/agent-orchestrator
**Merged:** 2026-03-26
**Author:** jleechan2015
**Stats:** +11/-8 in 2 files

## Summary
Fixes two bugs in `ao doctor` lifecycle-worker detection in `packages/cli/scripts/ao-doctor.sh`:

1. **Field index bug**: `\$11` was used to extract the binary path, but for `node /path/ao lifecycle-worker` the command path is at `$12` (since `$11` = `/path/ao`). Changed to iterate fields and find the one matching `/ao$` (the binary path).

2. **Grep pattern too broad**: `grep 'lifecycle-worker'` matched file names like `ao-lifecycle-triage.md` in truncated `ps aux` output. Changed to require tr

## Raw Body
## Summary

Fixes two bugs in `ao doctor` lifecycle-worker detection in `packages/cli/scripts/ao-doctor.sh`:

1. **Field index bug**: `\$11` was used to extract the binary path, but for `node /path/ao lifecycle-worker` the command path is at `$12` (since `$11` = `/path/ao`). Changed to iterate fields and find the one matching `/ao$` (the binary path).

2. **Grep pattern too broad**: `grep 'lifecycle-worker'` matched file names like `ao-lifecycle-triage.md` in truncated `ps aux` output. Changed to require trailing space: `'lifecycle-worker '` (the actual binary name).

## Testing
- `bash packages/cli/scripts/ao-doctor.sh` runs without errors
- Lifecycle worker processes are correctly detected

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Adjusts `ao doctor --fix` stale worker identification, which can affect which PIDs get flagged or killed if the new matching logic misbehaves. Change is small and targeted, but it touches process-detection heuristics that vary across environments.
> 
> **Overview**
> Improves `ao doctor` lifecycle-worker detection to reduce false positives and correctly identify the `ao` binary used by running workers.
> 
> The worker list grep now matches `lifecycle-worker ` (with a trailing space) to avoid accidentally matching similarly named file paths in `ps` output, and the stale-binary check now scans all `ps` fields to find the `/ao` executable instead of assuming a fixed column (handling both direct `ao ...` and `node .../ao ...` invocations).
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 978bdc6592281d481d605497735fb586d7786f58. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->

## Summary by CodeRabbit

## Release Notes

* **Bug Fixes**
  * Improved lifecycle-w
