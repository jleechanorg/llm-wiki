# PR #53: fix(ci): resolve all lint and typecheck failures on main

**Repo:** jleechanorg/agent-orchestrator
**Merged:** 2026-03-21
**Author:** jleechan2015
**Stats:** +4/-5 in 2 files

## Summary
Main branch CI (Lint, Typecheck, Test jobs) has been failing for all PRs because of pre-existing errors on main itself. This blocked all 4 open PRs from going green.

## Background
Main branch CI (Lint, Typecheck, Test jobs) has been failing for all PRs because of pre-existing errors on main itself. This blocked all 4 open PRs from going green.

## Raw Body
## Background
Main branch CI (Lint, Typecheck, Test jobs) has been failing for all PRs because of pre-existing errors on main itself. This blocked all 4 open PRs from going green.

## Goals
- Fix lint: 0 errors
- Fix typecheck: clean pass
- Fix test: no regressions introduced

## Tenets
- Minimal changes — fix exactly what's broken, don't refactor
- No behavior changes to production code

## High-level description of changes

**eslint.config.js:**
- Add `**/dist-server/**` to ignores (built files were being linted)
- Add Node.js globals (process, console, setTimeout, etc.) for plugin/core/cli packages — base ESLint rule doesn't set environment globals in flat config
- Extend `scripts/**` glob to also cover `packages/*/scripts/**`
- Set `fixStyle: "inline-type-imports"` on `consistent-type-imports` to avoid conflict between `no-duplicate-imports` and TypeScript's `import type` pattern

**Duplicate import merges (7 files):**
- `parallel-retry.ts`, `session-exit-proof.ts`, `session-reaper.ts` — merge split value/type imports into single statements
- 4 test files — same pattern

**Unused variable fixes (5 files):**
- Remove `lstatSync` (agent-base), `PluginModule` (agent-cursor)
- Prefix `writeJsonl→_writeJsonl`, `worktreePath→_worktreePath` in tests
- Remove `ReactionResult` from failure-budget.test.ts

**Test fixes:**
- Use `vi.mocked(fn).mock.calls` instead of `fn.mock.calls` for functions not typed as mocks
- Cast `config.notifiers` injection via `unknown` (Notifier vs NotifierConfig types)
- Fix generic mock type in parallel-retry.test.ts
- Fix null→OutboxEntry cast via unknown in slack-outbox.test.ts
- Remove unused `eslint-disable` directive in webhook-ingress.test.ts
- Add `{ cause: err }` to preserve-caught-error in poller-github-pr

## Testing
- `pnpm lint`: 0 errors, 24 warnings (all pre-existing warnings)
- `pnpm typecheck`: clean
- `pnpm test`: 683/686 pass — 3 failures are pre-existing local-only failures caused by `~/.openclaw/agent-orchestrator.yaml` on 
