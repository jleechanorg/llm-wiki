# PR #54: fix(ci): regenerate pnpm-lock.yaml to fix CI failures

**Repo:** jleechanorg/agent-orchestrator
**Merged:** 2026-03-21
**Author:** jleechan2015
**Stats:** +5/-3 in 4 files

## Summary
(none)

## Raw Body
## Problem

`pnpm install --frozen-lockfile` fails in CI because `pnpm-lock.yaml` is out of sync with `package.json` in the web package:
- Lockfile had `link:` references instead of `workspace:*`
- `@tailwindcss/postcss` and `tailwindcss` were missing from lockfile

This caused **Test**, **Test Fresh Onboarding**, and **Typecheck** CI jobs to fail across all PRs.

## Fix

Regenerated `pnpm-lock.yaml` with `pnpm install`. Only 6 lines changed.

## Verification

- `pnpm install --frozen-lockfile` ✅
- `pnpm -r build` ✅  
- `pnpm test` ✅

## Notes

- **Scan for Secrets (gitleaks)** failure is unrelated — requires `GITLEAKS_LICENSE` repo secret
- **Dependency Review** only runs on PRs, not a main issue

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Low risk lint/typing tweaks: changes only ESLint configuration/comments and a minor TypeScript type assertion in batch spawn duplicate detection, with no behavioral changes expected.
> 
> **Overview**
> Relaxes ESLint `no-console` enforcement for Node-facing code by explicitly turning it off for scripts and Node packages, and annotates the `rebuild-node-pty` install script accordingly.
> 
> Cleans up small TypeScript/lint issues: switches a non-null assertion to a safer type assertion in `batch-spawn` issue/session mapping, and removes an unnecessary lint suppression around the optional dynamic `node-pty` import in `direct-terminal-ws`.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 3ec6c7143517280b8ee3eb30e93d2b6b438cae8e. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->

## Summary by CodeRabbit

* **Chores**
  * Updated linting rules for CLI build scripts to improve code quality.

* **Refactor**
  * Enhanced type safety in session batch processing logic.

<!-- end of auto-generated comment: release notes by code
