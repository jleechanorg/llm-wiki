# PR #286: [agento] fix(skeptic): route eval via AO worker + gate on 6-green eligibility

**Repo:** jleechanorg/agent-orchestrator
**Merged:** 2026-03-29
**Author:** jleechan2015
**Stats:** +944/-613 in 7 files

## Summary
Fix skeptic CI/cron workflows to:
1. **No API keys in GHA** — LLM evaluation routes via AO worker (Codex OAuth, no secrets)
2. **Gate on 6-green eligibility** — only trigger skeptic for PRs passing gates 1-5 (CI, merge conflicts, CR, Bugbot, comments)
3. **Docs-only fast-path skip** — PRs with only markdown/text files are skipped

## Background
User request verbatim:
> skeptic CI trigger should not use API keys. W ended to fix this. It should use AO workers for codex and use OAuth. Also it should only run on 6 green PRs not every push. The cron and CI should skip non 6 green PRs or docs only is fine to run.

## Test Plan
- [x] pnpm --filter @jleechanorg/ao-cli test -- skeptic — 64/64 pass
- [ ] CI workflows validated via PR (skeptic-gate exits early for non-6-green PRs, cron skips docs-only)

## Raw Body
## Summary

Fix skeptic CI/cron workflows to:
1. **No API keys in GHA** — LLM evaluation routes via AO worker (Codex OAuth, no secrets)
2. **Gate on 6-green eligibility** — only trigger skeptic for PRs passing gates 1-5 (CI, merge conflicts, CR, Bugbot, comments)
3. **Docs-only fast-path skip** — PRs with only markdown/text files are skipped

## Background

User request verbatim:
> skeptic CI trigger should not use API keys. W ended to fix this. It should use AO workers for codex and use OAuth. Also it should only run on 6 green PRs not every push. The cron and CI should skip non 6 green PRs or docs only is fine to run.

## Changes

### .github/workflows/skeptic-gate.yml
- **Removed**: CLI setup + ao skeptic verify execution from GHA runner
- **Removed**: API key inputs (never used but declared)
- **Added**: 6-green pre-check step (gates 1-5) — exits fast for non-eligible PRs
- **Kept**: Trigger comment post + polling step (lifecycle-manager AO worker handles LLM eval via Codex OAuth)

### .github/workflows/skeptic-cron.yml
- **Removed**: CLI install, skeptic-setup, ao skeptic verify calls
- **Removed**: OPENAI_API_KEY and ANTHROPIC_API_KEY from secrets
- **Added**: post_triggers step — pre-filters PRs to 6-green eligible + docs-only skip, posts trigger comments
- **Restructured**: AO worker posts VERDICT comment asynchronously; cron checks gate 7 + merges

### .github/actions/skeptic-setup/action.yml
- Removed anthropic_api_key and openai_api_key inputs
- Removed Codex/Claude CLI install (OAuth-based CLI runs via lifecycle-manager on local machine, not GHA)

### .github/actions/skeptic-run/action.yml
- Deleted (no longer used after removing CLI execution from skeptic-gate)

### Template skeptic workflows
- Synced packages/cli/src/templates/skeptic/skeptic-gate.yml and skeptic-cron.yml to match live workflow changes

### packages/cli/__tests__/commands/skeptic/skeptic.test.ts
- Added 7 docs-only gate tests (file extension classification)
- Added 13 6-green gate test
