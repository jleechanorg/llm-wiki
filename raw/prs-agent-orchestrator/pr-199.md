# PR #199: [agento] fix(hooks): remove no-op pr-green-check.py hook

**Repo:** jleechanorg/agent-orchestrator
**Merged:** 2026-03-26
**Author:** jleechan2015
**Stats:** +0/-225 in 1 files

## Summary
The `pr-green-check.py` UserPromptSubmit hook was designed to rate-limit green-check polling, but it always exits 0 and outputs nothing to stdout. This means it never actually blocks any prompt — it just burns 2 REST API calls (PR state + CR review state) on every single prompt submission for branches matching its regex.

The branch-name PR regex (`[apr][a-z]-[0-9]{2,4}`) is also overly broad, matching branches like `fix/bd-soc-v2` as PR #2.

Related bead: bd-soc

## Background
The `pr-green-check.py` UserPromptSubmit hook was designed to rate-limit green-check polling, but it always exits 0 and outputs nothing to stdout. This means it never actually blocks any prompt — it just burns 2 REST API calls (PR state + CR review state) on every single prompt submission for branches matching its regex.

The branch-name PR regex (`[apr][a-z]-[0-9]{2,4}`) is also overly broad, matching branches like `fix/bd-soc-v2` as PR #2.

Related bead: bd-soc

## Raw Body
## Background

The `pr-green-check.py` UserPromptSubmit hook was designed to rate-limit green-check polling, but it always exits 0 and outputs nothing to stdout. This means it never actually blocks any prompt — it just burns 2 REST API calls (PR state + CR review state) on every single prompt submission for branches matching its regex.

The branch-name PR regex (`[apr][a-z]-[0-9]{2,4}`) is also overly broad, matching branches like `fix/bd-soc-v2` as PR #2.

Related bead: bd-soc

## Goals

- Stop wasting 2 REST API calls per prompt on a hook that never blocks
- Remove dead code that gives false confidence about rate-limit protection

## Tenets

- Remove, don't fix — the hook's design (exit 0 always, no stdout) means it was never functional as a gate
- Clean removal — delete all artifacts (repo file, global file, settings registration, tmp state)

## High-level description of changes

Complete removal of the pr-green-check.py hook:
1. Deleted `.claude/hooks/pr-green-check.py` from the repo
2. Removed the hook registration from `~/.claude/settings.json` (UserPromptSubmit array)
3. Removed `~/.claude/hooks/pr-green-check.py` (global copy)
4. Cleaned up `/tmp/pr_green_check/` state directory

## Testing

- Verified `grep -n "pr-green-check" ~/.claude/settings.json` returns no matches
- Verified hook file no longer exists in repo or global hooks dir
- Verified git status shows only the expected deletion

## Low-level details

- **.claude/hooks/pr-green-check.py**: Deleted. 225-line Python script that always exited 0, made 2 gh API calls per invocation, wrote state to /tmp but never output anything to stdout or returned non-zero.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Low risk: this PR only deletes an unused Claude hook script; the main impact is that any implicit reliance on that hook’s side effects (e.g., its API polling/state files) will stop.
> 
> **Overview**
> Removes the `pr-green-chec
