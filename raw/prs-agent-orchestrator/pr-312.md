# PR #312: [agento] fix: metadata-updater.sh regex fix and simplification

**Repo:** jleechanorg/agent-orchestrator
**Merged:** 2026-03-30
**Author:** jleechan2015
**Stats:** +6/-6 in 1 files

## Summary
The `metadata-updater.sh` PostToolUse hook had accumulated over-engineered branch detection logic. Three specific improvements are needed:

1. **Env-assignment regex** was using `[^[:space:]]*` which greedily consumes the `=` character, causing `FOO=bar gh pr create` to not strip correctly.
2. **Sed escaping** unnecessarily escaped `|` which is not a BRE special char (delimiter is `/`, not `|`).
3. **Branch verification via `git rev-parse`** added latency and complexity for marginal safety — the

## Background
The `metadata-updater.sh` PostToolUse hook had accumulated over-engineered branch detection logic. Three specific improvements are needed:

1. **Env-assignment regex** was using `[^[:space:]]*` which greedily consumes the `=` character, causing `FOO=bar gh pr create` to not strip correctly.
2. **Sed escaping** unnecessarily escaped `|` which is not a BRE special char (delimiter is `/`, not `|`).
3. **Branch verification via `git rev-parse`** added latency and complexity for marginal safety — the pattern guard (`[/-]` in branch name) is sufficient to distinguish branches from paths.

## Raw Body
## Background
The `metadata-updater.sh` PostToolUse hook had accumulated over-engineered branch detection logic. Three specific improvements are needed:

1. **Env-assignment regex** was using `[^[:space:]]*` which greedily consumes the `=` character, causing `FOO=bar gh pr create` to not strip correctly.
2. **Sed escaping** unnecessarily escaped `|` which is not a BRE special char (delimiter is `/`, not `|`).
3. **Branch verification via `git rev-parse`** added latency and complexity for marginal safety — the pattern guard (`[/-]` in branch name) is sufficient to distinguish branches from paths.

## Goals
- Simplify `metadata-updater.sh` without losing functionality
- Fix the env-assignment regex bug
- Remove unnecessary complexity

## Testing
- `pnpm test` passes (4 pre-existing failures unrelated to this change)
- No functional behavior change for the `gh pr create` PR-url extraction path

## Changes
- `[^[:space:]]*` → `[^= ]*` in env-striping regex — stops at `=` instead of consuming it
- `sed 's/[&|\\]/\\&/g'` → `sed 's/[&\\]/\\&/g'` — removes unnecessary `|` escape
- Removes `git rev-parse --verify` branch verification in checkout/switch handlers — pattern guard (`[/-]` requiring separator in branch names) is sufficient

## Evidence

**Claim class**: unit
**CR approved**: 2026-03-30 18:50 UTC (formal APPROVED review)
**Verdict**: PASS — unit test evidence provided (4 pre-existing failures on main, not caused by this change)
**Test output** (pre-existing failures confirmed — same 4 failures with and without changes):
```
Test Files  2 failed | 63 passed (65)
Tests       4 failed | 1173 passed (1177)
```

The 4 failures (`wholesome.test.ts`, `config.test.ts`) are pre-existing on `main` and unrelated to this change.





<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Low risk shell-script changes limited to updating session metadata; main risk is mis-detecting a `git checkout` operand and writing an incorrect `branch` value.
> 
> **Overview**
> Improves 
