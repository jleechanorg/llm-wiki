# PR #177: [agento] fix(lifecycle): bd-6jc follow-up — SCM failure counter hygiene

**Repo:** jleechanorg/agent-orchestrator
**Merged:** 2026-03-25
**Author:** jleechan2015
**Stats:** +23/-14 in 1 files

## Summary
(none)

## Raw Body


## Resolved Comments
| Reviewer | File | Comment | Resolution |
|---|---|---|---|
| Codex/copilot-pull-request-reviewer/cursor | packages/core/src/lifecycle-manager.ts | Step-4 SCM throw below threshold falls through to defaults reporting agentDead=false | Added final agentDead fallback before defaults (step 5/6); also added agentDead guard to step-5 stuck check |
| Codex/copilot-pull-request-reviewer | packages/core/src/lifecycle-manager.ts | scmFailureCount never resets on successful detectPR, blocking no-PR kill path | Added finally block to step-3 detectPR to reset counter when detectPR succeeds |
| Copilot | packages/core/src/lifecycle-manager.ts | Final guard scmFailureCount===0 incorrectly gates no-PR kill when counter elevated from past errors | Removed && scmFailureCount===0 from final guard; step-3 finally handles counter hygiene |
| CI Lint | packages/core/src/__tests__/gh-headroom.test.ts | Unused import invalidateHeadroomCache (pre-existing on main) | Fixed in 0b6b2b57~1 |

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->
## Summary by CodeRabbit

* **Bug Fixes**
  * Fixed stale PR detection failures blocking subsequent operations
  * Improved agent state management and error recovery control flow for enhanced reliability
<!-- end of auto-generated comment: release notes by coderabbit.ai -->

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Touches session status determination/cleanup paths, so mistakes could prematurely kill sessions or suppress/trigger reactions incorrectly. Scope is limited to SCM failure counting and dead-agent/stuck fallthrough handling.
> 
> **Overview**
> Tightens SCM failure-counter handling in `determineStatus` by **resetting `scmFailureCount` after any successful `detectPR`** so earlier transient errors don’t keep blocking the no-PR kill path.
> 
> Adjusts dead-agent resolution so sessions are **killed immediately when the agent is dead and there’s no PR/SCM to wait on**, prevents the post
