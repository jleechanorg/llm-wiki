# PR #266: [agento] feat: centralize auto-merge enable/disable configuration (bd-n047)

**Repo:** jleechanorg/agent-orchestrator
**Merged:** 2026-03-29
**Author:** jleechan2015
**Stats:** +782/-272 in 10 files

## Summary
Add a centralized `defaults.autoMerge.enabled` kill-switch to the agent-orchestrator config that controls whether any `auto-merge` reaction can fire globally, with per-project override support.

## Background
Auto-merge was previously configured per-reaction (`reactions["approved-and-green"].action: "auto-merge"`), making it impossible to globally disable without touching every project config. This made emergency disablements error-prone and slow.

## Raw Body
## Summary

Add a centralized `defaults.autoMerge.enabled` kill-switch to the agent-orchestrator config that controls whether any `auto-merge` reaction can fire globally, with per-project override support.

## Background

Auto-merge was previously configured per-reaction (`reactions["approved-and-green"].action: "auto-merge"`), making it impossible to globally disable without touching every project config. This made emergency disablements error-prone and slow.

## Goals

- Add `defaults.autoMerge.enabled` — a single global kill-switch for all auto-merge reactions
- Add `projects[].autoMerge.enabled` — per-project override (can re-enable even if global is off)
- Maintain backward compatibility: absent config means auto-merge works as before

## Changes

| File | Change |
|------|--------|
| `packages/core/src/types.ts` | Add `AutoMergeConfig` interface; add to `DefaultPlugins` and `ProjectConfig` |
| `packages/core/src/config.ts` | Add `AutoMergeConfigSchema` Zod schema; wire into `DefaultPluginsSchema` and `ProjectConfigSchema` |
| `packages/core/src/lifecycle-manager.ts` | Add `isAutoMergeEnabled()` helper; guard auto-merge case and retry loop |
| `packages/core/src/__tests__/config-validation.test.ts` | 6 new config schema tests |
| `packages/core/src/__tests__/lifecycle-manager.test.ts` | 3 new lifecycle tests |
| `agent-orchestrator.yaml.example` | Document new config fields |
| `examples/auto-merge.yaml` | Update to use centralized config |

## Testing

- 6 config-validation tests: schema accepts all fields, rejects invalid values, per-project override
- 3 lifecycle-manager tests: global kill-switch, per-project disable, per-project re-enable
- All 1088 tests pass (4 pre-existing failures unrelated to this PR)

## Example config

```yaml
defaults:
  autoMerge:
    enabled: true      # set to false to globally disable
    waitSeconds: 300  # default GitHub auto-merge wait
    mergeMethod: squash

projects:
  my-app:
    autoMerge:
      enabled: false  # disable
