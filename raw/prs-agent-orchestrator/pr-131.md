# PR #131: [agento] fix(auton): remove deprecated poller check, add auto-merge config verification

**Repo:** jleechanorg/agent-orchestrator
**Merged:** 2026-03-24
**Author:** jleechan2015
**Stats:** +22/-3 in 1 files

## Summary
`/auton` was reporting two false positives that wasted diagnostic time:
1. **"Poller: STOPPED"** -- ao-pr-poller was removed in PR #352. Its absence is correct, not a bug.
2. **"No merge executor"** -- auto-merge is enabled (`approved-and-green: auto: true, action: auto-merge`). The skill was not checking the config, so it fabricated a gap that does not exist.

## Background
`/auton` was reporting two false positives that wasted diagnostic time:
1. **"Poller: STOPPED"** -- ao-pr-poller was removed in PR #352. Its absence is correct, not a bug.
2. **"No merge executor"** -- auto-merge is enabled (`approved-and-green: auto: true, action: auto-merge`). The skill was not checking the config, so it fabricated a gap that does not exist.

## Raw Body
## Background

`/auton` was reporting two false positives that wasted diagnostic time:
1. **"Poller: STOPPED"** -- ao-pr-poller was removed in PR #352. Its absence is correct, not a bug.
2. **"No merge executor"** -- auto-merge is enabled (`approved-and-green: auto: true, action: auto-merge`). The skill was not checking the config, so it fabricated a gap that does not exist.

## Goals

- Stop /auton from reporting the deprecated poller as a system issue
- Add explicit config verification for `approved-and-green` auto-merge so future diagnostics surface real gaps vs fabricated ones

## Tenets

- Skills must reflect current system state, not archaeology
- False positives in diagnostics are as harmful as false negatives -- they waste human attention

## High-level description of changes

In `.claude/skills/auton.md`:
- Removed `tail -50 /tmp/ao-pr-poller.log` from Step 3 (poller is deprecated)
- Added comment marking poller absence as expected/correct
- Added Step 3 #8: check `approved-and-green` config for `auto: true, action: auto-merge`
- Updated failure modes table: added poller-absent row (not a bug) and updated merge-executor row to check config

## Testing

Manual verification -- skill is documentation only, no runtime behavior.

## Low-level details

- **.claude/skills/auton.md**: Removed stale poller log check. Added auto-merge config check. Updated failure modes table to prevent false poller alarms.

## Resolved Comments

| Reviewer | File | Comment | Resolution |
|---|---|---|---|
| copilot-pull-request-reviewer | .claude/skills/auton.md | Step 8 YAML verification is brittle (grep -A3), misses action: at distance, treats absent auto: as gap when it defaults to true | Fixed in 8931a151 -- use yq when available, widen grep to +A10, clarify absent=defaults-true |
| chatgpt-codex-connector | .claude/skills/auton.md | grep matches commented template lines from agent-orchestrator.yaml.example | Fixed in 8931a151 -- added `grep -v '^\s*#'` to strip commented lines 
