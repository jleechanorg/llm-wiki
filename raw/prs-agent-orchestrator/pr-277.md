# PR #277: [agento] chore: add pre-push merge-conflict check and CR resolution workflow to CLAUDE.md

**Repo:** jleechanorg/agent-orchestrator
**Merged:** 2026-03-29
**Author:** jleechan2015
**Stats:** +26/-0 in 1 files

## Summary
PRs 263/266/267/273/276 are blocked at 5-6/7 green conditions due to three systemic failure patterns: (1) workers pushed to PRs with merge conflicts (dirty), (2) CR CHANGES_REQUESTED resolution without using the existing `extract-unresolved-comments.sh` and `cr-loop-guard.sh` scripts, (3) skeptic VERDICT: SKIPPED not guarded in the `approved-and-green` reaction.

## Background
PRs 263/266/267/273/276 are blocked at 5-6/7 green conditions due to three systemic failure patterns: (1) workers pushed to PRs with merge conflicts (dirty), (2) CR CHANGES_REQUESTED resolution without using the existing `extract-unresolved-comments.sh` and `cr-loop-guard.sh` scripts, (3) skeptic VERDICT: SKIPPED not guarded in the `approved-and-green` reaction.

## Raw Body
## Background
PRs 263/266/267/273/276 are blocked at 5-6/7 green conditions due to three systemic failure patterns: (1) workers pushed to PRs with merge conflicts (dirty), (2) CR CHANGES_REQUESTED resolution without using the existing `extract-unresolved-comments.sh` and `cr-loop-guard.sh` scripts, (3) skeptic VERDICT: SKIPPED not guarded in the `approved-and-green` reaction.

## Goals
- Prevent merge-conflict pushes at the harness level (pre-push check, not reactive reaction)
- Ensure CR CHANGES_REQUESTED resolution follows the established script-based workflow
- Guard against posting green signal when skeptic is SKIPPED or absent

## Tenets
- Prevention over reaction — check conditions before acting, not after
- Existing tools must be used — `extract-unresolved-comments.sh` and `cr-loop-guard.sh` already exist

## Changes
- **CLAUDE.md**: Added "Pre-push merge-conflict check" section requiring `gh pr view --json mergeableState` check before every push
- **CLAUDE.md**: Added "CR CHANGES_REQUESTED resolution workflow" section with script-based prioritization
- **CLAUDE.md**: Added "Skeptic SKIPPED — do not merge" section documenting the wait requirement
- **~/.openclaw/agent-orchestrator.yaml**: Updated `approved-and-green` reaction message to explicitly guard against posting green when skeptic VERDICT is SKIPPED or absent
- **Memory**: Saved `feedback_2026-03-29_pr-harness-gaps.md` to prevent recurrence

## Testing
- Pre-existing tests confirmed (wholesome tests use branch name fallback locally, config tests fail due to environment state)
- No new test required — this is an instruction/documentation change

## Low-level details
- `CLAUDE.md`: Added ~26 lines across 3 new sections after line 141
- `~/.openclaw/agent-orchestrator.yaml`: Appended ~2 lines to `approved-and-green` message

## Evidence
**Claim class**: unit

No code changes — this PR adds harness documentation only. Three new sections added to CLAUDE.md and one reaction message update in agent-orchestrat
