# PR #28: feat(core): implement Slack outbox with dead letter (bd-sw3)

**Repo:** jleechanorg/agent-orchestrator
**Merged:** 2026-03-20
**Author:** jleechan2015
**Stats:** +456/-0 in 4 files

## Summary
(none)

## Raw Body
Part of bd-pdt epic. Outbox queue + dead letter for reliable Slack notifications. 13 TDD tests.

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Adds new file-backed queueing/retry logic that affects how Slack notifications are persisted and processed, including timeout and concurrency/in-flight handling. Main risk is around correctness under concurrent `processNext` calls and file I/O edge cases (JSONL parsing, atomic rewrites).
> 
> **Overview**
> Adds a new `SlackOutbox` implementation that persists Slack send requests to a JSONL outbox, processes entries with *priority ordering* and *timeout-aware retries*, and moves exhausted items to a dead-letter file.
> 
> Includes safeguards for concurrent processing by marking entries `in-flight` durably before sending and by using atomic rewrites to avoid torn outbox state. Exposes the new API via `packages/core/src/index.ts`, adds a full Vitest suite for enqueue/process/drain/dead-letter behaviors, and updates `pnpm-lock.yaml` to include `@composio/ao-plugin-agent-cursor` in `packages/web`.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit e9a1f7d9994485566c5d62a04975c0154be50a05. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->
## Summary by CodeRabbit

* **New Features**
  * Persistent Slack message outbox with priority ordering (high/normal/low), automatic retries, timeout handling, and dead-letter storage; APIs to enqueue, process, drain, and inspect queue lengths.
* **Tests**
  * Added comprehensive tests covering enqueueing, priority ordering, retry/failure flows, dead-letter behavior, draining, and length metrics.
* **Chores**
  * Added new issue records describing orchestration and automation capabilities.
<!-- end of auto-generated comment: release notes by coderabbit.ai -->
