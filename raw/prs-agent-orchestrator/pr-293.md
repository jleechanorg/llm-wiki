# PR #293: [agento] docs: refresh skeptic architecture with current component map

**Repo:** jleechanorg/agent-orchestrator
**Merged:** 2026-03-29
**Author:** jleechan2015
**Stats:** +17/-6 in 1 files

## Summary
Refreshed skeptic architecture documentation to reflect current implementation:
- Added skeptic-cron-local.ts, skeptic-reviewer.ts, fork-claim-verification.ts, llm-eval.ts as named components
- Documented skeptic-gate chain (fires on PR open/sync) and skeptic-cron chain (fires every 30min)
- Added self-referential skeptic PR guidance (expected to fail gates 1/3/5 during development)
- Added hook stdin testing step to Skeptic Change Verification checklist
- Added hook stdin piping red flag

## Raw Body
## Summary
Refreshed skeptic architecture documentation to reflect current implementation:
- Added skeptic-cron-local.ts, skeptic-reviewer.ts, fork-claim-verification.ts, llm-eval.ts as named components
- Documented skeptic-gate chain (fires on PR open/sync) and skeptic-cron chain (fires every 30min)
- Added self-referential skeptic PR guidance (expected to fail gates 1/3/5 during development)
- Added hook stdin testing step to Skeptic Change Verification checklist
- Added hook stdin piping red flag

## Testing
Documentation-only change — no code, tests, or CI impact.

## Motivation
The skeptic architecture docs were stale — they omitted key components (skeptic-cron-local.ts, skeptic-reviewer.ts, fork-claim-verification.ts) that were added in recent PRs. The hook stdin bug (bd-upxh) required explicit documentation to prevent recurrence.


## Evidence

**Claim class**: unit (documentation-only change — no runtime code)

**Verdict**: PASS — docs refresh with correct component map, addressed all 3 Copilot review comments.

**Docs diff — key changes in CLAUDE.md skeptic section:**

```
- | `skeptic-cron.yml` | Cron: posts `SKEPTIC_CRON_TRIGGER` for eligible PRs, checks 7th gate + merges | GHA (no API keys) |
+ | `skeptic-cron.yml` | Cron: posts `SKEPTIC_CRON_TRIGGER` for eligible PRs, checks gate 6 (evidence) + gate 7 (skeptic) + merges | GHA (no API keys) |

- Note: `SKEPTIC_BOT_AUTHOR` defaults differ by design — `skeptic-cron.yml` uses `'jleechan2015'` (lifecycle-worker posts as local user), `skeptic-gate.yml` polling uses `'github-actions[bot]'` (GHA runner posts as GHA bot); the jq filter accepts both to cover both paths.
+ Note: `SKEPTIC_BOT_AUTHOR` defaults differ by layer — both `skeptic-gate.yml` and `skeptic-cron.yml` GHA YAMLs default to `'github-actions[bot]'` via `${{ vars.SKEPTIC_BOT_AUTHOR || 'github-actions[bot]' }}`; the CLI (`ao skeptic verify`) uses `GH_SKEPTIC_BOT_AUTHOR` with default `'jleechan2015'` (local user). The jq filter in `skeptic-gate.yml`
