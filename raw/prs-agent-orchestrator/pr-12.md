# PR #12: test(integration): add cursor and gemini agent integration tests

**Repo:** jleechanorg/agent-orchestrator
**Merged:** 2026-03-16
**Author:** jleechan2015
**Stats:** +827/-514 in 12 files

## Summary
Adds real integration tests for cursor-agent and gemini CLI plugins.

**Proved green locally (8/8 pass):**
- agent-cursor: 4/4 (isProcessRunning alive/exited, file created, fibonacci correct)
- agent-gemini: 4/4 (same assertions)

**Key details:**
- cursor: uses `--print --yolo`; skips if not authenticated
- gemini: passes `GEMINI_API_KEY` explicitly via tmux env
- Both auto-skip when prerequisites are missing

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -

## Raw Body
## Summary

Adds real integration tests for cursor-agent and gemini CLI plugins.

**Proved green locally (8/8 pass):**
- agent-cursor: 4/4 (isProcessRunning alive/exited, file created, fibonacci correct)
- agent-gemini: 4/4 (same assertions)

**Key details:**
- cursor: uses `--print --yolo`; skips if not authenticated
- gemini: passes `GEMINI_API_KEY` explicitly via tmux env
- Both auto-skip when prerequisites are missing

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Medium risk because `agent-base` shared logic is extended (session-path abstraction, system-prompt env support, hook command handling, and `ps` timeout) and will affect multiple agent plugins at runtime, not just tests.
> 
> **Overview**
> Adds real tmux-backed integration tests for the Cursor and Gemini agent plugins that run only when required binaries/auth are present, and verify an agent can write and execute a generated `fibonacci.py`.
> 
> Extends the shared `@composio/ao-plugin-agent-base` to support non-`.jsonl` session formats and custom session directories (used by Gemini’s `~/.gemini/tmp/<sha256>/chats` + `.json`), optional system-prompt delivery via an env var (writing inline prompts to a temp file), safer hook settings merging/preservation of `AO_DATA_DIR`, and a longer cached `ps` timeout.
> 
> Updates the Gemini plugin to use SHA-256 workspace hashing for session paths and `GEMINI_SYSTEM_MD`, adjusts Cursor/Gemini cost handling (no fallback pricing), refreshes Gemini unit tests for JSON sessions, and updates design docs/deps to reflect the new behavior and added plugins in integration-tests.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 2d1ef86218ffcf08735254f5af5cfdfc1e761d4b. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by 
