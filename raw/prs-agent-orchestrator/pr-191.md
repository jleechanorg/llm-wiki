# PR #191: [agento] fix(web): show EmptyState when all sessions are done

**Repo:** jleechanorg/agent-orchestrator
**Merged:** 2026-03-26
**Author:** jleechan2015
**Stats:** +136/-0 in 4 files

## Summary
Cherry-pick intent from ComposioHQ upstream commit `0fe63405`: show an EmptyState message when all sessions are in "done" status, instead of rendering a blank kanban board.

Our fork's `Dashboard.tsx` already had the core `hasKanbanSessions` logic (`KANBAN_LEVELS.some(...)`) — the missing piece was the `EmptyState` rendering call.

## Background
Cherry-pick intent from ComposioHQ upstream commit `0fe63405`: show an EmptyState message when all sessions are in "done" status, instead of rendering a blank kanban board.

Our fork's `Dashboard.tsx` already had the core `hasKanbanSessions` logic (`KANBAN_LEVELS.some(...)`) — the missing piece was the `EmptyState` rendering call.

## Raw Body
## Background

Cherry-pick intent from ComposioHQ upstream commit `0fe63405`: show an EmptyState message when all sessions are in "done" status, instead of rendering a blank kanban board.

Our fork's `Dashboard.tsx` already had the core `hasKanbanSessions` logic (`KANBAN_LEVELS.some(...)`) — the missing piece was the `EmptyState` rendering call.

## Goals

- Show an EmptyState component (terminal icon + message) when the kanban columns are all empty but done sessions still exist

## Tenets

- Minimal scope: only the two web UI files touched
- Mirror upstream's component shape without adopting the full kanban redesign suite

## High-level description

- Added `packages/web/src/components/Skeleton.tsx` — exports `EmptyState` with a terminal icon and configurable message, matching upstream's implementation
- Updated `Dashboard.tsx` to import `EmptyState` and render it between the kanban and the done-section when `!hasKanbanSessions && grouped.done.length > 0`
- The empty state message reads: "All sessions are done — nothing left in the kanban."

## Testing

- `pnpm --filter @jleechanorg/ao-web typecheck` passes (no errors)

## Low-level details

- **packages/web/src/components/Skeleton.tsx** (new): Mirrors upstream's `EmptyState` component — accepts optional `message` prop, renders a terminal SVG icon and a `<p>` with the message text. Accepts a default message about `ao start` when no custom message is passed.
- **packages/web/src/components/Dashboard.tsx**: Added `import { EmptyState } from "./Skeleton"` and a conditional render block between the kanban columns and the done sessions grid.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Low risk UI-only change that adds a new empty-state render path when the kanban has no active columns; minimal logic and covered by unit tests.
> 
> **Overview**
> Prevents the dashboard kanban from rendering as blank when a single project has *only* `done` sessio
