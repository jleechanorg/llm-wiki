# PR #157: chore: remove PreToolUse merge policy enforcement from metadata-updater hook

**Repo:** jleechanorg/agent-orchestrator
**Merged:** 2026-03-24
**Author:** jleechan2015
**Stats:** +8/-15 in 1 files

## Summary
- Removes PreToolUse `gh pr merge` enforcement block from `.claude/metadata-updater.sh`
- Hook now operates exclusively as a PostToolUse metadata updater (tracks `gh pr create`, `git checkout -b`, `gh pr merge`)
- The PreToolUse deny was redundant with `agentRules` ("NEVER MERGE") and caused confusion with bypassPermissions mode

## Test Plan
- [x] Hook still updates session metadata on `gh pr create`, `git checkout -b`, `git switch -c`
- [x] Hook still updates `status=merged` on `gh pr merge` in PostToolUse

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Removes an in-hook guardrail that previously denied `gh pr merge` during `PreToolUse`, so accidental/unauthorized merges now rely on external policy/config. Metadata update logic is otherwise small, but this

## Raw Body
## Summary
- Removes PreToolUse `gh pr merge` enforcement block from `.claude/metadata-updater.sh`
- Hook now operates exclusively as a PostToolUse metadata updater (tracks `gh pr create`, `git checkout -b`, `gh pr merge`)
- The PreToolUse deny was redundant with `agentRules` ("NEVER MERGE") and caused confusion with bypassPermissions mode

## Test plan
- [x] Hook still updates session metadata on `gh pr create`, `git checkout -b`, `git switch -c`
- [x] Hook still updates `status=merged` on `gh pr merge` in PostToolUse

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Removes an in-hook guardrail that previously denied `gh pr merge` during `PreToolUse`, so accidental/unauthorized merges now rely on external policy/config. Metadata update logic is otherwise small, but this touches workflow-safety behavior around merging.
> 
> **Overview**
> Removes the `PreToolUse` policy enforcement that denied agent-triggered `gh pr merge`, making `.claude/metadata-updater.sh` operate purely as a *PostToolUse* metadata updater.
> 
> Adds a fallback to read the hook event from `AO_HOOK_EVENT_NAME` when the JSON payload omits it, and simplifies merge tracking so any successful `gh pr merge` in `PostToolUse` updates session metadata to `status=merged` (no `AO_ALLOW_GH_PR_MERGE` gate).
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit fbfc247bd10de850759d8c7cfe6c48a17128dc50. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->

## Summary by CodeRabbit

* **Refactor**
  * Streamlined pull request merge handling by removing redundant validation logic and simplifying the merge metadata update process.

<!-- end of auto-generated comment: release notes by coderabbit.ai -->
