# PR #142: [agento] fix(metadata-updater): add hook_event guard to merge block

**Repo:** jleechanorg/agent-orchestrator
**Merged:** 2026-03-24
**Author:** jleechan2015
**Stats:** +11/-4 in 1 files

## Summary
Extract `hook_event_name` from the PostToolUse hook JSON to distinguish PreToolUse vs PostToolUse invocations. Re-introduce gh pr merge policy enforcement in PreToolUse (block when `AO_ALLOW_GH_PR_MERGE != 1`), allow PostToolUse to fall through for metadata updates, and only mark `status=merged` in PostToolUse.

## Test Plan
- [x] Local bash: verify PreToolUse blocks merge when AO_ALLOW_GH_PR_MERGE not set
- [x] Local bash: verify PostToolUse proceeds and updates metadata when AO_ALLOW_GH_PR_MERGE=1
- [x] All 811 core tests pass

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Raw Body
## Summary
Extract `hook_event_name` from the PostToolUse hook JSON to distinguish PreToolUse vs PostToolUse invocations. Re-introduce gh pr merge policy enforcement in PreToolUse (block when `AO_ALLOW_GH_PR_MERGE != 1`), allow PostToolUse to fall through for metadata updates, and only mark `status=merged` in PostToolUse.

## Changes
- Add `hook_event_name` extraction from hook JSON (both jq and grep fallback paths)
- Add `AO_HOOK_EVENT_NAME` env var fallback
- Update header comment to reflect PreToolUse/PostToolUse dual-role
- Block `gh pr merge` in PreToolUse unless `AO_ALLOW_GH_PR_MERGE=1`
- `status=merged` only set in PostToolUse

## Test plan
- [x] Local bash: verify PreToolUse blocks merge when AO_ALLOW_GH_PR_MERGE not set
- [x] Local bash: verify PostToolUse proceeds and updates metadata when AO_ALLOW_GH_PR_MERGE=1
- [x] All 811 core tests pass

🤖 Generated with [Claude Code](https://claude.com/claude-code)
