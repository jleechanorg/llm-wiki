# PR #348: [agento] fix(skeptic): deterministic evidence authenticity check + fix evidenceLabel display

**Repo:** jleechanorg/agent-orchestrator
**Merged:** 2026-04-02
**Author:** jleechan2015
**Stats:** +86/-2 in 2 files

## Summary
bd-7x6y: skeptic-prompt rule 10 evaluates evidence authenticity for every PR, but the 7-green status line showed '(see Rule 10)' — hiding whether the check actually passed or failed when evidenceRequired=false.

## Background
bd-7x6y: skeptic-prompt rule 10 evaluates evidence authenticity for every PR, but the 7-green status line showed '(see Rule 10)' — hiding whether the check actually passed or failed when evidenceRequired=false.

## Raw Body
## Background

bd-7x6y: skeptic-prompt rule 10 evaluates evidence authenticity for every PR, but the 7-green status line showed '(see Rule 10)' — hiding whether the check actually passed or failed when evidenceRequired=false.

## Goals

- Deterministic (no-LLM) check for fabricated evidence patterns in PR body
- Status line shows real PASS/FAIL instead of '(see Rule 10)'

## Tenets

- Minimal scope: only changes prompt.ts
- Deterministic check: fast, no LLM call needed, has access to pr.body
- Backwards compatible: rule 10 LLM instruction unchanged

## High-level description

Added FABRICATED_PATTERNS regex array and isEvidenceAuthentic() helper in prompt.ts. When evidenceRequired=false, the 7-green status line now shows PASS (Rule 10) if the PR body is clean, or FAIL (Rule 10) if fabricated patterns are detected. The Rule 10 LLM instruction was already active — this fixes the display only.

Patterns detected: simulated, example.com, <screenshot>, <value>, TODO, TBD, placeholder.

## Evidence

**Claim class**: unit-test-coverage

**Verdict**: PASS — 34 skeptic tests pass (was 20 before fix), 17 mergeGate tests pass, build succeeds.

```
$ pnpm --filter @jleechanorg/ao-cli test -- skeptic-structured
 RUN  v3.2.4 .../packages/cli
  ✓ skeptic-structured-output.test.ts (34 tests) 4ms
  Test Files  1 passed (1)
       Tests  34 passed (34)

$ pnpm --filter @jleechanorg/ao-cli test -- mergeGate
  ✓ mergeGate.debug.test.ts (7 tests) 3ms
  ✓ mergeGate.test.ts (10 tests) 3ms
  Test Files  2 passed (2)
       Tests  17 passed (17)

Build: pnpm run build succeeds
```

## Low-level details

- packages/cli/src/commands/skeptic/prompt.ts: Added FABRICATED_PATTERNS, isEvidenceAuthentic(body), updated evidenceLabel from '(see Rule 10)' to 'PASS (Rule 10)' / 'FAIL (Rule 10)'
- packages/cli/src/__tests__/skeptic/skeptic-structured-output.test.ts: Added 14 unit tests for isEvidenceAuthentic
<!-- COPILOT_TRACKING_START -->
## Resolved Comments
| Reviewer | File | Comment | Resolution |
