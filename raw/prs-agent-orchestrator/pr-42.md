# PR #42: feat(agent-base): remake PR #38 from Composio PR #538 baseline

**Repo:** jleechanorg/agent-orchestrator
**Merged:** 2026-03-21
**Author:** jleechan2015
**Stats:** +565/-173 in 35 files

## Summary
Recreates PR #38 on a fresh branch from main, using ComposioHQ/agent-orchestrator#538 as the baseline and adapting to fork package scope/config.

## Raw Body
## Summary
Recreates PR #38 on a fresh branch from main, using ComposioHQ/agent-orchestrator#538 as the baseline and adapting to fork package scope/config.

## Context
- Intended replacement for: #38
- Baseline reference: https://github.com/ComposioHQ/agent-orchestrator/pull/538

## Notes
- Installed dependencies and validated workspace install succeeds.
- One package-local test command currently fails on package entry resolution in packages/plugins/agent-base/src/index.test.ts after scope normalization; follow-up needed in this PR thread.

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Medium risk because it changes GitHub SCM PR state/mergeability and review-decision handling under rate limiting, which can affect automation decisions. Other changes are mostly CI/security workflow tweaks and test/lint hardening.
> 
> **Overview**
> **CI/security plumbing:** Updates GitHub Actions filters to the `@jleechanorg/*` scope, replaces `gitleaks-action` with a pinned CLI install+scan, makes `dependency-review` non-blocking, and expands `.gitleaks.toml` allowlists.
> 
> **Tooling/test hardening:** Adjusts ESLint to ignore `dist-server`, tune `consistent-type-imports`, and treat plugin/core/CLI code as Node (globals). Makes CLI/core tests more hermetic (PATH isolation, ignore `AO_CONFIG_PATH`) and aligns `@jleechanorg/ao-core` `typecheck` with the build tsconfig.
> 
> **Behavioral changes:**
> - `scm-github` now retries on rate limits and, for `gh pr view --json …`, falls back to `gh api` REST calls (including synthesizing `reviewDecision`/`reviews`), normalizes REST-vs-GraphQL merge payload shapes, and treats REST `{state:"closed", merged:true}` as merged.
> - `agent-claude-code` hook setup is now workspace-relative, idempotent (only writes when content changes), and refuses to write into a symlinked `.claude` directory.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 17f22b23cbc2ef21769e17bac7bce83cefc630e0. This will upd
