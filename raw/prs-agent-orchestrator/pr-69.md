# PR #69: ci: switch workflows to self-hosted runner labels

**Repo:** jleechanorg/agent-orchestrator
**Merged:** 2026-03-21
**Author:** jleechan2015
**Stats:** +10/-10 in 5 files

## Summary
- switched all requested workflows from `ubuntu-latest` to self-hosted runner labels
- matched the label expression pattern used in `jleechanorg/worldarchitect.ai`

## Raw Body
## Summary
- switched all requested workflows from `ubuntu-latest` to self-hosted runner labels
- matched the label expression pattern used in `jleechanorg/worldarchitect.ai`

## Updated workflows
- `.github/workflows/ci.yml`
- `.github/workflows/security.yml`
- `.github/workflows/integration-tests.yml`
- `.github/workflows/onboarding-test.yml`
- `.github/workflows/release.yml`

## Runner format used
`runs-on: ${{ fromJson(vars.SELF_HOSTED_RUNNER_LABELS || '["self-hosted","self-hosted-mikey"]') }}`

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Changes CI execution environment selection across multiple workflows, which can cause failures if self-hosted runner labels/availability differ from `ubuntu-latest`. PRs are kept on GitHub-hosted runners to reduce exposure, but pushes/releases now depend on runner configuration via repo/org variables.
> 
> **Overview**
> Updates GitHub Actions workflows to **conditionally use self-hosted runners** via `vars.SELF_HOSTED_RUNNER_LABELS` (defaulting to `['self-hosted','self-hosted-mikey']`).
> 
> For `ci.yml`, `integration-tests.yml`, `onboarding-test.yml`, and `security.yml`, *pull requests still run on `ubuntu-latest`* while other triggers switch to the configured self-hosted labels; `release.yml` always uses the self-hosted labels.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit a4605f3cd4780aed9e661c87e366574bf166675f. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->

## Summary by CodeRabbit

* **Chores**
  * Updated CI/CD pipeline runner configuration across all workflows (CI, integration tests, security scans, release, and onboarding) to use conditional logic: pull requests continue to run on standard GitHub-hosted runners, while other events utilize self-hosted runners with customizable label configuration f
