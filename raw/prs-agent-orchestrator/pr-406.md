# PR #406: [agento] fix: stabilize backfill warnings and PR 406 CI

**Repo:** jleechanorg/agent-orchestrator
**Merged:** 2026-04-11
**Author:** jleechan2015
**Stats:** +454/-31 in 4 files

## Summary
(none)

## Raw Body
### Background
This PR started as a lifecycle-manager fix for `backfillAllPRs === false`: reviewers flagged that the opt-out warning path was still calling SCM on every poll, and the `backfillAllPRs` JSDoc had drifted from the validated schema. While rescuing the PR to green, the branch also picked up CI/test drift that had to be corrected on the PR head itself: updated generated config expectations, strict-MCP command assertions, stale-worktree fetch self-heal behavior, and a cross-platform stale-worktree test fixture in `scm-github`.

### Goals
1. Throttle the backfill opt-out warning path so disabled projects do not spam SCM APIs or warn every poll.
2. Keep `backfillAllPRs` documentation aligned with the accepted config shape.
3. Preserve literal `{{context}}` replacement content in send-to-agent messages.
4. Make the PR's current head pass the full test matrix on GitHub and locally.

### Tenets
- Keep the lifecycle-manager fix scoped and regression-tested.
- Prefer minimal rescue changes that explain and stabilize real CI failures.
- Match PR metadata to the actual diff on the branch.
- Preserve existing behavior unless CI or review feedback showed a concrete break.

### High-level description of changes
The lifecycle-manager opt-out warning path now runs through a throttled helper that records a warn-level observation without hammering SCM, and the `backfillAllPRs` JSDoc now matches the boolean-or-undefined schema. The send-to-agent `{{context}}` interpolation keeps callback-based replacement semantics so literal dollar-sign sequences are preserved.

To get the PR green, the branch also absorbed current-head CI rescue fixes: config-generator expectations were updated for the codex default, Claude strict-MCP command assertions were aligned, stale checked-out worktrees now self-heal correctly in both `workspace-worktree` and `scm-github`, the CLI spawn test now resets `sessionManager.list()` deterministically, and the stale-worktree SCM test now derives its fixtu
