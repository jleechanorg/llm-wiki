# PR #405: [agento] chore(novel): ao-spawn daily runner + video evidence roadmap doc

**Repo:** jleechanorg/agent-orchestrator
**Merged:** 2026-04-11
**Author:** jleechan2015
**Stats:** +1032/-234 in 13 files

## Summary
This PR refactors the novel daily runner so the launchd path delegates real generation work to an AO tmux worker instead of embedding inline Node/OpenClaw logic in the plist-safe script. It also records the follow-on video evidence roadmap and keeps the supporting tests/config expectations aligned with current fork behavior.

## Background
This PR refactors the novel daily runner so the launchd path delegates real generation work to an AO tmux worker instead of embedding inline Node/OpenClaw logic in the plist-safe script. It also records the follow-on video evidence roadmap and keeps the supporting tests/config expectations aligned with current fork behavior.

## Raw Body
## Background
This PR refactors the novel daily runner so the launchd path delegates real generation work to an AO tmux worker instead of embedding inline Node/OpenClaw logic in the plist-safe script. It also records the follow-on video evidence roadmap and keeps the supporting tests/config expectations aligned with current fork behavior.

## Goals
1. Make `scripts/novel/run-daily.sh` a thin, launchd-safe AO entrypoint.
2. Preserve correctness with targeted shell and TypeScript test updates.
3. Capture the video-evidence follow-up plan in repo-visible documentation.

## Tenets
- Prefer AO worker dispatch over bespoke inline automation.
- Keep launchd-facing shell simple and fail closed on missing prerequisites.
- Fix tests and metadata so the PR describes the real branch contents.

## High-level description of changes
The daily runner now syncs `main`, gathers recent repo/CI context, spawns an AO tmux worker, polls for session completion, and validates that both daily output files were actually written before succeeding. Supporting changes tighten stale-worktree cleanup handling, trim SCM REST fallback output safely, align generated config and claude-code agent expectations, pin `axios` to the patched release, and add a roadmap for captioned tmux/UI video evidence. This branch also now includes the generated PR design doc for review context.

## Evidence
**Claim class**: shell + TypeScript implementation with automated tests (non-E2E).

- **Verdict**: PASS.
- **Terminal media**: N/A - no UI surface changed; verification is local automated test execution.
- **UI media**: N/A - no UI changes.
- **Design doc**: `docs/design/pr-designs/pr-405.md` and `docs/design/pr-designs/pr-405.html`.

## Testing
- `pnpm test`
- `bash tests/unit/test-run-daily.sh`

## Low-level details (top changed files)
- **`package.json`**: adds the `axios` override and ensures the shell regression test runs in the standard test flow.
- **`packages/cli/__tests__/commands/spawn.test.ts`**: keeps sp
