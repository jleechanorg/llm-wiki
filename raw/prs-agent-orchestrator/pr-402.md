# PR #402: [agento] chore: close bd-8y9 lifecycle API investigation beads (jc-investigate-7927)

**Repo:** jleechanorg/agent-orchestrator
**Merged:** 2026-04-07
**Author:** jleechan2015
**Stats:** +15/-15 in 1 files

## Summary
Session **jc-investigate-7927** completed the **bd-8y9** investigation (high per-cycle GitHub API usage in the fork lifecycle-worker). The optimizations described in `roadmap/gh-api-reduction-validation.md` were verified in the codebase; child beads that were still marked open were closed with pointers to the implementing files.

## Raw Body
## Summary

Session **jc-investigate-7927** completed the **bd-8y9** investigation (high per-cycle GitHub API usage in the fork lifecycle-worker). The optimizations described in `roadmap/gh-api-reduction-validation.md` were verified in the codebase; child beads that were still marked open were closed with pointers to the implementing files.

## Beads closed

| Bead | Close reason (code verified) |
|------|------------------------------|
| bd-8y9 | Investigation complete; follow-up **bd-sm7** remains (fallback path query merge) |
| bd-wg5 | `determineStatus` — CI failing returns before mergeability; CI pending skips mergeability (`lifecycle-manager.ts`) |
| bd-91z | `DEFAULT_TTL_MS=60000` in `gh-cache.ts` |
| bd-4nz | `scm.skipAutomatedCommentPolling` in `review-backlog.ts` |
| bd-yjo | `REVIEW_BACKLOG_INTERVAL=3` in `review-backlog.ts` |
| bd-att | `getBatchPRStatus` + lifecycle batch path in `scm-github` |

## Tests

- `pnpm build` and `pnpm test` (with `AO_WHOLESOME_PR_TITLE` set for local wholesome check) — pass.

Closes: bd-8y9 (beads)

## Evidence

**Claim class:** LOW (beads metadata only — no product code paths).

| Claim | Result | Proof |
|-------|--------|-------|
| bd-8y9 family beads closed with accurate close reasons | Confirmed | `.beads/issues.jsonl` diff; `br show bd-8y9` shows closed |
| Repo tests pass for this change | Confirmed | Local: `pnpm build` then `AO_WHOLESOME_PR_TITLE` set for wholesome + `pnpm test` — exit 0 |

**N/A:** UI / video / tmux — not applicable (tracker-only PR).

**Verdict:** PASS
**Claim floor override**: Beads-only PR touching `.beads/issues.jsonl` (JSONL metadata); claim class LOW — no product runtime paths. Tracker closure evidence is the issues file diff + local `pnpm` runs in Tests.



