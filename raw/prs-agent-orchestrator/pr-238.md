# PR #238: [agento] feat: cap send-to-agent retries at 3 + document idempotency (bd-5nxx, bd-5nt5)

**Repo:** jleechanorg/agent-orchestrator
**Merged:** 2026-03-28
**Author:** jleechan2015
**Stats:** +824/-45 in 12 files

## Summary
Updated at 2026-03-28T12:20:18Z

## Background
Updated at 2026-03-28T12:20:18Z

## Raw Body
## Background

Updated at 2026-03-28T12:20:18Z

## Evidence

**Claim class**: unit-test-coverage

**Verdict:** PASS (unit tests only)

### Test files modified
- `packages/core/src/__tests__/lifecycle-manager.test.ts` — +267/-2: retry cap at 3, persistent failure escalation, periodic skip, policy unit test
- `packages/core/src/__tests__/plugin-integration.test.ts` — +156/-1: 3-retry integration test, reaction action tests
- `packages/core/src/__tests__/wholesome.test.ts` — +20/-0: wholesome regression test



**Terminal output:** unit test pass

```
LifecycleManager _testing API tests
  ✓ caps send-to-agent retries at 3
  ✓ escalates after 3 failed retries
  ✓ periodic retries skip cap
  ✓ persistent failure triggers escalateAfter

Integration tests
  ✓ send-to-agent retried 3x on persistent failure
```

**Note:** No E2E or pipeline tests required — retry cap is exercised via unit + integration tests; no external system interactions involved.

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Touches `lifecycle-manager` reaction execution and retry/escalation behavior, which can affect automated PR handling; mitigated by extensive new unit/integration coverage and policy isolated in a fork-specific module.
> 
> **Overview**
> Adds a fork-specific retry policy that **defaults `send-to-agent` reactions to max 3 attempts** (while leaving other actions uncapped) and threads a new `isPeriodic` flag through `executeReaction` so periodic/stuck nudges don’t consume the cap.
> 
> Updates `lifecycle-manager` to use the policy, logs resolved action + periodicity, and exposes a `_testing` hook for directly invoking `executeReaction` in tests; `review-backlog` is updated to pass the new parameter. Expands tests to cover the retry cap, escalation after persistent failures, stable-status non-refiring, and a `respawn-for-review` path that uses `_testing` to avoid full lifecycle machinery.
> 
> Also updates docs/guidelines about reaction idempotency, adds generated PR design
