# PR #317: [agento] docs(roadmap): packages code audit — cleanup, plugins, Composio alignment

**Repo:** jleechanorg/agent-orchestrator
**Merged:** 2026-03-31
**Author:** jleechan2015
**Stats:** +57/-0 in 1 files

## Summary
Adds a static code audit of all `packages/*` (ao, cli, core, integration-tests, mobile, plugins, web) covering:

1. **Cleanup** — oversized core files, CLI/plugin map drift, misleading web server test titles, skeptic `--model gemini` vs implementation.
2. **Plugin boundaries** — Composio SDK split (`composio-core` vs `@composio/core`), prose-polish placement, integration-tests boundaries.
3. **Composio / upstream collision** — metadata URLs still pointing at ComposioHQ, mobile bundle IDs, absenc

## Raw Body
## Summary

Adds a static code audit of all `packages/*` (ao, cli, core, integration-tests, mobile, plugins, web) covering:

1. **Cleanup** — oversized core files, CLI/plugin map drift, misleading web server test titles, skeptic `--model gemini` vs implementation.
2. **Plugin boundaries** — Composio SDK split (`composio-core` vs `@composio/core`), prose-polish placement, integration-tests boundaries.
3. **Composio / upstream collision** — metadata URLs still pointing at ComposioHQ, mobile bundle IDs, absence of monkey-patching, web guard against `@composio/ao-core` imports.

## Deliverable

- `roadmap/packages-code-audit-2026-03-30.md` — table of findings with file paths, line references, category (a/b/c), severity, and recommended follow-ups.

## Testing

- Documentation-only change. Local `pnpm test` hit environment-specific failures (`~/.agent-orchestrator.yaml` present; wholesome PR title assertion uses branch name outside CI). CI should validate.

## Follow-up (not in this PR)

Address HIGH items: Gemini parity in `packages/cli/src/lib/plugins.ts` + `detect-agent.ts`; unify Composio SDK usage across notifier-composio and tracker-linear.

Made with [Cursor](https://cursor.com)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Documentation-only change adding an audit report; no runtime code or dependencies are modified.
> 
> **Overview**
> Adds `roadmap/packages-code-audit-2026-03-30.md`, a static audit report of `packages/*` highlighting **cleanup/consistency risks** (e.g., CLI agent/SCM map drift, Gemini parity gaps, oversized core files), **plugin-boundary concerns** (Composio SDK naming/loading mismatches), and **upstream/fork collision items** (plugin metadata URLs, mobile bundle IDs). The document includes prioritized findings with recommendations and explicit “not found” checks (e.g., no widespread monkey-patching, no `@composio/ao-core` imports in app code).
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 0
