# PR #182: [agento] feat(remotion): The Daily Lives of Workers — Remotion video

**Repo:** jleechanorg/agent-orchestrator
**Merged:** 2026-03-25
**Author:** jleechan2015
**Stats:** +3465/-0 in 8 files

## Summary
AO workers produce serialized fiction in `novel/the-daily-lives-of-workers.md`. This PR adds a Remotion (React-based video renderer) project that interprets the Day 1 entry as a ~55-second animated video — suitable for submission to the [Remotion Gallery](https://www.remotion.dev/prompts/submit).

## Background
AO workers produce serialized fiction in `novel/the-daily-lives-of-workers.md`. This PR adds a Remotion (React-based video renderer) project that interprets the Day 1 entry as a ~55-second animated video — suitable for submission to the [Remotion Gallery](https://www.remotion.dev/prompts/submit).

## Raw Body
## Background

AO workers produce serialized fiction in `novel/the-daily-lives-of-workers.md`. This PR adds a Remotion (React-based video renderer) project that interprets the Day 1 entry as a ~55-second animated video — suitable for submission to the [Remotion Gallery](https://www.remotion.dev/prompts/submit).

## Goals

- [ ] Provide a visual rendering of the serialized fiction narrative
- [ ] Produce a concrete artifact suitable for Remotion gallery submission
- [ ] Establish a reusable Remotion scaffold for future AO video projects

## High-level description of changes

New `remotion/` subdirectory containing a complete Remotion v4 project:

- `src/Root.tsx` — 8-scene storyboard (Title Card, Spawn, Designation/launchd, Six Green Conditions, Rate Limits, 3AM, Collaboration Bus, Coda)
- `src/index.ts` — Remotion root entry
- `remotion.config.ts` — Output format config
- `package.json` / `tsconfig.json` — project scaffolding
- `README.md` — scene map + gallery submission instructions

**Total runtime: ~55 seconds at 30fps**

## Testing

- `npx tsc --noEmit` — clean (0 errors)
- `npm install` — deps installed, 4 audit warnings (acceptable)
- Render pending: `npm run build` produces `out/daily-lives-of-workers.mp4`

## Rendering the video

```bash
cd remotion
npm install        # already done
npm start          # preview in browser
npm run build      # render out/daily-lives-of-workers.mp4
```

## Remotion Gallery Submission

Once rendered, submit at https://www.remotion.dev/prompts/submit:

| Field | Value |
|-------|-------|
| Title | The Daily Lives of Workers |
| Video | `out/daily-lives-of-workers.mp4` |
| Prompts | Full `src/Root.tsx` source + `remotion/README.md` as context |
| Tool | Claude Code |
| Model | Opus 4.6 |
| Credit | jleechan (GitHub) |

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Low risk because changes are isolated to a new `remotion/` subproject; main risk is repositor
