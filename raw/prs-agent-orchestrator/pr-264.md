# PR #264: [agento] fix(cli): simplify metadata-updater hook guardrails

**Repo:** jleechanorg/agent-orchestrator
**Merged:** 2026-03-28
**Author:** jleechan2015
**Stats:** +22/-33 in 2 files

## Summary
Metadata-updater hook guardrails and skeptic-gate workflow fixes.

## Background
AO-managed hooks were executing metadata writes without verifying PostToolUse, and the skeptic-gate polling loop had a broken jq filter that never found VERDICT comments.

## Raw Body
## Summary
Metadata-updater hook guardrails and skeptic-gate workflow fixes.

## Background
AO-managed hooks were executing metadata writes without verifying PostToolUse, and the skeptic-gate polling loop had a broken jq filter that never found VERDICT comments.

## Changes
- Hook: fix guard logic so metadata writes only run after PostToolUse (fixes empty hook_event bypass)
- Hook: escape pipes in sed replacement (fixes branch names with | breaking metadata writes)
- Skeptic-gate: deduplicate polling loop and fix --paginate --jq with .[][] page flattening
- Skeptic-gate: remove --slurpfile /dev/null (not a valid gh flag)

## Evidence
**Claim class**: unit

Hook script changes pass gitleaks scan. Pattern matching verified. CR nitpicks addressed in follow-up commits.

**Test output**:
```
gitleaks scan --source .
12:53PM  32m  1 commits scanned.
12:53PM  32m  scan completed in 17.8ms
             32m  no leaks found
✅ No secrets detected
```

**Verdict**: PASS
