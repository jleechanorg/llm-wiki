# PR #19: feat: implement bd-uxs.7 and bd-uxs.8

**Repo:** jleechanorg/agent-orchestrator
**Merged:** 2026-03-19
**Author:** jleechan2015
**Stats:** +1038/-18 in 8 files

## Summary
Implements two orchestration beads:

- **bd-uxs.7**: Add .ao-managed ignore list to worktree porcelain check
  - Adds `setupAoManagedExclude()` function to workspace-worktree plugin
  - Creates `.git/info/exclude` with AO-managed file patterns on worktree creation
  - Prevents worktree dirty regressions

- **bd-uxs.8**: Configurable merge-gate hook
  - Adds `MergeGateConfig` interface to `ProjectConfig`
  - Supports `requiredLabels`, `blockedLabels`, `requiredChecks`
  - Supports `minApprovals`,

## Test Plan
- [ ] Build passes
- [ ] TypeScript compilation succeeds
- [ ] Test worktree creation with new exclude patterns
- [ ] Verify merge gate config validation

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Touches orchestration control flow (reaction execution, poller spawning, and workspace git exclude manipulation), which can affect automated session spawning and developer worktrees, but changes are additive and guarded wi

## Raw Body
## Summary
Implements two orchestration beads:

- **bd-uxs.7**: Add .ao-managed ignore list to worktree porcelain check
  - Adds `setupAoManagedExclude()` function to workspace-worktree plugin
  - Creates `.git/info/exclude` with AO-managed file patterns on worktree creation
  - Prevents worktree dirty regressions

- **bd-uxs.8**: Configurable merge-gate hook
  - Adds `MergeGateConfig` interface to `ProjectConfig`
  - Supports `requiredLabels`, `blockedLabels`, `requiredChecks`
  - Supports `minApprovals`, `unchangedFiles`, `requiredFiles`
  - Supports `preMergeWebhook` for custom merge conditions

## Test plan
- [ ] Build passes
- [ ] TypeScript compilation succeeds
- [ ] Test worktree creation with new exclude patterns
- [ ] Verify merge gate config validation

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Touches orchestration control flow (reaction execution, poller spawning, and workspace git exclude manipulation), which can affect automated session spawning and developer worktrees, but changes are additive and guarded with fallbacks/notifications.
> 
> **Overview**
> **Worktree cleanliness (bd-uxs.7):** The `workspace-worktree` plugin now bootstraps an AO-managed `.git/info/exclude` section during `create()` and `restore()` (with a fallback for linked worktrees where `.git` is a file), and adds tests for first-write, idempotency, preservation of existing content, and the linked-worktree path.
> 
> **Orchestration controls:** Adds `MergeGateConfig` to project config/types with Zod validation (labels/checks/approvals/file-patterns + optional pre-merge webhook/timeout), and extends reactions with a new `parallel-retry` action that spawns multiple retry sessions (without reusing the original branch) and emits a `reaction.triggered` notification.
> 
> **Reliability hardening:** `poller-manager` now guards against overlapping interval polls, sanitizes `workItem.id` when generating `fix/...`
