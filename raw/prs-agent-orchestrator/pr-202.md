# PR #202: [agento] fix(lifecycle): mark dead-agent sessions killed when send-to-agent reaction is skipped

**Repo:** jleechanorg/agent-orchestrator
**Merged:** 2026-03-26
**Author:** jleechan2015
**Stats:** +167/-2 in 4 files

## Summary
(none)

## Raw Body
{"body":"### Background\nWhen a tmux session is killed manually, AO session metadata persists. The lifecycle-worker keeps polling it, detecting PR state (e.g. CHANGES_REQUESTED), and firing reactions (ao send) to a dead session. The existing skipForDead guard correctly prevents the send-to-agent reaction from executing, but the session remains in a non-terminal state (e.g., changes_requested) indefinitely — never getting cleaned up.\n\nBead: bd-5o1\n\n### Goals\n1. Dead-agent sessions with send-to-agent reactions transition to killed for terminal cleanup\n2. SCM-only reactions (auto-merge, notify, request-merge) continue to fire for dead agents\n3. Terminal cleanup (session kill, worktree removal) runs on the same poll cycle\n\n### Tenets\n- TDD: failing test written first, then implementation\n- Minimal diff: single insertion point, no refactoring\n- Fork isolation: additive change in lifecycle-manager.ts, no new files\n\n### High-level description of changes\nAdded a pre-persistence check in checkSession(): when agentDead is true and the transition would trigger a send-to-agent reaction that cannot be delivered to a dead agent, the effectiveStatus is overridden to killed. This causes the existing terminal cleanup path to fire sessionManager.kill(), removing the tmux session, worktree, and metadata.\n\n### Testing\n- 2 new unit tests added to lifecycle-manager.dead-agent.test.ts\n- 1 existing test updated: expectation changed from changes_requested to killed + cleanup assertion\n- All 85 lifecycle-manager tests pass (3 test files)\n- Pre-existing failures in unrelated test files confirmed on main\n\n### Low-level details\n- **packages/core/src/lifecycle-manager.ts**: Added 17-line block between BD-kki absorption and persistence. When agentDead and effectiveStatus would trigger send-to-agent, overrides effectiveStatus to killed.\n- **packages/core/src/__tests__/lifecycle-manager.dead-agent.test.ts**: Added 2 new tests, updated 1 existing test.\n\n### Verification\n-
