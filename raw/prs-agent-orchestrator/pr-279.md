# PR #279: [agento] fix(harness): add skeptic change verification protocol to CLAUDE.md

**Repo:** jleechanorg/agent-orchestrator
**Merged:** 2026-03-29
**Author:** jleechan2015
**Stats:** +16/-0 in 1 files

## Summary
Skeptic infrastructure changes frequently break across layers (CLI code, GHA YAML, lifecycle-worker, GitHub API). Isolated unit tests pass while the end-to-end chain silently fails — 10+ broken PRs were opened in a 24h window before this was recognized.

## Background
Skeptic infrastructure changes frequently break across layers (CLI code, GHA YAML, lifecycle-worker, GitHub API). Isolated unit tests pass while the end-to-end chain silently fails — 10+ broken PRs were opened in a 24h window before this was recognized.

## Raw Body
## Background
Skeptic infrastructure changes frequently break across layers (CLI code, GHA YAML, lifecycle-worker, GitHub API). Isolated unit tests pass while the end-to-end chain silently fails — 10+ broken PRs were opened in a 24h window before this was recognized.

## Goals
- Prevent skeptic-related PRs from being opened without end-to-end chain verification
- Require cross-layer consistency checks (CLI ↔ GHA YAML ↔ bot author)
- Define red flags that stop a skeptic PR from being considered ready

## Tenets
- Full-chain verification > unit test pass
- Skeptic is a cross-layer system; changes require cross-layer sign-off
- Smoke test (`--dry-run`) is the minimum bar

## High-level description of changes
Added a new `## Skeptic Change Verification Protocol` section to `CLAUDE.md` immediately after the existing `## Skeptic Architecture` section. The section includes:
1. A pre-PR verification checklist (full-chain test, cross-layer consistency, smoke test)
2. Red flags that indicate a skeptic PR is not ready

## Testing
- Read the new section; verify it appears immediately after "Skeptic Architecture" in CLAUDE.md
- No code changes — documentation only

## Low-level details
- **CLAUDE.md**: Added `## Skeptic Change Verification Protocol` section (16 lines) after the Skeptic Architecture block. Covers full-chain verification, cross-layer checklist, minimum smoke test, and red flags.

## Verification
- Section is present in CLAUDE.md at the correct location
- Branch pushed and PR created

## Evidence

**Claim class**: integration

**Verdict**: PASS

**Test output**: CLAUDE.md section verified at correct location (lines 95-110, after Skeptic Architecture section)

No code changes — documentation-only addition to CLAUDE.md. The new section codifies the existing cross-layer verification practice for skeptic infrastructure changes (CLI ↔ GHA YAML ↔ lifecycle-worker). No test artifacts generated; read-based verification only.

Closes bd-v9ua

🤖 Generated with [Claude Code](
