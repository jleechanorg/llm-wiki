# PR #32: feat(core): implement evidence bundle generation (bd-2gz)

**Repo:** jleechanorg/agent-orchestrator
**Merged:** 2026-03-20
**Author:** jleechan2015
**Stats:** +381/-0 in 3 files

## Summary
(none)

## Raw Body
Part of bd-pdt epic. Structured evidence bundles + review gate. 9 TDD tests.

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Medium risk because it introduces new merge-gating evidence logic that shells out to `git` and writes JSON artifacts, which could affect CI/automation behavior if inputs or parsing differ across environments.
> 
> **Overview**
> Implements a new **Evidence Bundle** API (`generateEvidenceBundle`, `reviewEvidenceBundle`, `writeEvidenceBundle`) that collects SCM CI checks plus `git diff` changed files and `--stat` metrics (using `baseBranch...HEAD`) into a single structured JSON bundle with an embedded verdict.
> 
> Adds a review function that fails the verdict when any CI check is not `passed`/`skipped` or when no files changed, plus a writer that persists `bundle.json` and `verdict.json`. Exposes these new utilities and types via `packages/core/src/index.ts`, with accompanying Vitest coverage for bundle generation, verdict rules, and file output.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 3f1dfcff27184d2f40c77e2af2c8635bb76f65c7. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->

## Summary by CodeRabbit

* **New Features**
  * Added evidence bundle generation and review capabilities for PR validation with CI check and file change analysis.
  * Expanded automation framework with support for webhook ingestion with HMAC validation, PR polling with respawn caps, parallel CI retry detection, Slack notifications with dead-letter handling, session cleanup, failure budget tracking, merge-gate enforcement, and auto-resolving stale PR reviews.

<!-- end of auto-generated comment: release notes by coderabbit.ai -->
