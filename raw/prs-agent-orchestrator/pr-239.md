# PR #239: [agento] chore(skeptic): sync live workflows from skeptic templates

**Repo:** jleechanorg/agent-orchestrator
**Merged:** 2026-03-28
**Author:** jleechan2015
**Stats:** +366/-44 in 11 files

## Summary
- sync .github/workflows/skeptic-gate.yml from packages/cli/src/templates/skeptic/skeptic-gate.yml
- sync .github/workflows/skeptic-cron.yml from packages/cli/src/templates/skeptic/skeptic-cron.yml

## Raw Body
## Summary
- sync .github/workflows/skeptic-gate.yml from packages/cli/src/templates/skeptic/skeptic-gate.yml
- sync .github/workflows/skeptic-cron.yml from packages/cli/src/templates/skeptic/skeptic-cron.yml

## Why
Live skeptic workflows were stale relative to merged template updates.

## Notes
This is Option (2): template sync first, before any additional direct gate fixes.

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Changes GitHub Actions merge gating and auto-merge behavior for all PRs, so mistakes could block merges or merge unexpectedly. Risk is moderated by fail-closed logic (missing/unknown skeptic verdict prevents merge).
> 
> **Overview**
> Updates the Skeptic GitHub Actions workflows to align with the current `ao`/Codex-based flow.
> 
> In `skeptic-cron.yml`, the job now installs Node 22, attempts to install Codex, installs `@jleechanorg/ao-cli`, runs `ao skeptic verify` per open PR, tightens several 7-green checks (CI status by immutable head SHA, clearer PASS/FAIL handling), and requires an explicit skeptic `VERDICT: PASS` pulled from PR issue comments; it also posts a PR comment when an auto-merge attempt fails.
> 
> In `skeptic-gate.yml`, the gate no longer runs skeptic directly; it instead resolves the latest skeptic verdict from existing PR comments (from either `jleechan-agent[bot]` or `github-actions[bot]`) and fails closed when no verdict is found.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit aba0ba279b2d529e5b24697957cdf4cf73811443. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->
## Summary by CodeRabbit

* **Chores**
  * Updated GitHub Actions workflows to run Skeptic differently, install required runtime/tools, and tighten merge-gate control flow.
  * Switched verdict sourcing to explicit PR comments and enforced fail-clo
