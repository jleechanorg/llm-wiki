# PR #285: [agento] test(skeptic): add skeptic-chain-integration test validating full skeptic chain

**Repo:** jleechanorg/agent-orchestrator
**Merged:** 2026-03-29
**Author:** jleechan2015
**Stats:** +588/-0 in 2 files

## Summary
- Adds `packages/core/src/__tests__/skeptic-chain-integration.test.ts` validating the full skeptic chain from `runSkepticReview` through `ao skeptic verify` to VERDICT comment posting and GHA jq filter matching
- 7 tests covering: PASS/FAIL/SKIPPED verdict parsing, subprocess args, postVerdict body format, and skeptic-gate.yml JQ filter correctness

## Test Plan
- [x] `pnpm --filter @jleechanorg/ao-core test -- --testNamePattern="skeptic chain"` — all 7 pass
- [x] `pnpm --filter @jleechanorg/ao-core test` — no regressions in existing tests
- [x] `pnpm lint` — passes (1 pre-existing lint error in unrelated file)

## Raw Body
## Summary

- Adds `packages/core/src/__tests__/skeptic-chain-integration.test.ts` validating the full skeptic chain from `runSkepticReview` through `ao skeptic verify` to VERDICT comment posting and GHA jq filter matching
- 7 tests covering: PASS/FAIL/SKIPPED verdict parsing, subprocess args, postVerdict body format, and skeptic-gate.yml JQ filter correctness

## Test plan

- [x] `pnpm --filter @jleechanorg/ao-core test -- --testNamePattern="skeptic chain"` — all 7 pass
- [x] `pnpm --filter @jleechanorg/ao-core test` — no regressions in existing tests
- [x] `pnpm lint` — passes (1 pre-existing lint error in unrelated file)

## Evidence

**Claim class**: unit

### Unit test coverage

| Criterion | Evidence |
|---|---|
| Test file added | `packages/core/src/__tests__/skeptic-chain-integration.test.ts` |
| Tests added | 7 tests (PASS verdict, FAIL verdict, SKIPPED verdict, postVerdict body format, JQ filter ×3) |
| All pass | `pnpm --filter @jleechanorg/ao-core test -- --testNamePattern="skeptic chain"` → 7/7 ✓ |
| No regressions | `pnpm --filter @jleechanorg/ao-core test` → no new failures |

### Implementation correctness

The 7 tests validate:
1. `runSkepticReview` returns PASS verdict with correct subprocess args (`--trigger-sha`, `--model`)
2. `runSkepticReview` returns FAIL verdict without throwing
3. `VERDICT: SKIPPED` is mapped to FAIL by `VERDICT_LINE_RE` regex
4. `postVerdict` body contains all required markers, ISO timestamp, and SHA trigger
5. JQ filter correctly matches fresh verdict comments (right author + SHA + timestamp)
6. JQ filter matches `github-actions[bot]` fallback path
7. JQ filter returns null when no matching verdict exists

**Test output**:
```
 RUN  v4.0.18 /Users/jleechan/.worktrees/agent-orchestrator/ao-1439

 ✓ packages/core/src/__tests__/skeptic-chain-integration.test.ts > skeptic chain integration > PASS verdict > returns PASS verdict and calls ao skeptic with --trigger-sha + --model
 ✓ packages/core/src/__tests__/skeptic-chain-integr
