# PR #103: [agento] fix: rate-limit pause state + session visibility safeguards

**Repo:** jleechanorg/agent-orchestrator
**Merged:** 2026-03-22
**Author:** jleechan2015
**Stats:** +724/-32 in 11 files

## Summary
- add global rate-limit pause state
- prevent re-pause loops
- improve session visibility while paused

## Raw Body
## Summary
- add global rate-limit pause state
- prevent re-pause loops
- improve session visibility while paused

## Context
Follow-up PR for GH API minimization redesign.

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Changes lifecycle polling behavior to pause worker session processing based on rate-limit detection and persisted metadata, which could impact orchestration if pause parsing/refresh logic is wrong. Also changes `ao stop` session-purge defaults and CLI flags, affecting cleanup semantics for existing users.
> 
> **Overview**
> Adds forked rate-limit handling that parses agent output for reset times, writes project-level pause metadata (including a new `globalPauseCreatedAt` provenance key for duration-based pauses), and enforces a grace window to prevent re-pause loops.
> 
> Updates `lifecycle-manager` polling to detect/apply pauses during runtime checks, skip non-orchestrator sessions while a project is paused (with mid-cycle pause-state refresh), and clear only the human-readable pause reason after expiry while preserving provenance keys.
> 
> Adjusts `ao stop` to **not** purge mapped OpenCode sessions by default (removes `--keep-session`; `--purge-session` is now opt-in), tweaks web responses to compute `globalPause` once per request, and updates/adds tests covering the new pause behavior and revised stop semantics.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit e66c21001ad4f8d7dc3a6ecabde5b4f65b23c6e2. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->
## Summary by CodeRabbit

* **New Features**
  * Per-project global pause now records a created-at timestamp.
  * Automatic rate-limit detection with parsing of reset times and project-level pause application.

* **Changes**
  * Stop command defaults to not purging sessions; legacy keep-
