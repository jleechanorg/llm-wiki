# PR #254: [agento] fix(backfill): recover repoDir, extract shared util, fix CR nits

**Repo:** jleechanorg/agent-orchestrator
**Merged:** 2026-03-28
**Author:** jleechan2015
**Stats:** +349/-66 in 6 files

## Summary
When claimPR fails in backfillUncoveredPRs, the cascade cleanup falls back to inline worktree removal. The recovery chain (worktree dir, project.path, sibling scan) can leave repoDir=null when the worktree directory was already deleted from disk. In that case, the if(repoDir) guard skipped branch deletion, leaving the local branch entry in the owning repo. Subsequent backfill attempts for the same PR permanently fail because git refuses to worktree add -b when the branch already exists locally.

## Raw Body
## Summary

When claimPR fails in backfillUncoveredPRs, the cascade cleanup falls back to inline worktree removal. The recovery chain (worktree dir, project.path, sibling scan) can leave repoDir=null when the worktree directory was already deleted from disk. In that case, the if(repoDir) guard skipped branch deletion, leaving the local branch entry in the owning repo. Subsequent backfill attempts for the same PR permanently fail because git refuses to worktree add -b when the branch already exists locally.

## Fix

Add findRepoPathForWorktree() as an absolute last-resort recovery step before the if(repoDir) guard. This walks up from the worktree path looking for .git, or scans git worktree list from homedir, catching orphaned entries that all prior discovery steps missed. Branch deletion then runs in the if(repoDir) block, preventing cascading poison across backfill retries.

## Testing

- TypeScript compilation passes: tsc --noEmit on packages/core/tsconfig.json returns zero errors for backfill-extensions.ts
- Build succeeds: npm run build completes successfully
- Logic mirrors the proven findRepoPathForWorktree() already in workspace-worktree/src/index.ts (lines 247-300)

## Evidence

**Claim class**: unit

TypeScript compilation: tsc --noEmit on packages/core/tsconfig.json shows zero errors in backfill-extensions.ts

Build: npm run build completes without error (confirmed at push time)

Logic: findRepoPathForWorktree() implementation matches the proven version in workspace-worktree (lines 247-300 of packages/plugins/workspace-worktree/src/index.ts)

The recovery step added to backfill-extensions.ts:

```typescript
// Absolute last resort: walk up from the worktree path or scan git worktree
// list from homedir to find the owning repo.
if (repoDir === null) {
  const fallback = await findRepoPathForWorktree(worktreeDir);
  if (fallback) {
    repoDir = fallback.repoPath;
    if (!branch) branch = fallback.branch || null;
  }
}
```

**Verdict**: PASS

<!-- CURSOR_S
