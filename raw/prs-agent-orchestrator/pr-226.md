# PR #226: [agento] feat: repeatable remotion video render skill + Drive upload pipeline

**Repo:** jleechanorg/agent-orchestrator
**Merged:** 2026-03-27
**Author:** jleechan2015
**Stats:** +595/-0 in 2 files

## Summary
Adds a self-contained skill and script for rendering the AO novel as an animated MP4 and uploading it to Google Drive.

## Raw Body

## Summary

Adds a self-contained skill and script for rendering the AO novel as an animated MP4 and uploading it to Google Drive.

## What changed

- **`.claude/skills/video-render/SKILL.md`** — documents the full render pipeline
- **`scripts/render-novel-video.sh`** — one-liner: parses novel → generates Remotion Root.tsx → renders MP4 → uploads to Drive

## How to test

```bash
bash scripts/render-novel-video.sh
```

Expected: `remotion/out/daily-lives-of-workers.mp4` rendered + uploaded to Google Drive.

## Deliverables

| Artifact | Path |
|----------|------|
| Skill doc | `.claude/skills/video-render/SKILL.md` |
| Render script | `scripts/render-novel-video.sh` |
| Reference video | `remotion/src/Root.tsx` (existing, used as template) |

## Evidence

- Script parses `novel/the-daily-lives-of-workers.md` successfully (111KB, verified)
- Template generates parameterized `Root.tsx` with 7-scene structure
- `gog drive upload` authenticated for `jleechan@gmail.com` (drive scope confirmed)
- Existing MP4 on Drive: `1k5JmkmTLcdqfsk3mye2nFbNSLCUiOmAk` ("The Daily Lives of Workers.mp4")

## Design Alignment

Follows the pattern from `remotion/` package (`remotion/src/Root.tsx`) — same 7-scene structure, same color palette, same animation approach. Skill file follows `.claude/skills/` convention established by `skeptic-agent.md` and `agento_report.md`.

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Introduces a new shell pipeline that overwrites `remotion/src/Root.tsx` and uploads rendered artifacts to Google Drive, so mistakes can clobber local source or publish unintended files.
> 
> **Overview**
> Adds a documented, one-command pipeline to turn `novel/the-daily-lives-of-workers.md` into a Remotion-rendered MP4 and upload it to Google Drive.
> 
> Introduces `scripts/render-novel-video.sh`, which validates `gog` Drive auth, parses title/date/prose from the novel, conditionally regenerates `remotion/src/Root.tsx` (with backup + hash-based caching / `--force
