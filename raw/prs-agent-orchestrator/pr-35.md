# PR #35: chore(beads): create bd-q9n + bd-pdt epics, close rate limit fixes

**Repo:** jleechanorg/agent-orchestrator
**Merged:** 2026-03-20
**Author:** jleechan2015
**Stats:** +18/-0 in 1 files

## Summary
GitHub GraphQL API rate limit (5000/hr) was fully exhausted. Root causes: PR poller making ~85 REST calls/cycle on 21 capped PRs, plus 15 agent sessions burning GraphQL. Fixed by closing stale PRs and optimizing the poller (in jleechanclaw repo).

Also created bd-pdt epic tracking 12 runtime gaps between jleechanclaw orchestration and AO.

## Background
GitHub GraphQL API rate limit (5000/hr) was fully exhausted. Root causes: PR poller making ~85 REST calls/cycle on 21 capped PRs, plus 15 agent sessions burning GraphQL. Fixed by closing stale PRs and optimizing the poller (in jleechanclaw repo).

Also created bd-pdt epic tracking 12 runtime gaps between jleechanclaw orchestration and AO.

## Raw Body
## Background

GitHub GraphQL API rate limit (5000/hr) was fully exhausted. Root causes: PR poller making ~85 REST calls/cycle on 21 capped PRs, plus 15 agent sessions burning GraphQL. Fixed by closing stale PRs and optimizing the poller (in jleechanclaw repo).

Also created bd-pdt epic tracking 12 runtime gaps between jleechanclaw orchestration and AO.

## Goals

Track beads for two new epics and record completion of rate limit fixes.

## Tenets

- Beads-only change, no functional code

## High-level description of changes

.beads/issues.jsonl updated with:
- bd-q9n epic (rate limit) + 4 child tasks (3 closed, 1 open)
- bd-pdt epic (runtime gaps) + 12 child tasks (all open)

## Testing

No functional changes. Beads bookkeeping only.

## Low-level details

- **.beads/issues.jsonl**: 17 new bead entries (2 epics + 15 tasks)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Low risk: updates only `.beads/issues.jsonl` bookkeeping with no runtime or production code changes.
> 
> **Overview**
> Adds new Beads tracking entries in `.beads/issues.jsonl`, including the `bd-q9n` epic for GitHub API rate-limit remediation (with child tasks marked done/open) and the `bd-pdt` epic for AO orchestration runtime gaps (with multiple new open tasks).
> 
> Also records completion of specific rate-limit fixes by closing related tasks and links new tasks to their parent epics via dependencies.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 038e6225ee641dfc1089033ae9718d0893c9cd07. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->

## Summary by CodeRabbit

* **Documentation & Planning**
  * Added tracking for multiple enhancement initiatives including orchestration components, PR polling and scanning improvements, webhook ingestion, session cleanup, merge-gate enforcement, lear
