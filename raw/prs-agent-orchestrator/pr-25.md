# PR #25: feat(core): implement ParallelRetryMonitor (bd-tzt)

**Repo:** jleechanorg/agent-orchestrator
**Merged:** 2026-03-20
**Author:** jleechan2015
**Stats:** +740/-0 in 2 files

## Summary
Part of bd-pdt epic. The `parallel-retry` reaction spawns N sessions but nobody monitors which succeeds. This adds CI monitoring, winner detection, and loser cleanup.

## Background
Part of bd-pdt epic. The `parallel-retry` reaction spawns N sessions but nobody monitors which succeeds. This adds CI monitoring, winner detection, and loser cleanup.

## Raw Body
## Background
Part of bd-pdt epic. The `parallel-retry` reaction spawns N sessions but nobody monitors which succeeds. This adds CI monitoring, winner detection, and loser cleanup.

## Goals
- Monitor CI status across parallel retry sessions (race group)
- Detect first-green-wins and declare winner
- Kill losing sessions when winner found (configurable via `killOnSuccess`)
- Provide race status tracking and active race listing

## Tenets
- TDD: 11 tests written first
- Clean dependency injection: SessionManager and PluginRegistry passed via constructor
- No global state: races stored in instance Map

## High-level description of changes
New `ParallelRetryMonitor` class in core package. Manages "race groups" — sets of parallel sessions competing to fix the same issue. Polls CI via SCM plugin, detects winners, kills losers.

## Testing
- 11 unit tests covering: race creation, maxParallel limiting, winner detection, pending/failing CI, loser kill logic, killOnSuccess:false, getRaceStatus, listActiveRaces, error on premature resolve
- All 11 passing

## Low-level details
- **packages/core/src/parallel-retry.ts**: New file (195 LOC). `ParallelRetryMonitor` class with `startRace`, `checkRace`, `resolveRace`, `getRaceStatus`, `listActiveRaces`. Types: `RaceGroup`, `RaceEntry`.
- **packages/core/src/__tests__/parallel-retry.test.ts**: New file (307 LOC). 11 tests with mocked SessionManager and SCM plugin.
- **packages/core/src/index.ts**: Added exports for `ParallelRetryMonitor`, `RaceGroup`, `RaceEntry`.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Introduces new orchestration logic that spawns and kills sessions based on CI state; incorrect status evaluation or concurrency/rollback handling could terminate the wrong sessions or leave retries running.
> 
> **Overview**
> Adds a new `ParallelRetryMonitor` that runs a *parallel retry “race”*: it spawns sessions per configured strategy (up to `maxP
