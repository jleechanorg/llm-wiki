# PR #217: fix(skeptic-gate): add missing permissions block + improve comment posting

**Repo:** jleechanorg/agent-orchestrator
**Merged:** 2026-03-27
**Author:** jleechan2015
**Stats:** +6/-2 in 1 files

## Summary
Fixes two bugs in skeptic-gate.yml that prevent skeptic findings from posting as PR comments:

1. **Missing permissions block** — gh pr comment needs pull-requests write permission on ubuntu-latest runners. skeptic-cron.yml already had this; skeptic-gate.yml was missing it, causing silent failure.
2. **head -c truncation** — truncating to 6000 bytes mid-line left orphaned output inside the triple-backtick code block. Added newline before the closing fence.

## Raw Body
## Summary

Fixes two bugs in skeptic-gate.yml that prevent skeptic findings from posting as PR comments:

1. **Missing permissions block** — gh pr comment needs pull-requests write permission on ubuntu-latest runners. skeptic-cron.yml already had this; skeptic-gate.yml was missing it, causing silent failure.
2. **head -c truncation** — truncating to 6000 bytes mid-line left orphaned output inside the triple-backtick code block. Added newline before the closing fence.

## Changes

- .github/workflows/skeptic-gate.yml: added permissions block (contents:read, pull-requests:write) + fixed comment body truncation (5800 bytes + trailing newline) + replaced || true on gh pr comment with || echo WARN so failures surface in workflow logs

## Evidence

**Claim class**: unit-test-coverage

**Verdict**: PASS

- CI: all checks pass (Lint, Typecheck, Test, Integration Tests, Evidence Gate, Skeptic Gate)
- Manual review: 6-line change, targeted and minimal
- The skeptic gate workflow ran successfully on PR #215 (PASS verdict, 50s) but comment was missing — confirming the permissions bug is the only blocker

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Low risk workflow-only change; it adjusts GitHub Actions permissions and PR-comment posting/truncation behavior without touching product code.
> 
> **Overview**
> Ensures the `Skeptic Gate` workflow can reliably post findings to PRs by explicitly granting `pull-requests: write` (and `contents: read`) permissions.
> 
> Hardens PR comment generation by truncating slightly earlier, adding a trailing newline before the closing code fence, and logging a warning when `gh pr comment` fails instead of silently swallowing the error.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit d880da3309eaadd619a4a13ec83d0f54d3dca91f. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated com
