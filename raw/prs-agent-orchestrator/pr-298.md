# PR #298: [agento] feat(session-reaper): add session TTL and dead-tmux reaping (orch-ju1)

**Repo:** jleechanorg/agent-orchestrator
**Merged:** 2026-03-30
**Author:** jleechan2015
**Stats:** +589/-1 in 4 files

## Summary
- Add sessionTtlMs config (default 4h) to kill sessions alive longer than configured TTL
- Add dead-tmux detection: tmux has-session failure triggers kill + optional respawn
- Dead tmux + open PR: respawn replacement worker targeting same PR number
- Dead tmux + merged/closed PR: clean up via sessionManager.kill (worktree cleanup)
- Sessions within startup grace period (120s default) are immune to TTL/dead-tmux killing
- 14 new tests covering TTL, dead-tmux, respawn, grace period, and error hand

## Background
Replaces ao-session-reaper.sh from jleechanclaw. All reaping logic now lives natively in AO lifecycle-manager.

## Test Plan
- [x] 14 new TTL/dead-tmux tests pass (session-reaper-ttl.test.ts)
- [x] All 1178 existing tests pass (pre-existing failures in config.test.ts and wholesome.test.ts unrelated)
- [x] TypeScript build passes
- [x] Lint passes (0 errors)

## Raw Body
## Summary
- Add sessionTtlMs config (default 4h) to kill sessions alive longer than configured TTL
- Add dead-tmux detection: tmux has-session failure triggers kill + optional respawn
- Dead tmux + open PR: respawn replacement worker targeting same PR number
- Dead tmux + merged/closed PR: clean up via sessionManager.kill (worktree cleanup)
- Sessions within startup grace period (120s default) are immune to TTL/dead-tmux killing
- 14 new tests covering TTL, dead-tmux, respawn, grace period, and error handling

## Background
Replaces ao-session-reaper.sh from jleechanclaw. All reaping logic now lives natively in AO lifecycle-manager.

## Test plan
- [x] 14 new TTL/dead-tmux tests pass (session-reaper-ttl.test.ts)
- [x] All 1178 existing tests pass (pre-existing failures in config.test.ts and wholesome.test.ts unrelated)
- [x] TypeScript build passes
- [x] Lint passes (0 errors)

## Evidence

**Claim class**: unit

**Verdict**: PASS

**Terminal output**:
```
vitest run src/__tests__/session-reaper-ttl.test.ts
  ✓ session-reaper-ttl.test.ts (15 tests) 57ms
  Test Files  1 passed (1)
  Tests      15 passed (15)
  Duration   57ms
```

All 15 tests:
- kills session that has exceeded its TTL
- skips session still within its TTL
- TTL is skipped during startup grace period
- TTL kills even when no other reap condition is met
- kills session when tmux has-session returns false (dead tmux)
- dead tmux + merged PR: kill + worktree cleanup via sessionManager.kill
- dead tmux + closed PR: kill + worktree cleanup
- dead tmux + open PR: respawn action returned
- dead tmux + no PR: kill but no respawn
- healthy tmux session is not killed by tmux check alone
- dead tmux + open PR where respawn errors: kill still succeeds, error recorded
- dead tmux respects startup grace period
- tmux health check skipped when session has no tmuxName
- multiple dead tmux sessions: all killed, respawns dispatched for open-PR sessions
- dead tmux + open PR + kill fails: no respawn (respawn only on su
