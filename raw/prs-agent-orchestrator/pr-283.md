# PR #283: [agento] fix(harness): Evidence Gate merged-PR bypass + DISMISSED CR stalling + media-check mandate

**Repo:** jleechanorg/agent-orchestrator
**Merged:** 2026-03-29
**Author:** jleechan2015
**Stats:** +115/-2 in 5 files

## Summary
Fixes three PR blocker failure patterns that have caused 9+ PRs to stall at various gates:

1. **Evidence Gate permanent failure on merged/closed PRs** — Evidence Gate CI checks now skip for merged/closed PRs (pre-merge gate has no function post-merge)
2. **DISMISSED CR review stalling `approved-and-green`** — lifecycle now checks CR's reviews[] directly when `reviewDecision` is null, so dismissed CR → fresh-review cycle works correctly
3. **Evidence Media check failures from placeholder text** 

## Raw Body
## Summary

Fixes three PR blocker failure patterns that have caused 9+ PRs to stall at various gates:

1. **Evidence Gate permanent failure on merged/closed PRs** — Evidence Gate CI checks now skip for merged/closed PRs (pre-merge gate has no function post-merge)
2. **DISMISSED CR review stalling `approved-and-green`** — lifecycle now checks CR's reviews[] directly when `reviewDecision` is null, so dismissed CR → fresh-review cycle works correctly
3. **Evidence Media check failures from placeholder text** — CLAUDE.md now mandates `/pr-media` before first push; new `/evidence-check` command validates locally

## Changes

### CI/Workflow fixes
- `.github/workflows/evidence-gate.yml` — job-level `if: github.event.pull_request.merged == false` skips entirely for merged PRs
- `.github/workflows/wholesome.yml` — added `_merged-guard` job; `evidence-section` and `evidence-media-attachment` now depend on it and skip for merged/closed PRs

### Core fix
- `packages/core/src/lifecycle-manager.ts` — when `reviewDecision` is null (DISMISSED state), checks CR's reviews[] directly. If CR ever approved the current SHA, transitions to `mergeable` so `approved-and-green` fires correctly.

### Documentation/Commands
- `CLAUDE.md` — added rule mandating `/pr-media` (or real code blocks) before first push; explains Evidence Gate media requirements
- `.claude/commands/evidence-check.md` — new `/evidence-check` command for local pre-push validation against Evidence Gate criteria

## Testing

- `pnpm -C packages/core test` — 1110/1114 pass (4 pre-existing failures unrelated to this change)
- TypeScript: no new errors introduced

## Evidence

**Claim class**: unit-test-coverage

**Verdict**: PASS

```
pnpm -C packages/core test -- --reporter verbose
Test Files  2 failed | 58 passed (60)
Tests       4 failed | 1110 passed (1114)
```

The 4 failures are pre-existing (wholesome.test.ts PR title check + config.test.ts) — verified by running tests on clean stash.

---

Closes beads: bd-fisn

<!
