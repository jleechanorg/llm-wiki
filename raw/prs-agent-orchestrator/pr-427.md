# PR #427: [agento] feat(agent-claude-code): plugin-level MiniMax routing via model prefix

**Repo:** jleechanorg/agent-orchestrator
**Merged:** 2026-04-11
**Author:** jleechan2015
**Stats:** +82/-13 in 2 files

## Summary
- Detects MiniMax models by `MiniMax-` prefix in `AgentLaunchConfig.model`
- For MiniMax models: sets `ANTHROPIC_BASE_URL=https://api.minimax.io/anthropic` and `ANTHROPIC_AUTH_TOKEN=$MINIMAX_API_KEY` / `ANTHROPIC_API_KEY=$MINIMAX_API_KEY` in worker env, omits `env -u ANTHROPIC_BASE_URL` from launch command
- For all other models: clears `ANTHROPIC_BASE_URL` (unchanged behavior â†’ Anthropic OAuth)
- Removes dead `~/.bashrc` AO-session conditional (was always overridden by the plugin)

**Net effe

## Test Plan
- [x] All 206 plugin tests pass (`pnpm --filter @jleechanorg/ao-plugin-agent-claude-code test`)
- [x] `getLaunchCommand` with `model=MiniMax-M2.7` produces `claude --strict-mcp-config ... --model 'MiniMax-M2.7'` (no `env -u`)
- [x] `getLaunchCommand` with `model=claude-3-7-sonnet` produces `env -u ANTHROPIC_BASE_URL claude ...` (unchanged)
- [x] `getEnvironment` with MiniMax model sets correct `ANTHROPIC_BASE_URL`, `ANTHROPIC_AUTH_TOKEN`, and `ANTHROPIC_API_KEY`

ðŸ¤– Generated with [Claude Code

## Raw Body
## Summary

- Detects MiniMax models by `MiniMax-` prefix in `AgentLaunchConfig.model`
- For MiniMax models: sets `ANTHROPIC_BASE_URL=https://api.minimax.io/anthropic` and `ANTHROPIC_AUTH_TOKEN=$MINIMAX_API_KEY` / `ANTHROPIC_API_KEY=$MINIMAX_API_KEY` in worker env, omits `env -u ANTHROPIC_BASE_URL` from launch command
- For all other models: clears `ANTHROPIC_BASE_URL` (unchanged behavior â†’ Anthropic OAuth)
- Removes dead `~/.bashrc` AO-session conditional (was always overridden by the plugin)

**Net effect:** `modelByCli.claude-code.model=MiniMax-M2.7` in `agent-orchestrator.yaml` now correctly routes claude-code workers to the MiniMax endpoint without bashrc involvement.

## Evidence

**Claim floor override**: Plugin env-routing change is exercised by the existing 206-test plugin suite which covers `getLaunchCommand` and `getEnvironment` with all model types. No new integration infrastructure is needed â€” the change is fully observable through unit-level snapshot tests of the launch command and environment record outputs.

**206 plugin tests passing** (claim type: test-coverage):

```
 âœ“ src/index.test.ts (101 tests) 15ms
 âœ“ src/__tests__/workspace-setup.test.ts (6 tests) 19ms
 âœ“ src/__tests__/activity-detection.test.ts (42 tests) 51ms
 âœ“ src/hook-script.git-branch.integration.test.ts (8 tests) 483ms
 âœ“ src/hook-script.pr-create.integration.test.ts (9 tests) 628ms
 âœ“ src/hook-script.merge-metadata.integration.test.ts (16 tests) 786ms
 âœ“ src/hook-script.prefix-guard.integration.test.ts (22 tests) 1465ms

 Test Files  8 passed (8)
      Tests  206 passed (206)
   Duration  2.21s
```

Key test coverage: `getLaunchCommand` and `getEnvironment` with MiniMax vs non-MiniMax models tested in `src/index.test.ts`.

## Test plan
- [x] All 206 plugin tests pass (`pnpm --filter @jleechanorg/ao-plugin-agent-claude-code test`)
- [x] `getLaunchCommand` with `model=MiniMax-M2.7` produces `claude --strict-mcp-config ... --model 'MiniMax-M2.7'` (no `env -u`)
- [x] `
