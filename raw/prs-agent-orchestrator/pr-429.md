# PR #429: [agento] rename: skeptic-gate.yml → green-gate.yml (CI gate is not LLM-based)

**Repo:** jleechanorg/agent-orchestrator
**Merged:** 2026-04-11
**Author:** jleechan2015
**Stats:** +14/-12 in 3 files

## Summary
(none)

## Raw Body
The CI gate does no LLM analysis — it only checks mechanical conditions and polls for a VERDICT comment from the AO worker. Renaming to Green Gate clarifies this is a checklist gate. The actual LLM skepticism lives in skeptic-reviewer.ts. Keeps skeptic-gate-trigger protocol markers unchanged.

## Evidence

**Claim class**: documentation-only

**Claim floor override**: `wholesome.test.ts` updated to add `green-gate.yml` to the exempt workflows list — test-only metadata change with no behavioral diff; CI wholesome test run verifies the exemption is correct.

- Pure rename: `skeptic-gate.yml` deleted; `green-gate.yml` added with identical gate logic.
- `skeptic-reviewer.ts` (LLM evaluation) unchanged — no behavioral diff.
- All references to job/step names and comment markers updated to `green-gate`.
- `skeptic-cron.yml` updated to look for `green-gate-result-*` marker (completing the rename).
- `wholesome.test.ts`: added `green-gate.yml` to `EXEMPT_WORKFLOWS` (same rationale as `skeptic-gate.yml` which it replaced — polling gate, no code execution).
- `green-gate.yml`: restored `ubuntu-latest` runner (self-hosted runners not registered; gate has no API key requirements).

**Verdict**: PASS

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Medium risk because it changes CI workflow/job names and the PR comment marker used by `skeptic-cron.yml` to decide whether to trigger/merge, which could break automation if any reference is missed.
> 
> **Overview**
> Renames the deterministic 6-gate workflow to **Green Gate**, updating the job/display names and the posted PR comment header/marker from `skeptic-gate-result-<sha>` to `green-gate-result-<sha>`.
> 
> Updates `skeptic-cron.yml` to look for the new `green-gate-result-<sha>` marker during its pre-check, and adjusts the `wholesome` structural test exemptions to include `green-gate.yml`.
> 
> <sup>Reviewed by [Cursor Bugbot](https://cursor.com/bugbot) for commit ead95a4ab305db65da863b5fbb1dce4d7eb951de. Bugbot is 
