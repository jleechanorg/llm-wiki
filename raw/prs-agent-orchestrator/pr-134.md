# PR #134: fix(agent-base): silence metadata-updater noise in non-AO sessions

**Repo:** jleechanorg/agent-orchestrator
**Merged:** 2026-03-23
**Author:** jleechan2015
**Stats:** +1/-1 in 1 files

## Summary
(none)

## Raw Body
### Background
In regular interactive Claude Code sessions (no `AO_SESSION` env var), the metadata-updater hook fires on every Bash tool call and emits a `systemMessage` banner. This creates noise on every single command.

### Goals
Silence the banner in non-AO sessions while preserving identical behavior.

### Tenets
Minimal change — one line, no behavioral change for AO worker sessions.

### High-level description of changes
Replace the noisy `systemMessage` JSON with a silent `{}` exit when `AO_SESSION` is unset.

### Testing
Verified locally: Bash commands in non-AO sessions no longer show the banner.

### Low-level details
- **`packages/plugins/agent-base/src/index.ts:187`**: Replace `{"systemMessage": "..."}` with `{}`.

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Single-line change in the metadata-updater hook output; low risk since it only suppresses a non-essential `systemMessage` in non-AO sessions.
> 
> **Overview**
> Silences the metadata-updater hook when `AO_SESSION` is unset by returning an empty `{}` JSON instead of emitting a `systemMessage`, eliminating noisy banners in regular interactive sessions while keeping AO worker behavior unchanged.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 8462712de9954c1b9eb287e64904590a16cd6d3d. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
