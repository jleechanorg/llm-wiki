# PR #301: [agento] fix: kill tmux session when PR merges/closes (bd-s6z1)

**Repo:** jleechanorg/agent-orchestrator
**Merged:** 2026-03-30
**Author:** jleechan2015
**Stats:** +242/-6 in 2 files

## Summary
AO workers continue running in tmux sessions after their associated PR has been merged or closed. These zombie sessions burn tokens and context on completed work. The lifecycle-worker detects merged/closed state but had no mechanism to kill the tmux session.

## Background
AO workers continue running in tmux sessions after their associated PR has been merged or closed. These zombie sessions burn tokens and context on completed work. The lifecycle-worker detects merged/closed state but had no mechanism to kill the tmux session.

## Raw Body
## Background
AO workers continue running in tmux sessions after their associated PR has been merged or closed. These zombie sessions burn tokens and context on completed work. The lifecycle-worker detects merged/closed state but had no mechanism to kill the tmux session.

## Goals
- Automatically kill tmux sessions when their associated PR is merged or closed
- Prevent zombie workers from consuming resources after PR completion
- Add unit tests for the sweeper logic

## High-level description of changes
Added tmux-session-sweeper.ts which integrates with the lifecycle-manager to detect merged/closed PRs and kill their tmux sessions. Tests verify sweep behavior for merged, closed, and still-open PRs.

## Evidence

**Claim class**: unit

**Test run output**:
pnpm -C packages/core test -- --coverage --run tmux-session-sweeper

✓ src/__tests__/tmux-session-sweeper.test.ts (36 tests) 18ms
Test Files  1 passed (1)
Tests       36 passed (36)

**Coverage** (tmux-session-sweeper.ts):
File                     | % Stmts | % Branch | % Funcs | % Lines
tmux-session-sweeper.ts  |   97.56 |    90.00 |  100.00 |   97.43

**Tests added** (tmux-session-sweeper.test.ts — 36 total):
- PR-state kill: merged PR killed immediately, closed PR killed immediately, open PR skipped
- Kill cap: merged session respects maxKillsPerSweep
- Dry-run: merged session not killed in dry-run mode
- Error handling: session-not-found error is benign
- Hash guard: hash mismatch skipped (belongs to other AO config)
- Ownership: tmuxName undefined skipped (ownership unverifiable)
- Edge cases: attached sessions, idle threshold, unparseable dates

**Verdict**: PASS

## Resolved Comments
| Reviewer | File | Comment | Resolution |
|---|---|---|---|
| chatgpt-codex-connector[bot] | tmux-session-sweeper.ts | P1: Match tmux hash before killing DB-tracked merged sessions | Fixed in a7030848 |
| coderabbitai[bot] | tmux-session-sweeper.ts | Major: Dont turn a hash-blind DB match into a destructive kill | Fixed in a7
