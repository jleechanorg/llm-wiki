# PR #384: [agento] docs: fix tryClaudePrint flag docs to match implementation

**Repo:** jleechanorg/agent-orchestrator
**Merged:** 2026-04-05
**Author:** jleechan2015
**Stats:** +4/-4 in 2 files

## Summary
Fixes documentation mismatches flagged by CodeRabbit CR on PR #383:

- `tryCodexPrint` described as `codex --print --no-input` but implementation uses `codex exec -` via stdin
- `tryClaudePrint` described as `claude --print --no-input` but implementation uses `--dangerously-skip-permissions --print` via stdin-pipe
- All four doc lines updated to match actual `packages/cli/src/lib/llm-eval.ts` implementation

## Raw Body
## Summary

Fixes documentation mismatches flagged by CodeRabbit CR on PR #383:

- `tryCodexPrint` described as `codex --print --no-input` but implementation uses `codex exec -` via stdin
- `tryClaudePrint` described as `claude --print --no-input` but implementation uses `--dangerously-skip-permissions --print` via stdin-pipe
- All four doc lines updated to match actual `packages/cli/src/lib/llm-eval.ts` implementation

## Changes

| File | Change |
|------|--------|
| `CLAUDE.md` | `tryCodexPrint` and `tryClaudePrint` description lines updated |
| `roadmap/claude-fork-reference.md` | same two lines updated |

No code change — the implementation was always correct; only the docs were stale.

## Evidence

**Terminal test output:**

```
$ grep -n "tryCodexPrint\|tryClaudePrint" CLAUDE.md roadmap/claude-fork-reference.md
CLAUDE.md:121:  `tryCodexPrint(prompt)` → codex `exec -` only (prompt via stdin)
CLAUDE.md:122:  `tryClaudePrint(prompt)` → claude `--dangerously-skip-permissions --print` only (stdin-pipe)
roadmap/claude-fork-reference.md:28:  `tryCodexPrint(prompt)` → codex `exec -` only (prompt via stdin)
roadmap/claude-fork-reference.md:29:  `tryClaudePrint(prompt)` → claude `--dangerously-skip-permissions --print` only (stdin-pipe)
```

Verified against `packages/cli/src/lib/llm-eval.ts`:
- `tryCodexPrint`: line 76 → `["exec", "-"]` with `input: prompt`
- `tryClaudePrint`: line 118 → `["--dangerously-skip-permissions", "--print"]` with `input: prompt`

**Claim class:** documentation-only

**Verdict:** PASS

## Resolved Comments

| Reviewer | File | Comment | Resolution |
|---|---|---|---|
| coderabbitai[bot] | CLAUDE.md:121-122 | `tryClaudePrint` described `--no-input` but impl uses `--dangerously-skip-permissions --print` | Fixed in commit 44537c77 |
| copilot-pull-request-reviewer | CLAUDE.md:121 | `tryCodexPrint` described `--print --no-input` but impl uses `exec -` | Fixed in commit 76d0cfac |
| copilot-pull-request-reviewer | roadmap/claude-fork-reference.md:
