# PR #132: [agento] fix(merge-gate): dismissed CHANGES_REQUESTED no longer bypasses CR merge gate

**Repo:** jleechanorg/agent-orchestrator
**Merged:** 2026-03-24
**Author:** jleechan2015
**Stats:** +32/-26 in 3 files

## Summary
A dismissed `CHANGES_REQUESTED` CodeRabbit review incorrectly allowed PRs to pass the CR merge gate. Dismissed reviews should not override the original assessment — a dismissed `CHANGES_REQUESTED` remains a valid blocker.

## Background
A dismissed `CHANGES_REQUESTED` CodeRabbit review incorrectly allowed PRs to pass the CR merge gate. Dismissed reviews should not override the original assessment — a dismissed `CHANGES_REQUESTED` remains a valid blocker.

## Raw Body
## Background

A dismissed `CHANGES_REQUESTED` CodeRabbit review incorrectly allowed PRs to pass the CR merge gate. Dismissed reviews should not override the original assessment — a dismissed `CHANGES_REQUESTED` remains a valid blocker.

## Goals

Fix the merge gate so dismissed CR reviews don't clear active blockers.

## Tenets

- No breaking API changes
- Fork isolation: CR helpers extracted to `merge-gate-coderabbit.ts` companion module

## High-level description of changes

Extract CodeRabbit review helpers into a new `merge-gate-coderabbit.ts` companion module. Fix `evaluateCoderabbitApproval` to treat dismissed `CHANGES_REQUESTED` as blocking (not clearing) the gate. Also enable `request_changes_workflow` and `auto_review` in `.coderabbit.yaml` to support CR's formal review states.

## Resolved Comments
| Reviewer | File | Comment | Resolution |
|---|---|---|---|
| coderabbitai[bot] | .coderabbit.yaml | path_filters should be flat array not nested object | Fixed: `path_filters: []` |
| coderabbitai[bot] | .coderabbit.yaml | auto_title_placeholder must be string not boolean | Fixed: `auto_title_placeholder: "<title>"` |
| cursor[bot] | .coderabbit.yaml | auto_title_placeholder should be string not boolean | Fixed: `auto_title_placeholder: "<title>"` |
| cursor[bot] | .coderabbit.yaml | path_filters should be flat array not nested object | Fixed: `path_filters: []` |
| cursor[bot] | .coderabbit.yaml | root-level approve: true not in CR schema | Fixed: removed `approve: true` (not needed; `reviews.request_changes_workflow: true` handles formal review states) |
| chatgpt-codex-connector[bot] | merge-gate-coderabbit.ts | defensive timestamp handling in sortReviewsNewestFirst | Fixed: uses `?.getTime() ?? 0` |

## Testing

- All existing unit/integration tests pass (CI: all checks SUCCESS)
- CodeRabbit: APPROVED
- Cursor Bugbot: neutral (no blocking findings; Medium risk noted)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Changes merge-readiness gatin
