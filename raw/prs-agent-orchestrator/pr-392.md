# PR #392: [agento] fix(evidence-gate): Fix 2 agentRules media wire + Fix 3 claim floor enforcement

**Repo:** jleechanorg/agent-orchestrator
**Merged:** 2026-04-09
**Author:** jleechan2015
**Stats:** +52/-0 in 2 files

## Summary
Implements **Fix 2** (bd-4ze23) and **Fix 3** (bd-7s0d) from `roadmap/evidence-theater-diagnosis.md`:

- **Fix 2:** `defaults.agentRules` in `agent-orchestrator.yaml.example` instructs workers to use user-scope `tmux-video-evidence` (`~/.claude/skills/tmux-video-evidence/`), `smoke-test-local` (`~/.claude/skills/smoke-test-local/`) for integration+ claims, and never `N/A` for Terminal media on code-change PRs at integration+.
- **Fix 3:** `evidence-gate.yml` enforces a claim-class floor: if the 

## Raw Body
## Summary

Implements **Fix 2** (bd-4ze23) and **Fix 3** (bd-7s0d) from `roadmap/evidence-theater-diagnosis.md`:

- **Fix 2:** `defaults.agentRules` in `agent-orchestrator.yaml.example` instructs workers to use user-scope `tmux-video-evidence` (`~/.claude/skills/tmux-video-evidence/`), `smoke-test-local` (`~/.claude/skills/smoke-test-local/`) for integration+ claims, and never `N/A` for Terminal media on code-change PRs at integration+.
- **Fix 3:** `evidence-gate.yml` enforces a claim-class floor: if the PR touches `.ts`, `.js`, `.py`, `.go`, `.sh`, or `.json` files and the claim is `documentation-only` or `unit`, the gate requires `**Claim floor override**:` with a non-empty justification or an upgraded claim class.

## Tests

`pnpm build && AO_WHOLESOME_PR_TITLE='[agento] fix(evidence-gate): Fix 2 agentRules media wire + Fix 3 claim floor enforcement' pnpm test`

## Evidence

**Claim class**: unit

**Verdict**: PASS

**Claim floor override**: evidence-gate.yml is self-referential — claim floor does not apply to the fix that implements claim floor (this PR’s diff has no `.ts`/`.js`/`.py`/`.go`/`.sh`/`.json` paths under the floor rule).

## Issues

- bd-4ze23 (Fix 2 — agentRules media)
- bd-7s0d (Fix 3 — claim floor)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Modifies a required GitHub Actions merge gate and can newly fail PRs based on diff contents and PR-body fields, which may block merges if misconfigured or if git diff computation is unreliable.
> 
> **Overview**
> Tightens the `Evidence Gate` workflow by checking out full git history and enforcing a **claim-class floor**: PRs marked `unit` or `documentation-only` now fail if the base...head diff includes code file types (`.ts/.js/.py/.go/.sh/.json`) unless a non-empty **Claim floor override** justification is provided.
> 
> Updates `agent-orchestrator.yaml.example` to add default `agentRules` directing workers (for integration+ claims) to capture terminal media via user-scope `tmux-video-evid
