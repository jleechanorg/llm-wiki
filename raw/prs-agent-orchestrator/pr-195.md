# PR #195: [agento] fix(scm-github): REST fallback for getCIChecksFromStatusRollup + getCISummary

**Repo:** jleechanorg/agent-orchestrator
**Merged:** 2026-03-26
**Author:** jleechan2015
**Stats:** +185/-53 in 2 files

## Summary
`getCIChecksFromStatusRollup` uses `gh pr view --json statusCheckRollup` — a GraphQL-backed command. When GraphQL is exhausted, the lifecycle-worker loses CI state visibility.

## Background
`getCIChecksFromStatusRollup` uses `gh pr view --json statusCheckRollup` — a GraphQL-backed command. When GraphQL is exhausted, the lifecycle-worker loses CI state visibility.

## Raw Body
## Background
`getCIChecksFromStatusRollup` uses `gh pr view --json statusCheckRollup` — a GraphQL-backed command. When GraphQL is exhausted, the lifecycle-worker loses CI state visibility.

## Goals
- `getCIChecksFromStatusRollup` falls back to REST check-runs endpoint when `gh pr view` is rate-limited
- `getCISummary` calls `getCIChecksFromStatusRollup` as a secondary fallback before returning "none"

## Tenets
- Preserve existing behavior when GraphQL is available
- Best-effort: return empty array rather than throwing on REST failures
- No new dependencies

## Testing
- 128 tests pass (up from 126)
- 3 new tests added covering secondary fallback behavior

## Low-level details
- **packages/plugins/scm-github/src/index.ts**: `getCIChecksFromStatusRollup` gains try/catch + REST fallback; `getCISummary` catch block gains secondary fallback call before returning "none"
- **packages/plugins/scm-github/test/index.test.ts**: 3 new tests covering secondary fallback behavior

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Touches CI status retrieval and summarization paths; incorrect fallback/mapping could misreport passing vs failing checks during rate-limit conditions, impacting merge gating behavior.
> 
> **Overview**
> Improves resilience of GitHub CI status detection when `gh` GraphQL endpoints are rate-limited by adding a REST `check-runs` fallback path.
> 
> Introduces a shared `fetchCheckRunsViaRest()` helper (using `gh api --paginate`) and updates `getCIChecksFromStatusRollup()` and the `gh pr view --json statusCheckRollup` REST synthesis to use it. `getCISummary()` now attempts this status-rollup path as a **secondary fallback** on rate-limit errors before returning `"none"`.
> 
> Updates tests to cover the new secondary fallback behavior (including passing/failing outcomes) and to assert `--paginate` usage in the REST check-runs call.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.c
