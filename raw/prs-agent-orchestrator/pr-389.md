# PR #389: [agento] chore: context compaction research + hygiene audit

**Repo:** jleechanorg/agent-orchestrator
**Merged:** 2026-04-11
**Author:** jleechan2015
**Stats:** +518/-231 in 19 files

## Summary
Documents the 2026-04-05/06 context-compaction investigation and hygiene audit work, and also carries the minimal inherited CI unblockers needed to keep this long-lived branch mergeable after `main` moved underneath it.

Supersedes #388.

## Background
This branch started as roadmap, bead, and operator-hygiene work around Claude Code context compaction. While driving the PR back to green, `main` picked up new failures that immediately affected this branch after rebase: a critical `axios` audit hit in the Composio notifier dependency path, plus several stale test expectations that no longer matched current defaults and recovery behavior. Those inherited blockers had to be repaired here so the PR could realistically reach green again.

## Raw Body
## Summary

Documents the 2026-04-05/06 context-compaction investigation and hygiene audit work, and also carries the minimal inherited CI unblockers needed to keep this long-lived branch mergeable after `main` moved underneath it.

Supersedes #388.

## Background

This branch started as roadmap, bead, and operator-hygiene work around Claude Code context compaction. While driving the PR back to green, `main` picked up new failures that immediately affected this branch after rebase: a critical `axios` audit hit in the Composio notifier dependency path, plus several stale test expectations that no longer matched current defaults and recovery behavior. Those inherited blockers had to be repaired here so the PR could realistically reach green again.

## Goals

- Preserve the context-compaction findings in durable roadmap artifacts.
- Keep the hygiene-audit and dispatch-task documentation aligned with the recorded workflow.
- Rebase the branch cleanly onto current `main`.
- Remove inherited CI blockers that are unrelated to the roadmap content but prevent this PR from going green.

## Tenets

- Keep the original documentation scope intact.
- Treat inherited CI breakage as unblocker work, not as an excuse to stop.
- Prefer minimal fixes: dependency pinning for the security regression, and test-only updates where production behavior is already correct.
- Avoid widening runtime behavior changes unless required by the blocker itself.

## High-level description of changes

The original branch contents remain: roadmap documentation for context compaction, a periodic context-hygiene audit, bead tracking, a dispatch-task skill, and the small `metadata-updater.sh` cleanup. On top of that, this update adds a root `pnpm` override to pin `axios@1.15.0` and refreshes several stale tests so they match current behavior: generated configs now default to Codex with `modelByCli`, task-queue tests no longer shell out to a real `br`, wholesome tests no longer assume `origin/HEAD` or PR cont
