# PR #31: feat(core): implement webhook ingress (bd-c9h)

**Repo:** jleechanorg/agent-orchestrator
**Merged:** 2026-03-20
**Author:** jleechan2015
**Stats:** +251/-0 in 4 files

## Summary
(none)

## Raw Body
Part of bd-pdt epic. HMAC validation + dedup + event queue. 10 TDD tests.

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Introduces new webhook-security logic (HMAC verification and delivery dedup) that will gate acceptance of incoming events; incorrect handling could drop or accept unintended webhook deliveries. Changes are otherwise additive and isolated behind a new `WebhookIngress` API with test coverage.
> 
> **Overview**
> Adds a new `WebhookIngress` module implementing `X-Hub-Signature-256` verification (constant-time compare), in-memory delivery-ID dedup with bounded FIFO eviction, and a simple FIFO event queue for normalized `SCMWebhookEvent`s.
> 
> Exports the new API from `packages/core/src/index.ts` and adds a Vitest suite covering signature validation, dedup/idempotency/eviction behavior, and queue FIFO semantics. Also updates `.beads/issues.jsonl` with new open epic/task entries (including `bd-c9h`).
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 947e616c9b067358867b967aef403f21355d0d66. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->

## Summary by CodeRabbit

## Release Notes

* **New Features**
  * Webhook ingress integration for GitHub App event processing with security validation
  * Pattern synthesis for generating fix strategy templates from recorded outcomes
  * PR poller plugin with automated session spawning and respawn limits
  * Session reaper for cleanup of orphaned sessions
  * Slack notification plugin with queue management
  * MCP agent mail integration for inter-agent messaging
  * Enhanced merge-gate with 6-condition enforcement
  * Failure budget tracking for retry management
  * Auto-resolution of stale PR review threads after fixes
  * Outcome tracking and persistence for fix strategies

* **Tests**
  * Added compre
