# PR #158: [agento] fix(scm-github): restore '/' in encoded branch name for REST ref deletion

**Repo:** jleechanorg/agent-orchestrator
**Merged:** 2026-03-24
**Author:** jleechan2015
**Stats:** +30/-3 in 2 files

## Summary
Fixes a bug in `scm-github` REST fallback branch deletion where `encodeURIComponent`
encodes `/` as `%2F`, breaking GitHub ref paths for branches containing slashes
(e.g. `feat/bd-pjh`).

## Raw Body
## Summary

Fixes a bug in `scm-github` REST fallback branch deletion where `encodeURIComponent`
encodes `/` as `%2F`, breaking GitHub ref paths for branches containing slashes
(e.g. `feat/bd-pjh`).

## Testing

- Build passes: `pnpm run build` ✓
- 16/16 existing scm-github tests pass (pre-existing `@jleechanorg/ao-core` resolution issue in test runner is unrelated)
- Code review: one-line fix

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Low risk: narrowly changes URL-encoding for REST branch ref deletion and adds a regression test; merge/auto-merge behavior is otherwise unchanged.
> 
> **Overview**
> Fixes `scm-github` REST-fallback branch cleanup after `mergePR` so branch names containing `/` are not broken by `encodeURIComponent` (now reverts `%2F` back to `/` while still encoding other unsafe characters).
> 
> Adds a unit test that forces the rate-limit fallback path and asserts the subsequent `gh api ... DELETE` ref uses `refs/heads/<branch>` with slashes preserved.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit d8a90aba4b1d3e6f7a3a81b9bd44bf75871d9c61. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->


## Evidence

**Claim class**: Unit test coverage

**Test file**: `packages/plugins/scm-github/test/index.test.ts`

**Evidence**:
- 126 tests pass (CI run #23500412783): `Test` + `Integration Tests` + `Test Fresh Onboarding` all SUCCESS
- New regression test: `"REST fallback branch deletion preserves slashes in branch name"` — exhausts 3 rate-limit retries, then verifies `DELETE` call args contain `refs/heads/feat/my-feature` (not `%2F`), 3003ms duration

**Verdict**: PASS

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->

## Summary by CodeRabbit

* **Bug Fixes**
  * Impr
