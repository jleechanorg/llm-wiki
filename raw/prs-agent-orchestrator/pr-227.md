# PR #227: [agento] ci: add wholesome structural test gate

**Repo:** jleechanorg/agent-orchestrator
**Merged:** 2026-03-27
**Author:** jleechan2015
**Stats:** +371/-0 in 3 files

## Summary
Structural quality gates (linting, type-checking, test coverage) run continuously — but they don't catch *process violations* like missing prefixes, new `@ts-ignore` directives, or `eslint-disable` added in a branch. The `wholesome` test suite enforces these at CI time with "multiple shots on goal": deterministic structural assertions that fail the build when the diff breaks repository invariants.

## Background
Structural quality gates (linting, type-checking, test coverage) run continuously — but they don't catch *process violations* like missing prefixes, new `@ts-ignore` directives, or `eslint-disable` added in a branch. The `wholesome` test suite enforces these at CI time with "multiple shots on goal": deterministic structural assertions that fail the build when the diff breaks repository invariants.

## Raw Body
## Background

Structural quality gates (linting, type-checking, test coverage) run continuously — but they don't catch *process violations* like missing prefixes, new `@ts-ignore` directives, or `eslint-disable` added in a branch. The `wholesome` test suite enforces these at CI time with "multiple shots on goal": deterministic structural assertions that fail the build when the diff breaks repository invariants.

## Goals

- Add a Vitest test suite (`wholesome.test.ts`) that asserts structural invariants on every PR
- Assert PR title has `[agento]` prefix + correct conventional-commit format
- Assert no `// @ts-ignore` / `// @ts-expect-error` added in source files
- Assert no `eslint-disable` directives added in the branch diff
- Assert no fork-isolation markers (`jleechanorg`, `openclaw`, etc.) added in high-conflict upstream files
- Assert all non-merge commits on the branch carry `[agento]` prefix
- Add a dedicated CI workflow (`.github/workflows/wholesome-checks.yml`) to run these checks on PR events

## Tenets

- **Fail-closed**: structural violations always fail the build, never silently pass
- **Local resolvable**: `BASE_BRANCH` resolves correctly in both CI and local environments
- **Shell-injection safe**: all git/gh calls use `execFileSync` with array args, never interpolated strings
- **No mocks**: tests read real files, run real git commands — no test doubles

## High-level description of changes

- `packages/core/src/__tests__/wholesome.test.ts` — 5 `describe` groups of Vitest structural tests
- `.github/workflows/wholesome-checks.yml` — dedicated CI workflow (runs on `pull_request` + `push` to `main`)
- `.github/workflows/ci.yml` — test job updated to: (a) pass `GITHUB_BASE_REF`, `GITHUB_HEAD_REF`, `GITHUB_TOKEN`, `GITHUB_REPOSITORY` env vars so wholesome tests resolve base branch and PR title correctly; (b) explicit `permissions: contents: read; pull-requests: read`

## Testing

**Test output:**
```
src/__tests__/wholesome.test.ts (8 tests | 6 passed 
