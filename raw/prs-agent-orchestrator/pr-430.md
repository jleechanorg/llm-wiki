# PR #430: [agento] fix(scm-github): handle non-AO worktree branch conflicts via ref-based recovery

**Repo:** jleechanorg/agent-orchestrator
**Merged:** 2026-04-11
**Author:** jleechan2015
**Stats:** +74/-9 in 2 files

## Summary
- Fix `checkoutPR` to handle "already checked out at" conflicts from non-AO worktrees
- Use `git update-ref` to redirect the shared branch to the PR head, then `git reset --hard` to update the AO worktree
- Verify by SHA comparison (not branch name) since the branch is shared with non-AO worktree
- Fix workspace-worktree TypeScript error (`cfg.branchNumber` doesn't exist)

## Test Plan
- [x] `ao spawn --project worldarchitect --claim-pr 6156 --agent gemini` works despite non-AO worktree holding branch checked out
- [ ] All existing `checkoutPR` tests still pass

🤖 Generated with [Claude Code](https://claude.ai/claude-code)

## Raw Body
## Summary

- Fix `checkoutPR` to handle "already checked out at" conflicts from non-AO worktrees
- Use `git update-ref` to redirect the shared branch to the PR head, then `git reset --hard` to update the AO worktree
- Verify by SHA comparison (not branch name) since the branch is shared with non-AO worktree
- Fix workspace-worktree TypeScript error (`cfg.branchNumber` doesn't exist)

## Evidence

- Spawned wa-886 with `ao spawn --project worldarchitect --claim-pr 6156 --agent gemini` — succeeded despite non-AO worktree `/Users/jleechan/projects/worktree_repro_copy` holding `feat/repro-copy-command`
- Before fix: `ao spawn --claim-pr 6156` failed with `fatal: 'feat/repro-copy-command' is already checked out at '/Users/jleechan/projects/worktree_repro_copy'`
- After fix: session created, worktree reset to PR head SHA, checkout verified by SHA comparison
- TypeScript builds cleanly: `pnpm --filter @jleechanorg/ao-plugin-scm-github build` passes

## Test plan

- [x] `ao spawn --project worldarchitect --claim-pr 6156 --agent gemini` works despite non-AO worktree holding branch checked out
- [ ] All existing `checkoutPR` tests still pass

🤖 Generated with [Claude Code](https://claude.ai/claude-code)
