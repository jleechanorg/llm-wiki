# PR #267: [agento] fix(skeptic): match markdown-bold VERDICT format in mergeGate.ts

**Repo:** jleechanorg/agent-orchestrator
**Merged:** 2026-03-29
**Author:** jleechan2015
**Stats:** +30/-2 in 2 files

## Summary
Fixes `VERDICT_LINE_RE` to correctly match markdown-bold verdict lines (e.g. `**VERDICT: FAIL**`) in addition to plain and blockquote formats.

**Root cause**: The old regex `/^(?:> ?\*\*)?VERDICT:/` required either nothing or `"> "` before `**VERDICT:` — the `\*\*` anchor covered only `"> **VERDICT:"`, not a standalone-bold line like `**VERDICT: FAIL**`.

**Fix**: `^(?:> ?)?\*?\*?VERDICT:` handles all three variants independently:
- plain:       `VERDICT: PASS`
- blockquote:  `> **VERDICT: SKIP

## Raw Body
## Summary

Fixes `VERDICT_LINE_RE` to correctly match markdown-bold verdict lines (e.g. `**VERDICT: FAIL**`) in addition to plain and blockquote formats.

**Root cause**: The old regex `/^(?:> ?\*\*)?VERDICT:/` required either nothing or `"> "` before `**VERDICT:` — the `\*\*` anchor covered only `"> **VERDICT:"`, not a standalone-bold line like `**VERDICT: FAIL**`.

**Fix**: `^(?:> ?)?\*?\*?VERDICT:` handles all three variants independently:
- plain:       `VERDICT: PASS`
- blockquote:  `> **VERDICT: SKIPPED**`  (skeptic-gate.yml SKIPPED fallback)
- markdown:    `**VERDICT: FAIL**`        (LLM output with bold)

Preserves the HTML-marker (`skeptic-agent-verdict`) and `ACCEPTED_AUTHORS` hardening from main.

## Testing

Unit tests (10 skeptic mergeGate tests pass):
```
✓ mergeGate.test.ts  (10 tests — includes new regression test)
```

## Changed files

- `packages/cli/src/commands/skeptic/verdict-utils.ts` — fixed `VERDICT_LINE_RE`
- `packages/cli/__tests__/commands/skeptic/mergeGate.test.ts` — regression test for `**VERDICT: FAIL**`

## Evidence

**Claim class**: unit test coverage

**Verdict**: PASS

Unit tests: 10 skeptic mergeGate tests pass (10/10)

```text
✓ mergeGate.test.ts  10 tests  3ms
```

