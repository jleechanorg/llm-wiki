# PR #161: fix(lifecycle-worker): increase default polling interval 30s->75s (bd-fmv)

**Repo:** jleechanorg/agent-orchestrator
**Merged:** 2026-03-24
**Author:** jleechan2015
**Stats:** +6/-4 in 1 files

## Summary
(none)

## Raw Body
Background: Default 30s polling x3 workers = 5400/hr > 5000 limit, causing hourly GraphQL exhaustion.

Goals: Bring consumption under 5000/hr with 3 concurrent workers.

Changes:
- Default interval 30s->75s (1350/hr with 3 workers)
- Adds GhRateLimitStatus utility for future adaptive back-off

Bead: bd-fmv

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Changes the lifecycle worker’s scheduling defaults, which could delay lifecycle detection/updates and alter cleanup cadence if misconfigured, but touches only internal CLI behavior with limited surface area.
> 
> **Overview**
> Reduces GitHub polling pressure by increasing the `lifecycle-worker` default `--interval-ms` from 30s to 75s (including the fallback used by `parseInterval`).
> 
> Tightens duration parsing by switching the orphan sweep interval to `parseDurationMs(..., 300_000)` so invalid values no longer fall back to the polling default, keeping orphan sweeping on its intended cadence.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 101b4665a8437df6b5d84f94f7baa097aa9688a9. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
