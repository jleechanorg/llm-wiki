# PR #71: fix(ci): symlink warn-not-throw + skip extended setup without config

**Repo:** jleechanorg/agent-orchestrator
**Merged:** 2026-03-21
**Author:** jleechan2015
**Stats:** +20/-6 in 3 files

## Summary
(none)

## Raw Body
Fix two systemic CI failures blocking all PRs: (1) Restore symlink warn-not-throw in agent-claude-code (AO agent reverted it), (2) Skip setup-extended.sh when no config file exists (fixes onboarding test).

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Changes filesystem/symlink handling for workspace hook installation and alters setup script execution conditions; could affect whether hooks or extended setup run in some environments, but is scoped and mainly reduces CI/onboarding failures.
> 
> **Overview**
> Prevents CI/setup failures by making the `claude-code` agent’s workspace hook setup *symlink-aware*: `.claude` symlinks are now resolved and only skipped when they point outside the workspace (or can’t be verified), instead of throwing.
> 
> Updates `scripts/setup.sh` to run `setup-extended.sh` **only when** both the script and the AO config file exist, otherwise printing a skip hint. Also removes an unused `Poller` type import from `lifecycle-manager.ts`.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit e8f73d3c5b940f946b6219ef63c49c06d192e93c. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->

## Summary by CodeRabbit

* **Bug Fixes**
  * Hook setup process now handles symbolic links gracefully, logging a warning instead of aborting execution.
  * Extended setup validation now requires both the configuration file and setup script to proceed; displays helpful guidance when configuration is missing, directing users to initialize it properly.

<!-- end of auto-generated comment: release notes by coderabbit.ai -->
