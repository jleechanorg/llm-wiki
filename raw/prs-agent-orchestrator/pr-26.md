# PR #26: feat(core): implement OutcomeRecorder (bd-nig)

**Repo:** jleechanorg/agent-orchestrator
**Merged:** 2026-03-20
**Author:** jleechan2015
**Stats:** +402/-159 in 7 files

## Summary
(none)

## Raw Body
Part of bd-pdt epic. OutcomeRecorder for JSONL persistence of fix strategy outcomes. 12 TDD tests.

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **High Risk**
> High risk because it introduces new disk-write side effects on terminal lifecycle transitions and deletes `evidence-bundle.ts` while `index.ts` and tests still reference `./evidence-bundle.js`, which could break builds if not replaced elsewhere.
> 
> **Overview**
> Adds a new `OutcomeRecorder` JSONL store with APIs to `record`, `query`, compute per-(strategy,action) win rates, and rank top actions, with explicit handling of malformed lines and a `clear()` operation.
> 
> Integrates outcome recording into `createLifecycleManager` as an optional dependency, writing a `RecordedOutcome` on first transition into terminal states (guarded so filesystem errors don’t impact polling). Extends session metadata and `RecordedOutcome` types to include `action`, `strategy`, and `errorClass`, and adds a comprehensive Vitest suite for the recorder.
> 
> Also removes the `evidence-bundle.ts` implementation from `core` (while exports/imports still reference `./evidence-bundle.js`).
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 4a73027eae09c0ff6329e9f59023493b5b881a86. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->
## Summary by CodeRabbit

* **New Features**
  * Outcome recording and analytics for tracking action results, computing win rates, and surfacing top strategies.
  * Added merge-gate enforcement and several orchestration integrations (evidence merging, pattern synthesis, PR polling, webhook ingress, session reaper, failure-budget routing, agent messaging, Slack outbox/dead-letter, retry monitoring, auto-resolve PR review threads).
  * Session metadata now persists an action label; lifecycle events record outcomes on 
