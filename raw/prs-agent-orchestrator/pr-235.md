# PR #235: [agento] chore: create bd-skp2 bead — closed PR fires changes-requested reaction

**Repo:** jleechanorg/agent-orchestrator
**Merged:** 2026-03-27
**Author:** jleechan2015
**Stats:** +2/-2 in 1 files

## Summary
PR #223 was CLOSED, but the lifecycle-manager's `changes-requested` reaction kept firing because `determineStatus` checks `reviewDecision === "changes_requested"` without first verifying the PR is OPEN. GitHub does not clear `reviewDecision` when a PR is closed, and the batch path's `batch.state` can be falsy/undefined, bypassing the closed guard.

## Background
PR #223 was CLOSED, but the lifecycle-manager's `changes-requested` reaction kept firing because `determineStatus` checks `reviewDecision === "changes_requested"` without first verifying the PR is OPEN. GitHub does not clear `reviewDecision` when a PR is closed, and the batch path's `batch.state` can be falsy/undefined, bypassing the closed guard.

## Raw Body
## Background
PR #223 was CLOSED, but the lifecycle-manager's `changes-requested` reaction kept firing because `determineStatus` checks `reviewDecision === "changes_requested"` without first verifying the PR is OPEN. GitHub does not clear `reviewDecision` when a PR is closed, and the batch path's `batch.state` can be falsy/undefined, bypassing the closed guard.

## Goals
- Track the bug for fix dispatch

## Tenets
- Harness gap identified via `/harness` must produce a tracking bead

## High-level description
Created `bd-skp2` bead documenting the root cause and proposed fix in `lifecycle-manager.ts`.

## Testing
Bead created, committed, and pushed.

## Low-level details
- `.beads/issues.jsonl`: appended `bd-skp2` bead with root cause analysis and fix proposal

## Verification
- `br list` confirms bead exists (DB note: recovery artifacts present, JSONL is valid)
- Branch pushed to origin

## Evidence

**Claim class**: integration

**What was done**: Appended `bd-skp2` bead to `.beads/issues.jsonl` via `br` tool, committed, and pushed to branch `feat/pr-223-bd-skp2-has-changes-requested-merge-conflict-first-re`. Rebase conflict in `issues.jsonl` resolved by keeping both beads (`bd-dfl` + `bd-skp2`).

**Bead entry**:
```
{"id": "bd-skp2", "title": "lifecycle-manager: closed PRs incorrectly fire changes-requested reaction", "status": "open", "priority": 2, "issue_type": "bug"}
```

**Verdict**: PASS — harness gap identified and tracked via bead; fix needs to be dispatched to a worker.

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Only updates the internal `.beads/issues.jsonl` tracker by appending a new bead; no runtime code paths are changed.
>
> **Overview**
> Adds a new tracking bead (`bd-skp2`) documenting a bug where `lifecycle-manager` can mis-route **closed PRs** into the `changes_requested` reaction due to `reviewDecision` being checked without confirming the PR is open.
>
> Also adjusts the JSONL tail formatting so `bd-dfl` is stored as a normal line
