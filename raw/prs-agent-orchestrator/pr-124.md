# PR #124: fix(core): avoid API retry storm when batch PR status hits rate limit

**Repo:** jleechanorg/agent-orchestrator
**Merged:** 2026-03-23
**Author:** jleechan2015
**Stats:** +61/-2 in 2 files

## Summary
(none)

## Raw Body
Fixes a bug where failure of the batch GraphQL API check falls back to 4 individual REST/GraphQL checks, which subsequently all fail due to the same rate limit, causing an API retry storm.

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Touches `lifecycle-manager` PR-status determination logic; incorrect rate-limit detection could prevent status updates by skipping the individual-call fallback or change behavior under transient SCM failures.
> 
> **Overview**
> Prevents a GitHub API *rate-limit* failure in `scm.getBatchPRStatus` from triggering the 4+ individual SCM fallback calls in `lifecycle-manager.determineStatus`, avoiding a retry/thundering-herd storm.
> 
> Adds coverage to ensure that when batch status throws a rate-limit error, the lifecycle check preserves the current session status and does not call `getPRState`/`getCISummary`/`getReviewDecision`/`getMergeability`.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 4cbaca1898b708f18f5e8fe4e0cd3b29cbfb7a37. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->

## Summary by CodeRabbit

* **Bug Fixes**
  * Improved handling of GitHub API rate limit errors by preserving session state and preventing unnecessary follow-up requests when rate limits are encountered.

* **Tests**
  * Added test coverage for rate limit error handling behavior in the lifecycle manager.

<!-- end of auto-generated comment: release notes by coderabbit.ai -->
