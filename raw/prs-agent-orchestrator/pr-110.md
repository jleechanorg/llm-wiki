# PR #110: docs+chore: GH API validation report, harness skills, .claude tracking

**Repo:** jleechanorg/agent-orchestrator
**Merged:** 2026-03-23
**Author:** jleechan2015
**Stats:** +1729/-66 in 8 files

## Summary
This PR bundles the GH API rate-limit reduction validation work with harness engineering improvements discovered during the validation process. Three rounds of validation (v1 to v2 to v3) exposed systematic gaps in how LLM agents approach real-world measurement tasks, leading to new skills and memories that prevent recurrence.

Related: bd-8y9 (investigate high per-cycle API call count)

## Background
This PR bundles the GH API rate-limit reduction validation work with harness engineering improvements discovered during the validation process. Three rounds of validation (v1 to v2 to v3) exposed systematic gaps in how LLM agents approach real-world measurement tasks, leading to new skills and memories that prevent recurrence.

Related: bd-8y9 (investigate high per-cycle API call count)

## Raw Body
## Background

This PR bundles the GH API rate-limit reduction validation work with harness engineering improvements discovered during the validation process. Three rounds of validation (v1 to v2 to v3) exposed systematic gaps in how LLM agents approach real-world measurement tasks, leading to new skills and memories that prevent recurrence.

Related: bd-8y9 (investigate high per-cycle API call count)

## Goals

1. Document runtime validation results for the fork GH API optimizations (poll interval, GhCache, batching)
2. Track .claude/ skills in the repo so harness improvements persist across clones
3. Add validation-gate, harness-engineering, evidence-standards, and auton skills
4. Unignore .claude/ directory (except ephemeral files) so skills/commands are version-controlled

## Tenets

- Real runtime measurement over code-analysis projections
- Fail-closed evidence verdicts -- never upgrade claim class to avoid INSUFFICIENT
- Harness-first: fix the system that prevents mistakes, not just the symptom

## High-level description of changes

**Validation report** (roadmap/gh-api-reduction-validation.md): v3 runtime validation measuring fork vs upstream API consumption with real PR workloads (N=3 trials). Documents per-cycle call counts, hourly projections, and GhCache effectiveness with honest claim-class assessments.

**Harness skills** (.claude/skills/): Four skills copied from user-global into the repo for portability:
- validation-gate.md -- Step 0 compromise disclosure before executing validation plans; real-test-first default
- harness-engineering.md -- 5-Whys protocol for both technical and agent-path failures
- evidence-standards.md -- Claim-class matrix with fail-closed verdict rules
- auton.md -- Autonomy diagnostic for AO worker coverage

**Auton command** (.claude/commands/auton.md): Slash command wrapper for the auton skill.

**.gitignore update**: Unignored .claude/ directory, added specific excludes for ephemeral files (scheduled_tasks.lock, worktrees/)
