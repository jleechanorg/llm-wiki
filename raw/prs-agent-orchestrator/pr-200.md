# PR #200: [agento] docs: close bd-ara.3 — stale session fixes via bd-s4t

**Repo:** jleechanorg/agent-orchestrator
**Merged:** 2026-03-26
**Author:** jleechan2015
**Stats:** +13/-13 in 1 files

## Summary
bd-ara.3 reported that sessions for merged/closed PRs were accumulating past the 15-session spawn gate because:
1. lifecycle-manager transitioned to "merged" but never sent a kill signal to the tmux runtime
2. session-reaper skipped terminal-status sessions (including merged)
3. maxKillsPerRun was 5 — too slow for 25+ zombie sessions

## Background
bd-ara.3 reported that sessions for merged/closed PRs were accumulating past the 15-session spawn gate because:
1. lifecycle-manager transitioned to "merged" but never sent a kill signal to the tmux runtime
2. session-reaper skipped terminal-status sessions (including merged)
3. maxKillsPerRun was 5 — too slow for 25+ zombie sessions

## Raw Body
## Background

bd-ara.3 reported that sessions for merged/closed PRs were accumulating past the 15-session spawn gate because:
1. lifecycle-manager transitioned to "merged" but never sent a kill signal to the tmux runtime
2. session-reaper skipped terminal-status sessions (including merged)
3. maxKillsPerRun was 5 — too slow for 25+ zombie sessions

## Goals

Close the bead with evidence that the fixes are implemented.

## Tenets

- Prefer minimal change — the code work was already done in bd-s4t
- Document the evidence so future developers understand what closed the issue

## High-level description

All three fixes from the issue are already implemented in the codebase:

| Fix | Implementation | Evidence |
|-----|----------------|---------|
| (a) lifecycle-manager kills tmux on terminal transition | `lifecycle-manager.ts` lines 1198, 1253, 1413 — calls `sessionManager.kill()` which calls `runtimePlugin.destroy()` | Commits 8edfef94, 2b1894e8 |
| (b) reaper kills zombie sessions (merged/closed PR, non-terminal status) | `session-reaper-extensions.ts:46` — `checkAndKillZombie()` runs BEFORE terminal-status skip | Commits 540bb7ec, 5ce0495c |
| (c) maxKillsPerRun raised to 15 | `session-reaper.ts:78` — default changed from 5→15 | Commit 2b1894e8 |

## Testing

957 tests pass (`pnpm -C packages/core test`):
- `session-reaper.test.ts`: zombie detection tests for merged + closed PR states
- `session-reaper-edge-cases.test.ts`: additional edge case coverage

## Verification

- [x] All 957 core tests pass
- [x] Zombie detection runs before terminal-status skip
- [x] lifecycle-manager calls kill() on terminal transitions
- [x] maxKillsPerRun = 15 in default config

Closes #bd-ara.3

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Low risk: changes are limited to internal `.beads/issues.jsonl` bookkeeping (status/metadata updates) with no runtime code impact.
> 
> **Overview**
> Closes `bd-ara.3` in `.beads/issues.jsonl` by marking it `closed`, adding a closure times
