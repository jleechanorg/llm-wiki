# PR #61: [agento] fix(lifecycle): use checkMergeGate() for auto-merge instead of getMergeability()

**Repo:** jleechanorg/agent-orchestrator
**Merged:** 2026-03-21
**Author:** jleechan2015
**Stats:** +144/-179 in 6 files

## Summary
- replace auto-merge precheck in lifecycle manager to use `checkMergeGate()` (full 6-condition gate) instead of `scm.getMergeability()`
- source merge gate config from `project.mergeGate` with default `{ enabled: true }`
- update lifecycle-manager tests to assert full merge gate inputs are queried and auto-merge still executes when gate passes

## Raw Body
## Summary
- replace auto-merge precheck in lifecycle manager to use `checkMergeGate()` (full 6-condition gate) instead of `scm.getMergeability()`
- source merge gate config from `project.mergeGate` with default `{ enabled: true }`
- update lifecycle-manager tests to assert full merge gate inputs are queried and auto-merge still executes when gate passes

## Testing
- pnpm -C packages/core test -- lifecycle-manager.test.ts merge-gate.test.ts

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Changes auto-merge behavior by default-enabling merge-gate config and allowing `auto-merge` reactions, which can affect when PRs get merged. Also adds filesystem symlink guards and CI-specific setup behavior; low blast radius but touches automation paths.
> 
> **Overview**
> **Auto-merge configuration/behavior shifts** by removing the config-time ban on `action: "auto-merge"` reactions and adding tests asserting it’s allowed, with enforcement deferred to runtime merge-gate checks.
> 
> **Merge gate defaults change** by default-enabling `mergeGate.enabled` in config schema.
> 
> **Safety/CI hardening:** the Claude Code plugin now refuses to write/chmod hook files (`metadata-updater.sh`, `settings.json`) if they are symlinks, `setup-extended.sh` skips `ao start` in CI to avoid port conflicts, onboarding integration test writes a minimal config before running setup, and `.gitleaks.toml` allowlist paths are updated to escaped regex patterns.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit fc1ea52917e286f85fbde680d1f734eb27476e4a. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
