# PR #43: fix(agent-base): add vitest, fix @jleechanorg scope imports in tests

**Repo:** jleechanorg/agent-orchestrator
**Merged:** 2026-03-21
**Author:** jleechan2015
**Stats:** +305/-1 in 6 files

## Summary
The agent-base plugin was missing a test script and vitest devDependency, and its test file had stale @composio/* import paths after the npm scope rename to @jleechanorg/*.

## Background
The agent-base plugin was missing a test script and vitest devDependency, and its test file had stale @composio/* import paths after the npm scope rename to @jleechanorg/*.

## Raw Body
## Background
The agent-base plugin was missing a test script and vitest devDependency, and its test file had stale @composio/* import paths after the npm scope rename to @jleechanorg/*.

## Goals
- Make pnpm --filter @jleechanorg/ao-plugin-agent-base test runnable
- Correct all import paths in tests to use the current @jleechanorg scope

## Tenets
- Minimal scope: only touch agent-base test infra, no logic changes
- No @composio/* references in this fork test files

## High-level description of changes
- Added vitest as devDependency and test script to agent-base/package.json
- Fixed 9 import paths in index.test.ts from @composio/ao-* to @jleechanorg/ao-*
- Restored index.test.ts which was absent from main
- Updated .beads/issues.jsonl and CLAUDE.md

## Testing
- Unit: index.test.ts covers plugin export shape and toAgentProjectPath path encoding
- Verified import paths match published scope in package.json

## Low-level details
- **packages/plugins/agent-base/package.json**: added vitest devDep and test script
- **packages/plugins/agent-base/src/index.test.ts**: fix all 9 @composio/* imports; restore file
- **pnpm-lock.yaml**: lockfile update for new vitest dep
- **.beads/issues.jsonl**: new bead entries
- **CLAUDE.md**: session-derived doc updates

Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Low risk: changes are limited to test infrastructure/docs and add no runtime logic changes, aside from updating dependencies and lockfile.
> 
> **Overview**
> Makes `@jleechanorg/ao-plugin-agent-base` testable by adding a `vitest`-backed `test` script/devDependency and updating `pnpm-lock.yaml` accordingly.
> 
> Restores a unit test suite (`src/index.test.ts`) that exercises the exported factory (`createAgentPlugin`) and helpers like `toAgentProjectPath`, using mocked Node FS/OS APIs.
> 
> Updates fork docs (`CLAUDE.md`) with enforced test-tier naming rules and a fail-closed evidence claim-class matrix, 
