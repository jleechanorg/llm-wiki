# PR #394: [agento] fix(evidence-gate): validate Terminal media URL is real artifact for integration+ claims (bd-vidcap)

**Repo:** jleechanorg/agent-orchestrator
**Merged:** 2026-04-11
**Author:** jleechan2015
**Stats:** +452/-131 in 10 files

## Summary
(none)

## Raw Body
## Summary (bd-vidcap Step 4)

After Fix 2 (agentRules / tmux-video-evidence callers via bd-4ze23), the gate now:

1. **FAIL** if Terminal media has **no HTTPS URL** or **N/A-only** for integration+ (Fix 1 behavior retained).
2. **FAIL** if URL is **example.com** placeholder (scoped to extracted URL).
3. **PASS** with **`::warning`**: HTTPS URL present but **not** matching a known artifact pattern — advisory only.
4. **Known patterns**: `.mp4`, `.gif`, `.cast`, `.webm`, `.mov`, `gist.github.com`, `asciinema.org`, `github.com/user-attachments/assets/`.
5. **Caption** must still mention **tmux** or **terminal**.

**Aligned:** `wholesome.yml` Evidence Has Media Attachment (same extraction + WARN line in logs).

**Docs:** `roadmap/video-evidence-roadmap.md`, `docs/evidence/strong-evidence-standard.md` (captioned terminal media example), `docs/evidence/README.md` pointer.

**Tests:** `scripts/test-evidence-gate-patterns.sh` + `pnpm --filter @jleechanorg/ao-core test` (set `AO_WHOLESOME_PR_TITLE` locally for wholesome PR-title assertions).

## Evidence

**Claim class**: unit  
**Verdict**: PASS

**Claim floor override**: Self-referential / CI-policy PR: touches `evidence-gate.yml`, `scripts/test-evidence-gate-patterns.sh`, and `package.json` (claim-floor extensions). Validation is via wholesome + `scripts/test-evidence-gate-patterns.sh` + `pnpm --filter @jleechanorg/ao-core test` as described in Summary/Tests — unit claim is appropriate.

**Real command output**:

```bash
$ bash scripts/test-evidence-gate-patterns.sh
OK: mp4 + tmux caption
OK: user-attachments
OK: gist .cast URL
OK: asciinema
OK: png URL triggers unknown-pattern path (warn-only in gate)
OK: markdown image mp4
All evidence-gate pattern tests passed.
```

**Terminal media**: N/A — unit claim (not integration+).

(Workflow-only change; validated via regression tests demonstrating correct URL extraction, known-pattern detection, and caption validation after fixing word-split bug in `has_tmux_caption()`.)

## 
