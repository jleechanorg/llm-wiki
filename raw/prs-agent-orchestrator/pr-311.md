# PR #311: [agento] fix: metadata-updater.sh regex fix and simplification

**Repo:** jleechanorg/agent-orchestrator
**Merged:** 2026-03-30
**Author:** jleechan2015
**Stats:** +17/-29 in 1 files

## Summary
The metadata-updater.sh PostToolUse hook had 3 issues: a regex that could over-match env-assignment prefixes, unnecessary character escaping in sed, and overly complex git verification for branch detection.

## Background
The metadata-updater.sh PostToolUse hook had 3 issues: a regex that could over-match env-assignment prefixes, unnecessary character escaping in sed, and overly complex git verification for branch detection.

## Raw Body
## Background
The metadata-updater.sh PostToolUse hook had 3 issues: a regex that could over-match env-assignment prefixes, unnecessary character escaping in sed, and overly complex git verification for branch detection.

## Goals
- Fix regex to correctly stop at `=` when stripping env-assignment prefixes
- Simplify sed value escaping (remove unneeded `|` from escape class)
- Simplify checkout/switch branch detection

## Tenets
- Minimal diff; preserve all functional behavior
- Pattern-based branch detection is sufficient guard for metadata updates

## Testing
- `bash -n .claude/metadata-updater.sh` passes (syntax check)

## Evidence
**Claim class**: unit
**Verdict**: PASS

```sh
$ bash -n .claude/metadata-updater.sh && echo "exit: \$?"
exit: 0
```

## Verification
- Hook syntax validated locally

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->
## Summary by CodeRabbit

* **Refactor**
  * Improved branch detection and token filtering during checkout/switch operations to make metadata updates more reliable.
  * Simplified validation logic that decides which branch tokens are accepted for metadata updates.
  * Streamlined metadata value escaping to reduce complexity while preserving correct escaping behavior.
<!-- end of auto-generated comment: release notes by coderabbit.ai -->

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Changes the PostToolUse hook’s parsing for `git checkout`/`git switch` branch updates; incorrect regex heuristics could cause missed or spurious `branch` metadata updates. Impact is limited to session metadata writes, not core runtime behavior.
> 
> **Overview**
> Tightens `metadata-updater.sh`’s metadata writing logic by simplifying how branch names are derived from `git checkout`/`git switch` commands, removing the prior `git rev-parse`/`symbolic-ref` verification and replacing it with direct token capture plus lightweight heuristics to avoid path/file-like operands.
> 
> Also adjusts `update_metadata_key`
