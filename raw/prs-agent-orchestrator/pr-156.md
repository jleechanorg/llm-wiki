# PR #156: docs(hooks): clean up metadata-updater to PostToolUse-only (bd-24k)

**Repo:** jleechanorg/agent-orchestrator
**Merged:** 2026-03-24
**Author:** jleechan2015
**Stats:** +6/-4 in 1 files

## Summary
- Removes PreToolUse enforcement references from header comment (hook is PostToolUse-only)
- Removes unused `AO_HOOK_EVENT_NAME` env-var fallback that was dead code

## Raw Body
## Summary
- Removes PreToolUse enforcement references from header comment (hook is PostToolUse-only)
- Removes unused `AO_HOOK_EVENT_NAME` env-var fallback that was dead code

## Testing
- Local hook execution verified (no functional changes to actual behavior)

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Documentation-only change to `.claude/metadata-updater.sh` header comments; no runtime logic is modified, so behavioral risk is minimal.
> 
> **Overview**
> Updates the header comment in `.claude/metadata-updater.sh` to reflect that the hook participates in both `PreToolUse` (blocking `gh pr merge` unless `AO_ALLOW_GH_PR_MERGE=1`) and `PostToolUse` (writing PR/branch metadata and marking merges only when allowed and in `PostToolUse`).
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 144a4796e52f61b7f188c4569b027f57de48d9d5. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->
## Summary by CodeRabbit

* **Chores**
  * Updated script documentation to clarify hook registration for pre- and post-tool events and when metadata updates run.
  * Clarified that merge-related metadata updates are only applied during post-tool events and when merging is explicitly allowed.
  * No runtime logic or behavior was changed; this is documentation-only clarification.
<!-- end of auto-generated comment: release notes by coderabbit.ai -->
