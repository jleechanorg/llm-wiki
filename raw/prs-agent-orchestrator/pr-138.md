# PR #138: [agento] fix: PostToolUse guard exemption for metadata-updater

**Repo:** jleechanorg/agent-orchestrator
**Merged:** 2026-03-24
**Author:** jleechan2015
**Stats:** +12/-5 in 1 files

## Summary
The `metadata-updater.sh` hook is registered on both PreToolUse and PostToolUse hooks, but had no way to distinguish between them. This caused the merge guard to fire on PostToolUse events (blocking legitimate metadata updates), and could mark `status=merged` before the merge command actually succeeded.

## Raw Body
## Summary

The `metadata-updater.sh` hook is registered on both PreToolUse and PostToolUse hooks, but had no way to distinguish between them. This caused the merge guard to fire on PostToolUse events (blocking legitimate metadata updates), and could mark `status=merged` before the merge command actually succeeded.

## Changes

- **`.claude/metadata-updater.sh`**: Added `hook_event_name` parsing to detect PreToolUse vs PostToolUse. Merge guard now only blocks on PreToolUse (allow PostToolUse to fall through). `status=merged` metadata update is gated on PostToolUse to ensure the merge actually succeeded before recording it.

## Testing

- Logic traced through PreToolUse and PostToolUse execution paths
- Guard fires correctly on PreToolUse, exempts PostToolUse
- Metadata update only triggers after confirmed success

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Adjusts enforcement around `gh pr merge` and metadata updates in a hook, which could inadvertently block/allow merges or mis-record merge status if the event detection is wrong.
> 
> **Overview**
> Updates `.claude/metadata-updater.sh` to parse `hook_event_name` so the hook can distinguish *PreToolUse* vs *PostToolUse* executions.
> 
> The `gh pr merge` guard now only denies merges during *PreToolUse* when not explicitly allowed, and the `status=merged` metadata write is gated to *PostToolUse* (and `AO_ALLOW_GH_PR_MERGE=1`) to avoid recording a merge before it succeeds.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit d8e055d0a2c133c9b75b36cb3488d57aca96c091. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->

## Summary by CodeRabbit

* **Chores**
  * Updated internal metadata handling for GitHub pull request merge operations with refin
