# PR #153: [agento] fix: clean up metadata-updater.sh hook comments and dead code

**Repo:** jleechanorg/agent-orchestrator
**Merged:** 2026-03-24
**Author:** jleechan2015
**Stats:** +4/-14 in 1 files

## Summary
- Removes stale PreToolUse/merge-policy enforcement comments from `.claude/metadata-updater.sh` — this is a PostToolUse-only hook
- Removes dead `AO_HOOK_EVENT_NAME` env-var fallback since the JSON `hook_event_name` field is always present

## Raw Body
## Summary
- Removes stale PreToolUse/merge-policy enforcement comments from `.claude/metadata-updater.sh` — this is a PostToolUse-only hook
- Removes dead `AO_HOOK_EVENT_NAME` env-var fallback since the JSON `hook_event_name` field is always present

## Testing
- No behavioral change — just comment and dead-code cleanup
- Hook script still runs correctly after rebase onto `origin/main`

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Low risk: only removes outdated comments and a redundant `AO_HOOK_EVENT_NAME` fallback path; behavior should remain unchanged unless a hook runner omits `hook_event_name` from stdin JSON.
> 
> **Overview**
> Updates `.claude/metadata-updater.sh` documentation to reflect that it is a *PostToolUse-focused* metadata updater, removing stale references to merge-policy enforcement.
> 
> Removes dead code that backfilled `hook_event` from `AO_HOOK_EVENT_NAME`, relying solely on the `hook_event_name` field from the hook’s stdin JSON.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 4241d6da2f58d388f4090a6c8d558fd289bd76a5. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->

## Summary by CodeRabbit

* **Bug Fixes**
  * Modified hook event handling to only populate from incoming data, removing reliance on environment variable fallback. This affects metadata update behavior in specific scenarios.

<!-- end of auto-generated comment: release notes by coderabbit.ai -->
