# PR #193: [agento] fix(prose-polish): address CR follow-up review comments

**Repo:** jleechanorg/agent-orchestrator
**Merged:** 2026-03-26
**Author:** jleechan2015
**Stats:** +147/-29 in 4 files

## Summary
Second follow-up to PR #183 / #186, addressing remaining review comments on the prose-polish plugin:

- **safePath** (mcp-tools.ts): Support workspace-relative paths (README.md, ./docs/ch1.md) by resolving them via `path.resolve()`; use `node:path.isAbsolute`/`normalize` for segment-level `..` traversal check instead of substring rejection; Windows absolute paths (C:\) now work
- **fixLine** (fixer.ts): Preserve leading indentation by extracting/restoring leading whitespace; collapse only intern

## Test Plan
- [x] Unit tests pass (13/13)
- [x] Build succeeds
- [x] CI: all checks pass

Closes #186 (follow-up)

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Touches file-path validation and file-writing behavior via `safePath`/MCP tools, which can affect what files are read/fixed and has security implications. Logic is small and now covered by new unit tests, reducing risk.
> 
> **Overview**
> Improves `prose-polish` auto-fixin

## Raw Body
## Summary

Second follow-up to PR #183 / #186, addressing remaining review comments on the prose-polish plugin:

- **safePath** (mcp-tools.ts): Support workspace-relative paths (README.md, ./docs/ch1.md) by resolving them via `path.resolve()`; use `node:path.isAbsolute`/`normalize` for segment-level `..` traversal check instead of substring rejection; Windows absolute paths (C:\) now work
- **fixLine** (fixer.ts): Preserve leading indentation by extracting/restoring leading whitespace; collapse only internal whitespace runs; trim residual artifact spaces after filler word removal

## Resolved Comments
| Reviewer | File | Comment | Resolution |
|---|---|---|---|
| chatgpt-codex-connector[bot] | mcp-tools.ts | P2: safePath rejects workspace-relative paths | Fixed — relative paths resolved via path.resolve |
| Copilot | fixer.ts | fixLine() collapses whitespace, breaking indentation | Fixed — leadingWs preserved, internal collapse only |
| Copilot | mcp-tools.ts | safePath rejects '..' substring, requires /-prefixed | Fixed — node:path segment-level traversal check |
| cursor[bot] | fixer.ts | Removing trim leaves artifact spaces | Fixed — trim() after filler removal cleans artifacts |

## Test plan
- [x] Unit tests pass (13/13)
- [x] Build succeeds
- [x] CI: all checks pass

Closes #186 (follow-up)

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Touches file-path validation and file-writing behavior via `safePath`/MCP tools, which can affect what files are read/fixed and has security implications. Logic is small and now covered by new unit tests, reducing risk.
> 
> **Overview**
> Improves `prose-polish` auto-fixing so `fixLine` preserves leading indentation and Markdown hard-breaks (2+ trailing spaces) while still removing filler words and collapsing only internal whitespace.
> 
> Updates MCP `safePath` validation to block only true `..` path-traversal segments while supporting workspace-rel
