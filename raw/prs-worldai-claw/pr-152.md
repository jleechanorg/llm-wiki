# PR #152: fix(gateway): operator scope header + model name normalization + agent routing (wc-4sn, wc-6ol, wc-on8)

**Repo:** jleechanorg/worldai_claw
**Merged:** 2026-03-30
**Author:** jleechan2015
**Stats:** +330/-33 in 14 files

## Summary
Three related gateway issues were causing turn pipeline failures and lifecycle test failures:
- **wc-4sn**: Turn pipeline 502 — SSE tests skipped due to OPENCLAW_UNAVAILABLE
- **wc-6ol**: OpenClaw model contract mismatch — backend stripping model prefix causing HTTP 400
- **wc-on8**: Lifecycle acceptance video test needed to prove end-to-end turn flow works

Root cause: backend used campaign UUID as gateway agent routing key. The local OpenClaw gateway waits 120s for a session registered to that

## Background
Three related gateway issues were causing turn pipeline failures and lifecycle test failures:
- **wc-4sn**: Turn pipeline 502 — SSE tests skipped due to OPENCLAW_UNAVAILABLE
- **wc-6ol**: OpenClaw model contract mismatch — backend stripping model prefix causing HTTP 400
- **wc-on8**: Lifecycle acceptance video test needed to prove end-to-end turn flow works

Root cause: backend used campaign UUID as gateway agent routing key. The local OpenClaw gateway waits 120s for a session registered to that UUID → times out → OPENCLAW_UNAVAILABLE.

## Raw Body
## Background

Three related gateway issues were causing turn pipeline failures and lifecycle test failures:
- **wc-4sn**: Turn pipeline 502 — SSE tests skipped due to OPENCLAW_UNAVAILABLE
- **wc-6ol**: OpenClaw model contract mismatch — backend stripping model prefix causing HTTP 400
- **wc-on8**: Lifecycle acceptance video test needed to prove end-to-end turn flow works

Root cause: backend used campaign UUID as gateway agent routing key. The local OpenClaw gateway waits 120s for a session registered to that UUID → times out → OPENCLAW_UNAVAILABLE.

## Goals
1. Fix OPENCLAW_UNAVAILABLE errors in turn pipeline SSE tests
2. Fix model name normalization stripping openclaw/ prefix
3. Prove lifecycle: setup → campaign → 3 real LLM turns with 12/12 evidence requirements

## Tenets
- Real LLM tests only — no mocks
- Minimal scope: fix the routing, not the architecture

## High-level description of changes

**`openclawClient.ts`**:
- Added `x-openclaw-scopes: operator.write` header (required by gateway)
- `_normalizeModelForGateway`: no-op — send model name unchanged (was stripping `openclaw/` prefix causing HTTP 400)
- `_buildGatewayAgentId`: use `requestId` (stream_xxx) not `campaignId` (UUID) as gateway routing key — stream IDs route via gateway's fast direct-Claude-API path
- `AbortSignal.timeout`: 120s → 300s for complex worldbuilding prompts (LLM 60-120s inference)

**`test_turn_pipeline.py`**:
- REQUEST_TIMEOUT_SECONDS: 60 → 180
- test_turn_submission_returns_sse: streaming:True to match SSE client

**`test_full_lifecycle_video.py`**:
- timeout_s: 90 → 180
- `_scroll_chat_to_latest`: best-effort with 5s timeout (canvas animation causes instability)

## Testing

### MCP Turn Pipeline (wc-4sn / wc-6ol)
- `test_turn_submission_creates_session` PASSED
- `test_turn_submission_returns_sse` PASSED (23 SSE chunks)
- `test_turn_response_includes_narrative` PASSED (scene_text with vivid prose)
- `test_session_state_persists_after_turn` PASSED

### Lifecycle Acceptance (wc-on
