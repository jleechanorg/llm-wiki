# PR #71: fix(mobile): remove networkClient singleton that bypasses AppContext

**Repo:** jleechanorg/worldai_claw
**Merged:** 2026-03-26
**Author:** jleechan2015
**Stats:** +9/-1596 in 2 files

## Summary
Bead `worldai_claw-6w9`: The exported `networkClient` singleton in `packages/mobile/src/services/networkClient.ts` used hardcoded `localhost:3000` and empty auth token, bypassing `AppContext` `baseUrl`/`authToken` settings. Any screen importing this singleton would not reflect user-configured backend URLs.

## Background
Bead `worldai_claw-6w9`: The exported `networkClient` singleton in `packages/mobile/src/services/networkClient.ts` used hardcoded `localhost:3000` and empty auth token, bypassing `AppContext` `baseUrl`/`authToken` settings. Any screen importing this singleton would not reflect user-configured backend URLs.

## Raw Body
## Background

Bead `worldai_claw-6w9`: The exported `networkClient` singleton in `packages/mobile/src/services/networkClient.ts` used hardcoded `localhost:3000` and empty auth token, bypassing `AppContext` `baseUrl`/`authToken` settings. Any screen importing this singleton would not reflect user-configured backend URLs.

## Discovery Step (evidence of existing PR check)

- Searched all open PRs via REST API (`/repos/jleechanorg/worldai_claw/pulls?state=all`) — **no existing PR** references bead `worldai_claw-6w9` or its head branch `feat/worldai_claw-6w9`.
- This is a **new PR** (not updated existing PR).

## Goals

- [x] Remove the hardcoded `networkClient` singleton export
- [x] Prevent any future code from accidentally importing the singleton and bypassing `AppContext`

## Tenets

- Minimal change — only remove dead code, don't refactor unrelated files
- `CampaignsScreen` already correctly uses `createNetworkClientFromConfig()` — no changes needed there

## High-level description of changes

Removed the `networkClient` singleton export and its `DEFAULT_BACKEND_URL`/`DEFAULT_AUTH_TOKEN` import from `networkClient.ts`. Updated the docblock on `createNetworkClientFromConfig()` to clarify it is the only correct way to get a network client. No production files imported the singleton — the fix closes the trap for future misuse.

## Testing

- Audit: Confirmed zero production files import the singleton `networkClient` (only `GameNetworkClient` class and factory functions are imported)
- `CampaignsScreen` already uses `createNetworkClientFromConfig(baseUrl, authToken)` from `AppContext`
- `GameScreen` uses `AppContext` values directly in fetch calls — unaffected
- TypeScript compilation skipped (ts-jest/expo deps not installed in this environment); changes are additive removals only — no new types or imports

## Low-level details

- **`packages/mobile/src/services/networkClient.ts`**: Removed `DEFAULT_BACKEND_URL`/`DEFAULT_AUTH_TOKEN` import and `export const networkCli
