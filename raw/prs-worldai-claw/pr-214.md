# PR #214: [antig] fix: companion state mutations not idempotent on tick replay

**Repo:** jleechanorg/worldai_claw
**Merged:** 2026-04-04
**Author:** jleechan2015
**Stats:** +72/-34 in 7 files

## Summary
Tick replay could cause state divergence because updateCompanion and UPDATE sessions SET state ran unconditionally on every tick while action inserts use INSERT OR IGNORE.

## Background
Tick replay could cause state divergence because updateCompanion and UPDATE sessions SET state ran unconditionally on every tick while action inserts use INSERT OR IGNORE.

## Raw Body
## Background
Tick replay could cause state divergence because updateCompanion and UPDATE sessions SET state ran unconditionally on every tick while action inserts use INSERT OR IGNORE.

## Goals
Ensure tick replay does not insert new actions blindly and instead becomes fully idempotent.

## Tenets
- Reliable deterministic state replication.

## High-level description
Modified saveCompanionAction to return a boolean indicating successful insert and updated faction_simulator to conditionally update state. Also fixed app.ts party creation endpoint responding with camelCase.

## Testing
Ran backend test suite.

## Low-level details

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Medium risk because it changes SQLite persistence semantics for companion actions and tweaks party join/leave request/response shapes, which could affect replay behavior and existing clients if they rely on the old fields.
> 
> **Overview**
> Prevents tick replays from silently diverging companion action history by making `SQLiteRepository.saveCompanionAction` idempotent: it now returns `true` on first insert, detects deterministic-id replays (ignoring `summary` drift) and returns `false` instead of generating a new row.
> 
> Updates `faction_simulator` to always persist companion state updates while relying on the action insert’s idempotency for replay safety, and adds a regression test covering the replay semantics.
> 
> Standardizes party endpoints to camelCase (`partyId`, `sessionIds`) and ensures `updated_at` is maintained on party/session updates; small UI polish includes wrapping the mobile tabs in `BottomSheetModalProvider` and adjusting web choice-card hover animation behavior.
> 
> <sup>Reviewed by [Cursor Bugbot](https://cursor.com/bugbot) for commit 7e3c72aa8d7bb04287f71974d47d569c84246b08. Bugbot is set up for automated code reviews on this repo. Configure [here](https://www.cursor.com/dashboard/bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated com
