# PR #225: [antig] Improve smoke test evidence to capture full turn envelope and verify contracts

**Repo:** jleechanorg/worldai_claw
**Merged:** 2026-04-07
**Author:** jleechan2015
**Stats:** +223/-122 in 2 files

## Summary
Implements remaining requirements for wc-t5s.

## Background
Implements remaining requirements for wc-t5s.

## Raw Body
## Background
Implements remaining requirements for wc-t5s.

## Goals
- Enhance smoke test envelope verification.

## Testing
- Modified `testing_mcp/test_worldai_claw_api.py`

## Low-level details
- Added choices count verification
- Added llm_diagnostics and rules_applied verification
- Added state_delta and mechanic_requests checks

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Adds a new self-hosted CI job that hits a local gateway and always uploads artifacts, and tightens integration-test response contracts/evidence bundling; failures are non-blocking but could increase CI noise or expose flaky dependencies on live LLM/gateway behavior.
> 
> **Overview**
> Adds a new `smoke-test` GitHub Actions job (non-fork, self-hosted runner) that runs `pytest` against the local gateway at `127.0.0.1:18789`, warns if `/health` is unreachable, and always uploads run-scoped smoke evidence artifacts.
> 
> Strengthens `testing_mcp` integration smoke coverage by validating more of the `/turn` response envelope (e.g., `choices` must be non-empty and `diagnostics` must exist) and refactors `test_smoke_campaign_3_actions` to always emit a canonical evidence bundle (with provenance and per-scenario pass/fail summary) even when assertions fail.
> 
> <sup>Reviewed by [Cursor Bugbot](https://cursor.com/bugbot) for commit c23c6196b5c0b36683b9687fd7dcac62a85d6e7a. Bugbot is set up for automated code reviews on this repo. Configure [here](https://www.cursor.com/dashboard/bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->
## Summary by CodeRabbit

* **Tests**
  * Stricter API response checks: responses must include diagnostics and a non-empty choices list; smoke test now collects evidence and summarizes per-scenario pass rates and history.

* **Chores**
  * CI smoke-testing added for non-fork builds to probe gateway availability; failures won’t block the workflow and test artifacts are always uploaded with 7-day 
