# PR #145: [agento] fix(web): align API terminology with backend — use worlds not campaigns

**Repo:** jleechanorg/worldai_claw
**Merged:** 2026-03-29
**Author:** jleechan2015
**Stats:** +55/-49 in 6 files

## Summary
- Fix `mapWorldCollectionResponse` to use `worlds` key only (removed `campaigns` fallback)
- Update Dashboard test mocks to use `worlds` key (was using `campaigns` key)
- Update App test mocks to use `worlds` key
- Update NewCampaignWizard UI copy from "Campaign" to "World" terminology throughout
- Update all affected tests to match new UI copy

## Raw Body
## Summary
- Fix `mapWorldCollectionResponse` to use `worlds` key only (removed `campaigns` fallback)
- Update Dashboard test mocks to use `worlds` key (was using `campaigns` key)
- Update App test mocks to use `worlds` key
- Update NewCampaignWizard UI copy from "Campaign" to "World" terminology throughout
- Update all affected tests to match new UI copy

## Testing
- `npm run test:web` — 154/154 tests pass

Closes wc-a7t

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Tightens the client-side API contract by removing the legacy `campaigns` fallback and throwing on unexpected list payloads, which could surface runtime errors if any environment still returns the old shape. UI/test text-only renames are low risk, but the stricter mapper affects dashboard/mobile world loading.
> 
> **Overview**
> Aligns frontend/shared terminology and payload expectations to **use `worlds` (not `campaigns`)** for world list responses.
> 
> `mapWorldCollectionResponse` now only accepts `{ worlds: [...] }` (or a raw array) and **throws** on object payloads missing `worlds`, and the New Campaign wizard copy/CTAs are updated from *Campaign* to *World* throughout. Tests are updated accordingly to mock `{ worlds: [...] }` and assert the new UI strings/navigation ids.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 3494d2d222b9a7f515ed7744310eddb24d3ee4ef. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->
## Summary by CodeRabbit

* **Refactor**
  * Replaced "Campaign" wording with "World" across the setup wizard, dashboard, and UI flows (labels, prompts, validation messages, summary headings, default untitled name, and final CTA).

* **Bug Fixes**
  * Data mapping now consistently uses world items rather than falling back to legacy campaign items; object responses missing worlds
