# PR #148: [agento] feat(web): validate world ID before navigating from wizard

**Repo:** jleechanorg/worldai_claw
**Merged:** 2026-03-29
**Author:** jleechan2015
**Stats:** +163/-13 in 3 files

## Summary
Fixes a race condition / bad navigation in the `NewCampaignWizard` "Launch World" flow. When the backend API response was malformed (no `id` field), the app silently navigated to `/game/undefined` instead of surfacing an error to the user.

## Test Plan
- [x] Manual: Dashboard → New Campaign → Dragon Knight → Launch → navigates to `/game/<valid-id>`
- [x] Manual: malformed API response shows error, no navigation to `/game/undefined`
- [x] `vitest run` — pre-existing test infrastructure issue (tracked separately)

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Modifies the `NewCampaignWizard` launch flow and its error handling, which directly affects world creation and r

## Raw Body
## Summary

Fixes a race condition / bad navigation in the `NewCampaignWizard` "Launch World" flow. When the backend API response was malformed (no `id` field), the app silently navigated to `/game/undefined` instead of surfacing an error to the user.

## Changes

### `packages/web/src/screens/NewCampaignWizard.tsx`
- `handleLaunch()` now validates `data.id` exists after a successful `POST /api/worlds`
- Throws if `id` is missing so the catch block shows an error state instead of navigating to `/game/undefined`
- Improved HTTP failure message to include status code and text

### `packages/web/tests/newCampaignWizard.test.tsx`
Added 3 new API error handling tests:
- **HTTP failure (500)** — error shown, no navigation
- **Missing `id` in response** — error shown ("World created but server returned no ID"), no navigation
- **Network error** — error shown, no navigation

### `packages/web/tests/dashboard.test.tsx`
- Rewrote navigation tests with `vi.mock('react-router-dom')` pattern (proper approach vs. `window.location.pathname` checks)

## Testing

Route flow verified manually: `Dashboard → New Campaign → Dragon Knight → Launch → /game/:id` with valid id navigates correctly; malformed responses surface error.

> ⚠️ **Known gap**: All `@testing-library/react` tests fail with `document is not defined` — this is a pre-existing vitest 2.x + Node 24 + jsdom 25 compatibility issue affecting the entire web test suite. The test file was already broken before these changes. Tracking separately.

## Test plan

- [x] Manual: Dashboard → New Campaign → Dragon Knight → Launch → navigates to `/game/<valid-id>`
- [x] Manual: malformed API response shows error, no navigation to `/game/undefined`
- [x] `vitest run` — pre-existing test infrastructure issue (tracked separately)

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Modifies the `NewCampaignWizard` launch flow and its error handling, which directly affe
