# PR #31: feat: Living Companions Phases 1-3 — autonomous companions with CRUD, DM force-edit, and catchup

**Repo:** jleechanorg/worldai_claw
**Merged:** 2026-03-05
**Author:** jleechan2015
**Stats:** +4030/-2497 in 46 files

## Summary
(none)

## Raw Body
## What This PR Does

Implements Living Companions (Phases 1–3) from `roadmap/roadmap_living_companions.md` — turning static party followers into persistent, autonomous agents that act while the player is offline and report back on return.

---

## Implemented Features

### Phase 1 — Core Companion System
New API endpoints and storage for managing player-owned companions:

| Endpoint | Description |
|----------|-------------|
| `POST /sessions/:id/companions` | Create companion (name, archetype, backstory) |
| `GET /sessions/:id/companions` | List all companions for a session |
| `GET /sessions/:id/companions/:companionId` | Fetch single companion |
| `PATCH /sessions/:id/companions/:companionId` | Update companion (name, autonomy_mode, stats, inventory) |
| `DELETE /sessions/:id/companions/:companionId` | Remove companion |

- `Companion` type and `CompanionAutonomyMode` enum added to `world/faction_types.ts`
- `companions` added to `ALLOWED_STATE_KEYS` in `game/state_reducer.ts`
- SQLite tables: `companions`, `companion_actions` (append-only action log)
- IDOR protection on all single-companion endpoints (403 if companion not in caller's session)

### Phase 2 — Autonomy Engine
Companions act deterministically (no extra LLM call) during the existing world scheduler tick:

- 7 autonomy modes: `passive`, `guard`, `explorer`, `trader`, `aggressive`, `quester`, `balanced`
- `resolveCompanionAction()` in `faction_simulator.ts` — seeded RNG, outcome tables per mode
- `processCompanionTicks()` runs inside `runSimulation()` → triggered when ≥4 elapsed in-game hours pass
- Action type mapping: `explorer→explore`, `trader→trade`, `balanced→autonomous`, `guard→guard`, `aggressive→combat`, `quester→quest`
- Companion events injected into the world event stream
- **Catch-up endpoint**: `GET /sessions/:id/companions/catchup`
  - Returns actions grouped by companion since last seen (cursor stored in `sessions.companions_last_seen`)
  - Cursor advances on each call — idempotent, n
