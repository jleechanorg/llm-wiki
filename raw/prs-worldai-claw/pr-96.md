# PR #96: test: snake_case regression guard for companion action deltas (WC-a7g)

**Repo:** jleechanorg/worldai_claw
**Merged:** 2026-03-26
**Author:** jleechan2015
**Stats:** +66/-0 in 1 files

## Summary
Task WC-a7g flagged a potential snake_case mismatch in companion action deltas — `actionDelta.bond_level` / `current_location` vs the camelCase `Companion` interface (`bondLevel`, `currentLocation`).

## Background
Task WC-a7g flagged a potential snake_case mismatch in companion action deltas — `actionDelta.bond_level` / `current_location` vs the camelCase `Companion` interface (`bondLevel`, `currentLocation`).

## Raw Body
## Background
Task WC-a7g flagged a potential snake_case mismatch in companion action deltas — `actionDelta.bond_level` / `current_location` vs the camelCase `Companion` interface (`bondLevel`, `currentLocation`).

## Goals
- Confirm `resolveCompanionAction` and `processCompanionTicks` use camelCase field names matching the `Companion` interface
- Add regression tests to prevent future snake_case drift in action deltas

## Tenets
- Verify before fix: confirm the bug exists in current code before changing production logic
- Guard rails over one-off fixes: add tests that catch the class of bug, not just one instance

## High-level description of changes
Investigation found the code already uses camelCase correctly (`actionDelta.bondLevel`, `actionDelta.currentLocation`). The snake_case usages elsewhere are at legitimate boundaries (DB columns, API request body, LLM prompt interfaces). Rather than a no-op fix, this PR adds 10 regression tests that guard against future snake_case drift across all 7 autonomy modes.

## Testing
- 54/54 tests pass in `companion_autonomy.test.ts` (10 new)
- New tests cover all 7 `CompanionAutonomyMode` values for `resolveCompanionAction`
- Tests verify `processCompanionTicks` actions across all modes
- Specific assertions for `bondLevel` (QUESTER) and `currentLocation` (EXPLORER) field naming

## Low-level details
- **packages/backend/tests/world/companion_autonomy.test.ts**: Added `actionDelta field naming — camelCase only (WC-a7g)` describe block with 10 tests. Each test runs `resolveCompanionAction` or `processCompanionTicks` across multiple seeds/ticks and asserts that `bond_level`, `current_location`, `autonomy_mode`, `personality_voice` never appear as keys in the returned delta.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Test-only change that adds guards against snake_case keys in companion action outputs; low risk aside from potential brittleness if output
