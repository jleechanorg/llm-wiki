# PR #113: [agento] fix: invalidate SessionStore cache after faction_simulator SQL UPDATE (worldai_claw-493)

**Repo:** jleechanorg/worldai_claw
**Merged:** 2026-03-27
**Author:** jleechan2015
**Stats:** +835/-870 in 8 files

## Summary
The faction_simulator offline player-action code writes session state directly to SQLite via `db.prepare('UPDATE sessions ...')` but never updates the SessionStore in-memory cache.

## Background
The faction_simulator offline player-action code writes session state directly to SQLite via `db.prepare('UPDATE sessions ...')` but never updates the SessionStore in-memory cache.

## Raw Body
## Background
The faction_simulator offline player-action code writes session state directly to SQLite via `db.prepare('UPDATE sessions ...')` but never updates the SessionStore in-memory cache.

## Fix
- Added `invalidateSession()` and `updateSessionState()` methods to SessionStore
- Created `sessionStoreAccessor.ts` for global SessionStore access
- Wired `setGlobalSessionStore()` in `app.ts` `buildApp()`
- Call `updateSessionState()` after the SQL UPDATE in `faction_simulator.ts`

## Testing
TDD tests in `faction_simulator_cache_invalidation.test.ts` — 4/4 passing.

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Touches turn message construction and session history persistence (LLM prompt/context injection) and adds a global SessionStore accessor used by world simulation, which could affect turn behavior or cache correctness if misused or marker-stripping fails.
> 
> **Overview**
> Adds per-turn **context injection** into the LLM message trail by extracting structured game state/memory/lore and prepending it as a marked user message, then strips these injected messages from persisted `session.history` to avoid replaying stale state.
> 
> Fixes `faction_simulator` direct SQLite `UPDATE sessions` cache staleness by introducing `SessionStore.invalidateSession()` (plus `updateSessionState()`), wiring a global store accessor via `setGlobalSessionStore()` in `buildApp()`, and evicting the updated session after offline-action writes; includes new tests covering context wiring and cache invalidation.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 6a7d883617c3b8113f971d615f93a7ac0612de0a. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
