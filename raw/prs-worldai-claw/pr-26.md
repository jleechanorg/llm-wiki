# PR #26: Ralph/pr25 bugfixes

**Repo:** jleechanorg/worldai_claw
**Merged:** 2026-02-28
**Author:** jleechan2015
**Stats:** +696/-216 in 14 files

## Summary
(none)

## Raw Body
<!-- CURSOR_SUMMARY -->
> [!NOTE]
> **Medium Risk**
> Medium risk due to changes in persistence schema/migrations (`entity_blocks.entity_type`), entity-chain append/locking behavior, and WebSocket message validation, which could impact live data and realtime clients if assumptions differ.
> 
> **Overview**
> **Session-scoped companions are now first-class.** Companion creation now persists/returns `sessionId`, repository mapping includes `sessionId`, and update flows add stronger session ownership checks to reduce cross-session edits.
> 
> **Entity-chain/IPFS reliability and performance improvements.** `getEntityChain()` now caches chains (with bounded eviction), `append()` is guarded/serialized to prevent hash-chain forks, and the `entity_blocks` schema adds/migrates `entity_type` (defaulting to `unknown`); IPFS local pinning adds a Node 18+ `fetch` guard.
> 
> **Realtime + world simulation fixes.** WebSocket handling adds `zod` message validation, removes magic readyState numbers, and cleans up duplicate connections; party leader promotion is persisted correctly; companion/world event generation and deterministic IDs are tightened, plus small RNG safety and name string fixes.
> 
> **Ops/docs updates.** Ralph bugfix PRD/progress tracking is expanded, and the helper script gains tool selection/branch handling.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit ddfb67bd886e1eaf2295e02febee2f7824dd8552. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->
## Summary by CodeRabbit

* **New Features**
  * Create, list, and update companions tied to a session via new session-scoped API endpoints; companion creations now include sessionId.
  * Entity-chain tools: cached chain access, serialized append operations, CID presence checks, and a verification report endpoint.

* **Bug
