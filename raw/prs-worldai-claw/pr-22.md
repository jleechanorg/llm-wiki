# PR #22: fix: prevent JSON leak and generic fallback choices

**Repo:** jleechanorg/worldai_claw
**Merged:** 2026-02-26
**Author:** jleechan2015
**Stats:** +137/-6 in 4 files

## Summary
- **claw-477**: Strip embedded JSON blocks from scene_text even when JSON parsing succeeds, preventing mixed-JSON turn outputs from leaking raw JSON into player-facing text
- **claw-g03**: Fix parseTurnResult to use proper choice coercion (`_coerceChoiceList` + `_isGenericChoiceFallback`) instead of `||` operator which incorrectly treats empty arrays as falsy
- **Evidence**: Add backend provenance (PID, port, cmdline) to methodology.md for better reproducibility

## Test Plan
- [ ] Run tests to verify no regressions
- [ ] Verify methodology.md contains backend info in test evidence

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Changes affect core turn parsing and choice fallback behavior, which can alter gameplay flow (especially terminal scenes) if edge cases were misunderstood. Evidence redaction/provenance changes are low risk but touch test harness output.
> 
> **Overview**
> Prevents p

## Raw Body
## Summary

- **claw-477**: Strip embedded JSON blocks from scene_text even when JSON parsing succeeds, preventing mixed-JSON turn outputs from leaking raw JSON into player-facing text
- **claw-g03**: Fix parseTurnResult to use proper choice coercion (`_coerceChoiceList` + `_isGenericChoiceFallback`) instead of `||` operator which incorrectly treats empty arrays as falsy
- **Evidence**: Add backend provenance (PID, port, cmdline) to methodology.md for better reproducibility

## Test plan

- [ ] Run tests to verify no regressions
- [ ] Verify methodology.md contains backend info in test evidence

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Changes affect core turn parsing and choice fallback behavior, which can alter gameplay flow (especially terminal scenes) if edge cases were misunderstood. Evidence redaction/provenance changes are low risk but touch test harness output.
> 
> **Overview**
> Prevents player-facing `scene_text` from including embedded structured/JSON blocks even when upstream JSON parsing succeeds (streaming turn payload parsing now always runs `stripStructuredBlocks`).
> 
> Tightens `OpenClawHTTPClient.parseTurnResult` parsing by (a) preserving explicit empty `choices: []` as a terminal result, (b) falling back only for non-array/invalid `choices` values or generic fallback options, and (c) guarding non-string `scene_text` to avoid `stripStructuredBlocks` type errors; adds unit tests covering these edge cases.
> 
> Enhances HTTP test evidence output by adding backend PID/port/cmdline to `methodology.md` and redacting likely secrets (e.g., `--token`, `--api-key`, `Bearer ...`) from recorded command lines.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 637567a41e5197620fa44fceb387edcc59fac7cb. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-
