# PR #51: refactor: codebase simplification — 1,518 lines removed

**Repo:** jleechanorg/worldai_claw
**Merged:** 2026-03-05
**Author:** jleechan2015
**Stats:** +157/-1518 in 52 files

## Summary
Whole-codebase /simplify sweep using 5 parallel Sonnet subagents. Each agent reviewed a major directory, reported findings, then executed fixes in isolated git worktrees.

**51 files changed, +156 -1,518 lines (net -1,362)**

### Bugs fixed
- `mcp/server.ts` JSONL capture wrote `\\n` (literal) instead of `\n` (newline) — corrupted capture output
- `mcp.campaign.test.ts` had swallowed try/catch tests that always passed regardless of behavior

### Deduplication (backend)
- Extracted shared `choice

## Test Plan
- [x] `tsc --noEmit` passes with zero errors (backend)
- [x] All Python files parse correctly (`ast.parse`)
- [ ] CI passes
- [ ] Existing test suites pass

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Mostly mechanical deletions and deduplication, but it touches backend request/response parsing and budget/choice utilities plus MCP persistence/capture paths, so subtle behavioral regressions are possible if any call sit

## Raw Body
## Summary

Whole-codebase /simplify sweep using 5 parallel Sonnet subagents. Each agent reviewed a major directory, reported findings, then executed fixes in isolated git worktrees.

**51 files changed, +156 -1,518 lines (net -1,362)**

### Bugs fixed
- `mcp/server.ts` JSONL capture wrote `\\n` (literal) instead of `\n` (newline) — corrupted capture output
- `mcp.campaign.test.ts` had swallowed try/catch tests that always passed regardless of behavior

### Deduplication (backend)
- Extracted shared `choice_utils.ts` from copy-pasted logic in `app.ts` + `openclawClient.ts`
- Renamed ambiguous `enforceBudget` → `enforceContextBlockBudget` in `token_budget.ts`
- Replaced per-call `new SQLiteCompanionRepository()` with singleton `getCompanionRepository()`
- Consolidated 6 inline `MockOpenClawClient` copies in tests → shared factory import
- Removed dead `extractJsonObject` wrapper method in `openclawClient.ts`

### Dead code removed (14 files deleted)
- `storage/sqlite.ts` (in-memory mock, not real SQLite), `sqliteStore.test.ts`
- `telemetry/turn_metrics.ts` unused functions, `telemetry.test.ts`
- `state_reducer.ts` deprecated `applyStateDelta`
- `HomeScreen.tsx` (byte-for-byte clone of CampaignsScreen)
- `sseClient.ts` + test (deprecated, replaced by streaming.ts)
- `domainToUi.ts` + test, `campaignMapper.ts` (mobile, never imported)
- `Header.tsx` + test (web, never imported)
- `game.ts` types (web, never imported)
- `puppeteer_file_proof.py`, `save_puppeteer_screenshots.py` (dead prototypes)

### Frontend cleanup
- Collapsed identical web/mobile alpha multiplier constants
- Removed duplicate `EventSourceLike` interface from `networkClient.ts`
- Fixed `AppProvider` calling `getInitialState()` 6x per render → 1x

### Python test deduplication
- `pick_free_port()`, `_coerce_sequence()`, `_payload_signature()`, `_is_truthy_env()`, `scroll_to_element()`, `wait_for_element()` — all deduplicated across testing dirs

### Config/dependency fixes
- `@testing-library/dom` move
