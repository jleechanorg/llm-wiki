# PR #222: [antig] refactor(tests): fix faction store singleton import leaks

**Repo:** jleechanorg/worldai_claw
**Merged:** 2026-04-05
**Author:** jleechan2015
**Stats:** +11/-12 in 4 files

## Summary
(none)

## Raw Body
Fixes 'ReferenceError' inside Jest from late-resolving promises bypassing the mocked environment by using static imports. Also removes dynamic await imports inside world.test.ts that broke singleton logic and failed faction expectations.

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Low risk: changes are primarily test fixes and import refactors, with no intended behavioral change to simulation logic beyond making module loading deterministic under Jest.
> 
> **Overview**
> **Removes dynamic `await import()` usage around `faction_store` to prevent Jest singleton/module-reset leaks.** `faction_simulator.ts` now uses static imports for `getOrCreateWorldState`/`saveWorldState`, and tests stop re-importing `faction_store` mid-test.
> 
> **Stabilizes flaky tests.** Updates `world.test.ts` to reuse a single `campaignId` per case (instead of `Date.now()` multiple times), bumps Jest timeouts in streaming-related tests, and tweaks the SSE session route streaming test input.
> 
> <sup>Reviewed by [Cursor Bugbot](https://cursor.com/bugbot) for commit f9b8c0c64d19405cca30e94be9cf571020a094aa. Bugbot is set up for automated code reviews on this repo. Configure [here](https://www.cursor.com/dashboard/bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
