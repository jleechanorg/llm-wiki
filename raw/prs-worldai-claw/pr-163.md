# PR #163: [agento] feat(api): POST /parties — party creation endpoint (wc-p1r)

**Repo:** jleechanorg/worldai_claw
**Merged:** 2026-03-31
**Author:** jleechan2015
**Stats:** +161/-10 in 4 files

## Summary
The `POST /parties` endpoint existed but lacked auth-gating, a `name` field, and optional `campaignId` support. The table schema also had no `name` or `campaign_id` columns.

## Background
The `POST /parties` endpoint existed but lacked auth-gating, a `name` field, and optional `campaignId` support. The table schema also had no `name` or `campaign_id` columns.

## Raw Body
## Background

The `POST /parties` endpoint existed but lacked auth-gating, a `name` field, and optional `campaignId` support. The table schema also had no `name` or `campaign_id` columns.

## Goals

- Add auth-gated party creation endpoint with `name` and optional `campaignId`
- Return created party object with `id`, `name`, `campaignId`, `createdAt`
- Update `GET /parties/:id` to return the new fields
- Add integration tests

## Tenets

- Follow existing auth patterns (`Bearer` token validation, default-account fallback)
- Use existing DB/schema patterns for the parties table
- Match test patterns from `test_worldai_claw_api.py`

## Changes

### `packages/backend/src/storage/sqlite_repository.ts`
- Add `name TEXT` and `campaign_id TEXT` columns to `parties` table

### `packages/backend/src/app.ts`
- `POST /parties`: auth-gate, accept `{name, campaignId?}`, insert into DB, return `201 {id, name, campaignId, createdAt}`
- `GET /parties/:id`: return `name` and `campaignId` fields in response

### `testing_mcp/test_parties.py` (new)
- `test_create_party_returns_201`: POST with `{name}` → 201 with `{id, name, createdAt}`
- `test_create_party_with_campaign_id`: POST with `{name, campaignId}` → 201 with both fields
- `test_create_party_persists`: create then GET → 200 with matching data

## Testing

- TypeScript compiles without errors in changed files
- `npm run build` pre-existing failures in `src/world/` unrelated to this change
- Integration tests via `pytest testing_mcp/test_parties.py -v`

Closes wc-p1r

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Touches a public API contract and introduces SQLite schema/migration changes for the `parties` table, which can break older clients or existing DBs if assumptions differ. Workflow change is low risk but could affect PR gate behavior if the `gh api` payload formatting is wrong.
> 
> **Overview**
> Adds an auth-gated `POST /parties` that requires a non-blank `name`, accepts optional `campaignId`, stores `acco
