# PR #67: ci: CodeRabbit ping on push (CodeRabbit review fixes only)

**Repo:** jleechanorg/worldai_claw
**Merged:** 2026-03-12
**Author:** jleechan2015
**Stats:** +73/-0 in 1 files

## Summary
Add CodeRabbit ping workflow with **only** the changes suggested by CodeRabbit in [PR #64](https://github.com/jleechanorg/worldai_claw/pull/64).

## Raw Body
## Summary
Add CodeRabbit ping workflow with **only** the changes suggested by CodeRabbit in [PR #64](https://github.com/jleechanorg/worldai_claw/pull/64).

## CodeRabbit suggestions applied
1. **issues: write** — Add permission (P1: 403 without it for PR comments)
2. **branches-ignore / tags-ignore** — Avoid runs on main pushes and tag pushes
3. **concurrency group** — Prevent duplicate comments on rapid pushes
4. **try/catch around createComment** — Resilience when one PR fails (rate limit, etc.)
5. **listComments `since` param** — Fix dedup on PRs with 50+ comments

## Diff
Single file: `.github/workflows/coderabbit-ping-on-push.yml`


Made with [Cursor](https://cursor.com)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Adds an automated GitHub Actions workflow that comments on open PRs and requires `pull-requests: write`/`issues: write` permissions, so misconfiguration could cause unwanted PR comment noise or permission-related failures.
> 
> **Overview**
> Adds a new GitHub Actions workflow, `coderabbit-ping-on-push.yml`, that runs on non-`main` branch pushes (excluding tags) and, for the matching open PR, posts a `@coderabbitai all good?` comment.
> 
> The workflow includes **concurrency controls** to avoid duplicate runs, a **4-hour cooldown** by scanning recent PR comments (using `since`) to prevent repeat pings, and wraps comment creation in `try/catch` for resilience; it also grants `issues: write` to allow commenting.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 7137aab1824320ed3b29b9f8e21e13350eb746fc. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
