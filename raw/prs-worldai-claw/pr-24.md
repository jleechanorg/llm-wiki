# PR #24: fix(mobile,web,backend): MVP P1 design gaps, SSE migration, schema alignment

**Repo:** jleechanorg/worldai_claw
**Merged:** 2026-02-27
**Author:** jleechan2015
**Stats:** +100/-169 in 6 files

## Summary
Complete MVP P1 design gap fixes from PR #15, backend hardening, and SSE schema alignment. All 645 tests across 3 packages remain green.

**6 files changed** | +100 -169 | 6 commits

## Raw Body
## Summary

Complete MVP P1 design gap fixes from PR #15, backend hardening, and SSE schema alignment. All 645 tests across 3 packages remain green.

**6 files changed** | +100 -169 | 6 commits

## Commits

| SHA | Description |
|-----|-------------|
| `72ec2f5` | fix(mobile,web): Address P1 design gaps from PR#15 |
| `4e8743c` | fix(backend): align SSE events payload with shared contracts |
| `6458ced` | Fix incompatible config shape passed to `connectToStream` (Cursor autofix) |
| `937556a` | fix: address PR #24 review comments (remove `as any`, move import to top) |
| `25e2534` | fix: remove duplicate getBaseUrl/getAuthToken from rebase conflict |
| `84ec8c3` | fix: address remaining PR #24 review comments (dead code, dup provider, dup guard) |

## Changes by Component

### Mobile — Provider Hierarchy & SSE Migration

#### `packages/mobile/src/App.tsx`
- **GAP-1**: Mounted `AmbientBackground` component for ambient visual theming
- **GAP-2**: Fixed provider hierarchy: `GestureHandlerRootView > AppProvider > ThemeProvider > ActionSheetProvider > AppNavigator`
- Removed redundant `BottomSheetModalProvider` wrapper (already provided inside `AppNavigator`)

#### `packages/mobile/src/state/gameStore.ts`
- **GAP-3**: Replaced `sseClient` import with `connectToStream` from `services/streaming.ts` (`react-native-sse`)
- Removed `as any` cast — now constructs a proper `StreamingServiceConfig` using `getBaseUrl()`/`getAuthToken()` getters

#### `packages/mobile/src/services/networkClient.ts`
- Added `getBaseUrl()` and `getAuthToken()` public getters to `GameNetworkClient`
- Enables `gameStore.ts` to build `StreamingServiceConfig` directly without unsafe casting

#### `packages/mobile/src/types/events.ts`
- Synced `DoneEvent.final` schema to match backend SSE payload:
  - Added `state_delta?: Record<string, unknown>`
  - Added `mechanic_requests?: MechanicRequest[]`
  - Added `domain_events?: Record<string, unknown>[]`
  - Added `ui_blocks?: UiBlock[]`
  - Added `world_event
