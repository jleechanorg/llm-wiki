# PR #19: feat: move SQLite DB from repo root to ~/.worldai_claw/

**Repo:** jleechanorg/worldai_claw
**Merged:** 2026-02-25
**Author:** jleechan2015
**Stats:** +111/-10 in 5 files

## Summary
- Default SQLite path changed from `process.cwd()/worldai_claw.db` to `~/.worldai_claw/worldai_claw.db`
- All worktrees share one database instead of duplicating per cwd
- `SQLITE_DB_PATH` env override unchanged for testing

## Raw Body
## Summary
- Default SQLite path changed from `process.cwd()/worldai_claw.db` to `~/.worldai_claw/worldai_claw.db`
- All worktrees share one database instead of duplicating per cwd
- `SQLITE_DB_PATH` env override unchanged for testing

## Production code changes
- **Before**: DB landed at repo root (or wherever server started), duplicated across worktrees
- **Now**: Default `~/.worldai_claw/worldai_claw.db`; `SQLITE_DB_PATH` overrides for tests
- **Why**: Worktree-safe, single shared DB per user

## Test changes
- New `db_path.test.ts`: env override, default path, worktree-independence
- Existing `sqlite_persistence.test.ts` uses `setDatabasePath()` (unchanged)

## Files
- `packages/backend/src/config/db_path.ts` (new)
- `packages/backend/tests/db_path.test.ts` (new)
- `packages/backend/src/server.ts`
- `packages/backend/src/storage/sqlite_repository.ts`
- `README.md`

Made with [Cursor](https://cursor.com)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Low risk: this only changes the default on-disk DB location and centralizes path resolution; main risk is unexpected data split if users relied on the old cwd-based path without the legacy file present.
> 
> **Overview**
> Moves the default SQLite persistence location from the repo/cwd to a per-user path at `~/.worldai_claw/worldai_claw.db`, while still allowing overrides via `SQLITE_DB_PATH` (preferred) or `WORLDCLAW_DB_PATH`.
> 
> Introduces a shared `resolveDbPath()` helper used by both server startup and the SQLite repository, including a *legacy upgrade fallback* to `./worldai_claw.db` when that file already exists, and updates the README accordingly.
> 
> Adds unit tests to lock in env var precedence, cwd-independence (worktree-safe behavior), and legacy-path detection.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 4f09aeb26e3e8b7e63b26293174aa4fa88e496b6. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot)
