# PR #87: feat: add dice mandate and no-dice-in-narrative rule to system instruction

**Repo:** jleechanorg/worldai_claw
**Merged:** 2026-03-26
**Author:** jleechan2015
**Stats:** +30/-9 in 5 files

## Summary
Add the dice mandate and no-dice-in-narrative rule to the Layer 1 system instruction (DICE MANDATE (Layer 1)).

## Raw Body
## Summary

Add the dice mandate and no-dice-in-narrative rule to the Layer 1 system instruction (DICE MANDATE (Layer 1)).

## Changes

- `packages/backend/src/llm/prompts/system_instruction.md`: Replace the incorrect inline dice notation in the DICE MANDATE section with the correct rules:
  - ALL attacks/saves REQUIRE dice rolls via mechanic_requests — NEVER skip or fabricate
  - NEVER show dice rolls in scene_text — no brackets, no rolled numbers
  - Dice results go in mechanic_requests/state_delta only

- `packages/backend/tests/systemInstruction.test.ts`: Add 3 TDD tests verifying:
  - Dice rolls are required via mechanic_requests, never skipped or fabricated
  - Dice rolls are never shown in scene_text
  - Dice results go in mechanic_requests/state_delta only

## Testing

- All 11 tests in systemInstruction.test.ts pass (11/11)
- 3 new dice mandate tests added and verified against the fixed instruction

Closes wc-3li

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Updates the core `SYSTEM_INSTRUCTION` prompt contract around dice handling, which can change LLM turn outputs and downstream parsing/UX expectations. Changes are mostly prompt text + tests, with only minor refactors elsewhere.
> 
> **Overview**
> Tightens the Layer 1 D&D 5e *dice mandate* in `system_instruction.md` to require uncertainty resolution via `mechanic_requests`/`state_delta` and explicitly forbid any dice notation or rolled numbers in `scene_text`.
> 
> Adds targeted tests in `systemInstruction.test.ts` to prevent regressions (requires dice for uncertain actions, forbids dice in `scene_text`, and constrains where roll results may appear). Also includes a few no-op refactors (switching `let` to `const`) in `app.ts`, `slm_narrator.ts`, and `p2p_sync.ts`.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit ec94e9c1f5b48965785620d45e40cead5e7f2ca8. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bu
