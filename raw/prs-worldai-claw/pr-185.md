# PR #185: [antig] fix: enforce token limit on no-lore path

**Repo:** jleechanorg/worldai_claw
**Merged:** 2026-04-03
**Author:** jleechan2015
**Stats:** +28/-9 in 3 files

## Summary
(none)

## Raw Body
Resolves wc-16i. The no-lore early return path now enforces the token limit using the same logic as the lore branch.

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Changes prompt-construction logic for the default (no-context) path, which can alter what the LLM receives under tight token budgets. Risk is moderated by added tests but could still affect downstream prompt structure/behavior when trimming occurs.
> 
> **Overview**
> Ensures `buildSystemInstructionWithLimit()` enforces the provided token budget even when `context` is `undefined` (previously could return the full `SYSTEM_INSTRUCTION` without trimming).
> 
> Adds a cached `BASE_SYSTEM_INSTRUCTION_TOKENS` fast-path for token counting, tightens fallback behavior to return only the trimmed instruction (or empty string) instead of re-trimming/returning a potentially over-limit base, and updates tests to assert truncation occurs for tiny limits on the no-context path. Also marks issue `wc-16i` as closed.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 8dbe5a2335ad450a62baa1fe7713195bf75153a4. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->
## Summary by CodeRabbit

* **Bug Fixes**
  * Improved enforcement of token limits for system instructions to ensure trimmed instructions respect limits and fallbacks are stricter.

* **Tests**
  * Updated tests to validate behavior under very small token limits and ensure truncated instructions meet limits.

* **Chores**
  * Closed a resolved issue and updated tracking records.
<!-- end of auto-generated comment: release notes by coderabbit.ai -->
