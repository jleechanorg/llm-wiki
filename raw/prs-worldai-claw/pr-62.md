# PR #62: migrate: migrate .beads to beads_rust (br) sqlite workflow

**Repo:** jleechanorg/worldai_claw
**Merged:** 2026-03-10
**Author:** jleechan2015
**Stats:** +15/-72 in 3 files

## Summary
- Update config.yaml to use local SQLite DB
- Update .gitignore for br SQLite files
- Update metadata.json to sqlite backend

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Low risk repo-only configuration change, but it can affect developer workflows if the new SQLite DB path/ignore rules don’t match local setups.
> 
> **Overview**
> Migrates `.beads` from the Dolt-backed workflow to **beads_rust (br) SQLite**, updating `c

## Raw Body
## Summary
- Update config.yaml to use local SQLite DB
- Update .gitignore for br SQLite files
- Update metadata.json to sqlite backend

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Low risk repo-only configuration change, but it can affect developer workflows if the new SQLite DB path/ignore rules don’t match local setups.
> 
> **Overview**
> Migrates `.beads` from the Dolt-backed workflow to **beads_rust (br) SQLite**, updating `config.yaml` to use a local SQLite DB (`no-daemon: true`, `db: .beads/worldai_claw.db`) and trimming legacy config fields.
> 
> Updates `.beads/metadata.json` to declare the `sqlite` database/backend, and extends `.beads/.gitignore` to ignore br-specific SQLite/history artifacts (e.g., `beads.db`, `br_history/`) while keeping collaboration defaults like `sync-branch` unchanged.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 427f757a9dfc4c3f4a8c1af5471c472e7fa549f1. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
