# PR #90: feat(system): add STRICT XP & state change consistency rule

**Repo:** jleechanorg/worldai_claw
**Merged:** 2026-03-26
**Author:** jleechan2015
**Stats:** +133/-0 in 2 files

## Summary
The LLM system instruction lacked an explicit, dedicated consistency rule for XP and permanent state changes. While the CAMPAIGN PROGRESSION, LIVING WORLD, AND REWARDS COHERENCE section covered the high-level requirement (XP in scene_text must match state_delta), a standalone "STRICT" enforcement clause was missing — making it easy for the LLM to accidentally narrate rewards without corresponding state mutations.

## Background
The LLM system instruction lacked an explicit, dedicated consistency rule for XP and permanent state changes. While the CAMPAIGN PROGRESSION, LIVING WORLD, AND REWARDS COHERENCE section covered the high-level requirement (XP in scene_text must match state_delta), a standalone "STRICT" enforcement clause was missing — making it easy for the LLM to accidentally narrate rewards without corresponding state mutations.

## Raw Body
## Background

The LLM system instruction lacked an explicit, dedicated consistency rule for XP and permanent state changes. While the CAMPAIGN PROGRESSION, LIVING WORLD, AND REWARDS COHERENCE section covered the high-level requirement (XP in scene_text must match state_delta), a standalone "STRICT" enforcement clause was missing — making it easy for the LLM to accidentally narrate rewards without corresponding state mutations.

## Goals

- Add an explicit "STRICT XP & STATE CHANGE CONSISTENCY" sub-rule to the system instruction that makes the single-turn co-occurrence requirement unambiguous.
- TDD: add 5 contract tests that verify all living-world and reward-coherence rules are present in the instruction text.

## Tenets

- Minimal, additive instruction changes — no existing rules removed or reworded.
- Rules must be universe-agnostic (no fantasy-specific keywords).
- TDD: tests written before implementation (RED → GREEN).

## High-level description of changes

**packages/backend/src/llm/prompts/system_instruction.md** — Added a 3-point "STRICT XP & STATE CHANGE CONSISTENCY" sub-rule inside the existing CAMPAIGN PROGRESSION, LIVING WORLD, AND REWARDS COHERENCE section:
1. Every permanent state change MUST appear in state_delta in the same response that narrates it in scene_text.
2. Never narrate a reward, penalty, or state change in scene_text without a corresponding state_delta field in the same turn.
3. Cross-reference: scene_text XP mentions must match state_delta.xp_delta exactly.

**packages/backend/tests/systemInstruction.test.ts** — Added a wc-zem: living world & reward coherence contract describe block with 5 TDD tests.

## Testing

- All 13 system instruction tests pass (8 existing + 5 new).
- Token budget unchanged: instruction remains under 10k token limit.
- promptContracts.test.ts SHA-256 hash test was already failing before this change (pre-existing issue).

## Low-level details

- packages/backend/src/llm/prompts/system_instruction.md: Added 4 lines
