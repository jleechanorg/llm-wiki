# PR #183: fix(api): resolve OPENCLAW_UNAVAILABLE 502 in turn pipeline

**Repo:** jleechanorg/worldai_claw
**Merged:** 2026-04-03
**Author:** jleechan2015
**Stats:** +19/-15 in 3 files

## Summary
(none)

## Raw Body
This PR resolves the 502 OPENCLAW_UNAVAILABLE error in the `/sessions/:id/turn` pipeline by fixing the model mapping bug in the `OpenClawClient`. Additionally, it fixes a type error parsing payloads in `app.ts` and fixes a bad assertion test in `chain_anchor_access_control.test.ts`. Closes bead wc-4sn.

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Medium risk because it changes OpenClaw model selection used by the turn pipeline; incorrect mapping could reroute requests or reintroduce `OPENCLAW_UNAVAILABLE` failures. Other changes are test/type-safety tweaks with low production impact.
> 
> **Overview**
> Fixes OpenClaw model resolution so the default model is consistently returned as an `openclaw/...` identifier (including handling `WORLDCLAW_DEFAULT_MODEL=openclaw` and numeric `endpointRef`), addressing `OPENCLAW_UNAVAILABLE`/502s in the turn pipeline.
> 
> Also tightens `ChainAnchor` access-control tests to require successful anchors with `result.error` strictly undefined (vs. merely not unauthorized), and adds an explicit response type assertion when parsing the sprite-generation API JSON payload in `app.ts`.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 9c0acb9285919eddcd6354bceae5e48623ebce6d. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->

## Summary by CodeRabbit

* **Tests**
  * Enhanced authorization control test suite to enforce stricter validation requirements, ensuring explicit error state expectations are properly validated across multiple access control scenarios

* **Refactor**
  * Improved backend model resolution logic to provide more consistent and reliable default configuration handling across different request patterns

<!-- end of auto-generated comment: release notes by coderabbit.ai -->
