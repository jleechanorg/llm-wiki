# PR #179: fix(backend): shared helper for persisted history dice reminder (#81)

**Repo:** jleechanorg/worldai_claw
**Merged:** 2026-04-03
**Author:** jleechan2015
**Stats:** +88/-24 in 3 files

## Summary
Implements worldai_claw#81 by extracting `chatMessagesForPersistedHistory()` so streaming and non-streaming `session.history` persistence share one code path (system drop, context-injection drop, DICE_REMINDER strip on user messages).

## Raw Body
## Summary
Implements worldai_claw#81 by extracting `chatMessagesForPersistedHistory()` so streaming and non-streaming `session.history` persistence share one code path (system drop, context-injection drop, DICE_REMINDER strip on user messages).

## Testing
`cd packages/backend && npx jest budget_message_sanitize`

Fixes jleechanorg/worldai_claw#81

Made with [Cursor](https://cursor.com)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Low risk refactor of `session.history` persistence that centralizes message filtering and changes dice-reminder stripping to only remove an exact trailing suffix on user messages.
> 
> **Overview**
> **Unifies how `session.history` is persisted after budget enforcement** by extracting a shared helper, `chatMessagesForPersistedHistory()`, and using it in all turn-processing paths.
> 
> The new helper drops `system` messages, removes context-injection user messages (those starting with `CONTEXT_INJECTION_MARKER`), and strips the per-turn dice reminder from *user* messages to prevent it from accumulating across turns. Adds Jest coverage for these sanitization rules, including suffix-only stripping behavior.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 85bab7ba95bb41b3ec56876e6f959f89ef7ad198. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->
## Summary by CodeRabbit

* **Refactor**
  * Unified and improved message-history processing to ensure consistent persisted conversation contents and to remove duplicated logic.

* **Bug Fixes**
  * Persisted history now consistently omits system-only entries and filters injected context markers from user messages; user reminders appended exactly as intended are trimmed only from persisted copies.

* **Tests**
  * Added tests validating history filtering and sanitization behavior.
<!-- end
