# PR #89: feat(wc-1xn): add prompt deprecation policy for canonical fields

**Repo:** jleechanorg/worldai_claw
**Merged:** 2026-03-26
**Author:** jleechan2015
**Stats:** +578/-0 in 4 files

## Summary
Issue wc-1xn: Define canonical output fields and deprecation policy for the system instruction contract.

## Background
Issue wc-1xn: Define canonical output fields and deprecation policy for the system instruction contract.

## Raw Body
## Background

Issue wc-1xn: Define canonical output fields and deprecation policy for the system instruction contract.

## Goals

1. Define which fields are canonical (stable contract).
2. Define which fields are deprecated (must not appear in output).
3. Provide runtime utilities to classify and warn about fields.
4. Add schema definitions for all canonical fields.

## Testing

- 34 TDD tests in packages/backend/src/llm/system_instruction.test.ts — all passing.

## Low-level details

- packages/backend/src/llm/system_instruction.ts: canonical field registry + deprecation policy
- packages/backend/src/llm/system_instruction.test.ts: 34 TDD tests

Closes #wc-1xn

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Low risk: changes are additive (field registry + warnings) and only introduce console warnings during LLM response parsing, without altering turn/state processing semantics.
> 
> **Overview**
> Defines a **canonical/deprecated field contract** for LLM JSON responses via a new `CANONICAL_FIELD_REGISTRY` (with basic schema/stability metadata) and helper utilities like `isCanonicalField`, `isDeprecatedField`, `getDeprecationWarning`, and `validateResponseFields`.
> 
> Adds runtime detection that **logs `[deprecated-field]` warnings** when parsed LLM responses include deprecated top-level fields, wired into both streaming turn parsing in `app.ts` and JSON/mixed-JSON parsing in `OpenClawHTTPClient.parseTurnResult`.
> 
> Expands `system_instruction.test.ts` with comprehensive tests covering registry contents, non-overlap, stability guarantees, and warning generation/validation behavior.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 47eca70c08148ad20ee31894cc5308ecd8d9742d. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->

## Summary by CodeRabbit

* **New
