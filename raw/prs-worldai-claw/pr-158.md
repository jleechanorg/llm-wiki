# PR #158: [agento] feat(storage): closeAll() for entity chain DB cleanup (worldai_claw-b9d)

**Repo:** jleechanorg/worldai_claw
**Merged:** 2026-03-31
**Author:** jleechan2015
**Stats:** +46/-2 in 2 files

## Summary
- Add `closeAll()` function that closes all tracked SQLite database connections and clears the entity chain cache. Call during application shutdown.
- Track database instances in a parallel `dbByCacheKey` map so `closeAll()` can clean them up.
- Fix LRU eviction path to also remove entries from `dbByCacheKey` when evicting.

## Test Plan
- [x] `packages/backend/tests/entity_chain_caching.test.ts` — 8 tests, all passing

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Medium risk because it introduces new global cache/DB lifecycle behavior and calls `db.close()`, which could break callers if invoked at the wrong time, though it limits closure to `:memory:` databases.
> 
> **Overview**
> Adds a parallel `dbByCacheKey` registry alongside the entity-chain cac

## Raw Body
## Summary

- Add `closeAll()` function that closes all tracked SQLite database connections and clears the entity chain cache. Call during application shutdown.
- Track database instances in a parallel `dbByCacheKey` map so `closeAll()` can clean them up.
- Fix LRU eviction path to also remove entries from `dbByCacheKey` when evicting.

## Test plan

- [x] `packages/backend/tests/entity_chain_caching.test.ts` — 8 tests, all passing

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Medium risk because it introduces new global cache/DB lifecycle behavior and calls `db.close()`, which could break callers if invoked at the wrong time, though it limits closure to `:memory:` databases.
> 
> **Overview**
> Adds a parallel `dbByCacheKey` registry alongside the entity-chain cache so cached chains can be associated with (and cleaned up with) their underlying SQLite connections.
> 
> Introduces `closeAll()` to close all tracked **in-memory** (`:memory:`) DB instances (deduped) and clear both caches, and updates the LRU eviction + `clearEntityChainCache()` path to also drop DB references.
> 
> Extends `entity_chain_caching.test.ts` to cover `closeAll()` behavior and makes teardown tolerant of DBs already closed by the new cleanup.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 2ab9003dc4fc6d8f559f0eec388f007e19fa9a69. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->

## Summary by CodeRabbit

* **New Features**
  * Introduced database connection lifecycle management with a cleanup function that properly releases all SQLite connections and clears cached data during application shutdown, ensuring efficient resource management.

* **Tests**
  * Added tests verifying the cleanup function closes all database
