# PR #199: fix(world): gate offline player actions with player_away flag

**Repo:** jleechanorg/worldai_claw
**Merged:** 2026-04-04
**Author:** jleechan2015
**Stats:** +9/-1 in 2 files

## Summary
(none)

## Raw Body
Fixes wc-zwi (offline player actions run every simulation tick without away-state gate). Only generates offline actions for player characters if the session parsedState explicitly has `player_away` set to true, preventing the background simulation from hijacking the player character during active play.

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Medium risk because it changes when player-character state mutations occur during world ticks and tightens request validation, which could alter gameplay behavior and reject previously accepted inputs.
> 
> **Overview**
> Prevents background world simulation from generating and applying *offline player-character actions* unless the session state indicates `player_away` (with fallback reads from nested `game_state`/`session_state`), avoiding unintended character updates during active play.
> 
> Tightens `turnRequestSchema` validation by requiring `openclaw_endpoint_ref` to be non-empty (in addition to the existing safe-path regex), which may cause some client requests that previously passed to now be rejected.
> 
> <sup>Reviewed by [Cursor Bugbot](https://cursor.com/bugbot) for commit 014eea351de12612f29bdfb00c219f539693bc78. Bugbot is set up for automated code reviews on this repo. Configure [here](https://www.cursor.com/dashboard/bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->
## Summary by CodeRabbit

* **Bug Fixes**
  * Autonomous player actions now run only when a player is explicitly marked as away.
  * Standardized endpoint reference validation so IDs must follow the new safe-character rules (fewer allowed special characters), preventing invalid refs.
<!-- end of auto-generated comment: release notes by coderabbit.ai -->
