# PR #32: test: testing_mcp improvements — filter fix, companion ticks, faction endpoint, strong assertions

**Repo:** jleechanorg/worldai_claw
**Merged:** 2026-03-02
**Author:** jleechan2015
**Stats:** +611/-51 in 8 files

## Summary
- **wc-6aa**: Renamed 4 tests whose names contained `turn`/`returns` as substrings, silently excluded by `-k "not ... turn ..."` filter
- **wc-c58**: Living world tests now use campaign-linked sessions; `world_events` field asserted every turn
- **wc-1m1**: Added `test_catchup_companion_action_payload` — verifies EXPLORER companion generates tick actions
- **wc-h8g**: Added `GET /campaigns/:id/factions` endpoint to app.ts; test asserts nested faction structure (`identity.{id,name,tier}`, `capabi

## Test Plan
- [x] 34/34 tests pass full suite (`python -m pytest testing_mcp/test_worldai_claw_api.py`)
- [x] Core filter (29/29) verified independently
- [x] All companion tick assertions now unconditional (no `if companions:` guards)
- [x] Faction field assertions match actual nested structure
- [x] LLM traces non-empty (10 trace files, 12–66 lines each)
- [x] All 8 beads closed: wc-6aa, wc-c58, wc-1m1, wc-h8g, wc-919, wc-lm4, wc-6to, wc-45j

🤖 Generated with [Claude Code](https://claude.com/claude-code)


## Raw Body
## Summary

- **wc-6aa**: Renamed 4 tests whose names contained `turn`/`returns` as substrings, silently excluded by `-k "not ... turn ..."` filter
- **wc-c58**: Living world tests now use campaign-linked sessions; `world_events` field asserted every turn
- **wc-1m1**: Added `test_catchup_companion_action_payload` — verifies EXPLORER companion generates tick actions
- **wc-h8g**: Added `GET /campaigns/:id/factions` endpoint to app.ts; test asserts nested faction structure (`identity.{id,name,tier}`, `capabilities.{resources,military,morale}`)
- **wc-919**: Concrete RPG action strings + `require_story=True` in living world tests
- **wc-lm4**: Added `test_catchup_multi_mode_companions` — guard/trader/balanced companions each produce distinct action types
- **wc-6to**: Strengthened conditional assertions — companion lists now unconditionally required non-empty after simulation fires
- **wc-45j**: Updated stale coverage notes; WC-9rl and wc-d9z upgraded to `covered`

## Test plan

- [x] 34/34 tests pass full suite (`python -m pytest testing_mcp/test_worldai_claw_api.py`)
- [x] Core filter (29/29) verified independently
- [x] All companion tick assertions now unconditional (no `if companions:` guards)
- [x] Faction field assertions match actual nested structure
- [x] LLM traces non-empty (10 trace files, 12–66 lines each)
- [x] All 8 beads closed: wc-6aa, wc-c58, wc-1m1, wc-h8g, wc-919, wc-lm4, wc-6to, wc-45j

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Adds a new public backend route that lazily generates and persists faction state, which could affect campaign initialization behavior and world-state consistency under concurrency. Remaining changes are mostly test/config updates but touch core world-state and SQLite persistence code paths.
> 
> **Overview**
> **Adds `GET /campaigns/:id/factions`** to return a campaign’s living-world factions, including lazy bootstrap (deterministic generation 
