# PR #30: feat(phases 2,4,5): WorldClaw Autonomy Engine, On-Chain Anchoring, P2P Sync — 18 stories complete

**Repo:** jleechanorg/worldai_claw
**Merged:** 2026-02-28
**Author:** jleechan2015
**Stats:** +2280/-46 in 16 files

## Summary
Implements all 18 user stories for WorldClaw Phases 2 (Autonomy Engine), 4 (On-Chain Anchoring), and 5 (P2P Sync) via the Ralph AI agent loop.

### Wave 1 — P0 Bugs (6 stories)
- **WC-a22**: Replace all `Math.random()` with `createSeededRng()` in `faction_simulator.ts` — deterministic replay
- **wc-qh7**: Replace `Date.now()` in trade item IDs with `deterministicId()` — deterministic IDs
- **WC-b65**: Add access control to `chain_anchor.ts anchorHash()` — only owner can update
- **WC-vsr**: Fix 

## Test Plan
- [ ] `npx tsc --noEmit` passes (verified after each commit)
- [ ] All 18 stories have `passes: true` in `ralph-prd.json`
- [ ] `GET /sessions/:id/companions/catchup` returns 200 with companion actions
- [ ] `dm_anchor_entity` MCP tool returns `{ status: 'not_configured' }` when env vars absent
- [ ] `ENABLE_P2P_SYNC=true` starts P2PSync alongside HTTP server

🤖 Generated with [Claude Code](https://claude.com/claude-code) via Ralph AI agent loop

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium

## Raw Body
## Summary

Implements all 18 user stories for WorldClaw Phases 2 (Autonomy Engine), 4 (On-Chain Anchoring), and 5 (P2P Sync) via the Ralph AI agent loop.

### Wave 1 — P0 Bugs (6 stories)
- **WC-a22**: Replace all `Math.random()` with `createSeededRng()` in `faction_simulator.ts` — deterministic replay
- **wc-qh7**: Replace `Date.now()` in trade item IDs with `deterministicId()` — deterministic IDs
- **WC-b65**: Add access control to `chain_anchor.ts anchorHash()` — only owner can update
- **WC-vsr**: Fix false success in `chain_anchor.ts anchor()` when env vars absent — returns `{ success: false, reason: 'not_configured' }`
- **WC-3pg**: Fix IPFS race condition in `entity_chain.ts append()` — pin after insert
- **wc-hnt**: Rename `verifyCids()` → `hasCids()` to reflect local-only check

### Wave 2 — P1 Bugs + Phase 2 Wiring (7 stories)
- **wc-a7g**: Fix `bond_level` → `bondLevel`, `current_location` → `currentLocation` in companion deltas
- **WC-a55**: Incorporate day+hour into companion RNG seed — different ticks produce different actions
- **WC-4j2**: Fix XP-to-level divisor in Explorer/Quester modes — companions can now level up
- **wc-fr8**: Add `appendWithTimestamp()` to `entity_chain.ts` — fork preserves original timestamps
- **WC-dto**: Wrap `entity_chain.ts append()` in `BEGIN IMMEDIATE` SQLite transaction — concurrent safety
- **wc-d9z**: Wire `processCompanionTicks()` into `runSimulation()` — Phase 2 core alive
- **WC-9rl**: Add `companion_actions` SQLite table + `saveCompanionAction()` — action history persisted

### Wave 3 — P2 Features (4 stories)
- **WC-xww**: Add `GET /sessions/:id/companions/catchup` endpoint — returns actions since last_seen
- **wc-p26**: Integrate `ChainAnchor` into `app.ts` with MCP tools `dm_anchor_entity` + `dm_verify_anchor`
- **wc-myj**: Add `PRAGMA foreign_keys = ON` in `entity_chain.ts` constructor
- **wc-6zk**: Implement `getCid()` DB lookup — `SELECT ipfs_cid FROM entity_blocks WHERE ...`

### Wave 4 — Phase 5 P2P (1 sto
