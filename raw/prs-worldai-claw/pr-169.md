# PR #169: fix: mitigate MCP error leak and apply Zod payload constraint validation

**Repo:** jleechanorg/worldai_claw
**Merged:** 2026-03-31
**Author:** jleechan2015
**Stats:** +110/-5 in 5 files

## Summary
This PR addresses Python security/logic regression equivalents in the worldai_claw TS architecture.

## Background
This PR addresses Python security/logic regression equivalents in the worldai_claw TS architecture.

## Raw Body
## Background
This PR addresses Python security/logic regression equivalents in the worldai_claw TS architecture.

## Goals
- Harden input validation across identifying payload attributes inside createSessionSchema and turnRequestSchema.
- Prevent path traversal variables or unsanitizable logic tags.
- Sanitize MCP backend Fastify error propagation so that filepaths or internal SQLite metadata doesn't leak.

## Testing
- Unit tests written for `contracts.test.ts` and `mcp/server.test.ts` verifying sanitized paths and logic. Note: some unrelated tests in `dev` were already consistently experiencing pipeline timeouts locally.

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Moderate risk because stricter regex validation may reject previously-accepted `campaign_id`/`world_id`/`party_id`/`openclaw_endpoint_ref` values, potentially breaking some clients. Error sanitization is low risk but changes externally-visible error messages for certain failures.
> 
> **Overview**
> **Security hardening for the MCP JSON-RPC surface.** MCP request failures now pass error messages through `sanitizeMcpErrorMessage`, masking SQLite/schema details and local filesystem paths before returning `-32603` responses.
> 
> **Stricter input validation for session/turn identifiers.** `createSessionSchema` and `turnRequestSchema` now constrain `campaign_id`, `world_id`, `party_id`, and `openclaw_endpoint_ref` to `^[a-zA-Z0-9_-]+$` to reduce injection/path traversal risk, with new unit tests covering both sanitization and validation behavior.
> 
> Minor housekeeping updates `.gitignore` for additional local artifacts.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit ba53f3a16481200e00bcebe23c3fa32d253274a9. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
