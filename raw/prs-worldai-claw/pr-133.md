# PR #133: chore: switch workflows to self-hosted runners

**Repo:** jleechanorg/worldai_claw
**Merged:** 2026-03-28
**Author:** jleechan2015
**Stats:** +1/-1 in 1 files

## Summary
worldai_claw had 1,881 runs in last 14 days as a private repo. Switching to self-hosted eliminates GitHub-hosted compute cost for main-push CI runs.

## Background
worldai_claw had 1,881 runs in last 14 days as a private repo. Switching to self-hosted eliminates GitHub-hosted compute cost for main-push CI runs.

## Raw Body
## Background

worldai_claw had 1,881 runs in last 14 days as a private repo. Switching to self-hosted eliminates GitHub-hosted compute cost for main-push CI runs.

## Changes

- `.github/workflows/test.yml`: Uses `ubuntu-latest` for pull_request events (safe for fork PRs), and self-hosted runners for non-fork main-push events. The runner label is configurable via `vars.SELF_HOSTED_RUNNER_LABELS` (default: `[\"self-hosted\",\"worldai-claw\"]`).
- `.github/workflows/coderabbit-ping-on-push.yml`: Runs on `ubuntu-latest` (lightweight notification job, no need for self-hosted).

## Security

Fork PRs are explicitly routed to GitHub-hosted `ubuntu-latest` runners via the conditional `runs-on` expression, preventing untrusted code from executing on the org-level self-hosted runner.

## Runner Setup (one-time, already complete)

The org-level self-hosted runner (labels: `self-hosted`, `worldai-claw`) is already registered and available. No new runner setup required for this PR.
