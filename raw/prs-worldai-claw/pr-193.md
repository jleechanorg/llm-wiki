# PR #193: [antig] refactor: centralize _branch_scoped_tmp_root usage

**Repo:** jleechanorg/worldai_claw
**Merged:** 2026-04-03
**Author:** jleechan2015
**Stats:** +6/-63 in 4 files

## Summary
(none)

## Raw Body
Closes wc-45x. This PR centralizes the `_branch_scoped_tmp_root` implementation by removing local redefinitions in the UI E2E tests (`test_openclaw_browser.py` and `test_openclaw_e2e.py`) and standardizing on the `testing_ui.config` version. It also migrates the test utilities legacy `EVIDENCE_ROOT` logic to rely on the same config endpoint instead of rebuilding it locally.

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Low risk refactor limited to test/evidence path plumbing; main impact is where screenshots/logs are written, which could break CI artifact collection if it expects the old paths.
> 
> **Overview**
> Standardizes where test artifacts are written by routing OpenClaw UI E2E tests and `worldai_claw_test_utils.get_suite_dir` through the shared `testing_ui.config._branch_scoped_tmp_root()` implementation.
> 
> Removes duplicated git-branch detection and legacy `/tmp/worldarchitectai`/locally-built `EVIDENCE_ROOT` logic, and documents that the canonical temp root is now `/tmp/worldai_claw/<branch>`.
> 
> <sup>Reviewed by [Cursor Bugbot](https://cursor.com/bugbot) for commit bb03a0502e9690e3d9f01e3e15752deb2e7516e6. Bugbot is set up for automated code reviews on this repo. Configure [here](https://www.cursor.com/dashboard/bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->
## Summary by CodeRabbit

* **Refactor**
  * Test infrastructure now uses a shared, branch-scoped temporary root for test evidence and artifacts, removing duplicated branch-detection logic.
  * Test setup simplified by centralizing environment path resolution and standardizing the temporary artifact location, reducing redundancy and improving maintainability.
<!-- end of auto-generated comment: release notes by coderabbit.ai -->
