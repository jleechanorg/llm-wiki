# PR #44: fix: JobLedger durability + TurnCoordinator multi-party isolation

**Repo:** jleechanorg/worldai_claw
**Merged:** 2026-03-05
**Author:** jleechan2015
**Stats:** +318/-16 in 5 files

## Summary
- **getTurnCoordinator**: Replaced module-level singleton with `Map<string, TurnCoordinator>` keyed by `partyId`. Each party now gets its own coordinator instance, preventing stale singleton bugs when multiple parties exist in one process.
- **JobLedger**: `getJobLedger(db)` now actually uses the `better-sqlite3` Database argument. Jobs are persisted to a new `job_proofs` table in SQLite, so the leaderboard survives server restarts. Falls back to in-memory when no DB is provided (backward compat

## Test Plan
- [x] 3 new tests for `getTurnCoordinator` factory (multi-party isolation, same-party reuse, per-party reset) — all pass
- [x] 3 new SQLite durability tests for `JobLedger` (persist+reload, AUTOINCREMENT IDs, backward compat) — skip gracefully when native module unavailable, pass on matching Node version
- [x] All 47 existing tests pass, 0 regressions
- [x] TypeScript compiles cleanly (`tsc --noEmit`)

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

>

## Raw Body
## Summary
- **getTurnCoordinator**: Replaced module-level singleton with `Map<string, TurnCoordinator>` keyed by `partyId`. Each party now gets its own coordinator instance, preventing stale singleton bugs when multiple parties exist in one process.
- **JobLedger**: `getJobLedger(db)` now actually uses the `better-sqlite3` Database argument. Jobs are persisted to a new `job_proofs` table in SQLite, so the leaderboard survives server restarts. Falls back to in-memory when no DB is provided (backward compatible).
- Added `job_proofs` table + indexes to `sqlite_repository.ts` `initializeTables`.

Addresses both Codex review findings from PR #31.

## Test plan
- [x] 3 new tests for `getTurnCoordinator` factory (multi-party isolation, same-party reuse, per-party reset) — all pass
- [x] 3 new SQLite durability tests for `JobLedger` (persist+reload, AUTOINCREMENT IDs, backward compat) — skip gracefully when native module unavailable, pass on matching Node version
- [x] All 47 existing tests pass, 0 regressions
- [x] TypeScript compiles cleanly (`tsc --noEmit`)

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Introduces new persistence paths and schema (`job_proofs`) and changes coordinator lifecycle semantics, which could affect runtime behavior and memory usage if callers relied on the old singletons.
> 
> **Overview**
> **JobLedger is now durable when a `better-sqlite3` DB is provided.** `JobLedger` accepts an optional SQLite `Database`, loads recent history on startup, writes new jobs to a new `job_proofs` table, and switches read paths (`getContributions`, `getLeaderboard`, `getPlayerJobs`, `getTotalJobs`) to SQL when DB-backed while keeping an in-memory fallback.
> 
> **Turn coordination is no longer a process-wide singleton.** `getTurnCoordinator` now returns a per-`partyId` instance from an LRU-capped registry, adds `releaseTurnCoordinator`, and extends `resetTurnCoordinator` to reset one or
