# PR #120: [agento] feat: implement server-side dice rolling (worldai_claw-398)

**Repo:** jleechanorg/worldai_claw
**Merged:** 2026-03-28
**Author:** jleechan2015
**Stats:** +624/-44 in 11 files

## Summary
(none)

## Raw Body
### Background
Implement server-side dice rolling logic to ensure fairness and prevent LLM fabrication.

### Goals
- Centralize dice rolling logic in DiceRoller.
- Support standard dice notation (e.g. 1d20+5).
- Automatically process 'roll' mechanic requests in the state reducer.
- Persist roll results in the game state (last_roll) for narrative context.

### Testing
- Unit tests in packages/backend/tests/dice_roller.test.ts.
- Integration tests in packages/backend/tests/state_reducer_dice.test.ts.
- Verified critical success/failure and invalid notation handling.

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Introduces server-side randomness and expands the `mechanic_requests` contract (`roll`, `notation`, `roll_result`, `roll_errors`), which can affect turn processing/state updates across clients and persistence if not handled consistently.
>
> **Overview**
> Adds **server-side dice rolling** so the backend, not the LLM, produces authoritative roll outcomes.
>
> Introduces `DiceRoller` (notation parsing + bounds + crypto RNG) and `DiceExecutor` to resolve `missing_die` and new `roll` mechanic requests, and wires execution into both streaming and non-streaming turn flows in `app.ts`.
>
> Extends the mechanic-request contract/types and parsing to support `roll` + `notation`, and updates `applyStateReducer` to record a compact `last_roll` summary in state (with a fallback roll path for direct reducer callers). Adds unit/integration tests covering notation handling, errors, and state updates.
>
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 4fef81cce7cef8899e0b5c336ee3bbdb3079addb.

<!-- COPILOT_TRACKING_START -->
## Copilot Comment Tracking

| # | Reviewer | File | Severity | Status | Summary |
|---|----------|------|----------|--------|---------|
| 1 | chatgpt-codex-connector | state_reducer.ts:600 | HIGH | ALREADY_IMPLEMENTED | `roll` branch fully implemented at lines 598-619 with DiceRoller.roll() call. Not stal
