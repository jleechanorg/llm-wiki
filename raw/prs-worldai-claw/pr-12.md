# PR #12: test(ui): add real OpenClaw setup wizard smoke test with probe validation

**Repo:** jleechanorg/worldai_claw
**Merged:** 2026-02-22
**Author:** jleechan2015
**Stats:** +1902/-420 in 19 files

## Summary
- Add a real-browser pytest smoke test for the OpenClaw setup wizard in `testing_ui/test_openclaw_setup_wizard_pytest.py`.
- Exercise `/api/openclaw/test-connection` and fail fast on non-success payloads while surfacing explicit backend response detail.
- Resolve gateway URL/token from env (with fallback to `~/.bashrc`) and require `OPENCLAW_GATEWAY_TOKEN` for deterministic execution.
- Retry once for transient `models endpoint returned JSON but no model entries` before hard-failing.
- Keep UI a

## Raw Body
## Summary
- Add a real-browser pytest smoke test for the OpenClaw setup wizard in `testing_ui/test_openclaw_setup_wizard_pytest.py`.
- Exercise `/api/openclaw/test-connection` and fail fast on non-success payloads while surfacing explicit backend response detail.
- Resolve gateway URL/token from env (with fallback to `~/.bashrc`) and require `OPENCLAW_GATEWAY_TOKEN` for deterministic execution.
- Retry once for transient `models endpoint returned JSON but no model entries` before hard-failing.
- Keep UI assertions for setup-to-dashboard transition and preserve full artifact generation: video, traces, screenshots, console/API logs.

## Validation
- Ran on local unified server at `http://localhost:9400` with real OpenClaw endpoint (`http://localhost:18789`) and valid token.
- Command:
  - `OPENCLAW_GATEWAY_TOKEN=<token> TEST_BASE_URL=http://localhost:9400 TEST_BACKEND_URL=http://localhost:9400 python3 -m pytest testing_ui/test_openclaw_setup_wizard_pytest.py -q`
- Run result: passed with all mandatory gates.
- Latest evidence manifest: `/tmp/worldai_claw/feat_openclaw-setup-proof-clean/ui_runs/20260222_015113/evidence_manifest.json`
- Video artifact: `/tmp/worldai_claw/feat_openclaw-setup-proof-clean/videos/testing_ui_test_openclaw_setup_wizard_pytest.py__test_openclaw_setup_wizard_connects/ec5f9404eeec4f8e16b270fc843d3bce.webm` (validated via ffprobe)

## Notes
- No mocks are used in this test path; it uses the real local server + real OpenClaw gateway endpoint.

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Touches backend request routing/auth and adds a new network-probing endpoint (`/openclaw/test-connection`) with URL filtering/timeouts; mistakes could expose SSRF risk or break SPA/API routing in production.
> 
> **Overview**
> Adds unified API+SPA serving in the Fastify backend (`app.ts`): serves `packages/web/dist` (index + `/assets/*`), treats `/setup`, `/new-campaign`, `/game/:id`, and `/settings` as SPA navigations, and introduces `/api/*` pass
