# PR #162: [agento] feat(world): add multi-process duplicate-tick safety (worldai_claw-e0c)

**Repo:** jleechanorg/worldai_claw
**Merged:** 2026-03-31
**Author:** jleechan2015
**Stats:** +447/-48 in 7 files

## Summary
Add SQLite-backed atomic check-and-set guard to prevent concurrent `world_scheduler` instances from double-processing the same campaign tick during rolling deploys or multi-instance deployments.

## Raw Body
## Summary

Add SQLite-backed atomic check-and-set guard to prevent concurrent `world_scheduler` instances from double-processing the same campaign tick during rolling deploys or multi-instance deployments.

## Changes

### packages/backend/src/world/world_scheduler_lease.ts
- Add `tick_started_at INTEGER DEFAULT 0` column to `world_scheduler_leases` table
- Add `isTickRecentlyStarted(worldId)` method: returns `true` if another owner started a tick within `TICK_STARVED_MS` (30 s)
- Export `TICK_STARVED_MS = 30_000`

### packages/backend/src/world/world_scheduler.ts
- Use `isTickRecentlyStarted()` as first-line guard before lease acquisition — skips campaigns where another process started a tick within the 30 s window
- Log duplicate skips as `WARN` (not `INFO`) to distinguish from normal operation

### packages/backend/tests/world_scheduler_concurrent.test.ts
- New test file verifying that concurrent scheduler instances cannot double-fire ticks for the same campaign

### packages/backend/tests/worldSchedulerLease.test.ts
- Add `describe('isTickRecentlyStarted')` unit tests covering: no-lease, own-lease, blocked-by-other, expired-window, and legacy-row cases

### packages/backend/tests/world_scheduler_companion_ticks.test.ts
- Update mocks to include `db.exec()` and `isTickRecentlyStarted()` for compatibility with new guard logic

## Test Results

```
PASS tests/world_scheduler_concurrent.test.ts  (5 tests)
PASS tests/worldSchedulerLease.test.ts         (8 tests)
PASS tests/world_scheduler_dedup.test.ts
PASS tests/world_scheduler_env_vars.test.ts
```

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Touches scheduling/locking and SQLite schema/migration paths; concurrency and timing bugs could cause missed ticks or duplicate processing if edge cases are wrong.
> 
> **Overview**
> Prevents multi-instance `WorldScheduler` deployments from double-processing the same campaign tick by adding SQLite-
