# PR #21: [agento] test: Phase 3 — MCP server integration tests (33 tests)

**Repo:** jleechanorg/ai_universe_living_blog
**Merged:** 2026-03-27
**Author:** jleechan2015
**Stats:** +2368/-5 in 14 files

## Summary
Phase 3 of the TDD roadmap: 33 integration tests (10 HTTP + 23 tools) for the Blog MCP server.

### tests/blog/server-http.test.ts (10 tests)
Supertest-based HTTP tests against the real Express app from `createBlogApp()`:
- `POST /mcp`: health_check, unknown method (-32601), missing id, malformed JSON (400), invalid jsonrpc (-32600), non-object body (400), no-auth-required
- `GET /health`, `GET /`, `GET /mcp`

### tests/blog/tools-integration.test.ts (23 tests)
Real `MemoryBlogStorage` + `create

## Raw Body
## Summary

Phase 3 of the TDD roadmap: 33 integration tests (10 HTTP + 23 tools) for the Blog MCP server.

### tests/blog/server-http.test.ts (10 tests)
Supertest-based HTTP tests against the real Express app from `createBlogApp()`:
- `POST /mcp`: health_check, unknown method (-32601), missing id, malformed JSON (400), invalid jsonrpc (-32600), non-object body (400), no-auth-required
- `GET /health`, `GET /`, `GET /mcp`

### tests/blog/tools-integration.test.ts (23 tests)
Real `MemoryBlogStorage` + `createBlogToolHandlers()` — no mocks:
- **Blog tools** (14): create_post (pr_created, novel_branch_entry, thread reuse), get_post, list_posts (repo/eventType/posterId filters + cursor pagination), update_post, get_thread (sorted, cross-repo isolated), list_threads
- **Repo tools** (4): register_repo (add, duplicate-throws), unregister_repo, update_repo, list_repos
- **chat_worker** (4): three-tier inference backend
  - Tier 1: `OPENCLAW_INFERENCE_URL` → POST to that URL
  - Tier 2: `ANTHROPIC_API_KEY` → WorkerChat → Anthropic API
  - Tier 3: regex-only voice extraction (no key required)

### Source changes (to support tests)
- `src/blog/tools.ts`: three-tier `chat_worker` implementation
- `src/blog/auth.ts`, `repo-registry.ts`, `github-client.ts`, `webhook.ts`, `scanner.ts`: copied from `feat/remote-mode-auto-scan`
- `src/novel/chat.ts`: WorkerChat (copied from `feat/remote-mode-auto-scan`)
- `CLAUDE.md`, `README.md`: updated tools list and env vars for three-tier inference

## Test results
```
npx vitest run tests/blog/
✓ tests/blog/server-http.test.ts (10 tests)
✓ tests/blog/tools-integration.test.ts (23 tests)
Test Files  2 passed (2)
Tests       33 passed (33)
```

## Notes
- Server is fully open (no API key auth required) — per design change
- `pr_updated` → `pr_edited` (correct enum value)
- `register_repo` requires `modes` field; registry throws on duplicate
- `get_thread` cross-repo isolation tested with separate ctx

🤖 Generated with [Claude Code](https://claud
