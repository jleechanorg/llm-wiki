# PR #12: feat: agent harness overlay

**Repo:** jleechanorg/ai_universe_living_blog
**Merged:** 2026-03-26
**Author:** jleechan2015
**Stats:** +497/-0 in 8 files

## Summary
Applies the agent harness engineering pattern to ai_universe_living_blog via **overlay mode** — adds the progressive-disclosure doc tree and structural validation without rewriting existing docs.

### What was added

- `AGENTS.md` — router pointing to `docs/agent/` and the validation script
- `docs/agent/index.md` — navigation hub
- `docs/agent/architecture.md` — extracted from existing `docs/ARCHITECTURE.md`
- `docs/agent/quality.md` — PR evidence standard (/4layer) extracted from `CLAUDE.md`
-

## Raw Body
## Summary

Applies the agent harness engineering pattern to ai_universe_living_blog via **overlay mode** — adds the progressive-disclosure doc tree and structural validation without rewriting existing docs.

### What was added

- `AGENTS.md` — router pointing to `docs/agent/` and the validation script
- `docs/agent/index.md` — navigation hub
- `docs/agent/architecture.md` — extracted from existing `docs/ARCHITECTURE.md`
- `docs/agent/quality.md` — PR evidence standard (/4layer) extracted from `CLAUDE.md`
- `docs/agent/reliability.md` — runtime behavior, self-healing, env vars
- `docs/agent/security.md` — trust boundaries, secrets, CORS
- `docs/agent/plans.md` — open PRs and Phase 2 roadmap
- `scripts/agent_repo_check.py` — structural validator (frontmatter, doc presence, routing links)

### Validation

```
python3 scripts/agent_repo_check.py
# PASS — all agent harness checks passed
```

### Pre-merge gate

All PRs must now pass:

```
npm test && python3 scripts/agent_repo_check.py
```

### CI note

CI enforcement for `agent_repo_check.py` is tracked in `docs/agent/plans.md` as a next step (not included in this PR to keep scope tight).

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Documentation-only changes plus a lightweight Python validator; low risk aside from potentially introducing a new local pre-merge requirement if teams adopt the suggested check.
> 
> **Overview**
> Adds an **agent-harness overlay**: a new `AGENTS.md` entrypoint plus a progressive-disclosure doc set under `docs/agent/` (architecture, quality gates, reliability, security, plans) with required frontmatter.
> 
> Introduces `scripts/agent_repo_check.py` to structurally validate the agent docs (required files, frontmatter keys/date format, and that `AGENTS.md`/`docs/agent/index.md` cross-reference), and updates the recommended pre-merge command to run `npm test` plus this validator.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 6ff56962713b83
