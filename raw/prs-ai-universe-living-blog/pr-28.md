# PR #28: feat(cli): implement --output=both and --output=none modes for branch-entry

**Repo:** jleechanorg/ai_universe_living_blog
**Merged:** 2026-03-29
**Author:** jleechan2015
**Stats:** +208/-6 in 2 files

## Summary
The `blog-cli branch-entry` command had a `--output` flag with three documented values (`file`, `both`, `none`) but only `file` was implemented. The design doc (Section B) specifies:

| Mode | Write file | POST to MCP |
|---|---|---|
| `file` (default) | ✅ | ❌ |
| `both` | ✅ | ✅ (best-effort) |
| `none` | ❌ | ❌ |

This PR wires up the missing modes.

## Background
The `blog-cli branch-entry` command had a `--output` flag with three documented values (`file`, `both`, `none`) but only `file` was implemented. The design doc (Section B) specifies:

| Mode | Write file | POST to MCP |
|---|---|---|
| `file` (default) | ✅ | ❌ |
| `both` | ✅ | ✅ (best-effort) |
| `none` | ❌ | ❌ |

This PR wires up the missing modes.

## Raw Body
## Background

The `blog-cli branch-entry` command had a `--output` flag with three documented values (`file`, `both`, `none`) but only `file` was implemented. The design doc (Section B) specifies:

| Mode | Write file | POST to MCP |
|---|---|---|
| `file` (default) | ✅ | ❌ |
| `both` | ✅ | ✅ (best-effort) |
| `none` | ❌ | ❌ |

This PR wires up the missing modes.

## Goals

- `--output=none`: skip the prompt file write entirely
- `--output=both`: write the prompt file AND post to MCP server as `novel_branch_entry`
- MCP posting is best-effort: if the server is unreachable, log a warning and succeed (file is the source of truth)

## Tenets

- File is always the source of truth for AO worker integration
- MCP server unavailability must never fail the CLI command
- No new dependencies

## High-level description of changes

**`src/cli/main.ts`**:
- `runBranchEntryCommand` now guards file write with `output !== 'none'`
- When `output === 'both'`, calls new `postPromptToMcp()` helper
- `postPromptToMcp()` sends JSON-RPC 2.0 `tools/call` → `create_post` with `eventType: 'novel_branch_entry'`; catches all errors and logs a warning

**`tests/cli/main.test.ts`**:
- 4 new tests using a `makeFetchMock()` helper that routes GitHub API and MCP calls
- Covers: `output=none` (no file), `output=file` (no MCP call), `output=both` (file + MCP), `output=both` with unreachable MCP

## Testing

- 223 tests pass (231 total, 8 skipped)
- `npm run typecheck` clean

### Layer 1: Unit tests
```
Tests  223 passed | 8 skipped (231)
```

### Layer 3: Manual verification
The `--output=both` flow is exercised by the new tests via fetch mocks. Real MCP posting is best-effort by design.

## Low-level details

- **`src/cli/main.ts`**: Added `output !== 'none'` guard around file write (L248-252). Added `postPromptToMcp()` function called when `output === 'both'` (L254+). Added `McpPostParams` interface.
- **`tests/cli/main.test.ts`**: Added `makeFetchMock()` factory that routes `api.github.com` calls
