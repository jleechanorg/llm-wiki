# PR #20: docs: living blog refactor design + TDD roadmap

**Repo:** jleechanorg/ai_universe_living_blog
**Merged:** 2026-03-27
**Author:** jleechan2015
**Stats:** +1358/-0 in 4 files

## Summary
Design + TDD roadmap for the living blog repo redesign (Phase 1). Based on 2026-03-27 design session where Jeffrey specified:
- CLI owns GH API fetching (Option A): `blog-cli branch-entry --session ao-832 --pr 42 --repo owner/repo`
- CLI fetches PR events, commits, checks, reviews directly via GitHub REST API
- CLI writes `novel/workers/{sessionId}.md`, optionally POSTs to MCP via `--output=both`
- MCP server for non-AO repos and interactive chat (Path B + Path C)
- FIFO bidirectional chat for w

## Background
Design + TDD roadmap for the living blog repo redesign (Phase 1). Based on 2026-03-27 design session where Jeffrey specified:
- CLI owns GH API fetching (Option A): `blog-cli branch-entry --session ao-832 --pr 42 --repo owner/repo`
- CLI fetches PR events, commits, checks, reviews directly via GitHub REST API
- CLI writes `novel/workers/{sessionId}.md`, optionally POSTs to MCP via `--output=both`
- MCP server for non-AO repos and interactive chat (Path B + Path C)
- FIFO bidirectional chat for workers that expose `~/.blog/inbox/{sessionId}`

## Raw Body
## Background

Design + TDD roadmap for the living blog repo redesign (Phase 1). Based on 2026-03-27 design session where Jeffrey specified:
- CLI owns GH API fetching (Option A): `blog-cli branch-entry --session ao-832 --pr 42 --repo owner/repo`
- CLI fetches PR events, commits, checks, reviews directly via GitHub REST API
- CLI writes `novel/workers/{sessionId}.md`, optionally POSTs to MCP via `--output=both`
- MCP server for non-AO repos and interactive chat (Path B + Path C)
- FIFO bidirectional chat for workers that expose `~/.blog/inbox/{sessionId}`

## Goals

1. Write the full design doc so Sonnet workers can implement in Phase 2
2. Write the TDD roadmap so tests drive implementation order

## What changed

### `docs/plans/2026-03-27-living-blog-refactor-design.md`
- Section A: CLI design — `branch-entry`, `daily-summary`, `chat`, `register-repo`, `config` commands
- Section B: CLI → MCP posting — output modes (`file`/`both`/`none`), `BLOG_SERVER_URL` env var
- Section C: MCP server surface — 13 tools, auth/scope model, rate limits
- Section D: Demo mode — `--repo owner/repo --demo` reads git history without AO
- Section E: FIFO bidirectional chat — write to `~/.blog/inbox/{sessionId}`, 5s timeout → WorkerChat fallback
- Section F: Full env var matrix (server vs CLI)

### `docs/plans/2026-03-27-tdd-roadmap.md`
- Phase 1: `GitHubClient` unit tests (12 tests)
- Phase 2: CLI argument parser + GH client integration (20 tests)
- Phase 3: MCP HTTP transport + tools integration (33 tests)
- Phase 4: CLI↔MCP E2E smoke (9 tests)
- Phase 5: FIFO chat fallback (5 tests)
- **Total: ~79 tests, 6 test files**

## Testing

Design doc — not code, no tests applicable.

## Next steps

1. Code review of these docs (this PR)
2. Sonnet workers pick up TDD roadmap and implement Phase 1 tests first, then implementation

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Primarily adds documentation and isolated ne
