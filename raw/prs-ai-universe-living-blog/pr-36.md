# PR #36: test(scanner): AutoScanner full poll-cycle integration tests (10 tests)

**Repo:** jleechanorg/ai_universe_living_blog
**Merged:** 2026-03-29
**Author:** jleechan2015
**Stats:** +303/-0 in 1 files

## Summary
(none)

## Raw Body
Adds 10 integration tests for the AutoScanner poll cycle: event routing (pr_created/pr_merged/pr_checks_passed), cursor deduplication, disabled/autoScan=false skips, GitHub API error resilience, stop() freezes polling. Uses real MemoryBlogStorage + mocked GitHubClient. 342 tests total (up from 332).

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Low risk: adds a new Vitest integration test suite only, exercising existing AutoScanner behavior without changing production code paths.
> 
> **Overview**
> Adds a new Vitest integration test suite (`tests/blog/scanner-integration.test.ts`) that runs the AutoScanner through full poll cycles using a real `MemoryBlogStorage`, a `RepoRegistry` persisted to a temp `dataDir`, and a mocked `GitHubClient`.
> 
> The tests verify event→post mapping for `PullRequestEvent` and `CheckRunEvent`, multi-event polls, cursor-based deduplication across intervals, repo skip conditions (`enabled=false` and `autoScan=false`), resilience to GitHub API errors, and that `stop()` halts further polling.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 8f96998c6375155602eb36ca79aa60f67a48c3fa. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->

## Summary by CodeRabbit

* **Tests**
  * Added comprehensive integration test suite for the AutoScanner, verifying correct post creation from event polling, deduplication across poll cycles, handling of disabled repositories, API error resilience, and proper shutdown behavior.

<!-- end of auto-generated comment: release notes by coderabbit.ai -->
