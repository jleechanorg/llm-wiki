# PR #41: [agento] feat(cli): Section K — list-threads, get-thread, update-post

**Repo:** jleechanorg/ai_universe_living_blog
**Merged:** 2026-03-29
**Author:** jleechan2015
**Stats:** +250/-2 in 2 files

## Summary
- Add 3 new blog-cli commands: list-threads, get-thread, update-post
- All POST to /mcp using existing MCP tools (list_threads, get_thread, update_post)
- 10 new parseArgs unit tests in tests/cli/thread-commands.test.ts

## Raw Body
## Summary
- Add 3 new blog-cli commands: list-threads, get-thread, update-post
- All POST to /mcp using existing MCP tools (list_threads, get_thread, update_post)
- 10 new parseArgs unit tests in tests/cli/thread-commands.test.ts

## Changes
- src/cli/main.ts: ParsedArgs fields + parseArgs switch cases + 3 handlers wired into main()
- tests/cli/thread-commands.test.ts: New file, 10 tests

## Testing
- npx vitest run tests/cli/thread-commands.test.ts: 10/10 pass
- npm test: 440 total (423 pass, 8 skip; 9 pre-existing failures in admin-commands.test.ts)
- npm run build: clean

Pre-existing failures (admin-commands.test.ts) were present on main before this PR.

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Adds new CLI commands that call MCP write/read tools (including `update_post`), so incorrect arg parsing or parameter mapping could lead to unintended data changes or confusing output.
> 
> **Overview**
> Adds three new `blog-cli` commands: **`list-threads`**, **`get-thread`**, and **`update-post`**, wiring them into `parseArgs` usage text and the main command dispatch.
> 
> Implements handlers that call MCP tools `list_threads`, `get_thread`, and `update_post`, including table/pretty output modes and a `--json` option for thread commands; `update-post` supports updating `title`, `content`, `tags`, and `status`.
> 
> Introduces a new Vitest suite (`tests/cli/thread-commands.test.ts`) covering parse-time validation and required-flag error paths for the new commands.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 32b30f5327f9db537e304f0b307020e2028f61fd. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
