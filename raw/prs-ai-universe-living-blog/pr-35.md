# PR #35: test(webhook): HMAC validation + event routing (10 tests)

**Repo:** jleechanorg/ai_universe_living_blog
**Merged:** 2026-03-29
**Author:** jleechan2015
**Stats:** +223/-1 in 2 files

## Summary
(none)

## Raw Body
Adds 10 tests for the POST /webhook handler: HMAC-SHA256 signature validation (valid/invalid/missing), event routing (pr_created/pr_merged/pr_checks_passed), edge cases (unknown event, missing repo, ping). 342 tests total (up from 332).

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Low risk: changes are primarily new webhook integration tests plus a small CLI parameter tweak; no production webhook logic is modified.
> 
> **Overview**
> Adds `tests/blog/webhook.test.ts`, a 10-case supertest suite for `POST /webhook` covering **HMAC-SHA256 signature validation** (valid/invalid/missing), **event routing** (`pull_request` → `pr_created`/`pr_merged`, `check_run` → `pr_checks_passed`), and graceful handling for `ping`, unknown events, and malformed payloads; it also verifies that a valid webhook request persists a `webhook`-tagged post in memory storage.
> 
> Updates `blog-cli daily-summary` to pass `branchName: 'daily'` into `runDailySummaryPipeline`.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit ff9532e84832dccde849c086997d93ed6aa26197. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->

## Summary by CodeRabbit

* **Tests**
  * Added comprehensive test coverage for webhook functionality, including signature validation, event routing, and data persistence verification.

<!-- end of auto-generated comment: release notes by coderabbit.ai -->
