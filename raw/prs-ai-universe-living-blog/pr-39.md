# PR #39: [agento] feat(section-i): webhook idempotency + optional X-API-Key auth

**Repo:** jleechanorg/ai_universe_living_blog
**Merged:** 2026-03-29
**Author:** jleechan2015
**Stats:** +215/-2 in 5 files

## Summary
Section I implements two production-hardening features for the blog MCP server:

- **I.1 Webhook idempotency** — deduplicate posts by `X-GitHub-Delivery` ID; same delivery replayed twice creates only one post (200, `duplicate: true`)
- **I.2 Optional X-API-Key auth** — when `AUTH_API_KEY` env var or `authApiKey` option is set, `POST /mcp` requires `X-API-Key` header; `GET` routes remain open

## Test Plan
- [x] `npm test` — 403 passed, 8 skipped (up from 394 → 403, +9 tests)
- [x] `npm run build` — tsc clean, no errors
- [x] Manual webhook deduplication smoke test (same delivery ID → 200 + `duplicate: true`)
- [x] Manual auth smoke test (`AUTH_API_KEY=xxx POST /mcp` without key → 401)

## Raw Body
## Summary

Section I implements two production-hardening features for the blog MCP server:

- **I.1 Webhook idempotency** — deduplicate posts by `X-GitHub-Delivery` ID; same delivery replayed twice creates only one post (200, `duplicate: true`)
- **I.2 Optional X-API-Key auth** — when `AUTH_API_KEY` env var or `authApiKey` option is set, `POST /mcp` requires `X-API-Key` header; `GET` routes remain open

## Changes

### I.1 Webhook idempotency
- `src/shared/types.ts`: Add `deliveryId?: string` to `Post` interface and `PostSchema`
- `src/blog/webhook.ts`: Before `create_post`, check if a post with the same `webhook:delivery:<id>` tag already exists; return `{ ok, duplicate, postId }` idempotent response if found
- Store delivery ID as `webhook:delivery:<deliveryId>` tag on every created post
- `tests/blog/webhook.test.ts`: 4 new tests (+14 total)

### I.2 Optional X-API-Key auth
- `src/blog/server.ts`: Add `authApiKey?: string` option to `createBlogApp`; when set, middleware validates `X-API-Key` header on `POST /mcp` (returns 401 if missing/wrong); `GET /health`, `/`, `/mcp`, `/metrics` remain open
- `tests/blog/server-http.test.ts`: 5 new tests (+21 total)

## Test plan

- [x] `npm test` — 403 passed, 8 skipped (up from 394 → 403, +9 tests)
- [x] `npm run build` — tsc clean, no errors
- [x] Manual webhook deduplication smoke test (same delivery ID → 200 + `duplicate: true`)
- [x] Manual auth smoke test (`AUTH_API_KEY=xxx POST /mcp` without key → 401)

## Testing notes

Tests use `MemoryBlogStorage` (real, not mocked). Webhook tests use `supertest` against a real Express app. Auth tests cover all 5 cases from the design doc.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Adds new request-gating on `POST /mcp` and changes webhook ingestion behavior to dedupe deliveries, which could affect integrations if misconfigured or if deduping misses/overmatches events.
> 
> **Overview**
> Hardens the 
