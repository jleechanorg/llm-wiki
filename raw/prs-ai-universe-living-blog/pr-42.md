# PR #42: [agento] feat(cli): Section L — update-repo, generate-api-key, replay-event

**Repo:** jleechanorg/ai_universe_living_blog
**Merged:** 2026-03-29
**Author:** jleechan2015
**Stats:** +179/-1 in 2 files

## Summary
Sections K and J added thread/write CLI commands. Section L completes 100% CLI coverage for all 19 MCP tools by adding the final 3 CLI commands: \`update-repo\`, \`generate-api-key\`, \`replay-event\`.

## Background
Sections K and J added thread/write CLI commands. Section L completes 100% CLI coverage for all 19 MCP tools by adding the final 3 CLI commands: \`update-repo\`, \`generate-api-key\`, \`replay-event\`.

## Raw Body
## Background

Sections K and J added thread/write CLI commands. Section L completes 100% CLI coverage for all 19 MCP tools by adding the final 3 CLI commands: \`update-repo\`, \`generate-api-key\`, \`replay-event\`.

## Goals

- All 19 MCP tools have a CLI wrapper
- 9 new parseArgs tests (432 total passing, 8 skipped)

## Tenets

- Minimize scope: parseArgs + HTTP callMcpTool, no new abstractions
- TDD: tests written first, all pass

## High-level description of changes

Added 3 new commands in \`src/cli/main.ts\`:
- **L.1 \`update-repo\`**: wraps \`update_repo\` tool — accepts \`--enabled\` and \`--auto-scan\` boolean flags
- **L.2 \`generate-api-key\`**: wraps \`generate_api_key\` tool — prints generated key
- **L.3 \`replay-event\`**: wraps \`replay_event\` tool — accepts \`--event-type\` and optional \`--count\`

## Testing

- 9 new parseArgs tests in \`tests/cli/admin-commands.test.ts\` — all pass
- Full suite: 432 passing, 8 skipped (6 Firestore emulator, 1 ANTHROPIC_API_KEY, 1 storage-factory)
- \`npm run build\` — no TypeScript errors

## Low-level details

- **\`src/cli/main.ts\`**: Added \`enabled?: boolean\`, \`count?: number\` to ParsedArgs; added 3 parseArgs cases; added 3 command handlers (\`runUpdateRepoCommand\`, \`runGenerateApiKeyCommand\`, \`runReplayEventCommand\`); wired into main() switch
- **\`tests/cli/admin-commands.test.ts\`**: 9 tests covering parseArgs validation for all 3 commands

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Adds new CLI entry points that can mutate repo settings and generate/print API keys, so mistakes in argument parsing or tool invocation could have operational impact. Changes are localized to CLI parsing/dispatch with accompanying tests, limiting blast radius.
> 
> **Overview**
> Adds **three new `blog-cli` admin commands** wired through `parseArgs` and the main command switch: `update-repo` (toggles `enabled`/`autoScan`), `generate-api-key` (calls `generate_api_key` and prints the key when returned),
