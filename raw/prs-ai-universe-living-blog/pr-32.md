# PR #32: feat(server): wire AUTO_SCAN_ENABLED and AUTO_SCAN_INTERVAL_MS env vars

**Repo:** jleechanorg/ai_universe_living_blog
**Merged:** 2026-03-29
**Author:** jleechan2015
**Stats:** +20/-0 in 1 files

## Summary
AutoScanner (scanner.ts) was implemented in PR #19 but never started by the server. Design doc Section F specifies both env vars. This PR closes that gap.

## Background
AutoScanner (scanner.ts) was implemented in PR #19 but never started by the server. Design doc Section F specifies both env vars. This PR closes that gap.

## Raw Body
## Background
AutoScanner (scanner.ts) was implemented in PR #19 but never started by the server. Design doc Section F specifies both env vars. This PR closes that gap.

## Goals
- Wire AUTO_SCAN_ENABLED=true to opt into background polling
- Wire AUTO_SCAN_INTERVAL_MS to configure polling cadence (default: 60000ms)

## Tenets
- Off by default — no breaking changes for existing deployments
- Minimal scope: env var wiring only, scanner implementation unchanged

## High-level description of changes
Added AUTO_SCAN_ENABLED and AUTO_SCAN_INTERVAL_MS to server config. Added import for createAutoScanner. In createBlogApp(), after webhook handler, conditionally start scanner.

## Testing
- 307 tests pass (28 files)
- AUTO_SCAN_ENABLED defaults false so existing tests unaffected
- Scanner tests in tests/blog/scanner.test.ts pass

## Low-level details
- **src/blog/server.ts**: Added import, two env var constants, auto-scanner start block (20 lines)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Adds an opt-in background polling loop that can generate new storage writes and GitHub API traffic when enabled, so misconfiguration could impact load/quotas. Default behavior remains unchanged because scanning is off unless `AUTO_SCAN_ENABLED=true`.
> 
> **Overview**
> Wires up the previously-implemented `AutoScanner` so the blog server can run background GitHub polling when explicitly enabled.
> 
> Adds `AUTO_SCAN_ENABLED` (off by default) and `AUTO_SCAN_INTERVAL_MS` (validated with a 60s fallback) env var handling, and conditionally instantiates and `start()`s the scanner during app initialization, logging the configured interval.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit ed08185d004e8b4474fd950e9fbb9085fe5ca7d7. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
