# PR #33: feat(cli): implement blog-cli daily-summary command (5 tests)

**Repo:** jleechanorg/ai_universe_living_blog
**Merged:** 2026-03-29
**Author:** jleechan2015
**Stats:** +174/-3 in 2 files

## Summary
blog-cli daily-summary was stubbed — it just printed 'Use: npm run dev:novel -- daily-summary'. The design doc (Section A) specifies this as a first-class CLI command.

## Background
blog-cli daily-summary was stubbed — it just printed 'Use: npm run dev:novel -- daily-summary'. The design doc (Section A) specifies this as a first-class CLI command.

## Raw Body
## Background
blog-cli daily-summary was stubbed — it just printed 'Use: npm run dev:novel -- daily-summary'. The design doc (Section A) specifies this as a first-class CLI command.

## Goals
- Replace stub with real runDailySummaryCommand
- Fetch posts from MCP server (best-effort; falls back to empty on unreachable)
- Call runDailySummaryPipeline with fetched posts and emit JSON output
- Add 5 tests covering the command behavior

## Tenets
- Consistent behavior with other commands (throws on missing --repo)
- Server unreachable is a safe no-op (skips with JSON output)
- No LLM required for skip path

## High-level description of changes
Added fetchPostsFromMcp() helper (paginated list_posts via MCP HTTP). Added runDailySummaryCommand() that uses it + runDailySummaryPipeline. Wired into the main switch. Added tests/cli/daily-summary.test.ts with 5 tests.

## Testing
332 tests pass (30 files). 5 new tests in tests/cli/daily-summary.test.ts.

## Low-level details
- **src/cli/main.ts**: Added fetchPostsFromMcp(), runDailySummaryCommand(), wired switch case (86 lines)
- **tests/cli/daily-summary.test.ts**: 5 tests for skip conditions and input validation (new file)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Adds a new CLI flow that performs paginated network calls to the MCP server and drives the daily-summary generation pipeline; failures or API shape mismatches could change CLI behavior and output.
> 
> **Overview**
> Implements `blog-cli daily-summary` as a first-class command instead of a stub. The command now **best-effort fetches** the day’s posts from the MCP server via paginated `list_posts`, filters them by `createdAt` date prefix, and then runs `runDailySummaryPipeline` using in-memory storage.
> 
> Outputs structured JSON for both *skipped* (includes `reason`/`date`) and successful runs (includes `date`, `sessionId`, `wordCount`, `postId`), and wires the command into the main CLI switch. Adds 5 new Vitest cases covering missing `--repo`, def
