# PR #45: test: verify self-hosted runner CI

**Repo:** jleechanorg/ai_universe_living_blog
**Merged:** 2026-03-29
**Author:** jleechan2015
**Stats:** +21/-0 in 2 files

## Summary
PR #44 merged self-hosted runner config. This test PR verifies that CI now runs on `[self-hosted, ai_universe_living_blog]` instead of `ubuntu-latest`.

## Background
PR #44 merged self-hosted runner config. This test PR verifies that CI now runs on `[self-hosted, ai_universe_living_blog]` instead of `ubuntu-latest`.

## Raw Body
## Background
PR #44 merged self-hosted runner config. This test PR verifies that CI now runs on `[self-hosted, ai_universe_living_blog]` instead of `ubuntu-latest`.

## Goals
- Confirm `Test + Agent Harness Validation` job runs on self-hosted runner
- Confirm Skeptic Gate, Novel Entry also fire on self-hosted

## Testing
Watch CI checks — if they pass with self-hosted runner label, the config is working.

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Low risk: workflow-only changes to make CI more tolerant of self-hosted macOS runner quirks; primary risk is masking real environment setup failures if the fallbacks don’t apply.
> 
> **Overview**
> Improves the CI workflow’s resilience on self-hosted macOS runners by allowing `actions/setup-java` and `actions/setup-python` to fail without failing the job, and adding a fallback step to set `JAVA_HOME`/`PATH` from a cached Temurin install when Java setup fails.
> 
> Adds a small evidence doc (`docs/evidence/self-hosted-runner-test.md`) to record the self-hosted runner verification run.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit d34094233a014dc5b07f5ce0b7b6345371376fcd. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->
## Summary by CodeRabbit

* **Documentation**
  * Added internal verification test documentation for self-hosted runner checks.

* **Chores**
  * Improved CI workflow to better detect and configure Java on self-hosted macOS runners, reducing spurious build failures.
<!-- end of auto-generated comment: release notes by coderabbit.ai -->
