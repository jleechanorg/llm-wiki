# PR #40: [agento] feat(cli): Section J write commands + repo management

**Repo:** jleechanorg/ai_universe_living_blog
**Merged:** 2026-03-29
**Author:** jleechan2015
**Stats:** +224/-2 in 2 files

## Summary
Section J adds the write-side CLI surface to `blog-cli`:
- `delete` — remove a post by ID via `delete_post` MCP tool
- `unregister-repo` — remove a registered repo via `unregister_repo` MCP tool
- `list-repos` — list all registered repos (`--json` for raw JSON)
- `export` — export repo posts/threads to JSON (`--output-file` or stdout)

## Raw Body
## Summary
Section J adds the write-side CLI surface to `blog-cli`:
- `delete` — remove a post by ID via `delete_post` MCP tool
- `unregister-repo` — remove a registered repo via `unregister_repo` MCP tool
- `list-repos` — list all registered repos (`--json` for raw JSON)
- `export` — export repo posts/threads to JSON (`--output-file` or stdout)

## Changes
- **src/cli/main.ts**: Added `ParsedArgs` fields, `parseArgs` cases, command handlers (`runDeleteCommand`, `runUnregisterRepoCommand`, `runListReposCommand`, `runExportCommand`), wired into `main()`
- **tests/cli/write-commands.test.ts**: 10 new parseArgs tests covering all 4 commands

## Testing
- `npx vitest run tests/cli/write-commands.test.ts` — 10/10 pass
- `npm test` — 413 passed, 8 skipped, 421 total
- `npm run build` — clean TypeScript compilation, no errors

## Low-level details
- **src/cli/main.ts**: `outputFile` field added to `ParsedArgs` to avoid collision with branch-entry's `output`. `callMcpTool` helper reused for all 4 commands. `writeFileSync` imported lazily in `runExportCommand`.
- **tests/cli/write-commands.test.ts**: Follows Section H `list-get-search.test.ts` pattern — parseArgs unit tests only, no HTTP server mocking.

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Adds new CLI commands that trigger write-side MCP operations (post deletion and repo unregister), which can cause irreversible data changes if misused; changes are otherwise localized to CLI parsing/dispatch and output formatting.
> 
> **Overview**
> Adds four new `blog-cli` commands—`delete`, `unregister-repo`, `list-repos`, and `export`—including new `parseArgs` cases, usage text updates, and wiring into `main()` dispatch.
> 
> Implements handlers that call MCP tools (`delete_post`, `unregister_repo`, `list_repos`, `export_repo`), adds `--output-file` support for `export`, and provides basic non-JSON table output for `list-repos`.
> 
> Introduces new Vitest coverage for argument parsing/validation for all four comm
