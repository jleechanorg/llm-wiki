# PR #135: Handle orphaned project references during migrations

**Repo:** jleechanorg/mcp_mail
**Merged:** 2025-12-18
**Author:** jleechan2015
**Stats:** +70/-0 in 1 files
**Labels:** codex

## Summary
- Clean orphaned project references before recreating the agents and messages tables so new foreign key enforcement does not fail
- Log and normalize orphaned project_ids to NULL while raising explicit errors for orphaned sender references during migration
- Centralize a module logger for reuse in migration helpers

## Raw Body
## Summary
- Clean orphaned project references before recreating the agents and messages tables so new foreign key enforcement does not fail
- Log and normalize orphaned project_ids to NULL while raising explicit errors for orphaned sender references during migration
- Centralize a module logger for reuse in migration helpers

## Testing
- `ruff check --fix --unsafe-fixes`
- `uvx ty check` *(fails with existing repository-wide diagnostics; no new type issues introduced in this change)*

------
[Codex Task](https://chatgpt.com/codex/tasks/task_e_694396198ac4832f9a0401cf4c3a4cc8)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Normalize orphaned `project_id` to NULL before recreating `agents`/`messages`, validate `sender_id` integrity in `messages`, and add a module logger.
> 
> - **Database Migrations**:
>   - **Agents**: Before table recreate, detect and NULL-out orphaned `project_id` values to avoid FK failures.
>   - **Messages**: Before table recreate, detect and NULL-out orphaned `project_id` values; validate `sender_id` references and fail with a clear error if orphaned.
> - **Logging**: Introduce module-level `logger` and use it to warn about normalized orphaned references.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 94e0063186bb80c59086474723a8f88a5a5d44ee. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
