# PR #85: Add optional project-key storage mode

**Repo:** jleechanorg/mcp_mail
**Merged:** 2025-11-29
**Author:** jleechan2015
**Stats:** +424/-248 in 19 files

## Summary
- add optional project-key anchored storage with git verification and prompt-based resolution instead of silent fallback
- add config toggles for project-key storage, prompt enablement, and local archive enablement; default remains .mcp_mail
- add integration coverage for project-key storage success, git enforcement, and prompt payload for missing repos

## Raw Body
## Summary
- add optional project-key anchored storage with git verification and prompt-based resolution instead of silent fallback
- add config toggles for project-key storage, prompt enablement, and local archive enablement; default remains .mcp_mail
- add integration coverage for project-key storage success, git enforcement, and prompt payload for missing repos

## Testing
- uv run pytest tests/integration/test_project_key_storage.py
- uv run ruff check --fix --unsafe-fixes
- uvx ty check # fails: existing repo-wide type issues unrelated to this change (see run output)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Adds optional git-verified project-key storage with promptable resolution, threads project_key through archive calls, and updates config, HTTP/CLI handling, docs, tests, and local git hook setup.
> 
> - **Storage (core change)**:
>   - Introduce `ProjectStorageResolutionError` and promptable resolution flow when `project_key` is invalid.
>   - Add project-key–anchored archive resolution (`_resolve_repo_root`, `ensure_archive_root`, `ensure_archive` now accept `project_key`).
>   - Enforce repo-root validation; support toggles: `STORAGE_PROJECT_KEY_ENABLED`, `STORAGE_LOCAL_ARCHIVE_ENABLED`, `STORAGE_PROJECT_KEY_PROMPT_ENABLED`.
> - **Server/HTTP/CLI plumbing**:
>   - Pass `project_key` (usually `project.human_key`) to all `ensure_archive(...)` call sites across tools/resources, guard installers, slots, and CLI flows.
>   - Wrap storage resolution errors as tool errors with recoverable prompts in `app.py`.
>   - HTTP views/resources resolve `(slug,human_key)` and use `project_key` when ensuring archives.
> - **Config**:
>   - Extend `StorageSettings` with new flags; load from env.
> - **Tests**:
>   - Add integration suite `tests/integration/test_project_key_storage.py` covering repo-local writes, git enforcement, and prompt payload.
> - **Docs**:
>   - README config matrix updated; usage examples include project-key mode.
>   - Add notes in `AGENTS.md` and `
