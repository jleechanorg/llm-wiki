# PR #78: Fix agent discovery UX for global namespace

**Repo:** jleechanorg/mcp_mail
**Merged:** 2025-11-27
**Author:** jleechan2015
**Stats:** +867/-458 in 6 files

## Summary
(none)

## Raw Body
<!-- CURSOR_SUMMARY -->
> [!NOTE]
> Introduce globally unique agent names with a global directory, enhance discovery (whois fallback + suggestions), support cross‑project messaging end-to-end, and update docs/tests accordingly.
> 
> - **Backend**:
>   - **Global Directory**: Add `resource://agents` listing all active agents globally with `project_*` metadata and `unread_count`; deprecate `resource://agents/{project_key}` (now filtered view with notice).
>   - **Discovery UX**: `whois` now falls back to global lookup, returns suggestions via new `_find_similar_agents`, and pulls commits from the agent's actual project; improved NOT_FOUND tips.
>   - **Messaging/Search**: `send_message` and `search_mailbox` use global agent lookup; errors and tips point to `resource://agents`; refine global inbox wording.
>   - **Resources**: Add global agents resource; tweak project/global inbox helpers and notices.
> - **Documentation**:
>   - Update `AGENT_ONBOARDING.md`, `CROSS_PROJECT_COORDINATION.md`, and `README.md` to reflect global agent namespace, cross‑project messaging, and new resource URIs; mark project-scoped directory as deprecated.
> - **Tests**:
>   - Add `tests/test_agent_discovery_ux.py` (similar-name suggestions, whois global fallback, commit scope).
>   - Update integration tests to respect global uniqueness and validate project-context search behavior.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 2c4f3828a68afd6e8b0051e0c8aeb1673a0c6895. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->
## Summary by CodeRabbit

* **New Features**
  * Agent names are globally unique (case-insensitive) and cross-project messaging is supported.
  * Global agent directory added (shows active agents and unread counts).
  * Lookup now falls back to global discovery and returns imp
