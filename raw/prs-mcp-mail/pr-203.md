# PR #203: fix: orchestration CLI integration test model placeholder and tool validation

**Repo:** jleechanorg/mcp_mail
**Merged:** 2026-02-02
**Author:** jleechan2015
**Stats:** +34/-7 in 4 files

## Summary
- Add `{model}` placeholder to `command_template.format()` with default "sonnet" to fix KeyError when orchestration library templates include `{model}`
- Update MCP tool validation to accept either core tools OR extended tools as both indicate successful MCP connectivity

## Test Plan
- [x] Run `python tests/integration/test_orch_cli_integration.py --cli claude` - all 5 tests pass
- [ ] Verify other CLI tests (cursor, codex, gemini) still work if installed

## Raw Body
## Summary
- Add `{model}` placeholder to `command_template.format()` with default "sonnet" to fix KeyError when orchestration library templates include `{model}`
- Update MCP tool validation to accept either core tools OR extended tools as both indicate successful MCP connectivity

## Test plan
- [x] Run `python tests/integration/test_orch_cli_integration.py --cli claude` - all 5 tests pass
- [ ] Verify other CLI tests (cursor, codex, gemini) still work if installed

## Test Results
```
  PASS orchestration: Available
  PASS cli: Installed and responding  
  PASS mcp_server: mcp-agent-mail reachable at 127.0.0.1:8765
  PASS mcp_tools: MCP Agent Mail tools available (core)
  PASS basic_invocation: CLI responded correctly
```

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Changes are limited to test harness/utility code and package version bumps; main risk is test flakiness if defaults don’t match a given CLI’s expected model name or tool output format.
> 
> **Overview**
> Fixes orchestration-based CLI command construction in `testing_llm/run_real_cli_test.py` and the integration test harness so `command_template` strings that include `{model}` no longer raise formatting errors, by injecting a quoted model value with sensible per-CLI defaults.
> 
> Updates MCP tool validation in `tests/integration/test_harness_utils.py` to treat a complete *core* toolset or a complete *extended* toolset as success (and improve the failure message accordingly), reducing false negatives when different CLIs expose different tool subsets.
> 
> Bumps the package version to `0.1.18` (and updates `uv.lock` accordingly).
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 62582a74b637423966911f5f3754e359ed23e977. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated com
