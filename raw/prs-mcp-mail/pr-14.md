# PR #14: Review commits from original repository

**Repo:** jleechanorg/mcp_mail
**Merged:** 2025-11-12
**Author:** jleechan2015
**Stats:** +7515/-1591 in 75 files

## Summary
(none)

## Raw Body
<!-- CURSOR_SUMMARY -->
> [!NOTE]
> Introduces build slots and a pre-push guard, adds share bundle update flow with signing/chunking, and optimizes search via materialized views/indexes with viewer and CLI enhancements.
> 
> - **Core/Workflow**:
>   - Add advisory build slots (`acquire_build_slot`, `renew_build_slot`, `release_build_slot`) and helpers (`amctl env`, `am-run`).
>   - Implement Git pre-push guard (conflict checks across pushed commits) plus `guard status`.
> - **Share/Export**:
>   - New `share update` to refresh existing bundles; resume, preview, and richer deploy flows.
>   - Support Ed25519 signing overwrite and database chunking with `chunks.sha256` manifest.
> - **Search/DB**:
>   - Build materialized views (`message_overview_mv`, `attachments_by_message_mv`) and performance indexes; add lowercase columns for case-insensitive search.
> - **Viewer/UI**:
>   - FTS search with LIKE fallback; add "Longest" sort; expose `body_length` and snippets; unified inbox improvements.
> - **CLI/HTTP**:
>   - New commands: `guard status`, `projects adopt`, `amctl env`, `am-run`, `mail status`; expanded resources/views and inbox/outbox helpers.
> - **Docs/CI/Tooling**:
>   - Expand `AGENTS.md` (worktrees, guards, build slots, ast-grep); README export section updates.
>   - CI: split Ruff lint/format; pre-commit: ruff hook updates and full-repo checks.
>   - Add extensive test coverage for guards, build slots, share update, and DB optimizations.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 785559f4130088c556507b951bad2552666a3e97. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->
## Summary by CodeRabbit

* **New Features**
  * Build slots: acquire/renew/release for long-running tasks.
  * Pre-push guard to enforce file reservations on git push.
  * Share update
