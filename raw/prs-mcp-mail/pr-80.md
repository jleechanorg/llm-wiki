# PR #80: Run and test integration tests locally

**Repo:** jleechanorg/mcp_mail
**Merged:** 2025-12-03
**Author:** jleechan2015
**Stats:** +239/-32 in 5 files

## Summary
(none)

## Raw Body
1. Fix glob pattern conflict detection for ** patterns
   - Added _glob_patterns_overlap() function that properly handles recursive ** glob patterns using pathlib.PurePath.match()
   - Patterns like src/backend/**/*.py now correctly conflict with src/backend/auth.py

2. Fix cross-project messaging recipient resolution
   - Added cross-project addressing support to send_message's _route()
   - Now supports project:{slug}#{agent} and name@project formats
   - Matches existing behavior in reply_message

3. Fix test_message_search to use correct core tool
   - Changed from search_messages (extended tool) to search_mailbox (core tool)
   - Updated result access to use structured_content["result"]

4. Fix test_delete_agent to use extended tool interface
   - Changed to use call_extended_tool for delete_agent invocation
   - Updated verification to handle whois returning error for deleted agents

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Improve extended tool invocation/return shape and cross-project messaging (auto-register placeholders, refined self-send), update integration tests to use core search and extended delete, and add conflict resolution docs.
> 
> - **Messaging**:
>   - Refine `send_message` routing: track unknown recipients as `dict[name -> kinds]`, skip auto-register for cross-project syntaxes, auto-create placeholders, re-route newly registered, and only allow self-send when not targeting another project.
> - **Extended Tools**:
>   - `call_extended_tool`: normalize `ToolResult` to plain payload, avoid double-wrapping, and consistently return `{"result": ...}`.
> - **Search**:
>   - Switch integration usage from extended `search_messages` to core `search_mailbox` (with `include_bodies`).
> - **Identity/Deletion**:
>   - Tests invoke `delete_agent` via `call_extended_tool` and handle wrapped/JSON text payloads; adjust `whois` verification to accept error or inactive state.
> - **Docs**:
>   - Add conflict resolution report and index under `docs
