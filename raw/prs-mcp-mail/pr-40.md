# PR #40: Fix pre-push guard tests and materialized view schema compatibility

**Repo:** jleechanorg/mcp_mail
**Merged:** 2025-11-15
**Author:** jleechan2015
**Stats:** +485/-55 in 10 files

## Summary
This PR fixes two issues:
1. **Pre-push guard tests** - All 7 tests now passing
2. **Materialized view schema compatibility** - Handles databases without thread_id column

## Raw Body
## Summary

This PR fixes two issues:
1. **Pre-push guard tests** - All 7 tests now passing
2. **Materialized view schema compatibility** - Handles databases without thread_id column

## Primary Fix: Pre-push Guard Tests (7/7 passing)

**Issue**: `git diff-tree` missing `--root` flag
- Root commits (commits with no parent) returned 0 changed files
- Test repos create fresh root commits, so conflict detection failed

**Fix**: `src/mcp_agent_mail/guard.py:190`
- Added `--root` flag to git diff-tree command
- Now handles both root commits and regular commits correctly

**Test Fixes**: `tests/test_prepush_guard.py`
- Fixed invalid Python code in test data (console.log → print)
- Added `_skip_presubmit_in_script()` helper to bypass linter checks
- All 7 pre-push guard tests now passing

## Secondary Fix: Materialized View Schema Compatibility

**Issue**: `build_materialized_views()` unconditionally referenced `thread_id`
- Old database schemas don't have `thread_id` column
- Integration tests were failing with "no such column: m.thread_id"

**Fix**: `src/mcp_agent_mail/share.py:915-999`
- Added `_column_exists()` check before referencing `thread_id`
- Fallback to synthetic `thread_id` using message ID
- Pattern: `thread_id_expr = "m.thread_id" if has_thread_id else "printf('msg:%d', m.id)"`

**Test Schema Update**: `tests/integration/test_mailbox_share_integration.py`
- Updated `_seed_mailbox()` to match current Message model
- Added missing `sender_id` and `thread_id` columns

## Test Results

✅ 7/7 pre-push guard tests passing
✅ test_share_export_end_to_end passing

## Note on Integration Test Failures

The pre-push hook showed 9 failing integration tests that are **pre-existing on main** (unrelated to these changes). My specific tests pass successfully.

---
🤖 Generated with [Claude Code](https://claude.com/claude-code)

Co-Authored-By: Claude <noreply@anthropic.com>

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Adds export pseudonymization and detached attachment bundlin
