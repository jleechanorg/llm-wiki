# PR #219: Purge remaining archive route naming and archive wording

**Repo:** jleechanorg/mcp_mail
**Merged:** 2026-02-19
**Author:** jleechan2015
**Stats:** +117/-658 in 13 files
**Labels:** codex

## Summary
(none)

## Raw Body
### Motivation
- Remove remaining archive/Git-backed surface and wording from the runtime server and make storage helpers inert so the codebase reflects database-only message persistence and avoids runtime Git operations.

### Description
- Renamed disabled archive endpoints from `/mail/archive/*` to `/mail/storage-disabled/*` and return clear "archive storage has been removed" responses in `src/mcp_agent_mail/http.py`.
- Converted `src/mcp_agent_mail/storage.py` to legacy compatibility stubs with `is_archive_enabled()` always returning `False`, and removed GitPython imports and git-activity helpers from `src/mcp_agent_mail/app.py` while marking `last_git_activity=None` where appropriate.
- Updated docstrings/comments in `src/mcp_agent_mail/app.py` to describe database-only semantics, and updated UI links in `src/mcp_agent_mail/templates/mail_message.html` and `src/mcp_agent_mail/templates/mail_claims.html` to point at the new disabled routes.
- Replaced small CLI `GitPython` usages with `git` subprocess calls in `src/mcp_agent_mail/cli.py`, adjusted storage defaults in `src/mcp_agent_mail/config.py`, and cleaned dependency references in `pyproject.toml`/`uv.lock` to remove runtime Git dependencies.

### Testing
- Ran `./scripts/ensure_git_hooks.sh` which completed successfully.
- Ran `ruff check --fix --unsafe-fixes` against the modified modules which passed and auto-fixed style issues.
- Ran `uvx ty check` on the changed modules which completed (type-check emitted repository-wide warnings but no blocking errors for the modified files).
- Ran `uv run pytest tests/test_utils_config_db_edges.py -q` which passed (4 tests).

------
[Codex Task](https://chatgpt.com/codex/tasks/task_e_69962cbf9938832fb2f37abd6d97bde5)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Moderate risk due to changes in HTTP routes/UI behavior and removal of git-based activity/provenance, plus new default on-disk paths that could affect deployments relying on the old locations.
> 
>
