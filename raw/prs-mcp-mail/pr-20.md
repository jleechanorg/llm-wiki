# PR #20: Set up automated code quality checks

**Repo:** jleechanorg/mcp_mail
**Merged:** 2025-11-10
**Author:** jleechan2015
**Stats:** +625/-3 in 7 files

## Summary
(none)

## Raw Body
<!-- CURSOR_SUMMARY -->
> [!NOTE]
> Introduces a pre-commit hook and setup script, expands CI with Bandit/Safety (Ty non-blocking), adds code quality/contributing docs, and updates dev dependencies.
> 
> - **CI**:
>   - Add `bandit` security scan and `safety` dependency vulnerability jobs in `.github/workflows/ci.yml`.
>   - Make `ty` type check non-blocking (`uvx ty check || true`).
>   - Run tests via targeted smoke tests and add caching for steps.
> - **Git Hooks**:
>   - Add `.githooks/pre-commit` to run `ruff --fix`, `ty check`, `bandit`, and `safety` (only Ruff blocks).
>   - Add `scripts/setup_git_hooks.sh` to install hooks.
> - **Docs**:
>   - Add `.github/CODE_QUALITY.md` quick reference.
>   - Add `CONTRIBUTING.md` with setup, hooks, checks, and CI details.
> - **Dependencies**:
>   - Add `bandit` and `safety` to `dev` optional dependencies in `pyproject.toml`.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 38dc2db079b47e0612b5959f182969434a3c21ed. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
