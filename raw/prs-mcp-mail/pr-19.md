# PR #19: Move MCP Mail implementation to correct repo

**Repo:** jleechanorg/mcp_mail
**Merged:** 2025-11-11
**Author:** jleechan2015
**Stats:** +1348/-7 in 15 files

## Summary
(none)

## Raw Body
- Create .mcp_mail/ directory structure for message logging
- Add messages.jsonl as source of truth (tracked in git)
- Configure .gitignore to track messages.jsonl but ignore SQLite cache
- Add README.md with architecture and usage documentation

This establishes the foundation for inter-agent communication with:
- Git-backed message persistence across sessions
- Support for parallel read/write access
- SQLite caching for performance (optional, local only)
- JSONL format for human-readable message storage

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Adds MCP Mail .mcp_mail structure/docs, presubmit hooks, and comprehensive integration tests, while standardizing CI to Python 3.13 with a new integration workflow and conditional smoke tests.
> 
> - **MCP Mail**:
>   - Add `.mcp_mail/` with `README.md`, `.gitignore`, and tracked `messages.jsonl`; root `.gitignore` updated to ignore SQLite cache but track messages.
> - **CI/CD**:
>   - Standardize workflows to Python `3.13` in `ci.yml`, `nightly.yml`, and `release.yml`.
>   - Add `integration.yml` to run MCP Mail integration tests and upload failure artifacts.
>   - Make smoke tests in `ci.yml` conditional on file existence.
> - **Presubmit Hooks**:
>   - Add `.pre-commit-config.yaml` and installable hook scripts (`scripts/pre-commit-hook.sh`, `scripts/pre-push-hook.sh`, `scripts/install-git-hooks.sh`).
>   - Add `docs/presubmit_hooks.md` with setup and troubleshooting.
> - **Tests**:
>   - Add `tests/integration/test_mcp_mail_messaging.py` covering messaging flows, parallel writes, filtering, git history, cross-repo refs, and malformed/empty handling.
> - **Code**:
>   - Minor cleanup in `src/mcp_agent_mail/app.py` (`_split_slug_and_query` type cast removed).
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit e1df0f8f3ed454482450f81da6fc6ae37056d3f7. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUM
