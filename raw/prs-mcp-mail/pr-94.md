# PR #94: Enhance Slack bidirectional sync and Slackbox bridge

**Repo:** jleechanorg/mcp_mail
**Merged:** 2025-12-19
**Author:** jleechan2015
**Stats:** +3375/-910 in 42 files
**Labels:** codex

## Summary
- document the bot-based Slack bidirectional sync design and setup flow
- map Slack event ingestion to Slack threads using message timestamps so MCP replies post back into the originating threads
- keep Slackbox webhook ingestion covered alongside additional Slack webhook thread-mapping tests

## Raw Body
## Summary
- document the bot-based Slack bidirectional sync design and setup flow
- map Slack event ingestion to Slack threads using message timestamps so MCP replies post back into the originating threads
- keep Slackbox webhook ingestion covered alongside additional Slack webhook thread-mapping tests

## Testing
- uv run pytest tests/integration/test_slack_webhook_api.py tests/integration/test_slackbox_webhook_api.py -q --disable-warnings --no-cov

------
[Codex Task](https://chatgpt.com/codex/tasks/task_e_692ae48ec30c832f90d74d04eee050c5)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Adds DB‑persisted Slack thread mapping with a singleton client, a new `/slackbox/incoming` endpoint with independent rate limits, archive-optional runtime artifacts, a `create_file_reservation` tool, and extensive docs/tests.
> 
> - **Slack Bidirectional Sync**:
>   - **DB Thread Mapping**: Persist Slack↔MCP thread links via `SlackThreadMapping` model; `SlackClient` singleton with DB-backed `map_thread`/lookups.
>   - **Webhook Ingestion**: Refactor `/slack/events` with signature/timestamp checks, dedupe cache, project routing by thread, and shared `_ingest_slack_bridge_message`.
>   - **Slackbox**: New `/slackbox/incoming` (token-verified) for legacy webhooks; independent rate limits for Slack vs Slackbox.
>   - **Threading**: Fallback parsing of `thread_id` like `slack_<channel>_<ts>`; ack notifications include `thread_id`; Slack message formatting trims recipient list.
> - **Config & Rate Limiting**:
>   - Support `~/.mcp_mail/credentials.json` with env precedence; add Slackbox settings and per-endpoint rate limits (`HTTP_RATE_LIMIT_SLACK*`, `*_SLACKBOX*`).
> - **Storage/Artifacts**:
>   - Gate archive ops with `is_archive_enabled`; add runtime artifact root (`~/.mcp_mail_runtime`) and `write_file_reservation_artifacts`; CLI/slots/guards work without archive.
> - **Guards**:
>   - Pre-commit/pre-push scripts accept file_reservations dir path, improve glob matching, honor releases/exp
