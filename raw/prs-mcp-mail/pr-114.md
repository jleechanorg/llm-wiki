# PR #114: Investigate message latency in SQLite

**Repo:** jleechanorg/mcp_mail
**Merged:** 2025-12-17
**Author:** jleechan2015
**Stats:** +795/-7 in 3 files

## Summary
(none)

## Raw Body
<!-- CURSOR_SUMMARY -->
> [!NOTE]
> Adds detailed latency instrumentation for message create/list/deliver/storage and comprehensive integration tests for immediate visibility, concurrency, and stress scenarios.
> 
> - **Observability/Performance**:
>   - Add precise latency instrumentation using `time.perf_counter()` with debug/info logs in `app.py` for `
>     _create_message`, `
>     _list_inbox` (and `
>     _list_inbox_basic`), and message delivery path (`
>     _deliver_message`).
>   - Enhance FTS-based global-inbox mention search: stricter types (`exclude_message_ids: set[int]`), ID guards, truncated merges respecting `limit`, and debug on failure.
>   - Improve recipient validation (ensure `recipient.id` present) and robust inbox ID set building.
> - **Storage**:
>   - Instrument `write_message_bundle` with per-phase timings (mkdir/write/digest/git_commit) and debug summary logging; introduce module logger.
> - **Tests**:
>   - Add `tests/integration/test_message_latency.py` with scenarios for immediate/cross-client visibility, concurrent send+fetch, rapid cycles, DB session resets, parallel sends, repeated fetch latency, fresh sessions, and high-concurrency stress.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 2dc353386027cfad3e569f6eceab56092e72ca77. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->
## Summary by CodeRabbit

* **Performance Monitoring**
  * Expanded latency instrumentation and logging across message creation, inbox listing, delivery, recipient lookup and storage paths to surface per-phase and end-to-end timings, counts, and identifiers for performance diagnosis.

* **Tests**
  * Added comprehensive integration tests covering immediate and cross-client visibility, concurrency/race conditions, rapid send/fetch cycles, DB session resets, p
