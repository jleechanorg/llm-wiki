# PR #90: Clarify global agent namespace and update real CLI tests

**Repo:** jleechanorg/mcp_mail
**Merged:** 2025-11-29
**Author:** jleechan2015
**Stats:** +207/-107713 in 1804 files

## Summary
- document that agent names are global and project_key is informational in tools and LLM manual tests
- enforce global agent reuse in register_agent and add coverage for reclaim/delivery to newest agent
- remove legacy .mcp_mail archive per request

## Raw Body
## Summary
- document that agent names are global and project_key is informational in tools and LLM manual tests
- enforce global agent reuse in register_agent and add coverage for reclaim/delivery to newest agent
- remove legacy .mcp_mail archive per request

## Testing
- uv run pytest tests/test_global_agent_uniqueness_modes.py
- uv run ruff check src/mcp_agent_mail/app.py tests/test_global_agent_uniqueness_modes.py

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Clarifies global agent naming semantics and enforces `register_agent` reuse, refreshes/renames real CLI + LLM tests (with a target‑validation script), and drops legacy `.mcp_mail` archives.
> 
> - **Agent registry & semantics**
>   - Enforce global agent reuse in `register_agent` (registering an existing name reuses the latest agent; supports reclaim/delivery to newest).
>   - Clarify that agent names are globally unique and `project_key` is informational; update docs (e.g., `CLAUDE.md`) and LLM test guidance.
> - **Tests & CLI updates**
>   - Add coverage for global agent uniqueness/reclaim flows (e.g., `tests/test_global_agent_uniqueness_modes.py`).
>   - Rename LLM test folder to `testing_llm/backend_conversation/` and add `testing_llm/validate_test_target.sh` to enforce targeting the AI‑Universe backend.
>   - Update real CLI integration tests to hit the correct backend; add/refresh evidence and harness scripts.
> - **Housekeeping**
>   - Remove legacy `.mcp_mail` archived messages/profiles and related artifacts.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 140d1150d1e8fe341a34bbd4e123035ebe07c680. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
