# PR #103: Include sender/recipient names in Slack webhook payloads

**Repo:** jleechanorg/mcp_mail
**Merged:** 2025-12-01
**Author:** jleechan2015
**Stats:** +95/-6 in 3 files

## Summary
- add sender and recipient lines to the Slack webhook mirror payload so agent names are visible in Slack
- extend slack mirror unit test to assert names appear in outgoing payload
- add opt-in live Slack integration test to send a real webhook (requires SLACK_LIVE_TEST=1 + SLACK_MCP_MAIL_WEBHOOK_URL)

## Raw Body
## Summary
- add sender and recipient lines to the Slack webhook mirror payload so agent names are visible in Slack
- extend slack mirror unit test to assert names appear in outgoing payload
- add opt-in live Slack integration test to send a real webhook (requires SLACK_LIVE_TEST=1 + SLACK_MCP_MAIL_WEBHOOK_URL)

## Testing
- uv run pytest tests/test_slack_mirror.py
- uv run pytest tests/integration/test_slack_mcp_mail_integration.py::test_slack_live_webhook_includes_agent_names (skips unless live env set)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Add From/To lines to Slack webhook mirror payload and update tests, including an opt-in live webhook check.
> 
> - **Slack Integration**:
>   - `mirror_message_to_slack`: include `*From:*` (from `frontmatter['from']`) and `*To:*` aggregated from `to`/`cc`/`bcc`; build multiline `text` before posting.
> - **Tests**:
>   - `tests/test_slack_mirror.py`: extend to assert `From/To` presence; add case handling missing names; keep skip logic for disabled mirror.
>   - `tests/integration/test_slack_mcp_mail_integration.py`: add `_live_mirror_payload` and `test_slack_live_webhook_includes_agent_names` (guarded by `SLACK_LIVE_TEST=1` + `SLACK_MCP_MAIL_WEBHOOK_URL`).
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit a413bf11967ca8c592845b4efb1bcc8ca08e8857. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->

## Summary by CodeRabbit

* **New Features**
  * Slack messages now display email sender names and recipient information (To, CC, BCC) in a structured format for improved visibility.

* **Tests**
  * Updated and added tests to verify sender and recipient information displays correctly in Slack messages, including handling of multiple recipients and edge cases.

<sub>✏️ Tip: You can customize this high-level summary in your review set
