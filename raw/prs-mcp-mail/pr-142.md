# PR #142: Remove unused archive storage config options

**Repo:** jleechanorg/mcp_mail
**Merged:** 2025-12-25
**Author:** jleechan2015
**Stats:** +2661/-784 in 51 files

## Summary
- Remove `project_key_storage_enabled` from StorageSettings
- Remove `local_archive_enabled` from StorageSettings
- Remove `project_key_prompt_enabled` from StorageSettings

These config options are no longer used since archive storage was removed. `is_archive_enabled()` now always returns `False`.

## Test Plan
- [x] Config loads correctly: `uv run python -c "from mcp_agent_mail.config import get_settings; get_settings()"`
- [x] Server starts successfully

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Storage/Config and Docs**
> - Removes archive-related options from `StorageSettings` and README; storage is SQLite-backed by default
> - Replaces hardcoded Authorization headers with `HTTP_BEARER_TOKEN` env in client configs
> - Bumps package to

## Raw Body
## Summary
- Remove `project_key_storage_enabled` from StorageSettings
- Remove `local_archive_enabled` from StorageSettings
- Remove `project_key_prompt_enabled` from StorageSettings

These config options are no longer used since archive storage was removed. `is_archive_enabled()` now always returns `False`.

## Test plan
- [x] Config loads correctly: `uv run python -c "from mcp_agent_mail.config import get_settings; get_settings()"`
- [x] Server starts successfully

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Storage/Config and Docs**
> - Removes archive-related options from `StorageSettings` and README; storage is SQLite-backed by default
> - Replaces hardcoded Authorization headers with `HTTP_BEARER_TOKEN` env in client configs
> - Bumps package to `0.1.15`; adds `jleechanorg-orchestration` to dev deps
> 
> **HTTP/ASGI transport**
> - Replaces custom stateless wrapper with header-normalizing ASGI adapter; mounts both `/mcp` and `/mcp/`, warns when `HTTP_PATH='/'` shadows routes
> - Normalizes Accept/Content-Type, fixes shutdown to suppress `CancelledError`, and uses FastAPI lifespan properly
> 
> **Agent/message model and CLI**
> - Makes `project_key` optional for read/ack tools; agents looked up globally; adjusts CLI acks commands (agent-first, project optional) and queries to not require project scoping
> 
> **Database migrations**
> - Deduplicates only ACTIVE agent names; temporarily disables FKs during table drops; recreates partial unique index for active agents
> 
> **Security**
> - Uses `usedforsecurity=False` for SHA-1 fingerprinting
> 
> **Tests**
> - Adds orchestration-based real-CLI runner and reusable test harness; many new integration tests (HTTP path/streaming, JWT, rate limits, Slack webhooks)
> - Updates tests to run within ASGI lifespan and drops archive-artifact assertions
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 8c7ee5672f2a0f4c552ef3
