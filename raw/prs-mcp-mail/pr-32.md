# PR #32: Reproduce the reported issue

**Repo:** jleechanorg/mcp_mail
**Merged:** 2025-11-12
**Author:** jleechan2015
**Stats:** +200/-3 in 3 files

## Summary
(none)

## Raw Body
<!-- CURSOR_SUMMARY -->
> [!NOTE]
> Apply LIMIT after WHERE clauses so since_ts filtering works in inbox/outbox queries, and add an integration test plus test .gitignore updates.
> 
> - **Messaging/DB (SQLAlchemy)**:
>   - Ensure `LIMIT` is applied after filters in `src/mcp_agent_mail/app.py` for:
>     - `_find_mentions_in_global_inbox` (FTS5 join path)
>     - `_list_inbox_basic`
>     - `_list_outbox`
> - **Tests**:
>   - Add integration test `tests/integration/test_since_ts_filter.py` validating `since_ts` filtering behavior with SQLite-backed `.mcp_mail` storage.
> - **Repo config**:
>   - Update `.mcp_mail/.gitignore` to ignore runtime test artifacts (e.g., `.gitattributes`, `projects/`).
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit cd16f359cb46ca9bf68026c5a20a6f4dac1ebc0c. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->
## Summary by CodeRabbit

* **Bug Fixes**
  * Ensured result limits are applied after all filtering so inbox and outbox queries return correct, filtered message sets.

* **Tests**
  * Added an integration test verifying since-timestamp filtering returns only post-checkpoint messages.

* **Chores**
  * Updated repository ignore patterns to exclude test artifacts.
<!-- end of auto-generated comment: release notes by coderabbit.ai -->
