# PR #88: Evaluate commits from original repository

**Repo:** jleechanorg/mcp_mail
**Merged:** 2025-12-07
**Author:** jleechan2015
**Stats:** +3704/-175 in 18 files

## Summary
(none)

## Raw Body
<!-- CURSOR_SUMMARY -->
> [!NOTE]
> Adds Product Bus (cross-repo inbox/search/summarization), upgrades installer and docs tooling, and enhances static share export with GitHub Pages support; includes new models, CLI/server tools, and tests.
> 
> - **Product Bus (cross-repo coordination)**
>   - Server: new tools `ensure_product`, `products_link`, `search_messages_product`, `fetch_inbox_product`, `summarize_thread_product`; resources `resource://product/{key}`, `resource://identity/{project}` in `src/mcp_agent_mail/app.py`.
>   - DB: add `Product` and `ProductProjectLink` models in `src/mcp_agent_mail/models.py`.
>   - CLI: `products` subcommands (`ensure`, `link`, `status`, `search`, `inbox`, `summarize-thread`).
> - **Installer & docs tooling**
>   - `scripts/install.sh`: Beads CLI auto‑install/verify, PATH wiring, `--port/--skip-beads`, on‑exit summary, optional doc snippet insertion.
>   - CLI: `docs insert-blurbs` to append README snippets into `AGENTS.md`/`CLAUDE.md`.
> - **Share/export pipeline**
>   - `share.py`: generate `README.md`, root `index.html` redirect, `.nojekyll`, `_headers`; expose `create_performance_indexes` via `__all__`.
>   - Wizard (`scripts/share_to_github_pages.py`): reuse existing repo, set HTTPS remote, force push for updates.
>   - CLI `share` commands: option handling refinements; tests expect new README wording.
> - **Tests & misc**
>   - New tests: identity markers and pathspec overlap; update integration test assertions.
>   - `.gitignore`: ignore `.agent_prompt*`.
>   - Documentation: add upstream evaluation and cherry‑pick plan/results files.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit fc5b8ac1b37d8782b0b7e2a8284bef96b544ce37. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->
## Summary by CodeRabbit

* **New Features**
  * Pr
