# PR #415: [agento] feat: add check-pr-worker-coverage.sh for agento PR monitoring

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-03-27
**Author:** jleechan2015
**Stats:** +98/-0 in 1 files

## Summary
- Adds `scripts/check-pr-worker-coverage.sh` — a standalone script that cross-references open PRs in jleechanorg/agent-orchestrator against active AO sessions
- Reports a table (PR #, title, session, status) and exits 0 only when all open PRs have active sessions

## Background
AO needs to surface which open PRs are covered by active worker sessions vs. which are orphaned. This script provides a quick status check and is suitable for cron monitoring.

## Raw Body
## Summary
- Adds `scripts/check-pr-worker-coverage.sh` — a standalone script that cross-references open PRs in jleechanorg/agent-orchestrator against active AO sessions
- Reports a table (PR #, title, session, status) and exits 0 only when all open PRs have active sessions

## Background
AO needs to surface which open PRs are covered by active worker sessions vs. which are orphaned. This script provides a quick status check and is suitable for cron monitoring.

## Testing
- Script tested standalone: correctly identified 5 of 6 open PRs as covered; PR #216 flagged as uncovered and exit code 1 returned

## Evidence
- Run output: PR table with session linkage, UNCOVERED summary
- Script location: scripts/check-pr-worker-coverage.sh

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Adds a standalone monitoring script that reads GitHub PRs and local `ao` session state; risk is low since it’s isolated to tooling, with main concerns being CLI/JSON parsing robustness and environment dependencies (`gh`, `ao`, `python3`).
> 
> **Overview**
> Adds `scripts/check-pr-worker-coverage.sh`, a standalone script that cross-references **open GitHub PRs** in `jleechanorg/agent-orchestrator` with **active `ao session` entries** for the `agent-orchestrator` project.
> 
> The script prints a compact table (PR, truncated title, matched session name, session status) and exits **0 only when all open PRs have an active session**, otherwise lists uncovered PR numbers and exits **1** (with explicit error handling for missing/failed `gh`/`ao` calls).
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 5591e8d1bad9a3d2bad2deeba42268747e093e06. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->

## Summary by CodeRabbit

## New Features

* Added a verification script that checks coverage of ope
