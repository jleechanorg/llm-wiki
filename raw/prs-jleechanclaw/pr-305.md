# PR #305: [P2] fix: check token file readability before reading

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-03-21
**Author:** jleechan2015
**Stats:** +19/-5 in 1 files

## Summary
PR #301 was merged but had an unresolved CodeRabbit comment about the token resolution logic in `scripts/harness-analyzer.sh`.

## Background
PR #301 was merged but had an unresolved CodeRabbit comment about the token resolution logic in `scripts/harness-analyzer.sh`.

## Raw Body
## Background
PR #301 was merged but had an unresolved CodeRabbit comment about the token resolution logic in `scripts/harness-analyzer.sh`.

## Goals
- Fix the token resolution logic to properly check for readability before attempting to read token files

## Tenets
- Minimal change that addresses the comment

## High-level description of changes
- Changed file existence check (`-f`) to readability check (`-r`) before reading token files
- Added fallback to default token path when GITHUB_TOKEN is not set

## Testing
- Verified bash syntax with `bash -n`

## Low-level details
- **scripts/harness-analyzer.sh**: Changed `-f` to `-r` at two locations and added fallback logic for GITHUB_TOKEN env var

## Verification
- Bash syntax validated

Closes #301 (follow-up fix)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Low risk: small, isolated change to token lookup logic in a shell script, primarily tightening file readability checks and fallback behavior.
> 
> **Overview**
> Improves `resolve_gh_token` in `scripts/harness-analyzer.sh` to **check token file readability (`-r`) before reading** rather than just existence, avoiding attempts to read unreadable paths.
> 
> Also updates token resolution to treat `GITHUB_TOKEN` as a file path when readable and otherwise **fall back to the default token source path**, keeping existing literal-token behavior intact.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit ccbfbca0a38699703fcf94422797fd12886af6b0. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
