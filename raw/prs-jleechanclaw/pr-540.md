# PR #540: [P2] docs: sync workspace AGENTS ao session guidance

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-04-10
**Author:** jleechan2015
**Stats:** +15/-1 in 2 files

## Summary
The root `AGENTS.md` already documents the Agent Orchestrator managed-session behavior, but the tracked workspace copy `workspace/AGENTS.md` was missing the same section. That left the workspace-scoped instructions out of sync with the canonical repo-level instructions for AO-managed sessions.

Issue tracking note: `br` task creation is currently blocked in this repo by the known prefix-mismatch preflight error (`jleechan-p8f` vs `orch-*`; see existing bug `orch-f7l`).

## Background
The root `AGENTS.md` already documents the Agent Orchestrator managed-session behavior, but the tracked workspace copy `workspace/AGENTS.md` was missing the same section. That left the workspace-scoped instructions out of sync with the canonical repo-level instructions for AO-managed sessions.

Issue tracking note: `br` task creation is currently blocked in this repo by the known prefix-mismatch preflight error (`jleechan-p8f` vs `orch-*`; see existing bug `orch-f7l`).

## Raw Body
## Background
The root `AGENTS.md` already documents the Agent Orchestrator managed-session behavior, but the tracked workspace copy `workspace/AGENTS.md` was missing the same section. That left the workspace-scoped instructions out of sync with the canonical repo-level instructions for AO-managed sessions.

Issue tracking note: `br` task creation is currently blocked in this repo by the known prefix-mismatch preflight error (`jleechan-p8f` vs `orch-*`; see existing bug `orch-f7l`).

## Goals
- Keep the tracked workspace instruction copy aligned with the root `AGENTS.md`.
- Preserve the AO session metadata guidance in both instruction surfaces.
- Make this a minimal docs-only sync with no behavioral code changes.

## Tenets
- Prefer editing the existing tracked instruction surface over adding new files.
- Keep the change narrowly scoped to the missing guidance.
- Avoid config or runtime changes for a documentation-sync fix.

## High-level description of changes
Added the missing `## Agent Orchestrator (ao) Session` section to `workspace/AGENTS.md` so the workspace copy now matches the root `AGENTS.md`. The section explains that the workspace is AO-managed, that session metadata is normally updated automatically, and where to manually refresh metadata if needed.

## Testing
- Verification: `git diff --check -- workspace/AGENTS.md`
- Manual review: compared the tail of `AGENTS.md` and `workspace/AGENTS.md` to confirm the workspace copy now contains the same AO session block.
- No automated tests were run because this is a documentation-only change.

## Low-level details (up to 20 files)
- **File path**: `workspace/AGENTS.md` — added the missing AO session metadata section so the tracked workspace instructions stay in sync with the root `AGENTS.md` guidance.

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Low risk documentation sync plus a whitespace-only adjustment to the `skeptic-gate.yml` PR comment body; main risk is unintended impact on the dispatch marke
