# PR #306: Add OpenClaw research page publishing helper + Caddy setup docs

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-03-20
**Author:** jleechan2015
**Stats:** +90/-0 in 2 files

## Summary
- add `workspace/scripts/publish-research-page.sh` helper to publish static research pages
- add `workspace/docs/caddy-openclaw.md` setup/ops docs for Caddy + OpenClaw routing
- document local publish command and route conventions

## Raw Body
## Summary
- add `workspace/scripts/publish-research-page.sh` helper to publish static research pages
- add `workspace/docs/caddy-openclaw.md` setup/ops docs for Caddy + OpenClaw routing
- document local publish command and route conventions

## What was configured on host (not committed)
- installed Caddy via Homebrew
- wrote runtime config at `/opt/homebrew/etc/Caddyfile`
- started service with `brew services restart caddy`

## Verification run
```bash
caddy validate --config /opt/homebrew/etc/Caddyfile
brew services restart caddy
/Users/jleechan/.openclaw/workspace/scripts/publish-research-page.sh openclaw-research "OpenClaw Research Test"
curl -sS http://127.0.0.1/research/openclaw-research/ | head -n 5
curl -sS -o /dev/null -w "%{http_code}\n" http://127.0.0.1/healthz
```

Observed:
- config validated
- caddy service started
- test page served at `/research/openclaw-research/`
- health endpoint returned `200`

## Notes
- current Caddy listener is `:80` (HTTP)
- switch to real hostname in Caddyfile to enable automatic HTTPS certs

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Low risk: adds a standalone publishing helper script and ops documentation without modifying application/runtime code paths. Main risk is operational (script overwriting local files under the publish directory) and environment assumptions (requires `python3`).
> 
> **Overview**
> Adds a new `publish-research-page.sh` helper that generates a static `index.html` under `~/.openclaw/published/research/<slug>/`, validating the slug and HTML-escaping the title/body (optionally sourced from a markdown file).
> 
> Adds `caddy-openclaw.md` documentation describing how to reverse-proxy OpenClaw on `127.0.0.1:18789` and serve the published `/research/*` pages via Caddy, including basic validate/restart and URL conventions.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit fac55fce904f8bab652ca465f22b8d64861a4774. This will update automatically on new co
