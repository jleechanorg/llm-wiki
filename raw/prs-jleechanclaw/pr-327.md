# PR #327: chore: remove deprecated ao-backfill and ao-pr-poller scripts

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-03-21
**Author:** jleechan2015
**Stats:** +4/-1730 in 9 files

## Summary
PR #326 disabled auto-merge in these scripts, raising the question of whether they are needed at all. Investigation confirmed AO lifecycle workers now handle all spawn, cleanup, review-check, and PR monitoring natively. Both scripts were partially broken:
- ao-backfill.sh crashed every run due to declare -A (bash 3 on macOS /bin/bash)
- ao-pr-poller.sh had spawn failures (ao spawn without project context) and was redundant with lifecycle workers

## Background
PR #326 disabled auto-merge in these scripts, raising the question of whether they are needed at all. Investigation confirmed AO lifecycle workers now handle all spawn, cleanup, review-check, and PR monitoring natively. Both scripts were partially broken:
- ao-backfill.sh crashed every run due to declare -A (bash 3 on macOS /bin/bash)
- ao-pr-poller.sh had spawn failures (ao spawn without project context) and was redundant with lifecycle workers

## Raw Body
## Background
PR #326 disabled auto-merge in these scripts, raising the question of whether they are needed at all. Investigation confirmed AO lifecycle workers now handle all spawn, cleanup, review-check, and PR monitoring natively. Both scripts were partially broken:
- ao-backfill.sh crashed every run due to declare -A (bash 3 on macOS /bin/bash)
- ao-pr-poller.sh had spawn failures (ao spawn without project context) and was redundant with lifecycle workers

## Goals
- Remove ~1,744 lines of dead shell code
- Eliminate broken launchd jobs that waste cycles and log noise
- Consolidate on AO lifecycle workers as the single spawn/cleanup/monitor path

## Tenets
- AO lifecycle workers are the canonical scheduler - no parallel shell scripts
- Delete dead code, do not deprecate-rename

## High-level description of changes
Deleted 7 files (scripts, tests, launchd plists) and updated 2 files to remove references to the deleted scripts.

## Testing
- Verified com.agentorchestrator.lifecycle-jleechanclaw and lifecycle-worldarchitect are running and healthy
- Verified ao session cleanup --dry-run works natively
- Verified ao review-check covers idle-session detection
- Unloaded ai.agento.backfill launchd agent (was exit code 2 = broken)

## Low-level details

- **scripts/ao-backfill.sh**: Deleted. 509-line script for session spawn, cleanup, idle poke, merge gate, auto-merge. All handled by AO lifecycle-worker.
- **scripts/ao-pr-poller.sh**: Deleted. 576-line script for polling non-green PRs and spawning agents. Redundant with lifecycle-worker.
- **scripts/install-ao-pr-poller.sh**: Deleted. Installer for the removed poller.
- **scripts/ao-pr-poller-resume-tests.sh**: Deleted. Tests for the removed poller.
- **tests/test_ao_backfill_poked_at.sh**: Deleted. Tests for the removed backfill script.
- **launchd/ai.ao-pr-poller.plist.template**: Deleted. Template for removed poller.
- **launchd/ai.ao-pr-poller.plist.deprecated**: Deleted. Already-deprecated plist.
- **launchd/ai.ag
