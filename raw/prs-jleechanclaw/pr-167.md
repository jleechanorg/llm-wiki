# PR #167: fix(webhook): ignore pull_request.closed events in remediation queue

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-03-15
**Author:** jleechan2015
**Stats:** +6/-0 in 2 files

## Summary
- Add `pull_request.closed` to `_IGNORED_TRIGGER_TYPES` to prevent phantom remediation issues

## Raw Body
## Summary
- Add `pull_request.closed` to `_IGNORED_TRIGGER_TYPES` to prevent phantom remediation issues

## Changes
- Modified `src/orchestration/webhook_queue.py` to ignore `pull_request.closed` events
- Previously, closed PRs fell through without a workflow lane mapping, causing `SKIP_REASON_UNMAPPED_TRIGGER` in `pr_lifecycle.route_event()`

## Testing
- Ran webhook_queue tests: 23 passed
- Ran webhook_worker tests: 35 passed

## Known Limitations
- None

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Low risk: a narrow filter change that only prevents `pull_request.closed` from entering the remediation queue, covered by a focused unit test.
> 
> **Overview**
> Prevents closed PR webhooks from being enqueued by adding `pull_request.closed` to `webhook_queue._IGNORED_TRIGGER_TYPES`, so `normalize_event` returns `None` for these events.
> 
> Adds a unit test (`test_normalize_event_pr_closed_returns_none`) to lock in the new ignore behavior.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 5d6701b0e872cd44e0cf249b2b1f88206f8a2680. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->

## Summary by CodeRabbit

* **Bug Fixes**
  * Fixed handling of closed pull request events to prevent them from triggering unwanted actions during event processing.

* **Tests**
  * Added test coverage for closed pull request event filtering.

<!-- end of auto-generated comment: release notes by coderabbit.ai -->
