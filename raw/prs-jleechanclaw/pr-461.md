# PR #461: [agento] fix(doctor): silence expected warnings for nvm Node 22 and unconfigured Discord

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-03-31
**Author:** jleechan2015
**Stats:** +83/-36 in 3 files

## Summary
Two doctor.sh WARNs fire on every monitor cycle despite being expected, correct configuration:
1. WARN: channels.discord.token:missing/placeholder -- Discord is intentionally not configured
2. WARN: gateway service uses Node from a version manager -- CLAUDE.md mandates nvm Node 22; Homebrew Node 24 breaks native modules (better-sqlite3 ABI mismatch)

## Background
Two doctor.sh WARNs fire on every monitor cycle despite being expected, correct configuration:
1. WARN: channels.discord.token:missing/placeholder -- Discord is intentionally not configured
2. WARN: gateway service uses Node from a version manager -- CLAUDE.md mandates nvm Node 22; Homebrew Node 24 breaks native modules (better-sqlite3 ABI mismatch)

## Raw Body
## Background
Two doctor.sh WARNs fire on every monitor cycle despite being expected, correct configuration:
1. WARN: channels.discord.token:missing/placeholder -- Discord is intentionally not configured
2. WARN: gateway service uses Node from a version manager -- CLAUDE.md mandates nvm Node 22; Homebrew Node 24 breaks native modules (better-sqlite3 ABI mismatch)

## Goals
- Both conditions emit [PASS] not [WARN]
- Non-v22 nvm paths still warn (genuine risk signal preserved)

## Tenets
- Do not suppress real warnings -- only codify known-good policy

## High-level description of changes
scripts/doctor.sh:
- Discord missing/placeholder: warn to pass (Discord not configured = expected)
- nvm Node v22.x path: new pass branch before existing warn; non-v22 nvm paths still warn

## Testing
Ran bash scripts/doctor.sh in worktree -- both now emit [PASS]:
  [PASS] gateway service uses nvm Node 22 (/Users/jleechan/.nvm/versions/node/v22.22.0/bin/node) -- correct per policy
  [PASS] Discord not configured (channels.discord.token empty/placeholder); skipped Discord probe

## Low-level details
- scripts/doctor.sh:728: warn to pass for Discord placeholder
- scripts/doctor.sh:469-471: add v22 pass-branch before existing warn for other nvm versions

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Touches automation that can auto-merge PRs and operational health checks; mistakes could block merges or mask real outages, though changes are mostly defensive filtering and clearer gating.
> 
> **Overview**
> **Skeptic cron CI gating is hardened** by retrying `check-runs` fetches, correctly aggregating paginated results, and ignoring self-referential gate checks while still treating stale third-party `in_progress` checks (e.g. `cursor[bot]`) as non-blocking.
> 
> **Operational health checks are made less noisy and more accurate**: `doctor.sh` now treats `nvm` Node `v22.x` as a pass, gates Discord probing behind `channels.discord.enabled` (failing only when enabled but token mis
