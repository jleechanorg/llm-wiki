# PR #45: fix(orchestration): start HeartbeatPoller alongside TaskPoller (ORCH-uti)

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-03-05
**Author:** jleechan2015
**Stats:** +585/-0 in 5 files

## Summary
- `run_service()` started `TaskPoller` but never `HeartbeatPoller` → agents were never registered in Mission Control
- Health check always showed ❌ for \"MC agents registered\"
- Fixes ORCH-uti

## Raw Body
## Summary
- `run_service()` started `TaskPoller` but never `HeartbeatPoller` → agents were never registered in Mission Control
- Health check always showed ❌ for \"MC agents registered\"
- Fixes ORCH-uti

## Changes
- **Before**: `mc_backend_service.py` started task poller only; no heartbeat thread was spawned
- **After**: When `MISSION_CONTROL_IN_PROCESS_POLLER=1`, both task poller and heartbeat poller start; heartbeat calls `sync_agents_to_mission_control(board_id, client)` every 60s (configurable via `MISSION_CONTROL_HEARTBEAT_INTERVAL_SECONDS`)
- Added `start_in_process_heartbeat_poller()` — mirrors `start_in_process_task_poller()` pattern
- Added `src/tests/test_mc_backend_service.py` — 13 tests covering `_is_enabled`, `_resolve_poll_interval_seconds`, `start_in_process_heartbeat_poller`, and `run_service` integration

## Testing
- `PYTHONPATH=src python -m pytest src/tests/test_mc_backend_service.py -v` → 13/13 passed
- `PYTHONPATH=src python -m pytest src/tests/ -q` → 606 passed, 4 skipped

## Known Limitations
- Heartbeat is only active when `MISSION_CONTROL_IN_PROCESS_POLLER=1` (integrated service mode). Standalone task poller LaunchAgent setups need a separate heartbeat process (tracked separately).

🤖 Generated with [Claude Code](https://claude.ai/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Adds new background threads and a subprocess-driven sync loop that calls external `openclaw` and Mission Control HTTP endpoints; misconfiguration or runtime failures should be reviewed to ensure they fail safely and don’t mask operational issues.
> 
> **Overview**
> When running the Mission Control backend in “in-process poller” mode (`MISSION_CONTROL_IN_PROCESS_POLLER=1`), the service now also starts a daemon heartbeat thread that periodically syncs OpenClaw agent status into Mission Control, so agents get registered/updated without a separate process.
> 
> This introduces `heartbeat_bridge.sync_agents_to_mission_control()` (calls `opencl
