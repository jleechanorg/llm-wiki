# PR #157: [agento] fix(mem0): fix autoRecall userId mismatch (Python snake_case vs Node.js camelCase)

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-03-15
**Author:** jleechan2015
**Stats:** +311/-1 in 6 files

## Summary
- Node.js `openclaw-mem0` extension filters Qdrant by `userId` (camelCase)
- Python `mem0` library stores with `user_id` (snake_case)
- This caused all Python-ingested facts (1,531 points) to be invisible to autoRecall

## Test Plan
- [ ] Sent "What was the mass-deletion incident on 2026-03-04?" to #ai-slack-test
- [ ] OpenClaw replied correctly: `git rm -r .openclaw-backups/` at score 0.745
- [ ] Verified injected `<relevant-memories>` block contains the mass-deletion fact

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Touches the memory persistence path by mutating Qdrant payloads after writes and introduces a new Groq-backed session-ingest hook;

## Raw Body
## Summary
- Node.js `openclaw-mem0` extension filters Qdrant by `userId` (camelCase)
- Python `mem0` library stores with `user_id` (snake_case)
- This caused all Python-ingested facts (1,531 points) to be invisible to autoRecall

## Changes
- `add_memory()` now calls `qdrant.set_payload(userId=user_id)` after each add
- Ran one-time migration on 1,531 existing Qdrant points to add `userId`

## Test plan
- [ ] Sent "What was the mass-deletion incident on 2026-03-04?" to #ai-slack-test
- [ ] OpenClaw replied correctly: `git rm -r .openclaw-backups/` at score 0.745
- [ ] Verified injected `<relevant-memories>` block contains the mass-deletion fact

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Touches the memory persistence path by mutating Qdrant payloads after writes and introduces a new Groq-backed session-ingest hook; failures or misconfiguration could cause missing/incorrect memory entries.
> 
> **Overview**
> Enables automatic end-of-session “fact capture” by adding `scripts/auto_fact_capture.py`, which reads the last ~20 messages from a session JSONL, calls Groq to extract 1–3 facts, and stores them via `mem0_shared_client.add_memory()`.
> 
> Fixes cross-tool recall compatibility by updating `add_memory()` to also set a camelCase `userId` payload on the newly created Qdrant points (using a cached `QdrantClient`), aligning Python-ingested memories with the Node `openclaw-mem0` autoRecall filter. Documentation/test notes were updated accordingly.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 1ef61df9aee99d55b381e2f035e62d52ea059d90. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->

## Summary by CodeRabbit

* **Bug Fixes**
  * Fixed user identifier propagation in storage systems to im
