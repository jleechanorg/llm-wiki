# PR #71: mctrl: harden retries, async ACP roundtrip, and dispatch env isolation

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-03-10
**Author:** jleechan2015
**Stats:** +346/-227 in 6 files

## Summary
- harden notifier delivery with transient retry/backoff and shared outbox defaults
- align reconciliation/supervisor wait windows with notifier runtime budget
- make `test_mcp_dispatch_roundtrip` async and validate full real ACP roundtrip behavior
- fix dispatch subprocess environment so `ai_orch` works under `PYTHONPATH=src`

## Raw Body
## Summary
- harden notifier delivery with transient retry/backoff and shared outbox defaults
- align reconciliation/supervisor wait windows with notifier runtime budget
- make `test_mcp_dispatch_roundtrip` async and validate full real ACP roundtrip behavior
- fix dispatch subprocess environment so `ai_orch` works under `PYTHONPATH=src`

## Changes
- `src/orchestration/openclaw_notifier.py`: transient failure classification, bounded retry behavior, and runtime accounting used by reconciliation
- `src/orchestration/reconciliation.py` and `src/orchestration/supervisor.py`: use shared outbox defaults and notifier-aware timing
- `src/orchestration/dispatch_task.py`: run `ai_orch`/`orch` with a sanitized env (drops `PYTHONPATH`) to prevent local package shadowing; keep stale-wrapper fallback path
- `tests/test_mcp_dispatch_roundtrip.py`: async ACP flow (`asyncio.run` + async helpers), DM verification path, and real ACP long-path coverage
- `tests/test_openclaw_notifier.py`, `tests/test_reconciliation.py`, `tests/test_openclaw_configs.py`, `tests/test_mvp_loopback_e2e.py`: coverage updates for notifier/reconciliation/runtime/config behavior
- `scripts/install-launchagents.sh`: stricter token flag validation
- `openclaw-config/SOUL.md`, `openclaw-config/TOOLS.md`, `AGENTS.md`, `.gitignore`, `openclaw-config/openclaw.json`: config/docs/default-path updates tied to mctrl runtime behavior

## Testing
- `source ~/.profile && PYTHONPATH=src python -m pytest tests/test_mcp_dispatch_roundtrip.py::test_mcp_dispatch_roundtrip -q`
- `source ~/.profile && PYTHONPATH=src python -m pytest tests/test_mcp_dispatch_roundtrip.py -q`
- `PYTHONPATH=src python -m pytest tests/test_openclaw_configs.py -q`
- `PYTHONPATH=src python -m pytest tests/test_mcp_dispatch_roundtrip.py -k "test_slack_history_handles_null_messages or test_mcp_agent_send_receive" -q`

## Known Limitations
- Long ACP E2E remains runtime-dependent on local OpenClaw/Slack environment availability and token validity.

<!-- CU
