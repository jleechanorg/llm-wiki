# PR #192: fix(ao-backfill): honor backfillAllPRs in respawn gate + jq filter

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-03-16
**Author:** jleechan2015
**Stats:** +5/-3 in 2 files

## Summary
- Fix respawn gate to honor backfillAllPRs flag
- Fix jq filter syntax on line 84
- Fix documentation API path

## Raw Body
## Summary
- Fix respawn gate to honor backfillAllPRs flag
- Fix jq filter syntax on line 84
- Fix documentation API path

## Testing
- Verified jq filter works correctly

🤖 Generated with Claude Code

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Touches the `ao-backfill.sh` automation that decides which PRs to spawn/respawn sessions for; mistakes here could lead to missed PR coverage or unexpected session churn. Changes are small and localized to filtering/logging logic.
> 
> **Overview**
> Fixes `ao-backfill.sh` PR selection and respawn behavior so projects with `backfillAllPRs=true` reliably include non-`[agento]` PRs, and updates the jq filter used to list `[agento]` PRs to a more robust form.
> 
> Also improves respawn logging by making the reason explicit (`[agento]` confirmed vs `backfillAllPRs=true`), and updates the roadmap doc to use the correct `gh api repos/{owner}/{repo}/pulls/{pull_number}/comments` endpoint.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 9fd5602435c9123808d22d4f3e8a69baa1e8f84b. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
