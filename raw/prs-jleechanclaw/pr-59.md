# PR #59: feat: MVP loopback notifier + session supervisor wiring

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-03-08
**Author:** jleechan2015
**Stats:** +4601/-878 in 72 files

## Summary
- remove in-process TaskPoller startup from Mission Control backend path; keep heartbeat mirror optional via board config
- add persisted bead<->session registry (`session_registry.py`) with atomic updates
- add OpenClaw loopback notifier (`openclaw_notifier.py`) with MCP-mail primary + JSONL fallback/drain
- wire supervisor reconciliation to transition missing sessions to `needs_human` and emit `task_needs_human`
- add MVP docs/audit docs and restore missing webhook parser/event exports require

## Raw Body
## Summary
- remove in-process TaskPoller startup from Mission Control backend path; keep heartbeat mirror optional via board config
- add persisted bead<->session registry (`session_registry.py`) with atomic updates
- add OpenClaw loopback notifier (`openclaw_notifier.py`) with MCP-mail primary + JSONL fallback/drain
- wire supervisor reconciliation to transition missing sessions to `needs_human` and emit `task_needs_human`
- add MVP docs/audit docs and restore missing webhook parser/event exports required by evidence tests

## Beads
- closed: ORCH-xnf
- closed: ORCH-g8c.1
- closed: ORCH-g8c.2
- updated progress: ORCH-g8c.3, ORCH-g8c.4

## Testing
- `PYTHONPATH=src python -m pytest src/tests -q`
- Result: `479 passed, 4 skipped`

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Touches core orchestration lifecycle and notification delivery (tmux liveness, git commit detection, Slack/OpenClaw messaging) and changes runtime Slack gateway policy (`allowBots`), so misconfiguration could cause missed or noisy notifications.
> 
> **Overview**
> Introduces a **bead-native async dispatch + supervision path**: `dispatch_task` spawns `ai_orch` sessions/worktrees, records `start_sha`, and persists a bead↔session mapping; `supervisor` periodically reconciles the registry, marks missing sessions as `finished` vs `needs_human` based on new commits, and emits `task_finished`/`task_needs_human` notifications.
> 
> Adds an **OpenClaw notifier with reliability semantics** (`notify_openclaw` via `openclaw agent`/MCP, JSONL outbox fallback + drain) plus Slack DM/thread “started/done” posts, and provides launchd install/run scripts and an `mctrl_status` helper. The Mission Control backend startup path is changed to **stop starting the in-process TaskPoller** (dispatch is OpenClaw→`ai_orch`), optionally running only a heartbeat mirror.
> 
> Extends GitHub webhook bridging with HMAC verification and trusted-actor filtering, and updates OpenClaw config/docs to enforce `dispatch_
