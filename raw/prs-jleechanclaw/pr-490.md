# PR #490: [agento] fix(harness): detect stale session locks that cause silent message loss

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-04-04
**Author:** jleechan2015
**Stats:** +64/-9 in 3 files

## Summary
2026-04-04: OpenClaw stopped responding to Slack DMs for ~6 hours. Gateway reported HTTP 200 and all 6 canary checks passed. Root cause: pid=59865 (a dead agent subprocess) held stale `.lock` files in `agents/main/sessions/`, causing every inbound message to hit `"session file locked (timeout 10000ms)"` and be silently dropped.

## Background
2026-04-04: OpenClaw stopped responding to Slack DMs for ~6 hours. Gateway reported HTTP 200 and all 6 canary checks passed. Root cause: pid=59865 (a dead agent subprocess) held stale `.lock` files in `agents/main/sessions/`, causing every inbound message to hit `"session file locked (timeout 10000ms)"` and be silently dropped.

## Raw Body
## Background

2026-04-04: OpenClaw stopped responding to Slack DMs for ~6 hours. Gateway reported HTTP 200 and all 6 canary checks passed. Root cause: pid=59865 (a dead agent subprocess) held stale `.lock` files in `agents/main/sessions/`, causing every inbound message to hit `"session file locked (timeout 10000ms)"` and be silently dropped.

## Goals

Make this failure class detectable before it causes an outage — at canary time, at doctor time, and via documented diagnosis procedure.

## Tenets

- Harness fixes at multiple layers (detect early, detect often)
- HTTP 200 is not sufficient proof of functional health

## High-level description of changes

Three harness fixes:
1. `staging-canary.sh` check 7: scans for dead-owner `.lock` files in sessions dir before any deploy
2. `doctor.sh` Session health section: same lock check + warns on excessive `.tmp` accumulation
3. `CLAUDE.md`: documents the failure mode, symptom, and fix procedure

## Testing

- Canary ran with 3 stale locks present → correctly FAILED check 7
- After clearing stale locks → 7/7 PASS
- doctor.sh runs clean with new section

## Low-level details

- **scripts/staging-canary.sh**: Check 7 added. Parses lock files (JSON format `{"pid":N,...}`). Fails if any lock is held by a dead pid.
- **scripts/doctor.sh**: New "Session health" section before openclaw.json validation. Same dead-pid detection + warn if >50 stale .tmp files.
- **CLAUDE.md**: New bullet under Hung gateway — "Session lock silent failure" with symptom, cause, fix command, and the key insight that HTTP 200 does not prove message processing.

Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Medium risk because it changes operational gating (`staging-canary.sh`) and diagnostics (`doctor.sh`), which could block deploys or miss issues if the lock parsing/PID checks behave unexpectedly across environments.
> 
> **Overview**
> Adds proactive detection of *silent Slack me
