# PR #428: chore: route all CI test runs to self-hosted runners

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-03-29
**Author:** jleechan2015
**Stats:** +22/-2 in 2 files

## Summary
All test runs should use self-hosted runners.

## Background
All test runs should use self-hosted runners.

## Raw Body
## Background
All test runs should use self-hosted runners.

## Changes
- skeptic-gate.yml: ubuntu-latest → self-hosted via SELF_HOSTED_RUNNER_LABELS (skips fork PRs)
- skeptic-cron.yml: ubuntu-latest → self-hosted via SELF_HOSTED_RUNNER_LABELS

**Optional**: `vars.SELF_HOSTED_RUNNER_LABELS` — defaults to `["self-hosted","jleechanclaw"]` if unset.

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Changes CI/merge-gating workflows to skip fork/cross-repo PRs and alters which PRs are considered for automated merging; misconfiguration could unintentionally stop verdicts/auto-merges or miss eligible PRs.
> 
> **Overview**
> Tightens the Skeptic automation to **only operate on non-draft, same-repo PRs** by filtering `skeptic-cron`’s PR list with `head.repo.id == base.repo.id` (and skipping null `head.repo`), updating logging accordingly.
> 
> Updates `skeptic-gate` to **not run on fork/cross-repo PRs** via a job-level `if:` guard, and adds documentation comments clarifying how verdict posting and `skeptic-cron`’s merge gate interact.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit fb013a3646faa06445b1f679b1ade01a6f3881da. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->
## Summary by CodeRabbit

* **Chores**
  * CI now only processes non-draft, same-repo pull requests and explicitly skips forked PRs to prevent unintended workflow continuation.
  * Preflight check added to verify the GitHub CLI is installed and fail early with a clear message if missing.
  * Workflow step labels and outputs clarified for clearer reporting when PRs are excluded.
<!-- end of auto-generated comment: release notes by coderabbit.ai -->
