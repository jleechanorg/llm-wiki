# PR #429: [agento] fix(ci): treat cancelled check runs as neutral in CI gate

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-03-29
**Author:** jleechan2015
**Stats:** +19/-3 in 2 files

## Summary
- skeptic-cron.yml: jq filter now treats `cancelled` as neutral alongside `success`, `skipped`, `neutral`
- Concurrency cancellation creates cancelled check runs on superseded commits — these should not count as CI failures

## Background
PR #428 was blocked because the cron ran against a stale SHA snapshot where a concurrency-cancelled check run was counted as a CI failure. Cancelled runs are not failures — they simply never completed because a newer commit superseded them.

## Raw Body
## Summary
- skeptic-cron.yml: jq filter now treats `cancelled` as neutral alongside `success`, `skipped`, `neutral`
- Concurrency cancellation creates cancelled check runs on superseded commits — these should not count as CI failures

## Background
PR #428 was blocked because the cron ran against a stale SHA snapshot where a concurrency-cancelled check run was counted as a CI failure. Cancelled runs are not failures — they simply never completed because a newer commit superseded them.

## Testing
- Local jq test confirms: cancelled is now treated as neutral

## Blocking
This unblocks PR #428 (self-hosted runner routing) from the 7-green merge gate.

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Touches GitHub Actions workflows that gate and auto-merge PRs; misconfiguration could block merges or allow unintended merges. Logic change is small but impacts CI status interpretation and runner environment assumptions.
> 
> **Overview**
> Updates `skeptic-cron` and `skeptic-gate` to run on configurable self-hosted runners via `vars.SELF_HOSTED_RUNNER_LABELS` (with a default label set).
> 
> Hardens the self-hosted environment by adding fast-fail preflight checks for required CLIs (`gh`, `jq`, `base64`) and PCRE-capable `grep`, and adds an explicit `gh` availability check in the gate workflow.
> 
> Fixes the 7-green CI evaluation to treat `cancelled` check runs as *non-failures* (neutral) when computing overall CI status, preventing concurrency-cancelled runs from blocking merges.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit b261666b70f012666e675ddd9c56f3b9f9ccf580. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->

## Summary by CodeRabbit

* **Chores**
  * Enhanced CI workflow robustness with prerequisite validation checks and detailed error reporting for
