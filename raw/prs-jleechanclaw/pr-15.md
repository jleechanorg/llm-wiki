# PR #15: fix(config): stabilize second-opinion MCP tool wiring

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-03-03
**Author:** jleechan2015
**Stats:** +616/-132 in 7 files

## Summary
- switch second-opinion policy from `tools.allow` to additive `tools.alsoAllow`
- align second-opinion tool IDs with adapter-safe names (`second-opinion-tool_agent_second_opinion`, `second-opinion-tool_rate_limit_status`, `second-opinion-tool_health-check`)
- enable/configure `openclaw-mcp-adapter` in baseline and discord bot templates
- update discord bot README to document the exact allowlisted tool IDs

## Raw Body
## Summary
- switch second-opinion policy from `tools.allow` to additive `tools.alsoAllow`
- align second-opinion tool IDs with adapter-safe names (`second-opinion-tool_agent_second_opinion`, `second-opinion-tool_rate_limit_status`, `second-opinion-tool_health-check`)
- enable/configure `openclaw-mcp-adapter` in baseline and discord bot templates
- update discord bot README to document the exact allowlisted tool IDs

## Why
Current config intermittently fails second-opinion calls because tool IDs and policy behavior were mismatched.

## Testing
- `jq empty openclaw-config/openclaw.json`
- `jq empty discord-eng-bot/openclaw.json`
- live verification against local gateway showed `second-opinion-tool_health-check` and `second-opinion-tool_rate_limit_status` callable.

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Moderate risk because it changes OpenClaw tool permission configuration and enables MCP adapter wiring, which could affect which tools are callable and break second-opinion requests if misconfigured. Added regression tests reduce the chance of security or config drift issues, especially for the public Discord profile.
> 
> **Overview**
> Improves reliability of second-opinion tool calls by switching configs to additive `tools.alsoAllow` and standardizing the adapter-safe tool IDs (e.g. `second-opinion-tool_agent_second_opinion`).
> 
> Enables/configures `openclaw-mcp-adapter` in both the main committed profile and the Discord `consensus` backup, wiring the server URL via `${SECOND_OPINION_MCP_URL}` and `toolPrefix: true`, while moving Discord runtime out of the main baseline (Discord disabled there) into the dedicated `discord-eng-bot/` profile backup.
> 
> Adds `src/tests/test_openclaw_configs.py` to enforce sandbox isolation for the public Discord bot, ensure core tools remain explicitly present in main `alsoAllow`, and verify MCP adapter wiring stays enabled and env-var driven; also adds a reference doc explaining Firebase JWT auto-refresh and 
