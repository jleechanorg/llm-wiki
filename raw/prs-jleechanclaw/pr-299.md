# PR #299: [agento] Auto /er evidence review + slash command discovery for non-Claude agents

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-03-20
**Author:** jleechan2015
**Stats:** +50/-9 in 2 files

## Summary
Implements automation for agents to run evidence review (/er) automatically after pushing fixes, and adds guidance for non-Claude agents (Codex, Cursor, etc.) to discover and execute slash commands.

## Background
Implements automation for agents to run evidence review (/er) automatically after pushing fixes, and adds guidance for non-Claude agents (Codex, Cursor, etc.) to discover and execute slash commands.

## Raw Body
## Background
Implements automation for agents to run evidence review (/er) automatically after pushing fixes, and adds guidance for non-Claude agents (Codex, Cursor, etc.) to discover and execute slash commands.

## Goals
1. Agents automatically run /er evidence review after every push
2. Non-Claude agents can discover and execute slash command logic from .claude/commands/*.md files

## Tenets
- Prefer config changes over code (agent-orchestrator.yaml, CLAUDE.md)
- Keep existing agent workflows intact

## Changes
- **agent-orchestrator.yaml**: Added "MANDATORY AFTER EVERY PUSH" section to all project agentRules — after posting @coderabbitai all good?, agents now run /er for evidence review
- **CLAUDE.md**: Added "Slash Command Discovery for Non-Claude Agents" section explaining how to discover commands from ~/.claude/commands/*.md and .claude/commands/*.md

## Testing
- Verified YAML syntax is valid
- Grepped all agentRules sections to confirm updates applied

## Beads
- orch-s70: Auto /er evidence review after push
- orch-zat: Slash command discovery for non-Claude agents

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Low risk: config/docs-only changes that adjust agent workflow instructions; primary risk is unintended behavior changes if agents follow the updated post-push `/er` gating incorrectly.
> 
> **Overview**
> Adds explicit guidance for *non-Claude agents* to discover slash commands by reading `~/.claude/commands/*.md` and `.claude/commands/*.md`, with repo-local commands taking precedence.
> 
> Tightens `agent-orchestrator.yaml` agentRules by formalizing the post-`git push` loop: always comment `@coderabbitai all good?`, only run `/er` after CodeRabbit returns APPROVE/LGTM (and in some projects after criteria 1–5 are met), and clarifies slash-command discovery plus an MCP mail footgun (`register_agent`) to avoid.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab
