# PR #158: chore: remove orphaned genesis tests and update CLAUDE.md

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-03-15
**Author:** jleechan2015
**Stats:** +9/-423 in 5 files

## Summary
- Removed 4 orphaned test files that depended on the deleted `src/genesis/` module
- Updated CLAUDE.md to remove stale references to `src/genesis/`

## Raw Body
## Summary
- Removed 4 orphaned test files that depended on the deleted `src/genesis/` module
- Updated CLAUDE.md to remove stale references to `src/genesis/`

## Changes
The `src/genesis/` module was deleted in commit `d99eae50d` because it had "no external callers", but the test files that depended on it were left behind. This caused import errors when running the test suite.

## Testing
- Verified `PYTHONPATH=src python -m pytest src/tests/test_ao_cli.py -q` passes (16/16 tests)

## Known Limitations
- No CI checks on this repo

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Low risk because this only deletes broken tests and updates documentation; runtime code paths are unchanged. Main risk is reduced coverage if any of these tests were still meaningful, but they were already failing due to missing imports.
> 
> **Overview**
> Removes four orphaned `tests/test_genesis_*.py` files that imported the now-deleted `src/genesis` module, eliminating import errors when running the default pytest suite.
> 
> Updates `CLAUDE.md` to remove `src/genesis/` references and clarify the split between default integration tests in `tests/` vs orchestration unit tests in `src/tests/`, including updated recommended pytest commands.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit d24245a4b0684ce61557ae6bbe820c0a63c898d1. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
