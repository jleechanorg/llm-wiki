# PR #330: feat: agent-aware slash command dispatch in /claw

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-03-21
**Author:** jleechan2015
**Stats:** +40/-36 in 1 files

## Summary
PR #329 added slash command resolution to /claw, but testing revealed /simplify is a Claude Code plugin not a file-based command. The fallback just said not found without helping non-Claude agents.

## Background
PR #329 added slash command resolution to /claw, but testing revealed /simplify is a Claude Code plugin not a file-based command. The fallback just said not found without helping non-Claude agents.

## Raw Body
## Background
PR #329 added slash command resolution to /claw, but testing revealed /simplify is a Claude Code plugin not a file-based command. The fallback just said not found without helping non-Claude agents.

## Goals
- Claude Code agents execute slash commands directly (built-in skill support)
- Non-Claude agents get web search instructions + common command descriptions
- File-based commands/skills still inlined as before

## Tenets
- Agent-appropriate dispatch: same task, different strategy per agent type
- Graceful degradation: always provide enough context to act

## High-level description of changes
Replaced the not found fallback with an agent-type-aware dispatch block. Claude Code agents told to execute directly. Non-Claude agents told to web search and given common command hints.

## Testing
- Claude Code agent: executed /simplify natively, found 6 items, PASS
- Cursor agent: followed web search path, understood /simplify, found 6 matching items, PASS
- Both wrote findings to /tmp/openclaw/test-{claude,cursor}-simplify.log

## Low-level details
- **.claude/commands/claw.md**: Rewrote fallback in Step 2.5 with agent-type-aware instructions.

Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Low risk documentation/script change limited to `/claw`’s task text construction, though it may alter how downstream agents interpret unresolved slash commands.
> 
> **Overview**
> `/claw`’s *slash command resolution* logic is updated to unify command/skill lookup into a single “resolved content/source” path and to improve the *not-found* behavior.
> 
> When a referenced `/<cmd>` can’t be found as a file, the dispatched task now includes **agent-aware instructions**: Claude Code agents are told to run the slash command directly, while non-Claude agents are guided to web-search the command and given brief examples for common commands.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab
