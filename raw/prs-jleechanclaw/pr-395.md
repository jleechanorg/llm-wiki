# PR #395: feat(claw): add learning-loop gate (Step A5.5) to /claw Path A

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-03-24
**Author:** jleechan2015
**Stats:** +372/-18 in 2 files

## Summary
`/integrate` auto-triggers `/learn`, but `/claw` tasks that directly create PRs bypass `/integrate`, silently breaking the learning loop. Caught 2026-03-24. Resolves orch-xlu.

Note: claw.md also contains the full Path A (ao spawn routing) implementation from a prior change — this PR is scoped only to Step A5.5 (learning-loop gate).

## Background
`/integrate` auto-triggers `/learn`, but `/claw` tasks that directly create PRs bypass `/integrate`, silently breaking the learning loop. Caught 2026-03-24. Resolves orch-xlu.

Note: claw.md also contains the full Path A (ao spawn routing) implementation from a prior change — this PR is scoped only to Step A5.5 (learning-loop gate).

## Raw Body
## Background
`/integrate` auto-triggers `/learn`, but `/claw` tasks that directly create PRs bypass `/integrate`, silently breaking the learning loop. Caught 2026-03-24. Resolves orch-xlu.

Note: claw.md also contains the full Path A (ao spawn routing) implementation from a prior change — this PR is scoped only to Step A5.5 (learning-loop gate).

## Goals
Ensure any `/claw` coding task that bypasses `/integrate` still captures learnings at the end.

## Tenets
- Fail closed: gate failure appends `/learn` rather than silently skipping
- Minimal scope: only adds Step A5.5; doesn't change existing Path A/B routing

## High-level description of changes
Added Step A5.5 to `/claw` Path A (between A5 and A6): checks if composed task has `/integrate`, `/learn`, or explicit N/A reason; appends `/learn` if absent. Tightened has_na regex with word boundaries. Fixed fail-open bug (empty HAS_LEARN now treated as no). Also fixed: task file note extension, `ao list projects` command.

## Testing
Manual gate verification. LLM orchestration commands — no executable unit tests.

## Low-level details
- **.claude/commands/claw.md**: Step A5.5 added; has_na regex tightened; empty HAS_LEARN fail-closed.

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->
## Summary by CodeRabbit

* **Documentation**
  * Updated `/claw` command docs to describe two routing paths: a local spawn-and-send workflow for coding tasks and an async gateway flow for others
  * Added task-type classification, automatic issue/bead creation when none provided, and slash-command resolution before dispatch
  * Documented parallel session behavior, temp task-file retention for manual resend, and a learning-loop gate that appends a learning step when needed
<!-- end of auto-generated comment: release notes by coderabbit.ai -->

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Updates the `/claw` orchestration to classify and route tasks, spawn/send to `ao` sessions, and append `/learn` w
