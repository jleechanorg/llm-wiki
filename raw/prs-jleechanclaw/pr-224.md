# PR #224: [P1] fix(escalation-router): fail-closed CR gate + add CI/thread checks before MergeAction

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-03-16
**Author:** jleechan2015
**Stats:** +128/-198 in 4 files
**Labels:** P1

## Summary
(none)

## Raw Body
Add fail-closed CR gate and CI/thread checks before triggering MergeAction.

---
**Priority: P1 — Breaks green loop**

Without this fix, the escalation router can trigger MergeAction without verifying CodeRabbit approval or passing CI. PRs can be incorrectly marked ready. Doesn't stop autonomy entirely but allows bad merges through the gate.

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Medium risk because it changes auto-merge decision logic (adds CI/thread gates and fail-closed behavior) and adjusts PR auto-merge/backfill scripts, which could block merges or change automation behavior if the checks behave unexpectedly.
> 
> **Overview**
> Tightens auto-merge safety checks in `escalation_router._route_merge_ready` by making the CodeRabbit gate *fail-closed* and adding additional gates (CI must be green and unresolved review threads must be zero) before returning `MergeAction`, all only when auto-merge is enabled.
> 
> Improves escalation anomaly alerting by recognizing the current action names (`KillAndRespawnAction`, `NotifyJeffreyAction`) and sending a preformatted Slack message via `notify_slack_outbox_alert`, which now supports an optional `message` override.
> 
> Fixes `scripts/ao-backfill.sh` auto-merge filtering to treat “no required checks” as CI pass, corrects mergeable handling to match the REST boolean, and removes a duplicated pass-4 auto-merge block.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 720c49b705fbf9298a8c78ac50a4831cc091f6ff. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
