# PR #267: docs(roadmap): AO vs Python orchestration — what each layer does

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-03-17
**Author:** jleechan2015
**Stats:** +559/-0 in 5 files

## Summary
- Documents the three-layer orchestration architecture: AO lifecycle-worker (inner loop), ao-pr-poller (outer loop), mctrl supervisor (registry watcher)
- Clarifies what is redundant vs complementary between AO and the Python layer
- Corrects earlier overstatement (orch-ag46) that Python layer was mostly redundant

## Raw Body
## Summary
- Documents the three-layer orchestration architecture: AO lifecycle-worker (inner loop), ao-pr-poller (outer loop), mctrl supervisor (registry watcher)
- Clarifies what is redundant vs complementary between AO and the Python layer
- Corrects earlier overstatement (orch-ag46) that Python layer was mostly redundant

## Changes
- **New:** `roadmap/AO_VS_PYTHON_ORCHESTRATION.md` — comprehensive comparison doc
- **Beads:** orch-ag46 epic created with 8 child tasks, orch-ag46.8 closed (this doc)

## Testing
Documentation only — no code changes.

## Known Limitations
- Also includes an unrelated prior commit (`feat: add /research command + PR workflow improvements`) that was ahead of main

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Primarily documentation/policy updates plus a new isolated test file; no production code paths are modified, so behavioral risk is low.
> 
> **Overview**
> Adds a new roadmap doc (`roadmap/AO_VS_PYTHON_ORCHESTRATION.md`) clarifying the division of responsibilities between AO (lifecycle-worker), `scripts/ao-pr-poller.sh` (PR scanning/session spawning), and the Python supervisor layer, and which parts are considered redundant vs complementary.
> 
> Extends repository *operator/agent* guidance by updating `workspace/SOUL.md` with a mandatory post-push review loop and an evidence-review-before-merge protocol, and adds a `workspace/.gemini/commands/research.toml` research command template.
> 
> Updates the beads issue tracker (`.beads/issues.jsonl`) with new gap/roadmap items and adds `src/tests/test_zombie_detection.py` to validate key `ao-pr-poller` zombie-session detection/merge-gate heuristics via bash subprocess tests.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 5f416bb608ec5ed234c575566fc83c2588ff9ee9. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>

