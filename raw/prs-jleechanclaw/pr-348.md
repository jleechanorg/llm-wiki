# PR #348: [P1] fix: agents auto-run /er headless via evidence-reviewer subagent

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-03-21
**Author:** jleechan2015
**Stats:** +24/-19 in 2 files

## Summary
Agento agents were instructed to run /er to satisfy the evidence review condition in the merge gate. However, /er is a slash command that only works in interactive Claude Code sessions — ao-spawned headless agents cannot invoke it directly. This left the evidence review step as the last manual intervention blocking full autonomy.

## Background
Agento agents were instructed to run /er to satisfy the evidence review condition in the merge gate. However, /er is a slash command that only works in interactive Claude Code sessions — ao-spawned headless agents cannot invoke it directly. This left the evidence review step as the last manual intervention blocking full autonomy.

## Raw Body
## Background

Agento agents were instructed to run /er to satisfy the evidence review condition in the merge gate. However, /er is a slash command that only works in interactive Claude Code sessions — ao-spawned headless agents cannot invoke it directly. This left the evidence review step as the last manual intervention blocking full autonomy.

## Goals

- Agents automatically run evidence review headlessly when their PR reaches criteria 1-5
- Evidence verdict is posted as a PR comment so merge_gate.py can detect it
- No manual human intervention required to satisfy criterion 6

## Tenets

- Prefer existing tooling: use the evidence-reviewer subagent that already exists
- Minimal rule changes: only update agent instruction text, no new Python code needed

## Changes

agent-orchestrator.yaml — updated all 13 agent rule locations to instruct agents to spawn the evidence-reviewer subagent (Agent tool, subagent_type=evidence-reviewer), collect its PASS/WARN/FAIL verdict, and post it as a PR comment before declaring the PR green.

## Testing

- grep -c evidence-reviewer subagent agent-orchestrator.yaml -> 13
- grep -c run /er and wait|run /er to get agent-orchestrator.yaml -> 0
- Config file only — no code changes

🤖 Generated with Claude Code

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Config/documentation-only updates to agent instruction text; low risk, but could change agent behavior around when/how evidence review is triggered.
> 
> **Overview**
> Updates the merge-readiness playbook so agents **no longer attempt interactive `/er`**, and instead spawn the headless `evidence-reviewer` subagent to run evidence review, require a PASS verdict, and post the verdict as a PR comment before declaring a PR green.
> 
> Aligns related automation messages (e.g. `approved-and-green`, `agent-stuck`) and project-specific `agentRules` to the same evidence-reviewer workflow, and adds `workspace/AGENT_BASE_RULES.md` as a placeholder for workspace-local rule overlays.
> 
