# PR #438: [agento] [P1] fix(monitor): rename ao6green to ao7green — script and criteria now consistent

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-03-29
**Author:** jleechan2015
**Stats:** +9/-9 in 4 files

## Summary
- Rename `scripts/ao6green-pr-monitor.sh` → `scripts/ao7green-pr-monitor.sh` (and launchd wrapper)
- Fix stale trajectory threshold comment inside the script: `green_count >= 6` → `>= 7` (criteria were already 7-green; only the comment was wrong)
- Update log path from `ao6green-pr-monitor.log` → `ao7green-pr-monitor.log`
- Fix all CLAUDE.md PR Status Report Format references to use 7-green threshold and correct script name

## Background
The monitor script was updated to 7-green criteria (commit `7f3d46c42`) but the script name (`ao6green`), internal trajectory comment, log path, and CLAUDE.md references were not updated. This left stale 6-point terminology in the codebase.

Fixes orch-jqy (stale 6-point green table in repo CLAUDE.md).

## Raw Body
## Summary
- Rename `scripts/ao6green-pr-monitor.sh` → `scripts/ao7green-pr-monitor.sh` (and launchd wrapper)
- Fix stale trajectory threshold comment inside the script: `green_count >= 6` → `>= 7` (criteria were already 7-green; only the comment was wrong)
- Update log path from `ao6green-pr-monitor.log` → `ao7green-pr-monitor.log`
- Fix all CLAUDE.md PR Status Report Format references to use 7-green threshold and correct script name

## Background
The monitor script was updated to 7-green criteria (commit `7f3d46c42`) but the script name (`ao6green`), internal trajectory comment, log path, and CLAUDE.md references were not updated. This left stale 6-point terminology in the codebase.

Fixes orch-jqy (stale 6-point green table in repo CLAUDE.md).

## Testing
- `bash -n` passes on both renamed scripts
- All script references updated in-place

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Low risk: this is primarily a rename and documentation/config alignment; only the monitor’s on-track threshold check changes from 6 to 7, which could affect alerting but not core data or security paths.
> 
> **Overview**
> Updates PR monitoring to be consistently **7-green** across the repo.
> 
> Renames references from `ao6green-pr-monitor` to `ao7green-pr-monitor` in `CLAUDE.md`, the cron entry, and the launchd wrapper, and switches the default log filename to `ao7green-pr-monitor.log`. Also updates the script’s trajectory logic to treat PRs as on-track only when `green_count >= 7` (and age ≤ 3h), aligning behavior with the documented 7-point criteria.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit cc274606b501ca46c9eff191ed4f6ceb9c4b2d02. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->

## Summary by Cod
