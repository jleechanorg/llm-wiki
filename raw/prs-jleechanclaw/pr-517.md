# PR #517: feat: mem0 embedding fix, minimax config tests, docs, AO progress

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-04-06
**Author:** jleechan2015
**Stats:** +418/-28 in 10 files

## Summary
Bundles harness improvements that were pending on `main` working tree (targeted `git add` only — no workspace screenshots).

### Included
- **mem0**: `scripts/mem0_shared_client.py` — pass OpenAI `dimensions` for 768-d Qdrant collections; `check_compatibility=false` for qdrant-client. Unit tests in `src/tests/test_mem0_shared_client.py`.
- **openclaw-mem0 plugin**: postinstall patch for upstream mem0ai OpenAI embedder (`dimensions: this.embeddingDims`); vitest for patch idempotency; `npm run pos

## Raw Body
## Summary
Bundles harness improvements that were pending on `main` working tree (targeted `git add` only — no workspace screenshots).

### Included
- **mem0**: `scripts/mem0_shared_client.py` — pass OpenAI `dimensions` for 768-d Qdrant collections; `check_compatibility=false` for qdrant-client. Unit tests in `src/tests/test_mem0_shared_client.py`.
- **openclaw-mem0 plugin**: postinstall patch for upstream mem0ai OpenAI embedder (`dimensions: this.embeddingDims`); vitest for patch idempotency; `npm run postinstall`.
- **Config tests**: `TestMinimaxProviderConsistency` in `tests/test_openclaw_configs.py` (stale `minimax` plugin + model/provider alignment).
- **Docs**: `CLAUDE.md`, `.claude/skills/openclaw-models.md` — `minimax-portal/MiniMax-M2.7`, timeout ≤600.
- **Other**: `agent-orchestrator.yaml`, `scripts/ao-progress-reporter.sh` updates.

### Already on `main` (not in this PR diff)
- `scripts/doctor.sh` / `monitor-agent.sh` live-dir resolution — merged separately (`0b767f323d`, `6982f7eda2`).

### Tests
```bash
python3 -m pytest tests/test_openclaw_configs.py -k TestMinimaxProviderConsistency -q
python3 -m pytest src/tests/test_mem0_shared_client.py -q
cd extensions/openclaw-mem0 && npm test
```

Made with [Cursor](https://cursor.com)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Medium risk because it changes memory embedding behavior (OpenAI `dimensions`) and adds install-time patching of a third-party dependency, plus modifies Slack reporting automation; misconfig or upstream changes could break installs or memory queries.
> 
> **Overview**
> Updates the harness to align MiniMax usage on **`minimax-portal/MiniMax-M2.7`** (OAuth via `minimax-portal-auth`) and documents/enforces the **`timeoutSeconds ≤ 600`** constraint, including new config tests that fail fast on stale `minimax` plugin entries or model/provider prefix mismatches.
> 
> Fixes mem0/Qdrant interoperability by requesting OpenAI embedding vectors with explicit `dimensions` (matching 
