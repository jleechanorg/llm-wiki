# PR #95: feat: add proof payload to task completion notifications

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-03-12
**Author:** jleechan2015
**Stats:** +194/-1 in 3 files

## Summary
- Add proof URLs (PR URL, commit URL, changed file list) to task completion notifications
- Enforces the SOUL.md Autopilot Policy contract requiring proof in completion updates

## Raw Body
## Summary
- Add proof URLs (PR URL, commit URL, changed file list) to task completion notifications
- Enforces the SOUL.md Autopilot Policy contract requiring proof in completion updates

## Changes
- `src/orchestration/reconciliation.py`: Added `_get_repo_info()` and `_get_proof_payload()` helpers
- The reconciliation loop now includes proof in the notification payload:
  - `repo`: owner/repo format
  - `pr_url` and `pr_number`: if a PR exists for the branch
  - `commit_sha` and `commit_url`: full GitHub commit URL
  - `changed_files` and `changed_files_count`: list of files modified

## Testing
- Ran reconciliation tests: 9 passed
- Ran gh_integration tests: 52 passed
- Verified imports work correctly

## Known Limitations
- PR detection is best-effort (uses `gh pr list` to find PR by branch)
- If no remote origin or gh not authenticated, proof fields will be empty

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Moderate risk because it changes the reconciler’s completion notification payload and adds best-effort `git`/`gh` subprocess calls that could behave differently across environments (missing remotes/auth, timeouts). Failures are mostly swallowed, but incorrect/missing proof fields could affect downstream automation or expectations.
> 
> **Overview**
> Updates the autopilot documentation to formalize a **Deterministic Slack Thread Response Contract** and a **Proof-First** requirement (completion updates must include a PR/commit/artifact URL).
> 
> On the runtime side, `reconciliation.py` now enriches `task_finished` notifications with a best-effort “proof payload”: resolves `owner/repo` from the worktree’s `origin`, attempts PR discovery via `gh` (`detect_pr`), and includes `commit_sha`/GitHub `commit_url` plus a `start_sha..HEAD` changed-file list/count.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 51170c4a44a024ed68586aa28d2a01fbc9f9e77a
