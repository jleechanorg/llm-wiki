# PR #443: [agento] [P1] fix(rebuild-native-modules): skip monorepo container packages without entry point

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-03-30
**Author:** jleechan2015
**Stats:** +28/-4 in 3 files

## Summary
- Guard the require check in `rebuild-native-modules.sh` to skip packages without a `package.json`
- Fixes the infinite rebuild loop triggered by `@rolldown`, a monorepo container package that has platform-specific sub-packages but no root entry point

## Raw Body
## Summary
- Guard the require check in `rebuild-native-modules.sh` to skip packages without a `package.json`
- Fixes the infinite rebuild loop triggered by `@rolldown`, a monorepo container package that has platform-specific sub-packages but no root entry point

## Changes
- `scripts/rebuild-native-modules.sh`: Added `package.json` existence check before `require()` test in both the pre-rebuild detection loop and the post-rebuild verification loop
- `@rolldown` now logs `SKIP: @rolldown has no package.json (monorepo container)` and is excluded from the mismatch detection

## Testing
- Ran `bash ~/.openclaw/scripts/rebuild-native-modules.sh` — exits cleanly with `@rolldown skipped`
- Confirmed via direct `node -e "require(...)"` that `@rolldown/binding-darwin-arm64` still loads correctly

## Fixes
- Closes orch-12c (health-check rebuild loop for @rolldown)

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Medium risk because it changes GitHub Actions runner selection (self-hosted → GitHub-hosted), which can affect CI behavior and permissions/environment assumptions. The native-module rebuild changes are localized but alter detection logic for scoped packages and may change which modules get rebuilt/skipped.
> 
> **Overview**
> **CI:** Updates `skeptic-cron` and `skeptic-gate` workflows to run on `ubuntu-latest` instead of dynamically-selected self-hosted runners.
> 
> **Runtime tooling:** Improves `scripts/rebuild-native-modules.sh` by correctly identifying scoped native packages (full `@scope/name`) and skipping packages that lack a `package.json` (monorepo container packages), preventing repeated rebuild attempts while keeping the post-rebuild verification consistent.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 3cd7bfd6d6c85ce4449447cf882c14cd6dd333ca. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab
