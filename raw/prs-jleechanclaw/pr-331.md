# PR #331: [agento] chore: remove AGENT_BASE_RULES.md

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-03-21
**Author:** jleechan2015
**Stats:** +0/-94 in 1 files

## Summary
`workspace/AGENT_BASE_RULES.md` was used as a shared rules file injected into AO agent sessions via `agentRulesFile:` in `agent-orchestrator.yaml`. As part of a config consolidation, its unique content (pre-exit checklist, MCP mail inbox polling protocol, when-stuck escalation steps) was merged into `defaults.agentRules` and the `agentRulesFile:` references were removed from all projects.

## Background
`workspace/AGENT_BASE_RULES.md` was used as a shared rules file injected into AO agent sessions via `agentRulesFile:` in `agent-orchestrator.yaml`. As part of a config consolidation, its unique content (pre-exit checklist, MCP mail inbox polling protocol, when-stuck escalation steps) was merged into `defaults.agentRules` and the `agentRulesFile:` references were removed from all projects.

## Raw Body
## Background
`workspace/AGENT_BASE_RULES.md` was used as a shared rules file injected into AO agent sessions via `agentRulesFile:` in `agent-orchestrator.yaml`. As part of a config consolidation, its unique content (pre-exit checklist, MCP mail inbox polling protocol, when-stuck escalation steps) was merged into `defaults.agentRules` and the `agentRulesFile:` references were removed from all projects.

## Goals
Delete the now-dead file.

## Tenets
- No functional change — content was already migrated
- Minimize config surface area

## High-level description of changes
Deletes `workspace/AGENT_BASE_RULES.md`. No other changes.

## Testing
No tests needed — file deletion only. The `agentRulesFile:` references were already removed from `agent-orchestrator.yaml` so nothing reads this file.

## Low-level details
- **workspace/AGENT_BASE_RULES.md**: deleted — content consolidated into `defaults.agentRules` in `agent-orchestrator.yaml`

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Low risk: deletes an unused rules markdown file with no runtime code changes, so impact is limited to repo content/maintenance.
> 
> **Overview**
> Removes `workspace/AGENT_BASE_RULES.md`, a now-dead agent rules document that is no longer referenced after rules were consolidated elsewhere.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit acd75afd8cf0c439dfaedb5cc8283fa1914b6036. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->

## Summary by CodeRabbit

* **Chores**
  * Removed internal documentation file. No impact to end-user functionality.

<!-- end of auto-generated comment: release notes by coderabbit.ai -->
