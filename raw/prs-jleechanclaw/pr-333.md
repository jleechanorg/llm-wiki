# PR #333: fix: update stale CLAUDE.md green table + fix launchd plist variable expansion

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-03-21
**Author:** jleechan2015
**Stats:** +71/-21 in 4 files

## Summary
(none)

## Raw Body
## Summary\n- update the PR & Merge Protocols 6-point green criteria table in CLAUDE.md to the current canonical conditions\n- harden launchd python selection so mem0 extraction never prefers macOS system python (/usr/bin/python3)\n- add regression tests covering launchd HOME placeholder usage and python selection policy\n\n## Testing\n- python3 -m pytest tests/test_pr51_hardening.py -q

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Touches macOS service installation and interpreter selection logic, which can block installs or change which Python is used for launchd-managed services if assumptions differ across machines.
> 
> **Overview**
> Tightens PR merge protocol documentation by updating the 6-point “green” criteria wording and making the evidence-review PASS requirement explicitly match an exact comment format.
> 
> Fixes launchd template variable expansion by replacing a literal `$HOME` default with the `@HOME@` placeholder in `ai.agento-manager.plist.template`, ensuring `install-launchagents.sh` can reliably substitute paths.
> 
> Hardens `scripts/install-launchagents.sh` Python selection for mem0/qdrant-client on macOS: it no longer prefers `/usr/bin/python3`, and it now fails closed (exits with a clear error) when no suitable interpreter is found. Adds regression tests to enforce both the `@HOME@` placeholder rule in `launchd/*.plist.template` and the “never use system Python for mem0 detection” policy.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit c94e49e7c10ba0dddb2d5dedadf5775ef1b7b61a. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->
## Summary by CodeRabbit

* **Documentation**
  * Refined PR merge eligibility wording and tightened validation checks for automated review signals.

* **Bug Fixes**
  * Improved Python interpreter selectio
