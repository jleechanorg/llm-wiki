# PR #339: [P1] fix: ao is canonical for coding tasks — preserve+expand task text, ban sessions_spawn

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-03-21
**Author:** jleechan2015
**Stats:** +33/-0 in 2 files

## Summary
When OpenClaw dispatches a coding task to a worker, it has two paths: its internal sessions_spawn tool (nested LLM, no worktree) and ao spawn (external CLI, real git worktree + tmux). For orch-han, OpenClaw chose sessions_spawn and silently rewrote the task — dropping /claw and mctrl_test repo instructions. The worker also never auto-submitted (sessions_spawn pastes but does not press Enter).

## Background
When OpenClaw dispatches a coding task to a worker, it has two paths: its internal sessions_spawn tool (nested LLM, no worktree) and ao spawn (external CLI, real git worktree + tmux). For orch-han, OpenClaw chose sessions_spawn and silently rewrote the task — dropping /claw and mctrl_test repo instructions. The worker also never auto-submitted (sessions_spawn pastes but does not press Enter).

## Raw Body
## Background
When OpenClaw dispatches a coding task to a worker, it has two paths: its internal sessions_spawn tool (nested LLM, no worktree) and ao spawn (external CLI, real git worktree + tmux). For orch-han, OpenClaw chose sessions_spawn and silently rewrote the task — dropping /claw and mctrl_test repo instructions. The worker also never auto-submitted (sessions_spawn pastes but does not press Enter).

## Goals
- Ban sessions_spawn for any task involving code, files, or PRs
- Enforce ao as the canonical dispatch path regardless of how the request arrived
- Task text must be the user's original words + memory expansion — never a condensed rewrite

## Tenets
- Config over code: fix policy files, not the binary
- Preserve intent: user's exact words reach the worker; memories add context, never replace

## High-level description of changes
SOUL.md and dispatch-task/SKILL.md now state:
1. sessions_spawn is banned for coding tasks (no worktree, no auto-submit, allows silent rewrite)
2. ao is canonical — applies from Slack, HTTP gateway, cron, and inline prompts
3. Task construction = user's original text verbatim + appended memory context from /mem-search

## Testing
Bug observed in session b8ac647d JSONL: sessions_spawn tool call with OpenClaw-written condensed task text instead of user's full description. Policy takes effect on next gateway reload.

## Low-level details
- **workspace/SOUL.md**: Agent Dispatch Policy section — ban, ao rule, preserve+expand task rule, small/large threshold
- **skills/dispatch-task/SKILL.md**: NEVER use sessions_spawn block + preserve+expand task construction rule
