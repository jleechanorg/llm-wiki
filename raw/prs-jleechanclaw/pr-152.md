# PR #152: fix(mem0): fix session scanner to find all Claude/Codex sessions

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-03-15
**Author:** jleechan2015
**Stats:** +89/-20 in 1 files

## Summary
- Claude sessions were completely missed (29,592 files) — UUID-named JSONL with no `sessions/` in path
- Codex `archived_sessions/` and `sessions_archive/` dirs were missed (7,108 files)
- Scanner now finds **43,120 sessions** (was 9,182)

## Test Plan
- [ ] Dry-run count: `PYTHONPATH=. python3 -c "from scripts.mem0_extract_facts import *; ..."` shows ~43k sessions
- [ ] Full rescan: `python3 scripts/mem0_extract_facts.py --since 999999 --workers 2`

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Changes session discovery rules and adds parallel processing in batch mode, which can affect which sessions are processed and increase concurrent load on mem0/qdrant. Failures

## Raw Body
## Summary
- Claude sessions were completely missed (29,592 files) — UUID-named JSONL with no `sessions/` in path
- Codex `archived_sessions/` and `sessions_archive/` dirs were missed (7,108 files)
- Scanner now finds **43,120 sessions** (was 9,182)

## Changes
- `_is_session_file(path, agent_id)`: per-source detection replacing the broken `"sessions" in path` check
  - Claude: UUID regex match on filename
  - Codex: ancestor dir in `{sessions, archived_sessions, sessions_archive}`, skip `worktrees/` + `.beads/`
  - OpenClaw: unchanged (`sessions/` in path)
- Added `ThreadPoolExecutor` parallelism with `--workers N` (default 4)
- Progress logging every 100 sessions

## Test plan
- [ ] Dry-run count: `PYTHONPATH=. python3 -c "from scripts.mem0_extract_facts import *; ..."` shows ~43k sessions
- [ ] Full rescan: `python3 scripts/mem0_extract_facts.py --since 999999 --workers 2`

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Changes session discovery rules and adds parallel processing in batch mode, which can affect which sessions are processed and increase concurrent load on mem0/qdrant. Failures are handled per-session, but misclassification or racey state updates could lead to missed or duplicate processing.
> 
> **Overview**
> Fixes session scanning so batch extraction finds the right JSONL files per source: **Claude** UUID-named files in project dirs, and **Codex** files under `sessions`/`archived_sessions`/`sessions_archive` while skipping `worktrees`/`.beads`/`history.jsonl`.
> 
> Batch `--since` runs now process pending sessions in parallel via `ThreadPoolExecutor` with a new `--workers` flag (default 4) and periodic progress logging, while still tracking processed sessions by `(path, mtime)` in state.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit f12c7f87824ca3d102bb31643702aae624b52e40. This will update automatically on new commits. Configur
