# PR #494: [agento] feat(launchd): add antig cmux loop + orch-7b7 slack check scripts

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-04-04
**Author:** jleechan2015
**Stats:** +165/-1 in 5 files

## Summary
Local session work: antig cmux loop automation (sends /loop to antig cmux terminal every 10min via launchd) and orch-7b7 PR status check script.

## Background
Local session work: antig cmux loop automation (sends /loop to antig cmux terminal every 10min via launchd) and orch-7b7 PR status check script.

## Raw Body
## Background
Local session work: antig cmux loop automation (sends /loop to antig cmux terminal every 10min via launchd) and orch-7b7 PR status check script.

## Goals
- Automate /loop delivery to antig cmux workspace:2/surface:4 on a 10min interval
- Auto-kill the loop after 8h via companion launchd job
- Provide a Slack-posting PR status check for orch-7b7 (ao-2313)

## Tenets
- Launchd for persistence (survives shell exit)
- Self-limiting (kill plist stops runaway loops after 8h)
- Scripts follow existing patterns in scripts/

## High-level description of changes
- `launchd/ai.openclaw.antig-cmux-loop.plist`: fires every 600s, runs antig-cmux-loop.sh
- `launchd/ai.openclaw.antig-cmux-loop-kill.plist`: fires every 28800s (8h), removes the loop job
- `scripts/antig-cmux-loop.sh`: uses cmux CLI to send /loop + Enter to workspace:2/surface:4
- `scripts/orch-7b7-slack-check.sh`: checks ao-2313 worktree PR status, posts to #all-jleechan-ai thread

## Testing
- Scripts follow existing launchd plist patterns in repo
- antig-cmux-loop.sh uses same cmux send pattern as other cmux scripts

## Low-level details
- **launchd/ai.openclaw.antig-cmux-loop.plist**: StartInterval=600, RunAtLoad=true, logs to ~/.openclaw/logs/
- **launchd/ai.openclaw.antig-cmux-loop-kill.plist**: StartInterval=28800, RunAtLoad=false (manual kill on 8h)
- **scripts/antig-cmux-loop.sh**: exports CMUX env vars, calls cmux send + send-key
- **scripts/orch-7b7-slack-check.sh**: checks worktree branch, PR state via gh, CI skeptic/evidence checks, posts to Slack thread

## Verification
- N/A (scripts are straightforward shell/plist files)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Adds new macOS `launchd` automation and a Slack-posting script that invoke external tools (`cmux`, `gh`, `curl`) and use hard-coded user paths, which can fail noisily or affect local environments if misconfigured.
> 
> **Overview**
> Adds a new macOS `launchd` job `ai.openclaw.antig-cmux-loop` that runs every 10
