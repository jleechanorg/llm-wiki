# PR #538: fix: replace hardcoded paths with HOME for portability

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-04-09
**Author:** jleechan2015
**Stats:** +8/-8 in 6 files

## Summary
Replace hardcoded `/Users/jleechan` paths with `$HOME` in 6 scripts for multi-user portability.

## Raw Body
## Summary
Replace hardcoded `/Users/jleechan` paths with `$HOME` in 6 scripts for multi-user portability.

## Changes
- `stability-report.launchd.sh`: /Users/jleechan/.openclaw → $HOME/.openclaw
- `simplify-daily.sh`: /Users/jleechan/project_agento → $HOME/project_agento
- `orch-7b7-slack-check.sh`: /Users/jleechan/.worktrees → $HOME/.worktrees
- `openclaw-upgrade-safe.sh`: NVM paths → $HOME
- `mem0-native-module-watchdog.sh`: NVM paths → $HOME
- `install-openclaw-launchd.sh`: NVM path → $HOME

## Why
smartclaw PR #14 flagged these as portability blockers. Other users cannot run these scripts.

🤖 Generated with Claude Code

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Low risk path-substitution change limited to shell scripts; primary risk is mis-resolving expected locations if a user’s environment/home layout differs from the previous hardcoded paths.
> 
> **Overview**
> Updates several operational scripts to use `"${HOME}"` instead of hardcoded `/Users/jleechan` paths (NVM `node`/`npm` binaries, OpenClaw scripts, and local worktree/repo directories).
> 
> This makes launchd installers, watchdog/upgrade tooling, Slack status checks, and the stability-report wrapper runnable across different user accounts without editing absolute paths.
> 
> <sup>Reviewed by [Cursor Bugbot](https://cursor.com/bugbot) for commit c872f4788e363db90a8f7a2910888ad4bd7c56af. Bugbot is set up for automated code reviews on this repo. Configure [here](https://www.cursor.com/dashboard/bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
