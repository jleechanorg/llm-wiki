# PR #160: fix(soul): make memory citation actionable with 8-char ID prefix

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-03-15
**Author:** jleechan2015
**Stats:** +2714/-3 in 17 files

## Summary
- Memory citations now include an 8-char ID prefix so `_(Memory: X)_` has a real value to show
- SOUL.md updated to cite by bracket ID with fallback `_(From memory)_`

## Raw Body
## Summary
- Memory citations now include an 8-char ID prefix so `_(Memory: X)_` has a real value to show
- SOUL.md updated to cite by bracket ID with fallback `_(From memory)_`

## Changes
- Plugin injects `[id.slice(0,8)]` prefix on each memory bullet in the `<relevant-memories>` block
- SOUL.md citation pattern updated to use the bracket ID

## Testing
- Manual verification: injected memories now have IDs visible in citations

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Adds a large new memory plugin plus a new Python `agent_end` capture path that writes to Qdrant and calls external LLM APIs; mistakes could leak/store unintended data or affect recall quality.
> 
> **Overview**
> Introduces a new `extensions/openclaw-mem0` Mem0-backed memory plugin (platform + OSS) that provides memory tools, CLI commands, auto-recall/auto-capture hooks, and per-agent memory namespaces.
> 
> **Recall output is made actionable** by prefixing injected memories with an 8-char ID (`[deadbeef]`) so responses can cite specific memories; `workspace/SOUL.md` updates the citation guidance accordingly.
> 
> Adds a fallback *Python* `scripts/auto_fact_capture.py` for extracting 1–3 facts from session JSONL via Groq and storing them via `mem0_shared_client`, plus a Qdrant payload compat fix to also write camelCase `userId` so Node-side recall can see Python-ingested points.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 9c1d09aa0fb86c7cb60c40c0a2f5f04ab9990ed0. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->

## Summary by CodeRabbit

* **New Features**
  * Automatic fact extraction and storage from user sessions
  * Cross-session memory recall capabilities verified

* **Bug Fixes**
  * Fixed memory payload sync
