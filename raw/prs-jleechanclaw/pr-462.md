# PR #462: [agento] [P2] docs: 3-stage openclaw dev pipeline documentation

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-04-01
**Author:** jleechan2015
**Stats:** +950/-17 in 4 files

## Summary
Documents the 3-stage openclaw dev pipeline (orch-1ps epic) completed 2026-03-31.

- Creates `docs/STAGING_PIPELINE.md` — full reference doc with ASCII + Mermaid architecture diagrams
- Creates `docs/STAGING_PIPELINE.html` — dark-mode HTML version with interactive Mermaid diagram
- Updates `README.md` — adds "3-Stage Dev Pipeline" section and docs/ table entry

## Raw Body
## Summary

Documents the 3-stage openclaw dev pipeline (orch-1ps epic) completed 2026-03-31.

- Creates `docs/STAGING_PIPELINE.md` — full reference doc with ASCII + Mermaid architecture diagrams
- Creates `docs/STAGING_PIPELINE.html` — dark-mode HTML version with interactive Mermaid diagram
- Updates `README.md` — adds "3-Stage Dev Pipeline" section and docs/ table entry

## What the pipeline covers

| Stage | What | Status |
|-------|------|--------|
| 1 | Staging branch + `~/.openclaw-staging/` worktree | Done |
| 2 | `staging-promote.sh` — 6/6 canary → merge staging→main | Done (PR #459) |
| 3 | CI gate — portable checks on every PR | Done (PR #458) |
| 4 | Git hooks for auto-restart | **Open (P1)** — documented as known gap |

## Testing

- Documentation review — all links, anchors, and code blocks verified
- HTML renders Mermaid diagram with dark theme (matches `ORCHESTRATION_SYSTEM_DESIGN.html` template)
- Existing `test_canary_integration.py` passes (3/3)

## Known limitation

orch-1ps.4 (git hooks for auto-restart) is documented as an open P1 gap. The workaround (manual `staging-gateway.sh restart`) is documented in both docs.

Closes #orch-1ps

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Medium risk because it changes PR-gating behavior in `skeptic-gate` (diff retrieval, docs-only skipping, and fail/exit conditions), which can affect merge eligibility. The added docs/HTML are low risk but increase repo size and maintenance surface.
> 
> **Overview**
> Adds end-to-end documentation for the **3-stage OpenClaw dev pipeline**: a new `docs/STAGING_PIPELINE.md` reference (with ASCII/Mermaid diagrams) plus a rendered `docs/STAGING_PIPELINE.html`, and updates `README.md` to include a quick overview and link.
> 
> Hardens the `skeptic-gate` workflow by fetching the *actual unified diff* from GitHub (`Accept: application/vnd.github.v3.diff`), **skipping docs-only PRs**, increasing LLM res
