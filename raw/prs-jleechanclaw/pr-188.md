# PR #188: feat(orchestration): implement Sprint 1 - auto-merge gate and stuck d…

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-03-16
**Author:** jleechan2015
**Stats:** +513/-25 in 8 files

## Summary
(none)

## Raw Body
…etection fix

- orch-ou0t: Add MergeAction that calls gh pr merge (squash) with user preference gate
- orch-9smj: Add WaitForCIAction to avoid false positives on slow CI
- Mark Sprint 1 complete in roadmap

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Medium risk because it changes escalation behavior (stuck-session handling) and introduces automated `gh pr merge` execution, which can affect production PR lifecycle if misrouted or misconfigured.
> 
> **Overview**
> Implements **Sprint 1 orchestration closure** by routing `merge.ready` to a new `MergeAction` that can call `gh pr merge` (merge method validated, controlled by `~/.openclaw/state/user_preferences.json`) and logs/DMs results.
> 
> Hardens **`session.stuck` routing** to check CI status and return a `WaitForCIAction` (configurable grace period) when runs are `pending`/`in_progress`, avoiding unnecessary kill+respawn on slow CI; updates CI status mapping to distinguish active vs merely pending.
> 
> Updates tests to cover the new wait-for-CI action and CI-gated stuck routing, refreshes roadmap status entries, and appends several new items to `.beads/issues.jsonl`.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 9c0a87894de147045ad65f58f57d9976176b6672. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->
## Summary by CodeRabbit

* **New Features**
  * Automated PR merge capability with configurable merge methods; explicit wait-for-CI action for CI-aware decisions

* **Documentation**
  * Roadmap updated with implementation status, per-item completion indicators, and status tracking

* **Tests**
  * Added tests covering CI-wait behavior, CI-driven routing for stuck sessions, and merge-related scenarios; public CI-wait action exposed for testing

* **Chores**
  * Bulk update of issue records: 
