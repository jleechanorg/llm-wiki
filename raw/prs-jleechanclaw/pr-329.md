# PR #329: feat: resolve slash commands before /claw dispatch

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-03-21
**Author:** jleechan2015
**Stats:** +80/-1 in 1 files

## Summary
`/claw run /simplify on this repo` dispatched successfully to the OpenClaw gateway, but the agent didn't execute `/simplify` — it asked "say go ahead" instead. Root cause: OpenClaw has no way to discover slash command definitions, and `/claw` was forwarding raw text without resolving commands.

## Background
`/claw run /simplify on this repo` dispatched successfully to the OpenClaw gateway, but the agent didn't execute `/simplify` — it asked "say go ahead" instead. Root cause: OpenClaw has no way to discover slash command definitions, and `/claw` was forwarding raw text without resolving commands.

## Raw Body
## Background
`/claw run /simplify on this repo` dispatched successfully to the OpenClaw gateway, but the agent didn't execute `/simplify` — it asked "say go ahead" instead. Root cause: OpenClaw has no way to discover slash command definitions, and `/claw` was forwarding raw text without resolving commands.

## Goals
- `/claw` resolves slash commands before dispatch so OpenClaw receives actionable instructions
- Support commands (`.claude/commands/`) and skills (`~/.claude/skills/`)
- Maintain existing behavior for non-command tasks

## Tenets
- Fix at dispatch time (Option A) for immediate impact
- Resolution order matches Claude Code's own priority: repo-local > global > skills
- No changes to SOUL.md in this PR (Option B is separate)

## High-level description of changes
Added Step 2.5 to the `/claw` command that detects slash commands in the task description, resolves them to their full definition files, and inlines the content into the dispatch payload. Also updated the Notes section documenting resolution behavior.

## Testing
- Manual: dispatched `/claw run /simplify on this repo` before fix → agent asked "go ahead"
- After fix: command definition would be inlined into payload
- Harness analysis via `/harness` identified this as a knowledge gap + LLM path error

## Low-level details
- **.claude/commands/claw.md**: Added Step 2.5 with regex-based slash command extraction, three-tier file lookup (repo-local commands → global commands → skills), and payload enrichment. Updated Notes section with resolution documentation.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Low risk: changes are limited to the `.claude/commands/claw.md` dispatch script and primarily adjust how task text is constructed before sending to the gateway. Main risk is incorrect slash-command detection or file lookup causing unexpected task text to be sent.
> 
> **Overview**
> Updates `/claw` to **resolve referenced slas
