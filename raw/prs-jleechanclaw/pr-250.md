# PR #250: [P1] feat(mem0): add --project filter for jleechanclaw-only ingest

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-03-18
**Author:** jleechan2015
**Stats:** +130/-28 in 4 files

## Summary
- Add `--project` flag to `mem0_extract_facts.py` to filter Claude sessions by project hash
- Fixes ORCH-lpcn: random Feb sampling failed (0/10 recall) because jleechanclaw sessions are minority in ~/.claude/projects/

## Raw Body
## Summary
- Add `--project` flag to `mem0_extract_facts.py` to filter Claude sessions by project hash
- Fixes ORCH-lpcn: random Feb sampling failed (0/10 recall) because jleechanclaw sessions are minority in ~/.claude/projects/

## Changes
- Added `CLAUDE_PROJECT_FILTER` global and `--project` CLI argument
- Updated `scan_sessions_since()` to only scan matching project directory
- Tested: 13,509 sessions in jleechanclaw vs 42,241 total

## Testing
- Syntax check: `python3 -m py_compile scripts/mem0_extract_facts.py` ✅
- Help output shows new `--project` flag ✅
- Filter test confirms 13,509 jleechanclaw sessions vs 42,241 total ✅

## Known Limitations
- Must know the project hash (e.g., `-Users-jleechan--openclaw`) - can be found via `ls ~/.claude/projects/`

Closes ORCH-lpcn

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Moderate risk because it changes batch ingestion behavior and alters runtime config defaults (Slack thread history scope and TTS provider), which can affect message context and audio output in production-like setups.
> 
> **Overview**
> Adds a `--project` option to `scripts/mem0_extract_facts.py` to restrict `--since` batch scans of Claude sessions to a single project directory, with guardrails (reject path-like values, disallow with `--session`, and require non-negative `--since`).
> 
> Updates configs to switch TTS from ElevenLabs to Edge and changes Slack thread history loading from `thread` to `channel` scope; adjusts tests accordingly and adds unit coverage for `project_filter` path-traversal validation.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 5fa1790648482db9b90a790eb30cddd38ab2e196. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->
## Summary by CodeRabbit


