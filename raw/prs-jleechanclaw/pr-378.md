# PR #378: fix(bug-hunt): gate @openclaw escalation on positive bug count (#242)

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-03-24
**Author:** jleechan2015
**Stats:** +20/-4 in 1 files

## Summary
- Aligns daily bug-hunt notifications with [issue #242](https://github.com/jleechanorg/jleechanclaw/issues/242): do not append `@openclaw Please fix these bugs using agento` unless at least one bug is counted.
- Makes per-agent JSON parsing accept a top-level array or a `findings` array inside an object (common LLM output shape).
- Ignores non-numeric jq output when summing counts so arithmetic stays safe.

## Raw Body
## Summary
- Aligns daily bug-hunt notifications with [issue #242](https://github.com/jleechanorg/jleechanclaw/issues/242): do not append `@openclaw Please fix these bugs using agento` unless at least one bug is counted.
- Makes per-agent JSON parsing accept a top-level array or a `findings` array inside an object (common LLM output shape).
- Ignores non-numeric jq output when summing counts so arithmetic stays safe.

## Changes
- `scripts/bug-hunt-daily.sh`: set `OPENCLAW_BUG_ESCALATION` only when `ACTUAL_BUGS > 0`; extend jq bug-count expression; sanitize `AGENT_BUGS` before adding to `ACTUAL_BUGS`.

## Testing
- `bash -n scripts/bug-hunt-daily.sh`
- `python -m pytest tests/ -q` (208 passed, 82 skipped)

## Known limitations
- If an agent emits findings in another JSON shape, counts may still read as zero until the parser is extended again.

Fixes #242

Made with [Cursor](https://cursor.com)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Low risk shell-script change limited to reporting/notification formatting and more defensive JSON parsing; main risk is under/over-counting findings if unexpected JSON shapes appear.
> 
> **Overview**
> Adjusts the daily bug-hunt report to **only mention `@openclaw` when at least one bug is actually counted**, avoiding false escalations.
> 
> Makes bug counting more robust by accepting agent outputs as either a top-level JSON array or an object containing a `.findings` array, and sanitizes non-numeric `jq` results before summing into `ACTUAL_BUGS`.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit d88a4dc84b5058899c56c6df1710716ab04efdc3. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
