# PR #373: fix(launchd): wire scheduled jobs to correct UUIDs, add missing scripts

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-03-24
**Author:** jleechan2015
**Stats:** +125/-373 in 14 files

## Summary
6 schedule plists had dead UUIDs silently failing every scheduled run. jobs.json was migrated to new healthcheck jobs but the plists were never updated. Two scripts referenced by newer templates also did not exist.

## Background
6 schedule plists had dead UUIDs silently failing every scheduled run. jobs.json was migrated to new healthcheck jobs but the plists were never updated. Two scripts referenced by newer templates also did not exist.

## Raw Body
## Background

6 schedule plists had dead UUIDs silently failing every scheduled run. jobs.json was migrated to new healthcheck jobs but the plists were never updated. Two scripts referenced by newer templates also did not exist.

## Goals

1. Make launchd the primary scheduler for all repeating jobs
2. Remove 6 broken stale plists
3. Add missing scripts so all launchd templates resolve
4. Disable 5 migrated jobs in cron/jobs.json to prevent double-execution
5. Fix workspace-report-weekly Label/filename mismatch

## Tenets

- Launchd plists must reference scripts that exist
- Jobs must actually run when kickstarted, not just load

## High-level description of changes

Deleted 6 broken root-level plists (dead UUIDs): backup-4h20, daily-checkin-9am/12pm/6pm, genesis-memory/pattern-weekly.

Added two missing scripts: docs-drift-review.sh (runs docs_audit.sh + scans docs/context/), daily-openclaw-research.sh (calls openclaw agent with research prompt).

Fixed workspace-report-weekly.plist.template Label to match filename.

Updated install-openclaw-scheduled-jobs.sh: removed 4 dead legacy IDs from MIGRATED_JOBS map.

Updated install-launchagents.sh Linux section: replaced dead UUID systemd timer entries with script-based timers.

## Testing

Ran scripts/install-openclaw-scheduled-jobs.sh: 8 plists installed, 5 gateway cron jobs disabled in jobs.json, openclaw PATH smoke-test passed.

Ran launchctl kickstart for morning-log-review, cron-backup-sync, docs-drift-review -- all produced real log output. jobs.json: all 5 migrated jobs enabled: false.

## Low-level details (11 files)

- backup-4h20.plist (deleted): UUID 882c6964 not in jobs.json
- daily-checkin-9am/12pm/6pm.plist (deleted x3): UUIDs not in jobs.json
- genesis-memory/pattern-weekly.plist (deleted x2): string keys not valid UUIDs
- scripts/docs-drift-review.sh (new): runs docs_audit.sh + lists docs/context/ freshness
- scripts/daily-openclaw-research.sh (new): wraps openclaw agent with tips research message
- lau
