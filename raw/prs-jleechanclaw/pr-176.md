# PR #176: fix: trap backfill PID on exit + wire shared utils in auto_review_trigger

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-03-16
**Author:** jleechan2015
**Stats:** +25/-27 in 2 files

## Summary
Fixes two review comments from PR #173 (#2937307476, #2937307478).

## Test Plan
- [ ] python -m pytest src/tests/ -q passes
- [ ] plist: kill notifier PID manually, confirm script exits cleanly

🤖 Generated with Claude Code

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Medium risk because it changes launchd-managed process lifecycle (EXIT trap/kill behavior) and the reviewed-PR idempotency persistence path/write semantics, which could affect restarts or review retries if incorrect.
> 
> **Overview**
> Ensures the `agento-manager` launchd script reliably tears 

## Raw Body
## Summary
Fixes two review comments from PR #173 (#2937307476, #2937307478).

## Changes

**launchd/ai.agento-manager.plist.template**
- Capture BACKFILL_PID after backgrounding backfill loop
- Add EXIT trap to kill backfill when notifier crashes — ensures launchd KeepAlive gets a clean restart instead of hanging wait

**src/orchestration/auto_review_trigger.py**
- Replace inline _ensure_state_dir() and hand-rolled atomic write with ensure_state_dir() + atomic_json_write_single() from path_util/jsonfile_util
- Gives the new utility modules their first real importer (resolves dead-code comment)

## Test plan
- [ ] python -m pytest src/tests/ -q passes
- [ ] plist: kill notifier PID manually, confirm script exits cleanly

🤖 Generated with Claude Code

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Medium risk because it changes launchd-managed process lifecycle (EXIT trap/kill behavior) and the reviewed-PR idempotency persistence path/write semantics, which could affect restarts or review retries if incorrect.
> 
> **Overview**
> Ensures the `agento-manager` launchd script reliably tears down the background backfill loop by capturing `BACKFILL_PID` and adding an `EXIT` `trap` to kill its child processes when the foreground notifier exits/crashes, enabling clean `KeepAlive` restarts.
> 
> Refactors `auto_review_trigger` reviewed-PR tracking to use shared `ensure_state_dir` and `atomic_json_write_single`, broadens load error handling to `OSError`, and makes `_save_reviewed_prs` return success so `mark_as_reviewed` logs/handles persistence failures instead of assuming writes succeed.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 40b90fa9bd0468c08ae0b10b6b0267af83781840. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->
## Summary by CodeRabbit

* **Bug Fix
