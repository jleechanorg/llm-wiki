# PR #340: [agento] feat: coordinate on pre-existing CI failures + MCP mail before stop

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-03-21
**Author:** jleechan2015
**Stats:** +13/-0 in 1 files

## Summary
ao-288 was working on PR #70 in agent-orchestrator. It correctly identified that CI failures were pre-existing (failing on main), but silently stopped — no MCP mail, no coordination, no fix attempt.

## Background
ao-288 was working on PR #70 in agent-orchestrator. It correctly identified that CI failures were pre-existing (failing on main), but silently stopped — no MCP mail, no coordination, no fix attempt.

## Raw Body
## Background
ao-288 was working on PR #70 in agent-orchestrator. It correctly identified that CI failures were pre-existing (failing on main), but silently stopped — no MCP mail, no coordination, no fix attempt.

## Goals
- Agents must send MCP mail when they find pre-existing CI failures
- Agents must attempt to fix or escalate pre-existing failures, not ignore them
- Agents must send MCP mail before ending any session

## Tenets
- Coordination over silence
- Fix or escalate, never just observe
- Minimal scope — only changes `agent-orchestrator` project agentRules

## High-level description of changes
Two new rule blocks in `agent-orchestrator` project agentRules:
1. **PRE-EXISTING CI FAILURES**: verify on main → MCP mail → fix PR or escalate → never stop silently
2. **MCP MAIL BEFORE STOPPING**: mandatory `send_message` before ending any session

## Testing
Next ao session on agent-orchestrator that hits pre-existing CI failures should send MCP mail and open a fix PR.

## Low-level details
- **agent-orchestrator.yaml**: +13 lines to `agent-orchestrator` project agentRules

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Low risk: documentation/config-only changes that affect agent operational behavior, not runtime code paths. Main risk is unintended workflow friction or noisy notifications if the new rules are too strict.
> 
> **Overview**
> Adds two new rule blocks to `agent-orchestrator.yaml` for the `agent-orchestrator` project: **(1)** when CI failures are determined to be pre-existing on `main`, agents must verify on `main`, notify via MCP mail, and either open a fix PR or escalate (no silent stopping); **(2)** agents must send a final MCP mail “Session complete” message *before* ending any session.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 38c7b1369fb3dc7600957c14e36bebc92e5768d0. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR
