# PR #261: feat(autonomy): wire Pass 4 auto-merge to unified 7-condition gate

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-03-17
**Author:** jleechan2015
**Stats:** +22/-58 in 1 files

## Summary
- Replace 58 lines of inline shell condition checks in `ao-backfill.sh` Pass 4 with a single call to the unified Python `check_merge_ready()` gate
- All 7 merge conditions now enforced in one place (`src/orchestration/merge_gate.py`), already tested with 28 passing tests via PR #255

## Test Plan
- [x] 28 unit/integration tests pass (`python -m pytest src/tests/test_merge_gate.py`)
- [ ] Run gate live against a real PR to verify end-to-end
- [ ] Verify ao-backfill skips PRs that fail any of the 7 conditions

🤖 Generated with [Claude Code](https://claude.com/claude-code)

Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Changes the auto-merge decision logic for live PRs by replacing multiple inline `gh` checks with a singl

## Raw Body
## Summary
- Replace 58 lines of inline shell condition checks in `ao-backfill.sh` Pass 4 with a single call to the unified Python `check_merge_ready()` gate
- All 7 merge conditions now enforced in one place (`src/orchestration/merge_gate.py`), already tested with 28 passing tests via PR #255

## Changes
- `scripts/ao-backfill.sh` Pass 4: removed inline 4-condition check, replaced with `PYTHONPATH=src python3 -c "from orchestration.merge_gate import check_merge_ready; ..."` 
- Gate returns `can_merge` boolean + `blocked_reasons` list — backfill logs the reasons and skips blocked PRs

## Beads
- orch-q6ni.1: Wire check_merge_ready() into ao-backfill Pass 4
- orch-q6ni.2: Agent resolves threads after fixing
- orch-q6ni.3: Agent runs /er before declaring done

## Test plan
- [x] 28 unit/integration tests pass (`python -m pytest src/tests/test_merge_gate.py`)
- [ ] Run gate live against a real PR to verify end-to-end
- [ ] Verify ao-backfill skips PRs that fail any of the 7 conditions

🤖 Generated with [Claude Code](https://claude.com/claude-code)

Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Changes the auto-merge decision logic for live PRs by replacing multiple inline `gh` checks with a single Python gate; misconfiguration or gate bugs could block or allow merges unexpectedly.
> 
> **Overview**
> **Pass 4 auto-merge in `scripts/ao-backfill.sh` now delegates merge-readiness to the unified Python gate** by calling `orchestration.merge_gate.check_merge_ready()` and parsing its JSON output.
> 
> This removes the previous inline shell checks (CI/mergeable/thread/CodeRabbit + extra hard gates) and instead **enforces all 7 merge conditions via the shared gate**, logging aggregated `blocked_reasons` when a PR is not mergeable before proceeding with `gh pr merge --squash --auto`.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit e0f28750ae7ddac3461d976fae3aa022f8d22e
