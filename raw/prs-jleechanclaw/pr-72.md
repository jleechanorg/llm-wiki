# PR #72: docs: Replace Me design — autonomous agent memory & identity

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-03-10
**Author:** jleechan2015
**Stats:** +3476/-82 in 35 files

## Summary
- Augment OpenClaw memory: `memoryFlush` now writes to both MEMORY.md (existing) AND a structured knowledge graph (`memory-server` MCP)
- Add 7 `memory-server_*` tools to `alsoAllow` enabling OpenClaw to write/query the graph at compaction
- 12-question memory quality test suite — 12/12 passing
- Phased memory upgrade roadmap (daily cron → knowledge graph → Zoe context injection)

## Raw Body
## Summary
- Augment OpenClaw memory: `memoryFlush` now writes to both MEMORY.md (existing) AND a structured knowledge graph (`memory-server` MCP)
- Add 7 `memory-server_*` tools to `alsoAllow` enabling OpenClaw to write/query the graph at compaction
- 12-question memory quality test suite — 12/12 passing
- Phased memory upgrade roadmap (daily cron → knowledge graph → Zoe context injection)

## Changes

### openclaw-config/openclaw.json
- `memoryFlush.prompt`: augmented from single MEMORY.md append → 3-step: session notes + MEMORY.md append + knowledge graph write
- `alsoAllow`: added `memory-server_create_entities`, `memory-server_add_observations`, `memory-server_search_nodes`, `memory-server_open_nodes`, `memory-server_read_graph`, `memory-server_create_relations`, `memory-server_delete_observations`

### roadmap/MEMORY_UPGRADE_PLAN.md (new)
Phased plan for memory → Zoe-style context injection:
- Phase 1 (ORCH-s0v.2): daily launchd job for `build_memory.py` + Slack/email failure alerts
- Phase 2 (ORCH-s0v.1): knowledge graph as structured layer (memory-server already running — no Mem0 needed)
- Phase 3 (ORCH-fc9.1): dispatch skill update to call `memory-server_search_nodes` before spawning agent

### testing_llm/ (new)
- `MEMORY_QUALITY_TEST.md`: 12 tough questions spanning decision history, technical specifics, cross-repo patterns, judgment, graceful failures
- `artifacts/`: all 12 answer JSONs from OpenClaw (12/12 pass after adding Critical Incidents to workspace/MEMORY.md)

### scripts/extract_patterns.py (deleted)
Replaced by `scripts/build_memory.py` — which does collect + LLM synthesis (haiku) in one pipeline vs the old no-synthesis raw dump.

### scripts/setup-genesis-ghost.sh, validate-genesis-memory-l0.sh (new)
Supporting scripts for genesis memory validation.

## Testing
- Memory quality: asked OpenClaw 12 hard questions via Slack (#ai-slack-test), 12/12 correct answers
- Knowledge graph: seeded with 6 entities + 7 relations, verified via `mcp__memory-s
