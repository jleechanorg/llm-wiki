# PR #493: [agento] [P2] feat(monitor): wire monitor-agent into staging→prod deploy gate

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-04-04
**Author:** jleechan2015
**Stats:** +19/-3 in 2 files

## Summary
Jeffrey requested that the staging→prod deploy gate run monitor-agent before pushing to prod, block on failure, and alert both Slack (#all-jleechan-ai C09GRLXF9GR) and email.

## Background
Jeffrey requested that the staging→prod deploy gate run monitor-agent before pushing to prod, block on failure, and alert both Slack (#all-jleechan-ai C09GRLXF9GR) and email.

## Raw Body
## Background
Jeffrey requested that the staging→prod deploy gate run monitor-agent before pushing to prod, block on failure, and alert both Slack (#all-jleechan-ai C09GRLXF9GR) and email.

## Goals
1. Run monitor-agent BEFORE pushing to prod — BLOCK push if monitor fails
2. Alert Slack (#all-jleechan-ai) AND email with failure details + recommendations
3. Hourly monitor execution via launchd (already exists)
4. Part of bootstrap.sh install (already wired via install-openclaw-launchd.sh)

## Tenets
- Prefer existing infrastructure over new files
- Email is secondary to Slack; failures must not exit on email errors
- Explicit channel routing over implicit defaults

## High-level description of changes
Most of the required infrastructure already existed (deploy.sh gate, launchd plist, bootstrap.sh install). This PR makes two targeted changes:

1. **monitor-agent.sh**: Changed FAILURE_SLACK_TARGET default from C0AKYEY48GM → C09GRLXF9GR (#all-jleechan-ai). Added email alerting via send-alert-email.sh in the STATUS=PROBLEM block — fires BEFORE Slack delivery so email always runs as backup.

2. **scripts/deploy.sh**: Updated die() hardcoded Slack target from C0AKYEY48GM → C09GRLXF9GR for consistency.

## Testing
- bash -n monitor-agent.sh — syntax OK
- bash -n scripts/deploy.sh — syntax OK
- PYTHONPATH=src python -m pytest tests/test_openclaw_configs.py -q — 25 passed, 71 skipped

## Verification
Existing deploy-gate infrastructure (added in prior commits):
- deploy.sh Stage 1 already calls monitor-agent against staging, blocks on failure
- deploy.sh Stage 4 already calls monitor-agent against production, blocks on failure
- die() already sends both Slack and email alerts with remediation recommendations
- ai.openclaw.monitor-agent.plist installed by bootstrap.sh

This PR updates the Slack channel target from C0AKYEY48GM → C09GRLXF9GR across monitor-agent.sh and deploy.sh, and adds email as a backup delivery path that fires even if Slack delivery fails.

Closes #6uf

<!--
