# PR #16: fix: task poller dispatch + mc_client boards path

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-03-03
**Author:** jleechan2015
**Stats:** +534/-73 in 7 files

## Summary
- Fix `update_task` to use `/api/v1/boards/{board_id}/tasks/{task_id}` (old `/api/v1/tasks/{id}` returns 404)
- Auto-wire `build_claudem_dispatch()` in `TaskPoller.__post_init__` when `MINIMAX_API_KEY` is set (prevents `ai_orch` receiving invalid `"claudem"` CLI)
- Fix `start-mc.sh` to read `LOCAL_AUTH_TOKEN` from `.env` instead of placeholder

## Raw Body
## Summary
- Fix `update_task` to use `/api/v1/boards/{board_id}/tasks/{task_id}` (old `/api/v1/tasks/{id}` returns 404)
- Auto-wire `build_claudem_dispatch()` in `TaskPoller.__post_init__` when `MINIMAX_API_KEY` is set (prevents `ai_orch` receiving invalid `"claudem"` CLI)
- Fix `start-mc.sh` to read `LOCAL_AUTH_TOKEN` from `.env` instead of placeholder

## Changes
- `mc_client.py`: `update_task` gains optional `board_id` param; routes to board-scoped path when provided
- `task_poller.py`: `_dispatch_task` passes `board_id=self.board_id` to all `update_task` calls; `__post_init__` auto-wires claudem dispatch
- `test_mc_client.py`: `TestUpdateTaskEndpointPath` class (2 tests); removed dangling assertion
- `test_task_poller.py`: `TestMinimaxDispatchWiring` class (3 tests); updated `update_task` assertions to include `board_id`
- `scripts/start-mc.sh`: token read from `.env`

## Testing
- 52/52 tests pass (`test_mc_client.py` + `test_task_poller.py`)
- Live e2e: task `ac80fb0a` updated to `in_progress` via `PATCH /api/v1/boards/{board_id}/tasks/{id}` against running MC on port 9010

## Known Limitations
- `update_task` retains fallback to `/api/v1/tasks/{id}` when `board_id=None` for backwards compatibility

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Changes task dispatch behavior (now concurrent and uses direct CLI subprocesses instead of `ai_orch`) and updates Mission Control API paths/payloads; this can affect task execution and status updates if endpoints/CLIs differ across environments.
> 
> **Overview**
> **Mission Control integration is updated to use board-scoped task APIs.** `mc_client` now prefers `/api/v1/boards/{board_id}/tasks` for create/update/dependency operations, normalizes legacy `custom_fields` into `custom_field_values`, adds `TaskStatus.REVIEW`, and retries `update_task` without custom fields on `422`.
> 
> **Task dispatch is refactored to be more reliable and faster.
