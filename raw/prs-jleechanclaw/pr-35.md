# PR #35: Batch B: deterministic PR lifecycle automation state machine

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-03-05
**Author:** jleechan2015
**Stats:** +585/-0 in 5 files

## Summary
- Implement deterministic PR lifecycle evaluator for Mission Control/OpenClaw integration (Batch B MVP).
- Add GraphQL `reviewThreads` unresolved-thread extraction path (no REST comment-count heuristic).
- Add CodeRabbit request/evaluation loop behavior with retry, rate-limit quorum policy, and fallback evidence payload.
- Add config-driven retry/backoff/timeout and task correlation metadata (`task_id`, `repo`, `pr_number`).

## Raw Body
## Summary
- Implement deterministic PR lifecycle evaluator for Mission Control/OpenClaw integration (Batch B MVP).
- Add GraphQL `reviewThreads` unresolved-thread extraction path (no REST comment-count heuristic).
- Add CodeRabbit request/evaluation loop behavior with retry, rate-limit quorum policy, and fallback evidence payload.
- Add config-driven retry/backoff/timeout and task correlation metadata (`task_id`, `repo`, `pr_number`).

## Policy decisions implemented
- CodeRabbit rate-limit fallback quorum: **2 human approvals required**.
- Trigger mode: **webhook preferred**, automatically falls back to polling when webhook is unavailable.
- Fallback evidence schema: generalized payload with `schema_version`, `kind`, `summary`, `correlation`, `checks`, `review`, `runtime`, and `artifacts`.

## Files touched
- `src/orchestration/lifecycle_reactions.py`
- `src/tests/test_lifecycle_reactions.py`
- `docs/mcp-mail-openclaw-mission-control-design.md`

## Testing
- `PYTHONPATH=src pytest -q src/tests/test_lifecycle_reactions.py`
- Result: `71 passed`

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Introduces new background threads and periodic subprocess/network calls (OpenClaw CLI + Mission Control PUTs) in the backend service path, which can affect runtime stability and operational behavior if misconfigured or failing.
> 
> **Overview**
> Adds a new supervised runtime (`mc_backend_service.py`) that can optionally start the inbox `TaskPoller` alongside the Mission Control API server, controlled via env flags and interval settings, plus unit tests covering env parsing and heartbeat/poller wiring.
> 
> Introduces an agent heartbeat sync loop (`heartbeat_bridge.py`) that shells out to `openclaw agents list --json` and upserts agent metadata into Mission Control, backed by a new env-configured `MissionControlClient.upsert_agent`. Also adds a `TDD_EXECUTION_ROADMAP.md` documenting a Red→Green→Refactor plan for follow-up workflow work.
> 
> <sup>Written by [Cursor
