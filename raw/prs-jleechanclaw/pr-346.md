# PR #346: fix: cleanup_zombie_worktree — correct git porcelain parsing, add tests

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-03-21
**Author:** jleechan2015
**Stats:** +823/-0 in 2 files

## Summary
- **Bug fix**: `cleanup_zombie_worktree` in `ao-pr-poller.sh` silently failed to remove stale worktrees because the grep key was wrong (`^path $wt_path` vs git's actual `worktree /path` porcelain format) and it passed metadata dir paths to `git worktree remove` instead of actual checkout paths.
- **Test added**: `ao-pr-poller-worktree-cleanup-test.sh` — 5 unit tests covering the awk path extraction, main-worktree safety guard, empty-not-found case, old buggy grep regression, and tmux kill loop.

## Raw Body
## Summary
- **Bug fix**: `cleanup_zombie_worktree` in `ao-pr-poller.sh` silently failed to remove stale worktrees because the grep key was wrong (`^path $wt_path` vs git's actual `worktree /path` porcelain format) and it passed metadata dir paths to `git worktree remove` instead of actual checkout paths.
- **Test added**: `ao-pr-poller-worktree-cleanup-test.sh` — 5 unit tests covering the awk path extraction, main-worktree safety guard, empty-not-found case, old buggy grep regression, and tmux kill loop.

## Changes
- **scripts/ao-pr-poller.sh**: Replace `.git/worktrees/*` metadata-dir iteration + broken grep with `git worktree list --porcelain | awk` that correctly parses `worktree /path` porcelain blocks, extracts the actual checkout path, and passes it to `git worktree remove`.
- **scripts/ao-pr-poller-worktree-cleanup-test.sh** (new): Unit tests for the worktree cleanup logic.

## Testing
- `bash scripts/ao-pr-poller-worktree-cleanup-test.sh` → 5/5 PASS
- `bash -n scripts/ao-pr-poller.sh` → Syntax OK

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Touches automated `git worktree remove`/tmux cleanup logic; a parsing mistake could delete the wrong checkout, though the change adds explicit main-worktree skipping and exact session matching to reduce that risk.
> 
> **Overview**
> Fixes `cleanup_zombie_worktree` in `scripts/ao-pr-poller.sh` to correctly locate and remove stale git worktrees by parsing `git worktree list --porcelain` (`worktree /path` blocks) and passing the *actual checkout path* to `git worktree remove`, instead of iterating `.git/worktrees/*` metadata dirs with a non-matching `^path` grep.
> 
> Hardens zombie tmux cleanup by matching sessions via exact branch suffix comparison (avoids regex injection/prefix collisions) and adds a new `scripts/ao-pr-poller-worktree-cleanup-test.sh` bash test script covering the porcelain parsing, main-worktree safety guard, not-found behav
