# PR #380: [P1] fix: schedule plist install path + monitor summary verbosity

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-03-24
**Author:** jleechan2015
**Stats:** +126/-27 in 3 files

## Summary
doctor.sh fails because `ai.openclaw.schedule.harness-analyzer-9am` plist is not installed. Root cause: `install-openclaw-scheduled-jobs.sh` searches for plists in `openclaw-config/` (wrong directory) instead of `launchd/` (where they actually live). Also, the monitor Slack summary shows all passing checks which is noisy.

## Background
doctor.sh fails because `ai.openclaw.schedule.harness-analyzer-9am` plist is not installed. Root cause: `install-openclaw-scheduled-jobs.sh` searches for plists in `openclaw-config/` (wrong directory) instead of `launchd/` (where they actually live). Also, the monitor Slack summary shows all passing checks which is noisy.

## Raw Body
## Background
doctor.sh fails because `ai.openclaw.schedule.harness-analyzer-9am` plist is not installed. Root cause: `install-openclaw-scheduled-jobs.sh` searches for plists in `openclaw-config/` (wrong directory) instead of `launchd/` (where they actually live). Also, the monitor Slack summary shows all passing checks which is noisy.

## Goals
- Fix schedule plists to be installed and registered on macOS
- Reduce monitor Slack noise by hiding passing checks

## Changes

### fix/schedule-plist-install: install-openclaw-scheduled-jobs.sh
- Lines 168 and 198: change `$CONFIG_DIR/ai.openclaw.schedule.*.plist` → `$REPO_ROOT/launchd/ai.openclaw.schedule.*.plist`
- `$CONFIG_DIR` = `openclaw-config/` (empty/no plists) — plists are in `launchd/`

### fix/monitor-summary: monitor-agent.sh
- Remove GREEN_ROWS section from Slack report (passing checks never shown)
- Append probe summaries to RED/YELLOW row details for better diagnostics:
  - Gateway health → `HTTP_GATEWAY_SUMMARY`
  - Slack read → `PROBE_REQUEST_SUMMARY`
  - Slack send → `GATEWAY_PROBE_SUMMARY`
  - Thread replies → `THREAD_REPLY_SUMMARY`

## Testing
- `bash -n` passes for both scripts
- Full test suite: 401 passed, 4 pre-existing failures unchanged
- `ls launchd/ai.openclaw.schedule.*.plist` finds all 3 schedule plists

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Low risk: adds new `launchd` plist templates and adjusts Slack report formatting/verbosity without changing core probe logic.
> 
> **Overview**
> Adds new `launchd` plist templates for scheduled jobs (including `harness-analyzer` weekdays at 9am PT and a weekly `orch-health` runner) with explicit paths, logging, and environment setup.
> 
> Updates `monitor-agent.sh` Slack output to **omit passing checks entirely** (removing the green table) and to include brief probe summaries inline for failing rows (gateway health, Slack read/send, thread replies) to make alerts less noisy and more actionable.
> 
> <sup>Written by [Cursor Bugbot](https:
