# PR #434: [agento] fix(monitor): standardize PR report output format

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-03-29
**Author:** jleechan2015
**Stats:** +55/-12 in 2 files

## Summary
- Standardize `scripts/ao6green-pr-monitor.sh` output to `PR #<n> — age: <Xh Ym> — status: concerning|ok`
- Add `age` field showing hours+minutes (Xh Ym)
- Map trajectory to standard status: on-track → ok, at-risk/off-track → concerning
- Add CLAUDE.md rule enforcing this format for all automation and sessions

## Raw Body
## Summary

- Standardize `scripts/ao6green-pr-monitor.sh` output to `PR #<n> — age: <Xh Ym> — status: concerning|ok`
- Add `age` field showing hours+minutes (Xh Ym)
- Map trajectory to standard status: on-track → ok, at-risk/off-track → concerning
- Add CLAUDE.md rule enforcing this format for all automation and sessions

## Changes

- `scripts/ao6green-pr-monitor.sh`: update echo format + compute_age_hm() helper
- `CLAUDE.md`: add PR Status Report Format section

## Testing

- Script reviewed: `bash -n` passes; output format verified against prior format
- No functional logic changes — only output formatting

## Merge after

PR #433 (harness engineering) should be merged first.

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Low risk: changes are limited to monitoring script output formatting and age display logic, with no effect on the underlying 7-check evaluation beyond how results are reported.
> 
> **Overview**
> Standardizes `scripts/ao6green-pr-monitor.sh` to emit a single, consistent PR status line: `PR #<n> — age: <Xh Ym> — status: concerning|ok`, replacing the prior multi-field table-style output.
> 
> Updates age computation to include hours+minutes (`compute_age_hm`) and derives `ok` vs `concerning` from trajectory (on-track when `green_count >= 6` and PR age `<= 3h`). Adds a `CLAUDE.md` rule requiring all automation and agent sessions to use this exact reporting format.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit a51af83ba47f481af41bac7d5b50254f93b2f161. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
