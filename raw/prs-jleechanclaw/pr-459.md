# PR #459: [agento] [P0] feat(orch-1ps.2): add staging-promote.sh — auto-promote staging→prod

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-03-31
**Author:** jleechan2015
**Stats:** +160/-0 in 1 files

## Summary
- Adds `scripts/staging-promote.sh`: the Stage 2 gate in the 3-stage openclaw dev pipeline (orch-1ps epic).
- Runs `staging-canary.sh --port 18790` (6-point health check against staging gateway).
- If all 6 checks pass: merges `staging → origin/main` in the staging worktree, then `git pull --ff-only` in `~/.openclaw/` (prod).
- If any check fails: exits without promoting.

## Raw Body
## Summary
- Adds `scripts/staging-promote.sh`: the Stage 2 gate in the 3-stage openclaw dev pipeline (orch-1ps epic).
- Runs `staging-canary.sh --port 18790` (6-point health check against staging gateway).
- If all 6 checks pass: merges `staging → origin/main` in the staging worktree, then `git pull --ff-only` in `~/.openclaw/` (prod).
- If any check fails: exits without promoting.

## Changes
- **scripts/staging-promote.sh** (new, 172 lines): fail-closed promotion script with:
  - Worktree guard: checks `~/.openclaw-staging/` is a valid git worktree (not a plain directory) before doing anything.
  - Branch guard: verifies the worktree is on the `staging` branch.
  - Canary gate: calls `staging-canary.sh --port 18790`; exits if canary fails.
  - Merge: `git merge --no-ff origin/main` in the staging worktree, then `git push origin staging`.
  - Prod sync: `git pull --ff-only origin main` in `~/.openclaw/`.
  - Idempotent: safe to re-run; re-verifies canary on each run.
  - Caller documentation in header (cron job via `staging-canary-cron.sh` — orch-1ps.3, or manual post-merge).

## Testing
- Shellcheck: clean (0 issues).
- Canary integration tests: 3/3 pass (`tests/test_canary_integration.py`).
- Manual: script structure verified against `staging-canary.sh` exit codes and output format.

## Automation completeness
This script is **called by** `scripts/staging-canary-cron.sh` (orch-1ps.3 — cron-triggered after staging PR merges). The cron trigger is implemented separately in orch-1ps.3, per design.

Closes #orch-1ps.2.

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Automates merging and pushing to `main`, so misconfiguration (wrong worktree/branch/remote state) could promote unintended code despite the added guards. Changes are isolated to a new script and are fail-closed on canary failure.
> 
> **Overview**
> Adds `scripts/staging-promote.sh`, an automated *fail-closed* promotion step that runs the existing staging canary (`staging-canary.sh --port 1879
