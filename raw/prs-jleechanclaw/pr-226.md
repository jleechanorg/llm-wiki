# PR #226: [P1] fix(action-executor): remove premature/post-merge thread resolve calls, fix inline imports

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-03-16
**Author:** jleechan2015
**Stats:** +59/-26 in 3 files
**Labels:** P1

## Summary
(none)

## Raw Body
Remove premature thread resolve calls and fix inline import issues in action-executor.

---
**Priority: P1 — Breaks green loop**

Premature resolve calls mark threads as resolved before the fix is verified, masking real issues. Post-merge calls can fail loudly and cause the action-executor to error out. Inline imports are a code quality issue that fails CI linting.

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Changes affect auto-merge gating and CI evaluation logic; misconfiguration or `gh` failures could now block merges or alter merge readiness behavior.
> 
> **Overview**
> Tightens merge readiness checks to be **fail-closed** when deterministic preconditions can’t be verified.
> 
> `scripts/ao-backfill.sh` now treats PRs with *no required CI checks* as failing the “CI green” gate, preventing auto-merge in that case.
> 
> `escalation_router.py` hardens auto-merge gating by blocking when the CodeRabbit check errors, and by ensuring CI is checked against the correct PR head branch (fetching it via `gh pr view` when missing and blocking the merge if it can’t be determined).
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 30d772e2a1eef8cb7babd9d2d2d9bf5624e1cd5e. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->

## Summary by CodeRabbit

* **Bug Fixes**
  * Fixed CI status evaluation to correctly fail when no required checks exist.

* **Chores**
  * Adjusted code formatting and spacing in scripts.

* **Refactor**
  * Enhanced merge validation pipeline with pre-merge checks for CI status, code review threads, and gate conditions. Merge operations now fail-safe and escalate to human review when deterministic checks fail.

<!-- end of auto-generated comment: release notes by coderabbit.ai -->
