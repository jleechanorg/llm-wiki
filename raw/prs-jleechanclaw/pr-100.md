# PR #100: fix: make supervisor multi-registry and set agento as default orchestrator lane

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-03-11
**Author:** jleechan2015
**Stats:** +385/-28 in 10 files

## Summary
- make supervisor reconcile multiple registry files in one loop (including sibling workspace roots sharing the same `.messages` backend)
- remove single-root/cwd coupling that caused mctrl runs to stay `in_progress` when they were dispatched from a different repo root
- update OpenClaw policy docs so orchestrator layers are explicit: `agento` default for change requests, `mctrl` opt-in lane

## Raw Body
## Summary
- make supervisor reconcile multiple registry files in one loop (including sibling workspace roots sharing the same `.messages` backend)
- remove single-root/cwd coupling that caused mctrl runs to stay `in_progress` when they were dispatched from a different repo root
- update OpenClaw policy docs so orchestrator layers are explicit: `agento` default for change requests, `mctrl` opt-in lane

## Changes
- `src/orchestration/supervisor.py`
  - added `MCTRL_REGISTRY_PATHS` support
  - added auto-discovery of sibling `.tracking/bead_session_registry.jsonl` files when using default path and shared `.messages`
  - reconciles + archives across all resolved registry paths each tick
- `tests/test_supervisor.py`
  - added coverage for env path parsing, sibling auto-discovery, and multi-registry `run_once()` reconciliation
- config/policy updates
  - `openclaw-config/TOOLS.md`
  - `openclaw-config/SOUL.md`
  - `openclaw-config/skills/agento/SKILL.md`
  - `openclaw-config/skills/dispatch-task/SKILL.md`
  - clarify orchestrator vs direct-agent layers, make `agento` default lane for code-change requests, keep mctrl for explicit/thread-lifecycle cases

## Testing
- `pytest -q tests/test_supervisor.py tests/test_reconciliation.py tests/test_openclaw_notifier.py`
- Result: `46 passed`

## Known Limitations
- auto-discovery currently scans sibling directories under the current working directory parent; cross-tree registries still require `MCTRL_REGISTRY_PATHS`

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Changes supervisor reconciliation and completion notification pathways, which can affect task state transitions and Slack/OpenClaw delivery behavior. Risk is mitigated by added unit coverage but still touches operational control-plane code.
> 
> **Overview**
> **Supervisor now supports multi-registry reconciliation.** `orchestration/supervisor.py` can reconcile/arch ive across multiple registry files via `MCTRL_REGISTRY_PATHS`, and can auto-discover sibling 
