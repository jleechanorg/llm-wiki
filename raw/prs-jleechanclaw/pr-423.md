# PR #423: [agento] feat(ao-runner): self-hosted GitHub Actions runner PyPI package

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-03-28
**Author:** jleechan2015
**Stats:** +2633/-32 in 22 files

## Summary
Extracts the Docker-based self-hosted runner setup from worldarchitect.ai into a reusable `ao-runner` PyPI package installable across all jleechanorg repos.

- `ao-runner install --repo owner/repo` — one-command setup with launchd auto-start
- `ao-runner status/start/stop/uninstall` — lifecycle management
- Config at `~/.config/ao-runner/`, logs at `~/Library/Logs/ao-runner/`
- Ephemeral Docker containers via `myoung34/github-runner:ubuntu-noble`

## Test Plan
- `python -m pytest src/tests/test_ao_runner_*.py` — 19 unit + integration tests pass (config, launchd, runner, integration)
- Manual smoke test: `ao-runner install --repo jleechanorg/jleechanclaw` to verify containers register in GitHub Settings

## Raw Body
## Summary

Extracts the Docker-based self-hosted runner setup from worldarchitect.ai into a reusable `ao-runner` PyPI package installable across all jleechanorg repos.

- `ao-runner install --repo owner/repo` — one-command setup with launchd auto-start
- `ao-runner status/start/stop/uninstall` — lifecycle management
- Config at `~/.config/ao-runner/`, logs at `~/Library/Logs/ao-runner/`
- Ephemeral Docker containers via `myoung34/github-runner:ubuntu-noble`

## Test plan

- `python -m pytest src/tests/test_ao_runner_*.py` — 19 unit + integration tests pass (config, launchd, runner, integration)
- Manual smoke test: `ao-runner install --repo jleechanorg/jleechanclaw` to verify containers register in GitHub Settings

## Notes

- **worldai_claw workflow update**: separate PR — update `.github/workflows/test.yml` to add self-hosted runner pattern for main-push jobs
- **agent-orchestrator**: already has correct pattern in `ci.yml` — just needs `SELF_HOSTED_RUNNER_LABELS` repo variable set
- **jleechanclaw workflows**: `skeptic-gate` and `skeptic-cron` stay on `ubuntu-latest` — no change needed

Closes #ORCH-3WTW

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Adds a new executable packaging surface (Python CLI + launchd + Docker) and introduces automated GitHub/Slack API calls via a scheduled cost-monitor, which could affect developer machines if misconfigured. Workflow merge-gate logic also changes how Bugbot findings are filtered, impacting auto-merge behavior.
> 
> **Overview**
> Introduces `ao-runner`, a new pip-installable CLI that provisions and manages Docker-based GitHub Actions self-hosted runners, including per-repo YAML config, GitHub registration-token fetching, and a macOS `launchd` LaunchAgent that auto-starts runners on login.
> 
> Adds the supporting runner shell scripts (Docker container/volume lifecycle + pre-job hook), updates `pyproject.toml` to publish `ao-runner` as the project with a console entrypoint, and adds unit/integration tests 
