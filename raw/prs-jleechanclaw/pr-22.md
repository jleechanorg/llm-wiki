# PR #22: config: wire Genesis cron integration and document gateway dependency

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-03-03
**Author:** jleechan2015
**Stats:** +60/-7 in 3 files

## Summary
- add a dedicated Genesis weekly cron job in tracked OpenClaw cron config
- document exactly where cron integration lives in this repo
- document that runtime execution depends on OpenClaw gateway health

## Raw Body
## Summary
- add a dedicated Genesis weekly cron job in tracked OpenClaw cron config
- document exactly where cron integration lives in this repo
- document that runtime execution depends on OpenClaw gateway health

## Changes
- Updated `openclaw-config/cron/jobs.json`
  - Added `genesis-memory-curation-weekly`
  - Schedule: `0 22 * * 0` (`America/Los_Angeles`)
  - Payload: curate last 7 days of `memory/*.md` into `MEMORY.md`
- Updated `README.md`
  - Added "Cron integration location" section + quick verify commands
- Updated `docs/GENESIS_DESIGN.md`
  - Clarified source-of-truth config path and explicit gateway dependency

## Testing
- `jq . openclaw-config/cron/jobs.json` ✅
- Python JSON load check confirms `genesis-memory-curation-weekly` exists ✅

## Known limitations
- This PR wires tracked config + docs.
- It does not auto-deploy `openclaw-config/cron/jobs.json` into `~/.openclaw/cron/jobs.json`.

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Low risk: changes are limited to tracked cron configuration and documentation, with no application logic changes; main risk is unintended scheduled runs once the config is synced into `~/.openclaw/cron/jobs.json`.
> 
> **Overview**
> Adds a new tracked cron job, `genesis-memory-curation-weekly`, scheduled for Sundays at 10pm PT (`0 22 * * 0`) to prompt an `agentTurn` that curates the last 7 days of `memory/*.md` into `MEMORY.md`.
> 
> Updates docs (`README.md`, `docs/GENESIS_DESIGN.md`) to point to `openclaw-config/cron/jobs.json` as the repo source of truth, emphasize syncing to `~/.openclaw/cron/jobs.json`, and note that job execution depends on the OpenClaw gateway being up.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 5d0a566d32e081a00baff4401373d970941bc0c2. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai 
