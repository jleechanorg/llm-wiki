# PR #524: [P1] chore(beads): consolidate prefixes to orch-* and fix validation errors

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-04-06
**Author:** jleechan2015
**Stats:** +303/-302 in 4 files

## Summary
The beads tracker had a mix of `jleechan-*` and `orch-*` prefixes in `.beads/issues.jsonl`, causing `br list` to error out during prefix validation when `issue-prefix` was set to `jleechan`.

## Background
The beads tracker had a mix of `jleechan-*` and `orch-*` prefixes in `.beads/issues.jsonl`, causing `br list` to error out during prefix validation when `issue-prefix` was set to `jleechan`.

## Raw Body
## Background
The beads tracker had a mix of `jleechan-*` and `orch-*` prefixes in `.beads/issues.jsonl`, causing `br list` to error out during prefix validation when `issue-prefix` was set to `jleechan`.

## Goals
- Consolidate all beads IDs to use the `orch-*` prefix consistently.
- Fix schema validation errors in `issues.jsonl`.
- Unblock `br` CLI usage for all agents.

## Tenets
- Consistency: Use a single prefix for all issues.
- Validity: Ensure all JSONL entries pass `br` validation checks.

## High-level description
- Updated `.beads/config.yaml` to set `issue-prefix: orch`.
- Renamed all `jleechan-*` references to `orch-*` in `.beads/issues.jsonl`.
- Fixed `orch-9nt` where `closed_at` was before `created_at`.
- Updated `roadmap/README.md` to align with the new prefix consistency.

## Testing
- Verified `br list` works without errors.
- Verified `br show orch-f7l` (the issue being addressed) works and shows closed status.
- Verified `br sync --import-only --rebuild` passes.

## Low-level details
- `.beads/config.yaml`: changed `issue-prefix` to `orch`.
- `.beads/issues.jsonl`: global replace of `jleechan-` with `orch-`; manual fix for `orch-9nt` timestamp.
- `roadmap/README.md`: updated issue references and descriptive text.

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Low risk config-only change that updates the Beads issue ID prefix; impact is limited to tooling that reads `.beads/config.yaml`.
> 
> **Overview**
> Updates Beads tracker configuration by changing `.beads/config.yaml` to use the `orch` `issue-prefix` instead of `jleechan`, aligning newly created/validated issue IDs with the `orch-*` namespace.
> 
> <sup>Reviewed by [Cursor Bugbot](https://cursor.com/bugbot) for commit 8ce4ba40558d67cd269106857e12c2cc69f0adc3. Bugbot is set up for automated code reviews on this repo. Configure [here](https://www.cursor.com/dashboard/bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->


