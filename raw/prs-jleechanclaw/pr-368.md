# PR #368: [P0] fix: tighten green criteria #3 — require body_len > 0 for genuine CR APPROVED

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-03-23
**Author:** jleechan2015
**Stats:** +1/-1 in 1 files

## Summary
Workers checking condition 3 against the criteria list in `agent-orchestrator.yaml` skipped the `body_len > 0` check, since that logic only appeared in trigger messages (lines 369, 375), not in the criteria definition itself.

Evidence: PR #361 (jc-491) saw `state: APPROVED, body_len: 0` and workers declared green without running the genuine-detection jq command.

## Background
Workers checking condition 3 against the criteria list in `agent-orchestrator.yaml` skipped the `body_len > 0` check, since that logic only appeared in trigger messages (lines 369, 375), not in the criteria definition itself.

Evidence: PR #361 (jc-491) saw `state: APPROVED, body_len: 0` and workers declared green without running the genuine-detection jq command.

## Raw Body
## Background
Workers checking condition 3 against the criteria list in `agent-orchestrator.yaml` skipped the `body_len > 0` check, since that logic only appeared in trigger messages (lines 369, 375), not in the criteria definition itself.

Evidence: PR #361 (jc-491) saw `state: APPROVED, body_len: 0` and workers declared green without running the genuine-detection jq command.

## Goals
- Prevent workers from accepting empty-body APPROVED as genuine
- Require explicit `body_len > 0` verification before declaring green

## Tenets
- Minimal change — only the criteria text updated, no logic changes
- CLAUDE.md already had the body_len check; this aligns agent-orchestrator.yaml to match

## Changes
- `agent-orchestrator.yaml` line 43: Updated green criteria #3 to explicitly require `APPROVED state AND body length > 0`, with the verification jq command and explanation inline

## Testing
- No code changes — YAML config only
- Syntax validated via `gh pr create` and push

## Beads
orch-jp8u

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Low risk: documentation/config text-only change that tightens the review gating instruction, with no runtime logic changes.
> 
> **Overview**
> Updates the "green PR" checklist in `agent-orchestrator.yaml` to treat CodeRabbit approval as genuine only when the latest `APPROVED` review has a non-empty body, explicitly calling out empty-body approvals from thread resolution as invalid.
> 
> Adds an explicit `gh api ... | jq` verification command inline so workers consistently check `body_len > 0` before declaring a PR green.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 4ae41c2de33dd690004f56193e0c53f3bad27627. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
