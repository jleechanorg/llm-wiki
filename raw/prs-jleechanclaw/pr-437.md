# PR #437: [agento] orch-jq2: dropped-thread-followup launchd + Slack MCP nudge system

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-03-29
**Author:** jleechan2015
**Stats:** +412/-3 in 5 files

## Summary
- Adds `dropped-thread-followup.sh` (launchd every 4h): scans monitored Slack channels for cold threads where user asked agent to do work but no action was taken, nudges via Slack API with idempotency guard
- Adds launchd plist `ai.openclaw.schedule.dropped-thread-followup` (4h interval)
- Updates `HEARTBEAT.md`: adds 30-min periodic task section for dropped thread check cadence
- Updates `TOOLS.md`: adds Slack MCP reminder to prevent future "forgot Slack MCP" incidents
- Fixes `SOUL.md` §5 memo

## Raw Body
## Summary

- Adds `dropped-thread-followup.sh` (launchd every 4h): scans monitored Slack channels for cold threads where user asked agent to do work but no action was taken, nudges via Slack API with idempotency guard
- Adds launchd plist `ai.openclaw.schedule.dropped-thread-followup` (4h interval)
- Updates `HEARTBEAT.md`: adds 30-min periodic task section for dropped thread check cadence
- Updates `TOOLS.md`: adds Slack MCP reminder to prevent future "forgot Slack MCP" incidents
- Fixes `SOUL.md` §5 memories-used trigger: now fires on both explicit `memory_search` results AND pre-loaded `<relevant-memories>` consumed silently

## Testing

- Script tested in DRY_RUN mode: clean output, correctly skips threads with recent agent replies
- Script tested in real mode: posts nudge replies via Slack API, idempotency state file written
- 6 dropped-thread followups posted immediately to active Slack threads via Slack MCP
- openclaw.json plugin installs audited — **no duplicate openclaw-mem0 entries found** in live `~/.openclaw/openclaw.json` or `openclaw.json.redacted`

## Files

- `scripts/dropped-thread-followup.sh` — new thread audit script
- `launchd/ai.openclaw.schedule.dropped-thread-followup.plist` — 4h launchd job
- `workspace/HEARTBEAT.md` — 30-min periodic task section added
- `workspace/TOOLS.md` — Slack MCP reminder added
- `workspace/SOUL.md` — memories-used trigger now covers pre-loaded memories

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Introduces an automated scheduled process that reads and posts to Slack using stored tokens; misclassification or configuration errors could spam channels or leak operational details via logs.
> 
> **Overview**
> Adds a new scheduled “dropped thread” follow-up system: a `launchd` plist runs `scripts/dropped-thread-followup.sh` every 4 hours with Slack token env injection and log paths.
> 
> The script scans recent threads in configured Slack cha
