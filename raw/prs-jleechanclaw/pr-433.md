# PR #433: [agento] fix: harness engineering improvements (2026-03-28)

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-03-29
**Author:** jleechan2015
**Stats:** +164/-66 in 9 files

## Summary
Harness engineering improvements: fixes for CRITICAL and Major CodeRabbit review comments from the previous PR session, plus alignment with canonical 7-green merge criteria.

## Raw Body
## Summary
Harness engineering improvements: fixes for CRITICAL and Major CodeRabbit review comments from the previous PR session, plus alignment with canonical 7-green merge criteria.

## Changes

### scripts/harness-analyzer.sh
- Make `gh auth token` fallback opt-in via `HARNESS_ANALYZER_ALLOW_GH_CLI_FALLBACK=1`
- Add `-E` flag to final grep so alternation patterns work correctly
- Remove `-r` from second grep (was bypassing exclusion filters)
- Fix merge conflict marker

### scripts/install-launchagents.sh
- `install_plist` now propagates launchctl bootstrap failure via exit code
- Gateway teardown reordered: new plist verified installable BEFORE legacy torn down
- `OPENCLAW_BIN` validation added with `|| true` on resolver call
- `_openclaw_resolve` extended with nvm/pnpm/bun/Homebrew paths
- Redundant `OPENCLAW_BIN` null-check removed

### scripts/install-ao-manager.sh
- Log hint now shows both launchd stdout/stderr path and ao-manager internal path

### health-check.sh
- `install_gateway` regenerates plist via `install-launchagents.sh` when missing

### tests/test_openclaw_configs.py
- `isinstance(token, str)` guard added to Slack token assertions

### .github/workflows/skeptic-cron.yml
- Enumerate all non-draft PRs with `same_repo` flag (cross-repo PRs evaluated, not skipped)

### CLAUDE.md / workspace/AGENTS.md / workspace/SOUL.md
- Dedup routing policy, align PR green criteria with canonical 7-green definition
- Daemon name corrected to `ai.mctrl.supervisor`

## Testing
- Unit tests pass (pytest src/tests/ — 331 passed)
- CI: Skeptic Gate success, Bugbot neutral, mergeable clean
- Smoke test: plist template sed substitution produces valid XML

## CodeRabbit Review Coverage
All 13 inline CR comments addressed (10 fixed, 3 acknowledged).
Consolidated reply: https://github.com/jleechanorg/jleechanclaw/pull/433#issuecomment-4150243327

<!-- COPILOT_TRACKING_START -->
## Comment Tracking

| # | Reviewer | File | Severity | Status | Summary |
|---|----------|-----
