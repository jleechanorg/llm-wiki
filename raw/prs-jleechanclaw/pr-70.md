# PR #70: fix(mctrl): harden orchestration reliability and Slack evidence

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-03-09
**Author:** jleechan2015
**Stats:** +2509/-5957 in 66 files

## Summary
- harden mctrl dispatch, reconciliation, and notification semantics so tasks are only marked finished once the branch is reviewable on `origin`
- make Slack delivery evidence durable in the real MCP and loopback E2E tests
- archive stale session registry entries and normalize stored trigger timestamps
- document the reliability plan and require agents to push before stopping

## Raw Body
## Summary
- harden mctrl dispatch, reconciliation, and notification semantics so tasks are only marked finished once the branch is reviewable on `origin`
- make Slack delivery evidence durable in the real MCP and loopback E2E tests
- archive stale session registry entries and normalize stored trigger timestamps
- document the reliability plan and require agents to push before stopping

## Changes
- `src/orchestration/reconciliation.py`
  - require remote branch reachability before emitting `task_finished`
  - classify local-only commits as `task_needs_human` with explicit salvage action
- `src/orchestration/dispatch_task.py`
  - append push instructions to dispatched tasks
  - rename tmux sessions to bead-traceable unique names
- `src/orchestration/openclaw_notifier.py`
  - normalize missing trigger timestamps before persistence/posting
  - improve Slack completion text for local-only commit cases
- `src/orchestration/session_registry.py` and `src/orchestration/supervisor.py`
  - normalize stored `slack_trigger_ts`
  - archive stale terminal mappings whose worktrees are gone
- `openclaw-config/skills/dispatch-task/SKILL.md`
  - explain that work is not complete until pushed and reviewable on `origin`
- `roadmap/MCTRL_RELIABILITY_EXECUTION_PLAN.md`
  - capture the reliability tranche and execution order
- E2E and unit tests
  - persist DM/thread evidence artifacts for real Slack runs
  - harden polling windows and null-safety around Slack API responses
  - add coverage for reconciliation, notifier, registry archival, and worktree/session naming

## Testing
- `source ~/.profile && pytest tests/test_mvp_loopback_e2e.py tests/test_openclaw_notifier.py tests/test_reconciliation.py tests/test_session_registry.py tests/test_worktree_reuse.py -q`
- `source ~/.profile && pytest tests/test_mvp_loopback_e2e.py -q`
- `pytest testing_mcp/test_mcp_gateway_roundtrip.py -q` is currently being rerun after merging `origin/main`; it had passed earlier on the pre-merge reliability bra
