# PR #143: docs(roadmap): Phase 4 E2E unblocking — check_ci_status, _call_llm, webhook daemon

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-03-14
**Author:** jleechan2015
**Stats:** +482/-239 in 2 files

## Summary
Documents the three items that block black-box E2E testing of the orchestration layer after PR #134 merges. Adds per-item implementation spec with code sketches, test cases, and commit plans. Cuts beads orch-mner, orch-gwli, orch-u8gy.

## Raw Body
## Summary

Documents the three items that block black-box E2E testing of the orchestration layer after PR #134 merges. Adds per-item implementation spec with code sketches, test cases, and commit plans. Cuts beads orch-mner, orch-gwli, orch-u8gy.

## Changes

| Item | Bead | Before | After |
|------|------|--------|-------|
| `check_ci_status` | orch-mner | placeholder stub returns pending | Spec: poll GH Actions via `gh run list`, map to green/red/pending |
| `_call_llm` | orch-gwli | raises NotImplementedError | Spec: Anthropic SDK → claude-sonnet-4-6, fallback to `claude -p` subprocess |
| Webhook daemon | orch-u8gy | not mentioned | Spec: `webhook_daemon.py`, `launchd/ai.openclaw.webhook.plist`, gateway glue, smoke test |

Updated summary table with phase status (done / scaffolded / not started).

## Testing

Roadmap-only doc change — no executable code. Pre-existing test failures on `main` (5 tests in `test_escalation_e2e.py`) are fixed in PR #134, not here.

## Known Limitations

- Depends on PR #134 merging first
- `_call_llm` wiring requires ANTHROPIC_API_KEY in environment

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Low risk because this PR only updates roadmap/design documentation and does not change runtime code paths. Main risk is documentation drift or misleading implementation guidance.
> 
> **Overview**
> Updates the orchestration design/roadmap docs to define the **Phase 4 “E2E unblocking” work** needed after PR #134: implement real CI polling via `check_ci_status` (GH Actions via `gh run list`), wire autonomous PR review to Claude by implementing `pr_review_decision._call_llm`, and add a production `webhook_daemon` (entrypoint + launchd plist + gateway glue + smoke/integration tests).
> 
> Also refreshes the design doc to reflect the current webhook ingress/worker pipeline, deterministic vs LLM decision boundaries (including confidence-gated escalation), and adds an update
