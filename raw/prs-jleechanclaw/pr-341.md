# PR #341: [P1] fix: dispatch-task skill — call ao spawn directly, retire dispatch_task.py path

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-03-21
**Author:** jleechan2015
**Stats:** +36/-32 in 1 files

## Summary
dispatch_task.py was deleted from mctrl (ao_cli.py is now a tombstone: 'use ao CLI directly'). But the dispatch-task skill still told OpenClaw to run it. Additionally, OpenClaw was using sessions_spawn (internal nested LLM) and ai_orch (old Python runner) instead of ao (agento).

Two bugs this fixes:
1. Worker not auto-submitting — ai_orch pastes prompt but does not press Enter; ao send handles this
2. dispatch_task.py no longer exists — skill was pointing at a deleted module

## Background
dispatch_task.py was deleted from mctrl (ao_cli.py is now a tombstone: 'use ao CLI directly'). But the dispatch-task skill still told OpenClaw to run it. Additionally, OpenClaw was using sessions_spawn (internal nested LLM) and ai_orch (old Python runner) instead of ao (agento).

Two bugs this fixes:
1. Worker not auto-submitting — ai_orch pastes prompt but does not press Enter; ao send handles this
2. dispatch_task.py no longer exists — skill was pointing at a deleted module

## Raw Body
## Background
dispatch_task.py was deleted from mctrl (ao_cli.py is now a tombstone: 'use ao CLI directly'). But the dispatch-task skill still told OpenClaw to run it. Additionally, OpenClaw was using sessions_spawn (internal nested LLM) and ai_orch (old Python runner) instead of ao (agento).

Two bugs this fixes:
1. Worker not auto-submitting — ai_orch pastes prompt but does not press Enter; ao send handles this
2. dispatch_task.py no longer exists — skill was pointing at a deleted module

## Goals
- Skill instructs ao spawn + ao send directly (no Python middleware)
- Worker auto-submits immediately after spawn (no manual Enter)
- One canonical dispatch path

## High-level description of changes
dispatch-task/SKILL.md step 4: replaced dispatch_task.py invocation with ao spawn <bead-id> -p jleechanclaw + ao send <session> --file <taskfile>

## Low-level details
- **skills/dispatch-task/SKILL.md**: updated dispatch command and explanation (4 lines changed)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Low risk documentation-only change, but it alters the canonical operational runbook for dispatching agents; mistakes could cause mis-dispatch or missing Slack-thread linkage.
> 
> **Overview**
> Updates the `dispatch-task` skill to retire the old `mctrl`/`dispatch_task.py` invocation and instead dispatch work via `ao spawn` + `ao send` (file-based prompt) so tasks auto-submit.
> 
> Tightens the runbook around Slack-thread determinism by explicitly recording `SLACK_TRIGGER_TS`/`SLACK_TRIGGER_CHANNEL`, storing them in bead notes alongside the `ao` session name, and adds guidance on deriving the correct `ao` project ID and reporting `ao` command failures.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit daa98bfbb1814d995df35332effba4c5017d771e. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: rel
