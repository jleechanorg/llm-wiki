# PR #526: [P1] fix(ao7green): address actionable items from merged PRs #522 and #525

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-04-06
**Author:** jleechan2015
**Stats:** +295/-266 in 4 files

## Summary
PR #522 and #525 were merged, but several actionable items from CodeRabbit and Bugbot reviews remained unresolved. This PR addresses those items to ensure the `ao7green` monitor and launchd migration are robust.

## Background
PR #522 and #525 were merged, but several actionable items from CodeRabbit and Bugbot reviews remained unresolved. This PR addresses those items to ensure the `ao7green` monitor and launchd migration are robust.

## Raw Body
## Background
PR #522 and #525 were merged, but several actionable items from CodeRabbit and Bugbot reviews remained unresolved. This PR addresses those items to ensure the `ao7green` monitor and launchd migration are robust.

## Goals
- Address all outstanding review comments from #522 and #525.
- Ensure the `approved-and-green` reaction always runs strict checks.
- Improve logging and installation script robustness.

## Tenets
- Safety: Don't allow old 'PR is green' comments to bypass strict verification.
- Robustness: Use `mktemp` and check for dependencies (`jq`).
- Completeness: Align `ao7green-pr-monitor.sh` with the updated merge policy (allow CANCELLED).

## High-level description
- Updated `agent-orchestrator.yaml` to remove the short-circuit logic for 'PR is green' comments.
- Updated `ao7green-pr-monitor.sh` to allow `CANCELLED` conclusions and improved output capture.
- Refined installation scripts (`install-launchagents.sh`, `install-openclaw-scheduled-jobs.sh`) for better error handling and atomicity.

## Testing
- `plutil -lint` on plist: OK.
- `PYTHONPATH=src python -m pytest tests/test_openclaw_configs.py -q`: PASS.
- `PYTHONPATH=src python -m pytest tests/test_evidence.py -q`: PASS.

## Evidence
```
launchd/ai.openclaw.schedule.ao7green-jleechanclaw.plist: OK
25 passed, 74 skipped in 0.09s
24 passed in 0.02s
```

## Low-level details
- `agent-orchestrator.yaml`: Removed fast-path, renamed 'Mergeable' to 'Merge state', allowed CANCELLED.
- `scripts/ao7green-pr-monitor.sh`: Allowed CANCELLED, wrapped in tee, optimized API calls.
- `scripts/install-launchagents.sh`: Added `jq` warning, used `mktemp`.
- `scripts/install-openclaw-scheduled-jobs.sh`: Removed ao7green from `MIGRATED_JOBS`.

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Modifies PR auto-merge gating and the `ao7green` monitor/installer scripts; mistakes could cause premature merges or missed blockers, or leave duplicate scheduled jobs running.
> 
> **Overview**
> Strengthens t
