# PR #362: docs: sync audit output + fix FILE_MAP and document broken cron job

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-03-23
**Author:** jleechan2015
**Stats:** +392/-7 in 5 files

## Summary
- Synced fresh `SYSTEM_SNAPSHOT.md` and `DOC_GAPS.md` from 2026-03-22 audit run
- Fixed `FILE_MAP.md`: added missing `CLAUDE.md`, corrected `memory/` path (lives in `~/.claude/projects/.../memory/`, not repo root), annotated `USER.md`/`MEMORY.md` as symlinks to `workspace/`
- Added `LEARNINGS.md` entry documenting broken `healthcheck:weekly-error-trends` cron job (references non-existent `scripts/backup_cron_jobs.sh` and `docs/context/CRON_JOBS_BACKUP.*`)

## Raw Body
## Summary
- Synced fresh `SYSTEM_SNAPSHOT.md` and `DOC_GAPS.md` from 2026-03-22 audit run
- Fixed `FILE_MAP.md`: added missing `CLAUDE.md`, corrected `memory/` path (lives in `~/.claude/projects/.../memory/`, not repo root), annotated `USER.md`/`MEMORY.md` as symlinks to `workspace/`
- Added `LEARNINGS.md` entry documenting broken `healthcheck:weekly-error-trends` cron job (references non-existent `scripts/backup_cron_jobs.sh` and `docs/context/CRON_JOBS_BACKUP.*`)

## Testing
- Ran `scripts/docs_audit.sh` — no required gaps detected
- Verified all root files (`AGENTS.md`, `SOUL.md`, `TOOLS.md`, `HEARTBEAT.md`, `MEMORY.md`, `CLAUDE.md`, `scripts/docs_audit.sh`) exist
- Verified `openclaw health` and `openclaw --version` match SYSTEM_SNAPSHOT
- Confirmed `memory/` does not exist at repo root (corrected FILE_MAP entry)

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Low risk: changes are documentation and a small shell-script tweak to sanitize/normalize `openclaw` command output, with no production code paths affected.
> 
> **Overview**
> **Operational docs refresh and incident logging.** Updates `docs/context/FILE_MAP.md` to clarify `USER.md`/`MEMORY.md` symlinks, add `CLAUDE.md`, and point runtime memory to `~/.openclaw/memory/`.
> 
> Adds a new 2026-03-22 entry to `docs/context/LEARNINGS.md` documenting that `healthcheck:cron-backup-sync` references a missing `scripts/backup_cron_jobs.sh` and expected `CRON_JOBS_BACKUP.*` outputs.
> 
> Improves `scripts/docs_audit.sh` by forcing `NO_COLOR=1` and piping `openclaw` outputs through a `sanitize()` filter to strip ANSI codes and normalize `~/.openclaw` paths; also commits new HTML exports `docs/context/LEARNINGS.html` and `docs/context/SYSTEM_SNAPSHOT.html`.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 3c0fd70a10db9a428fa7f4cc3c5d5b8f24884252. This will update automatically on new commits. Configure [her
