# PR #231: feat: add GCP Cloud Run webhook receiver (Phase 1)

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-03-16
**Author:** jleechan2015
**Stats:** +555/-0 in 6 files

## Summary
(none)

## Raw Body
- FastAPI app with Slack signature verification
- URL verification challenge handler
- Forwards to local OpenClaw (port 18789)
- Dockerfile for Cloud Run deployment
- 15 unit tests

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Introduces a new internet-facing webhook service that handles Slack signature verification and forwards events into OpenClaw; mistakes in verification/forwarding or env configuration could drop or accept unintended traffic.
> 
> **Overview**
> Adds a new `webhook-receiver` FastAPI service intended for GCP Cloud Run to receive Slack Events (`POST /slack/events`), **verify Slack HMAC signatures with timestamp replay protection**, and forward payloads to OpenClaw (`OPENCLAW_URL/slack/events`) using an optional bearer token.
> 
> Includes `GET /health` and `/` endpoints, a generic unauthenticated forwarder (`POST /webhook/generic`), plus Cloud Run packaging via a new `Dockerfile`, dependency pin ranges in `requirements.txt`, an `.env.example`, and a unit test suite covering signature checks, challenge handling, and forwarding behavior.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit f7c127736c55b1f44565a9c083e7caa9183ed784. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
