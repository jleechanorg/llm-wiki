# PR #359: fix: delete ao-pr-poller.sh (superseded by AO lifecycle-worker)

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-03-21
**Author:** jleechan2015
**Stats:** +0/-576 in 1 files

## Summary
The `ao-pr-poller.sh` script was a cron/launchd-based poller that scanned for non-green PRs and respawned AO agents. PR #322 deprecated the launchd plist (`ai.ao-pr-poller.plist.deprecated`) but left the script in place.

The AO lifecycle-worker now handles this natively — no separate poller needed.

## Background
The `ao-pr-poller.sh` script was a cron/launchd-based poller that scanned for non-green PRs and respawned AO agents. PR #322 deprecated the launchd plist (`ai.ao-pr-poller.plist.deprecated`) but left the script in place.

The AO lifecycle-worker now handles this natively — no separate poller needed.

## Raw Body
## Background
The `ao-pr-poller.sh` script was a cron/launchd-based poller that scanned for non-green PRs and respawned AO agents. PR #322 deprecated the launchd plist (`ai.ao-pr-poller.plist.deprecated`) but left the script in place.

The AO lifecycle-worker now handles this natively — no separate poller needed.

## Goals
- Complete the retirement started in PR #322 by deleting the script itself
- Eliminate the source of confusion (script still present but shouldn't be used)

## Tenets
- Minimal scope: delete only the deprecated script, nothing else

## High-level description of changes
Deletes `scripts/ao-pr-poller.sh` (576 lines). The launchd plist was already deprecated in #322.

## Testing
No tests needed — pure deletion of dead code.

## Low-level details
- **scripts/ao-pr-poller.sh**: Deleted. Superseded by AO lifecycle-worker installed via `scripts/install-ao-lifecycle-agent-orchestrator.sh` (added in #322).

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Low risk: this PR only deletes a deprecated polling/respawn shell script and does not change runtime logic elsewhere.
> 
> **Overview**
> Removes the legacy `scripts/ao-pr-poller.sh` launchd/cron poller (576 LOC) that scanned PRs and respawned AO agents.
> 
> This completes the retirement of the poller now that agent lifecycle is handled elsewhere, reducing confusion from having an unused script in the repo.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit d895b9d80e4e7b882ca32c1d906f958efd7f7a7b. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
