# PR #142: feat(agento): proactive recovery — auto-stop merged PRs, respawn stuck sessions

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-03-14
**Author:** jleechan2015
**Stats:** +1053/-31 in 14 files

## Summary
- **agento-notifier.py**: recovery dispatch on AO lifecycle events — `merge.completed` → `ao stop`, `reaction.escalated` (agent-stuck) → `ao spawn` with 60s cooldown guard
- **ao-backfill.sh**: two new passes — merged-PR cleanup via `gh pr list --head`, and liveness respawn for stuck/killed sessions >30min (reads metadata files directly, clears `pr=` field before spawn)
- **roadmap/AGENTO_PROACTIVE_RECOVERY_DESIGN.md**: design analysis covering 4 options (external scripts, AO plugin, webhook han

## Raw Body
## Summary

- **agento-notifier.py**: recovery dispatch on AO lifecycle events — `merge.completed` → `ao stop`, `reaction.escalated` (agent-stuck) → `ao spawn` with 60s cooldown guard
- **ao-backfill.sh**: two new passes — merged-PR cleanup via `gh pr list --head`, and liveness respawn for stuck/killed sessions >30min (reads metadata files directly, clears `pr=` field before spawn)
- **roadmap/AGENTO_PROACTIVE_RECOVERY_DESIGN.md**: design analysis covering 4 options (external scripts, AO plugin, webhook handler, fork) with resolved implementation Q&A

## Changes

| File | Change |
|---|---|
| `scripts/agento-notifier.py` | Recovery dispatch table on `/ao-notify` events |
| `~/.openclaw/scripts/ao-backfill.sh` | Pass 1: merged cleanup; Pass 2: liveness respawn |
| `roadmap/AGENTO_PROACTIVE_RECOVERY_DESIGN.md` | New design doc |
| `.beads/` | Fix prefix case for br 0.1.28; new beads orch-01b through orch-aat |

## Fixes caught in review

- `session.stuck` → changed to `reaction.escalated` trigger (avoids respawn loop every 10min poll)
- `gh pr view <branch>` → `gh pr list --head <branch>` (view takes number not branch name)
- Session pass iterated dirs → fixed to iterate plain files (sessions are key=value files, not subdirs)

## Testing

- agento-notifier.py: verify with `curl -X POST http://127.0.0.1:18800/ao-notify -d '{"event":{"type":"merge.completed","projectId":"worldai-pr5961"}}'`
- ao-backfill.sh: dry-run by setting AO_BIN to `echo` and checking log output

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Introduces new automation that can stop/spawn Agent Orchestrator sessions based on webhook events and cron heuristics; misclassification or environment differences could cause unintended session churn or missed cleanup.
> 
> **Overview**
> Adds **proactive recovery for Agent Orchestrator (agento) sessions** by reacting to lifecycle events and periodically reconciling PR/session state.

