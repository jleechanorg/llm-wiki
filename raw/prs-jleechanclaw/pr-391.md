# PR #391: feat(commands): enhance /roadmap with situation survey, find-or-create beads/docs, Claude memory

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-03-24
**Author:** jleechan2015
**Stats:** +271/-0 in 3 files

## Summary
/roadmap created duplicate beads and docs without checking if relevant ones already existed. It also had no memory integration. This PR adds a survey phase and find-or-create logic.

## Background
/roadmap created duplicate beads and docs without checking if relevant ones already existed. It also had no memory integration. This PR adds a survey phase and find-or-create logic.

## Raw Body
## Background
/roadmap created duplicate beads and docs without checking if relevant ones already existed. It also had no memory integration. This PR adds a survey phase and find-or-create logic.

## Goals
- Phase 0 surveys memories + existing roadmap/*.md + open beads before acting
- Find-or-create: update existing bead/doc if match found; never duplicate
- Memory write: save planning decisions to Claude auto-memory after session

## Tenets
Find-or-create over create-always. Minimal edits. Phase 0 on every invocation.

## High-level description of changes
Added Phase 0 (situation survey), find-or-create logic in step 3, and memory write in step 16 of the execution workflow.

## Testing
Read-through verification. No code — command definition only.

## Low-level details
- **.claude/commands/roadmap.md**: Phase 0 reads memories + scans roadmap/ + scans open beads; step 3 adds find-or-create for beads and docs; step 16 adds Claude auto-memory write
- **.claude/commands/r.md**: alias, unchanged

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Low risk: changes are limited to Claude command orchestration/docs and a log entry, with no production code paths affected. Primary risk is behavioral—`/roadmap` may now update existing beads/docs instead of creating new ones if matching heuristics are too broad or narrow.
> 
> **Overview**
> Strengthens the `/.claude/commands/roadmap.md` workflow by adding a **Phase 0 situation survey** that checks Claude auto-memories, existing `roadmap/*.md` files, and open beads before taking action.
> 
> Introduces explicit **find-or-create rules** to *update matching* beads/docs (including appending dated update sections) rather than creating duplicates, and adds an optional **Claude memory write** step to persist roadmap decisions after a session.
> 
> Adds a new `/.claude/commands/r.md` alias that forwards to `/roadmap`, and appends one entry to `workspace/memory/mcp-mail-ack-log.md`.
> 
> <sup>Written by [Cursor Bugbot](https://cur
