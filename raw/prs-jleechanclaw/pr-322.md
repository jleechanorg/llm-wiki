# PR #322: [agento] fix: launchctl domain, retire ao-pr-poller, add lifecycle-worker for agent-orchestrator

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-03-21
**Author:** jleechan2015
**Stats:** +156/-4 in 4 files

## Summary
Three linked harness bugs: bd-eh1 (launchctl wrong domain), orch-oox (ao-pr-poller crash loop), orch-ies (no lifecycle-worker for agent-orchestrator).

## Background
Three linked harness bugs: bd-eh1 (launchctl wrong domain), orch-oox (ao-pr-poller crash loop), orch-ies (no lifecycle-worker for agent-orchestrator).

## Raw Body
## Background
Three linked harness bugs: bd-eh1 (launchctl wrong domain), orch-oox (ao-pr-poller crash loop), orch-ies (no lifecycle-worker for agent-orchestrator).

## Goals
1. Fix launchctl domain so legacy agento cleanup actually works on uninstall
2. Stop ao-pr-poller crash loop
3. Start watching agent-orchestrator PRs autonomously

## Tenets
- Fresh from main, minimal scope, preserve deprecated plist

## Changes
- launchctl bootout "system/$label" → "gui/$(id -u)/$label" (bd-eh1)
- ai.ao-pr-poller.plist → .deprecated, removed from install.sh (orch-oox)
- New com.agentorchestrator.lifecycle-agent-orchestrator.plist (orch-ies)
- New scripts/install-ao-lifecycle-agent-orchestrator.sh
- install.sh: replaces ao-pr-poller with lifecycle-agent-orchestrator

## Testing
- Correct macOS user-domain syntax verified: gui/$(id -u)/label
- Plist modeled 1:1 on working lifecycle-jleechanclaw (PID 1133)

Closes bd-eh1, orch-oox, orch-ies.

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Moderate risk because it changes launchd install/uninstall behavior and introduces a new always-on lifecycle worker that injects `GITHUB_TOKEN` into a user LaunchAgent; misconfiguration could break local automation or leak credentials via logs/plist permissions.
> 
> **Overview**
> Adds a new per-user launchd LaunchAgent (`com.agentorchestrator.lifecycle-agent-orchestrator`) to run `ao lifecycle-worker agent-orchestrator` at login with `KeepAlive`, logging to `~/.openclaw/logs` and using injected `GITHUB_TOKEN`/`GH_TOKEN`.
> 
> Introduces `install-ao-lifecycle-agent-orchestrator.sh` to install/uninstall the new plist with placeholder substitution, Node path detection, and legacy cleanup that bootouts/removes the old `ai.ao-pr-poller` agent.
> 
> Updates `scripts/install.sh` to stop installing `ao-pr-poller`, install the new lifecycle worker instead, and fixes legacy uninstall cleanup to use the correct `launchctl` user domain (`gui/$(id -u)`) while also including `ai.ao-pr-poller` in
