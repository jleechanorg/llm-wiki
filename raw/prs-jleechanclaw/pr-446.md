# PR #446: fix: dropped-thread script API call method

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-03-30
**Author:** jleechan2015
**Stats:** +193/-45 in 4 files

## Summary
(none)

## Raw Body
GET with query params instead of POST with JSON body for conversations.replies — was silently returning empty for many threads

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Moderate operational risk because it changes how the dropped-thread cron script calls Slack APIs and handles error parsing, which could affect thread auditing behavior if parameters/response handling are wrong; other changes are low impact.
> 
> **Overview**
> Fixes `dropped-thread-followup.sh` thread retrieval by switching `conversations.replies` from a POST-with-JSON body to a GET with URL-encoded query params, and makes the script fail fast when Slack returns `.ok=false` instead of silently treating missing messages as empty.
> 
> Updates `build_memory.py` to resolve `SOUL.md` and `MEMORY.md` paths via `Path.resolve()` before existence checks/writes (more robust when those files are symlinks).
> 
> Refreshes the `CRON_JOBS_BACKUP` snapshots, expanding from 5 to 10 jobs and updating several AO/life reminder job definitions and metadata.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 3a423dee3ff80e19a2fe4bb9cc36e2f0edc12c96. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->
## Summary by CodeRabbit

* **Bug Fixes**
  * Consistent path resolution for symlinked files during file operations.
  * Thread message retrieval updated to use query-style requests and stricter API response handling.

* **Documentation**
  * Updated scheduled jobs snapshot: expanded job set, added several reminders/status jobs, removed/renamed a legacy job, and refreshed metadata.
<!-- end of auto-generated comment: release notes by coderabbit.ai -->
