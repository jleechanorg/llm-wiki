# PR #482: chore(skills): MiniMax provider + cmux autoapprove (sync from prod clawhub)

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-04-03
**Author:** jleechan2015
**Stats:** +1214/-0 in 8 files

## Summary
Syncs harness content that was present locally under `~/.openclaw_prod` (clawhub installs + lock) into the repo.

## Raw Body
## Summary
Syncs harness content that was present locally under `~/.openclaw_prod` (clawhub installs + lock) into the repo.

## Changes
- **`.clawhub/lock.json`** — records `add-minimax-provider` (v2.0.0) alongside existing `cmux`.
- **`skills/add-minimax-provider/`** — clawhub skill for MiniMax provider setup.
- **`skills/cmux-codex-autoapprove/`** — skill + launchd-oriented approval worker script.
- **`.gitignore`** — add `/openclaw.prod.json` so live prod config next to `openclaw.json` is never committed.

## Not included
- **`openclaw.prod.json`** was left untracked and is now ignored (contains live credentials).

## How to verify
- `git check-ignore -v openclaw.prod.json` → should report the new rule when that file exists locally.

Made with [Cursor](https://cursor.com)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Adds a new automated approval worker that can send keystrokes to terminal sessions based on heuristic/LLM classification, which could mis-approve prompts if detection/classification is wrong. Other changes are primarily new documentation/config metadata and an ignore rule to prevent committing prod credentials.
> 
> **Overview**
> Syncs new ClawHub skill content into the repo by recording `add-minimax-provider` in `.clawhub/lock.json` and adding a full MiniMax provider setup skill (docs + metadata) covering API-key and OAuth portal configurations.
> 
> Adds a new `cmux-codex-autoapprove` skill including a Python worker (`cmux_codex_approve_launchd.py`) that scans `cmux` terminal surfaces for approval dialogs, optionally classifies them via `codex`/`claude`, and automatically sends `Enter`/`1`/`y` to approve.
> 
> Updates `.gitignore` to ignore `/openclaw.prod.json` to reduce risk of committing live prod credentials.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 9f74654d0128ad32d56dc04ce9f2aa5a8ff2bc1c. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?
