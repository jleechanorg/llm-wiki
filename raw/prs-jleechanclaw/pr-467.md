# PR #467: chore: deduplicate repo CLAUDE.md (~22% token reduction)

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-04-01
**Author:** jleechan2015
**Stats:** +33/-209 in 1 files

## Summary
`~/.claude/CLAUDE.md` (global, 43k chars) and the repo `CLAUDE.md` (was 36k chars) have significant overlap. Claude Code loads both, so duplicate sections waste context tokens and trigger the performance warning (`⚠Large CLAUDE.md will impact performance`).

## Background
`~/.claude/CLAUDE.md` (global, 43k chars) and the repo `CLAUDE.md` (was 36k chars) have significant overlap. Claude Code loads both, so duplicate sections waste context tokens and trigger the performance warning (`⚠Large CLAUDE.md will impact performance`).

## Raw Body
## Background

`~/.claude/CLAUDE.md` (global, 43k chars) and the repo `CLAUDE.md` (was 36k chars) have significant overlap. Claude Code loads both, so duplicate sections waste context tokens and trigger the performance warning (`⚠Large CLAUDE.md will impact performance`).

## Goals

- Reduce repo CLAUDE.md token count by ~20% without losing any information
- Remove exact duplicates of sections already in the global CLAUDE.md
- Condense verbose reference sections (code examples, command blocks)

## Tenets

- **No information loss** — every removed section either exists in global CLAUDE.md or was condensed, not deleted
- **Repo-specific stays** — gateway, openclaw, VM ops, testing_* rules remain
- **Global rules stay global** — automation completeness, testing philosophy base principles

## High-level description of changes

Removed 3 duplicate sections (automation completeness, testing philosophy base, PR description sections) that are already in `~/.claude/CLAUDE.md`. Condensed 5 verbose sections (Slack posting, gateway ops, beads CLI, agent model config, repository purpose/project structure) by removing redundant code blocks and merging overlapping listings.

Result: **36,362 → 28,494 chars (-7,868 chars, ~22% reduction)**.

## Testing

- Visual diff review: all condensed sections preserve the essential information
- No code files changed — config/docs only

## Low-level details

- **CLAUDE.md**: Removed "Automation completeness" section (exact dup of global). Trimmed "Testing Philosophy" to repo-specific `testing_*` rules only. Removed "PR Description Sections" (global has full version). Merged "Project Structure" file tree into "Repository Purpose". Condensed "Beads" from 36 lines to 4. Condensed "Slack — Posting" from 47 lines to 13. Condensed "OpenClaw Agent Model" from 25 lines to 6 (points to skill). Condensed "Gateway" from 24 lines to 6.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> 
