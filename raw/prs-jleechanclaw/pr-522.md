# PR #522: [P1] fix(launchd): ao7green copy scripts to OPENCLAW_HOME + migrate gateway cron

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-04-06
**Author:** jleechan2015
**Stats:** +39/-3 in 3 files

## Summary
Follow-up to merged https://github.com/jleechanorg/jleechanclaw/pull/521

## Raw Body
## Summary
Follow-up to merged https://github.com/jleechanorg/jleechanclaw/pull/521

## Changes
- **install-launchagents.sh**: Copy `ao7green-pr-monitor*.sh` into `~/.openclaw/scripts/` before loading the LaunchAgent (fresh installs / non-`~/.openclaw` worktrees).
- **install-openclaw-scheduled-jobs.sh**: Add gateway cron job `64f2399d-33f4-4451-be87-05350d2b2590` to `MIGRATED_JOBS` so `install-openclaw-scheduled-jobs.sh` disables it when migrating.
- **install-launchagents.sh**: After installing the ao7green plist, disable that cron id in live `cron/jobs.json` and HUP the gateway (same behavior as other migrated jobs).
- **plist**: `StandardOutPath` → `/dev/null` (monitor already `tee`s to `AO_MONITOR_LOG`; avoids duplicate lines from launchd).

## Testing
- `plutil -lint` on plist
- `python -m pytest tests/test_openclaw_configs.py -q`

Made with [Cursor](https://cursor.com)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Moderate risk because it changes macOS job installation/migration behavior and disables an existing gateway cron job, which could stop the monitor from running if paths or jq edits are wrong.
> 
> **Overview**
> Ensures the `ao7green-jleechanclaw` LaunchAgent works outside a `~/.openclaw` worktree by copying `ao7green-pr-monitor.launchd.sh` and `ao7green-pr-monitor.sh` into `~/.openclaw/scripts` before loading the plist.
> 
> Migrates the corresponding gateway cron job (`64f2399d-33f4-4451-be87-05350d2b2590`) to launchd by adding it to `MIGRATED_JOBS` and by updating live `cron/jobs.json` to disable it (then HUPing the gateway) after LaunchAgent install.
> 
> Reduces duplicate logging by setting the plist’s `StandardOutPath` to `/dev/null` since the monitor script already `tee`s to `AO_MONITOR_LOG`.
> 
> <sup>Reviewed by [Cursor Bugbot](https://cursor.com/bugbot) for commit 7a727fb54cf652f7e2ae00b18052c96c5e90d3b9. Bugbot is set up for automated code reviews on this repo. Configure [here](https://www.cursor.com/dashboard/bugbot).</sup>
