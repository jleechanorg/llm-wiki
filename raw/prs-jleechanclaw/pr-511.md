# PR #511: [agento] fix(launchd): remove duplicate com.openclaw.gateway plist template

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-04-05
**Author:** jleechan2015
**Stats:** +27/-86 in 5 files

## Summary
(none)

## Raw Body
### Background
Root cause of recurring gateway orphan processes: `launchd/` directory has BOTH `ai.openclaw.gateway.plist` (canonical) AND `com.openclaw.gateway.plist` (legacy). `install-launchagents.sh` installs both during deploy Stage 3.5, creating two competing gateway instances on the same port/state-dir → lock storms → WS pong starvation.

### Goals
Eliminate the source of duplicate gateway plists so deploys never create competing instances.

### Changes
1. Delete `launchd/com.openclaw.gateway.plist` (legacy template)
2. Simplify `deploy.sh` Stage 4 to reference only `ai.openclaw.gateway` (remove com.openclaw.gateway fallback)

### Testing
`gateway-preflight.sh --fix` already cleaned up the installed duplicate this session. This PR prevents it from being recreated on next deploy.

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Touches macOS service management and deploy automation for the production gateway; a wrong label/path could prevent the gateway from starting or could break restarts on deploy.
> 
> **Overview**
> Removes the legacy `com.openclaw.gateway` launchd plist to prevent two gateway agents from being installed/started concurrently on the same port/state dir.
> 
> Updates deployment and installer scripts to *only* manage `ai.openclaw.gateway`, including cleaning up any installed legacy plist and adding a deploy-time assertion that `ai.openclaw.gateway` is loaded while `com.openclaw.gateway` is absent. Also adjusts the canary’s Node binary discovery to query the canonical launchd label and aligns gateway log/state paths to `~/.openclaw_prod`.
> 
> <sup>Reviewed by [Cursor Bugbot](https://cursor.com/bugbot) for commit d1c74ad6ffa792398ee3766a26ea99edb6e273ae. Bugbot is set up for automated code reviews on this repo. Configure [here](https://www.cursor.com/dashboard/bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->

## Summary by CodeRabbit

## Release Notes

**Chores**
- 
