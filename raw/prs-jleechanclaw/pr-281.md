# PR #281: fix: harness-analyzer crontab grep, weekday schedule, Red Lines, PR comments API

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-03-19
**Author:** jleechan2015
**Stats:** +383/-103 in 9 files

## Summary
- Fix broken crontab deprecation check (grep -rq produced no output for pipeline)
- Fix harness-analyzer-9am plist weekday schedule: 1-5 (Sun-Thu) → 2-6 (Mon-Fri)
- Fix AGENTS.md Red Lines detection: add to ISSUES_FOUND when missing
- Fix PR comment dedup: use `issues/{pr}/comments` instead of `pulls/{pr}/comments` for body comments

## Raw Body
## Summary

- Fix broken crontab deprecation check (grep -rq produced no output for pipeline)
- Fix harness-analyzer-9am plist weekday schedule: 1-5 (Sun-Thu) → 2-6 (Mon-Fri)
- Fix AGENTS.md Red Lines detection: add to ISSUES_FOUND when missing
- Fix PR comment dedup: use `issues/{pr}/comments` instead of `pulls/{pr}/comments` for body comments

## Changes

| File | Fix |
|------|-----|
| `scripts/harness-analyzer.sh` | Crontab: use `grep -rh` to output lines, filter by content before flagging |
| `scripts/harness-analyzer.sh` | AGENTS_MISSING_REDLINES: add to ISSUES_FOUND |
| `scripts/harness-analyzer.sh` | Prior-comment check: `repos/.../issues/$pr_num/comments` (body comments) |
| `launchd/ai.openclaw.schedule.harness-analyzer-9am.plist` | Weekday 1-5 → 2-6 (Mon-Fri) |

## Testing

- Crontab: `grep -rh ... | grep -v ... | grep -q .` correctly detects lines mentioning crontab (excluding backup/legacy)
- Plist: launchd weekday 2=Mon … 6=Fri
- PR dedup: `/issues/$pr_num/comments` returns PR body comments; `/pulls/$pr_num/comments` returns review comments only

Made with [Cursor](https://cursor.com)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Medium risk because it tightens merge-gating behavior (more fail-closed paths) and changes how evidence approval is detected, which could block merges unexpectedly if GitHub APIs or comment formats differ in practice.
> 
> **Overview**
> Adds a new *memory lookup* health probe by running `openclaw memory search` in both `monitor-agent.sh` and `scripts/doctor.sh`, with specific detection for `better-sqlite3`/Node native module mismatches and surfaced diagnostics.
> 
> Hardens merge gating in `src/orchestration/merge_gate.py`: CodeRabbit `APPROVED` must match the current HEAD SHA (and blocks if SHA is missing), unresolved-thread checking is now *fail-closed* if GraphQL can’t fetch thread state, and the evidence gate now prefers a machine-readable `<!-- reviewer-verdict: ... sha:... -->` marker in issue comments fro
