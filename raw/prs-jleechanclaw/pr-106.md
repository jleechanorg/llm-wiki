# PR #106: feat: agento worktree requirement and AO dashboard launchd management

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-03-12
**Author:** jleechan2015
**Stats:** +97/-0 in 3 files

## Summary
- Document agento worktree requirement: always use new git worktree unless already inside an AO-managed session
- Document CI/PR comment auto-resolution via AO reactions configuration
- Add launchd plist for AO dashboard at port 3011
- Update install-launchagents.sh and doctor.sh to manage and verify AO dashboard

## Raw Body
## Summary
- Document agento worktree requirement: always use new git worktree unless already inside an AO-managed session
- Document CI/PR comment auto-resolution via AO reactions configuration
- Add launchd plist for AO dashboard at port 3011
- Update install-launchagents.sh and doctor.sh to manage and verify AO dashboard

## Changes

### 1. agento SKILL.md
- Added "Worktree Requirement" section documenting that agento MUST always use a new git worktree
- Added "CI & PR Comment Auto-Resolution" section documenting automatic reactions

### 2. AO Dashboard Launchd
- Created `openclaw-config/ai.agent-orchestrator.dashboard.plist` for launchd management
- Dashboard runs on port 3011 via `~/bin/ao dashboard`
- Logs to `~/.agent-orchestrator/logs/dashboard.log`

### 3. install-launchagents.sh
- Added installation of AO dashboard plist
- Added verification of AO dashboard launchd registration
- Added log location output

### 4. doctor.sh
- Added checks for AO dashboard plist (repo and live)
- Added launchctl runtime check for AO dashboard registration and state
- Added port 3011 listener check

## Testing
- Verified plist syntax is valid
- Changes are minimal and additive to existing workflows
- Doctor checks use warn (not fail) for AO dashboard to avoid blocking if not installed

## Known Limitations
- AO dashboard plist is optional - doctor will warn but not fail if not installed
- Existing test failures in test_openclaw_configs.py are pre-existing (Slack MCP config issues, unrelated to this PR)

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Primarily additive config/docs, but it introduces a new launchd service definition (with hardcoded local paths) and extends health checks, which could affect developer setup if installed or validated on different machines.
> 
> **Overview**
> Adds a new macOS launchd plist (`openclaw-config/ai.agent-orchestrator.dashboard.plist`) to run the Agent-Orchestra
