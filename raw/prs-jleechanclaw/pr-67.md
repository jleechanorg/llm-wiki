# PR #67: test: harden MCP roundtrip and launchd startup follow-ups

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-03-08
**Author:** jleechan2015
**Stats:** +752/-1566 in 24 files

## Summary
- land the MCP roundtrip test hardening that missed PR #59 after it merged
- harden launchd installers so gateway, Mission Control, frontend, startup-check, and mctrl supervisor survive local restart paths more reliably
- add regression coverage for launch-agent installation, startup-check runtime resolution, and non-placeholder Mission Control token handling

## Raw Body
## Summary
- land the MCP roundtrip test hardening that missed PR #59 after it merged
- harden launchd installers so gateway, Mission Control, frontend, startup-check, and mctrl supervisor survive local restart paths more reliably
- add regression coverage for launch-agent installation, startup-check runtime resolution, and non-placeholder Mission Control token handling

## Changes
- cherry-pick the MCP roundtrip test fix onto a fresh branch from `origin/main`, including payload-first OpenClaw CLI parsing and isolated session IDs in `tests/test_mcp_dispatch_roundtrip.py`
- install `ai.openclaw.gateway` via `openclaw gateway install --force`, preserve the live gateway token, and refresh `~/.openclaw/startup-check.sh` from repo state during `scripts/install-launchagents.sh`
- add checked-in `openclaw-config/ai.openclaw.startup-check.plist` and harden startup-check script resolution for launchd's minimal environment
- preserve or generate a valid local Mission Control token instead of stamping `your-local-auth-token-here` into launchd services
- switch launchd reload paths to `launchctl bootstrap`/`bootout`, add `ThrottleInterval` for mctrl supervisor, and widen PATH for launchd-managed scripts
- fix `tests/test_openclaw_configs.py` repo-root resolution and add targeted launch-agent installer tests

## Testing
- `source ~/.profile && source ~/.openclaw/set-slack-env.sh && PYTHONPATH=src python -m pytest tests/test_mcp_dispatch_roundtrip.py -vv`
- `PYTHONPATH=src python -m pytest tests/test_openclaw_configs.py -k 'LaunchAgentInstallers' -vv`
- `./scripts/install-launchagents.sh`
- `./scripts/install-mctrl-supervisor.sh`
- verified live launchd state with `launchctl print` for `ai.openclaw.gateway`, `ai.openclaw.mission-control`, `ai.openclaw.startup-check`, and `ai.mctrl.supervisor`
- verified live listeners on `127.0.0.1:18789`, `127.0.0.1:9010`, and `127.0.0.1:3000`

## Known Limitations
- the older evidence bundle issue remains tracked separately in `bd-b8j`
- `bd-ug
