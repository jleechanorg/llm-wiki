# PR #74: feat(agento): Agent-Orchestrator integration via keyword routing

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-03-10
**Author:** jleechan2015
**Stats:** +347/-7 in 5 files

## Summary
- Adds AO (Agent-Orchestrator) as an alternative dispatch path, triggered by saying "agento" in OpenClaw chat
- OpenClaw LLM decides at inference time whether to use mctrl (bead-native) or agento (GitHub-native AO)
- Verified end-to-end: webhook → Slack delivery working

## Raw Body
## Summary
- Adds AO (Agent-Orchestrator) as an alternative dispatch path, triggered by saying "agento" in OpenClaw chat
- OpenClaw LLM decides at inference time whether to use mctrl (bead-native) or agento (GitHub-native AO)
- Verified end-to-end: webhook → Slack delivery working

## Changes (vs origin/main)

### New Files
- **`openclaw-config/skills/agento/SKILL.md`** (76 lines): OpenClaw skill — detects "agento" keyword, maps intent to `ao spawn/send/status` commands, gives immediate chat reply
- **`scripts/agento-notifier.py`** (99 lines): Minimal HTTP server (port 18800) that receives AO webhook events and posts formatted messages to #ai-slack-test

### Modified Files
- **`.beads/metadata.json`**: Simplified from Dolt-specific settings to local `beads.db` file config
- **`.gitignore`**: Added exclusions for `.beads/issues.jsonl`, `.beads/dolt*`, `.claude/scheduled_tasks.lock`

## Bug Fixes Applied (PR #74 review)
- **P1**: Fixed invalid shebang (`#!/usr/bin/env python3 -u` → `#!/usr/bin/env python3`)
- **P1**: Fixed token resolution — added SLACK_USER_TOKEN fallback
- **Medium**: Fixed token exposure — replaced subprocess curl with urllib.request
- **Medium**: Fixed null event field handling (`data.get("event", {})` → `data.get("event") or {}`)
- **Medium**: Fixed null data field in event (`event.get("data", {}).get(...)` → `(event.get("data") or {}).get(...)`)
- **Medium**: Added type check for non-dict JSON payloads (returns 400)
- **Docs**: Fixed project ID examples (worldai → worldai-claw-agento)
- **Docs**: Fixed channel reference (#all-jleechan-ai → #ai-slack-test)

## Testing
- `curl POST http://127.0.0.1:18800/ao-notify` → message appeared in #ai-slack-test ✓
- Notifier running as launchd daemon, survives restarts ✓
- `~/bin/ao --version` → 0.1.0 ✓
- All CI checks passing

## Known Limitations
- Slack channel is #ai-slack-test (bot not yet in #all-jleechan-ai — needs manual invite)
- AO config changes (`agent-orchestrator.yaml`) are in `~/projects_refer
