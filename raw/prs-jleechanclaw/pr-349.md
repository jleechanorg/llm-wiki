# PR #349: [P1] fix: restore register_agent call with agent_name alias (was silently disabled)

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-03-21
**Author:** jleechan2015
**Stats:** +7/-7 in 1 files

## Summary
- MCP server (mcp_agent_mail) now accepts agent_name as an alias for name in register_agent, so agents calling it with the natural agent_name parameter (matching fetch_inbox convention) no longer get a Pydantic validation error
- agent-orchestrator.yaml agentRules restored to call register_agent with agent_name before send_message
- The previous workaround (Do NOT call register_agent) was counterproductive: send_message also requires the sender to be a registered agent, so silently skipping regi

## Raw Body
## Summary
- MCP server (mcp_agent_mail) now accepts agent_name as an alias for name in register_agent, so agents calling it with the natural agent_name parameter (matching fetch_inbox convention) no longer get a Pydantic validation error
- agent-orchestrator.yaml agentRules restored to call register_agent with agent_name before send_message
- The previous workaround (Do NOT call register_agent) was counterproductive: send_message also requires the sender to be a registered agent, so silently skipping registration broke MCP mail entirely

## Changes
- src/mcp_agent_mail/app.py (mcp_mail repo): add agent_name: Optional[str] = None parameter to register_agent, resolve agent_name to name alias before use
- agent-orchestrator.yaml: restore register_agent call with agent_name, remove incorrect warning note, update send_message to use the same session name

## Testing
- Live smoke test: curl register_agent with agent_name against the running server: SUCCESS (agent id 43 registered)
- Live smoke test: curl fetch_inbox with agent_name: SUCCESS (empty inbox returned)
- jleechanclaw test suite: 205 passed, 81 skipped
- MCP mail PR: https://github.com/jleechanorg/mcp_mail/pull/42

🤖 Generated with Claude Code

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Changes the required MCP mail workflow in `agent-orchestrator.yaml`; misconfiguration could prevent agents from registering/sending messages and break operational notifications.
> 
> **Overview**
> Updates `agent-orchestrator.yaml` agent rules to **require** calling `mcp__mcp-agent-mail__register_agent` at task start (with `agent_name=<session_name>`) before any `send_message` calls.
> 
> Aligns all MCP mail sending to use the same `<session_name>` for `sender_name` (including the pre-exit “session complete” message), replacing the prior guidance that discouraged registration and/or used a fixed sender.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit f9436151c91be2fe9be6d8
