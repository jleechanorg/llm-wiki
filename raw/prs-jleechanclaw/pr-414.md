# PR #414: [agento] [P1] fix(doctor.sh): 3 health-check improvements (bd-yk9h, bd-23ej, bd-oueo)

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-03-27
**Author:** jleechan2015
**Stats:** +75/-40 in 2 files

## Summary
Three independent `doctor.sh` / `monitor-agent.sh` health-check fixes bundled in one PR:

- **bd-yk9h**: `detect_ao_dashboard_port()` hardcoded fallback `3011`; actual AO dashboard port is `3020`. Fixed to read `agent-orchestrator.yaml` first, defaulting to `3020`.
- **bd-23ej**: `monitor-agent.sh` token probe reported `FAIL:gateway.auth.token:health_http=000` when the gateway is down (connection refused). Fixed to distinguish `http=000` → `FAIL:gateway.down:connection_refused`.
- **bd-oueo**: R

## Raw Body
## Summary

Three independent `doctor.sh` / `monitor-agent.sh` health-check fixes bundled in one PR:

- **bd-yk9h**: `detect_ao_dashboard_port()` hardcoded fallback `3011`; actual AO dashboard port is `3020`. Fixed to read `agent-orchestrator.yaml` first, defaulting to `3020`.
- **bd-23ej**: `monitor-agent.sh` token probe reported `FAIL:gateway.auth.token:health_http=000` when the gateway is down (connection refused). Fixed to distinguish `http=000` → `FAIL:gateway.down:connection_refused`.
- **bd-oueo**: Redacted roundtrip check skipped when `doctor.sh` ran from a plain shell (env vars not in launchd context). Fixed by moving the launchd env hydration to run *before* the roundtrip check.

## Testing

- `PYTHONPATH=src python -m pytest tests/test_openclaw_configs.py -q` → 29 passed, 76 skipped (skips expected — require real env vars)

## Changes

| File | Change |
|------|--------|
| `scripts/doctor.sh` | `detect_ao_dashboard_port()`: read `agent-orchestrator.yaml` port, default to `3020`; move launchd env hydration earlier |
| `monitor-agent.sh` | Distinguish `http=000` (gateway down) from auth failure in gateway token probe |

Closes #945

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Low risk: changes are limited to local diagnostic scripts and error classification, with no production runtime logic beyond clearer health-check outcomes.
> 
> **Overview**
> Tightens health-check behavior in `scripts/doctor.sh` and `monitor-agent.sh` to reduce false failures and improve diagnostics.
> 
> `doctor.sh` now detects the AO dashboard port more accurately by reading `--port` args from the launchd plist, falling back to `agent-orchestrator.yaml`, and defaulting to `3020` (instead of `3011`). It also hydrates environment variables from the gateway launchd plist earlier so the redacted-config roundtrip check can run correctly outside a launchd context.
> 
> `monitor-agent.sh`’s gateway token probe now treats `curl` HTTP `000` as *gateway down* and reports specific f
