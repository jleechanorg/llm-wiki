# PR #110: Add dedicated OpenClaw monitor agent files

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-03-12
**Author:** jleechan2015
**Stats:** +873/-0 in 14 files

## Summary
(none)

## Raw Body
Ports the monitor-agent PR from mctrl_test into jleechanclaw.

Included in this port:
- monitor agent shell script + launchd plist + model/auth configs
- monitor workspace docs/templates
- targeted tests for monitor alert gating and doc/security guardrails
- timeout watchdog for monitor agent invocation
- tightened secret-handling guidance in workspace docs

Validation run locally:
- pytest -q test_monitor_agent_config.py (6 passed)
- bash -n openclaw-config/monitor-agent.sh

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Adds a new periodic monitor agent that can send alerts to Slack and modifies the LaunchAgents installer flow, which could impact local operations/alert noise if misconfigured.
> 
> **Overview**
> Adds a new `monitor-agent` that runs periodically via launchd and invokes `openclaw agent --agent monitor` with a strict prompt, lockfile, timeout watchdog, and output sanitization before optionally sending a Slack alert only when `STATUS=PROBLEM`.
> 
> Wires rollout into `scripts/install-launchagents.sh` by syncing `openclaw-config` to `~/.openclaw`, installing `monitor-agent.sh`, and conditionally loading `ai.openclaw.monitor-agent.plist`, plus adds monitor-specific model/auth config stubs and a `workspaces/monitor` documentation/template workspace.
> 
> Introduces targeted pytest coverage to ensure Slack alert gating, strict `STATUS=` parsing (ignoring historical mentions), timeout behavior, installer wiring, and security guidance in the new workspace docs.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 64d13bcaa8c702e918e54c43a517e8a5b5c8312e. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->
## Summary by CodeRabbit

* **New Features**
  * Added automated monitoring agent with periodic health checks and problem detection.
  * Integrated S
