# PR #164: fix(webhook): include workflow lane in remediation issue title

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-03-15
**Author:** jleechan2015
**Stats:** +21/-5 in 1 files

## Summary
- Added workflow lane context to remediation issue titles so agents know what workflow to run
- When `pull_request.opened` events trigger remediation, the issue now includes "(comment-validation)"

## Raw Body
## Summary
- Added workflow lane context to remediation issue titles so agents know what workflow to run
- When `pull_request.opened` events trigger remediation, the issue now includes "(comment-validation)"

## Changes
- Modified `webhook_worker.py` to call `route_event` from `pr_lifecycle` to get the workflow lane
- Issue title now includes workflow lane: "Remediate pull_request.opened on PR#163 @ cafe1234abcd (comment-validation)"
- Previously, the issue lacked workflow context, making it harder for agents to know what to do

## Testing
- All webhook_worker tests pass (28 tests)
- All pr_lifecycle tests pass (7 tests)  
- All webhook_queue tests pass (23 tests)

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Low risk string/metadata change, but it adds a new dependency on `pr_lifecycle.route_event` during dispatch which could affect webhook processing if routing inputs/behavior change.
> 
> **Overview**
> Remediation dispatch now calls `pr_lifecycle.route_event` to derive a `workflow_lane` for PR events and appends it to the AO `issue` title passed to `ao_spawn` (e.g., `(... comment-validation)`), giving agents explicit workflow context.
> 
> No dispatch behavior changes beyond the new routing call and updated issue formatting; existing retry/"already exists" handling remains the same.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 0454a00ec06f5853e93d87f4ab34bd61b108e92f. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
