# PR #387: feat(soul): immediate text ack for Slack task requests

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-03-24
**Author:** jleechan2015
**Stats:** +6/-0 in 1 files

## Summary
(none)

## Raw Body
### Background
OpenClaw only added an :eyes: emoji reaction when receiving a task request in Slack threads. In busy threads, emoji reactions are invisible to users, causing them to wonder if the request was received (\"shouldn't the ack be immediate?\").

### Goals
- OpenClaw posts an immediate \"On it — [summary]\" text reply before starting work on any task request
- Users get a visible signal within seconds, not just an emoji reaction

### Tenets
- Minimum scope: one COMMIT block in SOUL.md only
- Trigger-based rule per the Promise Gate protocol

### High-level description of changes
Added `## COMMIT: slack-task-ack` to `workspace/SOUL.md` (the file openclaw's session-init scans). Rule: when a Slack thread message contains a task/action request, post \"On it — [≤8 word summary]\" immediately before doing work.

### Testing
- Manual: post a task request to OpenClaw in #ai-slack-test, verify text ack appears before full result
- Harness fix identified via `/harness for why` protocol — 5 Whys traced to missing COMMIT scope

### Low-level details
- **workspace/SOUL.md**: Added `## COMMIT: slack-task-ack` block (8 lines) above existing `mcp-mail-ack` block

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Low risk: adds a single behavioral policy block in `workspace/SOUL.md` with no runtime code or data-handling changes.
> 
> **Overview**
> Adds a new **behavioral commitment** `## COMMIT: slack-task-ack` in `workspace/SOUL.md` requiring OpenClaw to immediately post a short in-thread text acknowledgement ("On it — …") when a Slack-directed message contains a task request.
> 
> The rule includes an explicit exclusion for multi-step/PR/worktree-dispatched work (delegated to `ao` via `dispatch-task`), keeping the ack requirement scoped to inline tasks.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 563d4d2f620a8e2731fbe9f090249fa62a7c04d3. This will update automatic
