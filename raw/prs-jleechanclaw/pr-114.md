# PR #114: chore: add Claude Code metadata-updater hook for Agent Orchestrator

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-03-12
**Author:** jleechan2015
**Stats:** +152/-0 in 2 files

## Summary
- Adds Claude Code PostToolUse hook that automatically tracks PR creation, branch switches, and PR merges in session metadata
- Configures hook in `.claude/settings.json` to run on Bash tool invocations

## Raw Body
## Summary
- Adds Claude Code PostToolUse hook that automatically tracks PR creation, branch switches, and PR merges in session metadata
- Configures hook in `.claude/settings.json` to run on Bash tool invocations

## Changes
- `.claude/metadata-updater.sh`: New hook script that parses command output and updates `$AO_DATA_DIR/$AO_SESSION` metadata file
- `.claude/settings.json`: Hook configuration to trigger metadata-updater on Bash tool use

## Testing
- Local verification: Hook executes on `gh pr create`, `git checkout -b`, `git switch -c`, and `gh pr merge` commands

## Known Limitations
- Requires `AO_SESSION` environment variable to be set in the Claude Code session

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Low risk: adds a local Claude Code hook script and config that only runs after successful Bash tool calls and writes to a session metadata file.
> 
> **Overview**
> Adds a Claude Code `PostToolUse` hook that runs `.claude/metadata-updater.sh` after Bash tool invocations.
> 
> The new script parses hook JSON (via `jq` or a fallback), detects `gh pr create`/`gh pr merge` and `git checkout`/`git switch` branch changes, and atomically updates key/value entries (e.g., `pr`, `branch`, `status`) in `$AO_DATA_DIR/$AO_SESSION` when available.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 00d82370e56bd53d234339b88385f48554ca944f. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->
## Summary by CodeRabbit

* **New Features**
  * Automatic session metadata tracking for Agent Orchestrator operations.
  * Captures successful shell actions: PR creation, branch creation/switching, and merge status.
  * Persists session state across Git and GitHub workflows.
  * Only records successful Bash tool 
