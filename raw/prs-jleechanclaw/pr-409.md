# PR #409: feat: migrate living-blog:novel-hourly-status from gateway cron to launchd

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-03-27
**Author:** jleechan2015
**Stats:** +78/-1 in 3 files

## Summary
(none)

## Raw Body
Migrate the living blog hourly status cron from gateway-managed cron/jobs.json to launchd (git-tracked).

**Changes:**
- `scripts/living-blog-status.sh` — hourly status script using `openclaw agent`
- `launchd/ai.openclaw.schedule.living-blog-status.plist.template` — launchd plist (hourly)
- `docs/CRON_MIGRATION.md` — updated migration map + checklist
- `cron/jobs.json` — gateway cron job disabled (local, gitignored)

**Root cause fix:** Cron prompt now explicitly names `jleechanorg/ai_universe_living_blog` to prevent hallucinated repo names (was hallucinating `worldarchitect/living-blog-server`).

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Low risk: adds a new launchd-scheduled script and documentation updates, with primary impact limited to when/where an hourly status job runs (potential for missed or duplicate posts if misconfigured).
> 
> **Overview**
> Moves `living-blog:novel-hourly-status` off gateway `jobs.json` and into a new hourly launchd job (`ai.openclaw.schedule.living-blog-status`) backed by `scripts/living-blog-status.sh`.
> 
> The new script runs `openclaw agent` with a tightened prompt that explicitly references `jleechanorg/ai_universe_living_blog` for PR state checks, and `docs/CRON_MIGRATION.md` is updated to include this job in the migration map/checklist and label verification loop.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 80b4e00ea1d37a026a225381a50467eb3aefb828. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
