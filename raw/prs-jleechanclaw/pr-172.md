# PR #172: fix(ao-backfill): support backfillAllPRs flag + auto-prune stale worktrees

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-03-16
**Author:** jleechan2015
**Stats:** +653/-66 in 8 files

## Summary
- Add `backfillAllPRs: true` project config flag — ao-backfill.sh handles ALL open PRs for designated projects, not just `[agento]`-tagged ones
- Auto-prune stale git worktrees before each spawn cycle to unblock branch checkouts
- Add liveness respawn (PASS 2): finds `stuck`/`killed` sessions older than 30min and respawns them
- Add `--limit 1000` to all `gh pr list` calls to prevent truncation
- Fix Python env interpolation in heredocs using `AO_CONFIG_ARG`/`PROJECT_ID_ARG` env vars
- Tab-separ

## Raw Body
## Summary
- Add `backfillAllPRs: true` project config flag — ao-backfill.sh handles ALL open PRs for designated projects, not just `[agento]`-tagged ones
- Auto-prune stale git worktrees before each spawn cycle to unblock branch checkouts
- Add liveness respawn (PASS 2): finds `stuck`/`killed` sessions older than 30min and respawns them
- Add `--limit 1000` to all `gh pr list` calls to prevent truncation
- Fix Python env interpolation in heredocs using `AO_CONFIG_ARG`/`PROJECT_ID_ARG` env vars
- Tab-separated MAPPINGS format (4 fields) to prevent field corruption when defaultBranch is empty
- Add `agent-orchestrator` fork project to yaml (jleechanorg/agent-orchestrator)
- Add global NEVER MERGE rule: agents must not run `gh pr merge`, only post 'awaiting human merge review'

## Changes
- `scripts/ao-backfill.sh` — full rewrite with backfillAllPRs, worktree prune, respawn passes
- `agent-orchestrator.yaml` — new project, NEVER MERGE agentRules, approved-and-green auto:true

## Testing
Manually verified by running ao-backfill.sh and observing sessions spawned for all open jleechanclaw PRs.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Modifies automation that spawns/stops/respawns Agent Orchestrator sessions and expands scope to all open PRs, which could create unexpected load or incorrect session lifecycle actions if misconfigured. Also changes install/bootstrap and health-check behavior, so regressions may impact developer environments and monitoring.
> 
> **Overview**
> Updates AO automation to support a per-project `backfillAllPRs` flag so `ao-backfill.sh` can spawn sessions for *all* open PRs (not just `[agento]`), with higher `gh pr list` limits and a new pre-pass that prunes stale git worktrees to unblock checkouts.
> 
> Adds/adjusts lifecycle handling: `ao-backfill.sh` now includes cleanup for merged/closed default-branch PRs and a liveness respawn pass that re-spawns `stuck`/`kille
