# PR #83: fix: reconciler false deferral, missing push instruction, notification text

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-03-10
**Author:** jleechan2015
**Stats:** +1298/-700 in 18 files

## Summary
- **reconciliation.py**: `_remote_branch_exists` returned `None` (transient) when `git fetch` failed with `"couldn't find remote ref"`, causing the reconciler to defer events indefinitely for agents that committed but never pushed. Fixed to return `False` for this definitive case.
- **dispatch_task.py**: `_task_with_push_instruction` skipped the push reminder when a task mentioned `git commit` but not `git push`. Added explicit `elif` so commit-only tasks still get the push instruction appended.

## Raw Body
## Summary

- **reconciliation.py**: `_remote_branch_exists` returned `None` (transient) when `git fetch` failed with `"couldn't find remote ref"`, causing the reconciler to defer events indefinitely for agents that committed but never pushed. Fixed to return `False` for this definitive case.
- **dispatch_task.py**: `_task_with_push_instruction` skipped the push reminder when a task mentioned `git commit` but not `git push`. Added explicit `elif` so commit-only tasks still get the push instruction appended.
- **openclaw_notifier.py**: `_notification_text` for `task_finished` now produces `"Ready for review."` matching test expectations and user-facing conventions.

## Changes (before → after → why)

| File | Before | After |
|------|--------|-------|
| `reconciliation.py` | `git fetch` fails → `None` (deferred forever) | `"couldn't find remote ref"` → `False` (not pushed) |
| `dispatch_task.py` | Task with `git commit` but no `git push` → no push reminder | Push reminder appended in `elif` branch |
| `openclaw_notifier.py` | Generic `_notification_text` missing "Ready for review." | `task_finished` produces human-readable text with "Ready for review." |

## Testing

- Found via `test_slack_loopback_roundtrip` real E2E: initially `emitted 0 events` → after bug 1 fix: `task_needs_human` → after bug 2 fix: `task_finished` → after bug 3 fix: thread reply should include "Ready for review."
- 397 non-E2E unit tests pass (`tests/` excluding E2E)
- Added: `TestRemoteBranchExists` (2 tests), `TestTaskWithPushInstruction` (5 tests)

## Known Limitations

- This branch diverged from main before PR 72; conflicts exist in `openclaw_notifier.py` and `tests/test_openclaw_notifier.py` — merging requires conflict resolution.
- The full E2E `test_slack_loopback_roundtrip` still needs `source ~/.profile && source ~/.openclaw/set-slack-env.sh` to run (real credentials required).

Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Me
