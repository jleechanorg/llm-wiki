# PR #186: docs: fix AO escalation mermaid diagram in HTML + html-always policy

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-03-16
**Author:** jleechan2015
**Stats:** +369/-206 in 3 files

## Summary
- Fix AO Escalation section in `docs/ORCHESTRATION_SYSTEM_DESIGN.html` — was showing PR Lifecycle sequenceDiagram, now has correct `flowchart TD` with semantic colors
- Add `Docs: Always Generate HTML Alongside Markdown` policy to `CLAUDE.md`

## Raw Body
## Summary
- Fix AO Escalation section in `docs/ORCHESTRATION_SYSTEM_DESIGN.html` — was showing PR Lifecycle sequenceDiagram, now has correct `flowchart TD` with semantic colors
- Add `Docs: Always Generate HTML Alongside Markdown` policy to `CLAUDE.md`

## Changes
- `docs/ORCHESTRATION_SYSTEM_DESIGN.html`: AO Escalation section now renders correct Mermaid flowchart (purple AO nodes, cyan brain, green agents, red alerts)
- `CLAUDE.md`: policy section requiring `docs/FOO.md` → `docs/FOO.html` whenever a doc is created/updated

## Testing
- Open HTML in browser — AO Escalation section renders as flowchart, not sequenceDiagram

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Documentation-only changes: updates generated HTML/Markdown diagrams and adds a docs authoring policy, with no runtime code impact. Main risk is broken rendering if the Mermaid CDN is unavailable or HTML generation drifts from the new policy.
> 
> **Overview**
> Updates `ORCHESTRATION_SYSTEM_DESIGN.html` to use Mermaid-rendered diagrams (replacing prior ASCII/incorrect content), adds a **Color Legend** section + sidebar link, and initializes Mermaid with a CDN load plus a graceful fallback message when blocked.
> 
> Cleans up `ORCHESTRATION_SYSTEM_DESIGN.md` Mermaid blocks by removing inline init directives and normalizing diagram/link formatting to align with the HTML output.
> 
> Adds a new `CLAUDE.md` policy requiring every `docs/*.md` change to be accompanied by a matching `docs/*.html` update using the orchestration doc’s dark-mode template and Mermaid setup.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 1287de26524d45cdd01bfb8b03b82008f912ebab. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->
## Summary by CodeRabbit


