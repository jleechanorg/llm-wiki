# PR #47: fix: workflow security tests + LifecyclePoller exception logging (ORCH-apz, ORCH-1g8)

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-03-05
**Author:** jleechan2015
**Stats:** +27/-5 in 3 files

## Summary
- Align workflow security tests with renamed trigger file (ORCH-apz)
- Log LifecyclePoller poll_fn exceptions instead of silently swallowing them (ORCH-1g8)

## Raw Body
## Summary
- Align workflow security tests with renamed trigger file (ORCH-apz)
- Log LifecyclePoller poll_fn exceptions instead of silently swallowing them (ORCH-1g8)

## Changes
- Workflow security test filename fix to match renamed trigger file
- src/orchestration/lifecycle_reactions.py: Added logging, replaced bare except-pass with _logger.warning(exc_info=True)
- src/tests/test_lifecycle_reactions.py: Added test_poll_fn_exception_logged

## Testing
- All 63 lifecycle tests pass
- CI validates workflow security tests

## Known Limitations
- None

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Low risk: changes are limited to logging behavior in a background poll loop and test updates for a renamed GitHub Actions workflow/permission assertion.
> 
> **Overview**
> Improves observability in `LifecyclePoller` by logging (instead of silently swallowing) exceptions thrown by the configured `poll_fn`, and adds a test to assert the warning is emitted.
> 
> Updates the PR-agent trigger workflow security tests to read the renamed workflow file (`agent-pr-fix-trigger.yml`) and to assert the stricter `permissions: {}` configuration rather than individual read scopes.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit da7fd884dd64408dcf8ebc12a2637cac09928daa. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
