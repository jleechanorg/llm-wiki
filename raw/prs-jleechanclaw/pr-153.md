# PR #153: chore: track agent-orchestrator.yaml in repo

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-03-15
**Author:** jleechan2015
**Stats:** +260/-12 in 6 files

## Summary
- Track `agent-orchestrator.yaml` in jleechanclaw repo for version control
- Add all active jleechanorg repos: worldarchitect, ralph, claude-commands, worldai-claw, mctrl-test
- Token replaced with `${OPENCLAW_AO_HOOK_TOKEN}` env var reference (was hardcoded, blocked by gitleaks)

## Raw Body
## Summary
- Track `agent-orchestrator.yaml` in jleechanclaw repo for version control
- Add all active jleechanorg repos: worldarchitect, ralph, claude-commands, worldai-claw, mctrl-test
- Token replaced with `${OPENCLAW_AO_HOOK_TOKEN}` env var reference (was hardcoded, blocked by gitleaks)

## Changes
- Merged `defaults.agentRules` from reference yaml (explicit START HERE + read-all-comments protocol)
- Added `reactions`: bugbot-comments, merge-conflicts (with resolve instructions), improved agent-stuck/agent-needs-input messages
- Added `plugins.scm-github` section
- Added `/copilot`+`/fixpr` finalization rule (only when primary code is done, not mid-implementation)
- Added `/team-mini` + PR size rules: >1000 delta → break into sub-PRs with TDD roadmap; tests committed separately from impl

## Testing
- `agent-orchestrator.yaml` at `~/agent-orchestrator.yaml` is the live config (symlink or manual sync)
- Gitleaks passes (no real tokens committed)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Medium risk because it changes local automation/launchd setup and webhook auth wiring; misconfiguration could break orchestrator reactions or backfill spawning across repos.
> 
> **Overview**
> Tracks a new repo-managed `agent-orchestrator.yaml` that defines multi-repo projects, GitHub SCM plugin config, OpenClaw webhook notifier, and reaction routing/rules (including stricter agent/PR workflow guidance).
> 
> Updates setup/docs and launchd installers to treat `~/agent-orchestrator.yaml` as the canonical config (created via symlink in `scripts/bootstrap.sh`), switch working directories to `~/.openclaw`, and require/inject `OPENCLAW_AO_HOOK_TOKEN` for AO→gateway webhook notifications (including validation and plist env vars).
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 853b97c9e4d71c872abbe4c81702ad59ff109284. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
