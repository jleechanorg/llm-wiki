# PR #539: fix(monitor): Slack E2E matrix harness fixes + staging config repair + skeptic webhook

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-04-10
**Author:** jleechan2015
**Stats:** +389/-34 in 11 files

## Summary
- **monitor-agent.sh**: Fix 4 bugs causing false-red STATUS=PROBLEM on every matrix run:
  1. Sender token precedence — `SLACK_USER_TOKEN` (human xoxp) now wins over `OPENCLAW_SLACK_USER_TOKEN` (bot-scoped)
  2. Timeout too short — `SLACK_E2E_TIMEOUT_SECONDS` now defaults to 180s (replies arrive 25-160s after probe)
  3. RUN_CANARY gate — matrix was gated behind `if [ "$RUN_CANARY" = "1" ]`, silently disabling it; gate removed
  4. Bot-authored sender detection — `slack_post_message_author_kind(

## Test Plan
- [x] `bash -n monitor-agent.sh` — syntax clean
- [x] `bash tests/test_monitor_slack_e2e_matrix.sh` — 19/19 pass (includes mixed-identity, delayed-reply, and bot-sender regression cases)
- [x] `pytest tests/test_openclaw_configs.py -k 'slack_e2e_matrix or install_launchagents or staging or monitor'` — 50/50 pass
- [x] Staging gateway confirmed on 18810 with correct config

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> T

## Raw Body
## Summary

- **monitor-agent.sh**: Fix 4 bugs causing false-red STATUS=PROBLEM on every matrix run:
  1. Sender token precedence — `SLACK_USER_TOKEN` (human xoxp) now wins over `OPENCLAW_SLACK_USER_TOKEN` (bot-scoped)
  2. Timeout too short — `SLACK_E2E_TIMEOUT_SECONDS` now defaults to 180s (replies arrive 25-160s after probe)
  3. RUN_CANARY gate — matrix was gated behind `if [ "$RUN_CANARY" = "1" ]`, silently disabling it; gate removed
  4. Bot-authored sender detection — `slack_post_message_author_kind()` flags bot/app top-level no-mention probes as `invalid_sender_*` (rc=7) rather than false product failures

- **scripts/install-launchagents.sh**: Staging overlay now forces `gateway.port=18810`, keeps Slack enabled, removes unsupported `stagingDerivedFrom` key

- **scripts/doctor.sh**: Adds `check_shared_slack_socket_tokens()` — warns when staging and prod share Slack tokens

- **scripts/check-pr-worker-coverage.sh + src/orchestration/webhook.py**: Skeptic webhook trigger and coverage diagnostics (original branch work)

## Test plan

- [x] `bash -n monitor-agent.sh` — syntax clean
- [x] `bash tests/test_monitor_slack_e2e_matrix.sh` — 19/19 pass (includes mixed-identity, delayed-reply, and bot-sender regression cases)
- [x] `pytest tests/test_openclaw_configs.py -k 'slack_e2e_matrix or install_launchagents or staging or monitor'` — 50/50 pass
- [x] Staging gateway confirmed on 18810 with correct config

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Touches operational monitoring and deployment safety checks (Slack token classification, launchd profile config, and webhook-triggered `ao skeptic` dispatch), so misconfigurations could create false alarms or missed/extra automation runs. Changes are mostly additive/guarded with new tests, but they affect live reliability gates.
> 
> **Overview**
> **Reduces false failures in the Slack E2E matrix and tightens staging/prod safety checks.** The
