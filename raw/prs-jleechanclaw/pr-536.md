# PR #536: [P2] Policy: require captioned tmux/UI evidence videos + PR gist summaries

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-04-09
**Author:** jleechan2015
**Stats:** +32/-0 in 2 files

## Summary
(none)

## Raw Body
### Background
Evidence standards were partially defined but did not consistently require captioned videos, GitHub-native attachment URLs, sanitized output paths, or PR-level test/gist summaries. This made evidence review inconsistent across agents.

### Goals
1. Require captioned video evidence for terminal and browser claims.
2. Make browser UI video mandatory for user-facing changes.
3. Require GitHub native PR video attachments and standardized PR evidence summary fields.
4. Prevent machine-specific path leakage in published evidence.

### Tenets
- Evidence must be reviewable directly in PR context.
- Published artifacts should be portable and privacy-safe.
- User-facing claims require user-facing proof.

### High-level description of changes
- Added a repo-scope evidence media policy in `CLAUDE.md` under PR/evidence criteria.
- Added matching repo-scope policy in `workspace/AGENTS.md` so non-Claude agents see the same requirement baseline.

### Testing
- Manual verification:
  - Confirmed both files render and include new mandatory requirements for:
    - tmux video
    - UI video for user-facing changes
    - captions for both
    - GitHub `user-attachments` URLs
    - PR summary + self-contained gist URL
    - machine-path redaction
- No runtime code paths changed (docs/policy-only update).

### Low-level details (up to 20 files)
- **File path**: `CLAUDE.md`
  - Added `Evidence Media Requirements (Repo Scope)` section in the PR & merge protocol block.
  - Codifies mandatory captioned tmux/UI evidence videos, attachment URL expectations, PR summary + gist requirements, and path redaction.
- **File path**: `workspace/AGENTS.md`
  - Added `Evidence Media Policy (Repo Scope)` section.
  - Mirrors the same rules for agents reading AGENTS policy first.

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Low risk documentation-only change that tightens PR evidence requirements; no runtime code paths or automation logic are modified.
> 
> **Overview**
> Adds a *
