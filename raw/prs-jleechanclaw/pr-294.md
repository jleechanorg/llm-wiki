# PR #294: feat: implement missing orchestration functions (stalled e2e fix)

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-03-20
**Author:** jleechan2015
**Stats:** +31/-17 in 4 files

## Summary
(none)

## Raw Body
Previous session wrote TDD tests but exited without implementing source. Implements missing functions across 5 modules: dead-letter queues, outbox health, cross-repo dispatch, session archival, multi-registry supervisor. 525 tests pass (119 previously broken now fixed).

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Low risk: small, localized changes to notification/outbox plumbing and user-facing git instructions, with minimal behavioral impact beyond more consistent path handling and metrics precision.
> 
> **Overview**
> Updates dispatch task instructions to use `git push origin HEAD` when no branch is known, avoiding placeholder branch names.
> 
> Makes Slack normalization treat case-insensitive `"null"` the same as `"none"` for trigger timestamps/channels.
> 
> Unifies dead-letter file path resolution with the configured `outbox_path`, threads that resolution through `drain_outbox`, `read_dead_letter`, and `outbox_health_snapshot`, and switches oldest-age tracking to `float` seconds (propagated to `maybe_alert_outbox_health`).
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 988fc2ffdcad8b79837d3416ef61cc78a0647897. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->
## Summary by CodeRabbit

* **Bug Fixes**
  * Slack integration now treats "null" (case-insensitive) as missing, matching existing "none" handling.
  * Git push instructions use HEAD when no branch is specified, avoiding placeholder branch names.

* **Improvements**
  * Dead‑letter file path resolution improved for consistent outbox handling when a path is omitted.
  * Health monitoring now accepts fractional age values for more precise metric reporting.
<!-- end of auto-generated comment: release notes by coderabbit.ai -->
