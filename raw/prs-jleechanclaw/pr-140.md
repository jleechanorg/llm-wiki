# PR #140: fix(mem0): expose infer param + raise MemoryWriteError on silent drop

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-03-14
**Author:** jleechan2015
**Stats:** +242/-46 in 4 files

## Summary
- `mem0.add(infer=True)` (default) silently returned `{}` when gpt-4o-mini refused to extract facts — no error, no way to detect the drop
- Added `MemoryWriteError` raised when `infer=True` returns empty results
- Exposed `infer` param on `add_memory()` so callers can opt into guaranteed writes with `infer=False`

## Raw Body
## Summary

- `mem0.add(infer=True)` (default) silently returned `{}` when gpt-4o-mini refused to extract facts — no error, no way to detect the drop
- Added `MemoryWriteError` raised when `infer=True` returns empty results
- Exposed `infer` param on `add_memory()` so callers can opt into guaranteed writes with `infer=False`

## Changes

| | Before | After |
|---|---|---|
| Silent drop | `add_memory(text)` → `{"results": []}`, nothing stored | Raises `MemoryWriteError` with actionable message |
| Guaranteed write | Not possible | `add_memory(text, infer=False)` → `event:ADD`, score 0.967 on recall |
| CLI | No control | `--no-infer` flag bypasses LLM extraction |

## Testing

New test doc: `testing_llm/mem0_recall_test_20260314.md` — 4 tests, all PASS:

| Test | Result |
|------|--------|
| T1 — Connectivity | PASS (103 points, correct host/collection) |
| T2 — Semantic recall | PASS (scores 0.683 / 0.785, correct facts) |
| T3 — Add→recall roundtrip | PASS (`infer=False` score 0.967; `infer=True` raises `MemoryWriteError`) |
| T4 — Legacy filter | PASS (filter logic correct) |

Bead: `orch-palt` (closed)

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Changes default `add_memory()` behavior to raise on empty `infer=True` results, which could break existing callers that relied on silent success. Scope is limited to the shared mem0 client/CLI and docs, with no broad architectural refactors.
> 
> **Overview**
> Prevents silent data loss in `scripts/mem0_shared_client.py` by adding `MemoryWriteError` and raising it when `mem0.add(..., infer=True)` returns no `results` (LLM refusal/dedup), instead of returning an empty payload.
> 
> Adds an `infer` parameter to `add_memory()` (default `True`) plus a CLI `add --no-infer` flag to bypass LLM extraction and force verbatim writes.
> 
> Includes a new `testing_llm/mem0_recall_test_20260314.md` evidence doc for connectivity, recall, and roundtrip behav
