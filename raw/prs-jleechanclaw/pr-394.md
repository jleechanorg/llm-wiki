# PR #394: fix(mem0): correct config — ollama nomic-embed-text embedder + groq LLM (match existing collection)

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-03-24
**Author:** jleechan2015
**Stats:** +11/-10 in 3 files

## Summary
PR #389 merged the mem0 hooks but with wrong config values — `text-embedding-3-small` (1536 dims, OpenAI) and `gpt-4o-mini`. The actual `openclaw_mem0` Qdrant collection was built with `nomic-embed-text` (768 dims, Ollama) and the stack uses Groq for LLM inference.

## Background
PR #389 merged the mem0 hooks but with wrong config values — `text-embedding-3-small` (1536 dims, OpenAI) and `gpt-4o-mini`. The actual `openclaw_mem0` Qdrant collection was built with `nomic-embed-text` (768 dims, Ollama) and the stack uses Groq for LLM inference.

## Raw Body
## Background

PR #389 merged the mem0 hooks but with wrong config values — `text-embedding-3-small` (1536 dims, OpenAI) and `gpt-4o-mini`. The actual `openclaw_mem0` Qdrant collection was built with `nomic-embed-text` (768 dims, Ollama) and the stack uses Groq for LLM inference.

## Goals

Make the hooks actually work against the existing collection.

## Changes

- `embedding_model_dims`: 1536 → 768
- Embedder: openai/text-embedding-3-small → ollama/nomic-embed-text (localhost:11434)
- LLM: openai/gpt-4o-mini → groq/llama-3.3-70b-versatile (GROQ_API_KEY)

No OpenAI key needed at all. Verified recall returns real memories (scores 0.70–0.79).

## Testing

```
echo '{"prompt": "mem0 architecture qdrant"}' | python3 ~/.claude/hooks/mem0_recall.py
# Returns 6 real memories from openclaw_mem0 with scores 0.70-0.79 ✅
```

## Low-level details

- **.claude/hooks/mem0_config.py**: Two config corrections — embedder provider/model/dims, LLM provider/model/api_key env var.

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Changes the embedding dimensions and swaps providers/credentials for both embedding and LLM inference, which can break memory search/save if the local Ollama or Groq config is wrong.
> 
> **Overview**
> Fixes the mem0 hook configuration to match the existing Qdrant `openclaw_mem0` collection by switching embeddings to **Ollama** `nomic-embed-text` and updating `embedding_model_dims` from `1536` to `768`.
> 
> Moves LLM inference from **OpenAI** to **Groq** (`llama-3.3-70b-versatile`) and updates `mem0_recall.py`/`mem0_save.py` to gate execution on `GROQ_API_KEY` (no longer `OPENAI_API_KEY`).
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit d39ce3dd37f51ed0e4e23bcbeffde1478e5df973. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
