# PR #435: chore: move coderabbit-ping-on-push to self-hosted runner

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-03-29
**Author:** jleechan2015
**Stats:** +1/-1 in 1 files

## Summary
- Moves `coderabbit-ping-on-push.yml` from `ubuntu-latest` to self-hosted runner
- Completes full self-hosted migration for all non-trivial private repo workflows
- Uses `vars.SELF_HOSTED_RUNNER_LABELS` override pattern consistent with other workflows

## Test Plan
- [ ] Workflow picks up self-hosted runner on next push to a non-main branch
- [ ] CR ping comment still posts successfully

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Low risk: only changes the GitHub Actions runner selection for an existing workflow, with no code or permission changes. Main risk is workflow execution failing if the self-hosted runner labels/availability are misconfigured.
> 
> **Overview**
> Switches 

## Raw Body
## Summary
- Moves `coderabbit-ping-on-push.yml` from `ubuntu-latest` to self-hosted runner
- Completes full self-hosted migration for all non-trivial private repo workflows
- Uses `vars.SELF_HOSTED_RUNNER_LABELS` override pattern consistent with other workflows

## Test plan
- [ ] Workflow picks up self-hosted runner on next push to a non-main branch
- [ ] CR ping comment still posts successfully

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Low risk: only changes the GitHub Actions runner selection for an existing workflow, with no code or permission changes. Main risk is workflow execution failing if the self-hosted runner labels/availability are misconfigured.
> 
> **Overview**
> Switches the `coderabbit-ping-on-push` GitHub Actions workflow from `ubuntu-latest` to a self-hosted runner, using `vars.SELF_HOSTED_RUNNER_LABELS` (with a default label set) to select the runner dynamically.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 63f1aa3868cb8547f32d54784411c88ac5ed5c58. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
