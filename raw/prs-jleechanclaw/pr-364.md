# PR #364: feat: add workspace observability report (weekly Monday 10am PT)

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-03-23
**Author:** jleechan2015
**Stats:** +635/-9 in 5 files

## Summary
- New `scripts/workspace-report.sh` — weekly jleechanclaw workspace health audit across 8 dimensions (worktrees, file parity, backups, launchd jobs, sessions, gateway probe, open PRs, evidence metrics)
- Output: dated markdown report with **exec summary + top-3 recommendations at the top**, full detail below
- Exit 1 when failures detected (suitable for monitoring automation); configurable via `OPENCLAW_WORKSPACE_REPORT_OUTDIR` and `OPENCLAW_WORKSPACE_REPORT_CHANNEL`
- New `ai.openclaw.schedule.

## Test Plan
- [x] Smoke test: `bash scripts/workspace-report.sh` — all 8 sections produce correct output
- [x] Shellcheck: info-level only (no errors/warnings)
- [x] `PYTHONPATH=src python -m pytest tests/ -q` — 208 passed, 82 skipped

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Adds a new scheduled job and a large shell script that probes local services and optionally posts to Slack/GitHub, so failures/missing dependencies could

## Raw Body
## Summary
- New `scripts/workspace-report.sh` — weekly jleechanclaw workspace health audit across 8 dimensions (worktrees, file parity, backups, launchd jobs, sessions, gateway probe, open PRs, evidence metrics)
- Output: dated markdown report with **exec summary + top-3 recommendations at the top**, full detail below
- Exit 1 when failures detected (suitable for monitoring automation); configurable via `OPENCLAW_WORKSPACE_REPORT_OUTDIR` and `OPENCLAW_WORKSPACE_REPORT_CHANNEL`
- New `ai.openclaw.schedule.workspace-report.plist` — launchd schedule (Monday 10am PT)
- Fix `install-launchagents.sh` and `install-openclaw-scheduled-jobs.sh`: both referenced `CONFIG_DIR="$REPO_DIR/openclaw-config"` — that directory was deleted in `4e57fa88`

## Test plan
- [x] Smoke test: `bash scripts/workspace-report.sh` — all 8 sections produce correct output
- [x] Shellcheck: info-level only (no errors/warnings)
- [x] `PYTHONPATH=src python -m pytest tests/ -q` — 208 passed, 82 skipped

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Adds a new scheduled job and a large shell script that probes local services and optionally posts to Slack/GitHub, so failures/missing dependencies could create noisy alerts or nonzero exits in automation.
> 
> **Overview**
> Adds a new weekly *Workspace Observability Report* job (`scripts/workspace-report.sh`) that generates a dated markdown report with an executive summary and issues list by auditing worktrees, policy-file parity, backups, launchd registration/state, AO/tmux sessions, gateway health, open PR/CI status via `gh`, and evidence bundle recency; it can optionally post the summary to Slack and exits nonzero on failures.
> 
> Introduces new launchd plists/templates to run the report every Monday at 10am PT and logs stdout/stderr to `~/.openclaw/logs/scheduled-jobs/`.
> 
> Simplifies `install-launchagents.sh` and `install-openclaw-scheduled-jobs.sh` to always treat the re
