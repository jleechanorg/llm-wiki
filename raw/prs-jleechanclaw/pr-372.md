# PR #372: harness: merge policy fix + /er re-run guard + autonomy beads

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-03-23
**Author:** jleechan2015
**Stats:** +15/-8 in 2 files

## Summary
(none)

## Raw Body
### Background
After `/auton` diagnostic revealed two critical harness gaps:
1. Workers posting green signal but nothing merging (no merge executor)
2. Workers stuck because /er re-run guard blocked re-verification after policy updates

Also adds orch-jp8u, orch-7px1, orch-89sa beads documenting autonomy learnings.

### Goals
- Workers that post 6/6 green signal now enable auto-merge (or direct merge) instead of waiting forever
- Workers can re-run /er when signal is stale (>2h) or new commits pushed
- Learning beads committed to main for future reference

### Tenets
- Minimal scope: only fix documented harness gaps
- Fail-closed: workers must still verify 6/6 before merging

### High-level description of changes
- `agent-orchestrator.yaml`: Updated merge policy comment — replaced "NEVER run gh pr merge" (no executor existed) with instruction to run `gh pr merge --squash --auto` after green signal
- `agent-orchestrator.yaml`: Relaxed /er re-run guard from "never re-run if PASS posted" to "skip only if PASS was posted <2h ago with no new commits"
- `.beads/issues.jsonl`: Add orch-jp8u (empty-body CR APPROVED fix), orch-7px1 (ao rebuild fix), orch-89sa (trivial PR CR behavior)

### Testing
- Workers jc-485, jc-488, jc-491, jc-492, jc-499 observed in tmux after update
- PR #368 (orch-jp8u fix) merged with empty-body CR APPROVED fix applied
- Merge policy tested: auto-merge enabled on #360, #362, #366 (auto-merge not supported on repo, so direct merge used)

### Low-level details
- **agent-orchestrator.yaml**: Lines ~52-54 merge policy; lines ~375 /er guard
- **.beads/issues.jsonl**: 3 new learning beads

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Changes modify agent merge behavior guidance to have workers run `gh pr merge --auto` after posting a green signal, which can directly affect PR merge flow if misapplied. Also tweaks the evidence-review re-run guard, potentially changing when agents re-
