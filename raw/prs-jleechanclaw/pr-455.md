# PR #455: [agento] harness: no direct ~/.openclaw/ edits rule (staging canary gate)

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-03-31
**Author:** jleechan2015
**Stats:** +8/-1 in 1 files

## Summary
PR #454 merged the staging canary. Direct edits to ~/.openclaw/ bypass it entirely, making the gate ineffective.

## Background
PR #454 merged the staging canary. Direct edits to ~/.openclaw/ bypass it entirely, making the gate ineffective.

## Raw Body
## Background
PR #454 merged the staging canary. Direct edits to ~/.openclaw/ bypass it entirely, making the gate ineffective.

## Goals
Add an explicit rule in CLAUDE.md covering all agent sessions (not just worktrees): changes to ~/.openclaw/ must go through a PR so the staging canary can catch regressions.

## Tenets
- Three narrow exceptions preserved (openclaw.json surgical updates, cron/jobs.json, emergency hot-fixes)
- Rule applies to ALL agents (Claude, Codex, Cursor, etc.) via CLAUDE.md

## High-level description of changes
Expanded the "Worktree Isolation" section in CLAUDE.md to cover all agent sessions, not just worktree-based ones. Added explicit list of permitted exceptions.

## Testing
Documentation change only — no code changed.

## Low-level details
- **CLAUDE.md**: Expanded worktree isolation rule from "worktree sessions only" to "all agent sessions". Added permitted exceptions list.

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Low risk: documentation-only change that tightens operational process around editing `~/.openclaw/`, with no runtime code or config logic modified.
> 
> **Overview**
> **Strengthens the harness editing policy** by expanding the “Worktree Isolation” guidance in `CLAUDE.md` into an **all-agent-session rule**: do not edit `~/.openclaw/` directly; make changes via worktree → commit → PR → merge → pull so the staging canary (`scripts/staging-canary.sh`) is not bypassed.
> 
> Adds an explicit **three-item exception list** (`openclaw.json` surgical updates, live `cron/jobs.json` management, and user-authorized emergency hotfixes with follow-up PR) and clarifies the expected workflow for updating live config.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 9e7abbd661581cb9591a533b20c64a17b2220e37. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
