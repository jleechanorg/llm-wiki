# PR #344: [P2] chore: delete ao-pr-poller.sh and all references

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-03-21
**Author:** jleechan2015
**Stats:** +13/-139 in 9 files

## Summary
The `ao-pr-poller.sh` script was the original shell-script-based PR polling path for the AO orchestration system. It has been superseded by the **canonical scheduling path: OpenClaw gateway cron + AO lifecycle-worker**. References to the deprecated script remain scattered across the codebase, creating confusion about which scheduling path is current.

## Background
The `ao-pr-poller.sh` script was the original shell-script-based PR polling path for the AO orchestration system. It has been superseded by the **canonical scheduling path: OpenClaw gateway cron + AO lifecycle-worker**. References to the deprecated script remain scattered across the codebase, creating confusion about which scheduling path is current.

## Raw Body
## Background

The `ao-pr-poller.sh` script was the original shell-script-based PR polling path for the AO orchestration system. It has been superseded by the **canonical scheduling path: OpenClaw gateway cron + AO lifecycle-worker**. References to the deprecated script remain scattered across the codebase, creating confusion about which scheduling path is current.

## Goals

- Remove all references to `ao-pr-poller.sh` from the jleechanclaw repo
- Ensure documentation points to the canonical scheduling path (OpenClaw gateway cron)
- Clean up legacy retirement logic that referenced the deprecated script

## Changes

| File | Change |
|------|--------|
| `roadmap/AO_PR_POLLLER_DESIGN.md` | **DELETED** — entire file was the design doc for the deprecated script |
| `roadmap/AO_VS_PYTHON_ORCHESTRATION.md` | Removed `ao-pr-poller.sh` section and all references; replaced with OpenClaw gateway cron as canonical scheduling |
| `roadmap/BUG_FIX_PLAN_2026_03_16.md` | Removed PR 5 section (ao-pr-poller hardening) |
| `roadmap/PR_REVIEW_2026-03-16.md` | Removed `ao-pr-poller.sh` jq fix mention |
| `roadmap/EVIDENCE_REVIEW_SCHEMA.md` | Updated implementation note to reference canonical scheduling |
| `scripts/install-ao-lifecycle-agent-orchestrator.sh` | Removed `_retire_legacy_ao_pr_poller()` function and its invocation |
| `scripts/install.sh` | Removed `ai.ao-pr-poller` from legacy cleanup loop; updated comment to reference canonical scheduling |
| `skills/agento/SKILL.md` | Removed legacy script references from rate-limit handling paragraph |
| `testing_llm/mem0_postingest_recall_20260316.md` | Removed script name from historical test result log |

## Testing

- `bash -n` syntax check: both modified shell scripts pass
- `rg "ao-pr-poller" .` confirms zero remaining references in repo

## Verification

- No `ao-pr-poller` references remain anywhere in the repo (confirmed via `rg`)
- Shell scripts pass `bash -n` syntax validation
- Documentation updated to reference canonical 
