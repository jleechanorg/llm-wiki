# PR #502: refactor(claw): replace Python task-classifier with LLM judgment

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-04-05
**Author:** jleechan2015
**Stats:** +26/-75 in 1 files

## Summary
- Remove Python inline script for task classification (coding vs read_only)
- Remove Python inline script for ISSUE_ID extraction from task description
- Replace both with LLM judgment (the LLM already has full context)
- Reduces shell complexity and removes fragile regex matching

## Test Plan
- [ ] Verify /claw still correctly classifies a coding task (e.g. "fix orch-sq2")
- [ ] Verify /claw still correctly classifies a read-only task (e.g. "what time is it")
- [ ] Verify ISSUE_ID is correctly extracted from task description

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Behavior changes from deterministic regex/Python checks to human/LLM judgment for task classification, issue ID extraction, runtime overrid

## Raw Body
## Summary

- Remove Python inline script for task classification (coding vs read_only)
- Remove Python inline script for ISSUE_ID extraction from task description
- Replace both with LLM judgment (the LLM already has full context)
- Reduces shell complexity and removes fragile regex matching

## Test plan

- [ ] Verify /claw still correctly classifies a coding task (e.g. "fix orch-sq2")
- [ ] Verify /claw still correctly classifies a read-only task (e.g. "what time is it")
- [ ] Verify ISSUE_ID is correctly extracted from task description

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Behavior changes from deterministic regex/Python checks to human/LLM judgment for task classification, issue ID extraction, runtime override, and `/learn` gating, which can alter routing and dispatch behavior if misjudged.
> 
> **Overview**
> Switches `/claw` routing logic from inline Python/regex detection to *LLM/manual judgment* for classifying tasks (`coding` vs `read_only`), extracting `ISSUE_ID`, and detecting the antigravity runtime override.
> 
> Updates the learning-loop gate to *fail-closed* guidance (append `/learn` unless confidently already covered) and removes the fallback `ao session ls` heuristics that could pick unrelated sessions in parallel mode, opting to error/warn for manual recovery instead.
> 
> <sup>Reviewed by [Cursor Bugbot](https://cursor.com/bugbot) for commit 61c0c82baa5203b56445f5579a6f4c9c5c134cdc. Bugbot is set up for automated code reviews on this repo. Configure [here](https://www.cursor.com/dashboard/bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->

## Summary by CodeRabbit

* **Refactor**
  * Simplified automated detection logic in command tooling; removed pattern-matching inference
  * Session name detection now explicitly fails with warning instead of attempting automatic inference
  * Learning loop behav
