# PR #212: feat: add escalation anomaly detector

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-03-16
**Author:** jleechan2015
**Stats:** +599/-2 in 4 files

## Summary
Add escalation anomaly detection to monitor action_log.jsonl for patterns.

### Changes

1. **src/orchestration/anomaly_detector.py** - New module that:
   - Reads ~/.openclaw/state/action_log.jsonl
   - Counts escalations per error_class in rolling 7-day window
   - Sends Slack DM via openclaw_notifier.py if any error_class has >=2 escalations
   - Provides summary with error class, count, first/last seen, recent reasons

2. **src/tests/test_anomaly_detector.py** - 14 tests covering:
   - File 

## Raw Body
## Summary

Add escalation anomaly detection to monitor action_log.jsonl for patterns.

### Changes

1. **src/orchestration/anomaly_detector.py** - New module that:
   - Reads ~/.openclaw/state/action_log.jsonl
   - Counts escalations per error_class in rolling 7-day window
   - Sends Slack DM via openclaw_notifier.py if any error_class has >=2 escalations
   - Provides summary with error class, count, first/last seen, recent reasons

2. **src/tests/test_anomaly_detector.py** - 14 tests covering:
   - File reading and malformed line handling
   - Escalation filtering by action type and time window
   - Error class grouping and counting
   - Anomaly detection with configurable threshold
   - Message formatting

3. **Cron job** - Added weekly job (Monday 09:00 America/Los_Angeles):
   - Job ID: 07f96e65-fb13-4c8a-8b32-1e046fc52fe7
   - Runs python -m orchestration.anomaly_detector --dry-run
   - Session target: isolated

### Testing

All 14 tests pass

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Introduces new log-parsing and Slack notification behavior, which could generate noisy/incorrect alerts if filtering or thresholds are wrong. Also adjusts PR poller respawn capping logic, affecting how often agents get spawned over time.
> 
> **Overview**
> Adds `orchestration/anomaly_detector.py`, which scans the last 7 days of `action_log.jsonl`, groups escalation-like actions by `error_class`, and sends a Slack DM (via `notify_slack_outbox_alert`) when any class meets/exceeds the threshold.
> 
> Updates `ao-pr-poller.sh` so `inc_cap` resets the respawn counter when the 12-hour window has expired (matching `check_cap`), preventing stale windows from permanently inflating counts. Includes a new `test_anomaly_detector.py` suite covering log parsing, escalation filtering/grouping, anomaly detection, and message formatting, plus a `MEMORY.md` notes update.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 06bd3686cfa599d20808
