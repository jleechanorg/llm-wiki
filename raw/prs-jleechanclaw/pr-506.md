# PR #506: [agento] feat(eloop): custom jleechanclaw eloop — drain dropped Slack thread backlog via /claw

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-04-05
**Author:** jleechan2015
**Stats:** +195/-1 in 2 files

## Summary
(none)

## Raw Body
Non-conflicting with #503 (different YAML keys). Supersedes #503 only if merge conflict.

### Background
jleechanclaw AO orchestrator has generic evolveLoop but no project-specific loop. Dropped Slack threads (no openclaw bot reply) accumulate. This adds a custom eloop that drains them.

### Goals
Drain dropped Slack thread backlog via /claw (newest-first, max 50). Fix openclaw issues. Propose new work items.

### Tenets
State-tracked. Concurrency-safe (max 3/cycle, skip if 5+ active sessions). Self-limiting at 50 items.

### High-level description of changes
1. skills/jleechanclaw-eloop.md — full 7-phase loop: load state, find dropped threads, dispatch via /claw, fix openclaw issues, propose beads, update state, recap
2. agent-orchestrator.yaml — jleechanclaw orchestratorRules gets CUSTOM ELOOP section pointing to skill

### Testing
YAML validated via python3 yaml.safe_load. Skill has executable bash commands.

### Low-level details
- skills/jleechanclaw-eloop.md: searches C0AJ3SD5C79, C0AKALZ4CKW, C09GRLXF9GR; detects threads with no bot reply; state at ~/.openclaw/workspace/claw-backlog-progress.json
- agent-orchestrator.yaml: 7-line orchestratorRules addition pointing to skill file

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Adds a new orchestrator-triggered workflow that can automatically spawn sessions and create beads based on Slack backlog and health checks, which could increase automation load or generate unintended work items if misconfigured.
> 
> **Overview**
> Adds a **project-specific custom evolve loop** for `jleechanclaw` that targets “dropped” Slack threads (no OpenClaw bot reply) and turns them into `/claw` work items with stateful progress tracking and concurrency limits.
> 
> Updates `agent-orchestrator.yaml` to reference the new loop each poll cycle, and introduces `skills/jleechanclaw-eloop.md` documenting the multi-phase process (state load/cleanup, Slack scanning, dispatching new sessions, running a staging canary, proposing f
