# PR #328: [P1] fix: delete retired test files, fix stuck threshold, fix dashboard

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-03-21
**Author:** jleechan2015
**Stats:** +15/-5421 in 18 files

## Summary
After PR #323 retired 19 Python orchestration modules, 14 test files and 2 test classes were left behind — all failing on import. The agent-stuck threshold (10m) was too aggressive, causing jc-434 to be killed mid-green-loop on PR #326 before it could see CR approval. The AO dashboard plist was broken (wrong node path, dev mode crash).

## Background
After PR #323 retired 19 Python orchestration modules, 14 test files and 2 test classes were left behind — all failing on import. The agent-stuck threshold (10m) was too aggressive, causing jc-434 to be killed mid-green-loop on PR #326 before it could see CR approval. The AO dashboard plist was broken (wrong node path, dev mode crash).

## Raw Body
## Background
After PR #323 retired 19 Python orchestration modules, 14 test files and 2 test classes were left behind — all failing on import. The agent-stuck threshold (10m) was too aggressive, causing jc-434 to be killed mid-green-loop on PR #326 before it could see CR approval. The AO dashboard plist was broken (wrong node path, dev mode crash).

## Goals
- All tests green (0 failures)
- Workers survive long enough to complete the green loop
- Dashboard launchd service starts reliably

## Tenets
- Delete dead code, don't fix it
- Config-level fix for stuck threshold (no AO code changes)
- Minimal scope

## High-level description of changes
1. Deleted 14 test files testing retired modules
2. Stripped retired test classes from test_evidence.py
3. Added skipif for qdrant_client-dependent tests
4. Raised agent-stuck threshold 10m to 30m, escalateAfter 20m to 1h
5. Fixed dashboard plist — nvm node path, next start production mode

## Testing
- `python -m pytest tests/` — 205 passed, 81 skipped, 0 failures
- `python -m pytest src/tests/` — 198 passed, 32 skipped, 0 failures
- Dashboard verified serving HTTP 200 on localhost:3020

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Mostly deletes tests and adjusts a local `launchd` plist; functional runtime code isn’t materially changed, with the main risk being reduced regression coverage from removing large test suites.
> 
> **Overview**
> Cleans up the test suite by **deleting multiple test files** that referenced now-retired `orchestration/*` Python modules, and by removing the corresponding webhook/lifecycle parsing coverage blocks from `tests/test_evidence.py`.
> 
> Makes `src/tests/test_ollama_embedder.py` more environment-tolerant by skipping the OpenAI and Qdrant-backed search tests when `qdrant_client` isn’t installed.
> 
> Updates `launchd/ai.agento.dashboard.plist` to start the dashboard via `npx next start` on port `3020`, with an explicit nvm Node path, `PORT` env var, and `WorkingDirectory` set to th
