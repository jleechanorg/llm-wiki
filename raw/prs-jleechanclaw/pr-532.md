# PR #532: [P2] feat: jleechanclaw custom eloop skill path (orch-eei)

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-04-07
**Author:** jleechan2015
**Stats:** +72/-3 in 7 files

## Summary
AO `orchestratorRules` and operators expect the dropped-thread eloop at `~/.openclaw/skills/jleechanclaw-eloop.md`, but bootstrap never created that path. This PR wires the canonical repo file (`skills/jleechanclaw-eloop.md`) via symlink and adds a Claude Code skill entry for discovery.

## Raw Body
## Summary
AO `orchestratorRules` and operators expect the dropped-thread eloop at `~/.openclaw/skills/jleechanclaw-eloop.md`, but bootstrap never created that path. This PR wires the canonical repo file (`skills/jleechanclaw-eloop.md`) via symlink and adds a Claude Code skill entry for discovery.

## Changes
- `scripts/bootstrap.sh`: mkdir `~/.openclaw/skills` and `ln -sf` to `skills/jleechanclaw-eloop.md`; add `--eloop-skill-only` for a lightweight refresh.
- `.claude/skills/jleechanclaw-eloop/SKILL.md`: pointer to the full procedure in `skills/jleechanclaw-eloop.md`.
- `skills/jleechanclaw-eloop.md`: document canonical paths (repo vs post-bootstrap symlink).
- `agent-orchestrator.yaml`: note that the runtime path is symlinked from the repo via bootstrap.
- `.claude/skills/evolve_loop/SKILL.md`: cross-link the jleechanclaw eloop skill folder.

## Testing
- `bash scripts/bootstrap.sh --eloop-skill-only` — verified symlink created.
- `PYTHONPATH=src python -m pytest tests/test_genesis_config.py ...` — **not present in this repo** (paths from task template); ran `pytest tests/test_backup_scripts.py tests/test_openclaw_configs.py -q` instead (36 passed).
- YAML load check on `agent-orchestrator.yaml`.

## Tracker
Session / branch label **orch-eei** (beads `orch-1dvu` closed with delivery note).

Made with [Cursor](https://cursor.com)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Medium risk because it changes `scripts/bootstrap.sh`, which runs during installs/updates and writes into user state (`~/.openclaw/skills`). The change is straightforward (symlink + new flag) but could break environments if paths differ or the skill file is missing.
> 
> **Overview**
> Ensures the `jleechanclaw-eloop` procedure is available at the runtime path expected by AO/orchestrator rules by having `scripts/bootstrap.sh` create `~/.openclaw/skills/jleechanclaw-eloop.md` as a symlink to `skills/jleechanclaw-eloop.md`, plus a new `--eloop-skill-only` mode for refreshing just t
