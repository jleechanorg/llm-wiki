# PR #397: fix(roadmap): preserve leading dash in project_key (remove .lstrip)

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-03-25
**Author:** jleechan2015
**Stats:** +2/-2 in 1 files

## Summary
PR #391 introduced Claude auto-memory read/write into `/roadmap` and `/r` commands. During review of PR #390, CodeRabbit identified that `.lstrip('-')` incorrectly strips the leading dash from Claude project directory paths.

Claude project dirs are named `-Users-jleechan-...` (WITH leading dash). The `.lstrip('-')` call strips it, causing memory writes to go to `~/.claude/projects/Users-jleechan-...` instead of `~/.claude/projects/-Users-jleechan-...`.

This fix was applied to the 5 other comma

## Background
PR #391 introduced Claude auto-memory read/write into `/roadmap` and `/r` commands. During review of PR #390, CodeRabbit identified that `.lstrip('-')` incorrectly strips the leading dash from Claude project directory paths.

Claude project dirs are named `-Users-jleechan-...` (WITH leading dash). The `.lstrip('-')` call strips it, causing memory writes to go to `~/.claude/projects/Users-jleechan-...` instead of `~/.claude/projects/-Users-jleechan-...`.

This fix was applied to the 5 other commands in PR #390, but roadmap.md was part of the already-merged PR #391, so it needs a separate follow-up.

## Raw Body
## Background
PR #391 introduced Claude auto-memory read/write into `/roadmap` and `/r` commands. During review of PR #390, CodeRabbit identified that `.lstrip('-')` incorrectly strips the leading dash from Claude project directory paths.

Claude project dirs are named `-Users-jleechan-...` (WITH leading dash). The `.lstrip('-')` call strips it, causing memory writes to go to `~/.claude/projects/Users-jleechan-...` instead of `~/.claude/projects/-Users-jleechan-...`.

This fix was applied to the 5 other commands in PR #390, but roadmap.md was part of the already-merged PR #391, so it needs a separate follow-up.

## Goals
Remove `.lstrip('-')` from both `project_key` derivations in `roadmap.md`.

## Tenets
- Minimal scope: two-line fix only

## High-level description of changes
Both occurrences of `.lstrip('-')` removed from `project_key` computation in `roadmap.md`.

## Testing
Manual: memory files now written to path with correct leading dash.

## Low-level details
- **.claude/commands/roadmap.md**: Lines 22 and 136 — remove `.lstrip('-')`, add comment.

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Low risk: a two-line change to path string normalization for Claude memory read/write, affecting only where roadmap memories are looked up and stored.
> 
> **Overview**
> Updates `.claude/commands/roadmap.md` to stop stripping leading `-` from the derived `project_key`, ensuring Claude auto-memory reads/writes target the correct `~/.claude/projects/-Users-.../memory/` directory.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 9ef7d1bf219562b6dbb1f32aab12aa7511469071. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->

## Summary by CodeRabbit

* **Bug Fixes**
  * Corrected project key derivation to preserve leading dashes in path transformations, ensuring prope
