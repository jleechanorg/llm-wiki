# PR #316: doctor: make legacy cron non-fatal + AO/MCP checks configurable

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-03-21
**Author:** jleechan2015
**Stats:** +71/-22 in 1 files

## Summary
- downgrade legacy migrated cron job ID findings from FAIL to WARN (non-fatal)
- make AO dashboard port probe configurable via `OPENCLAW_DOCTOR_AO_DASHBOARD_PORT`
- auto-detect AO dashboard port from launchd plist `--port` arg when available
- skip MCP Agent Mail probe cleanly when MCP adapter is not configured

## Raw Body
## Summary
- downgrade legacy migrated cron job ID findings from FAIL to WARN (non-fatal)
- make AO dashboard port probe configurable via `OPENCLAW_DOCTOR_AO_DASHBOARD_PORT`
- auto-detect AO dashboard port from launchd plist `--port` arg when available
- skip MCP Agent Mail probe cleanly when MCP adapter is not configured

## Why
OpenClaw monitor was red due to legacy migrated cron IDs being treated as hard failures, and AO dashboard probe was hardcoded to `3011` even when deployments moved ports. MCP probe should not warn when the adapter is intentionally disabled.

## Validation
- `bash -n scripts/doctor.sh`

