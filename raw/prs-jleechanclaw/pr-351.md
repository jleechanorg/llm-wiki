# PR #351: [agento] Orchestrator: single-source agent rules + reaction GraphQL / evidence-reviewer alignment

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-03-22
**Author:** jleechan2015
**Stats:** +65/-141 in 3 files

## Summary
Follow-up orchestration hygiene: **one source of truth** for agent policy in `agent-orchestrator.yaml` `defaults.agentRules` — no separate `workspace/AGENT_BASE_RULES.md` overlay (avoids drift/duplication).

## Raw Body
## Summary

Follow-up orchestration hygiene: **one source of truth** for agent policy in `agent-orchestrator.yaml` `defaults.agentRules` — no separate `workspace/AGENT_BASE_RULES.md` overlay (avoids drift/duplication).

## Changes

- **Remove `agentRulesFile: workspace/AGENT_BASE_RULES.md`** from the `jleechanclaw` project — orchestrator loads `defaults.agentRules` only.
- **Delete `workspace/AGENT_BASE_RULES.md`**.
- **Keep** the earlier reaction fixes on this branch: parameterized GraphQL for unresolved review threads in `approved-and-green`, and headless `evidence-reviewer` wording for evidence gate / `agent-stuck` where applicable.
- **Rewrite `src/tests/test_agent_base_rules.py`** to assert required protocol strings live under `defaults.agentRules` (parsed from `agent-orchestrator.yaml`) instead of a markdown file.

## Testing

- `python3 -c "import yaml; yaml.safe_load(open('agent-orchestrator.yaml'))"`
- `PYTHONPATH=src python -m pytest src/tests/test_agent_base_rules.py tests/test_pr51_hardening.py -q`

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Updates the orchestrator’s central policy/config strings and reaction instructions that drive automated agent behavior, so mistakes could mis-route notifications or change merge-gate handling despite being mostly text/config changes.
> 
> **Overview**
> Moves orchestration policy to a **single source of truth** by removing the `workspace/AGENT_BASE_RULES.md` overlay (and deleting the file) and updating tests to parse/validate required protocols directly from `agent-orchestrator.yaml` `defaults.agentRules`.
> 
> Refines `defaults.agentRules`/per-project rules and `reactions` messaging: simplifies MCP mail to `send_message` only (no `register_agent`), standardizes the green signal text to include ✅ and "awaiting auto-merge", adds explicit post-approval Major/Critical inline-comment blocking instructions, and tweaks reaction/triage prompts accordingly. Also updates a couple of project `path` values and d
