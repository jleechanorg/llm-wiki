# PR #91: SOUL: require real memory search before footer

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-03-11
**Author:** jleechan2015
**Stats:** +524/-190 in 7 files

## Summary
- require `memory_search` before every user-facing response
- define explicit footer states for success hits / success none / failure
- forbid claiming `Memory: none` when memory search errors

## Raw Body
## Summary
- require `memory_search` before every user-facing response
- define explicit footer states for success hits / success none / failure
- forbid claiming `Memory: none` when memory search errors

## Changes
- updated Memory section in `openclaw-config/SOUL.md`

## Testing
- manual validation via `openclaw agent` stress run with 12 queries
- evidence bundle under `/tmp/memory-hard-test-clean-1773139216`

## Known Limitations
- this PR is prompt/config enforcement; runtime hard-enforcement is still a separate concern

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Changes affect task dispatch routing/worktree creation and MCP request validation, which can break agent automation or tool calls if edge cases/regressions exist; new unit/E2E coverage reduces but doesn’t eliminate this risk.
> 
> **Overview**
> Updates `SOUL.md` to require a `memory_search` call before every user-facing response and standardizes the memory footer to distinguish *hits/none/error* states.
> 
> Hardens async dispatch for cross-repo tasks in `dispatch_task.py` by reducing false-positive repo detection (blocklist + high-confidence patterns), resolving target repos from common local locations, enforcing “repo must exist locally” for explicit targets, and cleaning up newly created worktrees when `ai_orch` fails.
> 
> Tightens MCP JSON-RPC handling in `mcp_http.py` to reject non-object `params`/tool `arguments`, updates unit tests, and adds a real-HTTP E2E test suite that exercises initialize/tools/resources/prompts and auth/error paths. Also refreshes `openclaw.json` (logging/diagnostics layout, memory search paths, Slack tool wiring, and channel token/env tweaks).
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 019b176a9d465be3812ebf5fc3315c8ab97a2aaa. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release 
