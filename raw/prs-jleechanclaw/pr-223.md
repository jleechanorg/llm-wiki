# PR #223: fix(auto-resolve-threads): use branch param to scope file detection to latest push

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-03-16
**Author:** jleechan2015
**Stats:** +7/-3 in 2 files

## Summary
Fixes a bug where the  parameter in  was being silently ignored.

## Raw Body
## Summary

Fixes a bug where the  parameter in  was being silently ignored.

## Problem

In  lines 339-343, the  function had an if/else where **both branches** called  with identical arguments:



This returned **all PR files** regardless of whether  was provided, causing threads from stale commits to be incorrectly resolved.

## Fix

When  is provided, now calls  to get only files changed in the latest push:



Bead: orch-n0a9

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Moderate risk because it changes which review threads get auto-resolved and relies on `gh`-based file detection for the head branch, which could cause under/over-resolving if the branch diff lookup is incomplete.
> 
> **Overview**
> `auto_resolve_threads_for_pr` now uses the provided `branch` to compute `changed_files` via `get_files_changed_in_push`, instead of always falling back to PR-wide file listing, preventing stale-commit review threads from being auto-resolved.
> 
> Also updates `.beads/issues.jsonl` with a few new/reshuffled tracking entries.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit b8b3e5db40a9f35800058db8b191162a81c39e37. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
