# PR #183: feat(backfill): pass 3 — poke idle sessions with open PR comments

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-03-16
**Author:** jleechan2015
**Stats:** +56/-0 in 1 files

## Summary
(none)

## Raw Body
Detects AO sessions that are running (tmux alive) but idle at shell prompt for >15min while PR still has open review comments. Sends a poke via tmux send-keys to restart the agent work loop. Fixes the gap where agents declare ready-to-merge and go idle, invisible to pass 2 which only fires on killed/stuck sessions.

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Adds a new backfill pass that inspects live tmux sessions and sends automated keystrokes based on GitHub PR comment state, which could inadvertently poke the wrong session or create noise if heuristics misfire.
> 
> **Overview**
> Adds a new **Pass 3** to `scripts/ao-backfill.sh` that detects AO sessions marked `running` but idle for >15 minutes (based on `updatedAt` and tmux pane prompt detection) and, when the claimed PR is still open with inline review comments, sends a guidance “poke” into the tmux session via `tmux send-keys` to restart agent work.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 79e460136441ebca7887531ac1296e465f175106. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
