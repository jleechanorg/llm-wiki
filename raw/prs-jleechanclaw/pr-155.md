# PR #155: feat(notify): parallel Slack+email escalation alerts + webhook daemon in install script

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-03-15
**Author:** jleechan2015
**Stats:** +269/-101 in 7 files

## Summary
- Escalation alerts now fire Slack DM **and** email simultaneously via `ThreadPoolExecutor`; succeeds if either channel works
- `ai.openclaw.webhook` daemon added to `install-launchagents.sh` (macOS launchctl + Linux systemd)

## Raw Body
## Summary

- Escalation alerts now fire Slack DM **and** email simultaneously via `ThreadPoolExecutor`; succeeds if either channel works
- `ai.openclaw.webhook` daemon added to `install-launchagents.sh` (macOS launchctl + Linux systemd)

## Changes

**`src/orchestration/action_executor.py`**
- Extract `_send_slack_dm()` and `_send_email_notification()` as module-level helpers
- Add `_make_real_notifier()` factory returning a notifier that fires both in parallel
- Email: `NOTIFY_EMAIL_TO` / `SMTP_HOST` / `SMTP_PORT` / `SMTP_USER` / `SMTP_PASSWORD` env vars; port 465 → `SMTP_SSL`, else `STARTTLS`
- `execute_action` now calls `_make_real_notifier()` instead of inline class

**`scripts/install-launchagents.sh`**
- Adds `ai.openclaw.webhook` install block after qdrant, before gateway
- macOS: `sed` substitutes `@HOME@`/`@PYTHON_PATH@` in `launchd/ai.openclaw.webhook.plist.template`, loads via `launchctl bootstrap`
- Linux: `install_systemd_service` with `WEBHOOK_PORT=19888` env

**`src/tests/test_action_executor.py`**
- 10 new tests across `TestSendSlackDm`, `TestSendEmailNotification`, `TestParallelNotification`

## Testing

- 395 tests pass (`PYTHONPATH=src python -m pytest src/tests/ -q`)
- Covers: no-token fallback, STARTTLS, SSL, SMTP error, parallel dispatch, slack-only success, email-only success

## Known Limitations

- Email config must be provided via env vars; no default SMTP server bundled
- `install-launchagents.sh` webhook block requires `launchd/ai.openclaw.webhook.plist.template` to exist (skips gracefully if missing)

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Adds new outbound email delivery and changes escalation notification behavior to succeed if either Slack or SMTP works; also modifies service installation to auto-run a webhook daemon, which can impact local environments and alerting reliability.
> 
> **Overview**
> **Escalation alerting is expanded from Slack-only to 
