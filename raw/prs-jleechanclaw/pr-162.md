# PR #162: fix(webhook): include workflow lane in remediation issue title

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-03-15
**Author:** jleechan2015
**Stats:** +19/-12 in 1 files

## Summary
- Added workflow lane context to remediation issue titles so agents know what workflow to run
- When `pull_request.opened` events trigger remediation, the issue now includes "(comment-validation)"

## Raw Body
## Summary
- Added workflow lane context to remediation issue titles so agents know what workflow to run
- When `pull_request.opened` events trigger remediation, the issue now includes "(comment-validation)" 

## Changes
- Modified `webhook_worker.py` to call `route_event` from `pr_lifecycle` to get the workflow lane
- Issue title now includes workflow lane: "Remediate pull_request.opened on PR#160 @ abc123 (comment-validation)"
- Previously, the issue lacked workflow context, making it harder for agents to know what to do

## Testing
- All webhook_worker tests pass (28 tests)
- All pr_lifecycle tests pass (7 tests)  
- All webhook_queue tests pass (23 tests)

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Low risk: only changes how remediation issue titles are constructed and makes `route_event` failures non-blocking, with no changes to core dispatch mechanics.
> 
> **Overview**
> Remediation dispatch now *optionally* derives a `workflow_lane` via `pr_lifecycle.route_event` and appends it to the spawned agent issue title (e.g., `Remediate … (lane)`), improving workflow context for agents.
> 
> Routing is wrapped in a broad `try/except` so failures to compute the lane do **not** fail the overall dispatch; it falls back to the prior behavior of emitting an issue title without lane context.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 1fc349ad5157d0bf55ca0d7a0ace2aa62bcae124. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->
## Summary by CodeRabbit

* **Improvements**
  * Remediation event routing now attempts to detect and include workflow lane information when available, improving message context.
  * Remediation messages include lane context when present, making notifications more informat
