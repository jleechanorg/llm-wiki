# PR #49: Mission Control: add feature-flagged cmux bridge dispatch

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-03-05
**Author:** jleechan2015
**Stats:** +0/-0 in 0 files

## Summary
- add `orchestration.cmux_bridge` with command wrappers for ping/list/send/read/new workspace/split/close
- integrate cmux path into task poller behind feature flag (default off)
- add docs/env examples for `CMUX_SOCKET_PATH` and `CMUX_SOCKET_PASSWORD`
- add TDD coverage for cmux bridge and task poller integration behavior

## Raw Body
## Summary
- add `orchestration.cmux_bridge` with command wrappers for ping/list/send/read/new workspace/split/close
- integrate cmux path into task poller behind feature flag (default off)
- add docs/env examples for `CMUX_SOCKET_PATH` and `CMUX_SOCKET_PASSWORD`
- add TDD coverage for cmux bridge and task poller integration behavior

## Testing
- `cd src && pytest -q tests/test_cmux_bridge.py tests/test_task_poller.py -k "cmux"` → `11 passed, 43 deselected`
- `cd src && pytest -q tests/test_cmux_bridge.py tests/test_task_poller.py` → `54 passed`

## Known Limitations
- feature remains disabled by default until env flag is enabled
- integration tests mock cmux execution instead of requiring live cmux app/socket

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Medium risk because it removes automation (GitHub Actions workflows) and many operational docs/scripts, which could break existing development/ops workflows and onboarding if anything still depended on these files.
> 
> **Overview**
> This PR performs a large cleanup by deleting repo-local configuration/docs/scripts tied to Beads, OpenClaw setup/backups, and Mission Control (including `.beads/` contents, many root markdown guides, helper shell scripts, and the `docker-compose.yml` for Mission Control).
> 
> It also removes the GitHub Actions comment-trigger workflows for creating Mission Control tasks, and updates `.gitignore` to more aggressively ignore Dolt/Beads local databases (`.doltcfg/`, `beads*`, `config.yaml`) and sensitive backup artifacts (`.openclaw-backups/`, `.beads/interactions.jsonl`).
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit f2c5e5eca7f6fa3b9181ad56069b395fef7ebc9f. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
