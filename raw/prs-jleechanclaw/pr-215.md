# PR #215: fix: update tar to 7.5.11 - CVE-2026

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-03-16
**Author:** jleechan2015
**Stats:** +601/-4 in 5 files

## Summary
(none)

## Raw Body
Updates tar to 7.5.11 for CVE-2026-31802, CVE-2026-29786, CVE-2026-26960

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Adds new orchestration logic that parses action logs and sends Slack DMs on repeated escalations, plus tweaks PR poller respawn capping; misclassification or noisy alerting could affect ops behavior. Dependency bump to `tar` is low risk but can impact build/runtime if downstream expects exact version pinning.
> 
> **Overview**
> Introduces an `anomaly_detector` that scans `action_log.jsonl` for escalation-related actions over a rolling 7-day window, groups by `error_class`, and sends a Slack DM when any class hits the threshold (default `>= 2`), with accompanying unit tests.
> 
> Tightens `ao-pr-poller.sh` respawn capping by resetting the cap window inside `inc_cap` when the 12-hour window has expired, aligning it with `check_cap` behavior.
> 
> Updates the `tar` dependency (and pnpm override) from `7.5.7` to `^7.5.11`, and extends internal `MEMORY.md` notes with Slack Socket Mode event-loss investigation findings.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit e6d2e0a88eb89da4566b0bb930ca23722575d586. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
