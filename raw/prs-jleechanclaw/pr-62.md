# PR #62: fix: tighten CodeRabbit ping policy — push-triggered only, no timer

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-03-07
**Author:** jleechan2015
**Stats:** +4/-0 in 2 files

## Summary
- Removed blind 10-minute cron (`pr-review-watchdog.sh`) that was pinging `@coderabbit-ai review` on every open non-approved PR unconditionally
- Clarified CodeRabbit Review Protocol in `CLAUDE.md` and `AGENTS.md`: only trigger after a fresh push lands on the PR branch

## Raw Body
## Summary
- Removed blind 10-minute cron (`pr-review-watchdog.sh`) that was pinging `@coderabbit-ai review` on every open non-approved PR unconditionally
- Clarified CodeRabbit Review Protocol in `CLAUDE.md` and `AGENTS.md`: only trigger after a fresh push lands on the PR branch

## Changes
- **Before:** `*/10 * * * * pr-review-watchdog.sh` ran every 10 min and pinged CodeRabbit on all open PRs regardless of activity
- **After:** Protocol explicitly states "only ping after a fresh commit is pushed; never on a timer"
- The cron entry has been removed from crontab; the script itself is preserved for manual use

## Testing
- Verified crontab no longer contains `pr-review-watchdog.sh` entry
- `CLAUDE.md` and `AGENTS.md` now contain explicit "no timer" guard

## Known Limitations
- The `pr-review-watchdog.sh` script still exists at `~/.openclaw/scripts/` for manual invocation

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Documentation-only change that clarifies when to trigger CodeRabbit re-reviews; no runtime or workflow automation logic is modified.
> 
> **Overview**
> Updates `AGENTS.md` and `CLAUDE.md` to explicitly forbid pinging CodeRabbit on a timer or without a preceding push, and to only trigger `@coderabbitai all good` after a fresh commit lands on the PR branch.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 2f2b53f90791767616d9702934e69ada4b08ebd9. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->

## Summary by CodeRabbit

* **Documentation**
  * Updated guidelines on proper usage of review automation to ensure it's triggered only after fresh commits, preventing unnecessary automated reviews.

<!-- end of auto-generated comment: release notes by coderabbit.ai -->
