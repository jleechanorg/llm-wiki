# PR #491: fix(harness): add protected openclaw.json keys — heartbeat.every must stay 5m

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-04-04
**Author:** jleechan2015
**Stats:** +12/-0 in 1 files

## Summary
On 2026-04-04, an agent changed agents.defaults.heartbeat.every from 5m to 30m in ~/.openclaw/openclaw.json. This caused doctor.sh to FAIL on every monitor run and the monitor-agent to report STATUS=PROBLEM. The constraint existed only in doctor.sh — no agent instruction stated the value was protected.

Root cause (from /harness analysis): agents mutate openclaw.json without knowing which keys doctor.sh validates. The constraint knowledge was absent from any file agents read before writing.

Rel

## Background
On 2026-04-04, an agent changed agents.defaults.heartbeat.every from 5m to 30m in ~/.openclaw/openclaw.json. This caused doctor.sh to FAIL on every monitor run and the monitor-agent to report STATUS=PROBLEM. The constraint existed only in doctor.sh — no agent instruction stated the value was protected.

Root cause (from /harness analysis): agents mutate openclaw.json without knowing which keys doctor.sh validates. The constraint knowledge was absent from any file agents read before writing.

Related: PR https://github.com/jleechanorg/jleechanclaw/pull/490 (merged same day) added stale session lock detection — companion fix.

## Raw Body
## Background

On 2026-04-04, an agent changed agents.defaults.heartbeat.every from 5m to 30m in ~/.openclaw/openclaw.json. This caused doctor.sh to FAIL on every monitor run and the monitor-agent to report STATUS=PROBLEM. The constraint existed only in doctor.sh — no agent instruction stated the value was protected.

Root cause (from /harness analysis): agents mutate openclaw.json without knowing which keys doctor.sh validates. The constraint knowledge was absent from any file agents read before writing.

Related: PR https://github.com/jleechanorg/jleechanclaw/pull/490 (merged same day) added stale session lock detection — companion fix.

## Goals

1. Make the heartbeat.every = 5m requirement visible to agents before they write
2. Document all doctor.sh-enforced keys as a "protected keys" table alongside the existing mutation safety section
3. Prevent recurrence of config drift that silently breaks monitoring

## Tenets

- Constraint knowledge belongs in files agents read before acting, not only in validators
- Minimal change: one table added to existing mutation safety section

## High-level description of changes

Added a "Protected keys" table to the openclaw.json mutation safety section in CLAUDE.md covering: heartbeat.every (5m), heartbeat.target (last), timeoutSeconds (<=600), maxConcurrent (<=3), subagents.maxConcurrent (<=3).

## Testing

- heartbeat.every restored to 5m in ~/.openclaw/openclaw.json (surgical update applied)
- No code changes — instruction-only fix

## Low-level details

- **CLAUDE.md**: Added protected keys table under openclaw.json mutation safety section. Covers all values that doctor.sh validate_heartbeat_config() and the WS churn section document as having fixed bounds.

## Verification

After merge + git pull in ~/.openclaw:
bash ~/.openclaw/scripts/doctor.sh | grep -E "heartbeat|PASS|FAIL"
Expected: all heartbeat checks PASS

Generated with Claude Code

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Low risk documentation-on
