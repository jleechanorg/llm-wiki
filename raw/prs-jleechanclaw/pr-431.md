# PR #431: fix(harness): fallback to gh auth token when env/file missing

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-03-29
**Author:** jleechan2015
**Stats:** +2/-1 in 1 files

## Summary
(none)

## Raw Body
Long-term fix: harness analyzer no longer hard-fails when GH_TOKEN/GITHUB_TOKEN file is absent. It now falls back to \[REDACTED_PAT] when available, so scheduled harness runs can execute on hosts with CLI auth but no token file.

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **High Risk**
> Touches GitHub authentication token resolution used for automation, and the current diff leaves an unresolved merge conflict marker (`<<<<<<< HEAD`) that would break the script at runtime.
> 
> **Overview**
> Updates `scripts/harness-analyzer.sh` to sanitize the fallback `gh auth token` output by stripping CR/LF characters before exporting `GH_TOKEN`.
> 
> **Note:** the change as shown includes an unresolved git conflict marker in `resolve_gh_token`, which would cause the analyzer script to fail to execute until resolved.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit b3d5b2c888112c4a3783fac0b9750c0a86c52d28. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->

## Summary by CodeRabbit

* **Chores**
  * Transitioned CI workflows to self-hosted runner infrastructure for improved control and reliability.
  * Added automated toolchain validation to ensure required utilities are available before execution.
  * Enhanced CI status evaluation logic to properly handle cancelled check runs.
  * Improved token authentication with additional fallback mechanism for automation processes.

<!-- end of auto-generated comment: release notes by coderabbit.ai -->
