# PR #90: mctrl: add outbox dead-letter, retry bounds, and status telemetry

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-03-11
**Author:** jleechan2015
**Stats:** +1275/-739 in 14 files

## Summary
- add bounded retry + dead-letter handling for mctrl outbox delivery failures
- add supervisor outbox health alerting (backlog/age/dead-letter) with cooldown
- extend `mctrl status` output to include outbox/dead-letter/retry telemetry
- add TDD coverage for notifier, supervisor alert cooldown, and status output

## Raw Body
## Summary
- add bounded retry + dead-letter handling for mctrl outbox delivery failures
- add supervisor outbox health alerting (backlog/age/dead-letter) with cooldown
- extend `mctrl status` output to include outbox/dead-letter/retry telemetry
- add TDD coverage for notifier, supervisor alert cooldown, and status output

## Changes
- `src/orchestration/openclaw_notifier.py`
  - Added `DEFAULT_DEAD_LETTER_PATH` and bounded retries (`retry_limit`) in `drain_outbox()`
  - Failed events now increment `_retry_count`; events exceeding limit are moved to dead-letter JSONL
  - Added `read_dead_letter()` and `outbox_health_snapshot()` helpers
  - Added `notify_slack_outbox_alert()` for operator-visible backlog/dead-letter alerts
- `src/orchestration/supervisor.py`
  - Added outbox health checks per tick and `maybe_alert_outbox_health()` with cooldown
  - Added env knobs: `MCTRL_DEAD_LETTER_PATH`, `MCTRL_OUTBOX_ALERT_THRESHOLD`, `MCTRL_OUTBOX_AGE_ALERT_SECONDS`, `MCTRL_OUTBOX_ALERT_COOLDOWN_SECONDS`
  - Added safe int env parsing fallback to defaults
- `src/orchestration/mctrl_status.py`
  - Added outbox summary line: pending count, oldest age, dead-letter count, retry histogram
  - Added CLI args: `--outbox-path`, `--dead-letter-path`
- `scripts/run-mctrl-supervisor.sh`
  - Export `MCTRL_DEAD_LETTER_PATH`
- Tests
  - Expanded `tests/test_openclaw_notifier.py` for retry increment, dead-letter routing, health snapshot
  - Expanded `tests/test_supervisor.py` for alert cooldown + invalid env fallback
  - Added `tests/test_mctrl_status.py` for outbox summary rendering

## Testing
- Targeted (changed areas):
  - `PYTHONPATH=src pytest -q tests/test_mctrl_status.py tests/test_openclaw_notifier.py tests/test_reconciliation.py tests/test_supervisor.py`
  - Result: `34 passed`
- Full suite run:
  - `PYTHONPATH=src pytest -q`
  - Result: failures are unrelated/pre-existing in this branch context:
    - missing `SLACK_USER_TOKEN` for real E2E (`tests/test_mvp_loopback_e2e.py`)
    - o
