# PR #238: Memory233232

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-03-16
**Author:** jleechan2015
**Stats:** +257/-2 in 3 files

## Summary
(none)

## Raw Body
Closing — garbage PR with auto-generated title from agent memory backfill. No meaningful changes to merge.

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Changes affect how mem0 memories are ingested/extracted by adding a new `customPrompt` and new ingestion paths, which can materially change stored data quality and volume. Risk is limited to memory/knowledge tooling but could impact downstream recall behavior if enabled in production configs.
> 
> **Overview**
> Extends the mem0 markdown backfill script to also ingest Claude Code auto-memory markdown files from `~/.claude/projects/*/memory`.
> 
> Updates the `openclaw-mem0` plugin configuration to include a `customPrompt` that tightens what durable facts get stored.
> 
> Adds `scripts/seed_product_knowledge.py` to seed mem0/Qdrant with curated product docs (e.g., `CLAUDE.md`, ADRs, roadmap) and selectively ingested Claude session JSONL conversations, with `--docs-only`, `--sessions`, `--all`, and `--dry-run` modes.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit dfc41336f4ffb5347a2ba227b84391c383c055b9. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
