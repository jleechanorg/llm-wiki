# PR #225: fix: expand PR green conditions from 4 to 6

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-03-16
**Author:** jleechan2015
**Stats:** +1620/-5 in 10 files

## Summary
- Expands the \"green\" PR definition from 4 to 6 conditions
- Adds explicit Cursor Bugbot check (condition 4)
- Adds evidence review gate (condition 6) — skipped if no evidence bundle
- Clarifies that ALL inline comments must be resolved even after CR APPROVE (condition 5)

## Raw Body
## Summary
- Expands the \"green\" PR definition from 4 to 6 conditions
- Adds explicit Cursor Bugbot check (condition 4)
- Adds evidence review gate (condition 6) — skipped if no evidence bundle
- Clarifies that ALL inline comments must be resolved even after CR APPROVE (condition 5)

## Changes
- `agent-orchestrator.yaml`: updated `agentRules` definition block + `approved-and-green` message

## Conditions (before → after)
| # | Before | After |
|---|--------|-------|
| 1 | CI green | CI green (unchanged) |
| 2 | No conflicts | No conflicts (unchanged) |
| 3 | No serious comments | CodeRabbit APPROVED (explicit) |
| 4 | CR approved | Cursor Bugbot finished, no blocking findings |
| 5 | — | All inline comments resolved (even post-APPROVE) |
| 6 | — | Evidence review PASS (skip if no evidence bundle) |

## Testing
Manually verified YAML syntax valid. Agents will pick up the new rules on next session start.

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Moderate risk because it adds new automation that kills tmux sessions/worktrees and calls GitHub thread-resolution logic after retries/merges, which could cause unintended cleanup or state changes if parsing/PR detection is wrong. Config changes also tighten merge-readiness gates and may block workflows if checks/tokens/evidence expectations are misaligned.
> 
> **Overview**
> Updates `agent-orchestrator.yaml` to redefine “green” PRs from **4 to 6 gates**, adding explicit *Bugbot completion*, *all inline threads resolved*, and an optional *evidence review PASS* requirement; the `approved-and-green` reaction message is updated to instruct these checks.
> 
> Adds automated cleanup for stuck/stale AO tmux sessions: `ao-pr-poller.sh` now includes a post-poll stale-session/worktree cleanup pass, and a new nightly `ao-session-reaper.sh` script reaps sessions based on age + PR state (capped kills).
> 
> Extends orchestration actions so `RetryAction` carries an optional `pr_url`, and `action_executor` will call `a
