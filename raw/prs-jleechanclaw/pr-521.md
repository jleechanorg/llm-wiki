# PR #521: [P1] feat(launchd): ao7green-jleechanclaw 30-min schedule plist

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-04-06
**Author:** jleechan2015
**Stats:** +63/-4 in 4 files

## Summary
Add launchd plist for the jleechanclaw PR monitor to replace the gateway-managed cron job (id: 64f2399d-33f4-4451-be87-05350d2b2590) with a durable launchd service.

## Test Plan
- [ ] CI passes
- [ ] Plist installs via ./scripts/install-launchagents.sh
- [ ] launchctl list | grep ao7green shows it running
- [ ] Log appears at ~/.openclaw/logs/scheduled-jobs/ao7green-jleechanclaw.log after one interval

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Low risk: adds a new macOS `launchd` scheduled job and wires it into the installer, with minor env-var plumbing changes to the monitor scripts.
> 
> **Overview**
> Adds a new macOS `launchd` plist (`ai.openclaw.sched

## Raw Body
## Summary
Add launchd plist for the jleechanclaw PR monitor to replace the gateway-managed cron job (id: 64f2399d-33f4-4451-be87-05350d2b2590) with a durable launchd service.

## Changes
- **launchd/ai.openclaw.schedule.ao7green-jleechanclaw.plist**: New plist, StartInterval=1800s, targets jleechanorg/jleechanclaw, posts to #ai-slack-test (C0AKALZ4CKW)
- **scripts/install-launchagents.sh**: Add install_plist call for the new plist

## Why
Gateway cron jobs don't survive gateway restarts reliably. The 30-min ao:jleechanclaw-30m-drive-to-7-green cron was dropping runs. Launchd is the durable alternative — starts at boot, survives gateway restarts, tracked in git.

## Test plan
- [ ] CI passes
- [ ] Plist installs via ./scripts/install-launchagents.sh
- [ ] launchctl list | grep ao7green shows it running
- [ ] Log appears at ~/.openclaw/logs/scheduled-jobs/ao7green-jleechanclaw.log after one interval

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Low risk: adds a new macOS `launchd` scheduled job and wires it into the installer, with minor env-var plumbing changes to the monitor scripts.
> 
> **Overview**
> Adds a new macOS `launchd` plist (`ai.openclaw.schedule.ao7green-jleechanclaw`) to run the `ao7green` PR monitor every 30 minutes (and at load) with repo/project/channel/log configured via environment variables.
> 
> Updates the launchd wrapper and monitor script to honor `AO_MONITOR_PROJECT`, `AO_MONITOR_CHANNEL`, and `AO_MONITOR_LOG` and to invoke the monitor via `${HOME}` instead of a hard-coded user path. The `install-launchagents.sh` installer now installs this new plist on macOS.
> 
> <sup>Reviewed by [Cursor Bugbot](https://cursor.com/bugbot) for commit 3cf056845dbacaa28e004cd8b736c0202024cce7. Bugbot is set up for automated code reviews on this repo. Configure [here](https://www.cursor.com/dashboard/bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->

## Summary by CodeRabbit

* **Cho
