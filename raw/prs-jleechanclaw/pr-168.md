# PR #168: fix(webhook): handle check_suite.completed events without PR association

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-03-16
**Author:** jleechan2015
**Stats:** +148/-22 in 5 files

## Summary
- Handle check_suite.completed and check_run.completed webhook events that are not associated with a PR (e.g., CI runs on main branch commits)
- Previously these events were silently dropped at the webhook bridge level

## Raw Body
## Summary
- Handle check_suite.completed and check_run.completed webhook events that are not associated with a PR (e.g., CI runs on main branch commits)
- Previously these events were silently dropped at the webhook bridge level

## Changes
- webhook_bridge.py: Allow check_suite/check_run events without PR to pass through (previously dropped all events without PR)
- pr_lifecycle.py: Add SKIP_REASON_NO_PR_ASSOCIATED for events without PR, update RouteDecision.pr_number to accept int | None, modify route_event() to skip events without PR with appropriate reason

## Testing
- All 421 src/tests pass

## Known Limitations
- Events without PR are skipped with SKIP_REASON_NO_PR_ASSOCIATED - no remediation is attempted since there's no PR to fix

🤖 Generated with Claude Code

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Changes webhook normalization and routing to accept `check_suite`/`check_run` events that lack a PR number and mark them as skipped, which could alter event intake/dispatch behavior for CI events on default branches.
> 
> **Overview**
> Ensures `check_suite`/`check_run` webhooks with no associated PR are no longer dropped at ingress; they now pass through `webhook_bridge.receive_github_event` and are classified by `pr_lifecycle.route_event` as **skipped/ineligible** with a new `no_pr_associated` skip reason.
> 
> Refactors skip-reason strings into a `SkipReason` `StrEnum`, updates `RouteDecision.pr_number` to allow `None`, and updates the worker to avoid dispatching remediation when `pr_number` is missing. Adds tests covering pass-through + skip behavior for PR-less check events and adjusts existing webhook tests for env-secret handling.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit dfb3db2558ada0f800aded30235089e5ef21e49f. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: re
