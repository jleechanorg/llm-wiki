# PR #150: feat(monitor): enable canary health check by default + teach agent to respond

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-03-15
**Author:** jleechan2015
**Stats:** +6/-4 in 3 files

## Summary
- Enable `RUN_CANARY` by default in `monitor-agent.sh` (was opt-in, now on by default)
- Add Health Check Protocol to `workspace/SOUL.md` so the agent knows it must reply `pong` to `[monitor-e2e-canary]` pings

## Raw Body
## Summary
- Enable `RUN_CANARY` by default in `monitor-agent.sh` (was opt-in, now on by default)
- Add Health Check Protocol to `workspace/SOUL.md` so the agent knows it must reply `pong` to `[monitor-e2e-canary]` pings

## Changes

**monitor-agent.sh**
- `RUN_CANARY` default: `0` → `1`
- Override still available via `OPENCLAW_MONITOR_RUN_CANARY=0`

**workspace/SOUL.md**
- New section: Health Check Protocol
- Agent must reply `pong` immediately when it receives a `[monitor-e2e-canary]` message
- Explicitly states that NO_REPLY or silence fails the health check and triggers alerts

## Testing
- Canary is an e2e Slack→gateway→agent→Slack reply check; enabling by default means every monitor run validates the full path
- `workspace/SOUL.md` is read directly by the openclaw agent, so the policy takes effect on next agent startup

## Known Limitations
- None

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Medium risk because it changes default monitoring behavior to actively post/poll Slack as `SLACK_USER_TOKEN`, which could increase alert volume or fail in environments without the token and affect operational noise.
> 
> **Overview**
> Enables the Slack inbound e2e canary check by default, with opt-out via `OPENCLAW_MONITOR_RUN_CANARY`, and ensures the launchd/`.bashrc` env hydration path actually picks up that override.
> 
> Adjusts the monitor’s Slack report so a successful canary is treated as *passing* (green) rather than a warning.
> 
> Updates documentation and agent guidance: README now reflects the new default and token requirement, and `workspace/SOUL.md` instructs the agent to reply `pong` **in the same thread** for `[monitor-e2e-canary]` messages.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 9cb3a716cacf2a353c29107c3ec783b0adeb3954. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comme
