# PR #43: fix(ci): prevent skipped PR-fix trigger on Slack mention

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-03-05
**Author:** jleechan2015
**Stats:** +20/-66 in 3 files

## Summary
- support Slack-style mention token `<@U0AEZC7RX1Q>` as valid trigger mention
- remove duplicate workflow file that caused duplicate issue_comment runs
- add TDD coverage for trigger strings and workflow dedupe

## Raw Body
## Summary
- support Slack-style mention token `<@U0AEZC7RX1Q>` as valid trigger mention
- remove duplicate workflow file that caused duplicate issue_comment runs
- add TDD coverage for trigger strings and workflow dedupe

## Why this was skipped
The workflow only matched `@jleechanclaw`, but the test comment used `<@U0AEZC7RX1Q>`, so the job-level `if` evaluated false and the run was marked skipped.

## Testing
- `python3 -m pytest src/tests/test_gh_integration.py -q`
- result: `48 passed`

## Known limitations
- trigger still requires trusted actor association (OWNER/MEMBER/COLLABORATOR), unchanged by this PR.

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Changes the conditions that start a self-hosted GitHub Actions job, so a mistake could cause missed or unintended task creation. Risk is mitigated by keeping the trusted-author guardrails and adding targeted workflow tests.
> 
> **Overview**
> Updates the `Agent PR Fix Trigger` GitHub Actions workflow to treat the Slack-style mention token `<@U0AEZC7RX1Q>` as a valid trigger alongside `@jleechanclaw`, including matching/removal logic so the instruction payload is cleaned correctly.
> 
> Removes the duplicate PR-fix trigger workflow to avoid double `issue_comment` runs, and adds tests ensuring only one trigger workflow exists and that both mention formats are accepted.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 829dcac4481cf7bbfb702f61e04d6976ae9e65ef. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->

## Summary by CodeRabbit

## Release Notes

* **Chores**
  * Enhanced PR workflow trigger mechanism to support additional trigger methods, improving workflow flexibility.
  * Removed deprecated PR trigger workflow.
  * Updated tests to validate the enhanced trigger mechanism.

<!-- end of auto-gen
