# PR #117: AO plumbing sync: add parity matrix and lifecycle/webhook TDD alignment

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-03-13
**Author:** jleechan2015
**Stats:** +1124/-39 in 12 files

## Summary
- Add Phase A AO compatibility matrix with adopt/adapt/retain decisions.
- Implement AO-compatible timed escalation via `escalate_after` parsing in lifecycle reactions.
- Extend PR lifecycle taxonomy with `check_run.completed.failure -> fixpr` mapping.
- Export bead updates to `.beads/issues.jsonl` (`ORCH-16t.1` closed; `ORCH-16t.3` and `ORCH-16t.5` in progress).

## Raw Body
## Summary
- Add Phase A AO compatibility matrix with adopt/adapt/retain decisions.
- Implement AO-compatible timed escalation via `escalate_after` parsing in lifecycle reactions.
- Extend PR lifecycle taxonomy with `check_run.completed.failure -> fixpr` mapping.
- Export bead updates to `.beads/issues.jsonl` (`ORCH-16t.1` closed; `ORCH-16t.3` and `ORCH-16t.5` in progress).

## Architecture Intent
- AO remains canonical for lifecycle/reaction taxonomy and webhook event naming.
- jleechanclaw keeps adapters only where AO does not provide equivalent behavior.

## Changes
- Lifecycle reactions:
  - add `_parse_escalate_after` in `src/orchestration/lifecycle_reactions.py`
  - enforce time-based escalation in `LifecycleManager.execute_reaction`
  - add tests for elapsed-duration escalation and invalid-duration fallback
- PR routing/webhooks:
  - map `check_run.completed.failure` -> `fixpr` in `src/orchestration/pr_lifecycle.py`
  - add `check_run` trigger normalization + head SHA extraction in `src/orchestration/webhook_bridge.py`
  - add tests for direct routing and webhook ingress behavior
- Planning/issue tracking:
  - add `roadmap/AO_OPENCLAW_PLUMBING_SYNC_PLAN.md` with parity matrix
  - export latest beads state to `.beads/issues.jsonl`

## Testing
- `pytest -q tests/test_lifecycle_reactions.py tests/test_pr_lifecycle.py tests/test_evidence.py`
- Result: `128 passed`

## Known Limitations / Follow-up Required
- `check_run` production payload PR linkage (`check_run.pull_requests`) is not fully parsed yet in current webhook PR-number extraction path; this needs a follow-up fix and realistic payload tests.
- This PR does not yet align OpenClaw notifier wire contract details (tracked in `ORCH-16t.2`).
- Backlog/decomposition convergence and duplicate-path cleanup remain for later phases (`ORCH-16t.4`, `ORCH-16t.6`).

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Touches core orchestration decision paths (reaction escalation timing and GitHub webhook→lane ro
