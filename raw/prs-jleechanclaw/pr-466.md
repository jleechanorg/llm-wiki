# PR #466: Enforce AO evidence review requirements

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-04-01
**Author:** jleechan2015
**Stats:** +69/-38 in 2 files

## Summary
- AO evidence review gating in this repo was permissive in practice (PASS/WARN accepted, API failures passed)

## Background
- AO evidence review gating in this repo was permissive in practice (PASS/WARN accepted, API failures passed)

## Raw Body
## Background
- AO evidence review gating in this repo was permissive in practice (PASS/WARN accepted, API failures passed)

## Changes
- Make  fail when evidence review cannot be verified (fail-closed)
- Require PASS-only verdict in evidence review gate
- Update  evidence request template to require UI evidence (captioned screenshots + video) and server/API/LLM evidence via gist payload links
- Align integration test for nonexistent PR expectations with fail-closed behavior

## Notes
This commit is scoped to the  repo worktree only and avoids changing  directly.

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Changes merge-gate behavior to block PRs unless a PASS evidence verdict is found and to fail-closed when GitHub review/comment data can’t be fetched, which can newly stop merges on API/CLI issues or missing /er results.
> 
> **Overview**
> Makes the evidence review merge gate **mandatory for AO PRs**: `check_evidence_review` now fails unless a latest `/er` verdict is **PASS** (treating `WARN` and `FAIL` as blocking) and also fails when no verdict is present.
> 
> Switches API-error handling from *fail-open* to *fail-closed only when both reviews and issue comments are unavailable*, with more explicit error reasons, and hardens `gh`/REST JSON parsing with decode error reporting. Updates the `/er` request template to include explicit global AO evidence artifact requirements, and adjusts integration tests to expect fail-closed behavior for nonexistent PRs.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit ff1da0069cd1c31169895033c229295e11f0b06a. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->
## Summary by CodeRabbit

* **New Features**
  * Evidence review gate is now mandatory: PRs without a PASS evidence review are blocked.
  * Evidence request mes
