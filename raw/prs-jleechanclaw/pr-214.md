# PR #214: fix(ao-pr-poller): detect AO worktrees + add agent-orchestrator mapping

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-03-16
**Author:** jleechan2015
**Stats:** +58/-51 in 12 files

## Summary
- `branch_checked_out` now checks `~/.worktrees/<project>/*/HEAD` for AO-managed worktrees — fixes spurious re-spawns on branches already active in AO sessions
- `repo_to_ao_project` adds `jleechanorg/agent-orchestrator → agent-orchestrator` — fixes "Unknown project:" error on every poll
- SOUL.md MCP mail protocol rewritten as mandatory Steps 1/2/3

## Raw Body
## Summary
- `branch_checked_out` now checks `~/.worktrees/<project>/*/HEAD` for AO-managed worktrees — fixes spurious re-spawns on branches already active in AO sessions
- `repo_to_ao_project` adds `jleechanorg/agent-orchestrator → agent-orchestrator` — fixes "Unknown project:" error on every poll
- SOUL.md MCP mail protocol rewritten as mandatory Steps 1/2/3

## Changes
- `lib/github-intake-lib.sh`: add `agent-orchestrator` case
- `scripts/ao-pr-poller.sh`: `branch_checked_out` checks both git worktrees AND `~/.worktrees/<ao_project>/*/HEAD`
- `workspace/SOUL.md`: MCP mail section is now a mandatory numbered protocol

## Testing
Evidence from `/tmp/ao-pr-poller.log`:
- `feat/ai-reviewer-stress-test` was in `~/.worktrees/jleechanclaw/jc-113` but poller re-spawned PR #204 → AO failed with "already checked out"
- `jleechanorg/agent-orchestrator #15` spawned with empty project name → "Unknown project:"

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Changes affect the PR polling/respawn gate and worktree detection, which could cause missed or extra agent spawns if the new branch/review-state checks are wrong. Remaining edits are mostly documentation and local `.beads` tracking data.
> 
> **Overview**
> Tightens `scripts/ao-pr-poller.sh` branch/worktree detection by checking both local git worktrees and AO-managed worktrees under `~/.worktrees/<project>/`, including common rebase-in-progress states, and makes review gating rely on the **latest** CodeRabbit/Bugbot review (with `per_page=100`) instead of any historical approval.
> 
> Updates repository Beads usage to consistently reference `br` (not `bd`) across docs/skills/roadmap, removes Dolt-specific Beads metadata/config entries, and appends several new issue records to `.beads/issues.jsonl`.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 9bb763fb0c1f75693481eff533f65b7e4601e07f. This will update aut
