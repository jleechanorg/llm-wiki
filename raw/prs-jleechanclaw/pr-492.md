# PR #492: [agento] fix(doctor): correct memory lookup command + skip when mem0 disabled

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-04-04
**Author:** jleechan2015
**Stats:** +6/-1 in 2 files

## Summary
doctor.sh fails with `[FAIL] Memory lookup command failed (rc=1)` on every run, causing monitor STATUS=PROBLEM. Two bugs:
1. Wrong command: `openclaw mem0 search` does not exist — correct is `openclaw memory search`
2. Missing disabled-plugin skip: when mem0 is intentionally disabled, output says plugin-disabled but rc=1. monitor-agent.sh handles this at line 671; doctor.sh was missing it.

## Background
doctor.sh fails with `[FAIL] Memory lookup command failed (rc=1)` on every run, causing monitor STATUS=PROBLEM. Two bugs:
1. Wrong command: `openclaw mem0 search` does not exist — correct is `openclaw memory search`
2. Missing disabled-plugin skip: when mem0 is intentionally disabled, output says plugin-disabled but rc=1. monitor-agent.sh handles this at line 671; doctor.sh was missing it.

## Raw Body
## Background

doctor.sh fails with `[FAIL] Memory lookup command failed (rc=1)` on every run, causing monitor STATUS=PROBLEM. Two bugs:
1. Wrong command: `openclaw mem0 search` does not exist — correct is `openclaw memory search`
2. Missing disabled-plugin skip: when mem0 is intentionally disabled, output says plugin-disabled but rc=1. monitor-agent.sh handles this at line 671; doctor.sh was missing it.

## Goals
- doctor.sh exits 0 when mem0 is disabled (WARN not FAIL)
- monitor STATUS=OK after next cycle

## Tenets
- Mirror monitor-agent.sh logic — don't diverge diagnostic behavior
- WARN for intentionally disabled features; FAIL for broken things

## High-level description of changes
Line 826: `openclaw mem0 search` to `openclaw memory search`
Add `plugin disabled` guard (warn+skip) before rc != 0 FAIL check

## Testing
- `bash scripts/doctor.sh` shows `[WARN] Memory lookup skipped (mem0 plugin disabled)` not FAIL
- Zero [FAIL] lines in output
- doctor.sh exits 0

## Low-level details
**scripts/doctor.sh** (+4/-1): fixed command name; added elif plugin-disabled guard

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Low risk: small change to an ops diagnostic script that prevents false failures; main impact is altered health-reporting behavior for memory checks.
> 
> **Overview**
> **Fixes false-negative failures in `scripts/doctor.sh`’s memory probe.** The probe now runs `openclaw memory search` (instead of the non-existent `openclaw mem0 search`).
> 
> When the mem0 plugin is intentionally disabled and the CLI prints `openclaw-mem0: plugin disabled`, the doctor now *WARNs and skips* rather than failing on a non-zero exit code.
> 
> Documentation (`CLAUDE.md`) adds a parity rule between `doctor.sh` and `monitor-agent.sh` diagnostics and a reminder to verify `openclaw` subcommands before scripting them.
> 
> <sup>Reviewed by [Cursor Bugbot](https://cursor.com/bugbot) for commit 1698ce918442b47dfa1b509d17a182df55c83901. Bugbot is set up for automated code 
