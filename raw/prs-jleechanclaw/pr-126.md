# PR #126: chore: rehome repo — ~/.openclaw/ as git root (ORCH-d6i)

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-03-14
**Author:** jleechan2015
**Stats:** +492/-1372 in 46 files

## Summary
- Move \`openclaw-config/\` contents to repo root via \`git mv\` (history preserved)
- Clone target is now \`~/.openclaw/\` — files are live in place, no sync needed
- Remove \`scripts/sync-openclaw-config.sh\` (replaced by \`scripts/bootstrap.sh\`)
- Update \`.gitignore\`, \`tests/\`, \`scripts/doctor.sh\`, \`CLAUDE.md\`, \`AGENTS.md\` for new layout

## Raw Body
## Summary
- Move \`openclaw-config/\` contents to repo root via \`git mv\` (history preserved)
- Clone target is now \`~/.openclaw/\` — files are live in place, no sync needed
- Remove \`scripts/sync-openclaw-config.sh\` (replaced by \`scripts/bootstrap.sh\`)
- Update \`.gitignore\`, \`tests/\`, \`scripts/doctor.sh\`, \`CLAUDE.md\`, \`AGENTS.md\` for new layout

## Changes
- **Before**: repo lives in \`~/project_jleechanclaw/jleechanclaw/\`, synced to \`~/.openclaw/\` via script
- **After**: repo IS \`~/.openclaw/\` — edit, commit, push directly
- \`SOUL.md\`, \`TOOLS.md\`, \`HEARTBEAT.md\`, \`health-check.sh\`, \`monitor-agent.sh\`, etc. now at repo root
- \`openclaw.json\` is gitignored (real tokens); \`openclaw.json.redacted\` is the tracked template
- \`agents/\`, \`skills/\`, \`lib/\`, \`launchd/\` all promoted to repo root from \`openclaw-config/\`
- \`doctor.sh\` updated: \`REPO_CONFIG\` now equals \`REPO_ROOT\`; redacted template used for JSON checks
- Pre-existing fix: add \`ThrottleInterval\` to \`scripts/mctrl-supervisor.plist.template\`

## Testing
- \`python -m pytest tests/test_openclaw_configs.py -q\` — 43 passed, 3 skipped
- \`bash scripts/doctor.sh\` — 65 pass, 0 warn, 2 fail (both pre-existing, not from migration)
  - FAIL 1: migrated cron job IDs missing from cron/jobs.json (pre-existing live config gap)
  - FAIL 2: openclaw gateway status not reporting "Runtime: running" (pre-existing plugin config warnings)
- Gateway is running and listening on port 18789 (verified via \`lsof -i :18789\`)

## Known Limitations
- The worktree at \`/Users/jleechan/project_jleechanclaw/worktree_debug\` is now stale — expected
- \`jleechan2015/openclaw_config\` repo is deprecated but not archived yet (Phase 5)
- Two pre-existing doctor.sh failures (cron job IDs, gateway status) are out of scope for this PR

Closes ORCH-d6i

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Moderate risk becaus
