# PR #464: [agento] fix(skeptic-cron): fix strftime("%s") jq datetime comparison

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-03-31
**Author:** jleechan2015
**Stats:** +1/-1 in 1 files

## Summary
- Fix `strftime("%s")` being called on a Unix timestamp integer, which jq parses as a broken-down time (year 1970), making the stale-check always fail and producing `CI: error` for ALL PRs
- Regression from `skeptic-cron` stale cursor[bot] check feature

## Test Plan
- Verify `jq -rs` CI expression now returns "success" for PR #461
- Run skeptic-cron manually to confirm PR #461 is 7-green and auto-merged

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Touches the auto-merge gating logic in `skeptic-cron`, so an incorrect condition could wrongly block or allow merges, but the change is a small, targeted bugfix to timestamp comparison.
> 
> **Overview**
> Fixes `skeptic-cron`’s CI gate by correcting the jq time comparison used in `skip_stale` for h

## Raw Body
## Summary
- Fix `strftime("%s")` being called on a Unix timestamp integer, which jq parses as a broken-down time (year 1970), making the stale-check always fail and producing `CI: error` for ALL PRs
- Regression from `skeptic-cron` stale cursor[bot] check feature

## Test plan
- Verify `jq -rs` CI expression now returns "success" for PR #461
- Run skeptic-cron manually to confirm PR #461 is 7-green and auto-merged

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Touches the auto-merge gating logic in `skeptic-cron`, so an incorrect condition could wrongly block or allow merges, but the change is a small, targeted bugfix to timestamp comparison.
> 
> **Overview**
> Fixes `skeptic-cron`’s CI gate by correcting the jq time comparison used in `skip_stale` for hung `cursor` check-runs. The new expression properly subtracts `.started_at | fromdateiso8601` from the current epoch (`$now`) instead of using a broken `strftime("%s")` conversion, preventing spurious `CI_STATUS=error` / always-failing stale detection.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit fe83f1e3e82e5c7e2ffb2e3fa4393e1cdc32c050. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->

## Summary by CodeRabbit

* **Chores**
  * Updated continuous integration workflow to improve accuracy in detecting stale background processes.

<!-- end of auto-generated comment: release notes by coderabbit.ai -->
