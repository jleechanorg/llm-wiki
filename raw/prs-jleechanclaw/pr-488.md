# PR #488: [agento] [P2] feat(evidence-review-gate): add comprehensive tests + webhook import hardening

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-04-04
**Author:** jleechan2015
**Stats:** +405/-2 in 2 files

## Summary
- Add 49-unit test suite for `evidence_review_gate.py` covering verdict extraction, GraphQL/REST API paths, fail-closed behavior, re-review latest-verdict-wins logic, and requester/CLI functions
- Move `symphony_daemon` import from lazy (inside `_dispatch()`) to module-level fail-fast in `webhook.py` with explicit `ImportError` tracking; `_dispatch` now raises a clear `DispatchError` with the root cause rather than masking it behind a generic "symphony dispatch failed" message

## Raw Body
## Summary
- Add 49-unit test suite for `evidence_review_gate.py` covering verdict extraction, GraphQL/REST API paths, fail-closed behavior, re-review latest-verdict-wins logic, and requester/CLI functions
- Move `symphony_daemon` import from lazy (inside `_dispatch()`) to module-level fail-fast in `webhook.py` with explicit `ImportError` tracking; `_dispatch` now raises a clear `DispatchError` with the root cause rather than masking it behind a generic "symphony dispatch failed" message

## Testing
- `PYTHONPATH=src python -m pytest src/tests/test_evidence_review_gate.py -v` → 49 passed
- Full suite `PYTHONPATH=src python -m pytest tests/ src/tests/ -q` → 697 passed, 108 skipped
- `python3 -c "from orchestration.webhook import WebhookIngress, RemediationWorker; print('OK')"` → module imports cleanly

## Low-level details

### src/tests/test_evidence_review_gate.py (new file)
- 49 unit tests across 6 test classes
- `TestExtractVerdict`: 15 parametrized cases covering `EVIDENCE BUNDLE REVIEW: X`, `Verdict: X`, and `**X**` formats; negative cases for malformed/non-verdict strings
- `TestGetReviews`: GraphQL path, empty reviews, **REST fallback** (rate-limit triggers gh api REST), malformed REST JSON → RuntimeError, unknown state → "commented"
- `TestGetIssueComments`: GraphQL path, empty, **REST fallback** (rate-limit triggers gh api REST)
- `TestCheckEvidenceReview`: PASS verdict from review/comment, WARN/FAIL → fail-closed, **both sources fail → fail-closed**, no verdict → fail-closed, **latest verdict wins on re-review**, one source fails → still works
- `TestBuildEvidenceReviewRequest`: includes `/er` command, correct blob/HEAD URL, PASS/FAIL verdict format request
- `TestRequestEvidenceReview`: success returns True, `CalledProcessError` → False, `TimeoutExpired` → False

### src/orchestration/webhook.py
- Added `try/except ImportError` at module level for `symphony_daemon` imports; stores `build_workflow`, `build_runner_script` as None on failure and captures `_s
