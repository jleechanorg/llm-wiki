# PR #484: docs: Slack MCP setup, HTTP 2064 guidance, SmartClaw audit sync

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-04-03
**Author:** jleechan2015
**Stats:** +217/-10 in 7 files

## Summary
- **CLAUDE.md**: Operational note for **HTTP 2064** / high load — avoid fan-out attach/diagnose; backoff and lower concurrency (same spirit as WS churn guidance).
- **workspace/TOOLS.md**: Cursor Slack MCP config (`~/.cursor/mcp.json`), restart requirement, redacted example path, usage vs curl fallbacks.
- **docs/examples/cursor-mcp-slack.example.json**: Placeholder-only example for Cursor MCP (no real tokens).
- **agent-orchestrator.yaml**: `defaults.model: claude-sonnet-4-6` aligned with proje

## Raw Body
## Summary

- **CLAUDE.md**: Operational note for **HTTP 2064** / high load — avoid fan-out attach/diagnose; backoff and lower concurrency (same spirit as WS churn guidance).
- **workspace/TOOLS.md**: Cursor Slack MCP config (`~/.cursor/mcp.json`), restart requirement, redacted example path, usage vs curl fallbacks.
- **docs/examples/cursor-mcp-slack.example.json**: Placeholder-only example for Cursor MCP (no real tokens).
- **agent-orchestrator.yaml**: `defaults.model: claude-sonnet-4-6` aligned with project `agentConfig`.
- **SMARTCLAW_PORTABILITY_AUDIT**: Markdown refresh + paired **.html** (dark layout, mermaid).
- **scripts/smartclaw-export-map.tsv**: Additional workflow/skill mappings from audit.

## Verification

- Slack bot token: `auth.test` returns **ok** (local smoke).
- `staging-gateway.pid` removed (was untracked junk).

## Merge

Per repo policy: do not merge without explicit approval; let CI + 7-green gate run.

Made with [Cursor](https://cursor.com)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Low risk: primarily documentation/audit artifacts plus a small `agent-orchestrator.yaml` default model setting change that may affect how new AO sessions pick a model.
> 
> **Overview**
> Adds operational guidance for handling LLM provider **HTTP 2064/high-load** conditions (avoid fan-out, use backoff, and reduce concurrency) and expands local docs for **Slack MCP** usage, including Cursor setup and a redacted `~/.cursor/mcp.json` example.
> 
> Updates `agent-orchestrator.yaml` to set a default `model` (`claude-sonnet-4-6`). Refreshes the SmartClaw portability audit output (updated counts/timestamp) and adds a generated dark-mode HTML version, along with updating the portability export map to include additional portable assets (e.g., `staging-canary-full.yml`, new skills/tests/launchd mappings).
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 14bfe43e6f41224a1f2be5ae44e963765e93baa0. This will update automati
