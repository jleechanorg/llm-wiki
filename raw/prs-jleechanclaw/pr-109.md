# PR #109: Add sym routing to launchd-managed Symphony daemon

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-03-12
**Author:** jleechan2015
**Stats:** +1292/-2 in 20 files

## Summary
- add a new `sym` OpenClaw skill for explicit Symphony routing
- add a repo-local launchd-managed Symphony daemon install/setup path
- add plugin-based task payload queueing (`generic_tasks`, `leetcode_hard`, `swe_bench_verified`)
- add helper scripts for freeform dispatch and 5-task benchmark batches
- document new Symphony scripts in README

## Raw Body
## Summary
- add a new `sym` OpenClaw skill for explicit Symphony routing
- add a repo-local launchd-managed Symphony daemon install/setup path
- add plugin-based task payload queueing (`generic_tasks`, `leetcode_hard`, `swe_bench_verified`)
- add helper scripts for freeform dispatch and 5-task benchmark batches
- document new Symphony scripts in README

## Implementation
- added daemon builder and launchd helpers in `src/orchestration/symphony_daemon.py`
- added plugin registry/types/loaders in `src/orchestration/symphony_plugins.py`
- added scripts:
  - `scripts/install-symphony-daemon.sh`
  - `scripts/setup-symphony-daemon.py`
  - `scripts/sym-dispatch.sh`
  - `scripts/enqueue-symphony-tasks.sh`
  - `scripts/prepare-symphony-payload.py`
  - `scripts/enqueue-symphony-memory-issues.exs`
  - `scripts/sym-send-5-leetcode-hard.sh`
  - `scripts/sym-send-5-swebench-verified.sh`
- added skill/config fixtures under `openclaw-config/skills/sym/` and `openclaw-config/symphony/`
- added tests for workflow/runner/plist builders, plugin parsing, and skill presence

## Validation
- `pytest -q tests/test_symphony_daemon.py tests/test_openclaw_configs.py`
  - `42 passed, 2 skipped`
- manual E2E:
  - installed daemon via `./scripts/install-symphony-daemon.sh`
  - queued freeform task via `./scripts/sym-dispatch.sh "..."`
  - queued plugin batches via `./scripts/sym-send-5-leetcode-hard.sh` and `./scripts/sym-send-5-swebench-verified.sh`
  - verified daemon API state via `curl http://127.0.0.1:<port>/api/v1/state`

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Adds new local daemon provisioning and task injection scripts (launchd, filesystem writes, and RPC into an Elixir node), which can fail in environment-specific ways if misconfigured. Core orchestration behavior change is otherwise isolated and covered by new unit tests.
> 
> **Overview**
> Adds a new **`sym` routing path** that dispatches tasks to a local, launchd-managed Symphony daemon instead of the existing m
