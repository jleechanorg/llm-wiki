# PR #51: ORCH-jym: validate and resolve repo targets via allowlist

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-03-05
**Author:** jleechan2015
**Stats:** +51/-0 in 4 files

## Summary
(none)

## Raw Body
## Summary\n- add allowlisted repo constants for approved repo routing\n- add strict owner/name repo validation\n- add repo target resolution with default fallback and validation\n- add targeted tests for unknown repo rejection and default resolution behavior\n\n## Test\n- python3 -m pytest -q src/tests/test_gh_integration.py

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Adds isolated validation/selection helpers plus tests, with no evidence of existing call sites being changed; primary risk is operational if the allowlist is incomplete when wired into routing.
> 
> **Overview**
> Adds a new `gh_integration` helper that *restricts GitHub repository targets* to a fixed `ALLOWED_REPOS` allowlist and enforces strict `owner/name` formatting via regex validation.
> 
> Introduces `resolve_repo_target()` to select a default repo when no input is provided while still applying allowlist/format checks, and adds pytest coverage for unknown repo rejection, default selection behavior, and format enforcement.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 6fa15c6dc69af4f4340ca0c3076ceb07189cdf0d. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
