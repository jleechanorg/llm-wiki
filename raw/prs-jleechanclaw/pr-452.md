# PR #452: [agento] fix(scripts): anchor spawn success grep to ✓ to prevent false positives

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-03-30
**Author:** jleechan2015
**Stats:** +923/-50 in 12 files

## Summary
When `ao spawn --claim-pr` fails during `git checkout` (e.g., branch locked by another worktree), the error message printed is: `"Session X was created, but failed to claim PR Y: ..."`. This message contains the substring "Session X was created, but failed to claim PR" which matched the unanchored grep pattern `"Session .* created and claimed PR"` in `github-intake.sh`, causing the poller to log SUCCESS even though checkout failed.

## Background
When `ao spawn --claim-pr` fails during `git checkout` (e.g., branch locked by another worktree), the error message printed is: `"Session X was created, but failed to claim PR Y: ..."`. This message contains the substring "Session X was created, but failed to claim PR" which matched the unanchored grep pattern `"Session .* created and claimed PR"` in `github-intake.sh`, causing the poller to log SUCCESS even though checkout failed.

## Raw Body
## Background
When `ao spawn --claim-pr` fails during `git checkout` (e.g., branch locked by another worktree), the error message printed is: `"Session X was created, but failed to claim PR Y: ..."`. This message contains the substring "Session X was created, but failed to claim PR" which matched the unanchored grep pattern `"Session .* created and claimed PR"` in `github-intake.sh`, causing the poller to log SUCCESS even though checkout failed.

## Goal
Prevent false SUCCESS signals when `ao spawn --claim-pr` fails.

## Root Cause
`github-intake.sh` line 190:
```bash
if [[ "$spawn_rc" -eq 0 ]] && echo "$spawn_output" | grep -q "Session .* created and claimed PR"; then
```
The error output `"Session X was created, but failed to claim PR Y: ..."` matches `"Session .* created and claimed PR"` (grep does substring match, not whole-line match).

## Fix
Anchor the grep pattern to `✓` — the checkmark character is set by `ora`'s `spinner.succeed()` and only appears in genuine success output, not in error messages.

- `github-intake.sh`: `grep -q "✓.*Session .* created and claimed PR"`
- `audit-pr-session-coverage.sh`: `grep -qE '✓.*Session.*created and claimed PR'` (in `is_claim_success()`)

## Testing
- [x] Verified error message format: `"✗ Session {id} was created, but failed to claim PR {n}: {reason}"` — contains "Session" but NOT "✓"
- [x] Verified success format: `✓ Session {id} created and claimed PR` — contains "✓"
- [x] Pattern `✓.*Session.*created and claimed PR` only matches success, not error

## Beads
- Closes orch-h5w

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Mostly operational script changes, but they affect automated AO dispatch, Slack followups, launchd jobs, and gateway upgrade/rebuild behavior; mistakes could cause missed dispatches or unintended restarts/rebuilds.
> 
> **Overview**
> Prevents false-positive AO dispatch successes by tightening the `ao spawn --claim-pr` succ
