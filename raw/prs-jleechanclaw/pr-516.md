# PR #516: [agento] fix(canary): add --prefix to npm ls so SDK version is detected correctly

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-04-05
**Author:** jleechan2015
**Stats:** +4/-3 in 1 files

## Summary
- Fix check 5 (SDK protocol version) in `staging-canary.sh` — `npm ls` was running from the worktree directory (no node_modules), returning empty and triggering the fail-closed error
- Add `--prefix ~/.openclaw` so `npm ls` resolves from the correct install location
- Also add `~/.openclaw` as a fallback path in the secondary node resolve
- Both staging (port 18810) and prod (port 18789) canaries now pass 9/9

## Test Plan
- [x] Worktree version of `staging-canary.sh --port 18810` → 9 PASS / 0 FAIL
- [x] Worktree version of `staging-canary.sh --port 18789` → 9 PASS / 0 FAIL
- [x] SDK 0.14.1 detected correctly (minor=14 ≤ 16, compatible)

## Raw Body
## Summary
- Fix check 5 (SDK protocol version) in `staging-canary.sh` — `npm ls` was running from the worktree directory (no node_modules), returning empty and triggering the fail-closed error
- Add `--prefix ~/.openclaw` so `npm ls` resolves from the correct install location
- Also add `~/.openclaw` as a fallback path in the secondary node resolve
- Both staging (port 18810) and prod (port 18789) canaries now pass 9/9

## Test plan
- [x] Worktree version of `staging-canary.sh --port 18810` → 9 PASS / 0 FAIL
- [x] Worktree version of `staging-canary.sh --port 18789` → 9 PASS / 0 FAIL
- [x] SDK 0.14.1 detected correctly (minor=14 ≤ 16, compatible)

## Testing
Real smoke test — ran both staging and prod canaries with the fixed script.

Fixes: staging-canary.sh check 5 failure (eloop cycle 2026-04-05)

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Low risk: updates only canary-script version detection logic without affecting runtime gateway behavior.
> 
> **Overview**
> **Fixes false failures in canary Check 5 (SDK protocol version).** The script now runs `npm ls @agentclientprotocol/sdk` with `--prefix "$HOME/.openclaw"` so it resolves from the OpenClaw install directory.
> 
> If `npm ls` still can’t find the SDK, the Node fallback resolver now also searches `~/.openclaw` (in addition to the Homebrew global install path) to detect locally installed packages.
> 
> <sup>Reviewed by [Cursor Bugbot](https://cursor.com/bugbot) for commit 69936d1e1e898f1da06cd5e7de85d3bfc22acb10. Bugbot is set up for automated code reviews on this repo. Configure [here](https://www.cursor.com/dashboard/bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->

## Summary by CodeRabbit

* **Chores**
  * Enhanced SDK protocol version detection by broadening package discovery paths, improving system initialization reliability and compatibility verification.

<!--
