# PR #320: [agento] fix: retire ao-pr-poller plist, add ao lifecycle-worker for agent-orchestrator

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-03-21
**Author:** jleechan2015
**Stats:** +4/-1 in 2 files

## Summary
Two beads identified during /auton diagnostic (2026-03-20):
- **orch-oox**: `ai.ao-pr-poller.plist` references the deprecated `scripts/ao-pr-poller.sh` shell script. The script was crashing (exit 32512 = command not found) on every 5-min invocation. The real PR polling mechanism is `ao lifecycle-worker` (npm binary) running via `com.agentorchestrator.lifecycle-*` launchd plists.
- **orch-ies**: No `com.agentorchestrator.lifecycle-agent-orchestrator` launchd plist existed. The 4 open PRs in `jlee

## Background
Two beads identified during /auton diagnostic (2026-03-20):
- **orch-oox**: `ai.ao-pr-poller.plist` references the deprecated `scripts/ao-pr-poller.sh` shell script. The script was crashing (exit 32512 = command not found) on every 5-min invocation. The real PR polling mechanism is `ao lifecycle-worker` (npm binary) running via `com.agentorchestrator.lifecycle-*` launchd plists.
- **orch-ies**: No `com.agentorchestrator.lifecycle-agent-orchestrator` launchd plist existed. The 4 open PRs in `jleechanorg/agent-orchestrator` were not being watched by any lifecycle worker.

## Raw Body
## Background
Two beads identified during /auton diagnostic (2026-03-20):
- **orch-oox**: `ai.ao-pr-poller.plist` references the deprecated `scripts/ao-pr-poller.sh` shell script. The script was crashing (exit 32512 = command not found) on every 5-min invocation. The real PR polling mechanism is `ao lifecycle-worker` (npm binary) running via `com.agentorchestrator.lifecycle-*` launchd plists.
- **orch-ies**: No `com.agentorchestrator.lifecycle-agent-orchestrator` launchd plist existed. The 4 open PRs in `jleechanorg/agent-orchestrator` were not being watched by any lifecycle worker.

## Goals
1. Stop the ao-pr-poller crash loop — remove it from active install
2. Start watching `jleechanorg/agent-orchestrator` PRs autonomously

## Tenets
- Preserve the deprecated plist as `.deprecated` for reference history
- Model the new plist exactly on the working `lifecycle-jleechanclaw` plist
- Minimal scope — only the two beads, no other changes

## High-level description of changes
- `launchd/ai.ao-pr-poller.plist` → renamed to `.deprecated` (no longer installed)
- `launchd/com.agentorchestrator.lifecycle-agent-orchestrator.plist` — new, runs `ao lifecycle-worker agent-orchestrator` with KeepAlive
- `scripts/install-ao-lifecycle-agent-orchestrator.sh` — new install/uninstall script
- `scripts/install.sh` — replaced ao-pr-poller entry with new lifecycle installer

## Testing
- `ao lifecycle-worker agent-orchestrator` runs successfully (ao binary at `~/bin/ao` verified)
- New plist modeled 1:1 on working `lifecycle-jleechanclaw` plist (PID 1133, exit 0)
- To activate: `./scripts/install-ao-lifecycle-agent-orchestrator.sh`

## Low-level details
- **`launchd/ai.ao-pr-poller.plist.deprecated`**: Preserved for reference. Was crashing every 5 min; no longer loaded by install.sh.
- **`launchd/com.agentorchestrator.lifecycle-agent-orchestrator.plist`**: KeepAlive=true, ThrottleInterval=30, logs to `/tmp/ao-lifecycle-agent-orchestrator.{log,err.log}`.
- **`scripts/install-ao-lifecycle-
