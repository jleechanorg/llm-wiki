# PR #486: [agento] fix(doctor): update GATEWAY_LABEL to com.openclaw.gateway

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-04-04
**Author:** jleechan2015
**Stats:** +255/-132 in 2 files

## Summary
PR #485 renamed the production gateway launchd label from `ai.openclaw.gateway` to `com.openclaw.gateway`, but `scripts/doctor.sh` line 9 still had the old label. This caused monitor-agent to report `STATUS=PROBLEM` with 4 FAIL checks despite the gateway being healthy.

## Background
PR #485 renamed the production gateway launchd label from `ai.openclaw.gateway` to `com.openclaw.gateway`, but `scripts/doctor.sh` line 9 still had the old label. This caused monitor-agent to report `STATUS=PROBLEM` with 4 FAIL checks despite the gateway being healthy.

## Raw Body
## Background

PR #485 renamed the production gateway launchd label from `ai.openclaw.gateway` to `com.openclaw.gateway`, but `scripts/doctor.sh` line 9 still had the old label. This caused monitor-agent to report `STATUS=PROBLEM` with 4 FAIL checks despite the gateway being healthy.

## Goals

Fix doctor.sh so it finds the correct plist/label and monitor reports STATUS=OK.

## Tenets

One-line fix only — no scope creep.

## High-level description of changes

Changed `GATEWAY_LABEL="ai.openclaw.gateway"` → `GATEWAY_LABEL="com.openclaw.gateway"` on line 9 of `scripts/doctor.sh`.

## Testing

Ran `bash scripts/doctor.sh` after the fix — zero [FAIL] or [WARN] lines, all gateway checks [PASS].

## Low-level details

- **scripts/doctor.sh line 9**: `GATEWAY_LABEL` updated to match installed plist name.

Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Low risk one-line shell constant update that only affects local diagnostics/monitoring output, not runtime gateway behavior.
> 
> **Overview**
> Fixes a false-negative in `scripts/doctor.sh` by updating `GATEWAY_LABEL` to the current launchd label `com.openclaw.gateway`, ensuring the script finds the right plist and stops reporting healthy gateways as failed.
> 
> <sup>Reviewed by [Cursor Bugbot](https://cursor.com/bugbot) for commit c49ce42c19f1c3e2d8924eef645b8eadcfec0d92. Bugbot is set up for automated code reviews on this repo. Configure [here](https://www.cursor.com/dashboard/bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->

## Summary by CodeRabbit

* **Chores**
  * Updated the gateway service registration identifier to ensure proper system service management and compatibility.

<!-- end of auto-generated comment: release notes by coderabbit.ai -->
