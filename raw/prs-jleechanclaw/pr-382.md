# PR #382: harness: CR body_len=0 fallback for trivial diffs — prevents manual unblocking loop

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-03-24
**Author:** jleechan2015
**Stats:** +5/-2 in 2 files

## Summary
Criterion 3 requires body_len > 0 on CR APPROVED reviews. CR gives body_len=0 on simple diffs even when genuinely reviewing. This session required manually unblocking 6 PRs (#374, #373, #376, #377, #378, #379). Harness escalation trigger fired.

## Background
Criterion 3 requires body_len > 0 on CR APPROVED reviews. CR gives body_len=0 on simple diffs even when genuinely reviewing. This session required manually unblocking 6 PRs (#374, #373, #376, #377, #378, #379). Harness escalation trigger fired.

## Raw Body
## Background
Criterion 3 requires body_len > 0 on CR APPROVED reviews. CR gives body_len=0 on simple diffs even when genuinely reviewing. This session required manually unblocking 6 PRs (#374, #373, #376, #377, #378, #379). Harness escalation trigger fired.

## Changes
- agent-orchestrator.yaml: two-path criterion 3 (body_len>0 OR body_len=0+CR verbal confirm) + 10-min timeout
- CLAUDE.md: mirrored update

## Testing
All 6 PRs today had body_len=0 + CR reply 'Yes, all good!' — path B covers them automatically.

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Low risk: documentation/config-only changes that adjust merge-gate validation criteria, with no runtime code paths modified.
> 
> **Overview**
> Clarifies the **6-point PR green criteria** to treat CodeRabbit `APPROVED` reviews as genuine via two paths: either a non-empty review body, or an empty-body `APPROVED` that is corroborated by CodeRabbit replying “all good”/✅ after `@coderabbitai all good?`.
> 
> Mirrors this rule update in both `CLAUDE.md` and `agent-orchestrator.yaml`, including guidance to prompt for confirmation and escalate to a human if CodeRabbit doesn’t reply within 10 minutes when `body_len=0`.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit de2c75ccf3ab54186d6c21a6ec4fbc13cd21b1fa. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
