# PR #135: fix: hardcode tokens directly in openclaw.json, eliminate all ${ENV_VAR} placeholders

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-03-14
**Author:** jleechan2015
**Stats:** +134/-1327 in 16 files

## Summary
The gateway was using \`\${ENV_VAR}\` placeholders in \`openclaw.json\` that were resolved from launchd plist \`EnvironmentVariables\` at runtime. When the plist and \`~/.bashrc\` drifted (after token rotation), Slack Socket Mode crashed with \`invalid_auth\`.

**Fix: delete all \${ENV_VAR} indirection — hardcode real tokens directly in \`openclaw.json\`.**

## Raw Body
## Summary

The gateway was using \`\${ENV_VAR}\` placeholders in \`openclaw.json\` that were resolved from launchd plist \`EnvironmentVariables\` at runtime. When the plist and \`~/.bashrc\` drifted (after token rotation), Slack Socket Mode crashed with \`invalid_auth\`.

**Fix: delete all \${ENV_VAR} indirection — hardcode real tokens directly in \`openclaw.json\`.**

## Changes

| Before | After |
|--------|-------|
| \`openclaw.json\` tracked in git with \`\${ENV_VAR}\` placeholders | \`openclaw.json\` gitignored, real tokens baked in directly |
| Gateway resolved tokens from plist \`EnvironmentVariables\` | Plist has no secrets; gateway reads only \`openclaw.json\` |
| All \`agents/*/agent/models.json\` tracked with \`\${OPENROUTER_API_KEY}\` etc | Gitignored; real API keys on disk, never committed |
| \`discord-eng-bot/openclaw.json\` tracked with 5 placeholders | Untracked via \`openclaw.json\` gitignore rule; real values on disk |
| \`launchd/\` plist template had 10 secret env var entries | All secret env vars removed from plist template |

## What was deleted / gitignored

- **Removed from git tracking:** \`openclaw.json\`, \`agents/*/agent/models.json\`, \`discord-eng-bot/openclaw.json\`
- **Deleted stale files:** \`openclaw.json.bak\`, 21 \`agents/main/agent/models.json.*.tmp\` files
- **Scrubbed from plist template:** \`DISCORD_BOT_TOKEN\`, \`MINIMAX_API_KEY\`, \`OPENCLAW_SLACK_APP_TOKEN\`, \`OPENCLAW_SLACK_BOT_TOKEN\`, \`OPENAI_API_KEY\`, \`XAI_API_KEY\`, \`GOG_KEYRING_PASSWORD\`, \`OPENCLAW_GATEWAY_TOKEN\`

## Bonus fixes (same branch)

- \`monitor-agent.sh\`: Phase 1 self-heal — when \`appToken\` probe fails, resolves any remaining \`\${ENV_VAR}\` in \`openclaw.json\` from \`~/.profile\`/\`~/.bashrc\` using atomic write (tempfile + \`os.replace\`)
- \`health-check.sh\`: split alert cooldown stamps into per-state (unhealthy/recovered), fix operator precedence in cooldown condition
- \`openclaw_notifier.py\`: dead-letter reporting improvements (oldest 
