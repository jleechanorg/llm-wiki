# PR #301: fix(harness): deterministic analyzer auth token resolution

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-03-20
**Author:** jleechan2015
**Stats:** +157/-4 in 2 files

## Summary
Fixes a harness engineering violation in [2026-03-20 09:01:08] === Starting Harness Engineering Analysis === where GitHub auth behaved non-deterministically based on whether  was set as a literal token or a file path.

## Raw Body
## Summary
Fixes a harness engineering violation in [2026-03-20 09:01:08] === Starting Harness Engineering Analysis === where GitHub auth behaved non-deterministically based on whether  was set as a literal token or a file path.

## Problem
The previous logic always treated  as a file path and failed when it was provided as a token string via env.

## Fix
- Added deterministic token resolution order:
  1.  env literal
  2.  env literal (when not a readable file path)
  3.  as file path
  4. default  file
- Normalized file token reads by stripping newlines.
- Improved error message with explicit remediation paths.

## Harness engineering alignment
- **Config-first/deterministic behavior:** one clear auth contract, no ambiguous fallback behavior.
- **Docs as infra:** code now reflects explicit runtime token policy.

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Medium risk because it changes GitHub authentication resolution for an automated job; failures would block cloning/PR/comment operations, though the change is small and now covered by focused tests.
> 
> **Overview**
> **Fixes non-deterministic GitHub token handling in `scripts/harness-analyzer.sh`.** Adds `resolve_gh_token()` to deterministically choose credentials in priority order (prefer `GH_TOKEN`, otherwise treat `GITHUB_TOKEN` as a literal unless it points to a file, then fall back to a token file) and normalizes file reads by stripping newlines.
> 
> Improves the missing-token error message with explicit remediation paths, and adds `tests/test_harness_analyzer_auth.sh` to validate all supported auth input modes (including newline trimming and failure when no token is available).
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit ad701e9ea1a457792e2ab8fe0833261ac9b75dd7. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release 
