# PR #408: [P2] feat(eloop): add System Harness Audit phase to detect structural gaps

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-03-27
**Author:** jleechan2015
**Stats:** +396/-28 in 9 files

## Summary
- Adds Phase 1e "System Harness Audit" to `/eloop` that detects structural gaps invisible to PR-level observation: orphaned `/claw` dispatches, stale worktrees, lifecycle-worker drift, and GraphQL rate limit exhaustion
- Updates Phase 2 decision tree so Phase 1e system gaps trigger Phase 3 even when PR metrics look healthy
- Adds Phase 3a-system to route system gaps through `/harness`

## Raw Body
## Summary
- Adds Phase 1e "System Harness Audit" to `/eloop` that detects structural gaps invisible to PR-level observation: orphaned `/claw` dispatches, stale worktrees, lifecycle-worker drift, and GraphQL rate limit exhaustion
- Updates Phase 2 decision tree so Phase 1e system gaps trigger Phase 3 even when PR metrics look healthy
- Adds Phase 3a-system to route system gaps through `/harness`

## Changes
- **`.claude/commands/eloop.md`** (new file): Full `/eloop` skill with Phase 1e, updated decision tree, and Phase 3a-system

## Testing
- Manual: Created a fake orphaned dispatch file (`/tmp/openclaw/.claw-track-test-orphan`) and verified Phase 1e detects it
- Verified decision tree modification triggers Phase 3 when system gaps are present
- Verified Phase 3a-system bash snippets are syntactically valid

Closes orch-sga

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Medium risk because it changes the `skeptic-cron` auto-merge gate behavior (including auto-posting verdict comments) and adjusts automation used to create/merge PRs and run launchd jobs, which could affect CI/merging if misconfigured.
> 
> **Overview**
> Introduces a new `.claude/commands/eloop.md` skill defining a 10-minute “evolve loop” that measures `[agento]` zero-touch rate and adds **Phase 1e system harness audit** checks (dispatch delivery/orphans, worktree buildup, lifecycle-worker drift, GitHub rate-limit budget) that can force deeper diagnosis even when PR metrics look healthy.
> 
> Hardens automation around the loop and PR lifecycle: `skeptic-cron.yml` now treats a missing skeptic verdict as *auto-postable* (and fail-closes on fetch errors) before merging, `mem0_recall.py` avoids emitting output when only stale memories were filtered, `simplify-daily.sh` detects work via “commits ahead” and uses the resolved base branch when creating PRs, and `review_stress_test/run.py` handles missing `ao` CLI as a caught error.

