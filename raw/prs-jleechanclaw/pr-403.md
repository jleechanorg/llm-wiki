# PR #403: [P2] feat(orch-k0e): PR session coverage audit + lifecycle-worker diagnostic skill

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-03-27
**Author:** jleechan2015
**Stats:** +685/-2 in 5 files

## Summary
- `scripts/audit-pr-session-coverage.sh`: cross-references open PRs with active AO sessions, claims orphaned PRs (no session, stale >1h), posts Slack summary. Idempotent — safe for cron.
- `skills/diagnose-lifecycle-worker.md`: guided triage skill for `claim_failed` loops. Covers ghost worktrees, wrong main-branch state, orphaned session metadata. Escalation path via MCP mail.

## Raw Body
## Summary
- `scripts/audit-pr-session-coverage.sh`: cross-references open PRs with active AO sessions, claims orphaned PRs (no session, stale >1h), posts Slack summary. Idempotent — safe for cron.
- `skills/diagnose-lifecycle-worker.md`: guided triage skill for `claim_failed` loops. Covers ghost worktrees, wrong main-branch state, orphaned session metadata. Escalation path via MCP mail.

## Testing
- Audit script run manually against jleechanclaw project: YAML parsing ✓, rate-limit guard ✓, Slack posting ✓
- Skill verified discoverable in Claude Code at `~/.claude/skills/diagnose-lifecycle-worker.md`
- Repo copy committed at `skills/diagnose-lifecycle-worker.md`

Closes #orch-k0e

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Adds a new automation script that can claim PRs via `ao spawn` and post to Slack; misconfiguration or parsing issues could trigger unintended claims or noisy alerts (mitigated by rate-limit checks and `AUDIT_DRY_RUN`).
> 
> **Overview**
> Introduces `scripts/audit-pr-session-coverage.sh`, a cron-friendly audit that cross-references open GitHub PRs with active AO sessions, flags *stale (1h+)* orphaned PRs, and attempts to auto-claim them via `ao spawn` with stricter success validation, rate-limit guarding, and optional `AUDIT_DRY_RUN`.
> 
> Adds a `skills/diagnose-lifecycle-worker.md` runbook for triaging `lifecycle-worker` `claim_failed` loops (ghost worktrees, wrong branch, orphaned metadata) plus an implementation plan doc and a smoke-test log artifact; updates `.coderabbit.yaml` changelog entries accordingly.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 392d9b3afdc2022fa9b01be04a823aab56868337. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->
## Summary by CodeRabbit

* **New Features**
  * Automated PR-audit job: cross-che
