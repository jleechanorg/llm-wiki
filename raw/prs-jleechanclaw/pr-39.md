# PR #39: feat(backup): harden openclaw backup — rsync, latest/ symlink, triple-scheduler stagger, watchdog

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-03-04
**Author:** jleechan2015
**Stats:** +0/-0 in 0 files

## Summary
- Replace shutil with rsync + post-redaction for faster incremental backups with `--delete` cleanup
- Add `latest/` symlink after each successful commit (consumers no longer scan timestamps)
- Fix all three scheduler timing conflicts (all fired at `:00`) — now staggered :00/:20/:40
- Fix wrong target repo in openclaw-cron job and sync script
- Add hourly `backup-watchdog.sh` that alerts via Slack + email if newest backup is >6h old
- Add one-time `consolidate-workspace-snapshots.sh` to rsync ~29

## Raw Body
## Summary

- Replace shutil with rsync + post-redaction for faster incremental backups with `--delete` cleanup
- Add `latest/` symlink after each successful commit (consumers no longer scan timestamps)
- Fix all three scheduler timing conflicts (all fired at `:00`) — now staggered :00/:20/:40
- Fix wrong target repo in openclaw-cron job and sync script
- Add hourly `backup-watchdog.sh` that alerts via Slack + email if newest backup is >6h old
- Add one-time `consolidate-workspace-snapshots.sh` to rsync ~295 misrouted workspace snapshots

## Changes

| File | Before → After |
|------|----------------|
| `scripts/backup-openclaw-full.sh` | Python shutil copy → rsync mirror + Python post-redaction + `latest/` symlink |
| `scripts/install-openclaw-backup-jobs.sh` | system cron `0 */4` → `40 */4`; add watchdog hourly cron |
| `openclaw-config/cron/jobs.json` | openclaw-cron: fix path (`workspace/openclaw` → `project_jleechanclaw/jleechanclaw`) + schedule `0 */4` → `20 */4` |
| `openclaw/scripts/sync_openclaw_full_redacted.sh` | clones `jleechanorg/openclaw` → `jleechanorg/jleechanclaw` |
| `scripts/backup-watchdog.sh` | **new** — hourly watchdog: Slack webhook + email if backup >6h stale |
| `scripts/consolidate-workspace-snapshots.sh` | **new** — one-time rsync of workspace snapshots into this repo |
| `roadmap/BACKUP_REDUNDANCY_DESIGN.md` | **new** — design doc covering all changes |

**Triple-scheduler stagger:**
| Scheduler | Offset | Config |
|-----------|--------|--------|
| launchd | ~:00 | `StartInterval: 14400` (unchanged) |
| openclaw-cron | :20 | `jobs.json` expr `20 */4 * * *` |
| system cron | :40 | crontab `40 */4 * * *` |

## Testing

- `python -m pytest src/tests/ -q` → **383 passed, 4 skipped** (no regressions)
- Backup script validated by reading before/after — rsync step, redaction pass, symlink creation all present
- Scheduler stagger verified in jobs.json diff and install script diff
- Wrong-repo fix confirmed in sync script and cron job message

##
