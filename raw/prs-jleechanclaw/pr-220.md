# PR #220: fix(orchestrator): add CI unstable handling rules to prevent agent stalls

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-03-16
**Author:** jleechan2015
**Stats:** +42/-0 in 2 files

## Summary
Add rules to SOUL.md (via agent-orchestrator.yaml) to prevent agents from stalling when CI is unstable:

### Changes

1. **CI Unstable / Failure — Act Immediately**: When  or any CI check shows FAILURE/in_progress:
   - Do NOT ask for permission. Do NOT wait.
   - Immediately run  to identify failing check
   - Then fix it with /copilot or direct code changes
   - This is a standing directive — never pause when CI is unstable

2. **Post-@coderabbitai Monitoring**: After posting :
   - Do NOT sto

## Raw Body
## Summary

Add rules to SOUL.md (via agent-orchestrator.yaml) to prevent agents from stalling when CI is unstable:

### Changes

1. **CI Unstable / Failure — Act Immediately**: When  or any CI check shows FAILURE/in_progress:
   - Do NOT ask for permission. Do NOT wait.
   - Immediately run  to identify failing check
   - Then fix it with /copilot or direct code changes
   - This is a standing directive — never pause when CI is unstable

2. **Post-@coderabbitai Monitoring**: After posting :
   - Do NOT stop and wait for human
   - Check CI status every 2 minutes with 
   - Loop until green (all pass) or red (definite failure)
   - Only exit when PR merged, closed, or explicit human instruction

### Testing

- Rules added to defaults section (applies to all projects)
- Rules also added to jleechanclaw project agentRules specifically

@coderabbitai all good?

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Updates operational rules and tightens `/ao-notify` authentication behavior; the webhook change could break existing callers that relied on tokenless, headerless access and may affect alerting/escalation flows.
> 
> **Overview**
> Adds explicit runbook-style agent rules to **immediately react to unstable/failing CI** (auto-identify failing checks, fix, push, and re-ping CodeRabbit) and to **continue monitoring CI after posting** `@coderabbitai all good?`, applied both to global defaults and the `jleechanclaw` project.
> 
> Hardens `src/orchestration/webhook_ingress.py` by rejecting `/ao-notify` requests with **no `Authorization` header when no bearer token is configured**, preventing accidental open access to the endpoint.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 9f53fa9dad92cd23dd6fb02e153a43816bcc91eb. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.
