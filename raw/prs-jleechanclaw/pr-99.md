# PR #99: fix: CodeRabbit ping workflow — correct handle, guardrails, docs

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-03-12
**Author:** jleechan2015
**Stats:** +590/-0 in 8 files

## Summary
- **CodeRabbit re-review ping:** Use exact handle `@coderabbitai all good?` (no hyphen; `@coderabbit` / `@coderabbit-ai` do not notify the bot).
- **Guardrails:** Post only after pushing fixes for CodeRabbit comments; do not ping on a timer to avoid spam/duplicates.
- **Docs:** `docs/coderabbit-ping-workflow.md` documents correct behavior and diagnosis.
- **Commands:** `.claude/commands/coderabbit.md` and `cr.md` added with correct text and guardrails.

## Raw Body
## Summary
- **CodeRabbit re-review ping:** Use exact handle `@coderabbitai all good?` (no hyphen; `@coderabbit` / `@coderabbit-ai` do not notify the bot).
- **Guardrails:** Post only after pushing fixes for CodeRabbit comments; do not ping on a timer to avoid spam/duplicates.
- **Docs:** `docs/coderabbit-ping-workflow.md` documents correct behavior and diagnosis.
- **Commands:** `.claude/commands/coderabbit.md` and `cr.md` added with correct text and guardrails.

## Changes
- New: `docs/coderabbit-ping-workflow.md`, `.claude/commands/coderabbit.md`, `.claude/commands/cr.md`.
- Aligns with existing AGENTS.md/CLAUDE.md CodeRabbit Review Protocol.

## Testing
- Manual: invoke `/coderabbit` only after a fix push; comment body must be exactly `@coderabbitai all good?`.

## Known limitations
- worldarchitect.ai automation (comment-validation body) and worldai_claw commands are fixed in separate PRs in those repos.

Made with [Cursor](https://cursor.com)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Adds a new Slack ingestion script that fetches data from the Slack API and writes/promotes files under `~/.openclaw/memory`, and tightens `install-launchagents.sh` to hard-fail if expected launchd labels aren’t registered (could break installs in edge environments).
> 
> **Overview**
> Adds a Claude `/coderabbit` command (and `/cr` alias) plus docs to standardize the CodeRabbit re-review ping, including guardrails and the exact `@coderabbitai all good?` comment text.
> 
> Introduces `scripts/slack_history_to_memory.py` to incrementally fetch Slack channel + thread history, redact obvious secrets/PII, stage markdown + `manifest.json`, and optionally promote artifacts into `~/.openclaw/memory/slack-history/` (with a per-channel state file).
> 
> Updates `scripts/install-launchagents.sh` verification to also check expected launchd labels (including scheduled jobs and optional Mission Control services) and exit non-zero if any are missing; adds roadmap docs for futur
