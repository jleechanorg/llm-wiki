# PR #123: docs: add mem0 crash-resume runbook

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-03-15
**Author:** jleechan2015
**Stats:** +328/-0 in 3 files

## Summary
- Add a crash-resume runbook to `roadmap/MEM0_MIGRATION_PLAN.md` for mem0 50Q validation runs.
- Document deterministic restart commands (attached + detached) and required preflight checks.
- Add explicit completion artifact paths and post-crash checkpoint/reporting steps.

## Raw Body
## Summary
- Add a crash-resume runbook to `roadmap/MEM0_MIGRATION_PLAN.md` for mem0 50Q validation runs.
- Document deterministic restart commands (attached + detached) and required preflight checks.
- Add explicit completion artifact paths and post-crash checkpoint/reporting steps.

## Changes
- Before: `MEM0_MIGRATION_PLAN.md` described migration goals and validation criteria, but had no concrete operator runbook for interrupted runs.
- After: Added **Crash Resume Runbook (Added March 13, 2026)** with:
  - Preflight recovery (`qdrant`, env key presence, `openclaw mem0 stats`)
  - Restart commands for attached and detached execution
  - Live progress checkpointing and partial pass-rate computation
  - Required completion artifacts (`qa-50.json`, `score.json`, `failures.json`)
  - Crash-again procedure with `br` tracking updates
- Why: avoid losing progress after CLI/session interruptions and make recovery repeatable.

## Testing
- Verified roadmap doc diff renders as expected.
- Operational verification run executed for mem0 50Q and produced artifacts under `/tmp/openclaw-mem0-fastpath/latest-50q/` including:
  - `qa-50.json`
  - `score.json`
  - `failures.json`
- Latest completed score observed during validation: `35/50` (`pass_rate=0.70`).

## Known Limitations
- Current mem0 stats still show `Total memories: 0` despite 50Q harness execution, indicating setup/ingestion parity remains incomplete and needs follow-up.
- Unrelated local modifications (`.claude/settings.json`, `.beads/.br_history/`) were intentionally excluded from this PR.

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Documentation-only updates plus an append to the `.beads/issues.jsonl` tracking database; low risk aside from potential merge conflicts in the JSONL artifact.
> 
> **Overview**
> Adds an operator-focused **crash-resume runbook** to `roadmap/MEM0_MIGRATION_PLAN.md` for restarting interrupted mem0 50Q validation runs, including preflight checks, attached/detached restart comma
