# PR #112: agento: AO-native remediation loop + intentional CI probe

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-03-12
**Author:** jleechan2015
**Stats:** +0/-2 in 1 files

## Summary
- route PR remediation policy to AO-native commands (`ao review-check`, `ao send`, `ao status`)
- enforce AO execution context from `~/projects_reference/agent-orchestrator`
- remove monitor-agent config/workspace bundle from this branch state
- add a temporary intentional failing test probe to validate AO CI/comment auto-remediation

## Raw Body
## Summary
- route PR remediation policy to AO-native commands (`ao review-check`, `ao send`, `ao status`)
- enforce AO execution context from `~/projects_reference/agent-orchestrator`
- remove monitor-agent config/workspace bundle from this branch state
- add a temporary intentional failing test probe to validate AO CI/comment auto-remediation

## Why
We want to verify agento can fix real CI failures and PR comments using AO's own lifecycle/reaction logic, without implementing a parallel remediation engine in this repo.

## Intentional Test Condition
This PR includes `test_intentional_ci_probe_failure` in `tests/test_openclaw_configs.py` which is expected to fail CI until agento remediates it.

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Whitespace-only change in `_task_with_push_instruction`; no functional logic changes beyond formatting of the generated task instructions.
> 
> **Overview**
> Cleans up minor formatting in `src/orchestration/dispatch_task.py` around the `gh pr create` instruction generation (removes stray blank/whitespace lines) while keeping the `--repo <owner/repo>` auto-detection behavior intact.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 91c827f0a36585410defbb91b2e7dce81a252bef. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->
## Summary by CodeRabbit

* **Bug Fixes**
  * Removed automatic pull request creation step from task orchestration. Git commit and push operations continue to function as expected.
<!-- end of auto-generated comment: release notes by coderabbit.ai -->
