# PR #56: feat(orch-8w8): add EXECUTION_FAILED status for non-zero ai_orch exit

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-03-05
**Author:** jleechan2015
**Stats:** +75/-2 in 3 files

## Summary
- Add `TaskStatus.EXECUTION_FAILED = "execution_failed"` to distinguish subprocess crashes from logic failures
- `_dispatch_via_ai_orch` includes the status in metadata on non-zero exit
- `_dispatch_task` uses status from metadata when present, falls back to `FAILED` for exceptions

## Test Plan
- [x] `test_task_status_execution_failed_value` — enum value is correct
- [x] `test_execution_failed_set_on_nonzero_exit` — non-zero exit returns EXECUTION_FAILED in metadata
- [x] `test_dispatch_task_uses_execution_failed_status` — task updated to EXECUTION_FAILED on subprocess failure
- [x] `test_dispatch_task_uses_failed_status_on_exception` — exceptions still use FAILED
- [x] Full suite: 141 tests pass

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
-

## Raw Body
## Summary
- Add `TaskStatus.EXECUTION_FAILED = "execution_failed"` to distinguish subprocess crashes from logic failures
- `_dispatch_via_ai_orch` includes the status in metadata on non-zero exit
- `_dispatch_task` uses status from metadata when present, falls back to `FAILED` for exceptions

## Test plan
- [x] `test_task_status_execution_failed_value` — enum value is correct
- [x] `test_execution_failed_set_on_nonzero_exit` — non-zero exit returns EXECUTION_FAILED in metadata
- [x] `test_dispatch_task_uses_execution_failed_status` — task updated to EXECUTION_FAILED on subprocess failure
- [x] `test_dispatch_task_uses_failed_status_on_exception` — exceptions still use FAILED
- [x] Full suite: 141 tests pass

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Small, well-tested change limited to task status reporting; primary risk is downstream consumers not expecting the new `execution_failed` status.
> 
> **Overview**
> Adds a new `TaskStatus.EXECUTION_FAILED` to distinguish non-zero `ai_orch` subprocess exits from general task failures.
> 
> Updates `TaskPoller` so `_dispatch_via_ai_orch` returns this status in metadata on non-zero exit and `_dispatch_task` uses a provided metadata status when marking the task as failed (while exceptions still map to `FAILED`). New tests cover the enum value, subprocess failure propagation, and the exception fallback behavior.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit efc245e5b3a83f6c6f58a8817b00a90b98b1a920. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->

## Summary by CodeRabbit

* **New Features**
  * Introduced a new execution failure status to enable better differentiation between orchestration execution failures and other types of task failures in
