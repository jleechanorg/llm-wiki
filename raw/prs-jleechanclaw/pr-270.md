# PR #270: fix(bug-hunt): correct date filter for merged PRs

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-03-19
**Author:** jleechan2015
**Stats:** +677/-679 in 9 files

## Summary
The daily bug hunt was reporting 0 PRs because  doesn't work with 269	feat: Phase 2 — MCP mail + merge_gate reviewer APPROVED (orch-tlkk.2)	feat/reviewer-loop-phase2	OPEN	2026-03-18T15:49:33Z.

**Fix:** Fetch all merged PRs and filter with jq using ISO date comparison:



There are 10+ merged PRs in jleechanclaw alone from the last 2 days that should have been scanned.

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Touches operational automation (`scripts/bug-hunt-daily.sh` + `launc

## Raw Body
## Summary

The daily bug hunt was reporting 0 PRs because  doesn't work with 269	feat: Phase 2 — MCP mail + merge_gate reviewer APPROVED (orch-tlkk.2)	feat/reviewer-loop-phase2	OPEN	2026-03-18T15:49:33Z.

**Fix:** Fetch all merged PRs and filter with jq using ISO date comparison:



There are 10+ merged PRs in jleechanclaw alone from the last 2 days that should have been scanned.

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Touches operational automation (`scripts/bug-hunt-daily.sh` + `launchd` plist) and introduces a new merge gate that parses GitHub comments/reviews; behavior changes could affect scheduled runs or merge-readiness decisions, though the evidence gate is fail-open on API errors.
> 
> **Overview**
> Fixes the daily bug-hunt job to reliably discover merged PRs by switching from the `gh api pulls` query to `gh pr list` + `jq` date filtering, then running parallel AO-spawned agents and aggregating their JSON outputs into a single report and Slack notification (with a GitHub-issue fallback if Slack fails).
> 
> Adds a new **evidence review merge gate** (`evidence_review_gate.py`) that can request `/er` reviews and detects PASS/WARN/FAIL verdicts in PR reviews/comments (fail-open on GitHub/`gh` errors), and updates docs/Claude command specs to reflect the streamlined `/er` flow and the new `/claw` Slack-dispatch workaround.
> 
> Also updates the CodeRabbit bot login in tests, tweaks orchestration design docs to include the evidence-review gate, adjusts the bug-hunt `launchd` plist working directory/script path, and ignores `bug_reports/` plus adds a local beads version marker.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit c9cff53f32eecccfeae114099c4466d94a732a35. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->
## Summary by CodeRabbit


