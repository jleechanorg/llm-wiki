# PR #515: chore: sync beads, mem0 recall, monitor PATH, AO workspace policy

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-04-05
**Author:** jleechan2015
**Stats:** +84/-45 in 10 files

## Summary
Syncs harness state and small operational tweaks.

## Raw Body
## Summary
Syncs harness state and small operational tweaks.

## Changes
- **Beads** (`.beads/issues.jsonl`): issue closes/opens and new orch items.
- **mem0 recall** (`.claude/hooks/mem0_recall.py`): `TOP_K` 6→4, `SCORE_THRESHOLD` 0.35→0.45 (fewer, higher-confidence chunks).
- **monitor-agent** (`monitor-agent.sh`): prepend `$HOME/.nvm/versions/node/v22.22.0/bin` to `PATH` so OpenClaw uses Node 22 before Homebrew.
- **AO** (`agent-orchestrator.yaml`): remove `evolveLoop` block under the claw project.
- **Docs** (`docs/context/DOC_GAPS.md`): regenerated timestamp.
- **MCP mail log** (`memory/mcp-mail-ack-log.md`): append recent ack lines.
- **Workspace** (`workspace/MEMORY.md`, `workspace/SOUL.md`): refresh project status and learned patterns.

## Excluded (not in this PR)
- `docs/context/SYSTEM_SNAPSHOT.md` — large machine-local `openclaw status` capture; not suitable to merge.
- `workspace/tasks/current.md` — personal calendar/tasks; left unstaged.

## Reviewer notes
Confirm **evolveLoop** removal and mem0 threshold changes match intent.

Made with [Cursor](https://cursor.com)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Changes affect operational guardrails (`doctor.sh`, `monitor-agent.sh`) and orchestration config, which could cause new health-check failures or alter runtime behavior if assumptions about config/Node versions are wrong.
> 
> **Overview**
> Tightens mem0 prompt-time recall by returning fewer, higher-confidence matches (`TOP_K` 6→4, score threshold 0.35→0.45).
> 
> Hardens operational checks by forcing Node 22 precedence in `monitor-agent.sh` and adding a `doctor.sh` failure gate requiring `plugins.slots.memory` to be set to `openclaw-mem0` (to prevent silent fallback to builtin memory). Also removes the `evolveLoop` block from `agent-orchestrator.yaml`.
> 
> Updates internal tracking/docs/logs (beads issue status sync, regenerated doc-gap timestamp, appended mail ack log, refreshed workspace status/pattern notes) and documents `plug
