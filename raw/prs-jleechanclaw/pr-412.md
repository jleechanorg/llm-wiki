# PR #412: [agento] [P2] feat(bd-z14m): Create a launchd KeepAlive plist for the OpenClaw gateway process

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-03-29
**Author:** jleechan2015
**Stats:** +149/-102 in 11 files

## Summary
The OpenClaw gateway previously ran without KeepAlive, meaning crashes or unexpected terminations left the gateway offline until manually restarted. This PR adds a proper macOS LaunchAgent plist with KeepAlive to ensure the gateway self-heals.

## Background
The OpenClaw gateway previously ran without KeepAlive, meaning crashes or unexpected terminations left the gateway offline until manually restarted. This PR adds a proper macOS LaunchAgent plist with KeepAlive to ensure the gateway self-heals.

## Raw Body
## Background
The OpenClaw gateway previously ran without KeepAlive, meaning crashes or unexpected terminations left the gateway offline until manually restarted. This PR adds a proper macOS LaunchAgent plist with KeepAlive to ensure the gateway self-heals.

## Goals
- Gateway process restarts automatically on crash (KeepAlive)
- Graceful migration from old label (ai.openclaw.gateway → com.openclaw.gateway)
- No port conflicts during migration

## Testing
- CI: Skeptic Gate ✓, Cursor Bugbot neutral ✓ (all prior findings on old commits, none on squashed HEAD)
- `python -m pytest tests/test_openclaw_configs.py -q` — 29 passed, 76 skipped
- Manual: bootstrap the plist, kill the gateway process, verify auto-restart within ThrottleInterval (30s)

## Changes (squashed single commit)
- **launchd/com.openclaw.gateway.plist** (new): KeepAlive LaunchAgent with ThrottleInterval=30, env vars for PATH/HOME/OPENCLAW_*, stdout/stderr → ~/.openclaw/logs/
- **scripts/install-openclaw-launchd.sh**: replaces old ai.openclaw.gateway plist with com.openclaw.gateway, bootouts old, creates log dir
- **health-check.sh / startup-check.sh / workspace-report.sh / doctor.sh**: updated to new label com.openclaw.gateway
- **scripts/install-ao-manager.sh**: updated log paths from /tmp/ to ~/.openclaw/logs/
- Fixes: XML malformed tag, bash operator precedence, HOMEBREW_BASH Intel Mac fallback, openclaw binary path resolution

Closes #bd-z14m

🤖 Generated with [Claude Code](https://claude.com/claude-code)
