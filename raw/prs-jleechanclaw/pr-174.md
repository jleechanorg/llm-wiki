# PR #174: feat(mem0): switch embedder to local Ollama (nomic-embed-text, 768 dims)

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-03-16
**Author:** jleechan2015
**Stats:** +692/-39 in 4 files

## Summary
- Replace OpenAI embedding API calls with local Ollama `nomic-embed-text`
- 5 TDD tests (red → green): provider dispatch, regression, live dims, raw client, config round-trip
- Qdrant collection recreated at 768 dims

## Raw Body
## Summary
- Replace OpenAI embedding API calls with local Ollama `nomic-embed-text`
- 5 TDD tests (red → green): provider dispatch, regression, live dims, raw client, config round-trip
- Qdrant collection recreated at 768 dims

## Changes
- `scripts/mem0_shared_client.py`: extract `_get_embedding(query, embedder_cfg)` — routes to Ollama or OpenAI based on config provider
- `src/tests/test_ollama_embedder.py`: new TDD suite, 5/5 passing
- `~/.openclaw/openclaw.json`: embedder switched to ollama, dims 1536→768

## Why
Ingest was slow due to OpenAI embedding network round-trips per session (408 timeouts + latency). Local Ollama embeddings are instant — only Groq LLM calls remain as network dependency.

## Testing
All 5 tests pass. Live Ollama integration test confirms 768-dim output.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Changes the embedding generation path used by raw Qdrant search, which can affect retrieval quality and runtime behavior depending on local Ollama availability/config. Adds real/integration tests that may be flaky or slow in CI if not properly excluded.
> 
> **Overview**
> Switches mem0’s query embedding generation to be **provider-driven** via a new `_get_embedding()` helper, adding support for local Ollama embeddings (`/api/embed`, default `nomic-embed-text`) while keeping OpenAI as a fallback.
> 
> Refactors `_search_via_raw_client()` to use the configured embedder (no longer hardcoding OpenAI), introduces typed embedder config (`EmbedderProvider`, `EmbedderConfig`), and adds test coverage including Ollama/OpenAI dispatch tests, a config round-trip fixture, and a **real e2e local ingest** test that writes to a temporary Qdrant collection and verifies search.
> 
> Updates `roadmap/MEM0_INGEST_RESUME.md` with an updated fully-local ingest plan, prerequisites, and new resume commands reflecting the Ollama-based stack and 768-dimension embeddings.
> 
> <sup>Written by
