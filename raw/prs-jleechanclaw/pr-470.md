# PR #470: [P2] feat(agent-orchestrator): canonical AO config symlink (orch-2u9d)

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-04-02
**Author:** jleechan2015
**Stats:** +62/-24 in 3 files

## Summary
Implements beads **orch-2u9d**: `~/agent-orchestrator.yaml` must symlink to the canonical harness file `~/.openclaw/agent-orchestrator.yaml`, not to an arbitrary worktree `REPO_ROOT`, so the AO CLI and pollers always read the live config.

## Raw Body
## Summary
Implements beads **orch-2u9d**: `~/agent-orchestrator.yaml` must symlink to the canonical harness file `~/.openclaw/agent-orchestrator.yaml`, not to an arbitrary worktree `REPO_ROOT`, so the AO CLI and pollers always read the live config.

## Changes
- **bootstrap.sh**: `ensure_agent_orchestrator_symlink()` prefers `$HOME/.openclaw/agent-orchestrator.yaml` when present; falls back to repo copy only if the canonical file is missing.
- **bootstrap.sh `--symlink-only`**: early exit for lightweight refresh.
- **install.sh**: runs `bootstrap.sh --symlink-only` on install (skipped on `--uninstall`).

## Testing
- `bash -n` on both scripts; `bash scripts/bootstrap.sh --symlink-only` (verified symlink → `~/.openclaw/agent-orchestrator.yaml`).
- `PYTHONPATH=src python -m pytest tests/ -q`: **231 passed**, 77 skipped (worktree has no `tests/test_genesis_*.py`; full `tests/` run instead).

Refs: orch-2u9d

Made with [Cursor](https://cursor.com)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Changes bootstrap/install behavior that rewrites `~/agent-orchestrator.yaml`, including backing up/replacing an existing file; mistakes here could break local agent-orchestrator setup on developer machines, but it’s limited to installer scripts.
> 
> **Overview**
> Ensures the Agent Orchestrator config is read from a single canonical location by making `~/agent-orchestrator.yaml` symlink to `~/.openclaw/agent-orchestrator.yaml` (with a fallback to the repo copy if the canonical file is missing).
> 
> Updates `scripts/bootstrap.sh` to manage this symlink more robustly (validate/update stale links, back up regular files, and error on unexpected file types) and adds a `--symlink-only` mode. `scripts/install.sh` now runs this lightweight symlink sync step automatically on install (skipped during `--uninstall`).
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 2ebaf293f81e8dde95bf203c1e993a6f15eb4de7. This will update automatically 
