# PR #298: fix: wire merge_gate into poller, fix ao spawn arg, add auto-merge

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-03-20
**Author:** jleechan2015
**Stats:** +79/-66 in 3 files

## Summary
The PR poller (`ao-pr-poller.sh`) has three reliability issues preventing full autonomy:
1. The green check uses loose bash conditions that don't match `merge_gate.py`'s strict 6-condition check (e.g., treating any CR review as "approved")
2. `ao spawn` receives the project name as a positional arg, which ao interprets as "Issue identifier" — causing "Multiple projects configured" errors
3. When a PR *is* green, the poller spawns another agent instead of merging

## Background
The PR poller (`ao-pr-poller.sh`) has three reliability issues preventing full autonomy:
1. The green check uses loose bash conditions that don't match `merge_gate.py`'s strict 6-condition check (e.g., treating any CR review as "approved")
2. `ao spawn` receives the project name as a positional arg, which ao interprets as "Issue identifier" — causing "Multiple projects configured" errors
3. When a PR *is* green, the poller spawns another agent instead of merging

## Raw Body
## Background

The PR poller (`ao-pr-poller.sh`) has three reliability issues preventing full autonomy:
1. The green check uses loose bash conditions that don't match `merge_gate.py`'s strict 6-condition check (e.g., treating any CR review as "approved")
2. `ao spawn` receives the project name as a positional arg, which ao interprets as "Issue identifier" — causing "Multiple projects configured" errors
3. When a PR *is* green, the poller spawns another agent instead of merging

## Goals

- Wire the poller's green check to `merge_gate.py --json` for authoritative 6-condition verification
- Fix `ao spawn` arg handling in both `ao-pr-poller.sh` and `github-intake.sh`
- Add auto-merge (`gh pr merge --squash --auto`) when all conditions pass

## Tenets

- Use existing merge_gate.py rather than duplicating condition logic in bash
- Minimal changes — only touch the green check + spawn + merge paths
- Fail-closed: if merge_gate.py fails, treat as not-green (spawn agent)

## High-level description of changes

Replaced the poller's 6-line bash green check (which missed CR APPROVED vs COMMENTED, SHA staleness, blocking comment severity, and evidence pipeline stages) with a single call to `python3 -m orchestration.merge_gate --json`. When `can_merge=true`, the poller auto-merges via `gh pr merge --squash --auto`. When not green, it logs the blocked reasons and spawns an agent as before.

Removed the project name positional arg from `ao spawn` calls — ao auto-detects the project from cwd (which we already set). This fixes the "Multiple projects configured" error that caused 100% spawn failures.

## Testing

- `bash -n` syntax check passes for both scripts
- `pytest tests/ src/tests/` — 1136 passed, 17 pre-existing failures (unrelated)
- Manual verification: poller log showed "Multiple projects configured" on every spawn; after removing the positional arg, spawns succeed

## Low-level details

- **scripts/ao-pr-poller.sh**: Replaced lines 371-398 (REVIEWS fetch, loose bash green 
