# PR #216: feat: backfill-memory-md includes Claude Code auto-memory files

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-03-16
**Author:** jleechan2015
**Stats:** +10/-0 in 1 files

## Summary
- Extends `backfill-memory-md-only.mjs` to also scan `~/.claude/projects/*/memory/*.md`
- Product info files written via Claude Code auto-memory (e.g. `project_worldarchitect_ai.md`, `project_worldai_claw.md`, `project_jleechanclaw_goals.md`) now get ingested into mem0/Qdrant when the backfill script runs

## Test Plan
- [ ] Run `node extensions/openclaw-mem0/backfill-memory-md-only.mjs` and verify the 3 new product files appear in `totalFiles` count and are chunked/added without errors

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Low risk: expands the backfill script’s input file set and does not change chunking or ingestion logic. Main risk is increased ingestion volume/duplicates or unintentionally indexing additional local files un

## Raw Body
## Summary

- Extends `backfill-memory-md-only.mjs` to also scan `~/.claude/projects/*/memory/*.md`
- Product info files written via Claude Code auto-memory (e.g. `project_worldarchitect_ai.md`, `project_worldai_claw.md`, `project_jleechanclaw_goals.md`) now get ingested into mem0/Qdrant when the backfill script runs

## Changes

**Before:** script only scanned `~/.openclaw/memory/*.md` + `~/.openclaw/workspace/MEMORY.md`

**After:** also iterates `~/.claude/projects/*/memory/` directories and adds any `.md` files found

**Why:** Claude Code auto-memory is a natural place to seed product/project context, and the backfill pipeline should pull from it without manual copying.

## Test plan

- [ ] Run `node extensions/openclaw-mem0/backfill-memory-md-only.mjs` and verify the 3 new product files appear in `totalFiles` count and are chunked/added without errors

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Low risk: expands the backfill script’s input file set and does not change chunking or ingestion logic. Main risk is increased ingestion volume/duplicates or unintentionally indexing additional local files under `~/.claude/projects`.
> 
> **Overview**
> Extends `backfill-memory-md-only.mjs` to also discover and ingest Claude Code auto-memory markdown files from `~/.claude/projects/*/memory/**/*.md` in addition to the existing OpenClaw memory locations.
> 
> The new files are appended to the same `files` list and flow through the existing chunking and `memory.add` ingestion path, affecting `totalFiles`/`totalChunks` counts and what gets indexed during a backfill run.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 49fd52b8f627ff100d3c63c9398680b2a394991f. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit
