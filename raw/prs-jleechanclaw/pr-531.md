# PR #531: feat(harness): user/repo layers, openclaw-harness skill, doctor lane backlog

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-04-09
**Author:** jleechan2015
**Stats:** +535/-7 in 9 files

## Summary
- **User vs repository harness:** Tracked copies under `docs/harness/` (`user-command-harness.md`, scope snippet for `harness-engineering`) plus `scripts/sync-harness-user-scope.sh` to sync the global `/harness` command from the repo after `git pull`.
- **Repo-local:** New `.claude/skills/openclaw-harness/SKILL.md` and expanded `.claude/commands/harness.md` overlay (OpenClaw gateway, canary, deploy, collision rule).

**Note:** `~/.claude/commands/harness.md` and `~/.claude/skills/harness-enginee

## Raw Body
## Summary

- **User vs repository harness:** Tracked copies under `docs/harness/` (`user-command-harness.md`, scope snippet for `harness-engineering`) plus `scripts/sync-harness-user-scope.sh` to sync the global `/harness` command from the repo after `git pull`.
- **Repo-local:** New `.claude/skills/openclaw-harness/SKILL.md` and expanded `.claude/commands/harness.md` overlay (OpenClaw gateway, canary, deploy, collision rule).

**Note:** `~/.claude/commands/harness.md` and `~/.claude/skills/harness-engineering/SKILL.md` were updated on disk in this workspace to match; those paths are outside the repo — run `bash scripts/sync-harness-user-scope.sh` on other machines or merge the snippet manually.

## Doctor / monitor parity

- `scripts/doctor.sh`: new section **Gateway lane backlog (err.log)** — warns if `lane wait exceeded` appears in the last ~400 lines of `${LIVE_OPENCLAW}/logs/gateway.err.log`.
- `CLAUDE.md`: doctor/monitor parity bullet now explicitly includes **lane backlog**.

## Docs

- `docs/harness/README.md` + `docs/harness/index.html` (dark template per repo convention).

Made with [Cursor](https://cursor.com)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Medium risk because it changes operational tooling (`doctor.sh` and PR-to-Slack reporting), which can affect on-call diagnostics and automated notifications if the new log parsing/API fallbacks behave unexpectedly.
> 
> **Overview**
> Adds a clear *user-scope vs repo-local* harness layering model by introducing tracked user-scope copies under `docs/harness/` (plus a generated `index.html`), and a new repo-local `openclaw-harness` skill to capture OpenClaw-specific gateway/deploy failure modes and verification steps.
> 
> Extends `scripts/doctor.sh` with a gateway lane-backlog check that scans `logs/gateway.err.log` for `lane wait exceeded` to catch “Slack silent while `/health` is 200” scenarios, and adds `scripts/sync-harness-user-scope.sh` to sync the tracked global `/harness` command in
