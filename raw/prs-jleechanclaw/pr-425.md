# PR #425: [agento] [P2] feat(monitoring): add GH Actions cost monitor

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-03-28
**Author:** jleechan2015
**Stats:** +309/-0 in 2 files

## Summary
- Adds `scripts/gh-actions-cost-monitor.sh` — queries today's workflow runs across three private repos, sums duration in minutes, and estimates daily cost at $0.002/min (Linux self-hosted runner rate). Fires Slack alert + GitHub issue when daily total exceeds $5.
- Adds `launchd/com.jleechanorg.gh-actions-cost-monitor.plist` — runs the script daily at 9pm.

## Raw Body
## Summary
- Adds `scripts/gh-actions-cost-monitor.sh` — queries today's workflow runs across three private repos, sums duration in minutes, and estimates daily cost at $0.002/min (Linux self-hosted runner rate). Fires Slack alert + GitHub issue when daily total exceeds $5.
- Adds `launchd/com.jleechanorg.gh-actions-cost-monitor.plist` — runs the script daily at 9pm.

## Testing
- `bash -n` syntax check: passed
- `gh api` pagination + jq filter tested against real `jleechanorg/jleechanclaw` — confirmed paginate returns per-page arrays correctly
- Script uses `awk` for float math (no `bc` dependency); `trap` ensures temp cleanup on exit

## Known Limitations
- `gh --paginate` returns separate JSON objects per page; jq filter `| @json` on each object (one per line) is the established pattern used by other scripts in this repo (e.g. `bug-hunt-daily.sh`)
- `gh actions cost` CLI does not exist on current gh version; manual API calculation is used

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Adds an automated, scheduled script that calls external APIs (GitHub via `gh`, Slack via `curl`) and creates GitHub issues, so misconfiguration or logic errors could spam alerts/issues or fail silently in cron-like execution.
> 
> **Overview**
> Adds a new daily GitHub Actions cost monitoring job: `gh-actions-cost-monitor.sh` queries today’s workflow runs across three org repos, estimates spend from run duration, and triggers Slack notification plus a GitHub issue when the total exceeds a fixed threshold.
> 
> Introduces a `launchd` plist to run the monitor at a scheduled time with its own PATH/working directory and log redirection.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 9438935870175ad8f941f89700470b42051c8796. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
