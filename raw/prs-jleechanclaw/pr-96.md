# PR #96: orchestration: make reconciliation remote-agnostic (no origin default)

**Repo:** jleechanorg/jleechanclaw
**Merged:** 2026-03-11
**Author:** jleechan2015
**Stats:** +165/-74 in 6 files

## Summary
- remove origin-only assumptions from reconciliation remote verification
- treat branch as finished when local HEAD is reviewable on any configured remote/upstream
- update Slack stranded-task wording to avoid origin-specific messaging

## Raw Body
## Summary
- remove origin-only assumptions from reconciliation remote verification
- treat branch as finished when local HEAD is reviewable on any configured remote/upstream
- update Slack stranded-task wording to avoid origin-specific messaging

## Changes
- Updated [src/orchestration/reconciliation.py](src/orchestration/reconciliation.py)
  - Replaced `_remote_branch_exists` origin-only flow with remote-agnostic verification over configured remotes + upstream/current-branch candidate refs.
  - Preserved transient failure handling (`None`) to avoid false terminal transitions during network instability.
  - Updated completion/stalled summaries to say "configured remote" instead of "origin".
- Updated [src/orchestration/openclaw_notifier.py](src/orchestration/openclaw_notifier.py)
  - Updated push_or_salvage notification text to remove origin-specific wording.
- Updated tests:
  - [tests/test_reconciliation.py](tests/test_reconciliation.py)
  - [tests/test_openclaw_notifier.py](tests/test_openclaw_notifier.py)

## Testing
- `pytest -q tests/test_reconciliation.py tests/test_openclaw_notifier.py`
- Result: `36 passed`
- Manual verification against real case:
  - `_remote_branch_exists('ai-orch-4121', '/tmp/ai-orch-worktrees/ai-orch-4121')` now returns `True` when work was pushed to non-origin remote with an upstream branch.

## Known Limitations
- This change fixes verification semantics for future reconciliations; historic incorrect `needs_human` entries still require explicit status correction if already written.
- Dispatch path still contains some origin-centric assumptions for worktree creation; this PR focuses on reconciliation/notifier correctness only.

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Changes reconciliation’s remote-push verification logic, which can affect whether tasks transition to `finished` vs `needs_human` and when notifications are sent. Main risk is misclassification under unusual git-remote setups or transient network errors
