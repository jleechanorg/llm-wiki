# PR #5782: Rename /ralph to /ralph_custom, free /ralph for plugin

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-02-26
**Author:** jleechan2015
**Stats:** +6/-6 in 1 files

## Summary
- Rename `.claude/commands/ralph.md` → `ralph_custom.md` so the project-level command no longer shadows the `ralph-marketplace` plugin
- `/ralph` now resolves to the plugin (`ralph-skills:ralph`) — PRD converter for Ralph autonomous execution
- `/ralph_custom` retains the old orchestrator workflow (Codex CLI, goal files, tmux sessions) using repo at `/Users/$USER/projects_other/ralph-orchestrator`

## Test Plan
- [ ] Verify `/ralph` resolves to the plugin skill (`ralph-skills:ralph`)
- [ ] Verify `/ralph_custom` invokes the orchestrator workflow
- [ ] Verify `ralph-skills:prd` still works for PRD generation

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Low risk documentation/config change limited to Claude command metadata and usage strings; main risk is user confusion if other docs or workflows still reference `/ralph`.
> 
> **

## Raw Body
## Summary
- Rename `.claude/commands/ralph.md` → `ralph_custom.md` so the project-level command no longer shadows the `ralph-marketplace` plugin
- `/ralph` now resolves to the plugin (`ralph-skills:ralph`) — PRD converter for Ralph autonomous execution
- `/ralph_custom` retains the old orchestrator workflow (Codex CLI, goal files, tmux sessions) using repo at `/Users/$USER/projects_other/ralph-orchestrator`

## Production Code Changes
- **Before**: `/ralph` invoked the project-level orchestrator command, hiding the plugin
- **Now**: `/ralph` → plugin (`ralph-skills:ralph`), `/ralph_custom` → orchestrator workflow
- **Why**: Let the ralph-marketplace plugin skills be the primary `/ralph` command

## Test plan
- [ ] Verify `/ralph` resolves to the plugin skill (`ralph-skills:ralph`)
- [ ] Verify `/ralph_custom` invokes the orchestrator workflow
- [ ] Verify `ralph-skills:prd` still works for PRD generation

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Low risk documentation/config change limited to Claude command metadata and usage strings; main risk is user confusion if other docs or workflows still reference `/ralph`.
> 
> **Overview**
> Renames the project-level Claude command documentation from `/ralph` to `/ralph_custom`, updating frontmatter (`category` → `type`) and all usage/examples/error text to match.
> 
> This effectively frees the `/ralph` name for another command source (e.g., a plugin) while keeping the existing orchestrator workflow available under `/ralph_custom`.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 54ce8cacb37f97584487bd4ed98661e6c24f5e85. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
