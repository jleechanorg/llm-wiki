# PR #5203: feat: Add --version flag to jleechanorg-pr-monitor CLI

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-02-10
**Author:** jleechan2015
**Stats:** +6/-0 in 1 files

## Summary
Add standard `--version` flag support to the `jleechanorg-pr-monitor` CLI tool.

**Key themes:**
- CLI standardization - add missing --version flag

## Raw Body
## Summary
Add standard `--version` flag support to the `jleechanorg-pr-monitor` CLI tool.

**Key themes:**
- CLI standardization - add missing --version flag

## Production Code Changes

### 1. `automation/jleechanorg_pr_automation/jleechanorg_pr_monitor.py` (+6/-0 lines)
**Purpose:** Main CLI entry point for PR monitoring automation

- **Added `__version__` import** - No version import → Import from package root → Access dynamic version from pyproject.toml
- **Added `--version` argument** - No version flag support → Added argparse version action with `%(prog)s {__version__}` format → Standard CLI convention for displaying version info

## Test Changes
- No test changes required (argparse built-in functionality)
- Manual verification:
  ```bash
  $ jleechanorg-pr-monitor --version
  jleechanorg-pr-monitor 0.2.128
  ```

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Small CLI-only change that doesn’t touch automation logic or data handling; primary risk is minor import/packaging issues if `__version__` isn’t available at runtime.
> 
> **Overview**
> Adds a standard `--version` flag to the `jleechanorg-pr-monitor` CLI, wiring it to the package’s `__version__` so the tool can print its installed/source-tree version via argparse’s built-in `version` action.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit ed14bb1410085fc0135142467a1448e74dc2e7ec. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->

## Summary by CodeRabbit

* **New Features**
  * Added --version command-line option to display the tool version information.

<!-- end of auto-generated comment: release notes by coderabbit.ai -->
