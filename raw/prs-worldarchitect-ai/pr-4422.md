# PR #4422: fix(smoke): Gate dev deploy on smoke tests + test both Cerebras & Gemini 3

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-02-01
**Author:** jleechan2015
**Stats:** +256/-46 in 6 files

## Summary
- Gate dev auto-deploy on MCP smoke test success
- Run smoke tests against BOTH Cerebras and Gemini 3 to catch provider-specific regressions
- Add provider-aware dice strategy (Gemini=code_execution, Cerebras=native_two_phase)
- Add fallback dice execution when code_execution doesn't produce dice rolls
- Update default Gemini model to `gemini-3-flash-preview`
- Improve smoke test debugging with retry logic and better dice validation

**Key themes:**
- Provider-specific dice strategies
- Fallback

## Raw Body
## Summary
- Gate dev auto-deploy on MCP smoke test success
- Run smoke tests against BOTH Cerebras and Gemini 3 to catch provider-specific regressions
- Add provider-aware dice strategy (Gemini=code_execution, Cerebras=native_two_phase)
- Add fallback dice execution when code_execution doesn't produce dice rolls
- Update default Gemini model to `gemini-3-flash-preview`
- Improve smoke test debugging with retry logic and better dice validation

**Key themes:**
- Provider-specific dice strategies
- Fallback mechanism for reliable dice execution
- CI/CD gating with smoke tests
- Improved debugging and test reliability

## Production Code Changes

### 1. `.github/workflows/auto-deploy-dev.yml` (+91 lines)
**Purpose:** Add smoke test job that gates dev deployment

- **Added `smoke-tests` job** - No job existed → Added job that runs after `deploy` completes → Gates deployment success on smoke test pass
- **Added `skip_smoke_tests` input** - No skip option → Added workflow_dispatch input with boolean → Allows emergency manual deploys
- **Boolean string coercion fix** - GitHub Actions passes booleans as strings → Compare against `'true'` string not boolean → Correct conditional evaluation

### 2. `mvp_site/dice_strategy.py` (+7/-5 lines)
**Purpose:** Provider-aware dice rolling strategy selection

- **Provider-based strategy selection** - Model-only check → Added `provider` parameter → Gemini uses code_execution, Cerebras/OpenRouter use native_two_phase
- **Explicit provider check** - `if model_name in MODELS_WITH_CODE_EXECUTION` → `if provider.lower() == "gemini" and model_name in ...` → Cerebras models won't incorrectly use code_execution strategy

### 3. `mvp_site/llm_providers/gemini_provider.py` (+41 lines)
**Purpose:** Fallback dice execution when code_execution fails

- **Track dropped dice tool_requests** - Dice requests were silently dropped → Now tracked in `dropped_dice_requests` list → Enables fallback execution
- **Added `roll_saving_throw` to tracking** - Onl
