# PR #5796: feat(ui): dragon welcome screen with twin_dragon.webp background

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-03-01
**Author:** jleechan2015
**Stats:** +1114/-207 in 10 files

## Summary
- Replace flat auth screen with immersive dragon welcome screen sourced from worldai_claw repo (commit b9b78ed)
- Full-viewport background using `twin_dragon.webp`, dark overlay + vignette, animated star sparkles, floating dragon emoji + gradient WorldAI title, styled Google sign-in button
- Update `docs/theme-mocks/styleguide_current.html` with `Welcome: GCP` section for design reference

## Raw Body
## Summary
- Replace flat auth screen with immersive dragon welcome screen sourced from worldai_claw repo (commit b9b78ed)
- Full-viewport background using `twin_dragon.webp`, dark overlay + vignette, animated star sparkles, floating dragon emoji + gradient WorldAI title, styled Google sign-in button
- Update `docs/theme-mocks/styleguide_current.html` with `Welcome: GCP` section for design reference

## Production Code Changes
- **`auth.js`**: `getLoggedOutAuthMarkup()` now returns dragon welcome card markup (`.welcome-screen`, `.welcome-card`, `.btn-google`, animated stars)
- **`style.css`**: Added dragon welcome CSS — `.welcome-bg` uses `/frontend_v1/assets/twin_dragon.webp`, keyframe animations for stars + floating logo, `.btn-google` Google sign-in button styling
- **`assets/twin_dragon.webp`**: New binary asset (144KB) copied from worldai_claw repo

## Test Changes
None — welcome screen is visual/markup only; no test infrastructure changes.

## Known Limitations
- Live server must be restarted (cache-busted temp dirs) for asset changes to propagate; `Cmd+Shift+R` hard-refresh required in browser

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Medium risk because it changes server-side frontend file fallback/serving behavior and alters theme default/migration logic, which can impact what assets/pages users receive and their persisted preferences.
> 
> **Overview**
> Replaces the logged-out auth view with a full-viewport **dragon welcome screen** (background image + overlays/vignette, star animation, redesigned Google SSO button) and updates logged-out layout CSS to avoid the previous max-width constraints.
> 
> Introduces a **rollout-controlled fantasy theme default**: `theme-bootstrap.js` and `theme-manager.js` now gate fantasy via query params/localStorage flag, migrate legacy `light` to `default`, and ensure only `default`/`fantasy` are selectable; the theme dropdown ordering/icon is 
