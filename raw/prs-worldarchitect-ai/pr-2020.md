# PR #2020: feat: implement auto-deploy to dev on push to main

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2025-11-14
**Author:** jleechan2015
**Stats:** +153/-86 in 2 files

## Summary
Implements automatic deployment to dev environment on push to main, adapted from the ai_universe repo autodeploy pattern.

## Test Plan
- [x] Deployed to dev manually: https://mvp-site-app-dev-i6xf2p72ka-uc.a.run.app
- [x] Health check passed (200 OK)
- [x] YAML syntax validated
- [ ] Will auto-deploy on next merge to main

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Replaces inline dev deployment with a reusable workflow and adds manual trigger, deploying to Cloud Run (worldarchitecture-ai) with health checks and deployment summaries.
> 
> - **CI/CD (GitHub Actions)**

## Raw Body
## Summary
Implements automatic deployment to dev environment on push to main, adapted from the ai_universe repo autodeploy pattern.

## Changes
- **auto-deploy-dev.yml**: Lightweight trigger workflow (reduced from 98 to 31 lines)
  - Triggers on push to `main` branch
  - Supports manual `workflow_dispatch` trigger for on-demand deploys
  - Delegates to reusable workflow
- **deploy-dev.yml**: New reusable deployment workflow (135 lines)
  - Supports both `workflow_call` and `workflow_dispatch`
  - Targets `mvp-site-app-dev` on `worldarchitecture-ai` GCP project
  - Includes health checks with retries
  - Rich deployment summaries with quick links

## Benefits
✅ Automatic deployment when code is merged to main
✅ Manual trigger support via GitHub UI
✅ Reusable workflow pattern (single source of truth)
✅ Health checks validate deployment
✅ Clear failure notifications

## Configuration
- **GCP Project**: `worldarchitecture-ai`
- **Region**: `us-central1`
- **Service**: `mvp-site-app-dev`
- **Deployment script**: `./deploy.sh mvp_site`

## Test plan
- [x] Deployed to dev manually: https://mvp-site-app-dev-i6xf2p72ka-uc.a.run.app
- [x] Health check passed (200 OK)
- [x] YAML syntax validated
- [ ] Will auto-deploy on next merge to main

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Replaces inline dev deployment with a reusable workflow and adds manual trigger, deploying to Cloud Run (worldarchitecture-ai) with health checks and deployment summaries.
> 
> - **CI/CD (GitHub Actions)**:
>   - **auto-deploy trigger (`.github/workflows/auto-deploy-dev.yml`)**:
>     - Simplified to delegate to reusable `deploy-dev.yml`.
>     - Triggers on push to `main` and supports `workflow_dispatch` with optional `reason`.
>     - Passes `reason` and `ref_name`; inherits secrets and sets minimal permissions.
>   - **new reusable workflow (`.github/workflows/deploy-dev.yml`)**:
>     - Supports `workflow_call` and `workflow_dispatc
