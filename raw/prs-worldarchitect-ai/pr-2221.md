# PR #2221: Refactor MCP HTTP handler and centralize settings defaults

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2025-12-01
**Author:** jleechan2015
**Stats:** +149/-160 in 6 files

## Summary
(none)

## Raw Body
- **Add beads chores for MCP handler refactor and settings defaults**
- **Unify MCP HTTP handler and centralize settings defaults**

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Consolidates MCP HTTP handlers via a factory and routes settings defaults through world_logic; updates tests to match unified behavior.
> 
> - **MCP server (mvp_site/mcp_api.py)**:
>   - **Unify HTTP handlers**: Replace duplicate `DualMCPHandler` and `MCPHandler` with factory `create_mcp_handler(...)` producing a single configurable `MCPHandler` for dual and HTTP-only modes.
>   - **run_server wiring**: Use factory in both dual and HTTP-only branches; keep `/health`, `/mcp`, and `/rpc` behavior consistent; suppress default HTTP logs.
> - **API Gateway (mvp_site/main.py)**:
>   - **Centralize settings defaults**: `GET /api/settings` now delegates to `world_logic.get_user_settings_unified` and returns settings directly (no manual `setdefault` defaults).
> - **Tests (mvp_site/tests/test_settings_api.py)**:
>   - Patch `mvp_site.world_logic.get_user_settings` alongside `firestore_service` and assert unified default values; keep update tests intact.
> - **Beads tracking**:
>   - Add/close chores reflecting handler refactor and settings defaults unification; update metadata.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 3a4ed2f102cdea8664fa75f76fb84b3830323af9. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
