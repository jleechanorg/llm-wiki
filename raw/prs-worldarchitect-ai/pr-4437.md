# PR #4437: fix(dice): stabilize prompt ordering, enforce code_execution cache metadata, and tighten tests

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-02-01
**Author:** jleechan2015
**Stats:** +428/-362 in 11 files
**Labels:** codex

## Summary
(none)

## Raw Body
### Motivation
- Prevent conflicting dice guidance and fabricated rolls by making code_execution dice instructions authoritative and ensuring they are present where needed in prompt sequences.
- Ensure cached explicit content includes tools metadata so code_execution remains available when reusing caches. 
- Improve robustness of evidence detection and tests to prevent regressions in dice integrity checks.

### Description
- Prompt ordering and builder behavior: add a runtime `AgentProtocol` check, ensure code_execution dice instructions are loaded early and re-appended after combat when needed, and avoid inline BaseAgent imports (`mvp_site/agent_prompts.py`).
- LLM request payload: stop mutating `user_input`; use `enforced_user_input` for code_execution enforcement text so downstream validation uses the original input (`mvp_site/llm_service.py`).
- Explicit cache metadata: track `has_code_execution` on cache managers, require rebuild when a model needs code_execution, and switch cached-content config fields to snake_case (`mvp_site/gemini_cache_manager.py`, relevant callers in `llm_service.py`).
- Code-exec evidence and detection: tighten RNG/DC regexes and parsing, add defensive no-raise handling, and annotate complex functions to satisfy linters (`mvp_site/llm_providers/gemini_code_execution.py`).
- Backfill and compatibility: preserve zero values when backfilling `dice_rolls` from `action_resolution.mechanics.rolls` (`mvp_site/structured_fields_utils.py`).
- Tests and test infra updates: move some inline test imports to module scope, update prompt-marker and code-execution evidence tests, add/adjust the smart-skill-checks integration test and documentation under `testing_mcp/dice/`, and update prompt usage expectations in unit tests (`mvp_site/tests/*`, `testing_mcp/dice/*`, documentation files).
- Documentation and prompts: make narrative examples strategy-agnostic (reference `dice_system_instruction.md`) and add markers to dice prompt files as needed (`mvp_sit
