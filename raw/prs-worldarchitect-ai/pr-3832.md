# PR #3832: feat: Remove React V2 frontend startup logic

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-01-19
**Author:** jleechan2015
**Stats:** +14/-142 in 1 files

## Summary
The React V2 UI has been completely removed from the local server startup process (`run_local_server.sh`). The local development environment now exclusively launches the Flask Backend and MCP Server.

## Raw Body
# Disable React V2 UI

## Summary
The React V2 UI has been completely removed from the local server startup process (`run_local_server.sh`). The local development environment now exclusively launches the Flask Backend and MCP Server.

## Changes
- **Removed** all V2-related CLI arguments (`--with-v2` / `--no-v2` logic).
- **Removed** React port selection logic.
- **Removed** React environment setup (`npm install`, `npx vite` execution).
- **Removed** React health checks and status reporting.
- **Updated** `run_local_server.sh` to focus solely on Flask and MCP services.

## Benefits
- **Simplified Startup**: Faster local server startup time.
- **Reduced Resource Usage**: No longer running an unused Node process.
- **Production Parity**: Aligns local execution closer to production (which never included V2).

## Testing
- [x] Ran `./run_local_server.sh` - Verified Flask and MCP start correctly without V2.
- [x] Ran `bash -n run_local_server.sh` - Verified script syntax is valid.

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Removes the React V2 frontend from local startup; the script now manages only the Flask backend and MCP server.
> 
> - Deletes React-related port selection, env setup (`npm install`, `npx vite`), process launch, and health checks
> - Adjusts port logic: no `REACT_PORT`; MCP port now only avoids `FLASK_PORT`; retains best-effort cleanup for old React port
> - Updates status output and URLs to exclude React; `print_server_config` called with empty React port
> - Keeps Flask startup intact (terminal/background modes), secret loading, and API health checks
> - Minor cleanup: deduplicated env exports and streamlined checks
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 8c7065119cec219155b2c163b508158cf11382e5. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
