# PR #5958: [agento] feat: add --thinking low to openclaw agent command in /claw

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-03-14
**Author:** jleechan2015
**Stats:** +1/-0 in 1 files

## Summary
- Add `--thinking low` flag to the openclaw agent command in `.claude/commands/claw.md`
- The flag is inserted between `-m "$ARGUMENTS"` and `--timeout 1200` to reduce thinking overhead for simpler tasks

## Raw Body
## Summary
- Add `--thinking low` flag to the openclaw agent command in `.claude/commands/claw.md`
- The flag is inserted between `-m "$ARGUMENTS"` and `--timeout 1200` to reduce thinking overhead for simpler tasks

## Production Code Changes
- Before: `nohup openclaw agent --agent main \ -m "$ARGUMENTS" \ --timeout 1200 \`
- After: `nohup openclaw agent --agent main \ -m "$ARGUMENTS" \ --thinking low \ --timeout 1200 \`

## Test Changes
- No test changes required - this is a configuration update to the /claw command

## Known Limitations
- None

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Low risk documentation/config tweak that only changes the CLI flags used by the `/claw` dispatch command; behavior impact is limited to agent runtime settings.
> 
> **Overview**
> Updates the `/claw` command definition in `.claude/commands/claw.md` to pass `--thinking low` to `openclaw agent`, adjusting the agent’s runtime mode while keeping the existing background dispatch flow and `--timeout 1200` unchanged.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit ff3f1f9afe695aa0a367285e560b005824dd8dcf. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
