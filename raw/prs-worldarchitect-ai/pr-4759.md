# PR #4759: fix(automation): align preflight mentions + test stability

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-02-03
**Author:** jleechan2015
**Stats:** +40/-25 in 3 files

## Summary
- cherry-pick PR #4670 changes into main
- default assistant mentions now include @copilot via shared config
- startup preflight now selects CLI in test/CI skip paths and logs consistently
- tighten test isolation for preflight/dispatch validation

## Raw Body
## Summary
- cherry-pick PR #4670 changes into main
- default assistant mentions now include @copilot via shared config
- startup preflight now selects CLI in test/CI skip paths and logs consistently
- tighten test isolation for preflight/dispatch validation

## Testing
- not run (cherry-pick)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Small configuration and control-flow tweaks around automation preflight and test scaffolding. Main risk is unintended behavior change in when preflight failures are tolerated, but it’s limited to explicit test/mock modes.
> 
> **Overview**
> Default assistant mention strings are centralized via `DEFAULT_ASSISTANT_MENTIONS` and updated to always include `@copilot`, with `JleechanorgPRMonitor` now using this shared default instead of a hardcoded value.
> 
> Startup preflight handling in `run_monitoring_cycle` is tightened: env flag checks are made case-insensitive, the chosen CLI is set consistently in skip paths, and test/mock modes can continue even if preflight validation fails while preserving consistent logging.
> 
> Tests are updated to be more isolated and correct: patch targets move to `jleechanorg_pr_monitor`, `ensure_base_clone` uses a temporary directory, and startup-preflight env overrides use `os.environ` to avoid brittle environment mocking.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 79e19ac14a87a2968ffe5b0195c52e958e90d2aa. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->

## Summary by CodeRabbit

* **Chores**
  * Updated default assistant mentions configuration to include an additional assistant option
  * Improved CI and testing workflow stability by refining startup preflight behavior—checks are now more permissive in test environments while maintaining production guardrails
  * Enhanced environmen
