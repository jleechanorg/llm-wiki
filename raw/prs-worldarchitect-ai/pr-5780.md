# PR #5780: testing_http: fix syntax error + add evidence bundle output

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-02-26
**Author:** jleechan2015
**Stats:** +302/-74 in 2 files

## Summary
(none)

## Raw Body
## Changes

- **Fix syntax error** in `test_http_browser_simulation.py:240` — empty `try:` block causing `IndentationError`. Added `from bs4 import BeautifulSoup` import check.
- **Add canonical evidence bundles** to `run_all_http_tests.py`:
  - `provenance.json` (git provenance, run metadata)
  - `evidence.md` (human-readable pass/fail table)
  - `results.jsonl` (machine-readable per-test results)
  - `README.md` (package manifest)
  - Output to `/tmp/worldarchitectai/<branch>/http_tests/iteration_NNN/`

## Verification

All 8/8 HTTP tests pass:
```
RESULTS: 8 passed, 0 failed, 0 skipped, 8 total
ALL TESTS PASSED
Evidence: /tmp/worldarchitectai/dev1771958311/http_tests/iteration_002
```

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Low risk because changes are confined to the `testing_http` test harness; main impact is additional `/tmp` artifact writing and reliance on `bs4` being preinstalled (no longer auto-installed).
> 
> **Overview**
> Adds an **evidence bundle** output to the HTTP test runner (`run_all_http_tests.py`), writing per-run artifacts under `/tmp/worldarchitectai/<branch>/http_tests/iteration_NNN/` (with a `latest` symlink) including `provenance.json` (git + run metadata), `evidence.md`/`README.md`, and `results.jsonl`.
> 
> Refactors test execution to return structured per-test results (skip/timeout handling, 60s subprocess timeout, terser console output, and degraded-mode server check) and removes the broken dependency auto-install block in `test_http_browser_simulation.py` (now relying on the `BeautifulSoup` import).
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit e5fed2b4d5d890958e06afc3a5a7b31054ea76f2. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->

## Summary by CodeRabbit

* **New Features**
  * HTTP tests now generate evidence
