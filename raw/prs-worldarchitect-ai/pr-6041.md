# PR #6041: docs(roadmap): exportcommands Python→shell simplification plan

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-03-23
**Author:** jleechan2015
**Stats:** +202/-0 in 1 files

## Summary
`exportcommands.py` is a 2347-line Python script that wraps `rsync -av` with a custom exception hierarchy, Windows shutil fallback, and semver README generation. The core operation is: rsync dirs, sed-filter strings, git push, open a PR.

## Background
`exportcommands.py` is a 2347-line Python script that wraps `rsync -av` with a custom exception hierarchy, Windows shutil fallback, and semver README generation. The core operation is: rsync dirs, sed-filter strings, git push, open a PR.

## Raw Body
## Background
`exportcommands.py` is a 2347-line Python script that wraps `rsync -av` with a custom exception hierarchy, Windows shutil fallback, and semver README generation. The core operation is: rsync dirs, sed-filter strings, git push, open a PR.

## Goals
- Document the research and decision for replacing exportcommands.py with a shell script
- Track the 4-phase migration (Phase 2 complete: exportcommands.sh written and working)

## Tenets
- No accidental complexity
- Shell is sufficient when Python complexity is not essential

## High-level description of changes
Adds `roadmap/2026-03-22-exportcommands-rsync-simplification.md`:
- Side-by-side Python vs rsync+shell capability table
- Decision: shell wins (all critical features, 97% fewer lines)
- 4-phase migration plan (Phase 1-2 complete)
- Risk assessment with mitigations

## Testing
No code changes. Documentation only.

## Low-level details
- **`roadmap/2026-03-22-exportcommands-rsync-simplification.md`**: New plan doc with research table and migration roadmap.

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Low risk because this PR only adds documentation and does not change any runtime code or deployment behavior.
> 
> **Overview**
> Adds a new roadmap doc, `roadmap/2026-03-22-exportcommands-rsync-simplification.md`, documenting the decision and phased plan to replace `exportcommands.py` with a simpler `rsync`-driven shell script.
> 
> The document includes a side-by-side capability comparison, an example shell implementation outline (rsync, content filtering, git push, PR creation), and a brief risk/mitigation checklist and migration status tracking.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 300eb01c5edb3b25b7e7e60c923d6c03beb2cee2. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-ge
