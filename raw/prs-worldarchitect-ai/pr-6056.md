# PR #6056: [agento] fix(hooks,automation): idle hook suppression, evidence guard, metadata hooks, Python modernization

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-04-04
**Author:** jleechan2015
**Stats:** +191/-98 in 8 files

## Summary
Local branch dev1774336573 had several harness improvements staged but unpushed. Pulled origin/main and creating PR.

## Background
Local branch dev1774336573 had several harness improvements staged but unpushed. Pulled origin/main and creating PR.

## Raw Body
## Background
Local branch dev1774336573 had several harness improvements staged but unpushed. Pulled origin/main and creating PR.

## Goals
- Suppress orchestrator idle hook text from polluting conversation context
- Add argument guard to /evidence_review
- Wire metadata-updater hooks in settings.json
- Modernize automation/ to Python 3.11+ built-in types
- Fix .husky/pre-commit to exclude automation/ from main repo ruff

## Tenets
Smallest safe change; no regressions; automation linting stays in its own toolchain.

## High-level description of changes
- compose-commands.sh: Detects idle hook injections and suppresses them (outputs blank line); adds debug marker to stderr
- evidence_review.md: Step 0 guard requires non-empty ARGUMENTS before dispatching codex/agent
- settings.json: Adds PreToolUse/PostToolUse hooks on Bash calls for metadata-updater.sh (auto-extracts PR URLs and branch names)
- automation/: Removes typing imports; uses dict/None union syntax and UTC constant (Python 3.11+); alphabetizes imports
- .husky/pre-commit: Adds grep -zv automation/ filter so 133 pre-existing ruff violations in automation/ do not block unrelated commits

## Testing
- git commit passes .husky/pre-commit cleanly
- Pre-push checks passed (type check + security scan in warnings-allowed mode)

## Low-level details
- .husky/pre-commit: grep -zv filter added to staged-file detection and all xargs calls
- compose-commands.sh: is_idle_hook detection before command stripping loop; printf newline exit
- settings.json: hooks key with PreToolUse/PostToolUse, timeout 5000ms
- automation_safety_manager.py: dict[str, int] | None replaces Optional[Dict[str, int]]; inline annotations updated
- automation_utils.py / utils.py: same typing modernization pattern

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Moderate risk because it changes Claude Code hook behavior (input suppression and new Pre/Post Bash hooks), which could affect command composition and tool execution flow if the
