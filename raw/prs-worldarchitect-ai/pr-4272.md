# PR #4272: Dice backfill: populate action_resolution from legacy data

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-01-31
**Author:** jleechan2015
**Stats:** +144/-81 in 5 files

## Summary
- Backfill action_resolution from outcome_resolution when dice data missing
- Add regression tests for backfill behavior and UI dice display requirements

## Raw Body
## Summary
- Backfill action_resolution from outcome_resolution when dice data missing
- Add regression tests for backfill behavior and UI dice display requirements

## Original PR
- https://github.com/jleechanorg/worldarchitect.ai/pull/4226

## Beads
- DICE-6de

## Testing
- Not run (split-only change)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Touches story pagination response shaping for all campaigns and changes when legacy merge logic runs; while merges are additive (only filling missing fields), it could subtly alter dice/audit visibility in the UI for mixed legacy entries.
> 
> **Overview**
> Fixes legacy dice display by adjusting `get_story_paginated` to merge `outcome_resolution` dice/audit data into an existing `action_resolution` whenever the outcome contains dice data (instead of only when the existing action resolution was missing dice).
> 
> Adds a regression test ensuring rolls are merged without overwriting existing `audit_events`, moves the CODE_EXECUTION backfill validation tests into `test_dice_integrity_helpers`, and updates the end-to-end UI dice regression fixture to match the current `roll_skill_check` tool result shape.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 3a25d43cf5f68a3450c654f20868dd0538ce6b8c. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->
## Summary by CodeRabbit

* **Bug Fixes**
  * Improved merging so legacy and incoming roll results are consistently preserved and shown in the UI, ensuring existing audit info is not overwritten.
  * Updated handling so new structured tool-result fields (skill, roll, modifier, dc, etc.) display correctly.

* **Tests**
  * Added tests covering roll backfill behavior across resolution strategies and updated end-to-end expectations for the new tool-result format.

<sub>✏️ Tip
