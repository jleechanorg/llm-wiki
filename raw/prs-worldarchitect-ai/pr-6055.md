# PR #6055: [agento] feat(openclaw): public OpenAI-compatible inference proxy (/v1/chat/completions)

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-04-05
**Author:** jleechan2015
**Stats:** +1828/-20 in 14 files

## Summary
- **New Flask routes**: `POST /v1/chat/completions` and `GET /v1/models` — forward external clients' inference requests to each user's personal OpenClaw gateway, returning OpenAI-compatible JSON or streaming SSE responses
- **Auth**: `worldai_` personal API keys (same mechanism as `/mcp` — no new auth layer needed; `@check_token` already handles prefix tokens)
- **URL resolution**: per-user `gateway_url` + `gateway_token` from Firestore settings (same `get_user_settings()` path as existing OpenC

## Background
WorldArchitect.AI already uses OpenClaw as its **client** (per-user local gateway → Flask → LLM). This PR adds the reverse direction: Flask exposes an **OpenAI-compatible server endpoint** so external tools (CLI, other apps) can route inference through the same per-user gateway, authenticated by personal API keys.

## Raw Body
## Summary
- **New Flask routes**: `POST /v1/chat/completions` and `GET /v1/models` — forward external clients' inference requests to each user's personal OpenClaw gateway, returning OpenAI-compatible JSON or streaming SSE responses
- **Auth**: `worldai_` personal API keys (same mechanism as `/mcp` — no new auth layer needed; `@check_token` already handles prefix tokens)
- **URL resolution**: per-user `gateway_url` + `gateway_token` from Firestore settings (same `get_user_settings()` path as existing OpenClaw client code in `llm_service.py`)
- **Security**: `validate_openclaw_gateway_url()` blocks private IPs and loopback for user-supplied URLs; `MAX_PROXY_MAX_TOKENS=8192` caps output; key hashes never logged

## Background
WorldArchitect.AI already uses OpenClaw as its **client** (per-user local gateway → Flask → LLM). This PR adds the reverse direction: Flask exposes an **OpenAI-compatible server endpoint** so external tools (CLI, other apps) can route inference through the same per-user gateway, authenticated by personal API keys.

## Files
| File | Change |
|---|---|
| `mvp_site/llm_providers/openai_proxy_provider.py` | **New** — payload parsing, gateway forwarding, SSE streaming relay |
| `mvp_site/main.py` (+147 lines) | **New routes** + module-level `_proxy_gateway_get` helper for testability |
| `mvp_site/tests/test_openai_inference_proxy.py` | **New** — 37 tests: payload validation, non-stream/stream forwarding, auth, error paths |

## Testing
- `pytest mvp_site/tests/test_openai_inference_proxy.py` — **37 passed**
- Ruff lint: clean (pre-commit enforced)
- Auth tests: valid key → 200, revoked key → 401, no auth → 401
- Gateway unreachable → 502, timeout → 504, invalid JSON → 400, missing gateway_url → 400
- Streaming: SSE content-type, `data: [DONE]` on completion

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **High Risk**
> Adds new authenticated public `/v1/*` inference proxy endpoints that forw
