# PR #2038: refactor: Extract Beads MCP installation into reusable function

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2025-11-17
**Author:** jleechan2015
**Stats:** +78/-72 in 1 files

## Summary
Extract inline Beads MCP server installation code into a dedicated `install_beads_mcp()` function for better code organization and reusability.

## Raw Body
## Summary
Extract inline Beads MCP server installation code into a dedicated `install_beads_mcp()` function for better code organization and reusability.

## Changes
- ✅ Created `install_beads_mcp()` function following existing installer pattern
- ✅ Moved all Beads MCP installation logic from inline code into function
- ✅ Follows same structure as `install_ios_simulator_mcp()` and other installers
- ✅ Uses `update_stats()` for proper statistics tracking
- ✅ Improves code maintainability and consistency

## Type of Change
- **Refactoring** - Pure code reorganization with no functional changes

## Benefits
1. **Better Organization** - Code is now modular and follows established patterns
2. **Easier Maintenance** - Function can be tested and updated independently
3. **Consistency** - Matches the structure of other MCP installer functions
4. **Reduced Duplication** - Installer logic is now in one reusable function

## Testing
- ✅ Pre-commit hooks passed
- ✅ No functional changes - pure refactoring
- ✅ Follows existing code patterns in mcp_common.sh

## Code Quality
- Lines changed: +73/-72 (net +1 line)
- Complexity: Reduced (extracted function)
- Readability: Improved (clearer separation of concerns)

## Reference
Pattern follows these existing functions in mcp_common.sh:
- `install_ios_simulator_mcp()`
- `install_github_mcp()`
- `install_perplexity_mcp()`

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Extracts Beads MCP setup into a reusable install_beads_mcp() function and replaces the previous inline installation logic with a single function call.
> 
> - **scripts/.claude/scripts/mcp_common.sh**:
>   - **New function**: `install_beads_mcp()` encapsulates Beads MCP server setup.
>     - Checks existing server, `beads-mcp` availability, and optional `bd` CLI path.
>     - Sets env flags (`BEADS_USE_DAEMON=1`, optional `BEADS_PATH`) and adds server via `${MCP_CLI_BIN} mcp add`.
>     - Uses centralized logging
