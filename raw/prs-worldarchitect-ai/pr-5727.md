# PR #5727: Settings: gate OpenClaw validation by provider selection

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-02-23
**Author:** jleechan2015
**Stats:** +269/-4 in 6 files

## Summary
- Provider-aware settings validation fix remains in place: OpenClaw gateway settings are validated/persisted only when OpenClaw is the effective provider.
- OpenClaw routing test setup has been hardened for flaky/misaligned token behavior based on GitHub review comments.

## Raw Body
## Summary
- Provider-aware settings validation fix remains in place: OpenClaw gateway settings are validated/persisted only when OpenClaw is the effective provider.
- OpenClaw routing test setup has been hardened for flaky/misaligned token behavior based on GitHub review comments.

## Code changes (latest)
### Provider behavior
- `mvp_site/settings_validation.py`
  - `validate_settings_batch` + `validate_settings_with_cross_validation` now support `target_llm_provider` and gate `openclaw_gateway_*` keys unless provider is `openclaw`.
- `mvp_site/world_logic.py`
  - `update_user_settings_unified` now resolves effective provider and passes it into validation.
- Tests:
  - `mvp_site/tests/test_settings_validation.py`
  - `mvp_site/tests/test_settings_api.py`

### OpenClaw E2E robustness
- `testing_ui/test_openclaw_e2e.py`
  - Added `_resolve_openclaw_test_user_gateway_token()` to avoid empty-primary env masking legacy token env.
  - Added `OPENCLAW_TOKEN_WAIT_TIMEOUT_S = 20` and a dedicated `_wait_for_openclaw_gateway_token_state()` helper.
  - Step 2 token proof wait now uses this dedicated timeout.
  - Simplified proof payload to explicit `gateway_token_set: true` after successful wait.

### New TDD coverage
- `testing_ui/test_openclaw_e2e_helpers.py`
  - Verifies primary token precedence and legacy fallback when primary is blank.
  - Verifies token-persistence wait helper uses `OPENCLAW_TOKEN_WAIT_TIMEOUT_S`.

## Notes
- Commits on branch:
  - `bcfc78b00`: provider-aware OpenClaw settings validation
  - `161f30015`: OpenClaw E2E token source + wait-timeout hardening + helper tests

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Touches settings persistence/validation flow and can change what user-supplied fields are saved or rejected; mistakes could silently drop intended OpenClaw config or allow invalid data when provider resolution is wrong.
> 
> **Overview**
> Prevents OpenClaw gateway fields from blocking provider switches by making settings validat
