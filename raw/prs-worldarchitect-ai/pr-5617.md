# PR #5617: chore: gitignore testmon WAL/SHM sidecar files and test-results

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-02-18
**Author:** jleechan2015
**Stats:** +3/-0 in 1 files

## Summary
- Add `.testmondata-shm`, `.testmondata-wal`, and `test-results/` to `.gitignore`
- These are transient SQLite sidecar files and test output directories that should not be tracked

## Test Plan
- [x] Verify untracked files no longer appear in `git status` after gitignore update

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Only adjusts `.gitignore`; no production code or runtime behavior changes.
> 
> **Overview**
> Updates `.gitignore` to also ignore Testmon’s transient SQLite sidecar files (`.testmondata-shm`, `.testmondata-wal`) and the `test-results/` output directory, reducing untracked test noise in `git s

## Raw Body
## Summary
- Add `.testmondata-shm`, `.testmondata-wal`, and `test-results/` to `.gitignore`
- These are transient SQLite sidecar files and test output directories that should not be tracked

## Production Code Changes
**`.gitignore`** - only had `.testmondata` → now also ignores `-shm`, `-wal` SQLite sidecar files and `test-results/` output directory → prevents untracked file noise from testmon runs

## Test plan
- [x] Verify untracked files no longer appear in `git status` after gitignore update

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Only adjusts `.gitignore`; no production code or runtime behavior changes.
> 
> **Overview**
> Updates `.gitignore` to also ignore Testmon’s transient SQLite sidecar files (`.testmondata-shm`, `.testmondata-wal`) and the `test-results/` output directory, reducing untracked test noise in `git status`.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit e77f36d76fb3e71e5039742cf3c06f0a3ac03967. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->

## Summary by CodeRabbit

* **Chores**
  * Updated project configuration to exclude additional temporary files and test artifacts from version control.

<!-- end of auto-generated comment: release notes by coderabbit.ai -->
