# PR #6014: fix(lw+streaming): persistence + P3 streaming fixes combined

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-03-19
**Author:** jleechan2015
**Stats:** +1065/-893 in 6 files

## Summary
- **rev-lw5lk / rev-lwuz5 (P1)**: Persist `full_state_updates` and `game_state_snapshot` in every story entry so Living World deltas are never lost; lazy-init `game_states` subcollection for old campaigns that predate it
- **rev-lw-warn-freeze-guard (P2)**: `_warn_if_living_world_missing` now accepts `should_freeze_time` and skips the warning when time is frozen, preventing false-positive LW warnings
- **rev-stream-sign-env (P3)**: Inject `STREAM_RESPONSE_SIGNING_SECRET` in test env so signature

## Raw Body
## Summary
- **rev-lw5lk / rev-lwuz5 (P1)**: Persist `full_state_updates` and `game_state_snapshot` in every story entry so Living World deltas are never lost; lazy-init `game_states` subcollection for old campaigns that predate it
- **rev-lw-warn-freeze-guard (P2)**: `_warn_if_living_world_missing` now accepts `should_freeze_time` and skips the warning when time is frozen, preventing false-positive LW warnings
- **rev-stream-sign-env (P3)**: Inject `STREAM_RESPONSE_SIGNING_SECRET` in test env so signature validation passes in local runs
- **rev-stream-warn-sse (P3)**: Surface LW warning strings in the SSE `done` event `warnings` field so clients can observe them
- **rev-stream-dirty-unconditional (P3)**: Gate `state_dirty=True` behind actual state changes rather than setting it unconditionally

## Production Code Changes
- `structured_fields_utils.py`: added `_build_traceability_fields()` → injects `full_state_updates` + optional `core_memories_snapshot` into story entry
- `world_logic.py`: `_persist_turn_to_firestore` injects `game_state_snapshot`; `_prepare_game_state_with_user_settings` lazy-inits missing `game_states` subcollection
- `streaming_orchestrator.py`: `_warn_if_living_world_missing` gets `should_freeze_time` param + freeze guard, returns `str | None`, wired into SSE done event; `state_dirty` conditioned on real state changes; retry scope narrowed; test signing secret injected

## Test Changes
No new tests in this PR (existing unit suite covers persistence and streaming paths).

## Known Limitations
- `rev-wa1u5` (character creation blocks LW instruction loading) and P2 investigation beads (`rev-wadm3`, `rev-wai47`) are tracked separately and not addressed here.

Closes: rev-lw5lk, rev-lwuz5, rev-lw-warn-freeze-guard, rev-stream-sign-env, rev-stream-warn-sse, rev-stream-dirty-unconditional

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Touches Firestore persistence and stream
