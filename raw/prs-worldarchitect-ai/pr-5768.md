# PR #5768: CI: centralize self-hosted selector

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-02-25
**Author:** jleechan2015
**Stats:** +19/-15 in 9 files

## Summary
- Centralize self-hosted runner selection across workflows via a single expression:
  - `runs-on: ${{ fromJson(vars.SELF_HOSTED_RUNNER_LABELS || '["self-hosted"]') }}`
- Keep fallback behavior unchanged (`ubuntu-latest` fallback jobs unchanged).
- Update self-hosted workflow docs to document the shared selector and optional repo variable.

## Raw Body
## Summary
- Centralize self-hosted runner selection across workflows via a single expression:
  - `runs-on: ${{ fromJson(vars.SELF_HOSTED_RUNNER_LABELS || '["self-hosted"]') }}`
- Keep fallback behavior unchanged (`ubuntu-latest` fallback jobs unchanged).
- Update self-hosted workflow docs to document the shared selector and optional repo variable.

## Why
If runner labels change again (for example temporary macOS-only tags), update `SELF_HOSTED_RUNNER_LABELS` instead of editing every workflow file.

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Config-only changes to workflow `runs-on` selectors and documentation; primary risk is misconfigured `SELF_HOSTED_RUNNER_LABELS` causing self-hosted jobs to not match a runner.
> 
> **Overview**
> **Centralizes self-hosted runner selection across CI workflows** by replacing hardcoded `runs-on: self-hosted` with a shared expression `fromJson(vars.SELF_HOSTED_RUNNER_LABELS || '["self-hosted"]')` in workflows like `claude-processor.yml`, `doc-size-check.yml`, `hook-tests.yml`, `mcp-smoke-tests.yml`, `pr-cleanup.yml`, `self-hosted-mvp-shard1.yml`, and `test-self-hosted-runner.yml`.
> 
> Updates the self-hosted runner documentation (`.github/workflows/README.md`) and runner validation report (`RUNNER_VERSION_TEST_REPORT.md`) to document the new selector and the optional `SELF_HOSTED_RUNNER_LABELS` repo variable for label changes without editing workflow files.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 1986445b726c2c6012b92a8b981c89ced20f31bf. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->

## Summary by CodeRabbit

* **Chores**
  * Enhanced CI/CD infrastructure to support configurable self-hosted runner selection through repository variables, enabling centralized management of runner labels and reducing the ne
