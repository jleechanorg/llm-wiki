# PR #2302: Add provider auth error handling and rate limit coverage

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2025-12-03
**Author:** jleechan2015
**Stats:** +136/-7 in 3 files
**Labels:** codex

## Summary
- add status_code-aware LLMRequestError constructor and reuse it across provider errors
- map provider authentication/authorization failures to explicit LLMRequestError responses and include rate-limit coverage in tests

## Raw Body
## Summary
- add status_code-aware LLMRequestError constructor and reuse it across provider errors
- map provider authentication/authorization failures to explicit LLMRequestError responses and include rate-limit coverage in tests

## Testing
- pytest mvp_site/tests/test_llm_service_error_handling.py


------
[Codex Task](https://chatgpt.com/codex/tasks/task_e_692fddb49c28832fb9a5fe43ad9e00fd)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Map 401/403 provider errors to explicit `LLMRequestError`s, add diagnostic parsing/final-response logs, and extend tests for auth/access failures.
> 
> - **Backend – Error Handling (`mvp_site/llm_service.py`)**:
>   - Map provider errors containing `"401"`/`"403"` to `status_code` 401/403 and raise `LLMRequestError` with clear messages.
>   - Preserve existing handling for `429`/`503`; continue surfacing `ContextTooLargeError` as 422.
> - **Logging**:
>   - Add parsed-response diagnostics and empty-narrative warnings in `get_initial_story` and `continue_story`.
>   - Promote final response length/structure logs to INFO.
> - **Exceptions (`mvp_site/llm_request.py`)**:
>   - `LLMRequestError` now sets `status_code` only when provided.
> - **Tests (`mvp_site/tests/test_llm_service_error_handling.py`)**:
>   - Add coverage asserting 401 (authentication) and 403 (access denied) are surfaced without retry.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 67c8651aecac2584a3591b0ed118b8de06d176d2. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
