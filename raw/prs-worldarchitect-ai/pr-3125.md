# PR #3125: Align rewards self-corrections and mechanical choice tests

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-01-06
**Author:** jleechan2015
**Stats:** +274/-107 in 12 files
**Labels:** codex

## Summary
- Persist `pending_system_corrections` in the initial game_state save (all modes) and clear stale corrections; avoid redundant Firestore write.
- Validate `LLMRequest.system_corrections` list type + item strings to ensure clean prompt injection.
- Tighten rewards tests: require REWARDS corrections, verify `RewardsAgent.matches_game_state()` is false once `rewards_processed=True`, and allow `continue_adventure` as the safe mechanical choice.
- Note: rewards handling uses detection + LLM self-corr

## Raw Body
## Summary
- Persist `pending_system_corrections` in the initial game_state save (all modes) and clear stale corrections; avoid redundant Firestore write.
- Validate `LLMRequest.system_corrections` list type + item strings to ensure clean prompt injection.
- Tighten rewards tests: require REWARDS corrections, verify `RewardsAgent.matches_game_state()` is false once `rewards_processed=True`, and allow `continue_adventure` as the safe mechanical choice.
- Note: rewards handling uses detection + LLM self-correction (no server-side enforcement).

## Testing
- Previously run:
  - python -m pytest mvp_site/tests/test_end2end/test_rewards_processed_enforcement_end2end.py
  - python -m pytest mvp_site/tests/test_end2end/test_rewards_agent_mechanical_end2end.py
  - python -m pytest testing_mcp/test_rewards_agent_real_e2e.py -k check_continue_options
- Not re-run after commits 13979ad / 18306cf.


------
[Codex Task](https://chatgpt.com/codex/tasks/task_e_695b5100d9f0832fbaa07f6f15903fd4)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Introduces bounded story payloads, formalizes the system-corrections flow, expands equipment slot handling, and tightens RewardsAgent behavior/tests.
> 
> - Limits campaign story to the last 300 entries to avoid oversized responses; adds unit tests for truncation
> - Validates `LLMRequest.system_corrections` (list of strings); persists/merges `pending_system_corrections` across turns and clears them when consumed; updates prompt docs on using corrections
> - Rewards flow/tests: require explicit `REWARDS_*` corrections, ensure `RewardsAgent.matches_game_state()` is false once `rewards_processed=True`, and allow a mechanical `continue_adventure` choice (no story options)
> - Equipment: supports named weapon slots (`weapon_main`, `weapon_secondary`), numbered rings (`ring_1`, `ring_2`), instrument slot; extraction recognizes more flat slots; display categorization updated
> - Prompt docs: add canonical equipment slots and forbidden mappings
> 
> <sup>Wr
