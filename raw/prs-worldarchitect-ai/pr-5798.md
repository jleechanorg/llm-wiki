# PR #5798: fix: expose ai_orch run defaults in top-level help

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-02-28
**Author:** jleechan2015
**Stats:** +529/-112 in 7 files

## Summary
- Add  support to ai_orch run parser (mutually exclusive with --no-worktree) and default to worktree mode.
- Add top-level help expansion when using usage: ai_orch [-h] [--version] {run,dispatcher,live,list,attach,kill} ...

AI Orchestration CLI (unified runtime + task dispatcher)

positional arguments:
  {run,dispatcher,live,list,attach,kill}
                        Commands
    run                 Run unified orchestration (orchestrate_unified
                        interface)
    dispatcher 

## Raw Body
## Summary
- Add  support to ai_orch run parser (mutually exclusive with --no-worktree) and default to worktree mode.
- Add top-level help expansion when using usage: ai_orch [-h] [--version] {run,dispatcher,live,list,attach,kill} ...

AI Orchestration CLI (unified runtime + task dispatcher)

positional arguments:
  {run,dispatcher,live,list,attach,kill}
                        Commands
    run                 Run unified orchestration (orchestrate_unified
                        interface)
    dispatcher          Direct TaskDispatcher utilities
    live                (Legacy) Start interactive AI CLI session
    list                (Legacy) List active tmux sessions
    attach              (Legacy) Attach to existing session
    kill                (Legacy) Kill a session

options:
  -h, --help            show this help message and exit
  --version             show program's version number and exit

Examples:
  # Unified orchestration (default mode)
  ai_orch run --agent-cli gemini,claude "Fix failing tests"
  ai_orch --agent-cli claude "Implement feature X"

  # TaskDispatcher helpers
  ai_orch dispatcher analyze --agent-cli codex "Refactor API handlers"
  ai_orch dispatcher create --agent-cli gemini "Fix PR #123 conflicts"

  # Legacy interactive tmux mode
  ai_orch live --cli codex
         so run-mode arguments (including runtime flags like --worktree/--no-worktree) are visible even in default run mode.
- Keep legacy command semantics unchanged and preserve task dispatcher/live behavior.

## Testing
- python3 -m pytest orchestration/tests/test_orchestrate_unified_args.py -q

Closes # (if applicable)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Updates CLI parsing and defaults (notably default `agent_cli` and how `--model` routes between `run` vs `live`), which could change runtime behavior for existing users despite being mostly a refactor.
> 
> **Overview**
> **Centralizes orchestration CLI argument definitions** by introducing `orchestration/c
