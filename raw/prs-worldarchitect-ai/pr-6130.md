# PR #6130: [agento] feat(cursor): portable AO metadata hooks for Cursor sessions

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-04-10
**Author:** jleechan2015
**Stats:** +612/-2 in 4 files

## Summary
- Add `.cursor/metadata-updater.sh` (executable) for Agent Orchestrator session metadata updates on `gh pr create`, branch switches, and allowed merges.
- Configure workspace-relative Cursor `PreToolUse` / `PostToolUse` hooks using `${workspaceFolder}` so clones and worktrees do not embed absolute paths.
- Resolve `~/.agent-orchestrator/*-worldarchitect.ai/sessions` automatically when `AO_DATA_DIR` is unset.

## Raw Body
## Summary
- Add `.cursor/metadata-updater.sh` (executable) for Agent Orchestrator session metadata updates on `gh pr create`, branch switches, and allowed merges.
- Configure workspace-relative Cursor `PreToolUse` / `PostToolUse` hooks using `${workspaceFolder}` so clones and worktrees do not embed absolute paths.
- Resolve `~/.agent-orchestrator/*-worldarchitect.ai/sessions` automatically when `AO_DATA_DIR` is unset.

## Production / repo changes
- **Before:** Local-only hook wiring pointed at a single worktree path and fixed AO sessions dir.
- **Now:** Hooks invoke the repo script from the opened workspace; AO sessions directory is discovered for the worldarchitect.ai orchestrator layout or overridden via `AO_DATA_DIR`.

## Test changes
- None (tooling only). Local: `python -m pytest tests/ -x -q` (47 passed, 4 skipped).

## Tracker
- Beads: `rev-mbr2` (AO session slug: `rev-z6ck`)

## Known limitations
- If multiple `*-worldarchitect.ai` orchestrator dirs exist under `~/.agent-orchestrator`, the first glob match is used unless `AO_DATA_DIR` is set explicitly.
- `${workspaceFolder}` substitution depends on Cursor hook behavior (aligned with other `.cursor` config patterns).

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Low risk: changes are limited to Cursor workspace hook configuration and a new local shell hook script plus regression tests, with no impact on runtime application code. Main risk is developer workflow disruption if hook parsing/guardrails block `gh` commands unexpectedly.
> 
> **Overview**
> Adds a workspace-relative Cursor hook (`.cursor/settings.json`) that runs a new `.cursor/metadata-updater.sh` on `PreToolUse`/`PostToolUse` to keep Agent Orchestrator session metadata in sync across clones/worktrees.
> 
> The hook script discovers the AO sessions directory when `AO_DATA_DIR` is unset, updates session keys for `gh pr create` (PR URL/status) and `git switch`/`checkout` (branch), and adds guardrails to require `[agento]` PR titles and 
