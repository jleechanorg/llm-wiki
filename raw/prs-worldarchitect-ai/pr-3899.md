# PR #3899: Fix Settings Auth & Homepage (Replacement for #3824)

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-01-25
**Author:** jleechan2015
**Stats:** +368/-86 in 8 files

## Summary
(none)

## Raw Body
This PR replaces #3824 which was accidentally closed.

## Changes
1. **Settings Auth:** Secure server-side route adjustment (no localStorage hacks).
2. **Homepage Fix:** Resolved blank page on custom ports (CSP, JS syntax/logic, HTML visibility).

## Verification
- Verified Settings page loads on direct navigation.
- Verified Homepage loads "Sign in with Google" on custom ports (e.g. 8050).

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> - **Centralized auth utilities**: New `authTokenManager` (token refresh scheduling, `getAuthHeaders`, test-mode bypass) and immediate Firebase init; safer auth UI handling and sign-in/out wiring.
> - **Routing/auth enforcement**: `app.js` now gates navigation on Firebase auth only, stores `redirect_after_login`, and shows `auth` view when unauthenticated.
> - **Settings via SPA**: `/settings` page served without server auth; SPA fetches and injects settings HTML and uses `getAuthHeaders` for `/api/settings` (still server-validated).
> - **CSP-friendly theme init**: Inline theme script removed from `index.html`; added `frontend_v1/js/theme-init.js` and marked `auth-view` as the default active view.
> - **Templates**: `base.html` now includes Firebase compat SDKs and `frontend_v1/auth.js` globally to support settings and auth.
> - **Dev tooling**: `run_local_server.sh` revamped for robust port selection, optional React v2 (disabled by default), clearer health checks, and safer env handling.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 9f7b8560e30d5fb4362b7b77f0e03fe966a83308. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->
## Summary by CodeRabbit

* **New Features**
  * Public auth token manager (refresh scheduling, auth headers, current user)
  * Early theme initializer to prevent theme flash
  * Firebase auth SDK integration for fro
