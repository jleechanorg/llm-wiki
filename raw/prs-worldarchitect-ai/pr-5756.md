# PR #5756: ci: extend self-hosted runner control-plane to coverage workflow

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-02-26
**Author:** jleechan2015
**Stats:** +238/-25 in 12 files

## Summary
- harden `.github/workflows/coverage.yml` with self-hosted runner control-plane primitives
- remove `venv` restore from `actions/cache` and force deterministic fresh virtualenv bootstrap
- add runner preflight + quarantine gate + health score update and artifact export
- update `docs/ci/runner-hosting-strategy.md` status/implementation notes
- record bead tracking update in `.beads/issues.jsonl`

## Raw Body
## Summary
- harden `.github/workflows/coverage.yml` with self-hosted runner control-plane primitives
- remove `venv` restore from `actions/cache` and force deterministic fresh virtualenv bootstrap
- add runner preflight + quarantine gate + health score update and artifact export
- update `docs/ci/runner-hosting-strategy.md` status/implementation notes
- record bead tracking update in `.beads/issues.jsonl`

## Why
This aligns coverage execution with the runner reliability direction: reduced host-drift risk, explicit infra health gating, and post-run evidence for runner quarantine decisions.

## Changes
- `.github/workflows/coverage.yml`
  - add `runner_preflight` and quarantine check/gate
  - remove `venv` from cache restore paths
  - switch to explicit `venv/bin/python` and `venv/bin/pip` usage (no activation dependency)
  - add runner health score update and upload `runner_health.json` / `runner_health_score.json`
  - include runner health summary in step summary output
- `docs/ci/runner-hosting-strategy.md`
  - mark status as `In Progress`
  - add implemented note for coverage workflow control-plane rollout
- `.beads/issues.jsonl`
  - add `REV-rcp-cov` closure entry for this rollout slice

## Validation
- JSONL integrity check: `jsonl-parse-ok`
- I did not run workflow execution locally in this environment.

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Changes CI execution and gating for multiple self-hosted workflows (preflight/quarantine) and rewires coverage to a fresh-venv model, which could block jobs if thresholds/scripts misbehave. No production runtime code is affected, but CI reliability and merge signal could be impacted.
> 
> **Overview**
> Adds a new `.github/actions/runner-control-plane` composite action to standardize self-hosted runner cleanup, tool-cache prep, preflight health checks, and a quarantine gate based on `scripts/runner_health_score.py`.
> 
> Updates `.github/workflows/coverage.yml` to run on self-hosted runners, **stop ca
