# PR #5620: fix(ci): resolve YAML parse error in fetch-pr-details action

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-02-18
**Author:** jleechan2015
**Stats:** +1/-8 in 1 files

## Summary
- **Fixed YAML parse error** in `.github/actions/fetch-pr-details/action.yml` that caused Deploy to Production workflow #33 to fail
- **Root cause**: Multi-line Python heredoc inside `run: |` block had lines starting at column 1, which terminates YAML block scalars
- **Fix**: Replaced 7-line Python script with single-line one-liner that stays properly indented within the YAML block

## Test Plan
- [x] YAML validates with `yaml.safe_load()` locally
- [x] Python truncation logic verified for short and long inputs
- [ ] CI checks pass on PR
- [ ] Re-trigger Deploy to Production workflow to verify fix

## Raw Body
## Summary
- **Fixed YAML parse error** in `.github/actions/fetch-pr-details/action.yml` that caused Deploy to Production workflow #33 to fail
- **Root cause**: Multi-line Python heredoc inside `run: |` block had lines starting at column 1, which terminates YAML block scalars
- **Fix**: Replaced 7-line Python script with single-line one-liner that stays properly indented within the YAML block

## Production Code Changes
**`.github/actions/fetch-pr-details/action.yml`** - before: multi-line Python heredoc with `import sys` / `text = sys.stdin.read()` at column 1 broke YAML parser → now: single-line `python3 -c 'import sys; t=sys.stdin.read(); sys.stdout.write(t[:500]+"..." if len(t)>500 else t)'` stays indented → why: lines at column 1 inside `run: |` terminate the YAML block scalar, causing `could not find expected ':'` parse error

## Test Plan
- [x] YAML validates with `yaml.safe_load()` locally
- [x] Python truncation logic verified for short and long inputs
- [ ] CI checks pass on PR
- [ ] Re-trigger Deploy to Production workflow to verify fix

## Known Limitations
- None — functionally identical truncation behavior

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Small CI-only change that refactors an inline script without altering workflow inputs/outputs or production logic.
> 
> **Overview**
> Fixes `.github/actions/fetch-pr-details/action.yml` YAML parsing by replacing the multi-line embedded Python truncation snippet with a single-line `python3 -c` expression that stays safely indented inside the `run: |` block.
> 
> Behavior remains the same: the PR body is still truncated to 500 characters and suffixed with `...` when over the limit.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 208ec07de366ca8966952d741abea42408f9b3f0. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR
