# PR #5561: feat(streaming): progressive planning_block.thinking extraction

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-02-19
**Author:** jleechan2015
**Stats:** +2712/-219 in 24 files

## Summary
Adds progressive display of `planning_block.thinking` during streaming. Previously, users saw a placeholder while the LLM's thinking was invisibly embedded in the JSON envelope.

## Raw Body
## Summary

Adds progressive display of `planning_block.thinking` during streaming. Previously, users saw a placeholder while the LLM's thinking was invisibly embedded in the JSON envelope.

## Changes

### streaming.js
- Added `_extractPlanningThinking()` method using the same character-walking approach as `_extractNarrativeFromRawEnvelope`, with a `planning_block` context guard
- Added `onPlanningThinking` callback with deduplication in the chunk handler
- Added hidden thinking display container to `createStreamingHandler` `onStreamStart`
- Wired up `onPlanningThinking` handler in `createStreamingHandler`

### test_streaming_planning_extraction.py [NEW]
- 13 unit tests for the extraction algorithm covering: complete/partial JSON, escape sequences, context guard, progressive chunks, choices array, god-mode isolation

## God Mode Status
God mode `display_text` is already working correctly — the backend sets `display_text = god_mode_display_text` when narrative is empty, and the frontend fallback chain handles it.

## Test Results
- `test_streaming_planning_extraction.py` — 13/13 PASSED
- `test_streaming_orchestrator.py` — 40/40 PASSED

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Touches core streaming display/extraction logic and expands test/evidence instrumentation (including auth header propagation and redaction), which can affect real-time UX and captured telemetry if edge cases in partial JSON parsing or header merging are missed.
> 
> **Overview**
> Improves streaming UX by extracting `planning_block.thinking` (and `god_mode_response` in structured envelopes) from partial JSON chunks and emitting it via a new `onPlanningThinking` callback, with a shared renderer and updated streaming entry DOM so the UI can show planning text during a stream instead of placeholders.
> 
> Strengthens streaming reliability and testability: stream routes now inherit MCP client default auth headers, streaming class cleanup is more consistent, and new unit/E2E tests 
