# PR #6018: [agento] docs: testing design for MCP, HTTP, and UI test cases

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-03-19
**Author:** jleechan2015
**Stats:** +2223/-0 in 7 files

## Summary
Creates a comprehensive test design document () that covers all core user flows across three testing layers:

### Testing Layers Covered

1. **MCP Tests** () - Protocol-level tests using the MCP client
2. **HTTP Tests** () - REST API tests using HTTP requests  
3. **UI Tests** () - Browser-based tests using Playwright

### Core User Flows Documented

1. **Authentication** - Google OAuth, test mode auth bypass, token refresh
2. **Campaign Creation** - 3-step wizard, title/setting/character config

## Raw Body
## Summary

Creates a comprehensive test design document () that covers all core user flows across three testing layers:

### Testing Layers Covered

1. **MCP Tests** () - Protocol-level tests using the MCP client
2. **HTTP Tests** () - REST API tests using HTTP requests  
3. **UI Tests** () - Browser-based tests using Playwright

### Core User Flows Documented

1. **Authentication** - Google OAuth, test mode auth bypass, token refresh
2. **Campaign Creation** - 3-step wizard, title/setting/character configuration
3. **Character Creation** - Race/class selection, stats, inventory management
4. **Story Interactions** - Chat responses, SSE streaming, chunk ordering validation
5. **Save/Load** - Campaign persistence, cloud sync, multi-slot saves
6. **Settings/Profile** - User preferences, model selection, profile management

### Implementation

The document includes:
- Architecture overview with layer dependencies
- Base class references and key methods
- Test case tables for each flow across all three layers
- Code patterns for MCP tests using 
- Evidence collection strategies
- Best practices for test isolation and parallel execution

This design document provides the foundation for implementing comprehensive automated tests across all testing layers.

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Low risk because changes only add documentation and new test modules; no production logic is modified, though the new tests may be flaky if run in CI due to dependence on a local server and loose assertions.
> 
> **Overview**
> Adds a new `docs/TESTING_DESIGN.md` that defines the planned testing strategy across **MCP**, **HTTP**, and **UI** layers, including core user flows, example patterns, evidence collection, and how to run each suite.
> 
> Introduces new `testing_http/test_core_flows_*.py` modules that act as cross-layer *smoke tests* for authentication, campaign creation, character management, story interactions (including SSE streaming), save/load, and sett
