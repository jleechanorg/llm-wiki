# PR #5966: feat(scripts): add check-pr-status.sh — poll PRs, seek CodeRabbit approval

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-03-15
**Author:** jleechan2015
**Stats:** +199/-3 in 5 files

## Summary
Add `scripts/check-pr-status.sh` that:
- Polls all open PRs in **worldarchitect.ai** and **jleechanclaw** every 5 minutes
- Posts `@coderabbitai all good?` on each PR (throttled 1/hr to avoid spam)
- `PING_CODERABBIT=false` to skip pings; `--once` for single run

## Raw Body
## Summary
Add `scripts/check-pr-status.sh` that:
- Polls all open PRs in **worldarchitect.ai** and **jleechanclaw** every 5 minutes
- Posts `@coderabbitai all good?` on each PR (throttled 1/hr to avoid spam)
- `PING_CODERABBIT=false` to skip pings; `--once` for single run

## Export integration
- Added to `mcp_scripts` in `.claude/commands/localexportcommands.md` and `.gemini/commands/localexportcommands.toml`
- Added to `mcp_helper_scripts` and `legacy_candidates` in `.claude/commands/exportcommands.py`
- `/exportcommands` and `/localexportcommands` will export this script to `~/.claude/scripts/`

## Usage
```bash
# Run once
./scripts/check-pr-status.sh --once

# Run loop (logs to /tmp/check-pr-status.log)
./scripts/check-pr-status.sh

# Disable CodeRabbit pings
PING_CODERABBIT=false ./scripts/check-pr-status.sh --once
```

Made with [Cursor](https://cursor.com)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Adds a new script that repeatedly queries PR status and can auto-comment on PRs, plus extends PR automation CLI paths; misconfiguration could spam comments or change automation behavior, but changes are isolated to tooling/scripts.
> 
> **Overview**
> Adds `scripts/check-pr-status.sh`, a polling loop that prints PR metadata, CI check status, and recent comments for configured PRs (or all open PRs), and can optionally post a throttled `@coderabbitai all good?` comment.
> 
> Extends the orchestrated PR runner to support **single-PR dispatch** via `--pr`, configurable `--agent-cli`, and an optional `--check-pr` mode that dispatches a lighter “address serious review comments” task instead of the full `/fixpr` batch.
> 
> Updates the Claude/Gemini export workflows to include `check-pr-status.sh` in MCP/script export lists so it gets installed into `~/.claude/scripts/`.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 5b79027efb6f2901327f2981b69b77fb198e0a5a. This will update automatically on new commits. Configur
