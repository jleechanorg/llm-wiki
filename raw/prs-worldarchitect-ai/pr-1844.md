# PR #1844: Add Codex MCP launcher with legacy bash support

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2025-10-08
**Author:** jleechan2015
**Stats:** +101/-33 in 4 files

## Summary
- add a repo-root Codex MCP launcher that reuses the shared installer and re-execs under Homebrew Bash when the system shell is too old
- make the shared mcp_common helpers portable by avoiding bash-4-only syntax and skipping --scope for the Codex CLI
- ensure test mode returns immediately so Codex smoke checks can run without provisioning every server

## Raw Body
## Summary
- add a repo-root Codex MCP launcher that reuses the shared installer and re-execs under Homebrew Bash when the system shell is too old
- make the shared mcp_common helpers portable by avoiding bash-4-only syntax and skipping --scope for the Codex CLI
- ensure test mode returns immediately so Codex smoke checks can run without provisioning every server

## Testing
- ./codex_mcp.sh --test

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Adds a new `codex_mcp.sh` launcher and updates shared logic to re-exec under Bash 4, conditionally pass scope flags, and support fast test-mode; updates callers and adds smoke tests.
> 
> - **Scripts**:
>   - **New**: `codex_mcp.sh` launcher that uses shared `scripts/mcp_common.sh`.
>   - **Claude Launcher**: Simplified to delegate to `mcp_common.sh` with `SCRIPT_DIR`/`MCP_LAUNCHER_PATH` and forward args.
> - **Shared Installer (`scripts/mcp_common.sh`)**:
>   - Add Bash 4 re-exec support via Homebrew paths; derive `MCP_LAUNCHER_PATH` if unset.
>   - Portable uppercasing (replace `^^` with `tr`).
>   - Introduce `MCP_SCOPE_ARGS` (empty for `codex`) and use it instead of hardcoded `--scope` in all `mcp add`/`add-json` sites.
>   - Parse `--test` from args; ensure test mode exits early safely (including return-after-safe_exit).
> - **Callsite Updates**:
>   - Replace direct `--scope "$MCP_SCOPE"` with `${MCP_SCOPE_ARGS[@]}` across add commands (e.g., `grok-mcp`, `render`, `react-mcp`, `ios-simulator-mcp`, `github-server`, `memory-server`, `perplexity-ask`, `filesystem`, `worldarchitect`, `serena`).
> - **Tests**:
>   - Add `scripts/tests/test_mcp_launchers.py` with smoke tests for Codex/Claude launchers and logging.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 7f059436b7c8b8c751abedfa4f5e09b61b17d76e. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes
