# PR #2109: Align service validation and sync test coverage

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2025-11-27
**Author:** jleechan2015
**Stats:** +3980/-1499 in 57 files
**Labels:** codex

## Summary
- align WorldAI create/list validation with shared error mapping and add pagination guards
- simplify service constructors to only accept injected world_logic and make optional field handling consistent
- convert service tests to synchronous calls matching API behavior and clean up dependency injection cases

## Raw Body
## Summary
- align WorldAI create/list validation with shared error mapping and add pagination guards
- simplify service constructors to only accept injected world_logic and make optional field handling consistent
- convert service tests to synchronous calls matching API behavior and clean up dependency injection cases

## Testing
- pytest mvp_site/tests/test_worldai_service.py mvp_site/tests/test_user_service.py

------
[Codex Task](https://chatgpt.com/codex/tasks/task_e_69255fe161cc832f8fdd55546b845dc1)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Implements parallel-safe request handling and model/settings updates, adds workflow export and auth tooling, and introduces an orchestrated PR automation runner with tests and docs.
> 
> - **Backend/API (Flask)**:
>   - Parallel-safe Firestore access via shared event loop + ThreadPoolExecutor (`mvp_site/main.py`), with new `run_blocking_io` and graceful shutdown.
>   - Rate limits configurable via env (`CAMPAIGN_*`, `SETTINGS_RATE_LIMIT`).
>   - Default Gemini model set to `gemini-3-pro-preview`; legacy 2.5 models auto-mapped.
>   - Response/model defaults updated across `gemini_response.py`, `world_logic.py`, and tests.
> - **Settings/UI**:
>   - Replace 2.5 models with `gemini-3-pro-preview` and `gemini-2.0-flash` in V1/V2 UIs and templates.
> - **Export tooling**:
>   - Export GitHub Actions workflows as examples, with content filtering and README (`exportcommands.py`, `export_config.py`).
>   - Fix script `REPO_ROOT` path on export; update exclusions (no longer exclude `automation/`).
> - **Automation**:
>   - New orchestrated PR runner with worktrees and TaskDispatcher (`automation/orchestrated_pr_runner.py`), cron entries, and integration into monitor (`--fixpr` mode, skip drafts).
>   - Version bump (`automation/pyproject.toml`).
> - **Auth tooling**:
>   - Configurable `auth-cli.mjs` and wrappers for WorldAI/AI Universe (`.claude/scripts/*`).
> - **Testing/Validation**:
>   - Rigorous concurrency test (`scripts/test
