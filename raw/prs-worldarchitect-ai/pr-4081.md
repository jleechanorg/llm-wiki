# PR #4081: fix: follow-ups for PR 4080 comments

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-01-26
**Author:** jleechan2015
**Stats:** +64/-8 in 4 files

## Summary
- Add warning logs when Firestore count aggregation falls back to stream counting
- Guard update_campaign_title to avoid creating orphan campaign docs
- Add mock-mode tests for update_campaign_title behavior

## Raw Body
## Summary
- Add warning logs when Firestore count aggregation falls back to stream counting
- Guard update_campaign_title to avoid creating orphan campaign docs
- Add mock-mode tests for update_campaign_title behavior

## Testing
- pytest mvp_site/tests/test_firestore_mock_mode_persistence.py

## Context
Follow-up to PR #4080 to address CodeRabbit review comments and add tests.

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> - **Firestore robustness/logging**: Add warnings when `count()` aggregation fails and falls back to streaming in `get_story_count` and Gemini count in `get_story_paginated`.
> - **Data integrity**: `update_campaign_title` now checks campaign existence and returns `False` instead of creating orphan docs; covered by new mock-mode tests.
> - **Tests**: Add tests validating title update behavior in mock Firestore singleton.
> - **Infra/Perf**: Dockerfile pre-downloads `fastembed` model (`BAAI/bge-small-en-v1.5`) to avoid runtime downloads; Gunicorn config enables `preload_app` and logs it for shared memory savings.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 45a85eb6eb0b29855f52881ee4f92ee97158484e. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->
## Summary by CodeRabbit

* **Bug Fixes**
  * Improved error handling for count and pagination operations with fallback streaming and enhanced logging.
  * Added validation to prevent updates to non-existent campaigns.

* **Chores**
  * Docker build now pre-downloads the embedding model to avoid runtime downloads and rate limits.
  * Server config updated to enable app preloading for memory efficiency.

* **Tests**
  * Added tests covering campaign title update behavior for missing and existing campaigns.

<sub>✏️ Tip: You can customize this high-level summary in your review settings.</sub>
<!-- end of aut
