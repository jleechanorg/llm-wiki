# PR #2327: Fix timeline log budget bug causing ContextTooLargeError

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2025-12-09
**Author:** jleechan2015
**Stats:** +731/-207 in 12 files

## Summary
- Fix timeline log budget calculation that was causing `ContextTooLargeError` in production
- The scaffold calculation did not account for timeline_log duplicating story content (~2x tokens)
- Add `TIMELINE_LOG_DUPLICATION_FACTOR = 2.05` to correctly budget for timeline log
- Delete obsolete model cycling task file and update docs with "No Model Switching" policy

## Test Plan
- [x] `test_timeline_log_budget_end2end.py` - 3 tests pass
- [x] `test_context_budgeting.py` - 2 tests pass
- [x] `test_adaptive_truncation.py` - 26 tests pass
- [x] `test_context_truncation.py` - 3 tests pass
- [ ] CI pipeline passes
- [ ] Deploy to dev and verify production game works

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Refactors story continuation to use trimmed, tiered entity tracking and lean payloads (no `npc_data`, esse

## Raw Body
## Summary

- Fix timeline log budget calculation that was causing `ContextTooLargeError` in production
- The scaffold calculation did not account for timeline_log duplicating story content (~2x tokens)
- Add `TIMELINE_LOG_DUPLICATION_FACTOR = 2.05` to correctly budget for timeline log
- Delete obsolete model cycling task file and update docs with "No Model Switching" policy

## Production Evidence

From the production game at `http://mvp-site-app-dev-i6xf2p72ka-uc.a.run.app`:
- Story context: 26,795 tokens
- Timeline log: 27,817 tokens
- Total: 54,612 tokens (nearly 2x the expected budget)

## Fix Applied

```python
# llm_service.py:3193-3201
TIMELINE_LOG_DUPLICATION_FACTOR = 2.05
available_story_tokens = int(available_story_tokens_raw / TIMELINE_LOG_DUPLICATION_FACTOR)
```

## Test plan

- [x] `test_timeline_log_budget_end2end.py` - 3 tests pass
- [x] `test_context_budgeting.py` - 2 tests pass
- [x] `test_adaptive_truncation.py` - 26 tests pass
- [x] `test_context_truncation.py` - 3 tests pass
- [ ] CI pipeline passes
- [ ] Deploy to dev and verify production game works

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Refactors story continuation to use trimmed, tiered entity tracking and lean payloads (no `npc_data`, essential `story_history`), clarifies that `timeline_log` isn’t serialized, and adds tests/docs around timeline-log budgeting and no model switching.
> 
> - **Backend (LLM context budgeting)**:
>   - **Entity tracking**: Add LRU-style tiering (`ACTIVE`/`PRESENT`/`DORMANT`) with trimmed fields via `_extract_recently_mentioned_entities`, `_tier_entities`, `_trim_entity_fields`, `_build_trimmed_entity_tracking`.
>   - **Lean payloads**: Exclude `npc_data` from `game_state` in `LLMRequest`; strip `story_history` to essential fields; add detailed size logging (`ENTITY_TRACKING_SIZE`, `GAME_STATE_BREAKDOWN`, `LLMREQUEST_PAYLOAD`).
>   - **Timeline log**: Keep `timeline_log_string` for diagnostics onl
