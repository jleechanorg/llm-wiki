# PR #5533: Fix final streaming UI scroll jump after completion

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-02-16
**Author:** jleechan2015
**Stats:** +2400/-101 in 20 files

## Summary
**Primary fix:** God Mode streaming completion now renders correctly.

On `origin/main`, God Mode streaming can finish with an empty `full_narrative`, so the frontend completes the stream and renders blank text even when `structured_response.god_mode_response` is populated.

This PR fixes that by:
- Emitting `done.display_text` server-side (falls back to `structured_response.god_mode_response` when narrative is empty)
- Teaching the streaming client/frontend completion path to prefer `display_te

## Raw Body
## Summary
**Primary fix:** God Mode streaming completion now renders correctly.

On `origin/main`, God Mode streaming can finish with an empty `full_narrative`, so the frontend completes the stream and renders blank text even when `structured_response.god_mode_response` is populated.

This PR fixes that by:
- Emitting `done.display_text` server-side (falls back to `structured_response.god_mode_response` when narrative is empty)
- Teaching the streaming client/frontend completion path to prefer `display_text` and fall back to `god_mode_response`

Secondary goals: reduce end-of-stream scroll jump by keeping the streaming DOM stable through completion; enrich streaming with early metadata/status signals.

CI is green, but there is a known functional gap: **scroll position still resets on campaign load** (user report; see Known Issues).

## Non-test Changes (Product/Runtime)
- `mvp_site/frontend_v1/app.js`
  - Reworked the streaming entry DOM to a stable layout (`.streaming-layout`) with explicit pre/tool-results/narrative/post containers.
  - Tool result rendering now appends into a dedicated container to reduce DOM churn.
  - Added streaming `metadata` and `status` handlers to render session header/resources/location signals during streaming.
  - Streaming completion now finalizes *inline* (fills pre/post structured sections, sets narrative text, clears cursor/markers) instead of re-rendering via `renderStoryEntryElement(...)`.
  - Streaming completion rendering now prefers `payload.display_text` and falls back to `structured_response.god_mode_response` if needed.
  - Adds `data-entry-index` stamp on the streaming element to support deterministic test correlation.
- `mvp_site/frontend_v1/js/streaming.js`
  - Added client handling for `status` and `metadata` stream events.
  - Ensures `done` emits a `display_text` (prefers server-provided `display_text`, falls back to accumulated chunks).
  - Expanded narrative extraction heuristics to include `display_text` and `god_
