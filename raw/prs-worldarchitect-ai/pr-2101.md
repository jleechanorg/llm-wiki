# PR #2101: Harden test bypass safety and async loop handling

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2025-11-24
**Author:** jleechan2015
**Stats:** +62/-34 in 3 files
**Labels:** codex

## Summary
- Gate test auth bypass behind explicit environment flags, log usage, and narrow CORS exposure to test-only headers
- Replace per-request event-loop creation with a shared background loop and rate-limit using ProxyFix-sanitized client IPs
- Tighten local tooling by matching exact backend process patterns, validating local Firebase creds, and cleaning import validator allowlists

## Raw Body
## Summary
- Gate test auth bypass behind explicit environment flags, log usage, and narrow CORS exposure to test-only headers
- Replace per-request event-loop creation with a shared background loop and rate-limit using ProxyFix-sanitized client IPs
- Tighten local tooling by matching exact backend process patterns, validating local Firebase creds, and cleaning import validator allowlists

## Testing
- ⚠️ `python -m pytest mvp_site/tests/test_god_mode_validation_recovery.py` *(fails in this environment because the path was not found; repo layout differs from expectation)*

------
[Codex Task](https://chatgpt.com/codex/tasks/task_e_6923abd98f90832f84e7bc3ada5ce1bf)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Harden auth and CORS for testing, switch Flask async handling to a shared background loop, and tighten local tooling and import validation.
> 
> - **Backend (Flask API)**
>   - Add a dedicated background asyncio event loop (threaded) and route async handlers via `run_in_background_loop()`; remove per-request loop handling.
>   - Harden auth: gate test bypass behind `TESTING` and `ALLOW_TEST_AUTH_BYPASS`, log activations, and restrict CORS test headers to testing mode.
>   - Use `get_remote_address()` for rate-limiting keys; keep `ProxyFix`.
> - **Tooling**
>   - `run_local_server.sh`: tighten backend process matching to `python -m mvp_site.main serve`; validate and export Firebase creds with helpful messages; update watchdog checks accordingly.
> - **Validation**
>   - `scripts/validate_imports.py`: narrow allowed conditional imports (remove broad `google.auth`, keep `google.auth._helpers`) and streamline allowlists/exemptions.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit c189e7846fb4e1863b22d855a284b8f9c15c88f0. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
