# PR #5225: Fix char creation and add level up agent

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-02-11
**Author:** jleechan2015
**Stats:** +2809/-878 in 21 files
**Labels:** codex

## Summary
Fixes five critical issues in level-up and character creation modal flows:
1. Test incorrectly checking for CharacterCreationAgent instead of LevelUpAgent
2. Server-side finish choice injection being overwritten by raw LLM response
3. **REV-vew8 (P1)**: Level-up finish choice routing to wrong completion flags
4. **REV-2bct (P1)**: Modal lock enforcement skipping active level-up
5. **REV-yfb4 (P2)**: Finish choice deduplication warning

**Key themes:**
- Test accuracy and agent detection
- Server

## Raw Body
## Summary
Fixes five critical issues in level-up and character creation modal flows:
1. Test incorrectly checking for CharacterCreationAgent instead of LevelUpAgent
2. Server-side finish choice injection being overwritten by raw LLM response
3. **REV-vew8 (P1)**: Level-up finish choice routing to wrong completion flags
4. **REV-2bct (P1)**: Modal lock enforcement skipping active level-up
5. **REV-yfb4 (P2)**: Finish choice deduplication warning

**Key themes:**
- Test accuracy and agent detection
- Server-side injection preservation
- Modal lock enforcement robustness
- Finish choice routing correctness

## Production Code Changes

### 1. `mvp_site/world_logic.py` - Initial Fix (+4/-1 lines)
**Purpose:** Core game state processing and response building

- **Use structured_fields planning_block instead of structured_response** - Line ~5354 copied raw LLM response planning_block → Now uses structured_fields["planning_block"] with server injections → Preserves server-side finish choice injection and reordering

**Root Cause:** The injection function correctly positioned the finish choice last and stored it in structured_fields["planning_block"], but then line 5354 copied the raw LLM response (which did not have the injected choice) into unified_response["planning_block"], overwriting the correctly-ordered version.

### 2. `mvp_site/world_logic.py` - Beads Follow-Up Fixes (+10/-4 lines)
**Purpose:** Fix finish choice routing and modal lock enforcement issues identified by creation2

- **REV-vew8: Fix level-up finish choice routing (Line 1400)** - Only checked for `complete_levelup` → Now checks for both `complete_levelup` and `finish_level_up_return_to_game` → Prevents canonical finish choice from incorrectly routing to character creation completion flags

- **REV-2bct: Fix modal lock stage validation (Line 1352)** - Returned early if `stage not in valid_stages` → Now also checks `level_up_in_progress` before returning → Prevents modal lock from being bypassed when `ch
