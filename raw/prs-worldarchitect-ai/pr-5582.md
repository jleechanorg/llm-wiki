# PR #5582: Revert "Revert "Allow configurable OpenClaw gateway port in Settings and use it at runtime""

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-02-20
**Author:** jleechan2015
**Stats:** +6109/-747 in 54 files

## Summary
Adds **OpenClaw** as a new selectable LLM provider with configurable localhost gateway port (default `18789`). Routes inference through local OpenClaw gateway while preserving existing GCP deployment path.

- **What**: New OpenClaw provider for LLM inference via local gateway
- **Why**: Enable local development with OpenClaw while maintaining production Gemini path
- **Risk**: Medium - touches core LLM dispatch and streaming paths

## Test Plan
```bash
# Run MCP tests
python3 testing_mcp/test_openclaw_settings_e2e.py
python3 testing_mcp/test_openclaw_llm_repro.py

# Run browser tests
python3 testing_ui/test_openclaw_settings_browser.py
python3 testing_ui/test_openclaw_e2e.py
```

## Raw Body
# OpenClaw Gateway Communication - PR Description

## Summary

Adds **OpenClaw** as a new selectable LLM provider with configurable localhost gateway port (default `18789`). Routes inference through local OpenClaw gateway while preserving existing GCP deployment path.

- **What**: New OpenClaw provider for LLM inference via local gateway
- **Why**: Enable local development with OpenClaw while maintaining production Gemini path
- **Risk**: Medium - touches core LLM dispatch and streaming paths

## Production Code Changes

### New Files Added

| File | Justification |
|------|---------------|
| `mvp_site/llm_providers/openclaw_provider.py` | Core provider module implementing HTTP client for OpenClaw gateway `/v1/chat/completions` with streaming + non-streaming support. Follows existing provider pattern (Gemini/Cerebras/OpenRouter). |
| `mvp_site/tests/test_openclaw_provider.py` | Unit tests for provider functions (import, model validation, gateway URL resolution). |
| `mvp_site/tests/test_openclaw_settings_validation.py` | Unit tests for new settings validators (`validate_openclaw_model`, `validate_openclaw_gateway_port`). |
| `docs/openclaw_gateway_design.md` | Design doc for OpenClaw gateway integration (protocol, transport, reliability, TDD plan). |

### Modified Files

| File | Before | After | Why |
|------|--------|-------|-----|
| `mvp_site/constants.py` | No `LLM_PROVIDER_OPENCLAW` | Added constant + rate limit | Enable provider selection |
| `mvp_site/llm_service.py` | No OpenClaw dispatch | Added `case constants.LLM_PROVIDER_OPENCLAW` | Route calls to new provider |
| `mvp_site/settings_validation.py` | No Openclaw validators | Added `validate_openclaw_model`, `validate_openclaw_gateway_port` | Validate new settings |
| `mvp_site/main.py` | No OpenClaw route | Added `/v1/providers/openclaw/*` routes | Expose provider info API |
| `mvp_site/templates/settings.html` | No OpenClaw UI | Added "OpenClaw Gateway (local)" section | User-facing settings |
| `mvp_sit
