# PR #5625: Refine V1 signed-out welcome with blended card + splash image

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-02-22
**Author:** jleechan2015
**Stats:** +356/-9 in 4 files
**Labels:** codex

## Summary
(none)

## Raw Body
### Motivation
- Prevent the redesigned V1 welcome/SSO UI from appearing inside signed-in/dashboard contexts and present it explicitly as a signed-out experience.  
- Match the visual direction requested (blend of the previous concept and the provided reference) with clearer hierarchy, spacing, and a cinematic splash area.

### Description
- Added `getLoggedOutAuthMarkup()` to `mvp_site/frontend_v1/auth.js` with updated copy (`Welcome to WorldAI`, `Continue your adventure`) and preserved the `id="signInBtn"` wiring.  
- Updated the Firebase auth state handler to toggle `body` classes (`is-authenticated` / `is-logged-out`) and to render the logged-out markup into `#auth-container` when no user is present.  
- Restyled the signed-out view in `mvp_site/frontend_v1/style.css` to provide a centered elevated auth panel, purple→navy background, left-aligned SSO card, full-width CTA styling, splash image overlay, and a fallback image path for static previews.  
- Added a standalone mock landing page `mvp_site/frontend_v2/public/landing_sso_mock.html` to preview the blended visual treatment in isolation.

### Testing
- Ran a static server and validated the page loads: `python3 -m http.server 4183 --directory mvp_site` (served the modified assets successfully).  
- Captured a Playwright screenshot of `http://127.0.0.1:4183/frontend_v1/index.html` to verify the blended visual direction and CTA wiring; the screenshot artifact was produced successfully.  
- Verified splash image fallback path works in the static preview (server log shows `/frontend_v2/public/image_reference/twin_dragon.png` loaded), and observed expected 404s for runtime API endpoints in the static-server environment (not a regression).

=== Git Status ===
On branch work
nothing to commit, working tree clean

[Dir: worldarchitect.ai | Local: work (no remote) | Remote: no upstream | PR: none]

------
[Codex Task](https://chatgpt.com/codex/tasks/task_e_69965dc52aec832f8491a4fb7c061d5b)

<!-- CURSOR_SUMMARY -->
---
