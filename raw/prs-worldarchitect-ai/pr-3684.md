# PR #3684: Fix: Extract dice_rolls from action_resolution.mechanics.rolls for Firestore

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-01-17
**Author:** jleechan2015
**Stats:** +345/-36 in 5 files

## Summary
- **Bug**: When LLM correctly places dice in `action_resolution.mechanics.rolls` (as instructed in `game_state_instruction.md:236`), the `dice_rolls` field remained empty `[]` in Firestore because `extract_structured_fields()` only read `dice_rolls` directly from the LLM response.
- **Fix**: After extracting `action_resolution`, now call `extract_dice_rolls_from_action_resolution()` to populate `dice_rolls` from `action_resolution.mechanics.rolls` (single source of truth).
- **Tests**: Added 3 r

## Test Plan
- [x] Created failing test reproducing the bug (RED)
- [x] Implemented minimal fix (CODE)
- [x] All 13 structured_fields_utils tests pass (GREEN)
- [x] All 19 action_resolution_utils tests pass
- [x] All 6 Firestore structured fields tests pass
- [x] Flakiness check: 3 consecutive runs pass

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Aligns dice handling to a single source of truth and fixes empty `dice_rolls` in storage.
> 
> - Updat

## Raw Body
## Summary

- **Bug**: When LLM correctly places dice in `action_resolution.mechanics.rolls` (as instructed in `game_state_instruction.md:236`), the `dice_rolls` field remained empty `[]` in Firestore because `extract_structured_fields()` only read `dice_rolls` directly from the LLM response.
- **Fix**: After extracting `action_resolution`, now call `extract_dice_rolls_from_action_resolution()` to populate `dice_rolls` from `action_resolution.mechanics.rolls` (single source of truth).
- **Tests**: Added 3 regression tests to verify extraction works correctly.

## Evidence from Investigation

Firestore data showed:
- `dice_rolls: []` (empty)
- `action_resolution.mechanics.rolls: [{'notation': '1d20+19', 'result': 32, 'dc': 15, 'success': True}]` (actual dice data)

The extraction function `extract_dice_rolls_from_action_resolution()` existed and worked correctly, but was not being called in `extract_structured_fields()`.

## Test plan

- [x] Created failing test reproducing the bug (RED)
- [x] Implemented minimal fix (CODE)
- [x] All 13 structured_fields_utils tests pass (GREEN)
- [x] All 19 action_resolution_utils tests pass
- [x] All 6 Firestore structured fields tests pass
- [x] Flakiness check: 3 consecutive runs pass

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Aligns dice handling to a single source of truth and fixes empty `dice_rolls` in storage.
> 
> - Updates `structured_fields_utils.extract_structured_fields` to always include `action_resolution`/`outcome_resolution`, extract `dice_rolls` from `action_resolution.mechanics.rolls`, and canonicalize legacy Think Mode `dice_rolls` dicts into `planning_check` rolls
> - Refreshes prompts (`game_state_examples.md`, `game_state_instruction.md`, `think_mode_instruction.md`) to direct dice into `action_resolution.mechanics.rolls` and note backend auto-populates `dice_rolls`; example blocks use `json`
> - Adds regression/unit tests ensuring extraction works
