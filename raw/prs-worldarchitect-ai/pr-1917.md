# PR #1917: fix: update MCP installer argument handling and Chrome stats

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2025-10-30
**Author:** jleechan2015
**Stats:** +53/-9 in 2 files
**Labels:** codex

## Summary
- normalize installer arguments so `--dry-run` still maps to `--test` while preserving product selection and pass-through flags
- restore Grok defaults and forward CLI arguments when invoking the shared installer for Claude, Codex, or both
- track Chrome Superpowers installation outcomes in the aggregated MCP statistics

## Raw Body
## Summary
- normalize installer arguments so `--dry-run` still maps to `--test` while preserving product selection and pass-through flags
- restore Grok defaults and forward CLI arguments when invoking the shared installer for Claude, Codex, or both
- track Chrome Superpowers installation outcomes in the aggregated MCP statistics

## Testing
- `bash -n scripts/install_mcp_servers.sh`
- `bash -n scripts/mcp_common.sh`


------
https://chatgpt.com/codex/tasks/task_e_6902fcb15244832f9fed495b83d606a9

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Normalizes argument handling, forwards flags to shared/common installs, sets Grok default model envs, and records Chrome Superpowers install outcomes in stats.
> 
> - **Installer (`scripts/install_mcp_servers.sh`)**:
>   - Normalize args: map `--dry-run` to `--test` while preserving all other flags; parse target (`claude|codex|both`) separately with default `claude`.
>   - Forward passthrough args to `mcp_common.sh` and when invoking sub-installs for `both`.
>   - Codex: load env vars and set `GROK_DEFAULT_MODEL="grok-3"` and `XAI_DEFAULT_CHAT_MODEL` fallback.
> - **Shared logic (`scripts/mcp_common.sh`)**:
>   - Chrome Superpowers: update aggregated stats (`TOTAL`, `SUCCESS`/`FAILURE`, `ALREADY_EXISTS`) and add exist log during install.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 9364a6fbe8e199245372152652f09c0f798ab6c0. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
