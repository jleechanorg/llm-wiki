# PR #5873: fix(statusline): two-line status with context bar and clickable PR URL

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-03-08
**Author:** jleechan2015
**Stats:** +34/-19 in 2 files

## Summary
- Line 1: git info \`[Dir | Local | Remote | PR:#N]\` truncated to terminal width to prevent wrap
- Line 2: always-shown context progress bar (\`ctx ██████░░░░ 60%\`) with green/yellow/orange/red color coding
- Line 3: plain PR URL for native terminal hyperlink detection (OSC 8 stripped by statusline renderer)
- Simplify \`statusLine.command\` to \`exec\` directly (removes fragile python3 stdin buffering)

## Raw Body
## Summary
- Line 1: git info \`[Dir | Local | Remote | PR:#N]\` truncated to terminal width to prevent wrap
- Line 2: always-shown context progress bar (\`ctx ██████░░░░ 60%\`) with green/yellow/orange/red color coding
- Line 3: plain PR URL for native terminal hyperlink detection (OSC 8 stripped by statusline renderer)
- Simplify \`statusLine.command\` to \`exec\` directly (removes fragile python3 stdin buffering)

## Production Code Changes
- \`.claude/hooks/git-header.sh\`: Two-line output (git info + ctx bar); third line adds PR URL when available
- \`.claude/settings.json\`: Simplified \`statusLine.command\` from complex python3 stdin read to direct \`exec\`

## Test Changes
None — visual status line change, verified manually.

## Known Limitations
- OSC 8 hyperlink sequences are stripped by the statusline renderer; plain URL relies on terminal native URL detection (iTerm2/Terminal.app Cmd+click)

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Only affects developer tooling output formatting and how the statusline hook is invoked; no production logic or data handling changes.
> 
> **Overview**
> Improves the Claude Code status line by printing **a truncated, width-aware git info line**, followed by an **always-visible context usage progress bar** with color thresholds.
> 
> Adds a third line that outputs the plain PR URL (when present) so terminals can auto-detect it, and simplifies `statusLine.command` in `.claude/settings.json` to `exec` the repo/home `git-header.sh` directly (removing the prior stdin buffering/fallback logic).
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 7a69dff57e020414c0eead35fdffff21f1928ea8. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- COPILOT_TRACKING_START -->
## Copilot Review Tracking

| Comment | Author | Severity | Stat
