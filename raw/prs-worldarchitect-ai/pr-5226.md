# PR #5226: Fix character creation schema drift + starter gear visibility + state_updates audit

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-02-10
**Author:** jleechan2015
**Stats:** +1349/-3274 in 20 files

## Summary
(none)

## Raw Body
Fixes beads:
- W-1at CharacterCreationAgent prompt stack omits game_state_instruction.md
- W-13v Starter gear seeded into inventory disappears once equipment slots exist
- W-339 Persist structured state updates for postmortems/debugging

Changes
- CharacterCreationAgent now includes `game_state_instruction.md` in its prompt stack (via REQUIRED_PROMPT_ORDER + agent_prompts builder).
- Campaign creation seeds starter items into `player_character_data.equipment.backpack` (canonical) instead of `inventory` list.
- equipment_display.extract_equipment_display now:
  - treats inventory list-only schema as backpack
  - merges top-level inventory list into equipment.backpack when both exist
- structured_fields_utils.extract_structured_fields now persists `state_updates_audit` (safe subset: player_character_data/custom_campaign_state/item_registry/etc) while keeping existing living-world filtered `state_updates` behavior.

Tests
- python -m unittest -q mvp_site.tests.test_equipment_display
- python -m unittest -q mvp_site.tests.test_structured_fields_utils
- python -m unittest -q mvp_site.tests.test_agents.TestCharacterCreationAgent.test_character_creation_agent_required_prompts
- python -m unittest -q mvp_site.tests.test_world_logic.TestCampaignStartingItems.test_create_campaign_god_mode_starter_gear_uses_equipment_backpack

Notes
- Updated end2end CharacterCreationAgent turn1 test to assert game_state_instruction is present in debug_info.system_instruction_files.
EOF

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Touches campaign initialization, equipment parsing/merging, and persisted structured-response fields; schema/compat logic changes could affect stored game state and UI displays if edge cases were missed.
> 
> **Overview**
> **Fixes character-creation schema drift and starter-gear visibility across mixed game-state shapes.** Character creation prompts now include `game_state_instruction.md` early (via `PromptBuilder` and `CharacterCreationAgent` require
