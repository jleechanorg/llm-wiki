# PR #5752: Pair-v2 custom args + SDUI blog benchmark task (TDD, dispatcher + bench harness)

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-02-24
**Author:** jleechan2015
**Stats:** +1799/-39 in 14 files

## Summary
This PR updates `pair_v2`/benchmark infrastructure and benchmark task artifacts with full visibility into runtime argument propagation and a new SDUI blog TDD scenario.

## Raw Body
## Summary

This PR updates `pair_v2`/benchmark infrastructure and benchmark task artifacts with full visibility into runtime argument propagation and a new SDUI blog TDD scenario.

## Changes vs `origin/main`

### 1) `/pairv2` + benchmark custom arg plumbing
- Added parser support for `--coder-extra-arg` and `--verifier-extra-arg` in `pair_execute_v2.py`.
- Threaded extra args into execution state and launch flow so live coder/verifier invocations receive them consistently.
- Added `pairv2` runner flag pass-through in `benchmark_pair_executors.py` for:
  - `run_pairv2_live_once`
  - `benchmark` and `benchmark_all` flows
  - fan-out benchmark flow
  - CLI parsing and execution wiring in `main()`

### 2) Orchestration CLI argument forwarding
- Updated `orchestration/task_dispatcher.py` to append agent-spec `cli_args` to every CLI attempt command.
- Added unit coverage validating appended attempt commands in `orchestration/tests/test_cli_support.py`.

### 3) Pair-v2 test coverage for custom args and validator behavior
- Added parse and propagation tests in `test_pair_v2.py`.
- Added benchmark parse/runner command argument coverage in `test_pair_v2_and_benchmark.py`.
- Captured end-to-end behaviors around `--validate-contracts-only` and extra-arg passthrough.

### 4) SDUI blog benchmark benchmark-task specification
- Added `testing_llm/pair/sdui_blog_tdd.md` as the SDUI blog benchmark implementation plan.
- Added benchmark task entry in `testing_llm/pair/benchmark_tasks.json` (`task_6_atlas_mall_pairv2_tdd` entry set).
- Added supporting evidence/benchmark bookkeeping in `.beads/beads.left.jsonl`.

## Testing
- Ran focused suite:
  - `./vpython -m pytest -q .claude/pair/tests/test_pair_v2.py .claude/pair/tests/test_pair_v2_and_benchmark.py`
  - `./vpython -m pytest -q orchestration/tests/test_cli_support.py -k "includes_cli_args" -vv`
- Ran a small smoke execution via validate-only /pairv2 with extra args via `/Users/jleechan/Downloads/pairv2/small_pairv2_smoke.py`.

#
