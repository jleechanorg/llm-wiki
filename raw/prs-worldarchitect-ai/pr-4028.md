# PR #4028: fix: God mode empty narrative with centralized is_god_mode() detection

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-01-28
**Author:** jleechan2015
**Stats:** +1149/-975 in 12 files

## Summary
God mode responses now keep `narrative` empty and the frontend reads `god_mode_response` directly. Empty-narrative handling in `world_logic.py` uses centralized god mode detection so case-insensitive modes and the `GOD MODE:` prefix are handled consistently.

### Key Changes

1. `mvp_site/narrative_response_schema.py`: `_combine_god_mode_and_narrative()` no longer copies `god_mode_response` into `narrative`.
2. `mvp_site/world_logic.py`: `_resolve_empty_narrative()` uses `constants.is_god_mode(u

## Raw Body
## Summary

God mode responses now keep `narrative` empty and the frontend reads `god_mode_response` directly. Empty-narrative handling in `world_logic.py` uses centralized god mode detection so case-insensitive modes and the `GOD MODE:` prefix are handled consistently.

### Key Changes

1. `mvp_site/narrative_response_schema.py`: `_combine_god_mode_and_narrative()` no longer copies `god_mode_response` into `narrative`.
2. `mvp_site/world_logic.py`: `_resolve_empty_narrative()` uses `constants.is_god_mode(user_input, mode)` and is wired into `process_action_unified`.
3. Tests updated to expect empty narrative in god mode (unit + MCP end-to-end).
4. `testing_mcp/` tests refactored to `MCPTestBase` patterns.
5. `.claude/commands/copilot.md` updated with comment refresh/outdated detection and routine coverage gating.

### Test Evidence

- `testing_mcp/test_god_mode_combined_narrative_real_api.py`: 3/3 passed
- `mvp_site/tests/test_god_mode_response_field.py`: 13/13 passed

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->

## Summary by CodeRabbit

## Release Notes

* **Bug Fixes**
  * Fixed god mode response handling to prevent narrative duplication; responses now properly isolated in dedicated field for frontend consumption.
  * Improved empty narrative fallback and error handling across scenarios.

* **Tests**
  * Significantly expanded test coverage for god mode response behavior and edge cases.
  * Refactored test structure for improved organization and maintainability.

<sub>✏️ Tip: You can customize this high-level summary in your review settings.</sub>

<!-- end of auto-generated comment: release notes by coderabbit.ai -->
