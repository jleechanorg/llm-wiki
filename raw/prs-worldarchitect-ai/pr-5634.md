# PR #5634: CI: make PR delta coverage report non-blocking

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-02-19
**Author:** jleechan2015
**Stats:** +3/-4 in 1 files

## Summary
- convert the PR delta coverage step from blocking to report-only
- keep per-file coverage analysis and warnings in job logs/summary
- preserve coverage comment generation behavior

## Raw Body
## Summary
- convert the PR delta coverage step from blocking to report-only
- keep per-file coverage analysis and warnings in job logs/summary
- preserve coverage comment generation behavior

## Why
Current PR coverage threshold failures block merges. This change keeps visibility while removing hard failure behavior.

## Changes
- `.github/workflows/test.yml`
  - rename step to `Report delta coverage (per-file, non-blocking)`
  - replace `::error:: + exit 1` with `::warning::` (no non-zero exit)

## Validation
- Workflow YAML updated and committed on branch `chore/coverage-report-only`
- Branch pushed to `origin/chore/coverage-report-only`

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Workflow-only change that relaxes a CI gate; the main risk is reduced enforcement allowing low-coverage changes to merge if reviewers don’t act on warnings.
> 
> **Overview**
> Updates the PR `coverage-report` workflow so the per-file delta coverage step is **non-blocking**: it’s renamed to `Report delta coverage (per-file, non-blocking)`, downgrades threshold failures from `::error::` to `::warning::`, and removes the `exit 1` that previously failed the job.
> 
> Coverage comments and the per-file coverage breakdown are still generated and logged, but low per-file coverage now only surfaces as warnings in the job output/summary.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 3bd7791de74086dd48e38367e099a3af6b880e77. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->

## Summary by CodeRabbit

* **Chores**
  * Delta coverage reporting is now non-blocking and will generate warnings instead of causing CI failures when coverage thresholds are breached.

<!-- end of auto-generated comment: release notes by coderabbit.ai -->
