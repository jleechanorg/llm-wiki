# PR #1805: Restore Perplexity MCP via new server

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2025-10-02
**Author:** jleechan2015
**Stats:** +180/-101 in 11 files
**Labels:** codex

## Summary
- reintroduce Perplexity coverage in `/perp`, `/presentation`, `/research`, and related orchestration docs using the new `perplexity_search_web` tool
- update `claude_mcp.sh`, token loading, and MCP search tests to install and validate the `@jschuller/perplexity-mcp` server when a Perplexity API key is present
- adjust the MCP health check to treat Perplexity as a premium server while maintaining backward compatibility with the legacy name

## Raw Body
## Summary
- reintroduce Perplexity coverage in `/perp`, `/presentation`, `/research`, and related orchestration docs using the new `perplexity_search_web` tool
- update `claude_mcp.sh`, token loading, and MCP search tests to install and validate the `@jschuller/perplexity-mcp` server when a Perplexity API key is present
- adjust the MCP health check to treat Perplexity as a premium server while maintaining backward compatibility with the legacy name

## Testing
- pytest mvp_site/tests/test_mcp_health.py -k test_mcp_config_has_all_servers *(skipped: MCP config not present in environment)*

------
https://chatgpt.com/codex/tasks/task_e_68dceaf3cdcc832f83c955d0f0d159a7

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Reintroduces Perplexity via `@jschuller/perplexity-mcp` and broadens multi-engine search (adds DuckDuckGo and Grok), updating command docs, install/token scripts, and tests with premium-server gating and legacy compatibility.
> 
> - **Search & Command Docs**:
>   - Expand multi-engine lineup to `Claude WebSearch`, `Perplexity`, `DuckDuckGo`, `Grok`, `Gemini`; update usage/flow in `/perp`, `/research`, `/presentation`, `/debug-protocol`, `/list`.
>   - Reference Perplexity `perplexity_search_web` and clarify recency/citations; update examples and synthesis sections.
>   - Normalize frontmatter `type` from `llm-orchestration` to `orchestration` in `/.claude/commands/*` where changed.
> - **MCP Install & Scripts**:
>   - `scripts/mcp_common.sh`: harden parallel maps under `set -u` (safe assigns/reads), improve parallel install/result collection; install flow acknowledges DDG in Batch 2 and premium Perplexity separately.
>   - `scripts/load_tokens.sh`: reorder args defaulting, lenient Perplexity key validation/loading, improved messages/status, updated token template and guidance.
> - **Tests & Utilities**:
>   - `mvp_site/tests/test_mcp_health.py`: treat Perplexity and Grok as premium (API-key gated); keep legacy `perplexity-ask` for backward compat; remove it from
