# PR #2031: fix: Address critical code review comments

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2025-11-16
**Author:** jleechan2015
**Stats:** +98/-93 in 2 files

## Summary
Fixes 2 critical issues identified by automated reviewers (Copilot AI and CodeRabbit) in claude-commands PR #104:

1. **Missing json import** - test_exportcommands.py uses json module but didn't import it
2. **BD_PATH non-existent path** - mcp_common.sh set non-existent path when bd CLI not found

## Raw Body
## Summary

Fixes 2 critical issues identified by automated reviewers (Copilot AI and CodeRabbit) in claude-commands PR #104:

1. **Missing json import** - test_exportcommands.py uses json module but didn't import it
2. **BD_PATH non-existent path** - mcp_common.sh set non-existent path when bd CLI not found

## Changes

### 1. Missing json import (test_exportcommands.py:7)
- **Issue**: File uses `json.dump()`, `json.load()`, `json.loads()`, `json.JSONDecodeError`
- **Fix**: Added `import json` to imports section
- **Impact**: Prevents runtime ImportError when tests execute

### 2. BD_PATH non-existent path (mcp_common.sh:2233)
- **Issue**: When bd CLI not found, still set `BD_PATH="$HOME/go/bin/bd"` (non-existent)
- **Fix**: Changed to `BD_PATH=""` to let beads-mcp use default fallback
- **Impact**: Allows MCP server to handle missing bd CLI gracefully

## Testing

- [x] Pre-commit hooks passed (trailing whitespace, end of file fixes applied)
- [x] Changes synchronized from claude-commands PR #104 (already merged)

## Related

- claude-commands PR: jleechanorg/claude-commands#104 (merged)

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Adjust Beads MCP setup to conditionally pass BEADS_PATH based on bd CLI presence and normalize whitespace in export command tests.
> 
> - **Scripts**:
>   - **`.claude/scripts/mcp_common.sh`**: Improve Beads MCP configuration
>     - Introduce `BD_FOUND` flag and detect `bd` via PATH or `$HOME/go/bin/bd`.
>     - Only add `--env BEADS_PATH=...` when `bd` is found (avoid passing empty/non-existent path).
>     - Update warning message to indicate default fallback when `bd` is missing.
> - **Tests**:
>   - **`.claude/commands/tests/test_exportcommands.py`**: Whitespace/formatting normalization (remove trailing spaces, minor spacing fixes).
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 007304e419d70e04d5424b1552a4e74d67412072. This will 
