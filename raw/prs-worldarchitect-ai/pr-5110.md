# PR #5110: Perf: make state-update logging serialization lazy

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-02-09
**Author:** jleechan2015
**Stats:** +714/-30 in 27 files

## Summary
- make hot-path state update logs lazy in `mvp_site/firestore_service.py`
- convert eager f-string logs to parameterized logging in GOD_MODE_SET flow in `mvp_site/world_logic.py`
- guard expensive `_truncate_log_json(...)` and `format_game_state_updates(...)` construction behind log-level checks

## Raw Body
## Summary
- make hot-path state update logs lazy in `mvp_site/firestore_service.py`
- convert eager f-string logs to parameterized logging in GOD_MODE_SET flow in `mvp_site/world_logic.py`
- guard expensive `_truncate_log_json(...)` and `format_game_state_updates(...)` construction behind log-level checks

## Why
This removes eager serialization and heavy string construction in logging paths when those log levels are disabled, addressing the performance regression in world logic tests.

## Validation
- `python3.11 -m pytest -q mvp_site/tests/test_world_logic.py --durations=20`
- `python3.11 -m pytest -q mvp_site/tests/test_world_logic.py`
- `python3.11 -m pytest -q mvp_site/tests/test_world_logic.py::TestMCPMigrationRedGreen::test_enhanced_logging_json_serialization_red_phase`
- `python3.11 -m py_compile mvp_site/firestore_service.py mvp_site/world_logic.py`

## Test Notes
`mvp_site/tests/test_world_logic.py` currently has 2 unrelated pre-existing failures in `TestFactionMinigameDefaults` (`'dict' object has no attribute 'narrative_text'`).

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Mostly logging/perf refactors, but it also changes campaign-creation parsing paths and Firestore state-write logging, which could affect runtime behavior and debuggability if response shapes differ.
> 
> **Overview**
> Reduces hot-path overhead by making expensive log formatting lazy: guards `_truncate_log_json(...)`/`format_game_state_updates(...)` behind level checks and converts f-string logs to parameterized logging in `update_state_with_changes`, `update_campaign_game_state`, and the `GOD_MODE_SET` flow.
> 
> Hardens campaign creation to accept dict-based `get_initial_story` responses (extracting `narrative_text`/`story_text`, `state_updates`, and structured fields without assuming an object API), and updates `TestFactionMinigameDefaults` to locate the `game_state` argument in `create_campaign` calls without relying on a fixed positional index.
> 
> Adds a `docs/ev
