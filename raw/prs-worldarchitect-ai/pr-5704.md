# PR #5704: feat: add split-pane-launch.sh script for Claude Code agent teams

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-02-22
**Author:** jleechan2015
**Stats:** +27/-2 in 2 files

## Summary
- Adds `split-pane-launch.sh` script for launching Claude Code with split-pane mode
- Script runs Claude Code in tmux with bypass permissions
- Supports model argument (default sonnet)
- Uses dynamic session names to avoid conflicts

## Test Plan
- [ ] Test `claudet` alias launches tmux session
- [ ] Verify split panes appear when using agent teams

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Introduces a script that runs `claude` with `--dangerously-skip-permissions` and spawns tmux sessions; misuse could broaden permissions or affect developer environments, though changes are isolated to tooling/tests.
> 
> **Overview**
> Adds `.claude/scripts/split-pane-lau

## Raw Body
## Summary
- Adds `split-pane-launch.sh` script for launching Claude Code with split-pane mode
- Script runs Claude Code in tmux with bypass permissions
- Supports model argument (default sonnet)
- Uses dynamic session names to avoid conflicts

## Test plan
- [ ] Test `claudet` alias launches tmux session
- [ ] Verify split panes appear when using agent teams

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Introduces a script that runs `claude` with `--dangerously-skip-permissions` and spawns tmux sessions; misuse could broaden permissions or affect developer environments, though changes are isolated to tooling/tests.
> 
> **Overview**
> Adds `.claude/scripts/split-pane-launch.sh`, a `claudet`-style helper that starts (or reuses) a tmux session and runs `claude` in `--teammate-mode tmux` with `--dangerously-skip-permissions`, defaulting the model to `sonnet` and using a unique session name.
> 
> Updates the export workflow test to stub `_create_pull_request` (which uses the `gh` CLI) so GitHub publish tests don’t require a real cloned repo and avoid false failures.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 7e15bfba614c2d573a00e81e9b5934941d7ceb92. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- COPILOT_TRACKING_START -->
## Copilot Review Tracking

| Comment ID | Reviewer | File:Line | Severity | Status | Summary |
|------------|----------|-----------|----------|--------|---------|
| 2837140975 | Copilot | split-pane-launch.sh:14 | CRITICAL | **Fixed** | Unquoted `$MODEL` in tmux command string allows command injection. Fixed via `MODEL_Q=$(printf '%q' "$MODEL")` in commit 8cc6ef3af. |
| 2837139434 | ChatGPT Codex | split-pane-launch.sh:14 | BLOCKING | **Fixed** | P2: Same unquoted `$MODEL` command injection risk. Resolved in commit 8cc6ef3af with prin
