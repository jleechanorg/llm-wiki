# PR #2048: Fix Claude Code settings validation errors

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2025-11-18
**Author:** jleechan2015
**Stats:** +1596/-7 in 7 files

## Summary
- Fixed hook matcher format in settings.json (object → string)
- Fixed agent frontmatter format in CLAUDE.md (quoted → unquoted)
- Added comprehensive settings maintenance skill guide

## Test Plan
- [x] Run /doctor to verify no validation errors
- [x] Verify hooks format is correct
- [x] Verify agent frontmatter parses successfully
- [x] Pre-commit hooks pass

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Adds official schema to settings, converts hook matchers to string format, introduces Python validators, fixes agent frontmatter, and adds docs for validation/maintenance and git branch tracking.
> 
> - **Settings**:
>   - Add `$

## Raw Body
## Summary
- Fixed hook matcher format in settings.json (object → string)
- Fixed agent frontmatter format in CLAUDE.md (quoted → unquoted)
- Added comprehensive settings maintenance skill guide

## Changes

### Settings & Agent Fixes
- **settings.json**: Convert hook matchers from object to string format
  - Changed `{"tools": ["*"]}` to `"*"` (new required format)
  - Added empty matchers `""` for UserPromptSubmit and Stop hooks
- **CLAUDE.md**: Fix agent frontmatter format
  - Removed quotes from name and description fields
  - Changed to unquoted YAML format required by Claude Code parser

### New Documentation
- **claude-code-settings-maintenance.md**: Comprehensive skill guide
  - Hook matcher format (string vs object)
  - Agent frontmatter format (unquoted values)
  - Validation protocol using /doctor
  - Web search strategy for official docs
  - Migration guide from old to new format
  - Common pitfalls and troubleshooting

### MCP Scripts
- **install_mcp_servers.sh**: Unified MCP server installer
- **mcp_common.sh**: Common MCP utilities and helpers

## Validation
- ✅ All /doctor validation errors resolved
- ✅ Hooks use correct string matcher format
- ✅ Agent frontmatter uses unquoted YAML values

## Test Plan
- [x] Run /doctor to verify no validation errors
- [x] Verify hooks format is correct
- [x] Verify agent frontmatter parses successfully
- [x] Pre-commit hooks pass

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Adds official schema to settings, converts hook matchers to string format, introduces Python validators, fixes agent frontmatter, and adds docs for validation/maintenance and git branch tracking.
> 
> - **Settings**:
>   - Add `$schema` to `.claude/settings.json` pointing to official schema.
>   - Convert hook `matcher` from object to string (e.g., `{"tools": ["*"]}` → `"*"`; `Write`, specific patterns).
>   - Use empty matchers `""` for `UserPromptSubmit` and `Stop`.
> - **Validation 
