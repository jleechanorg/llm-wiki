# PR #5685: beads: add LLM fingerprint comparison tracking bead

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-02-21
**Author:** jleechan2015
**Stats:** +48/-0 in 1 files

## Summary
- Adds `.beads/REV-schema-llm-fingerprint.md` tracking the apples-to-oranges LLM trace comparison problem found during schema prompt regression testing
- Main branch tests only captured MCP proxy traffic (no direct Gemini calls); branch tests captured direct Gemini HTTP with full `systemInstruction` payloads
- Documents root cause: main ran without a valid `GEMINI_API_KEY` so it never reached Gemini; branch used BYOK from Firestore
- Recommends re-running main branch tests with a valid API key f

## Raw Body
## Summary

- Adds `.beads/REV-schema-llm-fingerprint.md` tracking the apples-to-oranges LLM trace comparison problem found during schema prompt regression testing
- Main branch tests only captured MCP proxy traffic (no direct Gemini calls); branch tests captured direct Gemini HTTP with full `systemInstruction` payloads
- Documents root cause: main ran without a valid `GEMINI_API_KEY` so it never reached Gemini; branch used BYOK from Firestore
- Recommends re-running main branch tests with a valid API key for true LLM fingerprint parity comparison

## Production Code Changes

- **Before**: No tracking bead existed for this comparison gap
- **Now**: `.beads/REV-schema-llm-fingerprint.md` documents the problem, evidence paths, and next steps
- **Why**: CLAUDE.md requires beads tracking for all notable findings; HIGH-priority open item needing follow-up

## Test Changes

None — bead file only.

## Known Limitations

The underlying comparison gap is still open. A valid `GEMINI_API_KEY` in the server environment is needed to produce comparable main-branch Gemini HTTP traces.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Documentation-only change that adds a tracking note; no runtime, security, or data-handling behavior is modified.
> 
> **Overview**
> Adds a new HIGH-priority bead (`.beads/REV-schema-llm-fingerprint.md`) documenting that schema MCP regression artifacts on `origin/main` lack comparable LLM request/response traces (missing `llm_request_responses.jsonl`) while branch runs include direct Gemini `generateContent` captures.
> 
> The bead records evidence paths/log messages and recommends rerunning `schema_validation_*` on `main` with trace capture (and a valid `GEMINI_API_KEY`) to enable a normalized request-envelope diff for prompt/schema parity verification.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit d2d02ed13105a6516e9877c066dabc307c5dbd7
