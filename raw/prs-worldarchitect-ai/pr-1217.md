# PR #1217: Add venv pattern to .gitignore

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2025-08-08
**Author:** jleechan2015
**Stats:** +1/-0 in 1 files

## Summary
Added `venv` pattern (without trailing slash) to .gitignore to provide more comprehensive coverage for Python virtual environments.

## Raw Body
## Summary

Added `venv` pattern (without trailing slash) to .gitignore to provide more comprehensive coverage for Python virtual environments.

## Changes

- **Enhanced .gitignore**: Added `venv` pattern to complement existing `venv/` pattern
- **Better Coverage**: Now catches both files and nested directories named venv anywhere in the project

## Why This Change?

The `venv` pattern without a trailing slash provides broader coverage:
- `venv/` only catches directories named venv in the root
- `venv` catches files, directories, and nested venv directories anywhere

This ensures virtual environments are properly ignored regardless of where they're created in the project structure.

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->

## Summary by CodeRabbit

* **Chores**
  * Updated the ignore list to better handle Python virtual environment directories.

<!-- end of auto-generated comment: release notes by coderabbit.ai -->
