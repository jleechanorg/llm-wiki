# PR #4572: Restore: cache-busting pipeline for dice UI

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-02-08
**Author:** jleechan2015
**Stats:** +5386/-34 in 28 files

## Summary
Restores and completes the cache-busting pipeline for frontend assets, fixing production 404s and implementing content-addressed immutable caching.

**Key themes:**
- **Docker-based architecture** - Cache-busting runs during Docker build (not CI), ensuring hashed files are included in production images
- **Content-addressed assets** - MD5-hashed filenames (e.g., `app.c70a7330.js`) enable aggressive 1-year immutable caching
- **Environment-aware serving** - Flask routes handle hashed/non-hashed f

## Raw Body
## Summary

Restores and completes the cache-busting pipeline for frontend assets, fixing production 404s and implementing content-addressed immutable caching.

**Key themes:**
- **Docker-based architecture** - Cache-busting runs during Docker build (not CI), ensuring hashed files are included in production images
- **Content-addressed assets** - MD5-hashed filenames (e.g., `app.c70a7330.js`) enable aggressive 1-year immutable caching
- **Environment-aware serving** - Flask routes handle hashed/non-hashed files with appropriate cache headers for dev/test/production
- **Three-Evidence Rule compliance** - Runtime verification with real Flask server and HTTP header validation

---

## Production Code Changes

### 1. `mvp_site/main.py` (+145/-20 lines)

**Purpose:** Flask application - serves frontend assets and API routes

#### Cache-aware asset serving (lines 759-890)
- **Added `_resolve_frontend_folder()`** - No env override → Used hardcoded `frontend_v1` path → Now respects `FRONTEND_V1_DIR` env var → Enables `run_local_server.sh` to serve from temp dir with hashed assets
- **Added `_strip_content_hash()`** - No hash stripping → Dev mode failed when requesting hashed files → Extracts original filename from `app.abc12345.js` → Dev fallback works when cache-busted files missing
- **Added `_is_content_hashed_filename()`** - No detection of hashed files → Cache headers applied uniformly → Identifies content-hashed assets by 8-char hex pattern → Immutable caching only for hashed assets
- **Added `_is_dev_or_test_mode()`** - Dev mode detection scattered → Checked `FLASK_ENV` only → Centralized check for `WORLDAI_DEV_MODE`, `TESTING_AUTH_BYPASS`, `FLASK_ENV` → Consistent dev behavior across codebase

#### Immutable cache headers for hashed assets (line 843-847)
- **Cache-Control for hashed files** - No special caching → Browser cached JS/CSS indefinitely without version control → Sets `Cache-Control: public, max-age=31536000, immutable` for content-hashed files → Client ne
