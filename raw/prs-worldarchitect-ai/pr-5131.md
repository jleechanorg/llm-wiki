# PR #5131: Fix integrate force-mode checkout blockers

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-02-09
**Author:** jleechan2015
**Stats:** +289/-4 in 2 files

## Summary
- make `integrate.sh --force` resilient when branch checkout is blocked by transient untracked `.beads` runtime files
- include untracked files in force-mode auto-stash so checkout can proceed cleanly
- add checkout recovery path in force mode with clearer error output if retry still fails
- include current `.beads/issues.jsonl` state update on this branch

## Test Plan
- `bash -n integrate.sh`
- `./integrate.sh --force` (verified checkout no longer blocks on `.beads/.jsonl.lock` and integration completes)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Touches developer workflow automation and adds force-mode cleanup/stash behaviors that can remove untracked `.beads` files and alter local state if mis-triggered, though scope is limited to known runtime paths and `--force` only.
> 
> **Overview**
> Makes `integrate.sh --force` more resilient when sw

## Raw Body
## Summary
- make `integrate.sh --force` resilient when branch checkout is blocked by transient untracked `.beads` runtime files
- include untracked files in force-mode auto-stash so checkout can proceed cleanly
- add checkout recovery path in force mode with clearer error output if retry still fails
- include current `.beads/issues.jsonl` state update on this branch

## Changes
- `integrate.sh`
  - add `cleanup_checkout_blockers()` for untracked lock/socket/db temp files under `.beads`
  - change force stash to `git stash push -u ...`
  - wrap `git checkout main` with force-mode recovery/retry logic
- `.beads/issues.jsonl`
  - include updated issue tracker records present in this worktree

## Test Plan
- `bash -n integrate.sh`
- `./integrate.sh --force` (verified checkout no longer blocks on `.beads/.jsonl.lock` and integration completes)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Touches developer workflow automation and adds force-mode cleanup/stash behaviors that can remove untracked `.beads` files and alter local state if mis-triggered, though scope is limited to known runtime paths and `--force` only.
> 
> **Overview**
> Makes `integrate.sh --force` more resilient when switching back to `main` is blocked by transient, untracked `.beads` runtime files.
> 
> Force mode now (1) cleans up known untracked `.beads` lock/socket/pid/sqlite temp files before checkout (skipping DB WAL/SHM if the beads daemon is running), (2) stashes *including untracked files* via `git stash push -u`, and (3) wraps `git checkout main` with recovery + retry and clearer error output if the retry still fails.
> 
> Also updates `.beads/issues.jsonl` bookkeeping on this branch.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 5658b9b130a92623007960112391c8c96e3b0525. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated co
