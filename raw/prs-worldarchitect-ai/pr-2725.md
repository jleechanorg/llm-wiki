# PR #2725: fix(living-world): Fix duplicate world_events + add diagnostic tool

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2025-12-28
**Author:** jleechan2015
**Stats:** +566/-9 in 3 files

## Summary
- **Bug Fix**: Stop duplicating cumulative world_events to story entries
- **New Tool**: Add `scripts/check_living_world.py` diagnostic script

### Bug Fix Details
**Problem**: `world_events` from `game_state` (cumulative) were being copied to every story entry's `structured_fields`, causing all turns to display the same events (e.g., "Turn 27" events appearing in turns 280+).

**Solution**: Now extracts `world_events` from the LLM response's `structured_fields` (this turn's new events only) ins

## Test Plan
- [x] Added `test_world_events_not_duplicated_across_turns` E2E test
- [x] All living world tests pass
- [x] Verified fix with diagnostic script

🤖 Generated with [Claude Code](https://claude.com/claude-code)


<!-- This is an auto-generated comment: release notes by coderabbit.ai -->

## Raw Body
## Summary
- **Bug Fix**: Stop duplicating cumulative world_events to story entries
- **New Tool**: Add `scripts/check_living_world.py` diagnostic script

### Bug Fix Details
**Problem**: `world_events` from `game_state` (cumulative) were being copied to every story entry's `structured_fields`, causing all turns to display the same events (e.g., "Turn 27" events appearing in turns 280+).

**Solution**: Now extracts `world_events` from the LLM response's `structured_fields` (this turn's new events only) instead of `game_state.world_events` (cumulative).

### Diagnostic Tool
New script `scripts/check_living_world.py` queries Firestore to verify Living World updates are being generated:
```bash
python scripts/check_living_world.py <campaign_id> --turns 20
```

## Test plan
- [x] Added `test_world_events_not_duplicated_across_turns` E2E test
- [x] All living world tests pass
- [x] Verified fix with diagnostic script

🤖 Generated with [Claude Code](https://claude.com/claude-code)


<!-- This is an auto-generated comment: release notes by coderabbit.ai -->

## Summary by CodeRabbit

## Release Notes

* **Bug Fixes**
  * Fixed world event duplication where events from previous turns were incorrectly persisting across consecutive turns in campaign stories.

* **New Features**
  * Introduced a diagnostic tool for inspecting living world data, including event generation and persistence metrics across campaign turns.

* **Tests**
  * Added end-to-end test to validate proper world event isolation and verify events are not carried over from earlier turns.

<sub>✏️ Tip: You can customize this high-level summary in your review settings.</sub>

<!-- end of auto-generated comment: release notes by coderabbit.ai -->

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Fixes Living World event duplication and adds tooling to inspect stored updates.
> 
> - **Bug fix**: In `world_logic.py`, annotate and store only THIS TURN’s `world_events` from LLM `structured_fields` (checking `state_updates.wor
