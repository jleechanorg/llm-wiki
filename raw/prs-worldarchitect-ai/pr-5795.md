# PR #5795: fix(ui): match campaign list highlight to planning block style

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-02-28
**Author:** jleechan2015
**Stats:** +8/-7 in 1 files

## Summary
- Remove always-on `:first-child` purple highlight on first campaign item
- Fix hover bg from white `var(--bs-gray-100)` to dark `var(--hover-bg)`
- Add gold `var(--accent-color)` border on hover matching planning block choice-button style

## Raw Body
## Summary
- Remove always-on `:first-child` purple highlight on first campaign item
- Fix hover bg from white `var(--bs-gray-100)` to dark `var(--hover-bg)`
- Add gold `var(--accent-color)` border on hover matching planning block choice-button style

## Production Code Changes
**Before:** First campaign item always showed purple bg; hover showed white bg clashing with dark fantasy theme

**Now:** All items consistent dark bg; hover = subtle dark overlay + gold border identical to planning block choice selection

**Why:** `:first-child` selector was unintentional always-on highlight; hover used Bootstrap light-theme gray with no fantasy override

## Test Changes
None — visual-only CSS change in `fantasy.css`

## Known Limitations
Requires Cmd+Shift+R hard-refresh on browsers with cached CSS

Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Low risk visual-only CSS updates limited to the `fantasy` theme, mainly affecting campaign list hover/active selectors.
> 
> **Overview**
> Unifies `fantasy` theme campaign list interactions by switching hover and active styling to use `var(--hover-bg)` with an `var(--accent-color)` border (with `!important` to override downstream styles), matching the planning block choice-button look.
> 
> Removes the unintended always-on highlight of the first campaign item by dropping the `:first-child` rule and instead styling only `.list-group-item.active` for the dashboard selection state.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit fb7c4f064eae9332eb6a6239e0fa9c6362c84931. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->

## Summary by CodeRabbit

* **Style**
  * Refined campaign item styling in the fantasy theme with consistent hover and active state colors across d
