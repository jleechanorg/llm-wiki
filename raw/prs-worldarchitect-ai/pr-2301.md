# PR #2301: Disable temporal correction retries and surface violations to users

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2025-12-03
**Author:** jleechan2015
**Stats:** +76/-22 in 2 files

## Summary
- Disable temporal correction retries (`MAX_TEMPORAL_CORRECTION_ATTEMPTS=0`) to prevent multiple LLM calls
- Add user-facing temporal anomaly alerts via `god_mode_response` 
- Update test to match disabled corrections behavior

## Test Plan
- [x] `test_temporal_correction.py` passes with updated expectations
- [ ] Deploy to dev and verify continue story is faster
- [ ] Verify temporal anomaly message appears in frontend when violations occur

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Disables temporal correction retries and surfaces backward-time anomalies to users via `god_mode_response`, updating tests to expect a single LLM call and preserved user input.
> 
> - **Bac

## Raw Body
## Summary
- Disable temporal correction retries (`MAX_TEMPORAL_CORRECTION_ATTEMPTS=0`) to prevent multiple LLM calls
- Add user-facing temporal anomaly alerts via `god_mode_response` 
- Update test to match disabled corrections behavior

## Problem
Continue story actions were taking **2+ minutes** (~138 seconds) because temporal violations triggered up to **4 LLM calls**:
1. Initial call
2. Temporal correction retry #1
3. Temporal correction retry #2
4. Planning block generation

## Solution
- Disable retries (set max attempts to 0)
- Surface temporal violations to users with a visible warning in the narrative
- Accept the response with 1 LLM call instead of retrying

## User Experience
When temporal anomaly occurs, users now see:
```
⚠️ **TEMPORAL ANOMALY DETECTED**

The AI generated a response where time moved backward:
- **Previous time:** 1492 DR, Kythorn 1, Afternoon (15:15)
- **Response time:** 1492 DR, Kythorn 1, 15:00

This may indicate the AI lost track of the story timeline. 
The response was accepted but timeline consistency may be affected.
```

## Test plan
- [x] `test_temporal_correction.py` passes with updated expectations
- [ ] Deploy to dev and verify continue story is faster
- [ ] Verify temporal anomaly message appears in frontend when violations occur

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Disables temporal correction retries and surfaces backward-time anomalies to users via `god_mode_response`, updating tests to expect a single LLM call and preserved user input.
> 
> - **Backend (`mvp_site/world_logic.py`)**:
>   - Set `MAX_TEMPORAL_CORRECTION_ATTEMPTS` to `0` (disable retries).
>   - Add temporal anomaly surfacing: detect backward time and populate `prevention_extras["god_mode_response"]` with a warning; add logging.
>   - Skip legacy temporal warning text when retries are disabled; keep legacy path when enabled.
>   - Preserve original `user_input` for Firestore while using `l
