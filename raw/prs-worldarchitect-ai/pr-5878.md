# PR #5878: fix(settings): settings button click now renders settings page correctly

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-03-08
**Author:** jleechan2015
**Stats:** +171/-17 in 2 files

## Summary
- **Root cause**: `document.querySelector('.container.mt-4')` matched `#dashboard-view` (which carries that class), so `settings-view` was appended as a *child* of dashboard. When `showView('settings')` removed `active-view` from dashboard, dashboard became `display:none` and hid its child `settings-view` with it — despite `settings-view` itself having `active-view`.
- **Fix**: Use `document.getElementById('main-content')` to append `settings-view` as a sibling of other views, matching `index.ht

## Raw Body
## Summary

- **Root cause**: `document.querySelector('.container.mt-4')` matched `#dashboard-view` (which carries that class), so `settings-view` was appended as a *child* of dashboard. When `showView('settings')` removed `active-view` from dashboard, dashboard became `display:none` and hid its child `settings-view` with it — despite `settings-view` itself having `active-view`.
- **Fix**: Use `document.getElementById('main-content')` to append `settings-view` as a sibling of other views, matching `index.html` structure.
- **Secondary fix**: `loadSettingsPage()` was not awaited in `handleRouteChange()` — async race condition also caused blank renders (REV-82q).

## Production Code Changes

| Before | After | Why |
|--------|-------|-----|
| `loadSettingsPage()` (no await) | `await loadSettingsPage()` | Race condition: active-view was applied before DOM element existed |
| `querySelector('.container.mt-4')` | `getElementById('main-content')` | Wrong parent — dashboard-view matched the selector, hiding settings inside it |
| Manual hide-all + add-active-view | `await showView('settings')` | Reuse existing abstraction; removes duplicate logic |
| `views.settings` only set on first creation | Always kept in sync | Stale reference on re-navigation |
| `window.settingsListenersAttached` guard | Reset before `initializeSettings()` | innerHTML always destroys DOM — listeners must re-attach on every visit |
| `alert()` on error | `showErrorToast()` | Consistent error UX, non-blocking |

## Test Changes

- Added `testing_ui/test_settings_button_click.py` — 4-step browser regression:
  1. Dashboard loads, `#settings-btn` visible
  2. Click button → settings page **visibly** renders (`wait_for_selector state=visible`)
  3. Navigate back to dashboard
  4. Click button again → re-navigation renders correctly
- Previous test version used `.count() > 0` (DOM presence only) which produced false positives on blank pages; now uses `wait_for_selector(state='visible')` to assert viewpor
