# PR #5748: fix(ci): harden self-hosted venv cache reuse checks

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-02-24
**Author:** jleechan2015
**Stats:** +6/-4 in 1 files

## Summary
- hardens cached `venv` reuse checks in `.github/workflows/test.yml`
- only reuses cached `venv` when both `venv/bin/python` and `venv/bin/pip` exist and execute successfully
- recreates `venv` when cached wrapper scripts are stale/corrupt (prevents `exit 127` from missing `pip`)
- includes resolver dedupe so install step uses the same resolved interpreter path

## Raw Body
## Summary
- hardens cached `venv` reuse checks in `.github/workflows/test.yml`
- only reuses cached `venv` when both `venv/bin/python` and `venv/bin/pip` exist and execute successfully
- recreates `venv` when cached wrapper scripts are stale/corrupt (prevents `exit 127` from missing `pip`)
- includes resolver dedupe so install step uses the same resolved interpreter path

## Root Cause Addressed
Self-hosted CI could reuse a partially valid cached `venv` where Python existed but pip wrapper was missing/broken, causing dependency-install failures before tests.

## Validation
- YAML/conflict sanity checked locally after cherry-pick resolution
- branch pushed and ready for CI verification on PR checks

## Scope
- `.github/workflows/test.yml` only

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Workflow-only change that makes dependency setup more defensive; main risk is unexpected cache invalidation or timeout flakiness on slow runners.
> 
> **Overview**
> Hardens CI virtualenv cache reuse in `.github/workflows/test.yml` by only reusing `venv` when both `venv/bin/python` and `venv/bin/pip` exist and successfully run (with 30s timeouts), avoiding failures from partially restored/corrupt caches.
> 
> When recreating the environment, the install step now uses the interpreter path emitted by the earlier `python-version` resolver step, and errors out with a clearer message if that resolved interpreter is missing.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 909627a23e802aab29472c57cb4fa17bb6a0d0f3. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->
## Summary by CodeRabbit

* **Chores**
  * Improved test environment setup with stricter validation and timeout checks to ensure the Python runtime and package tool are usable.
  * Updated virtual environment creation 
