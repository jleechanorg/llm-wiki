# PR #3804: feat: remove test_mode auth bypass param

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-01-19
**Author:** jleechan2015
**Stats:** +29/-93 in 3 files

## Summary
(none)

## Raw Body
Removed the `test_mode` URL parameter authentication bypass from the frontend application to improve security. Verified that the backend `main.py` already lacks support for this parameter, relying instead on a header-based approach that is guarded by the `TESTING_AUTH_BYPASS` environment variable.

Changes:
- Removed `test_mode` check from `frontend_v1/js/settings.js`
- Removed `test_mode` check from `frontend_v1/auth.js`
- Removed `test_mode` check from `frontend_v1/app.js`

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Removes client-side `test_mode` authentication bypass to rely solely on Firebase auth.
> 
> - `app.js`: Delete test mode logic; always gate routes on `firebase.auth().currentUser`; simplify auth state handling; `downloadFile` now only uses Firebase tokens for headers
> - `auth.js`: Remove test-bypass headers and events; keep token refresh and `authTokenManager` for standard Firebase flow
> - `js/settings.js`: Drop URL-based test mode header injection; `getAuthHeaders` now always fetches Firebase token
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 28acf20ad0a16246b177c6c041ac1ef043231beb. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
