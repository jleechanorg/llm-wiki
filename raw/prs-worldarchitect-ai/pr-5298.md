# PR #5298: Fix /generatetest to mandate MCPTestBase for all E2E tests

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-02-12
**Author:** jleechan2015
**Stats:** +81/-127 in 1 files

## Summary
Fixed conflicting instructions in `/generatetest` skill that could lead to tests being generated without using the `MCPTestBase` class from `testing_mcp/lib`.

**Key themes:**
- Template consistency - removed conflicting standalone pattern
- Mandatory usage enforcement - changed from "prefer" to "MANDATORY"  
- Documentation clarity - explicit that MCPTestBase is the ONLY approved E2E pattern

## Raw Body
## Summary
Fixed conflicting instructions in `/generatetest` skill that could lead to tests being generated without using the `MCPTestBase` class from `testing_mcp/lib`.

**Key themes:**
- Template consistency - removed conflicting standalone pattern
- Mandatory usage enforcement - changed from "prefer" to "MANDATORY"  
- Documentation clarity - explicit that MCPTestBase is the ONLY approved E2E pattern

## Production Code Changes

### 1. `.claude/commands/generatetest.md` (+73/-123 lines)
**Purpose:** Skill definition for /generatetest command - instructs how to generate E2E tests

- **Removed conflicting standalone script template** - Had both MCPTestBase template AND standalone script template → Removed standalone template → Per CLAUDE.md, MCPTestBase is the established pattern (41 existing tests use it)
- **Updated CORE PRINCIPLES section** - Said "PREFER MCPTestBase" → Changed to "MANDATORY MCPTestBase" as first principle → Eliminate ambiguity about which pattern to use
- **Strengthened "When to Use" table** - Showed MCPTestBase as "YES - Always" → Changed to "YES - **MANDATORY**" with notes → Make it crystal clear this is not optional
- **Added verification note** - No mention of existing usage → Added "41 existing test files in testing_mcp/ all use MCPTestBase" → Provide evidence this is the established pattern
- **Replaced Phase 2 template** - Showed 150-line standalone script with manual server management → Replaced with 50-line MCPTestBase template with ctx helpers → Aligns with actual codebase patterns and reduces maintenance burden

## Test Changes
N/A - Documentation-only change to skill definition

## Known Limitations
None - This is a pure documentation fix to ensure generated tests follow established patterns

## Verification

The `MCPTestBase` class at `testing_mcp/lib/base_test.py`:
- ✅ Exists and is fully implemented (1278 lines)
- ✅ Used by 41 existing tests in the codebase
- ✅ Provides: automatic server management, evidence collection, campaign 
