# PR #2112: fix: REPO_ROOT path calculation and export genericization

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2025-11-26
**Author:** jleechan2015
**Stats:** +43/-28 in 6 files

## Summary
Fixes GitHub review comments from claude-commands PR #109.

## Test Plan
- [ ] Verify MCP scripts work correctly with fixed REPO_ROOT
- [ ] Verify export includes automation/ directory
- [ ] Verify pair-examples.md has no project-specific references

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Removes `automation/` from export exclusions, genericizes examples to `your_project`, improves MCP smoke-test workflow to locate rotating previews with pinned actions, and fixes `REPO_ROOT` path.
> 
> - **Export syste

## Raw Body
## Summary
Fixes GitHub review comments from claude-commands PR #109.

## Bug Fixes
- **REPO_ROOT path in scripts/mcp_common.sh**: Changed from `../..` to `..` for root scripts directory
  - The root `scripts/` directory only needs one level up to reach repo root
  - Previous change broke path resolution for MCP functionality

- **Export exclusion list**: Removed `automation/` from exclusion lists across:
  - `.claude/commands/exportcommands.md`
  - `.claude/commands/exportcommands.py`

## Documentation
- **pair-examples.md genericization**:
  - `mvp_site` → `your_project` (8 occurrences)
  - `worldarchitect-user-uploads` → `your-project-uploads`

## Test plan
- [ ] Verify MCP scripts work correctly with fixed REPO_ROOT
- [ ] Verify export includes automation/ directory
- [ ] Verify pair-examples.md has no project-specific references

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Removes `automation/` from export exclusions, genericizes examples to `your_project`, improves MCP smoke-test workflow to locate rotating previews with pinned actions, and fixes `REPO_ROOT` path.
> 
> - **Export system**:
>   - Includes `orchestration/automation` by removing it from exclusion lists in `exportcommands.md` and `exportcommands.py` (rsync/manual excludes, verification, commit/PR text, summary output).
> - **CI workflow** (`.github/workflows/mcp-manual-smoke-tests.yml`):
>   - Switches trigger to "Deploy PR Preview (Rotating Pool)"; finds deployed service via `pr-number` label; clearer no-preview handling; pins action SHAs; pins Node setup.
> - **Scripts**:
>   - Fixes `REPO_ROOT` in `scripts/mcp_common.sh` from `../..` to `..`.
> - **Documentation**:
>   - Genericizes `pair-examples.md` (`mvp_site` → `your_project`, S3 bucket and path references, file paths in examples).
> - **Tests**:
>   - Updates export tests to reflect inclusion of `orchestration/automation` and new exclusion set.
> 
> <sup>Written by [Cursor Bugbot
