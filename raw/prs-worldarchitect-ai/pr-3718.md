# PR #3718: world_time: document intercalary-day normalization and tidy month map

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-01-17
**Author:** jleechan2015
**Stats:** +62/-14 in 1 files
**Labels:** codex

## Summary
(none)

## Raw Body
### Motivation
- Clarify that month normalization prioritizes ordering over exact calendar fidelity for settings that include intercalary days, to avoid misleading consumers of `MONTH_MAP`.
- Remove a duplicate month key and simplify date-field validation to be clearer and lint-friendly.

### Description
- Added a note above `MONTH_MAP` explaining that some settings include intercalary days and that the normalization prioritizes ordering over exact calendar fidelity.  
- Normalized the abbreviated month entries to avoid a duplicate key for `may` while preserving coverage for full and short month names.  
- Simplified `_has_required_date_fields` day check to `return day != 0` to reduce verbosity and satisfy lint guidance.

### Testing
- Ran pre-commit hooks which include `ruff` linting and Python formatting, and they all passed.  
- No unit/integration tests were executed as part of this change; recommend running `./run_tests.sh` (or CI) before merge to confirm broader test coverage.

------
[Codex Task](https://chatgpt.com/codex/tasks/task_e_696ad0635208832f98e61ebfbb56a3b7)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **World time normalization and validation tweaks**
> 
> - Adds a note above `MONTH_MAP` clarifying intercalary-day handling and that normalization prioritizes ordering over exact fidelity
> - Reformats and deduplicates `MONTH_MAP`; removes abbreviated `"may"` to avoid duplicate key while retaining full/short month coverage
> - Simplifies `_has_required_date_fields` by replacing a verbose day check with `return day != 0`
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit ca1e298f803e724510259ccb70c21cd6cd602d7e. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
