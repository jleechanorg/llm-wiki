# PR #5544: Add Codex Web gh marker and task-end PR push guidance

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-02-15
**Author:** jleechan2015
**Stats:** +372/-0 in 4 files

## Summary
- add Codex Web setup marker output at `/tmp/codex_web_setup_status.env`
- include `GH_INSTALLED_THIS_RUN` and `GH_BIN` in the marker file
- surface marker status in maintenance script output
- add brief `AGENTS.md` guidance for Codex marker detection and automatic PR-remote push attempt at task completion

## Raw Body
## Summary
- add Codex Web setup marker output at `/tmp/codex_web_setup_status.env`
- include `GH_INSTALLED_THIS_RUN` and `GH_BIN` in the marker file
- surface marker status in maintenance script output
- add brief `AGENTS.md` guidance for Codex marker detection and automatic PR-remote push attempt at task completion

## Why
The marker gives a deterministic signal that Codex Web bootstrap ran and whether `gh` was installed in-container. The AGENTS note aligns task completion behavior with keeping PR branches updated automatically.

## Files Changed
- `scripts/codex_web_setup.sh`
- `scripts/codex_web_maintenance.sh`
- `AGENTS.md`

## Validation
- `bash -n scripts/codex_web_setup.sh`
- `bash -n scripts/codex_web_maintenance.sh`

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Adds network-downloaded tooling (`gh`/`gcloud`) and auth bootstrapping in setup scripts, which can affect developer/CI environments if assumptions about permissions, connectivity, or credentials differ.
> 
> **Overview**
> Improves Codex Web bootstrap by emitting a deterministic status marker at `/tmp/codex_web_setup_status.env` (including `GH_BIN`/`GH_INSTALLED_THIS_RUN` and gcloud fields), auto-installing `gh` when missing, and attempting `gh auth setup-git` for git credential wiring.
> 
> Adds optional non-interactive `gcloud` installation plus service-account auth via `GCLOUD_SERVICE_ACCOUNT_JSON`/`GCLOUD_PROJECT_ID`, and enhances the maintenance script to print `gh` and marker presence for easier debugging.
> 
> Introduces a sandbox shell test that builds a temporary bare remote + clone, stubs `gh`/`gcloud`, runs setup/maintenance, verifies the marker file is sourceable, and asserts upstream is re-established; documentation updates in `AGENTS.md` describe marker detection and task-end `git push` guidance.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit fa88290e61faa22f3ac45df6afdcd3232255a421. This will update automatically on new commits. 
