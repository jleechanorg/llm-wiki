# PR #6088: fix(deploy): remove PRODUCTION_MODE conflict in dev environments

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-04-04
**Author:** jleechan2015
**Stats:** +19/-12 in 2 files

## Summary
- **Root cause**: `deploy.sh` set `PRODUCTION_MODE=true` for ALL environments, then added `TESTING_AUTH_BYPASS=true` for dev/preview. The `_validate_production_environment()` check in `main.py` raises `RuntimeError` when both are present, crashing dev deploys.

- **Fix — `deploy.sh`** (3-way split):
  - `stable` / `staging`: `PRODUCTION_MODE=true` only — MCP auth enforced
  - `preview`: `PRODUCTION_MODE=true` only — required for SMOKE_TOKEN CI auth path (`main.py:1443-1451`). `TESTING_AUTH_BYPAS

## Raw Body
## Summary
- **Root cause**: `deploy.sh` set `PRODUCTION_MODE=true` for ALL environments, then added `TESTING_AUTH_BYPASS=true` for dev/preview. The `_validate_production_environment()` check in `main.py` raises `RuntimeError` when both are present, crashing dev deploys.

- **Fix — `deploy.sh`** (3-way split):
  - `stable` / `staging`: `PRODUCTION_MODE=true` only — MCP auth enforced
  - `preview`: `PRODUCTION_MODE=true` only — required for SMOKE_TOKEN CI auth path (`main.py:1443-1451`). `TESTING_AUTH_BYPASS` is blocked server-side for preview (`not is_preview` guard)
  - `dev`: `TESTING_AUTH_BYPASS=true:ALLOW_TEST_AUTH_BYPASS=true` only — no `PRODUCTION_MODE`, prevents RuntimeError

- **Fix — `mvp_site/main.py`**: Error message formatter now includes env var values (`TESTING_AUTH_BYPASS=true` not `TESTING_AUTH_BYPASS=`)

## Evidence

### Test results (pytest — pr-6040 venv)
```
mvp_site/tests/test_api_routes.py .... 17 passed
mvp_site/tests/test_deployment_build.py .... 4 passed
Total: 21 passed, 2 warnings
```

### CI status (new SHA 55de685)
```
commit state: pending (CodeRabbit)
PR mergeable: true
```

### Environment matrix (after fix)
| Environment | PRODUCTION_MODE | TESTING_AUTH_BYPASS | Notes |
|---|---|---|---|
| stable | true | — | MCP auth enforced |
| staging | true | — | MCP auth enforced |
| preview | true | — | SMOKE_TOKEN path (main.py:1443) |
| dev | — | true | Local/CI smoke tests |

Closes #rev-inic

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---
> [!NOTE]
> **Medium Risk** — changes env flags controlling authentication enforcement across deployments.
>
> Rescopes deployment env vars so **stable/staging/preview** get `PRODUCTION_MODE=true`, while **dev** gets `TESTING_AUTH_BYPASS` for CI smoke tests without triggering the production safety check.
>
> Tightens production safety diagnostics in `mvp_site/main.py` so the fatal `PRODUCTION_MODE` validation error reports full `NAME=value` of active bypass fla
