# PR #4224: Add component-level budget allocation for LLM requests

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-02-02
**Author:** jleechan2015
**Stats:** +6447/-136 in 33 files

## Summary
This PR implements intelligent token budget allocation for LLM requests with component-level compaction, ensuring story context quality while preventing any single component from consuming excessive tokens.

**Fixed 6 review comments using TDD approach:**
1. Fixed structured_response serialization (Major) - Pydantic models now properly serialize with `mode="json"`
2. Fixed campaign setting duplication (Medium) - Removed duplicate call in build_god_mode_instructions()
3. Fixed type guard on custo

## Raw Body
# Add Component-Level Budget Allocation for LLM Requests

## Summary

This PR implements intelligent token budget allocation for LLM requests with component-level compaction, ensuring story context quality while preventing any single component from consuming excessive tokens.

**Fixed 6 review comments using TDD approach:**
1. Fixed structured_response serialization (Major) - Pydantic models now properly serialize with `mode="json"`
2. Fixed campaign setting duplication (Medium) - Removed duplicate call in build_god_mode_instructions()
3. Fixed type guard on custom_campaign_state (Medium) - Added isinstance(dict) check for GameState objects
4. Fixed isinstance check on custom_state (Minor) - Added isinstance(dict) validation for dict-format game_state
5. Fixed duplicate critical memories (Minor) - Deduplication in fallback compaction path
6. Removed unused imports (Low) - Cleaned up 21 unused imports from llm_service.py

**Additional Critical Fixes (TDD):**
7. Fixed undefined test_file variable (CRITICAL) - Added inspect.getfile() for evidence bundles
8. Fixed Pydantic JSON serialization (CRITICAL) - datetime fields now serialize correctly
9. Fixed story context warning logic (IMPORTANT) - Warnings only emitted when reduction occurs

**Key themes:**
- Intelligent budget allocation with guaranteed minimums
- Component-specific compaction strategies
- Type safety improvements (defensive programming)
- Code deduplication and hygiene

---

## How Context Budget Allocation Works

### Token Budget Breakdown (Gemini 3 Flash)

| Stage | Tokens | Notes |
|-------|--------|-------|
| Context Window | 1,000,000 | Gemini 3 Flash full context |
| Compaction Limit | 300,000 | Capped for stability/latency |
| **Output Reserve** | **60,000 (20%)** | Reserved for LLM response generation |
| **Max Input** | **240,000** | Available for component allocation |

### Architecture Overview (Gemini 3 Flash - 300k Compaction Limit)

```
┌──────────────────────────────────────────────────────
