# PR #5995: fix(integrate): skip main checkout when branch is in a worktree

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-03-15
**Author:** jleechan2015
**Stats:** +23/-2 in 1 files

## Summary
- `integrate.sh` always tries `git checkout main`, which fails when `main` is checked out in a worktree (git prevents double-checkout of a branch)
- Detects `MAIN_IN_WORKTREE` upfront via `git worktree list --porcelain`
- Skips steps 1-2 (checkout + sync) when main is in a worktree
- Creates new branch from `origin/main` directly instead

## Test Plan
- [ ] Run `./integrate.sh --force` while `main` is checked out in a worktree → should succeed and create new branch from `origin/main`
- [ ] Run `./integrate.sh` normally (no worktree) → unchanged behavior

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Changes the integration flow to bypass local `main` checkout/sync when `main` is already checked out in a worktree, altering which commit is used as the new-branch base. 

## Raw Body
## Summary
- `integrate.sh` always tries `git checkout main`, which fails when `main` is checked out in a worktree (git prevents double-checkout of a branch)
- Detects `MAIN_IN_WORKTREE` upfront via `git worktree list --porcelain`
- Skips steps 1-2 (checkout + sync) when main is in a worktree
- Creates new branch from `origin/main` directly instead

## Changes
- `integrate.sh` only — 23 lines added, 2 lines changed

## Test plan
- [ ] Run `./integrate.sh --force` while `main` is checked out in a worktree → should succeed and create new branch from `origin/main`
- [ ] Run `./integrate.sh` normally (no worktree) → unchanged behavior

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Changes the integration flow to bypass local `main` checkout/sync when `main` is already checked out in a worktree, altering which commit is used as the new-branch base. Risk is moderate because it affects git branch creation/sync behavior and could change outcomes for repos where local `main` differs from `origin/main`.
> 
> **Overview**
> Prevents `integrate.sh` from failing when `main` is already checked out in a git worktree by detecting this upfront and **skipping the local `git checkout main` and local-main sync logic**.
> 
> When `main` is in a worktree, the script now creates the fresh integration branch directly from `origin/main` (instead of local `main`) and prints explicit “skipped” messaging for the affected steps.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit aa348af762f8ba9683180103c3efefe07c7568f5. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->

## Summary by CodeRabbit

* **Bug Fixes**
  * Enhanced the integration script to properly handle scenarios where the main branch is checked out in a git w
