# PR #5813: fix: cache context reclassification — eliminate double-billing + evidence quality

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-03-07
**Author:** jleechan2015
**Stats:** +3855/-429 in 36 files

## Summary
- **Cache/provably-fair compatibility fix (REV-wvh)**: Provably fair seed moved from `system_instruction` to a prepended content part — system_instruction stays static and cacheable. Cache is **never disabled**
- **Silent cache disable eliminated (REV-8gz)**: Removed all `effective_cache_name = None` patterns in `gemini_provider.py` that silently killed cache in code_execution and native tool flows
- **Double-billing fix**: Old story entries were sent in both cache prefix ($0.05/M) AND live JSON

## Test Plan
- [x] Cache hit rate ≥ 80% — verified at 89–93%
- [x] CACHE_REFERENCE appears in server logs — 12 entries
- [x] CACHE_NOT_HIT warnings = 0
- [x] Provably fair commitments generated for every request
- [x] N-1 promotion: old cache used during rebuild, new cache promoted next request
- [x] Rebuild threshold = 5 verified
- [x] 3 TDD unit tests for cache_name flow
- [x] 7 TDD unit tests for N-1 promotion
- [x] Prompt equivalence verified
- [x] Narrative coherence 3/3
- [x] 54 total unit tests pass (

## Raw Body
## Summary

- **Cache/provably-fair compatibility fix (REV-wvh)**: Provably fair seed moved from `system_instruction` to a prepended content part — system_instruction stays static and cacheable. Cache is **never disabled**
- **Silent cache disable eliminated (REV-8gz)**: Removed all `effective_cache_name = None` patterns in `gemini_provider.py` that silently killed cache in code_execution and native tool flows
- **Double-billing fix**: Old story entries were sent in both cache prefix ($0.05/M) AND live JSON ($0.50/M). Fixed by concatenating full `story_history` in the merged payload → **44% cost reduction**
- **N-1 cache promotion**: New caches staged as "pending", promoted on next request to avoid Gemini propagation delay (first-build cold miss eliminated)
- **TDD guards (REV-lfl)**: 3 new tests verify cache_name flows to API call, seed is in content (not system_instruction), and cache survives tool execution phases
- **Explicit cache on** (`constants.EXPLICIT_CACHE_ENABLED = True`): `reset_cache()` now deletes both cache generations remotely (no orphaned API resources)
- **Duplicate `_validate_and_repair_cache` removed** (`intent_classifier.py`): dead override was shadowing the new implementation; removed 67 lines

## Live Cache Verification Evidence

**Test**: `testing_mcp/cache/test_explicit_cache_verification.py`
**Date**: 2026-03-05 | **Git SHA**: `9e4079d243`

| Request | Prompt Tokens | Cached Tokens | Hit Rate |
|---------|--------------|---------------|----------|
| Measurement 1 | 91,247 | 84,659 | **92.8%** |
| Measurement 2 | 94,524 | 85,559 | **90.5%** |
| Post-rebuild 1 | 94,166 | 85,559 | **90.9%** |
| Post-rebuild 2 | 94,982 | 85,559 | **90.1%** |
| Post-rebuild 3 | 98,232 | 87,818 | **89.4%** |
| Post-rebuild 4 | 98,150 | 87,818 | **89.5%** |

### Before vs After

| Metric | Before | After |
|--------|--------|-------|
| `CACHE_REFERENCE` log entries | **0** (never appeared) | **12** (every request) |
| Cache hit rate | **0%** (silently disabled) |
