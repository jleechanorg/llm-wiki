# PR #6141: ops(ci): delete PR preview Docker images on cleanup to prevent Artifact Registry bloat

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-04-07
**Author:** jleechan2015
**Stats:** +68/-0 in 1 files

## Summary
- pr-cleanup.yml now deletes the Docker image from Artifact Registry when a PR is closed
- Uses the commit-sha label stored on the Cloud Run service to construct the image tag
- Prevents future gcr.io bloat (reached ~2.5TB from PR preview images)

## Raw Body
## Summary
- pr-cleanup.yml now deletes the Docker image from Artifact Registry when a PR is closed
- Uses the commit-sha label stored on the Cloud Run service to construct the image tag
- Prevents future gcr.io bloat (reached ~2.5TB from PR preview images)

## Production Code Changes
- `.github/workflows/pr-cleanup.yml`: Added "Delete PR preview Docker image" step to both jobs

## Test Changes
- None — CI/ops change validated on next PR close

## Known Limitations
- Existing images (1,500+) remain — manual deletion via GCP Console needed

Bead: rev-2m4j

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Adds a destructive `gcloud artifacts docker images delete` step; mislabeling or unexpected tag formats could delete the wrong image or fail cleanup, but it’s scoped to PR preview services on PR close.
> 
> **Overview**
> **PR preview cleanup now removes container images as well as releasing the server.**
> 
> In `.github/workflows/pr-cleanup.yml`, both the self-hosted and GitHub-hosted fallback jobs add a new step that reads the Cloud Run service’s `commit-sha` label, constructs the `gcr.io/${GCP_PROJECT}/${SERVICE_NAME}:${COMMIT_SHA}` tag, and deletes that image from Artifact Registry *before* removing service labels, preventing PR-preview image buildup.
> 
> <sup>Reviewed by [Cursor Bugbot](https://cursor.com/bugbot) for commit 2f44fe01abfd4508d3ee9e6bc1ee729fe1f6bc25. Bugbot is set up for automated code reviews on this repo. Configure [here](https://www.cursor.com/dashboard/bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->
## Summary by CodeRabbit

* **Chores**
  * Improved automated cleanup of PR preview Docker images: workflows now attempt to remove preview images only when a service is present, skip gracefully if no image is found, and report warnings instead of failing. The cleanup runs prior to removing PR labels from ser
