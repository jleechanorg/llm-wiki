# PR #6180: fix(wizard): normalize whitespace in campaign description input

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-04-11
**Author:** jleechan2015
**Stats:** +1/-1 in 1 files

## Summary
- Fix whitespace handling bug in campaign wizard description input (rev-xcak)
- `rawDescription.trim()` was used for the truthiness check but the untrimmed `rawDescription` was returned on the truthy branch
- Now returns `rawDescription.trim()` so leading/trailing whitespace is normalized

## Test Plan
- [ ] Enter a campaign description with leading/trailing spaces and verify they are stripped
- [ ] Verify empty/whitespace-only input still falls back to Dragon Knight default when applicable
- [ ] Verify normal input without extra whitespace is unaffected

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Low risk UI-side normalization: only changes how the campaign description is serialized before POSTing, reducing chances o

## Raw Body
## Summary
- Fix whitespace handling bug in campaign wizard description input (rev-xcak)
- `rawDescription.trim()` was used for the truthiness check but the untrimmed `rawDescription` was returned on the truthy branch
- Now returns `rawDescription.trim()` so leading/trailing whitespace is normalized

## Test plan
- [ ] Enter a campaign description with leading/trailing spaces and verify they are stripped
- [ ] Verify empty/whitespace-only input still falls back to Dragon Knight default when applicable
- [ ] Verify normal input without extra whitespace is unaffected

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Low risk UI-side normalization: only changes how the campaign description is serialized before POSTing, reducing chances of whitespace-only/whitespace-padded prompts bypassing the Dragon Knight default fast-path.
> 
> **Overview**
> Fixes campaign wizard submission to **normalize leading/trailing whitespace** in the `description` field. In `collectFormData()`, the truthiness check and the returned value now both use `rawDescription.trim()`, ensuring whitespace-only input falls back to the Dragon Knight default description and avoiding unnecessary slow LLM generation.
> 
> <sup>Reviewed by [Cursor Bugbot](https://cursor.com/bugbot) for commit bc90814adc649800e1a5e26902c05b66d29b8eec. Bugbot is set up for automated code reviews on this repo. Configure [here](https://www.cursor.com/dashboard/bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->

## Summary by CodeRabbit

## Bug Fixes
* Fixed description field handling in campaign creation to properly trim leading and trailing whitespace, ensuring consistent data storage and preventing submissions with whitespace-only descriptions.

<!-- end of auto-generated comment: release notes by coderabbit.ai -->
