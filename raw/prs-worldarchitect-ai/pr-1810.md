# PR #1810: Add smart fake code detection hook

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2025-10-02
**Author:** jleechan2015
**Stats:** +159/-0 in 3 files
**Labels:** codex

## Summary
- add a smart fake code detection hook that runs `/fake` headlessly with `claude -p --dangerously-skip-permissions --model sonnet` after each Write tool use
- document the new hook alongside existing tooling guidance
- register the hook in the project settings so it executes with other post-write safeguards

## Raw Body
## Summary
- add a smart fake code detection hook that runs `/fake` headlessly with `claude -p --dangerously-skip-permissions --model sonnet` after each Write tool use
- document the new hook alongside existing tooling guidance
- register the hook in the project settings so it executes with other post-write safeguards

## Testing
- not run (not requested)

------
https://chatgpt.com/codex/tasks/task_e_68ddfa3a0838832fbba3c9d623eb2472

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Introduces a post-Write hook that runs `/fake` via `claude -p` to audit touched files for placeholder/fake code, logs results, and is documented and integrated into settings.
> 
> - **Hooks**:
>   - **New**: `/.claude/hooks/smart_fake_code_detection.sh` runs `/fake` headlessly after `Write` operations, collects touched files from payload, normalizes paths, logs per-branch results under `~/.local/state/claude/`, enforces timeout (`SMART_FAKE_TIMEOUT`), and avoids recursion.
> - **Settings**:
>   - Register the hook in `/.claude/settings.json` under `PostToolUse` for `Write`, executing the new script alongside existing post-write hooks.
> - **Docs**:
>   - Update `/.claude/hooks/README.md` with a new section describing purpose, workflow, configuration, and detection scope of the Smart Fake Code Detection hook.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit b41d1ea2c819b91f5503a23b2cecb25141d0c6c4. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->

## Summary by CodeRabbit

- New Features
  - Introduced a Smart Fake Code Detection hook that audits recent file writes to flag potential fake or placeholder code and logs findings.
  - Automatically runs after write operations and post-tool actions, with timeouts and non-blocking behavior to avoid disrupting workflows.

- Documentation
  - Added user-f
