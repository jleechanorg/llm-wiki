# PR #6063: [agento] feat(avatar): add download avatar image functionality

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-03-30
**Author:** jleechan2015
**Stats:** +226/-38 in 5 files

## Summary
- Adds `GET /api/avatar` endpoint to download authenticated user's avatar image as attachment
- Adds `GET /api/campaign/<campaign_id>/avatar` endpoint for campaign avatars
- Probes GCS for existing avatar extension (jpeg/png/gif/webp) before download
- Returns `Content-Disposition: attachment` with correct extension
- Includes extension-probing helpers in `firestore_service.py`

## Test Plan
- [x] 31 unit tests pass (`test_avatar_api_unit.py`)
- [x] Avatar bucket/sizes test passes
- [x] Browser E2E test scaffold added (`testing_ui/test_avatar_download_ui.py`)

Closes orch-le1

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Adds new authenticated download endpoints that fetch binary data from GCS and infer file type via blob probing, which can impact availability/performance and error handling paths. Changes are otherwise localized with added unit coverage.
> 
> **Overvie

## Raw Body
## Summary
- Adds `GET /api/avatar` endpoint to download authenticated user's avatar image as attachment
- Adds `GET /api/campaign/<campaign_id>/avatar` endpoint for campaign avatars
- Probes GCS for existing avatar extension (jpeg/png/gif/webp) before download
- Returns `Content-Disposition: attachment` with correct extension
- Includes extension-probing helpers in `firestore_service.py`

## Test plan
- [x] 31 unit tests pass (`test_avatar_api_unit.py`)
- [x] Avatar bucket/sizes test passes
- [x] Browser E2E test scaffold added (`testing_ui/test_avatar_download_ui.py`)

Closes orch-le1

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Adds new authenticated download endpoints that fetch binary data from GCS and infer file type via blob probing, which can impact availability/performance and error handling paths. Changes are otherwise localized with added unit coverage.
> 
> **Overview**
> Adds authenticated download support for user and campaign avatars via `GET /api/avatar` and `GET /api/campaign/<campaign_id>/avatar`, returning the image bytes as an attachment with a content-type/extension-derived filename.
> 
> Implements best-effort GCS probing in `firestore_service` to detect the latest uploaded avatar format (`jpeg/png/gif/webp`) before downloading, and adds unit tests covering found/missing cases.
> 
> Minor housekeeping: centralizes the download extension map in `main.py`, adjusts the Playwright UI test to be a UI smoke check (and updates evidence path), and grants `statuses: read` in the `skeptic-cron` GitHub Actions workflow.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit bb54d9a5d0cfdca0cb2da4d21a56cb076f86715d. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->
## Summary by CodeRabbit

* **New Features**
  * Avatar download for user profiles (
