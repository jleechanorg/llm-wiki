# PR #6051: [agento] fix(openclaw,mcp): guard user_email=None + merge duplicate import

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-03-28
**Author:** jleechan2015
**Stats:** +194/-15 in 3 files

## Summary
Fixes two minor issues identified during skeptic review of PR #5879:
- Personal API key auth path now returns email from Firestore when available, preventing `user_email=None` crashes downstream
- Merged duplicate `streaming_orchestrator` import block

## Test Plan
- `TESTING_AUTH_BYPASS=true WORLDAI_DEV_MODE=true ./vpython -m pytest mvp_site/tests/test_openclaw_critical_fixes.py mvp_site/tests/test_settings_api.py` → 59/59 pass

Fixes #5879 follow-up.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Touches the request authentication path by changing the `lookup_personal_api_key` contract and propagating `user_email`, which could affect API key login behavior if callers aren’t upda

## Raw Body
## Summary
Fixes two minor issues identified during skeptic review of PR #5879:
- Personal API key auth path now returns email from Firestore when available, preventing `user_email=None` crashes downstream
- Merged duplicate `streaming_orchestrator` import block

## Changes
- `firestore_service.lookup_personal_api_key`: Return `(uid, email)` tuple; email fetched from Firestore user doc as best-effort
- `main.py` (`check_token`): Unpack `uid, user_email = ...` so Bearer token auth gets email when available
- `main.py` imports: Merged two consecutive `from mvp_site.streaming_orchestrator import` blocks into one
- `test_settings_api.py`: Updated 4 mock return values to match new tuple signature

## Test plan
- `TESTING_AUTH_BYPASS=true WORLDAI_DEV_MODE=true ./vpython -m pytest mvp_site/tests/test_openclaw_critical_fixes.py mvp_site/tests/test_settings_api.py` → 59/59 pass

Fixes #5879 follow-up.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Touches the request authentication path by changing the `lookup_personal_api_key` contract and propagating `user_email`, which could affect API key login behavior if callers aren’t updated; email lookup is best-effort and should be low-impact but still in a security-adjacent area.
> 
> **Overview**
> **Personal API key auth now propagates user email.** `firestore_service.lookup_personal_api_key` changes from returning just `uid` to returning a `(uid, email)` tuple, best-effort reading the user profile `email` and coercing non-string values to avoid downstream `.lower()` crashes.
> 
> `main.py`’s `check_token` updates the `worldai_` Bearer-token branch to unpack the tuple and pass the looked-up `user_email` through instead of always setting it to `None`. Tests update mocks for the new tuple return and add focused unit coverage for missing docs/uid, email coercion, and email lookup failures.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?t
