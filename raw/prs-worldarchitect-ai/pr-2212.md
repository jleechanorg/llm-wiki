# PR #2212: Remove unjustified fallbacks in mvp_site

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2025-12-01
**Author:** jleechan2015
**Stats:** +154/-114 in 6 files
**Labels:** codex

## Summary
- require Memory MCP functions to be present and let failures surface instead of defaulting to no-op handlers
- reject GOD_MODE state updates when game state reconstruction fails rather than silently creating a placeholder state
- simplify mock service wrappers to use the real logging utilities without permissive import fallbacks

## Raw Body
## Summary
- require Memory MCP functions to be present and let failures surface instead of defaulting to no-op handlers
- reject GOD_MODE state updates when game state reconstruction fails rather than silently creating a placeholder state
- simplify mock service wrappers to use the real logging utilities without permissive import fallbacks

## Testing
- not run (not requested)


------
[Codex Task](https://chatgpt.com/codex/tasks/task_e_692d0c530838832f97d8dfee72474aa0)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Replaces fallback behaviors with fail-fast errors across MCP memory, Firestore writes, and GOD_MODE updates; simplifies mocks and documents fallback strategy.
> 
> - **Backend (fail-fast enforcement)**
>   - **MCP memory (`mvp_site/mcp_memory_real.py`)**: Introduces `MCPMemoryError`; requires all MCP functions on init/injection; removes noop fallbacks; methods raise on missing/uninitialized handlers.
>   - **Firestore writes (`mvp_site/firestore_service.py`)**: Eliminates fallback document ID; raises `FirestoreWriteError` if `.id` missing or `document_id` not captured.
>   - **GOD_MODE state update (`mvp_site/world_logic.py`)**: Rejects updates when `GameState.from_dict` returns `None`; returns HTTP 500 error instead of creating a placeholder state.
> - **Tests/Mocks**
>   - **Mock wrappers**: Use direct `from mvp_site import logging_util` imports in `mocks/mock_llm_service_wrapper.py` and `mocks/mock_firestore_service_wrapper.py`, removing layered import fallbacks.
> - **Docs**
>   - Adds `mvp_site/FALLBACK_REVIEW.md` outlining current fallback logic and justifications.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit ecbc0f6a35da18d7f06501c28e175035279447ff. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->

## Summary by CodeRabbit

## Release Notes

* **D
