# PR #4772: Campaign upgrade helper + coverage

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-02-06
**Author:** jleechan2015
**Stats:** +298/-154 in 6 files

## Summary
(none)

## Raw Body
Summary:
- extract campaign upgrade choice injection to helper
- add unit coverage for tier resolution and planning block normalization
- add end2end validation for upgrade choice injection

Tests:
- python3.11 -m pytest mvp_site/tests/test_world_logic.py::TestCampaignUpgradeHelper -q
- python3.11 -m pytest mvp_site/tests/test_world_logic.py::TestLevelUpInjection::test_inject_levelup_choices_handles_non_dict_planning_block -q
- python3.11 -m pytest mvp_site/tests/test_agents.py::TestGenericPromptBuilder::test_resolve_campaign_tier_from_custom_state_dict -q
- python3.11 -m pytest mvp_site/tests/test_end2end/test_continue_story_end2end.py::TestContinueStoryEnd2End::test_campaign_upgrade_choice_injected -q

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Touches server-side response shaping in `world_logic` (choice injection/normalization), which can affect gameplay UI choices if edge cases are missed, but changes are mostly refactor + added test coverage.
> 
> **Overview**
> Extracts campaign-upgrade planning-block handling into a new `campaign_upgrade` helper, and reuses a shared `normalize_planning_block_choices` routine to harden server-side choice injection (campaign upgrade, level-up, and spicy-mode) against `None`/string/non-dict planning blocks and list-format `choices`.
> 
> Adds coverage for campaign tier resolution in `PromptBuilder`, unit tests for the new normalization/injection behavior, and an end-to-end test asserting `upgrade_campaign` is injected when the LLM omits it during `MODE_CAMPAIGN_UPGRADE`. Also updates the PR comment fetcher to ignore `[AI responder]` comments to avoid recursive processing.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 7ed96490780797bbba84d7f4653ff35598848369. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
