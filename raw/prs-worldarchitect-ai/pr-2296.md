# PR #2296: Remove LLM fallback retries and surface context errors

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2025-12-03
**Author:** jleechan2015
**Stats:** +279/-276 in 11 files
**Labels:** codex

## Summary
- remove model and context fallback chains and convert LLM calls to single-shot attempts
- raise explicit LLMRequestError instances for context overflow and provider overload cases and surface them through world_logic responses
- add coverage ensuring context-too-large and overload errors are surfaced without retries

## Raw Body
## Summary
- remove model and context fallback chains and convert LLM calls to single-shot attempts
- raise explicit LLMRequestError instances for context overflow and provider overload cases and surface them through world_logic responses
- add coverage ensuring context-too-large and overload errors are surfaced without retries

## Testing
- pytest mvp_site/tests/test_llm_service_error_handling.py
- PYTHONPATH=/workspace/worldarchitect.ai pytest mvp_site/tests/test_end2end/test_continue_story_end2end.py


------
[Codex Task](https://chatgpt.com/codex/tasks/task_e_692fc716467c832faddf5740408f1e26)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Replace model-cycling retries with single-shot LLM calls and propagate context/overload errors via LLMRequestError into API responses, updating docs and tests.
> 
> - **Backend AI (`mvp_site/llm_service.py`)**:
>   - Replace `*_with_model_cycling` with `_call_llm_api()` (no retries/fallbacks); remove fallback chains.
>   - Convert all call sites to `_call_llm_api(...)` and keep JSON-mode generation.
>   - Surface errors explicitly: wrap context overflow as `LLMRequestError(status_code=422)`; map provider overload/rate limit to 503/429.
>   - Keep token budget calc; minor log/message tweaks; import `LLMRequestError`.
> - **Request/Errors (`mvp_site/llm_request.py`)**:
>   - Add `status_code` to `LLMRequestError`; minor exception formatting.
> - **World Logic (`mvp_site/world_logic.py`)**:
>   - Catch `LLMRequestError` in campaign creation and story continuation loops and return standardized error responses.
>   - Minor readability tweaks (month map formatting, helpers).
> - **Docs**:
>   - Update README(s) and `CODE_REVIEW_SUMMARY.md` to reference `_call_llm_api()` and single-shot behavior.
> - **Tests**:
>   - Add `tests/test_llm_service_error_handling.py` to assert surfaced 422/503/429 without retries.
>   - Update existing tests to call `_call_llm_api` (remove cycling) and minor I/O tweaks.
>   - Preserve JSON-mode, token budget r
