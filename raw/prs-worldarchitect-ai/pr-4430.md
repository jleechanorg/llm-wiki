# PR #4430: fix: Disable explicit cache to restore code_execution for dice rolls

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-02-01
**Author:** jleechan2015
**Stats:** +5/-6 in 1 files

## Summary
- Disable explicit caching which breaks Gemini code_execution for dice rolls

## Test Plan
- [ ] Run dice tests to verify code_exec=True in logs

## Raw Body
## Summary
- Disable explicit caching which breaks Gemini code_execution for dice rolls

## Problem
Explicit cache was merged to main without the code_execution tool fix.
When cache is used, Gemini API constraint prevents adding tools, so code_execution=False.
This causes dice fabrication (LLM generates dice values without actual RNG).

## Production Code Changes

### 1. mvp_site/llm_service.py (+5/-6 lines)
**Purpose:** Disable explicit caching to restore code_execution capability

- Line ~1693: Changed explicit_cache_enabled = True to False with comment explaining the issue
- Line ~4233: Changed explicit_cache_enabled = True to False with reference to main comment

## Test Plan
- [ ] Run dice tests to verify code_exec=True in logs

## Known Limitations
| Issue | Severity | Notes |
|-------|----------|-------|
| Explicit caching disabled | Medium | Cost savings temporarily unavailable until proper fix merged |

Generated with Claude Code

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Low-scope change but it affects production LLM request behavior and cost/latency by removing caching for Gemini until a tool-compatible caching approach is implemented.
> 
> **Overview**
> **Disables Gemini explicit caching** by hard-setting `explicit_cache_enabled = False` in both the request path and the context-budget path in `llm_service.py`.
> 
> This is a targeted rollback to restore `code_execution` (used for server-verified dice rolls), since Gemini cached content cannot be used with tools and would otherwise force tool-less generation.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit bf81d5d85444d49478c45c6202c266113a576f6b. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
