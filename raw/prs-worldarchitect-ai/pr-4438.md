# PR #4438: fix: Invert explicit cache tests to pass when cache disabled

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-02-01
**Author:** jleechan2015
**Stats:** +91/-38 in 1 files

## Summary
- Inverted 3 explicit cache tests to verify cache IS disabled (instead of skipping them)
- Tests will FAIL when cache is re-enabled, serving as a reminder to fix them

**Key themes:**
- Test inversion pattern for temporary disabled features
- PR #4430 follow-up

## Raw Body
## Summary
- Inverted 3 explicit cache tests to verify cache IS disabled (instead of skipping them)
- Tests will FAIL when cache is re-enabled, serving as a reminder to fix them

**Key themes:**
- Test inversion pattern for temporary disabled features
- PR #4430 follow-up

## Production Code Changes
None - test file only.

## Test Changes
Modified `mvp_site/tests/test_end2end/test_explicit_cache_miss_end2end.py`:

### 1. `test_explicit_caching_disabled_pr4430` (was `test_explicit_caching_always_enabled_even_when_env_false`)
- **Inverted assertion** - Was `assert_called_once()` → Now `assert_not_called()` → Cache is disabled per PR #4430

### 2. `test_explicit_caching_disabled_for_code_execution_pr4430` (was `test_explicit_caching_not_disabled_for_code_execution`)
- **Inverted assertion** - Was `assert_called_once()` → Now `assert_not_called()` → Cache breaks code_execution

### 3. `test_explicit_caching_disabled_for_force_tool_mode_pr4430` (was `test_explicit_caching_not_disabled_for_force_tool_mode`)
- **Inverted assertion** - Was `assert_called_once()` → Now `assert_not_called()` → Cache breaks tool mode

All tests include detailed docstrings explaining how to revert when cache is re-enabled.

**Test results:** 12 passed, 0 skipped

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Test-only changes that adjust mocked call expectations; no production logic or data paths are modified.
> 
> **Overview**
> **Inverts explicit-caching integration tests to assert the cache path is *not* used** (PR #4430), reflecting that explicit caching is currently disabled.
> 
> The three affected tests now stub `_call_llm_api` to return a minimal response and verify `_call_llm_api_with_explicit_cache` is *not* called for normal requests, `code_execution` models, and `force_tool_mode`, with docstrings documenting how to revert when caching is re-enabled.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashb
