# PR #3139: test: Add mode parameter validation test with agent_mode verification

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-01-06
**Author:** jleechan2015
**Stats:** +532/-16 in 6 files

## Summary
- Remove story pagination pipeline: Firestore aggregation/query helpers, `/api/campaigns/<id>/story` endpoint, frontend load-older UI, and the real pagination E2E test; replace with a capped last-300 story response in `/api/campaigns/<id>` to stay under Cloud Run size limits; update API route tests/fake Firestore accordingly.
- Add mode-parameter validation harness and evidence capture (new `testing_mcp/test_mode_parameter_real_api.py`, evidence utils env masking tweaks), plus tighter validation

## Test Plan
- `python testing_mcp/test_mode_parameter_real_api.py --start-local`
  - Evidence: `/tmp/worldarchitect.ai/mode-parameter-test/mode_parameter_validation/summary.json` (7/7 passed; request_mode_match true for all scenarios).

## Raw Body
## Summary
- Remove story pagination pipeline: Firestore aggregation/query helpers, `/api/campaigns/<id>/story` endpoint, frontend load-older UI, and the real pagination E2E test; replace with a capped last-300 story response in `/api/campaigns/<id>` to stay under Cloud Run size limits; update API route tests/fake Firestore accordingly.
- Add mode-parameter validation harness and evidence capture (new `testing_mcp/test_mode_parameter_real_api.py`, evidence utils env masking tweaks), plus tighter validation utilities.
- Add opt-in raw LLM payload return: `include_raw_llm_payloads` flows HTTP → MCP → world_logic → llm_service; tests default to include via `TESTING_MCP_INCLUDE_RAW_LLM`.
- Log raw LLM payloads at INFO and respect return-to-story normalization expectations; update mock LLM wrapper signature.
- Genesis module import/test adjustments + requirements cleanup; misc beads metadata/venv script cleanups.

## Behavior Notes
- When mode=`god` and the user issues “Return to story”, the server normalizes `mode` to `character` before the LLM call. As a result, `raw_request_payload.game_mode` is `character` even though `mode_requested` is `god` (tests now assert this intentionally).

## Test Plan
- `python testing_mcp/test_mode_parameter_real_api.py --start-local`
  - Evidence: `/tmp/worldarchitect.ai/mode-parameter-test/mode_parameter_validation/summary.json` (7/7 passed; request_mode_match true for all scenarios).

