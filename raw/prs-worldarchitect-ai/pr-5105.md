# PR #5105: Prompt contract: require world_events on living-world turns

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-02-09
**Author:** jleechan2015
**Stats:** +91/-33 in 8 files

## Summary
- make `state_updates.world_events` explicitly REQUIRED in living-world prompt instructions (LLM contract only)
- reinforce this requirement in the runtime living-world prompt header
- resolve prompt contradictions that could cause confusion:
  - align cadence language to **every 3 turns OR every 24 game hours**
  - clarify `state_updates` emission guidance so minigame-specific delta guidance does not conflict with global `state_updates` presence requirement
  - clarify living-world payload shou

## Raw Body
## Summary
- make `state_updates.world_events` explicitly REQUIRED in living-world prompt instructions (LLM contract only)
- reinforce this requirement in the runtime living-world prompt header
- resolve prompt contradictions that could cause confusion:
  - align cadence language to **every 3 turns OR every 24 game hours**
  - clarify `state_updates` emission guidance so minigame-specific delta guidance does not conflict with global `state_updates` presence requirement
  - clarify living-world payload should include new turn deltas (avoid repeating historical events in `background_events`)

## Files Changed
- `mvp_site/prompts/living_world_instruction.md`
- `mvp_site/agent_prompts.py`
- `mvp_site/prompts/narrative_system_instruction.md`
- `mvp_site/prompts/game_state_instruction.md`

## Behavior
- no backend hard-fail/crash behavior added
- this change is prompt/instructional only: when LW instructions are included, model is told `state_updates.world_events` is required

## Validation
- `python3 -m py_compile mvp_site/agent_prompts.py`

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Primarily prompt/instruction updates plus dev/test server configuration toggles; low risk but could affect output shape expectations in living-world flows and local test stability.
> 
> **Overview**
> Strengthens the *living-world prompt contract* by explicitly stating that `state_updates.world_events` is **required** whenever living-world advancement is included, including a concrete JSON example and guidance to append only new background events rather than resending history.
> 
> Adds a matching runtime header note in `agent_prompts.py`, aligns narrative docs to describe the cadence as **every 3 turns OR every 24 game hours**, and clarifies faction-minigame guidance to emit only changed fields within `state_updates.custom_campaign_state.faction_minigame`.
> 
> Separately, makes Flask `debug`/`use_reloader` configurable via `WORLDAI_FLASK_DEBUG` and `WORLDAI_USE_RELOADER`, and 
