# PR #6104: fix(avatar): CR nits — constants consolidation, imports, storage error test

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-04-05
**Author:** jleechan2015
**Stats:** +116/-50 in 3 files

## Summary
- Add `AVATAR_EXTENSIONS` / `AVATAR_CONTENT_TYPE_BY_EXT` as single source of truth within `firestore_service.py`; remove all redundant local `allowed_extensions` sets and `content_type_map` dicts. (Note: `main.py` retains its own avatar constants for Flask upload routing — consolidating those is out of scope.)
- Replace bare `import firestore_service` in test methods with module-level `from mvp_site import firestore_service` (consistent canonical patch targets).
- Mock `_get_avatar_extension_for

## Raw Body
## Summary
- Add `AVATAR_EXTENSIONS` / `AVATAR_CONTENT_TYPE_BY_EXT` as single source of truth within `firestore_service.py`; remove all redundant local `allowed_extensions` sets and `content_type_map` dicts. (Note: `main.py` retains its own avatar constants for Flask upload routing — consolidating those is out of scope.)
- Replace bare `import firestore_service` in test methods with module-level `from mvp_site import firestore_service` (consistent canonical patch targets).
- Mock `_get_avatar_extension_for_user` in download unit test to avoid iteration-order non-determinism.
- Add `TestDownloadStorageErrorPropagation` regression tests asserting `RuntimeError` from storage is propagated (not swallowed) for both user and campaign avatar downloads.
- Fix `assertRaises(msg=...)` to `assertRaisesRegex` for proper exception message validation.
- Use sorted `tuple` for `AVATAR_EXTENSIONS` and fully-qualified `@patch` targets (`mvp_site.firestore_service.storage`); remove redundant `sorted()` call in `_probe_avatar_extension`.

Supersedes #6075

## Evidence

```
$ cd /Users/jleechan/.worktrees/worldarchitect/wa-148 && ./vpython -m pytest mvp_site/tests/test_avatar_api_unit.py -q 2>&1 | tail -5
33 passed in 2.45s
```

Pre-commit: ruff 0 violations, mypy warnings only (pre-existing), bandit no high findings.
All 33 avatar unit tests pass including new `TestDownloadStorageErrorPropagation` tests.

Supersedes PR #6075.

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Low risk: mostly refactors avatar MIME/extension constants and adjusts unit tests/patch targets, with added regression coverage for error propagation. Functional behavior should be unchanged aside from more deterministic tests and clearer failure surfacing.
> 
> **Overview**
> **Refactors avatar storage helpers** to use shared `AVATAR_EXTENSIONS` and `AVATAR_CONTENT_TYPE_BY_EXT` constants in `firestore_service.py`, removing duplicated per-function extension/MIME maps and avoiding redundant sorting during ex
