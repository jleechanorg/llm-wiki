# PR #5953: feat(codex): add Codex hooks and exportcommands support

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-03-14
**Author:** jleechan2015
**Stats:** +662/-1 in 7 files

## Summary
- Add `.codex/hooks/` with three hooks: `git-header.sh` (statusline), `pre-exec-hook.sh` (safety validator), `stop-git-header-json.sh` (session summary)
- Add `.codex/hooks.json` wiring pre-exec and stop hooks
- Update `/exportcommands` and `/localexportcommands` to include `.codex/hooks` and `.codex/skills` in exported bundles

## Raw Body
## Summary
- Add `.codex/hooks/` with three hooks: `git-header.sh` (statusline), `pre-exec-hook.sh` (safety validator), `stop-git-header-json.sh` (session summary)
- Add `.codex/hooks.json` wiring pre-exec and stop hooks
- Update `/exportcommands` and `/localexportcommands` to include `.codex/hooks` and `.codex/skills` in exported bundles

## Production Code Changes
- **Before**: Codex had no hooks; exportcommands did not export Codex config
- **Now**: Codex sessions get the same git statusline and safety checks as Claude sessions; both export commands bundle `.codex/hooks` and `.codex/skills`
- **Why**: Parity between Claude and Codex agent environments

## Test Changes
- Added `test_exportcommands.py` covering codex_hooks and codex_skills export paths

## Files changed (7)
- `.codex/hooks.json` — hook wiring config
- `.codex/hooks/git-header.sh` — git/branch statusline for Codex
- `.codex/hooks/pre-exec-hook.sh` — dangerous command safety checks
- `.codex/hooks/stop-git-header-json.sh` — stop hook with token/cost/ctx summary
- `.claude/commands/exportcommands.py` — adds codex_hooks + codex_skills export
- `.claude/commands/localexportcommands.md` — adds `~/.codex/hooks/` install step
- `.claude/commands/tests/test_exportcommands.py` — export tests

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Adds new executable hook scripts and updates export/install flows to copy and enable them, which can affect developer environments and command execution behavior if misconfigured. Changes are localized to tooling/export code and include tests, but involve filesystem permissions and hook wiring.
> 
> **Overview**
> Adds a new Codex automation bundle: `.codex/hooks/` (git status header, pre-exec safety validator, and stop-session JSON summarizer) plus `.codex/hooks.json` to wire the Stop hook.
> 
> Updates `/exportcommands` and `/localexportcommands` to export/install Codex skills + hooks alongside existing Claude assets, including copying `hooks.json`, setting `*
