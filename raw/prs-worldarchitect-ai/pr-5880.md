# PR #5880: fix: robust streaming LLM fallback — correct JSON guard, mock-mode unification, and de-duplicated narrative classifier

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-03-08
**Author:** jleechan2015
**Stats:** +112/-20 in 2 files

## Summary
- **Bug**: Short valid narratives (e.g. \"You wait.\", 9 chars) were silently dropped by an overly-aggressive `len(...) > 20` gate in the streaming fallback path, causing the raw `JSON_PARSE_FALLBACK_MARKER` sentinel to be returned to the client instead of the model's actual output.
- **Bug**: The `mock_services_mode` field in the streaming `done` payload used `os.getenv(\"MOCK_SERVICES_MODE\")` directly, missing per-request mock mode activated via `SMOKE_TOKEN` / Flask `g`.
- **Bug**: The JSON 

## Raw Body
## Summary

- **Bug**: Short valid narratives (e.g. \"You wait.\", 9 chars) were silently dropped by an overly-aggressive `len(...) > 20` gate in the streaming fallback path, causing the raw `JSON_PARSE_FALLBACK_MARKER` sentinel to be returned to the client instead of the model's actual output.
- **Bug**: The `mock_services_mode` field in the streaming `done` payload used `os.getenv(\"MOCK_SERVICES_MODE\")` directly, missing per-request mock mode activated via `SMOKE_TOKEN` / Flask `g`.
- **Bug**: The JSON guard that prevents bare JSON containers from being treated as narrative (`_raw_looks_like_story`) evolved through four fix iterations to reach its correct final form: `not isinstance(_parsed, str)` — excludes all non-string JSON values (dicts, lists, `null`, `true`, `false`, bare numbers) while correctly passing through JSON-quoted string narratives like `\"You wait.\"`.
- **Refactor**: Identical 14-line JSON-classification blocks in Phase 1 and Phase 2 of `continue_story_streaming()` extracted into a single `_classify_raw_narrative(text)` helper, covered by 15 unit tests.

## Why This PR Was Needed

OpenRouter streaming returns raw accumulated text when `parse_structured_response()` cannot parse a valid JSON envelope. This is a valid fast-path: if the model replied with plain prose (\"You wait.\", \"The door opens.\"), the generator should detect that and emit it as the narrative rather than an error.

Before this PR, three separate bugs prevented that fallback from working correctly:

1. **Minimum-length gate** (`len > 20`) rejected concise but valid model replies — any response under 21 characters was silently dropped.
2. **`os.getenv` vs `_is_mock_services_mode()`** — the `done` payload reported `mock_services_mode: false` even when a SMOKE_TOKEN request had activated per-request mock mode through Flask `g`, corrupting observability signals.
3. **JSON guard regressions** — the guard distinguishing real narrative from accidentally-streamed JSON went through fo
