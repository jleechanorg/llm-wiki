# PR #6052: fix(tests): convert 9 async tests to use asyncio.run()

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-03-31
**Author:** jleechan2015
**Stats:** +86/-69 in 4 files

## Summary
9 tests across 4 files used @pytest.mark.asyncio but pytest-asyncio is not installed, causing silent FAILED results.

## Background
9 tests across 4 files used @pytest.mark.asyncio but pytest-asyncio is not installed, causing silent FAILED results.

## Raw Body
## Background
9 tests across 4 files used @pytest.mark.asyncio but pytest-asyncio is not installed, causing silent FAILED results.

## Goals
Fix all 9 broken async tests without adding a new dependency.

## Tenets
No new dependencies. Minimal changes.

## High-level description of changes
Replaced @pytest.mark.asyncio / async def / await with sync def + asyncio.run(). Fixed 2 pre-existing lint issues (compound assertion, unused variable).

## Testing
All 4 changed files: 50 passed, 0 failed (previously 9 failed). Full unit suite: 4369 passed (9 fewer failures).

## Low-level details
- mvp_site/tests/test_openclaw_critical_fixes.py: 2 async tests + lint fixes
- mvp_site/tests/test_campaign_pagination.py: 2 async tests
- mvp_site/tests/test_mode_parameter_type_validation.py: 6 async tests
- mvp_site/tests/test_temporal_correction.py: 1 async test

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Low risk: changes are limited to test code and mainly adjust how async functions are executed without altering production logic.
> 
> **Overview**
> Updates several test modules to stop relying on `pytest-asyncio` by removing `@pytest.mark.asyncio`/`async def`/`await` patterns and running the target coroutines via `asyncio.run()`.
> 
> Also includes small test cleanups (e.g., import adjustments and clearer assertions) to resolve lint/formatting issues while keeping test intent unchanged.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 128eae30b5ae2fcf3bad42bb457f61f1d1a167a2. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
