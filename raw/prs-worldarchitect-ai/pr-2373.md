# PR #2373: Update auth-cli.mjs to support multi-project authentication

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2025-12-10
**Author:** jleechan2015
**Stats:** +399/-170 in 3 files

## Summary
- Sync auth-cli.mjs from ai_universe repo with multi-project support
- Now supports both AI Universe (`ai-universe-b3551`) and WorldAI (`worldarchitecture-ai`)
- Add `--project` flag for switching between Firebase projects
- Update ai-universe-auth.md skill documentation

## Test Plan
- [x] Verify `node auth-cli.mjs status` works
- [x] Verify `node auth-cli.mjs token` returns valid token
- [ ] Test `/secondo` command with updated script

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Revamps auth CLI to support selectable Firebase projects with per-project config/storage and strict token project validation, plus updated docs and a new WorldAI auth guide.
> 
> - **CLI/Auth (`.claude/scripts/auth-cli.mjs`)**
>   - Add mu

## Raw Body
## Summary
- Sync auth-cli.mjs from ai_universe repo with multi-project support
- Now supports both AI Universe (`ai-universe-b3551`) and WorldAI (`worldarchitecture-ai`)
- Add `--project` flag for switching between Firebase projects
- Update ai-universe-auth.md skill documentation

## Changes
- `.claude/scripts/auth-cli.mjs` - Multi-project auth script
- `.claude/skills/ai-universe-auth.md` - Updated documentation

## Test plan
- [x] Verify `node auth-cli.mjs status` works
- [x] Verify `node auth-cli.mjs token` returns valid token
- [ ] Test `/secondo` command with updated script

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Revamps auth CLI to support selectable Firebase projects with per-project config/storage and strict token project validation, plus updated docs and a new WorldAI auth guide.
> 
> - **CLI/Auth (`.claude/scripts/auth-cli.mjs`)**
>   - Add multi-project support with `--project/-p` flag and known configs (`ai-universe-b3551`, `worldarchitecture-ai`); derives env var prefixes per project.
>   - Store tokens per project at `~/.ai-universe/auth-token-<project-id>.json`; include `firebaseProjectId` in token data.
>   - Enforce config correctness and validate JWTs belong to the active Firebase project (on login, token read, and refresh).
>   - Auto-refresh preserves/rotates refresh tokens; `status` shows active Firebase project; `token` now requires valid config.
>   - Update MCP test to use `productionMcpUrl` and `get_second_opinion` tool; refine errors and UX messages.
> - **Docs**
>   - Update `ai-universe-auth.md` with multi-project usage, locations, troubleshooting, and examples.
>   - Add new `worldai-auth.md` skill with WorldAI-specific commands and storage path details.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit b79986a1641c45b3437a97f6f22f9a5a8b7754cf. This will update automatically on new commits. Configure [here](https://cursor.com/dashbo
