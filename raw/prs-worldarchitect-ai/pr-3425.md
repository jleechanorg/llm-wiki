# PR #3425: docs: Make guardrails handoff evidence reproducible and fix beads JSON formatting

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-01-10
**Author:** jleechan2015
**Stats:** +8/-1 in 2 files
**Labels:** codex

## Summary
(none)

## Raw Body
### Motivation
- Replace an ephemeral machine-local evidence path with a reproducible command so future maintainers can regenerate validation artifacts. 
- Fix malformed JSON in the beads issue entry to avoid parsing/display problems in tooling that reads `.beads/issues.jsonl`.
- Preserve handoff usefulness by making the reproduction steps explicit and portable. 

### Description
- Updated the roadmap entry in `roadmap/roadmap.md` to replace the `/tmp/...` evidence path with a reproducible command: `python testing_mcp/test_llm_guardrails_exploits.py --evidence` and a pointer to the scratchpad for setup. 
- Normalized escaping in the new bead entry in `.beads/issues.jsonl` so the `description` field contains real newlines/quotes and parses as valid JSON. 
- Committed the changes with message `Docs: Fix handoff evidence references [codex-automation-commit]` and staged the modified files `roadmap/roadmap.md` and `.beads/issues.jsonl`.

### Testing
- Ran pre-commit checks as part of `git commit` and the hooks passed successfully. 
- Verified that the updated `.beads/issues.jsonl` now parses as valid JSON using a local JSON load check. 
- No unit or integration tests were required because this is documentation-only work.

------
[Codex Task](https://chatgpt.com/codex/tasks/task_e_69619458a3d8832fba72396bf6710c68)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> - **Roadmap**: Replace ephemeral `/tmp/...` evidence path with a reproducible command (`python testing_mcp/test_llm_guardrails_exploits.py --evidence`) and pointer to setup docs.
> - **Beads JSONL**: Normalize/escape descriptions in `.beads/issues.jsonl` so entries are valid JSON with real newlines/quotes, improving parsing by downstream tooling.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 687d8f58ba10afcfbd0ace583e84b3ed1f7272f1. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
