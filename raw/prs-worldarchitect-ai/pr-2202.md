# PR #2202: Centralize timeout configuration

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2025-11-30
**Author:** jleechan2015
**Stats:** +163/-31 in 13 files
**Labels:** codex

## Summary
- add `scripts/timeout_config.sh` to export the shared 10-minute timeout and source it from deployment scripts while passing Cloud Run env vars
- align Gunicorn, MCP client, and both frontends to `WORLDARCH_TIMEOUT_SECONDS`, with docs (README/DEPLOYMENT/CLAUDE/AGENTS) reflecting the centralized guardrail
- extend timeout tests to cover the new environment override path

## Raw Body
## Summary
- add `scripts/timeout_config.sh` to export the shared 10-minute timeout and source it from deployment scripts while passing Cloud Run env vars
- align Gunicorn, MCP client, and both frontends to `WORLDARCH_TIMEOUT_SECONDS`, with docs (README/DEPLOYMENT/CLAUDE/AGENTS) reflecting the centralized guardrail
- extend timeout tests to cover the new environment override path

## Testing
- python -m pytest mvp_site/tests/test_gunicorn_config.py infrastructure/tests/test_gunicorn_integration.py | nl -ba

------
[Codex Task](https://chatgpt.com/codex/tasks/task_e_692c8251ece4832fac94cee21d99c008)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Centralizes the 600s timeout via scripts/timeout_config.sh and updates Gunicorn, MCP client, Cloud Run deploys, and both frontends to use WORLDARCH_TIMEOUT_SECONDS with corresponding tests and docs.
> 
> - **Infrastructure/Deploy**:
>   - Add `scripts/timeout_config.sh` exporting `WORLDARCH_TIMEOUT_SECONDS` (+ ms/min variants and frontend build vars).
>   - Source and apply in `deploy.sh` and `deploy_mcp.sh`; pass env vars and set service/load balancer timeouts from the centralized value.
> - **Backend**:
>   - Gunicorn config (`mvp_site/gunicorn.conf.py`) reads `WORLDARCH_TIMEOUT_SECONDS` and sets `timeout = REQUEST_TIMEOUT_SECONDS`.
>   - MCP client (`infrastructure/mcp_helpers.py`) uses centralized timeout for `MCPClient(timeout=...)`.
> - **Frontend**:
>   - v1 (`mvp_site/frontend_v1/api.js`): Add centralized request timeout, AbortController-based request/healthcheck timeouts; preserve external signals.
>   - v2 (`mvp_site/frontend_v2/src/services/api.service.ts`): Use env-derived `REQUEST_TIMEOUT_MS`; raise timeouts for create/interaction to default 10m.
> - **Tests**:
>   - Update/extend Gunicorn integration and unit tests to assert 600s default and honor `WORLDARCH_TIMEOUT_SECONDS` override (`infrastructure/tests/test_gunicorn_integration.py`, `mvp_site/tests/test_gunicorn_config.py`).
> - **Docs**:
>   - Add/clarify 600s t
