# PR #5781: docs(GEMINI): add SafeToAutoRun=true policy for git commit/push

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-02-26
**Author:** jleechan2015
**Stats:** +13/-0 in 1 files

## Summary
NEVER set `SafeToAutoRun=false` for `git commit` or `git push`. These are the most common cause of hung commands.

### Changes
- Added a **Safe-to-Auto-Run Commands** table under the Shell Command Safety section listing all git, gh CLI, and build/test commands that must always use `SafeToAutoRun=true`
- Added an `[!IMPORTANT]` callout explicitly prohibiting `SafeToAutoRun=false` for `git commit` and `git push`

### Rationale
A commit is local-only. A push has no destructive local side effects, a

## Raw Body
## Summary

NEVER set `SafeToAutoRun=false` for `git commit` or `git push`. These are the most common cause of hung commands.

### Changes
- Added a **Safe-to-Auto-Run Commands** table under the Shell Command Safety section listing all git, gh CLI, and build/test commands that must always use `SafeToAutoRun=true`
- Added an `[!IMPORTANT]` callout explicitly prohibiting `SafeToAutoRun=false` for `git commit` and `git push`

### Rationale
A commit is local-only. A push has no destructive local side effects, and the existing anti-hang policy handles timeouts. Setting `SafeToAutoRun=false` causes the agent to wait for user approval, which leads to hung commands when running in automated workflows.

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Documentation-only change that tightens agent command-execution guidance; no runtime code paths are affected.
> 
> **Overview**
> Adds an explicit **“Safe-to-Auto-Run Commands”** section to `GEMINI.md` enumerating `git`, `gh`, and common build/test commands that must always run with `SafeToAutoRun=true`.
> 
> Also adds an *IMPORTANT* callout forbidding `SafeToAutoRun=false` specifically for `git commit` and `git push` to reduce hangs in automated workflows.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 61f1b82933bf52408cf0b7bd9dfcc3678600a63c. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->

## Summary by CodeRabbit

* **Documentation**
  * Enhanced Shell Safety policy with Safe-to-Auto-Run Commands guidance
  * Documented safe command categories including git, GitHub CLI, and build/test commands
  * Added security warning for git commit and git push operations

<!-- end of auto-generated comment: release notes by coderabbit.ai -->
