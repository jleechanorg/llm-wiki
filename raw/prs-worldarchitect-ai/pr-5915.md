# PR #5915: fix(smoke): decouple SMOKE_TOKEN auth from mock LLM activation

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-03-09
**Author:** jleechan2015
**Stats:** +23/-6 in 2 files

## Summary
- Real E2E `/smoke` tests were receiving mock LLM responses because `SMOKE_TOKEN` unconditionally activated `g.mock_services_mode = True` server-side
- Scenario 7 (streaming timing contracts) detected `mock_services_mode=True` in the done payload → failed as "mock fallback in real mode"
- This is the root cause of the `[Real E2E] MCP Smoke Tests Failed` on PR #5895

## Raw Body
## Summary

- Real E2E `/smoke` tests were receiving mock LLM responses because `SMOKE_TOKEN` unconditionally activated `g.mock_services_mode = True` server-side
- Scenario 7 (streaming timing contracts) detected `mock_services_mode=True` in the done payload → failed as "mock fallback in real mode"
- This is the root cause of the `[Real E2E] MCP Smoke Tests Failed` on PR #5895

## Production Code Changes

**Before**: `main.py` SMOKE_TOKEN handler always set `g.mock_services_mode = True` when token matched

**Now**: Server only activates mock LLM when client also sends `X-Mock-Services: true`. SMOKE_TOKEN alone only bypasses auth.

**Why**: `/smoke` (real E2E) and mock CI smoke tests both send `SMOKE_TOKEN` for auth, but only mock tests should trigger mock LLM. The header `X-Mock-Services: true` is now the explicit signal.

## Test Changes

`testing_mcp/lib/base_test.py`: Only sends `X-Mock-Services: true` when `MCP_TEST_MODE != real`. Backward compatible — mock smoke tests (no `MCP_TEST_MODE=real`) keep existing mock behavior.

## Known Limitations

Pre-existing test failures (unrelated, present on main):
- `test_smoke_token_does_not_authenticate_noncanonical_stream_like_path_on_preview` (405 vs 401)
- `TestREVj18HexFilenameFalsePositives::test_known_false_positive_patterns`

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Small, targeted change to test-only authentication/mocking toggles; primary risk is misconfigured headers causing smoke tests to run with unexpected real/mock behavior.
> 
> **Overview**
> Real smoke/E2E runs authenticated via `SMOKE_TOKEN` no longer automatically enable mock services. Server-side, the preview `SMOKE_TOKEN` auth-bypass path now only sets `g.mock_services_mode` when the client also sends `X-Mock-Services: true`, and logs whether the request is running in real vs mock mode.
> 
> Test harness logic in `testing_mcp/lib/base_test.py` is updated to send `X-Mock-Serv
