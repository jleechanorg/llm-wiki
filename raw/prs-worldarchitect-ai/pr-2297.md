# PR #2297: Add diagnostic logging for empty narrative detection

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2025-12-03
**Author:** jleechan2015
**Stats:** +70/-6 in 1 files

## Summary
- Add INFO-level logging for parsed LLM responses (narrative_length, structured_response presence, raw_response_length)
- Add WARNING-level logging when narrative is empty, including raw response preview and structured field status  
- Upgrade final response logging from DEBUG to INFO for production visibility
- Apply same diagnostic logging to both `continue_story()` and `get_initial_story()` functions

## Test Plan
- [x] Verified syntax tests pass
- [x] Verified JSON mode preference tests pass
- [x] Verified imports work correctly

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Add INFO-level parsed-response diagnostics and WARNINGs for empty narratives, and promote final-response logs to INFO in both `get_initial_story()` and `continue_story()`.
> 
> - **Logging (diagnostics and visibility)**
>   - **Parsed response diagnostics**: In `get_initial_s

## Raw Body
## Summary

- Add INFO-level logging for parsed LLM responses (narrative_length, structured_response presence, raw_response_length)
- Add WARNING-level logging when narrative is empty, including raw response preview and structured field status  
- Upgrade final response logging from DEBUG to INFO for production visibility
- Apply same diagnostic logging to both `continue_story()` and `get_initial_story()` functions

## Context

This addresses the investigation of "[Error: No response from server]" issues where LLM returns valid JSON but with an empty `narrative` field. The new logging will help diagnose:

1. Whether the raw LLM response was received
2. What the response contained (preview of first 500 chars)
3. Whether structured fields (planning_block, session_header) were present despite empty narrative

## Example Log Output

**For every LLM response:**
```
📊 PARSED_RESPONSE: narrative_length=1234, structured_response=present, raw_response_length=5678
📝 FINAL_RESPONSE: narrative_length=1234, has_structured_response=True
```

**If narrative is empty:**
```
⚠️ EMPTY_NARRATIVE: LLM returned empty narrative. Raw response preview: {"session_header": "...", ...
⚠️ EMPTY_NARRATIVE: structured_response has planning_block=True, session_header=True
```

## Test plan

- [x] Verified syntax tests pass
- [x] Verified JSON mode preference tests pass
- [x] Verified imports work correctly

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Add INFO-level parsed-response diagnostics and WARNINGs for empty narratives, and promote final-response logs to INFO in both `get_initial_story()` and `continue_story()`.
> 
> - **Logging (diagnostics and visibility)**
>   - **Parsed response diagnostics**: In `get_initial_story()` and `continue_story()`, log `narrative_length`, `structured_response` presence, and `raw_response_length`.
>   - **Empty narrative warnings**: When `narrative_text` is empty, log a preview of the raw response (f
