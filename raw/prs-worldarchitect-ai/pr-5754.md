# PR #5754: chore: add shared classifier preflight and startup gating

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-03-04
**Author:** jleechan2015
**Stats:** +306/-10 in 8 files

## Summary
- Add shared preflight script for classifier model cache repair + warmup.
- Hook run_local_server.sh to preflight-check classifier before launching Flask.
- Replace Dockerfile inline model bootstrap with shared preflight_model.py.
- Add server startup gate in main.py so startup fails fast if semantic classifier is enabled and fails to initialize.

## Raw Body
## Summary
- Add shared preflight script for classifier model cache repair + warmup.
- Hook run_local_server.sh to preflight-check classifier before launching Flask.
- Replace Dockerfile inline model bootstrap with shared preflight_model.py.
- Add server startup gate in main.py so startup fails fast if semantic classifier is enabled and fails to initialize.

## Files
- scripts/preflight_model.py
- mvp_site/Dockerfile
- mvp_site/main.py
- run_local_server.sh

## Notes
Runtime integration PR depends on PR for classifier cache-heal internals:
- 

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Medium risk because it rewires multiple self-hosted GitHub Actions workflows (removing composite wrapper layers and changing checkout/execution paths) and alters production startup behavior via lazy imports + classifier fail-fast gating, which could cause CI or runtime regressions if assumptions differ in runner/prod environments.
> 
> **Overview**
> **Simplifies self-hosted CI execution** by deleting the `container-run` and `runner-control-plane` composite actions and removing `run-in-ci-container.sh` usage across workflows; workflows now run commands directly, standardize `runs-on` defaults to `SELF_HOSTED_RUNNER_LABELS || ["self-hosted"]`, and update tests/docs to ban references to the deleted wrappers.
> 
> **Improves cold-start and startup reliability/perf** by adding lazy-loading for heavy modules (Firestore + `google.genai`) in `mvp_site.main`, `mvp_site.streaming_orchestrator`, and `mvp_site.llm_providers`, adding gzip compression via `flask-compress`, and enforcing classifier readiness (cache validation/repair + optional startup timeout/fail-fast) including new build/runtime preflight scripts wired into the Docker build.
> 
> **Updates runtime + UX performance knobs**: raises Cloud Run `containerConcurrency` in `deploy.sh`, adds async CSS loading + deferred scripts + DNS preconnect/prefetch in `frontend_v1`, and introduces a fantasy-theme ember canvas backgrou
