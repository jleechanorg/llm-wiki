# PR #5870: fix(orch): derive --agent-cli help from CLI_PROFILES (adds minimax)

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-03-07
**Author:** jleechan2015
**Stats:** +7/-6 in 6 files

## Summary
- `--agent-cli` help text was hardcoded as `claude, codex, gemini, cursor` — missing `minimax`
- Now derives the list dynamically from `CLI_PROFILES.keys()` so any future additions are automatic

## Raw Body
## Summary
- `--agent-cli` help text was hardcoded as `claude, codex, gemini, cursor` — missing `minimax`
- Now derives the list dynamically from `CLI_PROFILES.keys()` so any future additions are automatic

## Changes
- `orchestration/cli_args.py`: replace hardcoded string with `', '.join(sorted(CLI_PROFILES.keys()))`

## Testing
```
$ ai_orch run --help
--agent-cli AGENT_CLI
    Agent CLI to use: claude, codex, cursor, gemini, minimax. ...
```

## Known Limitations
None — one-line change, no behavior change.

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Help-text and documentation updates only; behavior of CLI selection remains unchanged aside from exposing the already-supported `minimax` option in `--help` output.
> 
> **Overview**
> Updates orchestration CLI UX so `--agent-cli` help text is generated dynamically from `CLI_PROFILES.keys()` (instead of a hardcoded list), ensuring new CLIs like `minimax` automatically appear in `--help` output.
> 
> Refreshes `/orch` and `/orchestrate` command docs (Claude + Gemini command definitions) and adjusts a test description to reflect the expanded supported CLI list.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit dad1bd72c74ee64380a3db832a6d742a92da0309. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->
## Summary by CodeRabbit

* **New Features**
  * Added minimax as a supported agent CLI option alongside existing agent choices.

* **Documentation**
  * Updated CLI help and documentation to list minimax among supported agent CLI options and adjusted ordering in descriptions.
<!-- end of auto-generated comment: release notes by coderabbit.ai -->
