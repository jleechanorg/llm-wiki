# PR #6192: fix(frontend): show rewards_box when xp_gained=0

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-04-11
**Author:** jleechan2015
**Stats:** +162/-0 in 3 files

## Summary
- Adds TDD regression tests to prevent re-introduction of bead **rev-qcax** bug
- Bug: `if (fullData.rewards_box && fullData.rewards_box.xp_gained > 0)` in `app.js` line 924 hid the rewards box when `xp_gained == 0` (loot-only drops, gold awards, level-up scenarios without XP)
- The fix (`if (fullData.rewards_box)`) was already applied in PR #6161 (commit `a872098d7c`); this PR adds regression coverage so the fix cannot silently regress

## Raw Body
## Summary

- Adds TDD regression tests to prevent re-introduction of bead **rev-qcax** bug
- Bug: `if (fullData.rewards_box && fullData.rewards_box.xp_gained > 0)` in `app.js` line 924 hid the rewards box when `xp_gained == 0` (loot-only drops, gold awards, level-up scenarios without XP)
- The fix (`if (fullData.rewards_box)`) was already applied in PR #6161 (commit `a872098d7c`); this PR adds regression coverage so the fix cannot silently regress

## Production Code Changes

No production code changes — the fix was already shipped in PR #6161. This PR adds tests only.

## Test Changes

- **`mvp_site/tests/test_rewards_box_xp_zero_condition.py`** — 4 pytest assertions verifying:
  1. The buggy `xp_gained > 0` guard is absent from `app.js`
  2. The correct `if (fullData.rewards_box)` truthiness check is present
  3. `rb.xp_gained` is still read for display (regression guard)
  4. The `rewards-box` CSS class is still referenced

- **`mvp_site/tests/frontend/test_rewards_box_xp_zero.js`** — 3 Node.js static-analysis checks (same coverage, runs without vpython)

## Known Limitations

None — static analysis only, no runtime browser test required since the fix is already verified in production via PR #6161.

## Related

- Bead: rev-qcax
- Fixes: PR #6161 missed adding a regression test for the `xp_gained > 0` guard removal

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Low risk: adds only static-analysis regression tests and an issues metadata entry, with no production code changes or behavior changes.
> 
> **Overview**
> Adds regression tests to ensure the frontend `rewards_box` rendering is triggered by the presence of `fullData.rewards_box` and does **not** reintroduce the old `xp_gained > 0` guard that hid loot-only/gold-only/zero-XP rewards.
> 
> The new tests (Node and Python) also assert `rb.xp_gained` is still referenced for display and that the `rewards-box` section remains present. Updates `.beads/issues.jsonl` to record the related bead (`rev-qcax`).
