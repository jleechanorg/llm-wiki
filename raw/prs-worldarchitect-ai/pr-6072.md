# PR #6072: docs: /exportcommands orchestrated run with --agent-cli minimax

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-04-02
**Author:** jleechan2015
**Stats:** +561/-1400 in 9 files

## Summary
- Document how to run the `/exportcommands` workflow via Agent Orchestrator / `ai_orch` / `orchestrate_unified.py` using `--agent-cli minimax` (Claude Code via MiniMax API).
- Add frontmatter `argument-hint` so orchestrator UIs can surface the CLI pairing.
- Add a small `tests/` regression that locks the documented strings.

## Raw Body
## Summary
- Document how to run the `/exportcommands` workflow via Agent Orchestrator / `ai_orch` / `orchestrate_unified.py` using `--agent-cli minimax` (Claude Code via MiniMax API).
- Add frontmatter `argument-hint` so orchestrator UIs can surface the CLI pairing.
- Add a small `tests/` regression that locks the documented strings.

## Production / doc changes
- **Before**: `exportcommands.md` had no orchestration flag guidance for MiniMax.
- **Now**: Examples for `python3 orchestration/orchestrate_unified.py --agent-cli minimax` and `ai_orch run --agent-cli minimax`, plus `MINIMAX_API_KEY` and `CLI_PROFILES` pointers.

## Test changes
- `tests/test_exportcommands_agent_cli_docs.py` asserts the markdown contains the required snippets.

## Known limitations
- Does not change `exportcommands.py` behavior; orchestration flags are interpreted by `ai_orch` / orchestration, not the Python exporter script itself.


Made with [Cursor](https://cursor.com)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Medium risk because it changes the `/exportcommands` publishing flow (new `--dry-run`, token/remote handling, and stdout URL contract) and adjusts GitHub Actions comment/poll logic, which could affect automation behavior and CI gating.
> 
> **Overview**
> **Updates `/exportcommands` to a shell-first, contract-driven entrypoint.** The huge narrative in `exportcommands.md` is replaced with concise execution instructions, adds frontmatter `argument-hint`, documents orchestrated runs via `ai_orch`/`orchestration/runner.py` with `--agent-cli minimax`, and formalizes the final-stdout-line URL contract.
> 
> **Adds a canonical wrapper and a safe dry-run path.** New `exportcommands.sh` delegates to `exportcommands.py`; the exporter now supports `--dry-run` to fully build/clone/copy/README-generate while skipping commit/push/PR creation, avoids writing token-bearing remotes during dry-run, and prints the PR/compare URL only once as the final stdout line.
> 
> **Hardens r
