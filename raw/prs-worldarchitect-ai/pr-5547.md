# PR #5547: Codex Web: share setup logic; support GCLOUD_SERVICE_ACCOUNT_JSON_B64

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-02-15
**Author:** jleechan2015
**Stats:** +440/-82 in 3 files

## Summary
Improve Codex Web bootstrap reliability by making setup + maintenance share the same implementation, and ensuring `GCLOUD_SERVICE_ACCOUNT_JSON_B64` (base64) service-account auth works out of the box.

## Test Plan
- `bash scripts/tests/test_codex_web_setup_sandbox.sh`

## Raw Body
## Summary
Improve Codex Web bootstrap reliability by making setup + maintenance share the same implementation, and ensuring `GCLOUD_SERVICE_ACCOUNT_JSON_B64` (base64) service-account auth works out of the box.

## Changes
- Refactor `scripts/codex_web_setup.sh` to expose `codex_web_setup_main()` and keep script runnable directly.
- Update `scripts/codex_web_maintenance.sh` to source setup and call `codex_web_setup_main` (shared code path).
- Harden `GCLOUD_SERVICE_ACCOUNT_JSON_B64` decoding by stripping whitespace and validating JSON when decoding via python.

## Test Plan
- `bash scripts/tests/test_codex_web_setup_sandbox.sh`

## Notes
- This keeps supporting raw `GCLOUD_SERVICE_ACCOUNT_JSON` as well.

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Touches bootstrap/auth wiring and adds new download/install paths (curl/apt/brew/npm), which could fail or behave differently across environments. Changes are mostly additive and guarded with fallbacks, but impact developer/CI setup reliability.
> 
> **Overview**
> **Maintenance now reuses setup logic** by sourcing `codex_web_setup.sh` and invoking a new `codex_web_setup_main()` entrypoint, instead of running setup as a separate process.
> 
> **Setup bootstrap is expanded and more robust**: gcloud service-account auth now supports both `GCLOUD_SERVICE_ACCOUNT_JSON` and `GCLOUD_SERVICE_ACCOUNT_JSON_B64` (with whitespace stripping and JSON validation), and the script optionally installs/records additional tooling (`npm`, `kubectl`, `terraform`, `firebase`). The marker file `/tmp/codex_web_setup_status.env` is extended to capture these tool paths plus `GCLOUD_AUTH_SOURCE`, and the sandbox test is updated to assert the new marker fields.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit fb09b88c0fa67cfbfb5b24ddb9fc16754f6acba0. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
