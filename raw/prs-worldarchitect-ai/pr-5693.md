# PR #5693: fix: forward response_json_schema and enable strict_narrative for streaming

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-03-01
**Author:** jleechan2015
**Stats:** +22467/-8454 in 145 files
**Labels:** codex

## Summary
(none)

## Raw Body
### Motivation
- Ensure schema-aware structured output is actually enforced during streaming story continuations by passing the caller-supplied schema through provider streaming calls. 
- Make the `strict_narrative` parsing mode functional so non-god-mode streaming responses are validated more strictly. 
- Stabilize the new streaming test by making its prepared mock deterministic for MODE and user lookup.

### Description
- Forward `response_json_schema` into the Gemini streaming `config_params` when `json_mode=True` so provider calls receive the schema. 
- Make `parse_structured_response` respect `strict_narrative` by treating `god_mode_response` as a non-signal in strict mode, rejecting parsed JSON that lacks narrative-compatible fields when strict, and avoiding god-mode narrative combination when strict is active. 
- Update `test_continue_story_streaming_phase2_includes_schema_in_character_mode` to set `prepared.agent.MODE = "character"` and `prepared.user_id = "u1"` for deterministic mocks. 
- Post a consolidated `/commentreply` summary for the PR review thread and include an automation tracking marker in the local commit message.

### Testing
- Compiled the modified modules with `python -m py_compile mvp_site/llm_providers/gemini_provider.py mvp_site/narrative_response_schema.py mvp_site/tests/test_llm_service_context.py`, which succeeded. 
- Attempted to run the selected pytest suite with `python3 -m pytest -q mvp_site/tests/test_llm_service_context.py -k "phase2_includes_schema_in_character_mode or uses_json_mode_for_streaming_calls or emits_tool_and_state_events"`, which could not run in this environment due to a missing virtualenv activation script and an unavailable dependency (`flask`). 
- Executed the consolidated comment posting via `python3 .claude/commands/commentreply.py jleechanorg worldarchitect.ai 5691`, which completed and posted the consolidated summary comment to the PR successfully.

------
[Codex Task](https://chatgpt.com/codex/tasks/task_e_6
