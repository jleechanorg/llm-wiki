# PR #5495: Fix OpenClaw MCP script review issues

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-02-14
**Author:** jleechan2015
**Stats:** +267/-72 in 3 files

## Summary
(none)

## Raw Body
Follow-up fixes for OpenClaw MCP integration review items: token parsing, MCP env flags, setup script error handling, Linux systemd support, cron log handling, and README MCP alignment.

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Touches bootstrap/installer scripts that manage services and credentials (gateway token parsing and MCP config), so failures could break local agent setup across macOS/Linux, but scope is limited to developer tooling.
> 
> **Overview**
> Improves OpenClaw MCP integration by adding `openclaw` to the installer list and hardening the OpenClaw MCP setup path: token lookup now honors `OPENCLAW_URL`, parses `~/.openclaw/openclaw.json` via `jq` with an `awk` fallback, uses shared `DEFAULT_MCP_ENV_FLAGS`, and safely removes/reinstalls across scopes (optionally adding a user-scope entry when installed locally).
> 
> Enhances `setup-openclaw.sh` for resilience and Linux support: resolves the `openclaw` binary, provides clearer npm install guidance on failure, makes most setup steps non-fatal via `run_optional_command`, adds Linux `systemd --user` auto-start unit generation, and strengthens the cron health-check (expanded PATH, log truncation, and `crontab` presence checks). Documentation (`README-OPENCLAW.md`) is updated to reflect MCP support, Linux restart/backup commands, and optional log backups.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 645e5c9d5ce2a5142447eac99bd3bc9f5da9fada. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->
## Summary by CodeRabbit

* **New Features**
  * OpenClaw MCP server support with automated install, gateway auto-start, health checks and backup guidance
  * "Think" (planning) mode for LLM flows and related runtime behavior

* **Tests**
  * Dynamic smoke test modes with mock/real configs, added God 
