# PR #5828: feat(automation): run codex-update flow in headless mode

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-03-03
**Author:** jleechan2015
**Stats:** +12/-20 in 2 files

## Summary
(none)

## Raw Body
## Summary\n- switch  path to run Codex automation in headless mode\n- launch a fresh browser () instead of requiring existing CDP Chrome\n- add  CLI flag to  and wire it to Playwright launch\n\n## Validation\n-                                 [--use-existing-browser]
                                [--launch-new-browser] [--cdp-host CDP_HOST]
  --use-existing-browser
  --launch-new-browser  Launch a new browser instead of using existing one\n\n## Notes\n- This keeps existing manual headed/CDP options available, while making the monitor's codex-update automation run headless by default.

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Changes the `--codex-update` execution path to launch a new headless browser instead of connecting to an existing CDP session, which may affect automation reliability/login behavior and existing workflows that relied on pre-launched Chrome.
> 
> **Overview**
> `--codex-update` in `jleechanorg_pr_monitor.py` now runs Codex GitHub-mentions automation by **launching a fresh headless browser** (uses `--launch-new-browser --headless`) and removes the prior CDP accessibility pre-check/host+port wiring.
> 
> `codex_github_mentions.py` adds a `--headless` CLI flag and passes it through to `CodexGitHubMentionsAutomation`, enabling headless/headed selection while keeping existing `--use-existing-browser` vs `--launch-new-browser` modes.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 751facaa911de57a91c99f9b4ebd3501bf168789. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->

## Summary by CodeRabbit

* **Chores**
  * Updated automation startup flow to use headless browser mode, removing pre-flight accessibility checks.
  * Simplified browser initialization with new CLI flags for improved reliability and streamlined operation.
  * Refr
