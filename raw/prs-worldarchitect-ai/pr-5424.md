# PR #5424: Docs: finalize beads config and workspace guidance

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-02-13
**Author:** jleechan2015
**Stats:** +114/-77 in 5 files

## Summary
- Normalize Beads ignore/state patterns for local branch behavior.
- Add Beads sync branch setting in `.beads/config.yaml`.
- Document Beads usage with a new `.beads/README.md`.
- Add repo session-completion notes in `AGENTS.md`.
- Remove stale `.beads/beads.left.jsonl` from tracked state.

## Raw Body
## Summary
- Normalize Beads ignore/state patterns for local branch behavior.
- Add Beads sync branch setting in `.beads/config.yaml`.
- Document Beads usage with a new `.beads/README.md`.
- Add repo session-completion notes in `AGENTS.md`.
- Remove stale `.beads/beads.left.jsonl` from tracked state.

## Testing
- `git pull origin main` succeeded (after stashing local changes because of `.beads/issues.jsonl` skip-workspace sync state).
- No additional test suite was run (docs/config update only).

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Docs/config-only changes plus cleanup of a tracked export file; low risk aside from potentially hiding/retaining local state files via updated ignore patterns.
> 
> **Overview**
> This PR formalizes Beads workspace usage: it adds a `.beads/README.md` quickstart, pins Beads syncing to a dedicated `sync-branch: "beads-sync"` in `.beads/config.yaml`, and extends `AGENTS.md` with a mandatory session wrap-up checklist emphasizing `bd sync` and `git push`.
> 
> It also normalizes `.beads/.gitignore` to better separate machine-local state/locks (`.local_version`, `.jsonl.lock`, `export-state/`) from tracked data, and removes the stale tracked `.beads/beads.left.jsonl` export artifact.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 6f271f05fc5e6ca1abfa53785a931ba76e09ffac. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
