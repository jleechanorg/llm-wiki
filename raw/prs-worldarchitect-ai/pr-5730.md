# PR #5730: fix(ci): add SMOKE_TOKEN to preview-smoke-tests job env

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-02-23
**Author:** jleechan2015
**Stats:** +3/-1 in 1 files

## Summary
- Adds `SMOKE_TOKEN` and `STREAM_RESPONSE_SIGNING_SECRET` to the `preview-smoke-tests` job env
- Unblocks `/smoke` comments on all PRs immediately

## Raw Body
## Summary
- Adds `SMOKE_TOKEN` and `STREAM_RESPONSE_SIGNING_SECRET` to the `preview-smoke-tests` job env
- Unblocks `/smoke` comments on all PRs immediately

## Production Code Changes
**Before**: `preview-smoke-tests` job env missing `SMOKE_TOKEN` → every `/smoke` comment failed with _"SMOKE_TOKEN is required for remote /mcp smoke tests."_

**Now**: Both secrets passed to the job env → `mcp-smoke-tests.mjs` can authenticate against the preview server

**Why**: `issue_comment` triggered workflows always run from the default branch. The fix was on a feature branch but could never take effect until merged to main.

## Test Changes
None — CI-only change.

## Known Limitations
None.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> CI-only workflow changes that adjust trigger conditions and pass secrets to the job; low risk aside from potentially blocking smoke runs for untrusted commenters.
> 
> **Overview**
> **Tightens and fixes the `/smoke`-triggered Preview E2E workflow.** The `preview-smoke-tests` job now only runs for `/smoke` issue comments from trusted actors (`OWNER`/`MEMBER`/`COLLABORATOR`).
> 
> It also injects the missing secrets into the job environment (`SMOKE_TOKEN` and `STREAM_RESPONSE_SIGNING_SECRET`) so the preview smoke tests can authenticate/sign requests during execution.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit afebfde5aabef42f45f4216015d88589829d0ddf. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
