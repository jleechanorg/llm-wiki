# PR #5636: Add zoomed-out mid-stream screenshots for streaming mode-matrix runs

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-02-19
**Author:** jleechan2015
**Stats:** +30/-0 in 2 files

## Summary
- add optional mid-stream screenshot capture in `StreamingUiTestBase.submit_and_wait_for_completion`
- capture a zoomed-out full-page screenshot during active streaming (`*_mid_stream_zoomed_out`)
- enable this capture in `testing_ui/streaming/test_streaming_mode_matrix.py` for character/think/god runs

## Raw Body
## Summary
- add optional mid-stream screenshot capture in `StreamingUiTestBase.submit_and_wait_for_completion`
- capture a zoomed-out full-page screenshot during active streaming (`*_mid_stream_zoomed_out`)
- enable this capture in `testing_ui/streaming/test_streaming_mode_matrix.py` for character/think/god runs

## Why
- preserves normal post-done screenshots while adding a whole-screen mid-stream visual artifact for debugging and review
- keeps evidence in the standard `/tmp/worldarchitectai/...` path via existing `testing_ui` evidence plumbing

## Validation
- Executed `testing_ui` mode-matrix flows and verified per-mode mid-stream screenshot artifacts are produced in `/tmp/worldarchitectai/<branch>/...`.

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Test-only changes that add a best-effort screenshot side effect; main risk is minor flakiness or altered rendering due to temporary zoom manipulation during streaming.
> 
> **Overview**
> Adds an optional `capture_mid_stream_screenshot` flag to `StreamingUiTestBase.submit_and_wait_for_completion` to take a **single best-effort mid-stream screenshot** when an entry is actively streaming.
> 
> When enabled, the helper temporarily zooms the page out (`documentElement.style.zoom = '0.7'`), captures `"{mode}_mid_stream_zoomed_out"`, then restores zoom; `test_streaming_mode_matrix.py` turns this on for each mode run while keeping the existing post-completion screenshot.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit b6522b0e08536780bf7f8d3b500cb31b3d989ab8. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->

## Summary by CodeRabbit

* **New Features**
  * Added optional mid-stream screenshot capture capability during streaming test execution. Screenshots automatically zoom out for improved visibility of interm
