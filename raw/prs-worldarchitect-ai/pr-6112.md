# PR #6112: [agento] fix(skeptic-cron): Gate 3 DISMISSED non-blocking + Gate 6 fail-closed

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-04-06
**Author:** jleechan2015
**Stats:** +88/-71 in 1 files

## Summary
Supersedes PR #6080 (fix/evidence-gate-failclosed) which has merge conflicts.
fix/6080-gate3-dismissed is clean on origin/main with all fixes applied.

## Raw Body
supersedes #6080

## Summary
Supersedes PR #6080 (fix/evidence-gate-failclosed) which has merge conflicts.
fix/6080-gate3-dismissed is clean on origin/main with all fixes applied.

## Changes
- Gate 3: CR DISMISSED non-blocking (ANY APPROVED passes)
- Gate 6: fail-closed evidence, EVIDENCE_STATUS in ALL_PASS
- checks:read permission for check-runs API
- jq dead code fix (Bugbot finding)

Closes #6080.

Generated with Claude Code

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Changes the auto-merge gating logic in `skeptic-cron.yml`, which can directly affect whether PRs get evaluated/merged and may block or allow merges based on new fail-closed checks.
> 
> **Overview**
> Updates the `Skeptic Cron` GitHub Actions workflow to pass full PR metadata as a JSON array (instead of a space-separated list), fixing `gh api --paginate`/`--jq` handling and making downstream iteration/pagination more reliable.
> 
> Tightens merge eligibility checks: Gate 3 now passes if *any* `coderabbitai[bot]` review is `APPROVED` (ignoring later `DISMISSED`), Bugbot is evaluated via check-run conclusion on the PR’s head SHA, and a new fail-closed Evidence gate requires either an `evidence-review-bot` approval on the current head commit or all matching evidence check-runs to be successful. The final “all gates pass” decision now includes the evidence result, and skeptic verdict matching is constrained to verdict comments tied to the current head SHA trigger marker.
> 
> <sup>Reviewed by [Cursor Bugbot](https://cursor.com/bugbot) for commit 62188245b0e86072af08444a7788b394b86ebd17. Bugbot is set up for automated code reviews on this repo. Configure [here](https://www.cursor.com/dashboard/bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->
## Summary by CodeRabbit

* **Chores**
  * Workflow now emits a compact JSON array of open PRs and a PR count output for downstream steps.
  * Iteration updated to process PRs from th
