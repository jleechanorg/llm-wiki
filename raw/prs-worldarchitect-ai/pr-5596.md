# PR #5596: fix: Prevent doubled $PROJECT_ROOT in export path transformation

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-02-17
**Author:** jleechan2015
**Stats:** +4/-1 in 1 files

## Summary
- Fix critical bug in export filter where `mvp_site/` was being replaced with `$PROJECT_ROOT/` even when `$PROJECT_ROOT/` was already present
- This caused invalid paths like `$PROJECT_ROOT/$PROJECT_ROOT/mcp_api.py` in exported scripts

## Raw Body
## Summary
- Fix critical bug in export filter where `mvp_site/` was being replaced with `$PROJECT_ROOT/` even when `$PROJECT_ROOT/` was already present
- This caused invalid paths like `$PROJECT_ROOT/$PROJECT_ROOT/mcp_api.py` in exported scripts

## Production Code Changes
- **exportcommands.py**: Added check to skip replacement if `$PROJECT_ROOT/` already exists in content before applying the `mvp_site/` transformation

## Test Changes
- N/A - this is a bug fix with no test changes needed

## Known Limitations
- None

Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Small, localized change to a string-rewrite rule in the export tool; main risk is missing a valid substitution in edge cases.
> 
> **Overview**
> Fixes the export content filtering in `exportcommands.py` to avoid rewriting `mvp_site/` paths when the file already contains `$PROJECT_ROOT/`, preventing duplicated prefixes in exported scripts (e.g., `$PROJECT_ROOT/$PROJECT_ROOT/...`).
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 0a01fc5d64472ed63acee5c78527602292a957be. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->

## Summary by CodeRabbit

* **Bug Fixes**
  * Fixed an issue where path substitution could produce duplicate prefixes in exported content paths.

<!-- end of auto-generated comment: release notes by coderabbit.ai -->
