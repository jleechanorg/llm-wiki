# PR #5211: Add local state files to .beads/.gitignore

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-02-10
**Author:** jleechan2015
**Stats:** +214/-2 in 3 files

## Summary
- Add Beads local state files to `.beads/.gitignore`
- Prevent machine-specific runtime files from being tracked in git

**Key themes:**
- Gitignore configuration
- Beads runtime state management

## Raw Body
## Summary
- Add Beads local state files to `.beads/.gitignore`
- Prevent machine-specific runtime files from being tracked in git

**Key themes:**
- Gitignore configuration
- Beads runtime state management

## Production Code Changes

### 1. `.beads/.gitignore` (+7/-0 lines)
**Purpose:** Configure which Beads files should be ignored by git

- **Added local state file patterns** - No local state patterns → Added 5 patterns (.local_version, sync-state.json, last-touched, export-state/, .jsonl.lock) → These files are machine-specific runtime state that shouldn't be version controlled, similar to existing ignored files like daemon.lock and beads.db-*

## Test Changes
- No test changes (configuration only)

## Known Limitations
None - this is a straightforward gitignore addition following existing patterns.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Gitignore-only change; it only affects which local files are tracked and has no runtime or data-processing impact.
> 
> **Overview**
> Updates `.beads/.gitignore` to ignore additional Beads *local state* artifacts: `.local_version`, `sync-state.json`, `last-touched`, `export-state/`, and `.jsonl.lock`.
> 
> This prevents machine-specific runtime/state files from being tracked while keeping JSONL exports and config files as the source of truth in git.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 97904bbfcb1dd19b9881d67589e9c5465662726a. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
