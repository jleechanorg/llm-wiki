# PR #3669: Fix: Add regression test for missing context parameter in _maybe_get_gemini_code_execution_evidence

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-01-16
**Author:** jleechan2015
**Stats:** +91/-0 in 1 files

## Summary
Add regression test to prevent missing `context` parameter bug in `_maybe_get_gemini_code_execution_evidence()` calls.

## Raw Body
## Summary
Add regression test to prevent missing `context` parameter bug in `_maybe_get_gemini_code_execution_evidence()` calls.

## Changes
- Added `test_llm_service_context_parameter.py` with two tests:
  1. Verifies `continue_story()` calls `_maybe_get_gemini_code_execution_evidence()` with `context='continue_story'`
  2. Verifies function raises `TypeError` when `context` parameter is missing

## Benefits
- Prevents regression of bug where missing `context` parameter caused:
  ```
  TypeError: _maybe_get_gemini_code_execution_evidence() missing 1 required keyword-only argument: 'context'
  ```
- Ensures all calls to `_maybe_get_gemini_code_execution_evidence()` include the required `context` parameter
- The code fix is already in place at line 3758 in `llm_service.py`; this test ensures it doesn't regress

## Testing
- [x] Ran `pytest mvp_site/tests/test_llm_service_context_parameter.py` - all tests pass
- [x] Test verifies source code includes `context='continue_story'` in `continue_story()` function
- [x] Test verifies function signature requires `context` parameter
- [x] No regressions identified

## Related Issue
Fixes issue where `continue_story()` was missing `context` parameter in call to `_maybe_get_gemini_code_execution_evidence()` at line 3687-3691 (now at line 3754-3759).

## Checklist
- [x] Code follows project conventions
- [x] Tests added/updated as needed
- [x] Documentation updated if required (test docstrings)
- [x] No hardcoded values or credentials
- [x] Changes are backwards compatible

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Adds regression coverage to prevent regressions around the required `context` parameter.
> 
> - **New tests** in `mvp_site/tests/test_llm_service_context_parameter.py`
> - AST-based check that `continue_story()` calls `_maybe_get_gemini_code_execution_evidence` with `context`
> - Verifies `_maybe_get_gemini_code_execution_evidence` raises `TypeError` when `context` is omitted
> 
> <sup>Written by [Cursor Bugbot](https:
