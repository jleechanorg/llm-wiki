# PR #2054: Fix: continue_story() respects user Gemini model preference

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2025-11-19
**Author:** jleechan2015
**Stats:** +215/-62 in 3 files

## Summary
(none)

## Raw Body
Fixed bug where continue_story() ignored user model settings and always used DEFAULT_MODEL.

Root Cause: continue_story() lacked user_id parameter and model selection logic.

Solution: Added user_id parameter and implemented model selection from user settings.

Testing: Created test_gemini_model_selection.py with Red-Green TDD tests (both pass).

Evidence: GCP logs showed gemini-2.5-flash despite user selecting gemini-3-pro-preview.

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Centralizes Gemini model selection and updates both get_initial_story() and continue_story() to honor per-user model preferences, with tests and world_logic wiring.
> 
> - **Gemini model selection**:
>   - Add `_select_model_for_user(user_id)` to choose model via user settings with validation and mock-mode handling.
>   - Refactor `get_initial_story()` to use the helper (remove inline selection code).
>   - Update `continue_story()` to accept `user_id` and use `_select_model_for_user()`; remove input-count-based selection.
>   - Pass chosen `model_to_use` through truncation (`_truncate_context`) and as `chosen_model` for API calls.
> - **Integration**:
>   - In `world_logic.process_action_unified`, pass `user_id` into `gemini_service.continue_story()`.
> - **Tests**:
>   - Add `tests/test_gemini_model_selection.py` verifying both `continue_story()` and `get_initial_story()` use the user’s `gemini_model` (via `GEMINI_MODEL_MAPPING`) and not the default.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit bb7ef441a996382c753695a408b23f98d6d5c992. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->
## Summary by CodeRabbit

* **New Features**
  * Story generation now respects user-specific AI model preferences for both initial stories and continuations.
  * Continuations use enhanced prompts with entity t
