# PR #5837: Ralph: MiniMax support + PRD-based completion

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-03-04
**Author:** jleechan2015
**Stats:** +190/-16 in 5 files

## Summary
- add `minimax` as a first-class Ralph tool mode
- make Ralph stop when all PRD stories are already `passes: true`
- harden cleanup fallback artifacts for metrics and terminal captions
- update tests and README/help text for new behavior

## Raw Body
## Summary
- add `minimax` as a first-class Ralph tool mode
- make Ralph stop when all PRD stories are already `passes: true`
- harden cleanup fallback artifacts for metrics and terminal captions
- update tests and README/help text for new behavior

## Changes
- `ralph/lib/tools.sh`
  - add `minimax` to `resolve_tool_cmd`, `list_supported_tools`, and `tool_needs_stdin`
- `ralph/ralph.sh`
  - add `prd_all_passed()` helper
  - early-exit run when all stories pass
  - add MiniMax env bridge in run loop
  - ensure fallback `metrics.json` and `captions/terminal.srt` are always created in cleanup
- `ralph/tests/test_tools.sh`
  - add assertions for MiniMax tool behavior
- `ralph/tests/test_ralph.sh`
  - add no-op completion test when PRD already complete
- `ralph/README.md`
  - document `minimax` mode and completion behavior

## Testing
- `bash ralph/tests/test_tools.sh` (15/15 passed)
- `bash ralph/tests/test_status.sh` (13/13 passed)
- `bash ralph/tests/test_ralph.sh` (18/18 passed)

## Known Limitations
- Ralph only loops when at least one story in `ralph/prd.json` has `passes: false`.

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Introduces new external API/env wiring and adds process-termination logic (`kill` on Claude workers), which could affect local runs if matching/limits are wrong. Other changes are scoped to CLI behavior, documentation, and test updates.
> 
> **Overview**
> Adds a new `--tool minimax` mode that runs Claude through MiniMax by wiring Anthropic-compatible environment variables and enforcing `MINIMAX_API_KEY`, plus updates `tools.sh`/help/README accordingly.
> 
> Updates `run` completion semantics to **exit successfully when all PRD stories have `passes: true`** (including a no-op fast path before tool validation), and adds a Claude worker cap (`wait_for_convo_slot`) to prevent parallel runs from accumulating stuck Claude processes.
> 
> Hardens cleanup by always emitting fallback artifacts (`evidence/captions/terminal.srt` and `metr
