# PR #5384: Add passwordless clock sync for self-hosted runner auto-recovery

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-02-13
**Author:** jleechan2015
**Stats:** +185/-24 in 8 files
**Labels:** infrastructure

## Summary
Adds passwordless clock sync for self-hosted runner auto-recovery and reorganizes all runner-related content into a dedicated `self-hosted/` top-level directory.

**Problem**: After offline periods, runners with clock drift (+10min) cannot reconnect to GitHub:
1. Computer goes offline with clock +10min ahead
2. Runner loses connection to GitHub
3. Computer comes back online, runner tries to reconnect
4. GitHub validates token timestamp, sees future timestamp
5. GitHub rejects authentication → Ru

## Raw Body
## Summary

Adds passwordless clock sync for self-hosted runner auto-recovery and reorganizes all runner-related content into a dedicated `self-hosted/` top-level directory.

**Problem**: After offline periods, runners with clock drift (+10min) cannot reconnect to GitHub:
1. Computer goes offline with clock +10min ahead
2. Runner loses connection to GitHub
3. Computer comes back online, runner tries to reconnect
4. GitHub validates token timestamp, sees future timestamp
5. GitHub rejects authentication → Runner registration deleted
6. Manual re-registration required

**Solution**: Enable passwordless clock sync + logical directory organization for all runner content.

## Key Changes

### 1. Passwordless Clock Sync (New Feature)
- **Script**: `self-hosted/scripts/setup-passwordless-clocksync.sh`
- **Purpose**: One-time setup for automatic runner recovery
- **Features**:
  - Creates sudoers entry (only `sntp -sS time.apple.com`)
  - Updates monitor.sh to sync clock before restart
  - Provides LaunchAgent integration instructions
  - Comprehensive validation and testing

### 2. Directory Reorganization (Better Structure)
**Before**:
\`\`\`
scripts/
└── self-hosted/
    ├── setup-runner-with-drift.sh
    ├── setup-github-runner-redundant.sh
    ├── monitor.sh
    ├── restore-clock-drift.sh
    ├── crontab-example.txt
    └── README-crontab.md
\`\`\`

**After**:
\`\`\`
self-hosted/
└── scripts/
    ├── setup-runner-with-drift.sh
    ├── setup-github-runner-redundant.sh
    ├── setup-passwordless-clocksync.sh (NEW)
    ├── monitor.sh
    ├── restore-clock-drift.sh
    ├── crontab-example.txt
    └── README-crontab.md
\`\`\`

**Benefits**:
- ✅ Cleaner top-level repository structure
- ✅ Logical grouping of all runner-related content
- ✅ Easier for `/exportcommands` to discover and export
- ✅ Clear separation from general-purpose `scripts/` directory

### 3. Export Integration Updates
- Updated `exportcommands.py` to export from `self-hosted/scripts/`
- Auto-discovers all ru
