# PR #4447: fix(automation): Prevent PR monitor feedback loop with sub-PR filtering

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-02-01
**Author:** jleechan2015
**Stats:** +165/-0 in 2 files

## Summary
- Fix missing `COMMENT_VALIDATION_MARKER_PREFIX` causing repeated bot comment reprocessing
- Draft PRs (including `copilot/sub-pr-*` branches) already filtered by existing `isDraft` check

## Raw Body
## Summary
- Fix missing `COMMENT_VALIDATION_MARKER_PREFIX` causing repeated bot comment reprocessing
- Draft PRs (including `copilot/sub-pr-*` branches) already filtered by existing `isDraft` check

## Production Code Changes

### 1. `automation/jleechanorg_pr_automation/jleechanorg_pr_monitor.py` (+2 lines)
**Purpose:** Main PR automation monitor

- **Added `COMMENT_VALIDATION_MARKER_PREFIX` to marker check** - Marker missing from `_get_last_codex_automation_comment_time()` → Now included → Prevents bot comments after comment_validation from triggering reprocessing

## Test Changes
- Added `test_pr_monitor_feedback_loop.py` with 6 TDD tests
- Tests cover: draft filtering, marker recognition for all 5 workflow types
- All 6 tests pass

## Root Cause
The `_get_last_codex_automation_comment_time()` function checked for:
- `CODEX_COMMIT_MARKER_PREFIX`
- `FIX_COMMENT_MARKER_PREFIX`
- `FIX_COMMENT_RUN_MARKER_PREFIX`
- `FIXPR_MARKER_PREFIX`

But was **missing** `COMMENT_VALIDATION_MARKER_PREFIX`. This caused bot comments after `comment_validation` runs to be seen as "new", triggering infinite reprocessing.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Small, targeted change to automation marker detection plus unit tests; low blast radius but could affect when the monitor decides to re-run automation on PRs.
> 
> **Overview**
> **Prevents PR monitor reprocessing feedback loops** by treating `comment_validation` workflow comments as automation markers when computing the “last automation comment” timestamp in `JleechanorgPRMonitor._get_last_codex_automation_comment_time`.
> 
> Adds a new test suite (`test_pr_monitor_feedback_loop.py`) asserting draft PRs are never actionable and verifying all automation marker prefixes—including `COMMENT_VALIDATION_MARKER_PREFIX`—are recognized, plus an integration-style check for `filter_eligible_prs` behavior.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com
