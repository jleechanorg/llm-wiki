# PR #3805: feat: centralize local server port generation logic

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-01-19
**Author:** jleechan2015
**Stats:** +75/-14 in 2 files

## Summary
(none)

## Raw Body
This PR updates 'run_local_server.sh' to use the branch-specific deterministic port generation logic from 'testing_mcp/dev_server.py'.

**Changes:**
- 'testing_mcp/dev_server.py': Refactored to allow the 'port' command to run without external dependencies (moved 'requests' import).
- 'run_local_server.sh': Now calls 'testing_mcp/dev_server.py port' to determine the default Flask port based on the current branch name. Falls back to random port selection if the hashed port is unavailable.

**Benefits:**
- Ensures consistent port usage between the local runner script and the test suite.
- Reduces the likelihood of port conflicts by deterministically assigning ports to branches.

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Centralizes local port management and shares logic between the shell runner and test utility.
> 
> - `run_local_server.sh`: Prefer branch-specific hashed `FLASK_PORT` from `testing_mcp/dev_server.py port`; if unavailable, fall back to `dev_server.py random` (range 8100–8199) for Flask/React/MCP with collision avoidance and clearer error output.
> - `testing_mcp/dev_server.py`: Adds random free port selection (`random` CLI with `--min/--max`), `is_port_free`, optional `requests` dependency for health checks, and minor port range tweaks (hash base range now 8000–8099).
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 2b56fa44d5f6eb1cc2ec7d380186342ec90f33a4. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->
## Summary by CodeRabbit

* **Refactor**
  * Port selection now prefers a branch-specific hashed port and falls back to random selection when unavailable.
* **Bug Fixes**
  * Improved error reporting and cleanup when branch-port retrieval fails.
  * Startup health checks gracefully disable when optional networking support is missing, avoiding startup err
