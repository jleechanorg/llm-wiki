# PR #5564: chore(gemini): sync Claude commands to TOML format

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-02-16
**Author:** jleechan2015
**Stats:** +8598/-3058 in 160 files

## Summary
- Convert 158 .md command files to .toml format for Gemini CLI compatibility
- Fix TOML parsing issues (nested quotes, escape sequences)
- Add 8 new commands missing from Gemini folder

## Test Plan
- [x] `python3 -c "import tomllib; ..."` validates all 160 files

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Mostly prompt/documentation changes, but it alters operational command contracts (e.g., `/copilot` orchestration entrypoint and `/tmp` path conventions) that downstream automation may rely on; breakage would surface as failed command execution rather than runtime product bugs.
> 
> **Overview**
> Updates the `

## Raw Body
## Summary
- Convert 158 .md command files to .toml format for Gemini CLI compatibility
- Fix TOML parsing issues (nested quotes, escape sequences)
- Add 8 new commands missing from Gemini folder

## Changes
- **.gemini/commands/*.toml**: 160 files modified/added
- All files validated with Python tomllib

## Test Plan
- [x] `python3 -c "import tomllib; ..."` validates all 160 files

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Mostly prompt/documentation changes, but it alters operational command contracts (e.g., `/copilot` orchestration entrypoint and `/tmp` path conventions) that downstream automation may rely on; breakage would surface as failed command execution rather than runtime product bugs.
> 
> **Overview**
> Updates the `.gemini/commands` command library by converting/adding many slash commands to TOML and standardizing prompt formatting/escaping (e.g., switching to single-quoted TOML strings, fixing backslashes/newlines/grep patterns) to avoid parser issues.
> 
> Refactors several workflows and docs in those command prompts: replaces `/plan` references with `/planexec` across the catalog, rewrites `/copilot` to launch an external orchestration script (`.claude/scripts/copilot_execute.py`) rather than describing a manual multi-phase flow, and adds explicit ACTION_ACCOUNTABILITY response/spec guidance via a new `_copilot_reference.toml`.
> 
> Tightens PR-comment tooling prompts (`/commentfetch`, `/commentcheck`, `/commentreply`) to use repo+branch-scoped `/tmp/{repo}/{branch}/...` paths (with sanitization) and updates examples/README templates accordingly; also tweaks testing guidance like `/4layer` to emphasize a minimal-repro “ladder” and updates `/adde2e` to use the shared `End2EndBaseTestCase` and auth-bypass env var in its template.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 02628df157833652862163653f3fd6440b86d686. This will upd
