# PR #6172: [agento] feat(ci): extend tool-cache bootstrap to coverage, doc-size-check, hook-tests, mcp-smoke workflows

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-04-10
**Author:** jleechan2015
**Stats:** +29/-3 in 5 files

## Summary
- Apply `scripts/ci/setup-runner-tool-cache.sh` to all self-hosted workflows that call `actions/setup-python`, not just `test.yml`
- Prevents `/Users/runner` permission errors on self-hosted runners during Python setup

## Raw Body
## Summary
- Apply `scripts/ci/setup-runner-tool-cache.sh` to all self-hosted workflows that call `actions/setup-python`, not just `test.yml`
- Prevents `/Users/runner` permission errors on self-hosted runners during Python setup

## Production Code Changes
- `.github/workflows/coverage.yml`: add tool-cache step before `setup-python`
- `.github/workflows/doc-size-check.yml`: add tool-cache step before `setup-python`
- `.github/workflows/hook-tests.yml`: add tool-cache step before `setup-python`
- `.github/workflows/mcp-smoke-tests.yml`: add tool-cache step before `setup-python`

## Test Changes
- N/A (workflow YAML only)

## Known Limitations
- BD-fac (venv_utils.sh improvements) already uses writable fallback roots for shard jobs not calling `actions/setup-python`

## Closes
BEAD-8w7

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Touches multiple GitHub Actions workflows and changes runner environment setup; mistakes could break CI execution on self-hosted runners but no production code paths are affected.
> 
> **Overview**
> Adds a new pre-`setup-python` bootstrap step (`scripts/ci/setup-runner-tool-cache.sh`) to several self-hosted workflows (`coverage`, `doc-size-check`, `hook-tests`, `mcp-smoke-tests`) to ensure the runner tool cache is writable and avoid macOS `/Users/runner` permission failures.
> 
> Also tightens `doc-size-check` checkouts (pins `actions/checkout` and explicitly checks out the PR/head SHA with `persist-credentials: false`) and pins `actions/checkout` in `test-deployment.yml`.
> 
> <sup>Reviewed by [Cursor Bugbot](https://cursor.com/bugbot) for commit d92f579f149f9b6cfc1f269126a4c9b80c072c92. Bugbot is set up for automated code reviews on this repo. Configure [here](https://www.cursor.com/dashboard/bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->

## Summary by CodeRabbit

* **Chores**
  * Updated CI/CD workflows to improve build system initialization across multi
