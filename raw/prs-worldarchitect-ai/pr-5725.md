# PR #5725: Add preview-only MCP smoke auth bypass

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-02-22
**Author:** jleechan2015
**Stats:** +36/-2 in 5 files

## Summary
- Add a narrow preview-only `/mcp` smoke auth bypass in `mvp_site/main.py` using `X-Smoke-Test-Auth` plus the `SMOKE_SMOKE_TOKEN` secret.
- Inject `SMOKE_SMOKE_TOKEN` into PR preview deployment secrets in `.github/workflows/pr-preview.yml`.
- Pass `SMOKE_SMOKE_TOKEN` into preview smoke test job env in `.github/workflows/mcp-smoke-tests.yml`.
- Update `scripts/mcp-smoke-tests.mjs` to send `X-Smoke-Test-Auth` on smoke-check requests.

## Raw Body
## Summary
- Add a narrow preview-only `/mcp` smoke auth bypass in `mvp_site/main.py` using `X-Smoke-Test-Auth` plus the `SMOKE_SMOKE_TOKEN` secret.
- Inject `SMOKE_SMOKE_TOKEN` into PR preview deployment secrets in `.github/workflows/pr-preview.yml`.
- Pass `SMOKE_SMOKE_TOKEN` into preview smoke test job env in `.github/workflows/mcp-smoke-tests.yml`.
- Update `scripts/mcp-smoke-tests.mjs` to send `X-Smoke-Test-Auth` on smoke-check requests.

## Notes
- `validateSmokeModeContract` and `MOCK_SERVICES_MODE` behavior remain unchanged.
- Auth bypass is restricted to `ENVIRONMENT=preview`, `path == "/mcp"`, and a matching token.

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Touches authentication gating for the `/mcp` endpoint; although constrained to `ENVIRONMENT=preview`, misconfiguration could unintentionally weaken auth in non-preview deployments.
> 
> **Overview**
> Enables MCP smoke tests to hit preview deployments by allowing `X-Test-Bypass-Auth`/test user headers through CORS in `ENVIRONMENT=preview` and adding a narrow `/mcp` auth bypass that injects a synthetic user when `X-Test-Bypass-Auth: true` is present.
> 
> Updates both the Node smoke runner (`scripts/mcp-smoke-tests.mjs`) and the Python test MCP client (`testing_mcp/lib/mcp_client.py`) to automatically send `X-Test-Bypass-Auth: true` on requests. Also includes no-op comment bumps in the two CI workflow YAMLs to force reruns.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit a042ad712b7ce18feb97a7039979595b5864353f. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->
## Summary by CodeRabbit

* **Chores**
  * CI workflows annotated for smoke-test coordination in preview runs.
  * Preview deployments now accept a test-bypass header (X-Test-Bypass-Auth) and optional test user headers (X-Test-Use
