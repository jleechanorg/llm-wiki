# PR #5548: fix: add logging for gh pr view failures and expand wrap_prompt docstring

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-02-16
**Author:** jleechan2015
**Stats:** +85/-3 in 2 files

## Summary
Addresses two follow-up chores from PR #5546:

### REV-xpge0: Add logging for silent `gh pr view` subprocess failures
- Replaced silent `except Exception: pass` with `logger.debug("gh_pr_view_failed", ...)` including PR number and `exc_info=True`
- Added `logger.debug("gh_pr_view_nonzero_exit", ...)` for non-zero exit codes with returncode and stderr context

### REV-6kk7k: Expand `wrap_prompt` docstring
- Expanded docstring for the `wrap_prompt` parameter in `analyze_task_and_create_agents` to 

## Raw Body
## Summary

Addresses two follow-up chores from PR #5546:

### REV-xpge0: Add logging for silent `gh pr view` subprocess failures
- Replaced silent `except Exception: pass` with `logger.debug("gh_pr_view_failed", ...)` including PR number and `exc_info=True`
- Added `logger.debug("gh_pr_view_nonzero_exit", ...)` for non-zero exit codes with returncode and stderr context

### REV-6kk7k: Expand `wrap_prompt` docstring
- Expanded docstring for the `wrap_prompt` parameter in `analyze_task_and_create_agents` to document the full behavioral impact: subprocess skipping, prompt template bypass, `pr_context` injection control, and execution guidelines omission

### Tests
- Added `TestGhPrViewLogging` class with two new tests:
  - `test_gh_pr_view_exception_logs_debug`: Verifies `gh_pr_view_failed` event logged on exception
  - `test_gh_pr_view_nonzero_exit_logs_debug`: Verifies `gh_pr_view_nonzero_exit` event logged on non-zero exit

All 29 tests pass (including 2 new + 27 existing).

Closes: REV-xpge0, REV-6kk7k

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Low risk: adds debug-level logs and documentation plus unit tests, with no changes to core orchestration behavior beyond improved observability on `gh` subprocess failures.
> 
> **Overview**
> Improves PR-context detection robustness by replacing a silent `gh pr view` failure path with structured `logger.debug` events for both exceptions (`gh_pr_view_failed` with `exc_info`) and non-zero exit codes (`gh_pr_view_nonzero_exit` with return code and trimmed stderr).
> 
> Expands the `wrap_prompt` docstring in `analyze_task_and_create_agents` to explicitly document that disabling wrapping also skips `gh` subprocess PR detection, PR-mode prompt templates/guidelines, and `pr_context` injection. Adds unit tests asserting the new logging behavior for exception and non-zero `gh pr view` outcomes.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 2e77c2a5b9ca59c38163432e696eaaaae10c
