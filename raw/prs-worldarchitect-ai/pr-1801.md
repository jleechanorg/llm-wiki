# PR #1801: fix: Add GITHUB_TOKEN environment to cron for authenticated API access

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2025-10-01
**Author:** jleechan2015
**Stats:** +40/-0 in 1 files

## Summary
Fixes jleechanorg PR automation failing with "API rate limit exceeded" errors every 30 minutes since 17:30 on 2025-09-30.

**Root Cause**: Cron jobs don't inherit shell environment variables, causing the `gh` CLI to make unauthenticated API requests (60 requests/hour limit) instead of authenticated requests (5000/hour limit).

**Solution**: Updated crontab to source `~/.token` file before executing automation script, providing GITHUB_TOKEN for authenticated API access.

## Test Plan
- [x] Manual execution with sourced token successful
- [x] API rate limit confirmed not exceeded
- [x] Documentation created
- [ ] Verify next cron run at 23:30 succeeds automatically

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Adds a doc explaining how to source `~/.token` in cron to authenticate `gh` and verify the fix.
> 
> - **Documentation**:
>   - Add troubleshooting guide `docs/fix_gh_token_cron.md` covering:
>     - Problem an

## Raw Body
## Summary
Fixes jleechanorg PR automation failing with "API rate limit exceeded" errors every 30 minutes since 17:30 on 2025-09-30.

**Root Cause**: Cron jobs don't inherit shell environment variables, causing the `gh` CLI to make unauthenticated API requests (60 requests/hour limit) instead of authenticated requests (5000/hour limit).

**Solution**: Updated crontab to source `~/.token` file before executing automation script, providing GITHUB_TOKEN for authenticated API access.

## Changes
- Updated crontab entry: Added `. $HOME/.token &&` before script execution
- Added documentation: `docs/fix_gh_token_cron.md` with troubleshooting guide

## Testing
- ✅ Manual test: Successfully processed 17 PRs across 6 repositories
  - ai_universe: 8 PRs
  - ai_universe_frontend: 2 PRs  
  - worldarchitect.ai: 3 PRs
  - codex_plus: 2 PRs
  - worldai_ralph: 1 PR
  - worldai_genesis: 1 PR
- ✅ Verified: API rate limit shows 4769/5000 remaining (not actually rate limited)
- ⏳ Next automatic cron run at 23:30 will verify production operation

## Test Plan
- [x] Manual execution with sourced token successful
- [x] API rate limit confirmed not exceeded
- [x] Documentation created
- [ ] Verify next cron run at 23:30 succeeds automatically

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Adds a doc explaining how to source `~/.token` in cron to authenticate `gh` and verify the fix.
> 
> - **Documentation**:
>   - Add troubleshooting guide `docs/fix_gh_token_cron.md` covering:
>     - Problem and root cause: cron not inheriting env, `gh` making unauthenticated requests.
>     - Solution: source `~/.token` in crontab (`. $HOME/.token && ...`) to provide `GITHUB_TOKEN`.
>     - Verification results of manual run and testing timeline.
>     - Related references: `crontab -l`, `~/.token`, and log path.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 77da548f8b7e1517ce5f7d592e5eb25d79288ffe. Thi
