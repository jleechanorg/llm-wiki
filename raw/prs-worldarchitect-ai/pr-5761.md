# PR #5761: CI: disable non-core self-hosted jobs and pin MVP shards to mac

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-02-24
**Author:** jleechan2015
**Stats:** +39/-14 in 7 files

## Summary
- disable non-core self-hosted usage by moving migrated heavy workflows back to `ubuntu-latest`
- keep new self-hosted code in the MVP shard workflow, but disable harness autonomy jobs for now (`if: false`)
- restrict active self-hosted MVP shard jobs to mac runners via labels: `[self-hosted, claude, macOS, ARM64]`

## Raw Body
## Summary
- disable non-core self-hosted usage by moving migrated heavy workflows back to `ubuntu-latest`
- keep new self-hosted code in the MVP shard workflow, but disable harness autonomy jobs for now (`if: false`)
- restrict active self-hosted MVP shard jobs to mac runners via labels: `[self-hosted, claude, macOS, ARM64]`

## Changed workflows
- `.github/workflows/presubmit.yml`
- `.github/workflows/test.yml`
- `.github/workflows/coverage.yml`
- `.github/workflows/pr-preview.yml`
- `.github/workflows/self-hosted-mvp-shard1.yml`

## Validation
- `git diff --check`
- YAML parse check across changed workflow files

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Changes CI runner selection and disables some jobs, which can alter what runs in PRs and potentially reduce coverage if misconfigured; code changes are limited to workflows and a timeout shim.
> 
> **Overview**
> Shifts most CI workflows back to GitHub-hosted runners by replacing `[self-hosted, claude]` with `ubuntu-latest` for coverage generation, PR preview deploys, presubmit gates, and the main directory test matrix.
> 
> Self-hosted usage is narrowed to core MVP shard workflows: `harness-autonomy-*` jobs are temporarily disabled (`if: false`), and remaining shard/test-runner jobs are pinned to mac runners via `[self-hosted, claude, macOS, ARM64]`. `run_tests.sh` now provides a cross-platform `timeout` shim (preferring `gtimeout`/Homebrew coreutils) to keep CI timeouts working on macOS runners.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 8cc53acbba8164fb852cbb4e471e0709f019ef6b. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
