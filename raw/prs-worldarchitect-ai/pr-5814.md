# PR #5814: Design reset: OSS-first self-hosted runner cutover

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-03-02
**Author:** jleechan2015
**Stats:** +563/-1409 in 26 files

## Summary
Reset self-hosted runner direction to OSS-first and remove all custom runner wrapper/control-plane layers.

## Raw Body
## Summary
Reset self-hosted runner direction to OSS-first and remove all custom runner wrapper/control-plane layers.

## Changes
### OSS runner lifecycle (Mike Young)
- Keep runner lifecycle under `self-hosted-oss/` using upstream `myoung34/github-runner`.
- `self-hosted-oss/.env.example`, `start-runner.sh`, `stop-runner.sh`, `status.sh` are the canonical operator entrypoints.

### Remove custom wrapper/control-plane layers
- Delete `.github/actions/container-run`.
- Delete `.github/actions/runner-control-plane`.
- Delete `self-hosted-runner/` (custom container wrapper image/script path).
- Delete `.github/workflows/build-ci-runner-image.yml` (obsolete custom wrapper image build workflow).

### Workflow updates
- Convert wrapper-dependent workflow steps to native `run` steps.
- Remove runner-control-plane references from workflows.

### Non-runner code ports from #5811
- `mvp_site/streaming_orchestrator.py`
  - add `continue_story_streaming()` passthrough wrapper for test patchability
  - route streaming call through that wrapper
- `mvp_site/frontend_v1/index.html`
  - adjust script loading order so theme/animation/loading helpers are loaded before deferred app script

## Runner-code policy now defined
Any future runner-related code must include explicit justification:
- GitHub-runner-first evaluation
- Upstream OSS (`myoung34/docker-github-actions-runner`) evaluation
- Minimal custom surface + sunset/removal plan

## Notes
- This PR intentionally removes custom wrapper/control-plane code even if some CI jobs need follow-up adjustments.
- Follow-up work should align workflows fully with native GitHub Actions semantics while keeping runner lifecycle on `myoung34` OSS.


<!-- This is an auto-generated comment: release notes by coderabbit.ai -->
## Summary by CodeRabbit

## Release Notes

* **New Features**
  * Added OSS-first self-hosted GitHub Actions runner setup with Docker Compose configuration and bootstrap scripts.
  * Introduced Docker socket passthrough suppo
