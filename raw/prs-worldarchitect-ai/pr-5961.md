# PR #5961: [agento] fix: prevent thread leak in codex-api PR monitor

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-03-14
**Author:** jleechan2015
**Stats:** +79/-3 in 2 files

## Summary
- Add process tracking and thread safety valve to prevent kernel panics caused by accumulated threads from unsubprocess.Popen calls
- Fixes critical issue where codex-api PR monitor accumulated ~2450 threads over 6+ hours, triggering kernel spinlock deadlocks

## Raw Body
## Summary
- Add process tracking and thread safety valve to prevent kernel panics caused by accumulated threads from unsubprocess.Popen calls
- Fixes critical issue where codex-api PR monitor accumulated ~2450 threads over 6+ hours, triggering kernel spinlock deadlocks

## Production Code Changes
- **jleechanorg_pr_monitor.py**: Track all Popen handles in global list, reap periodically, exit if >500 threads
- **codex_cli_tasks.py**: Add 2-second rate limiting for worktree operations, atexit handler for cleanup

## Test Changes
- N/A (runtime monitoring fix)

## Known Limitations
- Launchd plist timeout changes require manual reload to take effect

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Medium risk because it changes long-running automation control flow (tracking/reaping subprocesses and exiting when thread counts are high), which could prematurely terminate runs if thresholds/cleanup logic misbehave.
> 
> **Overview**
> Prevents long-running `jleechanorg_pr_monitor.py` runs from accumulating leaked subprocess threads by **tracking all `subprocess.Popen` handles**, periodically reaping finished children, and adding a **thread-count safety valve** that exits when threads stay above 500 after reaping.
> 
> Hardens `codex_cli_tasks.py` worktree handling by **rate-limiting worktree creation**, tracking active temp worktrees for bounded growth, and registering an `atexit` cleanup to remove leftover `/tmp` worktrees on process exit.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 8e25218c3c3727ee8d0a3fea8ca8f59e32deb7f9. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->

## Summary by CodeRabbit

## Bug Fixes

* **Improved subprocess and thread management** during long-running automation t
