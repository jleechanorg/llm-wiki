# PR #6071: fix(world_logic): suppress stale level-up banner after character creation completes

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-04-02
**Author:** jleechan2015
**Stats:** +92/-13 in 2 files

## Summary
- suppress stale level-up signal after level-up completion when `character_creation_completed=true` and `level_up_complete=true`
- prevent stale `rewards_pending.level_up_available` from reactivating level-up banner on next turn

## Raw Body
## Summary
- suppress stale level-up signal after level-up completion when `character_creation_completed=true` and `level_up_complete=true`
- prevent stale `rewards_pending.level_up_available` from reactivating level-up banner on next turn

## Root cause
After finishing level-up modal flow, state marked completion flags but did not always clear `rewards_pending.level_up_available`. `_resolve_level_up_signal` could re-enable level-up active from stale signal.

## Fix
Add guard in `_resolve_level_up_signal` to force `level_up_active = False` when both completion flags are true.
Also set `state_changes["rewards_pending"] = {"level_up_available": False}` at the root level-up transition in `_enforce_character_creation_modal_lock` to persist the cleared state.

## Evidence

### Test Results
```
mvp_site/tests/test_level_up_stale_flags.py
15 passed, 2 warnings in 43.56s
```

New test: `test_rewards_pending_cleared_on_level_up_exit` verifies `rewards_pending.level_up_available` is set to `False` on level-up modal exit.

### CI Checks (6/6 PR checks SUCCESS)
- Test Deployment Build: SUCCESS
- Presubmit Checks: SUCCESS
- MCP Smoke Tests: SUCCESS
- WorldArchitect Tests (Directory-Based): SUCCESS
- Deploy PR Preview: SUCCESS
- Self-Hosted MVP Shards: SUCCESS

### CodeRabbit
CR APPROVED at 2026-04-02T01:08:33Z (commit d0503276b7)

### Inline Threads
All 4 bot threads resolved:
- cursor[bot] Medium (missing parallel guard): acknowledged — `_inject_modal_finish_choice_if_needed` has equivalent `level_up_exited` guard
- chatgpt-codex-connector[bot] P1 (blanket-disable): acknowledged — `_check_and_set_level_up_pending` resets `level_up_complete=False` for new level-ups
- Copilot E501: addressed — pre-commit auto-formatted; CI Ruff check passes
- cursor[bot] Low (rewards_pending overwrite): acknowledged — overwrite is intentional at level-up exit; `_check_and_set_level_up_pending` re-evaluates on next turn

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Touches core world
