# PR #6090: fix(deploy): separate PRODUCTION_MODE from test bypass flags

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-04-04
**Author:** jleechan2015
**Stats:** +203/-8 in 3 files

## Summary
The dev deployment was failing because  set  for ALL environments and then added / for dev/preview. This is a fatal combo -  raises  when  AND any bypass flag is set.

## Raw Body
## Summary

The dev deployment was failing because  set  for ALL environments and then added / for dev/preview. This is a fatal combo -  raises  when  AND any bypass flag is set.

## Changes

1. **deploy.sh**: Only set  for staging/stable. Dev/preview get  without .

2. **main.py**: Fixed the error message formatter in  which was dropping the VALUE part of NAME=VALUE pairs, showing 'NAME=' instead of 'NAME=VALUE'.

3. **test_validate_production_env.py** (NEW): Added 7 TDD tests covering all scenarios.

## Tests

- PRODUCTION_MODE + TESTING_AUTH_BYPASS raises RuntimeError
- PRODUCTION_MODE + ALLOW_TEST_AUTH_BYPASS raises RuntimeError
- PRODUCTION_MODE + both flags raises RuntimeError
- PRODUCTION_MODE + no bypass flags succeeds
- Error message shows full NAME=VALUE pairs (not NAME= with empty value)
- Dev mode + bypass flags succeeds
- SMOKE_TOKEN is non-fatal

## TDD Approach

Followed TDD discipline:
1. Wrote failing tests FIRST (established the bug - error message showed 'TESTING_AUTH_BYPASS=' instead of 'TESTING_AUTH_BYPASS=true')
2. Fixed the bug
3. Verified all 33 tests pass (7 new + 26 existing)

---
This PR resolves the dev deployment failure from run #23967883873.

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Touches deployment-time env var configuration and production safety checks; mistakes could either block deploys or accidentally relax auth enforcement, though changes are narrow and covered by new unit tests.
> 
> **Overview**
> Fixes Cloud Run deploy configuration to keep `PRODUCTION_MODE` mutually exclusive with test-auth bypass flags.
> 
> `deploy.sh` adds explicit `preview` environment handling, validates supported environments, and uses `gcloud --remove-env-vars` to clear stale `PRODUCTION_MODE`/bypass vars when switching between `dev` vs `preview`/`staging`/`stable`.
> 
> `mvp_site/main.py` improves `_validate_production_environment()` fatal error reporting to preserve full `NAME=VALUE` pairs, and a new `mvp_site/tests/test_validate_
