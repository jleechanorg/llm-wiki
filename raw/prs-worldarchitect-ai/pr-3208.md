# PR #3208: fix(rewards): Add double-processing detection and self-correction filtering

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-01-10
**Author:** jleechan2015
**Stats:** +928/-269 in 12 files

## Summary
Extracts **only the rewards self-correction fixes** from PR #3180 (which had mixed concerns including the unwanted NarrativeAgent abstraction).

## Raw Body
## Summary
Extracts **only the rewards self-correction fixes** from PR #3180 (which had mixed concerns including the unwanted NarrativeAgent abstraction).

## Changes
- **Double-processing detection**: `_detect_rewards_discrepancy()` now warns when XP increases while `rewards_processed` is already True
- **Correction filtering**: New `_filter_reward_corrections()` helper filters reward-specific system corrections
- **LLMRequest validation**: Validates `system_corrections` field is a list of strings
- **Test fixes**: Updated mechanical choice detection and error handling in E2E tests

## Why This PR Exists
PR #3180 mixed several concerns:
- ❌ NarrativeAgent abstraction (over-engineering - living world is already story-only on main)
- ✅ Rewards self-correction improvements (extracted here)

This PR contains only the rewards fixes.

## Testing
- [ ] Ran `./run_tests.sh` - all tests pass
- [x] Syntax checked

## Files Changed
| File | Purpose |
|------|---------|
| `mvp_site/world_logic.py` | Double-processing detection, filtering helpers |
| `mvp_site/llm_request.py` | Validation for system_corrections |
| `testing_mcp/test_rewards_agent_real_e2e.py` | Allow "continue_adventure" as mechanical choice |
| `testing_mcp/test_system_corrections_real_e2e.py` | Fix error handling and exit codes |

---
🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Improves rewards self-correction flow and evidence reporting while tightening input validation.
> 
> - **Rewards logic:** `_detect_rewards_discrepancy` now emits warnings when XP increases with `rewards_processed=True`; plumbs `system_warnings`, deduplicates, and cleans reward-related `pending_system_corrections` after successful processing
> - **Correction filtering:** Adds `_normalize_system_corrections`, `_is_reward_correction`, `_filter_reward_corrections`; persists/returns only reward-related corrections and deduplicates order-preserving
> - **Validation:** In `llm_reque
