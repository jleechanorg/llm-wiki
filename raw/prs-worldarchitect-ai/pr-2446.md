# PR #2446: Address review feedback for cursor automation workflows

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2025-12-17
**Author:** jleechan2015
**Stats:** +27/-18 in 4 files
**Labels:** codex

## Summary
- Harden the cursor command template test to check for the force flag as its own token and keep other CLI expectations intact
- Drop the unnecessary `package-dir` override in `pyproject.toml` to match the package layout
- Align copilot-lite instructions and tooling with commentfetch output, including copying results to the workdir, schema-tolerant jq checks, and posting replies for review-body comments

## Raw Body
## Summary
- Harden the cursor command template test to check for the force flag as its own token and keep other CLI expectations intact
- Drop the unnecessary `package-dir` override in `pyproject.toml` to match the package layout
- Align copilot-lite instructions and tooling with commentfetch output, including copying results to the workdir, schema-tolerant jq checks, and posting replies for review-body comments

## Testing
- pytest orchestration/tests/test_cli_support.py -k cursor

------
[Codex Task](https://chatgpt.com/codex/tasks/task_e_69424493ada4832fb5de7d7f94c9ac2f)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Add review-body comment handling and schema-tolerant copilot-lite workflow, harden Cursor test, and clean up packaging.
> 
> - **Comment processing (/commentreply)**:
>   - Fetch and include review-body comments (`GET /pulls/{pr}/reviews`), tag as `type: "review"`, and merge with inline/issue comments.
>   - Use distinct fetch commands; parse line-delimited JSON; fail gracefully if any fetch fails.
>   - Do not post placeholders when no Claude response; strictly skip.
> - **Copilot-lite workflow**:
>   - Safer branch sanitization via embedded Python (rejects leading dots and illegal chars).
>   - Pipe `commentfetch` output to `$WORK_DIR/comments.json`; fallback still supported.
>   - Update jq to be schema-tolerant: handle root array or `{comments: [...]}` for totals and missing-ID detection.
> - **Tests**:
>   - Cursor command template test uses `shlex.split` to assert `-f` as its own token; retains model/output checks.
> - **Build/packaging**:
>   - Remove unnecessary `[tool.setuptools].package-dir` override in `pyproject.toml`.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 5a4b955ca3af0b8a94439a33fbb16eaa43000ae6. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
