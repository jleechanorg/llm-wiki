# PR #4785: Automation: refine timestamp parsing and restore rolling-window global-run tests

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-02-04
**Author:** jleechan2015
**Stats:** +5/-7 in 1 files
**Labels:** codex

## Summary
(none)

## Raw Body
### Motivation

- Restore deterministic rolling-window behavior for global automation runs and ensure legacy data migrates without shifting window eligibility.  
- Remove a fragile/unused parsing parameter and avoid premature filtering when recording historical runs so tests can deterministically validate window semantics.  

### Description

- Normalize PR-attempt timestamps by interpreting naive timestamps in the host local timezone then converting to UTC in `_parse_timestamp`, removing the unused `convert_to_utc` parameter.  
- Consolidate global-run recording logic so `record_global_run()` delegates to `_record_global_run_at_time(run_time, now=...)`, and implement `_record_global_run_at_time` to support deterministic, timestamped test inserts without premature trimming.  
- Harden legacy fallback behavior so invalid legacy timestamps return `None`, enabling correct seeding from `start_date`/`last_run` instead of epoch.  
- Moved/updated rolling-window tests into the existing comprehensive automation test module and adjusted test timestamps to avoid boundary ambiguity (tests now exercise exclusion, enforcement, gradual expiration, and configurable window).  
- Posted a single consolidated PR response via the `/commentreply` workflow covering all review comments to avoid per-comment rate limits.  

### Testing

- Ran the focused automation test module with `python -m pytest automation/jleechanorg_pr_automation/tests/test_automation_safety_manager_comprehensive.py -q --no-cov`, and the suite passed: `53 passed`.  
- Executed the repository's consolidated comment poster (`/commentreply`) to publish a single summary reply covering outstanding review comments; the consolidated comment was posted to the PR (see the PR conversation).  
- Pre-push checks surfaced unrelated pre-existing linter/type/security notes in other areas of the repo (these were not introduced by this change).

------
[Codex Task](https://chatgpt.com/codex/tasks/task_e_698289dc2474832f8e00ebb80f4a6d
