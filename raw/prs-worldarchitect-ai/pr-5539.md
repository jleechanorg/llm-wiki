# PR #5539: Fix stale level-up modal guard behavior

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-02-15
**Author:** jleechan2015
**Stats:** +377/-22 in 7 files

## Summary
- add stale level-up guard logic in routing and world logic paths
- update tests for dict/list planning choices compatibility
- add real flow coverage for level-up modal behavior

## Raw Body
## Summary
- add stale level-up guard logic in routing and world logic paths
- update tests for dict/list planning choices compatibility
- add real flow coverage for level-up modal behavior

## Validation
- compared branch diff vs `origin/main`

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Touches core agent routing and modal/choice injection logic, so regressions could trap users in (or prematurely exit) level-up flows; changes are guarded and backed by expanded tests but affect critical gameplay state transitions.
> 
> **Overview**
> Fixes **stale level-up modal activation** by adding XP-threshold-based guards alongside existing explicit-flag/rewards-based checks, so `level_up_pending` alone won’t lock routing into `LevelUpAgent`/`CharacterCreationAgent` unless XP supports the next level (or `rewards_pending.level_up_available` is set).
> 
> Improves robustness for legacy state formats by tolerating scalar `experience` values and string boolean flags in world-logic modal-choice injection, and updates/extends tests (unit + integration) to cover sub-threshold vs threshold cases and planning-block `choices` being either dicts or lists.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit a463e0ad74def5b062bd4a7f1dc905e31ccedea5. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->
## Summary by CodeRabbit

* **Bug Fixes**
  * Prevents level-up modal/reactivation when a pending signal is stale (below XP threshold and no rewards); respects explicit true/false flags.
  * Handles legacy and varied XP formats to avoid unintended modal triggers and improves related logging.

* **Tests**
  * Expanded tests for level-up thresholds, modal injection edge cases, and legacy flag handling.
  * Test helpers updated to include level and experience parameters; integration test
