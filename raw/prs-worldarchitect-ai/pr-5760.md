# PR #5760: Complete: /fixpr 5755 - Cache corruption detection for LocalIntentClassifier

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-03-04
**Author:** jleechan2015
**Stats:** +286/-0 in 3 files

## Summary
- Add cache corruption detection and repair helpers to LocalIntentClassifier
- Detect dangling ONNX symlink targets and truncated blob directories before model load
- Remove corrupted cache dirs to allow clean re-download
- Add focused unit test for cache repair behavior

## Raw Body
## Summary
- Add cache corruption detection and repair helpers to LocalIntentClassifier
- Detect dangling ONNX symlink targets and truncated blob directories before model load
- Remove corrupted cache dirs to allow clean re-download
- Add focused unit test for cache repair behavior

## Production Code Changes
- **mvp_site/intent_classifier.py**: Added `_cache_candidates()`, `_is_corrupted_cache_dir()`, and `_validate_and_repair_cache()` methods to detect and repair corrupted FastEmbed cache before model load

## Test Changes  
- **mvp_site/tests/test_intent_classifier.py**: Added `test_validate_and_repair_cache_removes_corrupted_dir` test

## Test Results
All 5 tests pass including the new cache repair test.

🤖 Generated with [Claude Code](https://claude.ai/code) via MiniMax API

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Adds pre-load filesystem checks and automatic deletion of the FastEmbed model cache on suspected corruption, which could impact startup behavior if the heuristics misclassify a cache or interact with concurrent downloads.
> 
> **Overview**
> Adds a preflight FastEmbed cache validation step to `LocalIntentClassifier` that detects likely-corrupted model caches (missing/undersized `blobs`, dangling `.onnx` symlinks) and removes the cache directory to force a clean re-download, while skipping deletion if a `.downloading.lock` is present.
> 
> Model initialization now calls this repair step before constructing `TextEmbedding`, and tests are expanded to cover cache corruption detection and repair behavior.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit b9f424df6d6183d5ead5408542f5a23ad2075e6e. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->
## Summary by CodeRabbit

* **New Features**
  * Automatic cache integrity validation and repair
