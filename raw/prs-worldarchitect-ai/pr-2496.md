# PR #2496: Centralize combat disposition keywords and add CombatDisposition enum

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2025-12-19
**Author:** jleechan2015
**Stats:** +167/-13 in 4 files

## Summary
Addresses code review feedback on PR #2454's keyword-based approach by centralizing the combat disposition logic and adding formal type-safe structures.

### Changes:
- **constants.py**: Added `FRIENDLY_COMBATANT_TYPES` and `GENERIC_ENEMY_ROLES` frozensets with helper functions `is_friendly_combatant()` and `is_generic_enemy_role()`
- **game_state.py**: Refactored `_is_named_npc()` and `cleanup_defeated_enemies()` to use centralized constants instead of inline keyword sets
- **entities_pydantic.

## Test Plan
- [x] `test_combat_cleanup_comprehensive.py` - 4/4 passed
- [x] `test_npc_death_state_persistence.py` - all passed
- [x] `test_game_state.py` - all passed
- [x] `test_entity_classes.py` - 22/22 passed

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Centralizes combat disposition keywords/helpers, refactors cleanup logic to use them, and introduces a type-safe CombatDisposition enum with tests.
> 
> - **Combat disposition centralization**:
>   - Add `FRIENDLY_COMBATANT_TYPES`, `NEUTRAL_COMBATANT_TYPES`

## Raw Body
## Summary

Addresses code review feedback on PR #2454's keyword-based approach by centralizing the combat disposition logic and adding formal type-safe structures.

### Changes:
- **constants.py**: Added `FRIENDLY_COMBATANT_TYPES` and `GENERIC_ENEMY_ROLES` frozensets with helper functions `is_friendly_combatant()` and `is_generic_enemy_role()`
- **game_state.py**: Refactored `_is_named_npc()` and `cleanup_defeated_enemies()` to use centralized constants instead of inline keyword sets
- **entities_pydantic.py**: Added `CombatDisposition` enum (FRIENDLY/HOSTILE/NEUTRAL) with `from_type_string()` converter for future type-safe code paths

### Benefits:
1. **Centralized definitions** - Keywords defined in one place for easier updates
2. **Consistent normalization** - Helper functions handle `.lower().strip()` uniformly
3. **Extended coverage** - Added "hostile", "foe", "monster" keywords for better AI output handling
4. **Type safety foundation** - `CombatDisposition` enum enables future schema-level validation

## Test plan
- [x] `test_combat_cleanup_comprehensive.py` - 4/4 passed
- [x] `test_npc_death_state_persistence.py` - all passed
- [x] `test_game_state.py` - all passed
- [x] `test_entity_classes.py` - 22/22 passed

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Centralizes combat disposition keywords/helpers, refactors cleanup logic to use them, and introduces a type-safe CombatDisposition enum with tests.
> 
> - **Combat disposition centralization**:
>   - Add `FRIENDLY_COMBATANT_TYPES`, `NEUTRAL_COMBATANT_TYPES`, `GENERIC_ENEMY_ROLES` and helpers `is_friendly_combatant()`, `is_generic_enemy_role()` in `mvp_site/constants.py`.
> - **Refactor combat cleanup** (`mvp_site/game_state.py`):
>   - `_is_named_npc()` and `cleanup_defeated_enemies()` now use centralized helpers instead of inline keyword sets.
> - **Schemas** (`mvp_site/schemas/entities_pydantic.py`):
>   - Introduce `CombatDisposition` enum with `from_type_string()` mapping legacy type strings to FRIENDLY/HO
