# PR #5700: Enforce living world scene visibility in prompts

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-02-22
**Author:** jleechan2015
**Stats:** +315/-161 in 9 files

## Summary
- Enforce living world scene visibility in prompts so `scene_event` must appear in player narrative on the same turn.
- Require discovered `world_events` that have player-facing impact to be reflected in narrative language.
- Fix `process_action_unified` to include `processing_metadata` (with `raw_request_payload`) in the unified response when `include_raw_llm_payloads=True` was set — previously the dict was never copied into the response, causing LLM context tests to fail.
- Add `world_events` 

## Raw Body
## Summary
- Enforce living world scene visibility in prompts so `scene_event` must appear in player narrative on the same turn.
- Require discovered `world_events` that have player-facing impact to be reflected in narrative language.
- Fix `process_action_unified` to include `processing_metadata` (with `raw_request_payload`) in the unified response when `include_raw_llm_payloads=True` was set — previously the dict was never copied into the response, causing LLM context tests to fail.
- Add `world_events` to `GAME_STATE_PRIORITY_MEDIUM` in context compaction so living-world event state survives context compaction for cross-turn coherence.

## Files changed
- \`mvp_site/prompts/narrative_system_instruction.md\` — enforce scene_event narrative visibility
- \`mvp_site/prompts/living_world_instruction.md\` — enforce discovered event narrative consequences
- \`mvp_site/world_logic.py\` — copy \`processing_metadata\` into unified response (includes \`raw_request_payload\`)
- \`mvp_site/context_compaction.py\` — add \`world_events\` to MEDIUM priority bucket
- \`testing_mcp/test_living_world_llm_context.py\` — add \`_extract_raw_request_payload\` helper, fix \`turn3_events_available\` init, improve assertions
- \`testing_mcp/test_living_world_real_e2e.py\` — read \`player_turn\` from \`game_state\`, use \`extract_system_instruction_files\` for LW detection

## Testing (All Real - No Mocks)

**test_living_world_llm_context.py** — 5/5 PASSED (100%)
- TEST 1: world_events_in_llm_context ✅ — raw_request_payload captured, world_events in story_history
- TEST 2: event_referencing_in_narrative ✅ — LLM references previous world_events
- TEST 3: event_contradiction_detection ✅ — No contradictions across LW turns
- TEST 4: multi_session_living_world ✅ — Events persist across campaign reloads
- TEST 5: prompt_effectiveness_baseline ✅ — 3/3 campaigns, 9/9 LW turns generated events

**test_living_world_real_e2e.py** — 2/2 PASSED (100%)
- SCENARIO: living_world_multi_turn ✅ — 3/3 LW tur
