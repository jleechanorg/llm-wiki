# PR #4419: fix: Resolve merge conflicts for PR #4224

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-02-01
**Author:** jleechan2015
**Stats:** +47142/-2734 in 241 files

## Summary
- Resolves merge conflicts between `main` and `claude/plan-llm-compaction-zP0mW`

## Raw Body
## Summary
- Resolves merge conflicts between `main` and `claude/plan-llm-compaction-zP0mW`

## Changes
- **mvp_site/llm_service.py**: Combined budget allocator from PR with campaign_id injection from main
- **testing_mcp/lib/base_test.py**: Added `test_file` parameter from PR
- **.beads/issues.jsonl**: Merged issues from both branches

## Context
This PR fixes the merge conflicts blocking [PR #4224](https://github.com/jleechanorg/worldarchitect.ai/pull/4224).

Once merged, PR #4224 should be updated to reflect the resolved conflicts.

🤖 Generated with [Claude Code](https://claude.ai/code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Touches PR automation and comment-reply tooling, plus CI/cron configuration; behavior changes could affect how/when bots post and how worktrees/base clones are managed, but no production user-request path is modified.
> 
> **Overview**
> This PR mostly updates internal automation and tooling around PR handling. It simplifies `.claude/commands/commentreply.py` by removing the retry/error-classification plumbing, adding optional `parent_comment` context, and adjusting summary reporting/skip logic for already-replied threads.
> 
> PR automation worktree setup is hardened: `ensure_base_clone()` in both `automation/orchestrated_pr_runner.py` and `automation/jleechanorg_pr_automation/orchestrated_pr_runner.py` now proactively recovers from dirty/corrupt git state by cleaning/resetting and re-cloning on failures, with tests updated accordingly. Related automation changes include removing parallel dispatch/sequential flag support from `jleechanorg_pr_monitor.py`, updating cron templates/scripts (including adding a `moltbook-poster` cron entry), bumping the automation package version, and adding a minimal `moltbook_poster` package scaffold.
> 
> Misc updates: remove the `auto_resolve_conflicts` GitHub Actions workflow, tweak CI install logic for the `core` test group, improve Claude statusline/git header (show context %), expand `
