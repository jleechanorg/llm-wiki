# PR #5875: Move sign out button from campaigns page to settings

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-03-08
**Author:** jleechan2015
**Stats:** +11/-10 in 3 files

## Summary
- Removes the red Sign Out button from the My Campaigns page header
- Adds a grey Sign Out button at the bottom of the Settings page
- Uses event delegation in auth.js so the button works when settings is loaded via SPA navigation

## Raw Body
## Summary
- Removes the red Sign Out button from the My Campaigns page header
- Adds a grey Sign Out button at the bottom of the Settings page
- Uses event delegation in auth.js so the button works when settings is loaded via SPA navigation

## Production Code Changes
- **Before**: Sign Out button (red, `btn-danger`) lived next to the gear icon in the My Campaigns header
- **Now**: Sign Out button (grey, `btn-secondary`) lives at the bottom of the Settings page alongside "Back to Home"
- **Why**: Cleaner dashboard UX — sign out is an account action that belongs in settings, not cluttering the main campaign view

## Test Changes
No test changes required — existing auth.js test mocks are unaffected.

https://claude.ai/code/session_01Y9RzyFaaq6aipcwSHX5WKP

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Small UI and event-binding change with minimal logic impact; main risk is the sign-out button ID/DOM target mismatch preventing sign-out from working.
> 
> **Overview**
> Moves the **Sign Out** action out of the dashboard header by removing `#signOutBtnDashboard` from `index.html` and adding a new `#signOutBtnSettings` button to the bottom of `settings.html` next to “Back to Home”.
> 
> Updates `auth.js` to bind sign-out via document-level click delegation (so the handler works when Settings is loaded via SPA) and redirects to `/` after `auth.signOut()`.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 46fc0575039b6d3aa1aed0c458a9abb884a56ecb. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
