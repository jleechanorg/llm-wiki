# PR #5559: feat(automation): Add MiniMax CLI support to fixpr automation

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-02-16
**Author:** jleechan2015
**Stats:** +251/-4 in 5 files

## Summary
- Add MiniMax CLI as supported agent in fixpr automation
- Provides fallback when Gemini quota exhausted
- Uses Claude Code with MiniMax API endpoint

## Raw Body
## Summary
- Add MiniMax CLI as supported agent in fixpr automation
- Provides fallback when Gemini quota exhausted
- Uses Claude Code with MiniMax API endpoint

## Production Code Changes

### `orchestration/task_dispatcher.py`
- Added `MINIMAX_MODEL` constant
- Added `minimax` to `CLI_PROFILES` dict with:
  - `binary: "claude"` 
  - `env_set`: ANTHROPIC_BASE_URL, ANTHROPIC_MODEL, etc.
- Added `env_set` processing in runtime validation

### `automation/jleechanorg_pr_automation/jleechanorg_pr_monitor.py`  
- Added `minimax` preflight validation
- Sets MiniMax-specific env vars during validation

## Usage
```bash
jleechanorg-pr-monitor --fixpr --cli-agent minimax
# or as fallback:
jleechanorg-pr-monitor --fixpr --cli-agent gemini,minimax,cursor
```

## Test Results
- All existing CLI agent tests pass
- No coverage regression (already low coverage)

🤖 Generated with [Claude Code](https://claude.ai/code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Introduces a new execution path that alters CLI selection, environment variables, and runtime shell-script generation; misconfiguration could break agent dispatch or cause requests to hit the wrong endpoint/key.
> 
> **Overview**
> Adds a new `minimax` CLI option to the automation CLI chain, routing Claude Code through the MiniMax endpoint with a dedicated `MINIMAX_MODEL` and profile-driven environment overrides.
> 
> Updates preflight and runtime execution/validation to apply `env_set`, map `MINIMAX_API_KEY` to `ANTHROPIC_API_KEY` at runtime, force MiniMax model selection when chosen, and clean up MiniMax-specific env vars to avoid leaking into fallback CLIs.
> 
> Adds a unit test covering the MiniMax preflight env mapping behavior, and bumps package versions for `automation` and `orchestration`.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit b2af444ddf6c29a2b54034880442ca90a1b59281. This will update automatically on new commits. Configure [here](https://cursor.com/d
