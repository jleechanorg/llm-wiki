# PR #1122: fix: Fix integrate.sh --force functionality to properly stash uncommitted changes

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2025-08-02
**Author:** jleechan2015
**Stats:** +8/-1 in 1 files

## Summary
Fixes the integrate.sh script's --force functionality to properly handle uncommitted changes. Previously, the script would detect uncommitted changes, claim to proceed anyway, but then fail when `git checkout` refused to switch branches.

## Raw Body
## Summary

Fixes the integrate.sh script's --force functionality to properly handle uncommitted changes. Previously, the script would detect uncommitted changes, claim to proceed anyway, but then fail when `git checkout` refused to switch branches.

## Problem

The script had this logic flaw:
1. Detects uncommitted changes
2. In force mode, says "proceeding anyway (changes will be lost)"
3. Tries `git checkout main` which fails because git won't switch with uncommitted changes

## Solution

Now in force mode, the script:
1. Detects uncommitted changes
2. Properly stashes both staged and unstaged changes using `git stash push`
3. Provides clear recovery instructions (`git stash pop`)
4. Exits with error if stashing fails
5. Can proceed with `git checkout` since working directory is clean

## Testing

Tested with:
- ✅ Unstaged changes only
- ✅ Staged changes only  
- ✅ Both staged and unstaged changes
- ✅ Untracked files (correctly ignored by stash)
- ✅ Stash recovery instructions work correctly

## Changes

- Lines 137-144: Replace "changes will be lost" with proper stashing logic
- Add descriptive stash message with timestamp
- Add clear recovery instructions
- Add error handling for stash failures

🤖 Generated with [Claude Code](https://claude.ai/code)

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->
## Summary by CodeRabbit

* **New Features**
  * Uncommitted changes are now automatically stashed with a timestamped message when running in force mode, helping to preserve work before proceeding.
  * Users receive clear instructions on how to recover stashed changes.

* **Bug Fixes**
  * Prevents accidental loss of uncommitted changes by stashing them instead of discarding.
<!-- end of auto-generated comment: release notes by coderabbit.ai -->
