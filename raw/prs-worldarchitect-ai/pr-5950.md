# PR #5950: ci: pin Python to 3.12 on self-hosted runners

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-03-13
**Author:** jleechan2015
**Stats:** +19/-20 in 5 files

## Summary
- Change python-version from `'3.11 || 3.12'` to `'3.12'` in both jobs in test.yml

## Raw Body
## Summary
- Change python-version from `'3.11 || 3.12'` to `'3.12'` in both jobs in test.yml

## Production Code Changes
- **Before**: `'3.11 || 3.12'` — still attempts to download 3.11 first, fails on ARM64 runners with no pre-built binary
- **Now**: `'3.12'` — matches what's pre-installed on `myoung34/github-runner:ubuntu-noble`
- **Why**: Docker runner image only ships python3.12. Attempting 3.11 download fails on ARM64 (Mac self-hosted runners).

## Test Changes
None.

Generated with Claude Code

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Touches multiple CI workflows and runner preflight logic to change interpreter selection and cache keys; risk is limited to CI stability but could break builds on runners without the configured Python version.
> 
> **Overview**
> Standardizes CI to use a single Python version (defaulting to **3.12**) across coverage, hook tests, smoke tests, and main test workflows by switching `actions/setup-python` inputs to `${{ vars.PYTHON_VERSION || '3.12' }}` and updating related cache keys.
> 
> Updates CI scripts that locate Python (including `scripts/ci/runner_preflight.sh` and interpreter resolution in `test.yml`) to prefer `python3.12` / `python${{ vars.PYTHON_VERSION }}` and adjusts error messaging accordingly, reducing failures on self-hosted runners that don’t have 3.11 available.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 93c2db8683b672a52e354c6d0d4378c1ad815ce7. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- COPILOT_TRACKING_START -->
## Copilot Tracking Table

| Comment | Author | Severity | Status | Summary |
|---------|--------|----------|--------|---------|
| coverage.yml cache keys `python-3.11` | cursor[bot] | BLOCKING | **Fixed** | Cache key strings still had hardcoded `python-3.11` after python-version input was updated. Fixed in commit `93c2db8` — both key and res
