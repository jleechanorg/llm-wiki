# PR #5624: fix(ci): enable smoke test auth bypass for dev MCP endpoint

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-02-18
**Author:** jleechan2015
**Stats:** +10/-2 in 2 files

## Summary
- Fixed HTTP 401 on auto-deploy-dev smoke tests that prevented real LLM smoke validation
- Root cause: PRODUCTION_MODE=true enforces Firebase auth on /mcp, but smoke tests sent no auth headers
- Fix: Enable TESTING_AUTH_BYPASS=true on dev deployments only + add X-Test-Bypass-Auth header to smoke test requests

## Test Plan
- [ ] CI checks pass on PR
- [ ] After merge, auto-deploy-dev smoke tests pass with real LLM calls

## Raw Body
## Summary
- Fixed HTTP 401 on auto-deploy-dev smoke tests that prevented real LLM smoke validation
- Root cause: PRODUCTION_MODE=true enforces Firebase auth on /mcp, but smoke tests sent no auth headers
- Fix: Enable TESTING_AUTH_BYPASS=true on dev deployments only + add X-Test-Bypass-Auth header to smoke test requests

## Production Code Changes
**deploy.sh** - before: all environments get same env vars - now: dev environment additionally gets TESTING_AUTH_BYPASS=true and ALLOW_TEST_AUTH_BYPASS=true - why: CI smoke tests need to authenticate against the real MCP endpoint with real LLM calls; staging/production remain locked down

**scripts/mcp-smoke-tests.mjs** - before: no auth headers on health check and RPC calls - now: sends X-Test-Bypass-Auth: true header - why: the server check_token middleware accepts this header when TESTING_AUTH_BYPASS is enabled

## Test Plan
- [ ] CI checks pass on PR
- [ ] After merge, auto-deploy-dev smoke tests pass with real LLM calls

## Known Limitations
- Dev environment now accepts test bypass auth headers (acceptable for dev, not enabled on staging/production)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Introduces an authentication bypass path for deployed dev environments; low blast radius if correctly scoped, but misconfiguration could weaken access controls.
> 
> **Overview**
> Fixes dev auto-deploy smoke tests failing with `401` under `PRODUCTION_MODE` by enabling a **dev-only** auth bypass and updating the test client to use it.
> 
> `deploy.sh` now appends `TESTING_AUTH_BYPASS=true` and `ALLOW_TEST_AUTH_BYPASS=true` to Cloud Run env vars only for `ENVIRONMENT=dev`, while `scripts/mcp-smoke-tests.mjs` adds `X-Test-Bypass-Auth: true` headers to `/health` and MCP RPC requests.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit a350808e00af45c087534862be368c427c4ec4c4. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).
