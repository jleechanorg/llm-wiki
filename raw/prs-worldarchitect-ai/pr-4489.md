# PR #4489: fix(agents): add dice_roll_strategy param to DeferredRewardsAgent

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-02-01
**Author:** jleechan2015
**Stats:** +195/-0 in 3 files

## Summary
- Fix TypeError in DeferredRewardsAgent when called with dice_roll_strategy parameter
- Add dice_roll_strategy: str | None = None to build_system_instructions() signature
- Ensures Liskov Substitution Principle compliance with parent class

**Key themes:**
- Signature compatibility fix for polymorphic agent calls
- TDD-driven bug fix with comprehensive test coverage

## Raw Body
## Summary
- Fix TypeError in DeferredRewardsAgent when called with dice_roll_strategy parameter
- Add dice_roll_strategy: str | None = None to build_system_instructions() signature
- Ensures Liskov Substitution Principle compliance with parent class

**Key themes:**
- Signature compatibility fix for polymorphic agent calls
- TDD-driven bug fix with comprehensive test coverage

## Production Code Changes

### 1. mvp_site/agents.py (+3 lines)
**Purpose:** Agent class definitions for different game modes

- **Added dice_roll_strategy parameter to DeferredRewardsAgent.build_system_instructions()** - Method override was missing parameter from parent class (FixedPromptAgent) -> Added dice_roll_strategy: str | None = None -> llm_service.py passes this to all agents polymorphically, causing TypeError when DeferredRewardsAgent was routed to

## Test Changes
- Added mvp_site/tests/test_end2end/test_deferred_rewards_dice_strategy_end2end.py (4 tests)
- Added test_agents.py::test_build_system_instructions_accepts_dice_roll_strategy

**Test results:** All 9 DeferredRewardsAgent tests pass

Generated with Claude Code

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Small signature-compatibility change plus tests; behavior is unchanged aside from preventing a runtime `TypeError` when this agent is invoked with `dice_roll_strategy`.
> 
> **Overview**
> Fixes a polymorphic call crash by updating `DeferredRewardsAgent.build_system_instructions()` to accept the `dice_roll_strategy` keyword argument (while still not using it for prompt building).
> 
> Adds unit and end-to-end regression tests to ensure `DeferredRewardsAgent`’s method signature stays compatible with `FixedPromptAgent`/`llm_service.py` call patterns and prevents `TypeError` when deferred rewards routing is selected.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 6be1e7a4e894193b547fdbe3584cd6cf3ec1b886. This will update automatically on new commits. Configure [here](http
