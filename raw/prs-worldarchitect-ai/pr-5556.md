# PR #5556: chore: Update scripts and CI tooling

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-02-16
**Author:** jleechan2015
**Stats:** +67/-19 in 5 files

## Summary
(none)

## Raw Body
Split out from PR #4534 — independent CI/script improvements.

- Improve run_tests.sh test runner
- Add mcp_smoke_test.sh script
- Update test_dependency_analyzer and its test
- Enhance validate_imports.py

**5 files, ~60 lines** — zero schema coupling.

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Mostly CI/script changes, but they directly affect which tests run and how failures/timeouts are classified, so misconfiguration could mask failures or change CI signal.
> 
> **Overview**
> Tightens `run_tests.sh` CI behavior by expanding `--exclude-mcp` patterns (to catch common `*_mcp_test.py` / `test_*_mcp.py` naming), adding coverage-aware timeout floors (including a longer guardrail for `test_world_logic.py`), and hardening result collection so interrupted runs still record a failure marker and missing result files are treated as *skipped*.
> 
> Improves MCP smoke testing by preferring the project `venv` interpreter (via `PYTHON_EXEC`) and logging which Python is used.
> 
> Speeds up `scripts/test_dependency_analyzer.py` by caching glob expansions and adjusts its safety-threshold unit test accordingly. Updates `scripts/validate_imports.py` to fix `__future__` placement and allow conditional `selenium` imports (plus a clarified `numpy` comment).
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit e30b99a59f4ea7391d9547ce6970ba1bccdc4db7. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->

## Summary by CodeRabbit

* **Chores**
  * Improved test execution reliability with enhanced error handling for missing test results
  * Augmented timeout logic with coverage-aware adjustments for consistent test execution
  * Expanded test exclusion patterns to filter additional test file types
  * Added Python environment detection to automatically use project virtual envi
