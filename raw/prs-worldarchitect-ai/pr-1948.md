# PR #1948: Centralize Codex automation intro text

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2025-11-05
**Author:** jleechan2015
**Stats:** +97/-6 in 3 files
**Labels:** codex

## Summary
- centralize the Codex automation intro text in `codex_config` and expose helpers for formatting assistant mentions
- reuse the shared intro builder when composing PR monitor comments to avoid string drift
- update the targeting test to assert against the shared intro helper instead of a duplicated literal

## Raw Body
## Summary
- centralize the Codex automation intro text in `codex_config` and expose helpers for formatting assistant mentions
- reuse the shared intro builder when composing PR monitor comments to avoid string drift
- update the targeting test to assert against the shared intro helper instead of a duplicated literal

## Testing
- python -m pytest automation/jleechanorg_pr_automation/tests/test_pr_targeting.py *(fails repository coverage threshold even though targeted tests pass)*

------
https://chatgpt.com/codex/tasks/task_e_690a470beedc832f8c10f45f14c47c34

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Centralizes comment intro/assistant mentions in `codex_config` and updates PR monitor and tests to use the shared intro builder.
> 
> - **automation/jleechanorg_pr_automation**:
>   - **codex_config.py**:
>     - Add `compose_assistant_mentions`, `DEFAULT_ASSISTANT_MENTIONS`, and `CODEX_COMMENT_INTRO_BODY`.
>     - Introduce `build_comment_intro` with helpers `_extract_review_assistants` and `_format_review_assistants` to generate intro prose and mentions.
>     - Update `CODEX_COMMENT_TEMPLATE` to use `{comment_intro}` and adjust `build_default_comment` accordingly.
>   - **jleechanorg_pr_monitor.py**:
>     - Use `build_comment_intro` when composing the automation comment intro; update imports.
> - **Tests**:
>   - Update marker extraction test to use `build_comment_intro`.
>   - Add tests validating intro prose (no duplicate `@` in prose; no leading space when mentions are blank).
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 730e537b0a1f22e6dc88f739b5d102277958205a. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->
## Summary by CodeRabbit

* **Refactor**
  * Modularized comment-generation so the comment intro and assistant mentions are built centrally, enabling m
