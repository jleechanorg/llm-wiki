# PR #2510: feat(orch): Add --agent-cli with gemini default and document all options

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2025-12-21
**Author:** jleechan2015
**Stats:** +144/-20 in 4 files

## Summary
- Add new `--agent-cli` argument to `/orch` command that defaults to `gemini` CLI
- Document all optional arguments in both `orch.md` and `orchestrate.md`
- Support CLI chain for fallback (e.g., 'gemini,claude')

## Test Plan
- [x] Verify `python3 orchestration/orchestrate_unified.py --help` shows all new options
- [x] Verify `--agent-cli` defaults to "gemini"
- [ ] Test orchestration with different CLI options

Refs: PR #2492

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Introduce `--agent-cli` (default gemini) with fallback chain, wire it through orchestration flow, and document/tests updates across `/orch` and `/orchestrate`.
> 
> - **Orchestration CLI**:
>   - Add `--agent-cli` option with default `gemini`, support c

## Raw Body
## Summary
- Add new `--agent-cli` argument to `/orch` command that defaults to `gemini` CLI
- Document all optional arguments in both `orch.md` and `orchestrate.md`
- Support CLI chain for fallback (e.g., 'gemini,claude')

## Changes
1. **orchestration/orchestrate_unified.py**: Added `--agent-cli` argument with default "gemini"
2. **.claude/commands/orch.md**: Added options table and usage examples
3. **.claude/commands/orchestrate.md**: Added options table with comprehensive examples

## Available Options
| Option | Description | Default |
|--------|-------------|---------|
| `--agent-cli <cli>` | Agent CLI to use (claude, codex, gemini, cursor). Supports chain | gemini |
| `--context <path>` | Path to markdown file to inject into agent prompt | - |
| `--branch <name>` | Force checkout of specific branch | - |
| `--pr <number>` | Existing PR number to update | - |
| `--mcp-agent <name>` | Pre-fill agent name for MCP Mail | - |
| `--bead <id>` | Pre-fill bead ID for tracking | - |
| `--validate <cmd>` | Validation command to run after completion | - |
| `--no-new-pr` | Block new PR creation | false |
| `--no-new-branch` | Block new branch creation | false |

## Test plan
- [x] Verify `python3 orchestration/orchestrate_unified.py --help` shows all new options
- [x] Verify `--agent-cli` defaults to "gemini"
- [ ] Test orchestration with different CLI options

Refs: PR #2492

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Introduce `--agent-cli` (default gemini) with fallback chain, wire it through orchestration flow, and document/tests updates across `/orch` and `/orchestrate`.
> 
> - **Orchestration CLI**:
>   - Add `--agent-cli` option with default `gemini`, support comma-separated fallback chain, and validate against `CLI_PROFILES`.
>   - Propagate `agent_cli` into agent specs for both continuation and creation; include in logging and conditional option display (hide default unless explicitly provided).
> - **Documentation**:
>   - Update `/orch` and `/orchestrate` docs
