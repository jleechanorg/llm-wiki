# PR #4821: fix(settings): consume stdin in statusLine command

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-02-04
**Author:** jleechan2015
**Stats:** +1/-1 in 1 files

## Summary
Applies the stdin consumption fix identified in jleechanorg/claude-commands#231 review.

**Change:** Add `cat >/dev/null;` at the beginning of the statusLine bash command to consume any stdin that might be passed, preventing potential issues when the command is called in contexts with unexpected input data.

## Test Plan
- [x] Verify statusLine displays correctly in Claude Code
- [x] Verify no functional regression in git header display

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Single config change to a status-line shell command; main risk is minor portability/performance issues from the added `python3`/stdin handling.
> 
> **Overview**
> Ensures the Claude Code `statusLine` command no longer gets stuck or misbehaves when stdin is pre

## Raw Body
## Summary

Applies the stdin consumption fix identified in jleechanorg/claude-commands#231 review.

**Change:** Add `cat >/dev/null;` at the beginning of the statusLine bash command to consume any stdin that might be passed, preventing potential issues when the command is called in contexts with unexpected input data.

## Related

- Identified during review of: https://github.com/jleechanorg/claude-commands/pull/231
- Copilot review comment: statusLine command stdin handling change

## Test plan

- [x] Verify statusLine displays correctly in Claude Code
- [x] Verify no functional regression in git header display

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Single config change to a status-line shell command; main risk is minor portability/performance issues from the added `python3`/stdin handling.
> 
> **Overview**
> Ensures the Claude Code `statusLine` command no longer gets stuck or misbehaves when stdin is present by *non-blockingly* reading a small stdin payload (when piped) and forwarding it into `git-header.sh`.
> 
> If no repo-local or home `git-header.sh` is available, the existing fallback output (folder/branch/home) is unchanged.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 81535104b31336febe4b6bf17d3c84f6747f4e23. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->
## Summary by CodeRabbit

* **Chores**
  * Status line command updated to accept and forward optional payloads when available, improving header output behavior while preserving previous fallbacks for non-repo or detached states.
<!-- end of auto-generated comment: release notes by coderabbit.ai -->
