# PR #5740: feat(ci): add Python 3.12 setup script for self-hosted runners

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-02-23
**Author:** jleechan2015
**Stats:** +281/-0 in 2 files

## Summary
- Adds `scripts/setup-python-runner.sh` to bootstrap Python 3.12 with venv and pip on self-hosted runners
- Adds `scripts/RUNNER_DEPLOYMENT.md` with deployment guide (SSH, GitHub Actions workflow, and direct command options)
- Eliminates "ensurepip not available" errors on self-hosted runners
- Removes need for GitHub-hosted runner fallbacks

## Raw Body
## Summary
- Adds `scripts/setup-python-runner.sh` to bootstrap Python 3.12 with venv and pip on self-hosted runners
- Adds `scripts/RUNNER_DEPLOYMENT.md` with deployment guide (SSH, GitHub Actions workflow, and direct command options)
- Eliminates "ensurepip not available" errors on self-hosted runners
- Removes need for GitHub-hosted runner fallbacks

## Production Code Changes
- **Before**: Self-hosted runners lacked Python 3.12 venv/pip, causing CI failures and requiring fallback to GitHub-hosted runners
- **Now**: Bootstrap script installs Python 3.12, python3.12-venv, and python3-pip with full verification
- **Why**: Self-hosted runner migration (#5735) needs a reliable Python environment setup

## Test Changes
- No test changes - infrastructure/deployment scripts only

## Known Limitations
- Script requires sudo access on the runner machine
- Only supports Debian/Ubuntu-based runners (apt-get)

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Infra-only changes (new script/docs) with no production runtime impact; main risk is runner-specific behavior due to `apt-get` and requiring `sudo` on the host.
> 
> **Overview**
> Adds `scripts/setup-python-runner.sh`, a sudo-only Debian/Ubuntu bootstrap that installs `python3.12`, `python3.12-venv`, and `python3-pip`, verifies `venv`/`pip`, smoke-tests venv creation, optionally creates the repo `venv/`, and installs `mvp_site/requirements.txt`.
> 
> Adds `scripts/RUNNER_DEPLOYMENT.md` documenting how to roll this out to self-hosted runners (SSH or a scheduled/manual workflow), plus verification, troubleshooting, and rollback steps, to avoid CI failures from missing `ensurepip`/venv support.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 8312a663185339dc4a6025119e5d87e4f26b0d51. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY
