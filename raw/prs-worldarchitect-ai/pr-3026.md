# PR #3026: feat(automation): Add archive mode for completed Codex tasks

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-01-05
**Author:** jleechan2015
**Stats:** +193/-7 in 1 files

## Summary
- Add `archive_completed_task()` method to archive tasks showing 'View PR' instead of 'Update branch'
- Add `archive_completed_github_mentions()` method to batch archive completed tasks
- Add `--archive` and `--archive-limit` CLI arguments

## Test Plan
- [ ] Run with --archive flag on Codex page with completed tasks
- [ ] Verify only tasks with "View PR" are archived
- [ ] Verify tasks with "Update branch" are skipped

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Adds an optional task-archiving flow alongside the existing PR update automation.
> 
> - New `archive_mode` and `archive_limit` options with CLI flags `--archive` and `--archive-limit`
> - Implements `archive_completed_task()

## Raw Body
## Summary
- Add `archive_completed_task()` method to archive tasks showing 'View PR' instead of 'Update branch'
- Add `archive_completed_github_mentions()` method to batch archive completed tasks
- Add `--archive` and `--archive-limit` CLI arguments

## Usage
```bash
# Archive up to 5 completed GitHub Mention tasks
python3 -m jleechanorg_pr_automation.openai_automation.codex_github_mentions     --use-existing-browser --archive --archive-limit 5
```

## Test plan
- [ ] Run with --archive flag on Codex page with completed tasks
- [ ] Verify only tasks with "View PR" are archived
- [ ] Verify tasks with "Update branch" are skipped

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Adds an optional task-archiving flow alongside the existing PR update automation.
> 
> - New `archive_mode` and `archive_limit` options with CLI flags `--archive` and `--archive-limit`
> - Implements `archive_completed_task()` to detect `View PR` tasks and click archive via multiple selectors
> - Adds `archive_completed_github_mentions()` to batch-archive tasks up to a limit and report archived URLs
> - Updates `run()` to switch between archive mode and the existing `process_all_github_mentions()` update flow
> - Extends constructor to accept archive options
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 860174122b3b4037e03c0179786ea40f26a95a4e. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->

## Summary by CodeRabbit

* **New Features**
  * Added archive functionality to automatically process and archive completed tasks
  * New CLI options `--archive` and `--archive-limit` to enable archiving and control the number of items archived per run

<sub>✏️ Tip: You can customize this high-level summary in your review settings.</sub>

<!-- e
