# PR #2211: Centralize Firebase token header management

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2025-12-01
**Author:** jleechan2015
**Stats:** +116/-17 in 4 files
**Labels:** codex

## Summary
- add a shared authTokenManager helper for token refresh scheduling and consistent header generation
- reuse centralized auth headers across API calls, settings loading, and downloads to align handling for test bypass and Firebase tokens

## Raw Body
## Summary
- add a shared authTokenManager helper for token refresh scheduling and consistent header generation
- reuse centralized auth headers across API calls, settings loading, and downloads to align handling for test bypass and Firebase tokens

## Testing
- Not run (not requested)


------
[Codex Task](https://chatgpt.com/codex/tasks/task_e_692d0ce3e51c832f8b67725ec031d038)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Introduces a shared authTokenManager for consistent auth headers and scheduled token refresh, updates API/Settings/Downloads to use it, and adds a 401 token refresh retry path.
> 
> - **Auth**:
>   - Introduces `window.authTokenManager` with `getAuthHeaders(forceRefresh)`, `getCurrentUser()`, and token auto-refresh scheduling/clearing.
> - **API (`frontend_v1/api.js`)**:
>   - Uses `authTokenManager` for building auth headers (including test bypass) and sets default `Content-Type`.
>   - Merges headers consistently; supports forced token refresh on retries.
>   - Adds generic 401 handler to refresh Firebase ID token and retry once.
> - **App (`frontend_v1/app.js`)**:
>   - Settings page fetch and `downloadFile` now source auth headers from `authTokenManager` (fallbacks preserved).
> - **Settings (`frontend_v1/js/settings.js`)**:
>   - `getAuthHeaders` now delegates to `authTokenManager` when available; retains test/production fallbacks.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 6bfcdb0b143d856ad340d46d1155047f91e8a595. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
