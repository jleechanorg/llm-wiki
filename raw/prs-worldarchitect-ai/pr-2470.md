# PR #2470: Adapt Claude commands to Gemini slash commands

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2025-12-18
**Author:** jleechan2015
**Stats:** +29854/-0 in 140 files
**Labels:** codex

## Summary
- Add Gemini TOML counterparts for all remaining Claude commands, mirroring the .claude/commands directory hierarchy for full coverage.
- Preserve existing curated Gemini prompts and document that all Claude commands now have Gemini equivalents in README.

## Raw Body
## Summary
- Add Gemini TOML counterparts for all remaining Claude commands, mirroring the .claude/commands directory hierarchy for full coverage.
- Preserve existing curated Gemini prompts and document that all Claude commands now have Gemini equivalents in README.

## Testing
- Not run (prompt-only changes)


------
[Codex Task](https://chatgpt.com/codex/tasks/task_e_6943779083d0832fa1c462d3474a60a0)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Adds Gemini TOML counterparts for the full Claude command set with standardized execution workflows, validation gates, and MCP integrations.
> 
> - **Gemini Commands**: Introduces `.gemini/commands/*.toml` for the entire command suite (testing, PR/review, orchestration, requirements/spec-kit, MCP tools, deployment/server, utilities).
>   - Testing: `/testllm`, `/testuif`, `/tester`, `/testerc`, `/testmcp`, `/smoke`, `/testhttp`, plus evidence capture and dual‑agent verification.
>   - PR/Review: `/reviewdeep`, `/reviewe`, `/reviewsuper`, `/reviewstatus`, `/split` with guidelines generation and GitHub posting.
>   - Orchestration/Automation: `/execute`, `/orch*`, `/converge`, `/copilot*`, `/fixpr*`, `/gene/ralph`, `/subagentvalidate`.
>   - Spec/Requirements: Spec Kit (`/spec`, `/clarify`, `/plan-spec`, `/tasks-spec`, `/implement-spec`) and `/requirements-*`.
>   - MCP/Analysis: `/second_opinion` (alias `/secondo`), `/perp`, `/memory`, `/gstatus/gst`, `/usage`, `/timeout`.
>   - DevOps/Utilities: `/localserver`, `/runlocal`, `/deploy`, `/testserver`, `/pushlite/pushl`, `/newbranch/nb`, `/sync`, `/roadmap`, `/savetmp`.
> - **Standardized Workflows**: Defines execution phases, validation gates (Total Failure Protocol, anti‑false‑positive), evidence paths, visual/AI validation, trusted‑source security filtering, and PR artifact structure.
> - **Aliases & Parity**: Provides convenient aliases (e.g., `/copilotl`, `/conv`, `/pres/slide`) ensuring feature parity with `.claude/commands`.
> - **Scope**: Prompt/config additions only; no 
