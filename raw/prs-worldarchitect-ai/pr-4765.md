# PR #4765: fix(dice): improve fabrication detection and epic stats validation

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-02-04
**Author:** jleechan2015
**Stats:** +85/-26 in 5 files

## Summary
- Fix false positive dice fabrication warnings for non-dice code execution
- Allow epic attribute values (> 30) for epic campaigns

**Key themes:**
- Dice integrity improvements
- Epic campaign support

## Raw Body
## Summary
- Fix false positive dice fabrication warnings for non-dice code execution
- Allow epic attribute values (> 30) for epic campaigns

**Key themes:**
- Dice integrity improvements
- Epic campaign support

## Production Code Changes

### 1. `mvp_site/dice_integrity.py` (+7/-1 lines)
**Purpose:** Dice fabrication detection

- **Fix audit_events check** - Checked if list was non-empty → Now checks if events are dicts with dice fields (rolls/total/dc) → Prevents false positives for XP/calculation events

### 2. `mvp_site/narrative_response_schema.py` (+4/-4 lines)
**Purpose:** Attribute validation schema

- **Allow epic attributes** - Required values 1-30 → Now requires values >= 1 (no upper limit) → Supports epic campaigns with godly stats

## Test Changes
- Updated `test_attributes_out_of_range_logs_warning` → `test_attributes_epic_values_allowed`
- Added `test_attributes_zero_or_negative_logs_warning`

## Test Results
- 8/8 fabrication detection tests pass
- 4/4 attribute validation tests pass

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Touches dice-integrity/fabrication detection and schema validation paths that gate warnings/violations; behavior changes could alter when runs are flagged or accepted, especially with mixed legacy/new `audit_events` formats.
> 
> **Overview**
> Improves code-execution dice fabrication detection by treating `action_resolution.mechanics.audit_events` as dice evidence only when they contain dice-shaped dicts (or legacy non-empty strings when `rolls` are missing), reducing false positives/negatives around non-dice audit events.
> 
> Relaxes attribute validation and prompts to allow *epic* stats by removing the `<= 30` cap (still requiring positive integers), and updates tests accordingly (including coverage for legacy string `audit_events`, epic attributes, and zero/negative attributes).
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboar
