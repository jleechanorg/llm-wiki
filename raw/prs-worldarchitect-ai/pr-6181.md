# PR #6181: fix(world-logic): merge empty dict state_updates from Dragon Knight template

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-04-11
**Author:** jleechan2015
**Stats:** +2/-2 in 1 files

## Summary
- Fix falsy check on `state_updates` dict that skipped merging when the dict was empty `{}`
- `isinstance(state_updates, dict) and state_updates` is falsy for `{}` -- removed the truthy guard since `isinstance` already excludes `None` and non-dict types
- Fixed both occurrences: Dragon Knight template path (line 4803) and LLM response path (line 4890)

## Test Plan
- [ ] Verify Dragon Knight template with empty `state_updates` dict proceeds through merge path
- [ ] Verify non-dict state_updates are still skipped
- [ ] Verify populated dict state_updates still merge correctly

Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Low risk conditional-logic change; it only affects whether `update_state_with_changes` is invoked when `state_updates` is `{}` during campaign opening generation.
> 
>

## Raw Body
## Summary
- Fix falsy check on `state_updates` dict that skipped merging when the dict was empty `{}`
- `isinstance(state_updates, dict) and state_updates` is falsy for `{}` -- removed the truthy guard since `isinstance` already excludes `None` and non-dict types
- Fixed both occurrences: Dragon Knight template path (line 4803) and LLM response path (line 4890)

## Bead
rev-p3wg (from PR #6137)

## Test plan
- [ ] Verify Dragon Knight template with empty `state_updates` dict proceeds through merge path
- [ ] Verify non-dict state_updates are still skipped
- [ ] Verify populated dict state_updates still merge correctly

Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Low risk conditional-logic change; it only affects whether `update_state_with_changes` is invoked when `state_updates` is `{}` during campaign opening generation.
> 
> **Overview**
> Ensures `state_updates` are merged into `initial_game_state` whenever they are a dict, even if the dict is empty, instead of skipping the merge due to a falsy check.
> 
> Applies this fix in both the Dragon Knight template opening path and the normal LLM opening-story response path, making behavior consistent for `{}` vs non-dict `state_updates`.
> 
> <sup>Reviewed by [Cursor Bugbot](https://cursor.com/bugbot) for commit d8e76b6602f1bcf531c484050886362355af9213. Bugbot is set up for automated code reviews on this repo. Configure [here](https://www.cursor.com/dashboard/bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->

## Summary by CodeRabbit

* **Bug Fixes**
  * Fixed campaign initialization to ensure state updates are reliably applied during game startup across all campaign creation scenarios. State configurations are now consistently processed in all cases, including edge cases that were previously incomplete, improving overall game initialization and campaign setup stability.

<!-- end 
