# PR #2042: claude md instructions on using gh cli

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2025-11-17
**Author:** jleechan2015
**Stats:** +463/-45 in 5 files

## Summary
Complete overhaul of GitHub token management and CLI documentation across the repository. Removes `~/.token` file dependencies in favor of `GITHUB_TOKEN` environment variable, adds comprehensive GitHub CLI reference documentation, and fixes all bot review comments including redundant prefix patterns and /tmp policy violations.

## Raw Body
## Summary

Complete overhaul of GitHub token management and CLI documentation across the repository. Removes `~/.token` file dependencies in favor of `GITHUB_TOKEN` environment variable, adds comprehensive GitHub CLI reference documentation, and fixes all bot review comments including redundant prefix patterns and /tmp policy violations.

## Changes

### 1. Documentation Updates (CLAUDE.md)
- **GitHub Token Section**: Updated to reflect `GITHUB_TOKEN` environment variable usage
  - Removed all references to `~/.token` file
  - Clarified that gh CLI automatically uses `GITHUB_TOKEN` (no prefix needed)
  - Updated usage patterns to remove redundant `GITHUB_TOKEN=$GITHUB_TOKEN` prefix
- **GitHub CLI Installation**: Complete rewrite with policy compliance
  - Changed from `/tmp/gh_2.40.1_linux_amd64` to `~/.local/bin/gh` (fixes TEMPORARY FILE ISOLATION violation)
  - Added `mktemp -d` for temporary extraction directory
  - Added check for existing installation (Step 0)
  - Fixed tar extraction command (download to file first)
  - Removed redundant `GITHUB_TOKEN=$GITHUB_TOKEN` prefix from all commands
  - Updated all 6+ command examples to use `~/.local/bin/gh` path

### 2. New Skill Documentation (.claude/skills/github-cli-reference.md)
- Created comprehensive 334-line reference guide for agents
- **Sections included**:
  - Installation with policy compliance (mktemp, ~/.local/bin, conditional check)
  - Critical usage rules (full path, automatic env var, no redundant prefix)
  - Command reference (50+ examples): auth, repos, PRs, issues, workflows, labels, API
  - Troubleshooting guide (4 common errors with solutions)
  - Advanced patterns (scripting, JSON parsing, conditional checks)
  - Environment variables reference
  - Integration with other tools (jq, grep, scripts)
  - Best practices and quick copy-paste commands

### 3. Script Updates
- **`.claude/scripts/mcp_common.sh`**: 
  - Replaced `~/.token` file loading with `GITHUB_TOKEN` environment variable check
  -
