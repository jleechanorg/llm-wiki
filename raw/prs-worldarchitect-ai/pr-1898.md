# PR #1898: feat: Add dry-run support to MCP launchers and enhance local export

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2025-10-27
**Author:** jleechan2015
**Stats:** +369/-288 in 11 files

## Summary
- Added `--dry-run` flag to claude_mcp.sh and codex_mcp.sh for safe testing
- Enhanced /localexportcommands to export MCP launchers to ~/.claude/
- All required MCP dependencies exported to ~/.claude/scripts/
- Launchers work from home directory with location-aware sourcing

## Raw Body
## Summary
- Added `--dry-run` flag to claude_mcp.sh and codex_mcp.sh for safe testing
- Enhanced /localexportcommands to export MCP launchers to ~/.claude/
- All required MCP dependencies exported to ~/.claude/scripts/
- Launchers work from home directory with location-aware sourcing

## Changes
1. **MCP Launcher Enhancements**
   - claude_mcp.sh: Added --dry-run support (lines 9-17)
   - codex_mcp.sh: Added --dry-run support (lines 9-17)
   - Both convert --dry-run to --test flag for existing test mode

2. **Local Export Command Updates**
   - Exports claude_mcp.sh and codex_mcp.sh to ~/.claude/
   - Exports all MCP scripts to ~/.claude/scripts/
   - Sets executable permissions automatically
   - Added validation for launchers in checklist

## Testing
✅ Both launchers tested with dry-run from home directory:
```bash
cd ~ && ~/.claude/claude_mcp.sh --dry-run
cd ~ && ~/.claude/codex_mcp.sh --dry-run
```

Both produce expected output:
```
🔍 Detected platform: Mac (Darwin)
✅ Timeout command available: timeout
🚀 Installing MCP Servers with Enhanced Reliability...
📝 Logging to: /tmp/[product]_mcp_[timestamp].log
🧪 Test mode: Exiting early with success
```

## Benefits
- Safe testing of MCP installations without making changes
- System-wide availability of MCP launchers after /localexportcommands
- Location-aware sourcing works from both project root and ~/.claude/
- All dependencies properly exported and made executable

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Adds --dry-run to Claude/Codex MCP launchers, exports launchers/scripts via /localexportcommands, and refactors MCP scripts for safer removal, root discovery, and secure token handling.
> 
> - **MCP Launchers**
>   - Add `--dry-run` flag to `claude_mcp.sh` and `codex_mcp.sh` (alias for `--test`).
>   - Location-aware sourcing (works from repo root or `~/.claude`).
> - **Local Export** (`.claude/commands/localexportcommands.md`)
>   - Export MCP launc
