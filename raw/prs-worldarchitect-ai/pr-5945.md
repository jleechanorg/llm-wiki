# PR #5945: fix: use turn_number instead of player_turn for living world triggers

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-03-12
**Author:** jleechan2015
**Stats:** +684/-2 in 4 files

## Summary
- Fix living world events never triggering because trigger logic used `player_turn` which was always 0
- Now uses `turn_number` which is properly incremented

## Raw Body
## Summary
- Fix living world events never triggering because trigger logic used `player_turn` which was always 0
- Now uses `turn_number` which is properly incremented

## Root Cause
Campaign `JkKR510zImWiFiVHMGGV` had:
- `player_turn`: 0 (never incremented - likely due to god mode usage)
- `turn_number`: 40 (actual turn count)
- `last_living_world_turn`: 0 (never triggered)

The trigger check at `world_logic.py:5551` used `player_turn` which was always 0, so `turn_number < 1` always returned early, preventing living world events.

## Production Code Changes
- `mvp_site/world_logic.py:5551`: Use `turn_number` instead of `player_turn` for trigger check
- `mvp_site/world_logic.py:5956`: Use `turn_number` for world events fallback check  
- `mvp_site/world_logic.py:5279`: Sync `turn_number` increment alongside `player_turn`

## Test Changes
- `mvp_site/tests/test_end2end/test_living_world_end2end.py`: Add `turn_number` to test setup helper

## Known Limitations
- Requires bead rev-udih for tracking follow-up fix to centralize turn tracking

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Adjusts server-side turn counting and living-world trigger gating, which can change when world events fire for existing campaigns if counters are out of sync. Scope is localized but touches core game-state progression logic.
> 
> **Overview**
> Fixes living-world events not triggering when `player_turn` is stale (e.g., GOD mode) by gating trigger checks and fallback logic on `turn_number` instead.
> 
> Also keeps `turn_number` incremented alongside `player_turn` on normal (non-GOD, non-time-frozen) actions, and updates the living-world end-to-end test setup helper to persist `turn_number` for compatibility.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit c400aa51af40c14b8077f4bfb6df48b40f629f17. This will update automatically on new commits. Configure [here](https://curs
