# PR #6110: docs(testing): note evidence path in conftest

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-04-06
**Author:** jleechan2015
**Stats:** +490/-3284 in 19 files

## Summary
- Align MCP/UI testing evidence with repo-local `/tmp` layout (`/tmp/worldarchitectai/<repo_dir_name>/`) and document how to run script-style `testing_mcp` tests so evidence lands in the expected tree.
- Replace bulky in-repo evidence-standard copies with **stubs** pointing at user-scope canonical docs (`.cursor/rules/evidence-canonical.mdc`, `.claude/skills/…`, `.codex/skills/…`) and wire `CLAUDE.md` / `testing_mcp/CLAUDE.md` to those paths.
- Add **PR CI gate** (`.github/workflows/evidence-gat

## Raw Body
## Summary

- Align MCP/UI testing evidence with repo-local `/tmp` layout (`/tmp/worldarchitectai/<repo_dir_name>/`) and document how to run script-style `testing_mcp` tests so evidence lands in the expected tree.
- Replace bulky in-repo evidence-standard copies with **stubs** pointing at user-scope canonical docs (`.cursor/rules/evidence-canonical.mdc`, `.claude/skills/…`, `.codex/skills/…`) and wire `CLAUDE.md` / `testing_mcp/CLAUDE.md` to those paths.
- Add **PR CI gate** (`.github/workflows/evidence-gate.yml`) for changes under `testing_mcp/` or `testing_ui/` so bundle conventions stay enforced.
- Harden **skeptic-cron** verdict detection when `SKEPTIC_BOT_AUTHOR` lists multiple comma-separated GitHub logins; fix **beads** SQLite path in `.beads/config.yaml` and dedupe `.beads/issues.jsonl` rows.

## Production Code Changes

| Area | Before | Now | Why |
|------|--------|-----|-----|
| Evidence root | Fixed or less clearly tied to checkout dir | `EVIDENCE_TMP_ROOT_NAME` derived from `PROJECT_ROOT.name` in `testing_mcp/lib/evidence_utils.py` | Evidence bundles under `/tmp` must follow worktree/repo folder name without hardcoding `worldarchitect.ai` when the directory differs. |
| In-repo evidence “standards” | Large duplicated markdown | Short stubs + canonical pointer paths | Single source of truth in `~/.claude/skills/evidence-standards/`; avoids drift and huge PR noise. |
| CI | No dedicated evidence-structure gate for test harness PRs | `evidence-gate.yml` runs on `testing_mcp/**` and `testing_ui/**` | Catch missing version/constants/path assumptions before merge. |
| Skeptic cron merge job | Verdict filter assumed a single bot login | `jq` treats `SKEPTIC_BOT_AUTHOR` as comma-separated logins (trimmed) | Verdicts posted under alternate identities (e.g. PAT as user) still match. |
| Beads | `db: .beads/beads.db` (wrong relative base) | `db: beads.db` (relative to `.beads/`) | SQLite file resolves correctly for `bd`/`br` tooling. |
| Tracked noise | Committed 
