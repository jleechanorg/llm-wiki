# PR #3466: fix(automation): Allow --model param for all CLIs (not just Claude)

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-01-11
**Author:** jleechan2015
**Stats:** +344/-43 in 11 files

## Summary
(none)

## Raw Body
Removes the artificial restriction that prevented the `--model` parameter from being used with non-Claude CLIs (like Gemini). This allows users to specify `--model gemini-3-auto` or similar when using the Gemini CLI via the automation script.

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Enables model selection across all agent CLIs and makes Gemini model handling dynamic with sane defaults.
> 
> - Allow `--model` for any CLI by removing Claude-only gating; add `_normalize_model` validation and thread `model` through fix-comment/fixpr paths (`jleechanorg_pr_monitor.py`, `orchestrated_pr_runner.py`)
> - Orchestration: make Gemini command template use `{model}` and default to `gemini-3-auto`; set CLI-specific defaults when `model=='sonnet'` for Gemini/Cursor (`task_dispatcher.py`)
> - Update cron templates to pass `--model gemini-3-auto` for Gemini jobs (`automation/crontab.template`, `restore_crontab.sh`)
> - Add comprehensive tests for model normalization, dispatch behavior across CLIs, invalid model rejection, and Gemini template placeholders (`tests/...`)
> - Bump package versions: `automation` → 0.2.42, `orchestration` → 0.1.23
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 32f22a60b9b94f8157c3600f76f5782b79b9de1f. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->

## Summary by CodeRabbit

## Release Notes

* **Improvements**
  * Enhanced model parameter validation to work independently of CLI chain detection, ensuring consistent validation across all configurations.
  * Implemented CLI-specific default model assignment for better alignment with each agent's requirements.
  * Simplified model normalization logic with stricter validation standards.

<sub>✏️ Tip: You can customize this high-level summary in your review settings.</sub>

<!-- end of auto-generated comm
