# PR #1836: Fix automation daily reset and cut 0.1.1

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2025-10-07
**Author:** jleechan2015
**Stats:** +104/-11 in 8 files

## Summary
- reset global automation counter when a new day starts and normalize legacy data fields
- add regression test for the daily rollover and log reset metadata
- bump package metadata and changelog for 0.1.1 release

## Raw Body
## Summary
- reset global automation counter when a new day starts and normalize legacy data fields
- add regression test for the daily rollover and log reset metadata
- bump package metadata and changelog for 0.1.1 release

## Testing
- PYTHONPATH=automation pytest automation/jleechanorg_pr_automation/tests/test_automation_safety_manager_comprehensive.py --no-cov

## Release
- Built dist artifacts and attempted PyPI publish (token currently rejected with 403)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Fixes daily reset of the global automation limit with legacy date normalization and `last_reset` tracking, adds a rollover regression test, updates docs/metadata, and bumps version to 0.1.1.
> 
> - **Library – `jleechanorg_pr_automation`**:
>   - **Daily Reset Logic**: Normalize legacy `current_date` formats and correctly reset `total_runs` on date change in `automation_safety_manager.py`.
>     - Add `last_reset` timestamp management; validate and default missing fields; set on reset and in `_clear_global_runs()`.
>     - Initialize `global_runs.json` with `last_run` and `last_reset`.
> - **Tests**:
>   - Add rollover regression in `tests/test_automation_safety_manager_comprehensive.py` validating auto-reset and `last_reset` behavior.
> - **Docs**:
>   - Add PyPI publishing reference in `CLAUDE.md`.
>   - Update changelog with `0.1.1` notes in `automation/README.md` and egg-info.
> - **Metadata**:
>   - Bump version to `0.1.1` in `__init__.py`, `pyproject.toml`, and egg-info; adjust license metadata.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit cd2f460dbc5dfd9ff56a5ae11000892a04d0bbe2. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->

## Summary by CodeRabbit

* Bug Fixes
  * Daily automation limit now resets correctly after a date change, preventing overnight sta
