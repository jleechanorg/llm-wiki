# PR #5673: fix(llm_service): guard against dict current_location in _tier_entities

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-02-20
**Author:** jleechan2015
**Stats:** +153/-3 in 2 files

## Summary
- Production crash on campaign `uMkxhFVWRyGatnnV3qRj`: `AttributeError: 'dict' object has no attribute 'strip'` in `_tier_entities()` blocked every stream
- `world_data["current_location_name"]` stored as a dict (`{"id": "loc_ashwood_keep_001", "name": "Ashwood Keep"}`) instead of a plain string for some campaigns
- Same dict risk existed for NPC `current_location` fields inside `_tier_entities`

## Raw Body
## Summary

- Production crash on campaign `uMkxhFVWRyGatnnV3qRj`: `AttributeError: 'dict' object has no attribute 'strip'` in `_tier_entities()` blocked every stream
- `world_data["current_location_name"]` stored as a dict (`{"id": "loc_ashwood_keep_001", "name": "Ashwood Keep"}`) instead of a plain string for some campaigns
- Same dict risk existed for NPC `current_location` fields inside `_tier_entities`

## Production Code Changes

**`mvp_site/llm_service.py`**
- `_prepare_story_continuation` (line 5480): before → `current_location.strip()` crash on dict; now → `isinstance(dict)` guard extracts `name` or `id` key with warning log; why → coerce at the call site before passing downstream
- `_tier_entities` (line 461): before → NPC `npc_location.strip()` crash on dict; now → same guard for NPC location fields; why → defensive belt-and-suspenders for the same class of bad data

## Test Changes

**`mvp_site/tests/test_entity_tracking_budget.py`** — `TestLocationDictRegression` class (4 new tests):
- `test_current_location_as_dict_does_not_crash` — no AttributeError with `{id, name}` dict
- `test_current_location_as_dict_with_id_only` — no crash with `{id}` dict
- `test_npc_location_as_dict_does_not_crash` — NPC location dict handled gracefully
- `test_current_location_dict_degrades_gracefully_no_present_match` — confirms all NPCs fall to dormant (not crash) when raw dict reaches `_tier_entities`

Results: **13/13 passed** (9 existing + 4 new)

## Known Limitations

- When a dict `current_location` reaches `_tier_entities` directly (bypassing the call-site coercion), location-based PRESENT matching is lost — NPCs fall to DORMANT. This is acceptable degradation vs. a hard crash.
- Root cause of why location was stored as dict in Firestore for this campaign is not yet patched at write time.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Small defensive-type normalization plus tests; behavior chang
