# PR #5946: fix: centralize turn tracking, fix living-world UI rendering and payload normalization

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-03-13
**Author:** jleechan2015
**Stats:** +1247/-175 in 25 files

## Summary
- Centralize turn tracking via `_increment_turn_counter()` as single source of truth in `world_logic.py`
- Fix living-world payload normalization: handle `world_events.append` format (LLM sometimes returns `{append:[...]}` instead of `{background_events:[...]}`) so events surface in UI on every structured turn
- Fix living-world UI rendering: remove `debugMode` gate from `hasLivingWorldData` in `app.js` so all users (not just debug sessions) see world events
- Fix `game_state.turn_number` serial

## Raw Body
## Summary

- Centralize turn tracking via `_increment_turn_counter()` as single source of truth in `world_logic.py`
- Fix living-world payload normalization: handle `world_events.append` format (LLM sometimes returns `{append:[...]}` instead of `{background_events:[...]}`) so events surface in UI on every structured turn
- Fix living-world UI rendering: remove `debugMode` gate from `hasLivingWorldData` in `app.js` so all users (not just debug sessions) see world events
- Fix `game_state.turn_number` serialization regression exposed by centralization
- Add living-world instruction injection on every turn via `PromptBuilder` (`advances_time`-gated)
- Add evidence capture quality fix: store full JSON response body in `request_responses.jsonl` instead of 2000-char preview

## Production Code Changes

| File | Before → Now | Why |
|------|-------------|-----|
| `mvp_site/world_logic.py` | Scattered inline `player_turn += 1` | Single `_increment_turn_counter()` call; `normalize_world_events_for_story_payload` now handles top-level `append` key |
| `mvp_site/agent_prompts.py` | LW instruction injected only on interval turns | Injected every `advances_time=True` turn via `PromptBuilder` |
| `mvp_site/agents.py` | — | Wire LW instruction through agent prompt path |
| `mvp_site/game_state.py` | `turn_number` field missing from serialization | Added `turn_number` as canonical field; fixed coercion for non-int values |
| `mvp_site/frontend_v1/app.js` | `hasLivingWorldData = debugMode && (...)` | Removed `debugMode` gate; living-world panel visible to all users when data present; removed stale "(Debug)" label |

## Test Changes

- `mvp_site/tests/test_world_logic.py` — 88 new lines covering append normalization, turn counter, backfill
- `mvp_site/tests/test_world_time_progression.py` — 11 new lines covering append-payload conversion
- `mvp_site/tests/test_agents.py` — 35 new lines covering LW instruction injection gate
- `mvp_site/tests/test_prompts.py` — 50 new lines covering 
