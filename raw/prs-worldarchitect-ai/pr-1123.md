# PR #1123: fix: Handle unbound VIRTUAL_ENV variable in run_lint.sh

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2025-08-01
**Author:** jleechan2015
**Stats:** +1/-1 in 1 files

## Summary
Fixes a critical issue in the  script that causes  command failures when the linting step runs with .

## Raw Body
## Summary

Fixes a critical issue in the  script that causes  command failures when the linting step runs with .

## Problem

The current implementation checks  directly, which causes an 'unbound variable' error when  is not set and the script runs with strict error handling ().

## Solution

- Changed  to  on line 23
- Uses bash parameter expansion to handle unset variable case gracefully
- Returns empty string when  is unset instead of causing script failure

## Impact

- ✅ Fixes  command failures caused by linting step errors
- ✅ Maintains existing functionality when  is set
- ✅ Enables proper error handling with 
- ✅ Small but critical fix for development workflow stability

## Testing

- ✅ Tested with and without  set
- ✅ Verified script runs successfully with strict error handling
- ✅ Confirmed virtual environment activation still works correctly

This is a minimal, focused fix that resolves the linting workflow issue without any side effects.

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->

## Summary by CodeRabbit

* **Bug Fixes**
  * Improved detection of active Python virtual environments to prevent errors when the environment variable is unset.

<!-- end of auto-generated comment: release notes by coderabbit.ai -->
