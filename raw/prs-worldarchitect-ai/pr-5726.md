# PR #5726: docs: beads merge conflict prevention and worktree drift guidance

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-02-22
**Author:** jleechan2015
**Stats:** +39/-2 in 3 files

## Summary
- Switch `.beads/issues.jsonl` merge driver from `merge=beads` to `merge=union` (git built-in) — eliminates merge conflict markers permanently
- Document the union driver setup, recovery steps, and how the pre-commit hook auto-stages beads with every code commit
- Document worktree drift pattern: main repo dir beads can safely be discarded with `git checkout -- .beads/issues.jsonl`

## Test Plan
- [ ] Merge a branch that adds beads — verify no conflict markers appear in `.beads/issues.jsonl`
- [ ] Verify `git config merge.beads.driver` returns nothing (driver removed)

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Docs + git attribute configuration change only; low risk beyond potentially altering how `.beads/issues.jsonl` merges across branches.
> 
> **Overview**
> Switches `.beads/issues.jsonl` merging from a cu

## Raw Body
## Summary

- Switch `.beads/issues.jsonl` merge driver from `merge=beads` to `merge=union` (git built-in) — eliminates merge conflict markers permanently
- Document the union driver setup, recovery steps, and how the pre-commit hook auto-stages beads with every code commit
- Document worktree drift pattern: main repo dir beads can safely be discarded with `git checkout -- .beads/issues.jsonl`

## Production Code Changes

- `.gitattributes`: `merge=beads` → `merge=union`; removed unused `bd merge` custom driver from git config
- `.claude/skills/beads-issue-tracking.md`: added two new sections documenting the above

## Test Plan

- [ ] Merge a branch that adds beads — verify no conflict markers appear in `.beads/issues.jsonl`
- [ ] Verify `git config merge.beads.driver` returns nothing (driver removed)

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Docs + git attribute configuration change only; low risk beyond potentially altering how `.beads/issues.jsonl` merges across branches.
> 
> **Overview**
> Switches `.beads/issues.jsonl` merging from a custom `merge=beads` driver to git’s built-in `merge=union`, aiming to eliminate merge conflicts by concatenating JSONL records.
> 
> Updates beads workflow docs to explain the union-driver setup, how to detect/fix misconfiguration (including unsetting `merge.beads.driver`), and how worktrees can cause “main dir drift” in `.beads/issues.jsonl` that can be safely discarded.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 2019040306a6ac4a501cd941b5eea601b92bed6b. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->

## Summary by CodeRabbit

* **Documentation**
  * Added guides for preventing merge conflicts and managing worktree consistency in development w
