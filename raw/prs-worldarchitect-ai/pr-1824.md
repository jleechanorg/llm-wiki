# PR #1824: fix: Guard httpx/a2a imports in A2A integration tests

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2025-10-03
**Author:** jleechan2015
**Stats:** +14/-2 in 2 files

## Summary
Fixes P1 critical issue identified in PR #60 code review: httpx and A2AClient were used without importing in `orchestration/tests/test_a2a_integration.py`, causing ModuleNotFoundError during pytest collection when A2A SDK dependencies are unavailable.

## Test Plan
- [x] pytest collection succeeds with A2A dependencies unavailable
- [x] Tests skip gracefully when dependencies missing
- [x] Pre-commit hooks pass

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Guard A2A test imports with availability checks and permit conditional `httpx`/`a2a` in the import validator.
> 
> - **Tests**:
>   - Guard `httpx` and `A2AClient` imports in `orchestration/tests/test_a2a_integration.py` behind `A2A_AVAILABLE`; 

## Raw Body
## Summary
Fixes P1 critical issue identified in PR #60 code review: httpx and A2AClient were used without importing in `orchestration/tests/test_a2a_integration.py`, causing ModuleNotFoundError during pytest collection when A2A SDK dependencies are unavailable.

## Changes
- Added conditional imports for httpx and A2AClient after A2A_AVAILABLE check
- Provides stub types when dependencies unavailable to enable type checking
- Allows test module to load and skip cleanly when A2A SDK not installed

## Fixes
- P1 Issue from PR #60: pytest collection fails with ModuleNotFoundError

## Test Plan
- [x] pytest collection succeeds with A2A dependencies unavailable
- [x] Tests skip gracefully when dependencies missing
- [x] Pre-commit hooks pass

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Guard A2A test imports with availability checks and permit conditional `httpx`/`a2a` in the import validator.
> 
> - **Tests**:
>   - Guard `httpx` and `A2AClient` imports in `orchestration/tests/test_a2a_integration.py` behind `A2A_AVAILABLE`; provide stub types via `cast` when unavailable.
>   - Add `cast` to typing imports.
> - **Tooling**:
>   - Update `scripts/validate_imports.py` to allow conditional imports of `a2a` and `httpx` in tests.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit ddea2d4fbf2f0db456d05900081958942401150e. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
