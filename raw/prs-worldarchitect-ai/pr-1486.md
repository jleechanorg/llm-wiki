# PR #1486: Context Optimization: Disable GitHub MCP server by default

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2025-08-27
**Author:** jleechan2015
**Stats:** +44/-31 in 1 files

## Summary
Disable the GitHub MCP server by default to optimize context consumption, following the same pattern established in PR #1484.

## Raw Body
## Summary
Disable the GitHub MCP server by default to optimize context consumption, following the same pattern established in PR #1484.

## Changes
- ✅ **Environment Flag**: Added `GITHUB_MCP_ENABLED=${GITHUB_MCP_ENABLED:-false}` (disabled by default)
- ✅ **Function Creation**: Created `install_github_mcp()` function with environment guard
- ✅ **Conditional Logic**: Shows warning when disabled, full functionality when enabled
- ✅ **Pattern Consistency**: Follows exact same pattern as Playwright, React, and iOS Simulator servers

## Pattern Matching PR #1484
This change follows the established pattern from #1484:
```bash
# Environment flag
GITHUB_MCP_ENABLED=${GITHUB_MCP_ENABLED:-false}

# Function with guard
install_github_mcp() {
    if [[ "$GITHUB_MCP_ENABLED" != "true" ]]; then
        echo -e "${YELLOW}  ⚠️ GitHub MCP server disabled (set GITHUB_MCP_ENABLED=true to enable)${NC}"
        return 0
    fi
    # ... existing installation logic
}
```

## Context Impact
- **Token Reduction**: Reduces MCP server count by 1 (disabled by default)
- **Re-enablement**: Easy to enable with `GITHUB_MCP_ENABLED=true`
- **Consistent UX**: Same disable/enable pattern as other optional servers

## Testing
- ✅ Script syntax validation (`bash -n claude_mcp.sh`)
- ✅ Disabled mode shows proper warning message
- ✅ Function structure follows established patterns

## Re-enabling
To re-enable the GitHub MCP server:
```bash
export GITHUB_MCP_ENABLED=true
./claude_mcp.sh
```

🤖 Generated with [Claude Code](https://claude.ai/code)

Co-Authored-By: Claude <noreply@anthropic.com>

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->
## Summary by CodeRabbit

- New Features
  - Optional GitHub MCP server installation controlled by an environment flag (disabled by default).
  - When enabled, the server is set up as part of the parallel installation sequence.
  - Uses a personal access token for authorization.
  - Skips setup if already installed and provides clearer succ
