# PR #2308: fix: add explicit entity tracking token reserve to prevent context overflow

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2025-12-03
**Author:** jleechan2015
**Stats:** +381/-4 in 3 files

## Summary
- Fix ContextTooLargeError caused by entity tracking tokens not being budgeted in scaffold calculation
- Add `ENTITY_TRACKING_TOKEN_RESERVE` constant (10,500 tokens) for explicit reserve
- Replace insufficient 15% buffer (~2,700 tokens) with explicit entity tracking reserve

## Test Plan
- [x] Created `test_entity_tracking_budget.py` with 4 regression tests
- [x] All budget-related tests pass (15 tests)
- [x] Tests verify reserve covers maximum entity overhead

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Add a 10,500-token `ENTITY_TRACKING_TOKEN_RESERVE` to scaffold budgeting (replacing 15% buffer) and add tests to prevent post-truncation entity tracking from exceeding context limits.
> 
> - **LLM Service (context budg

## Raw Body
## Summary
- Fix ContextTooLargeError caused by entity tracking tokens not being budgeted in scaffold calculation
- Add `ENTITY_TRACKING_TOKEN_RESERVE` constant (10,500 tokens) for explicit reserve
- Replace insufficient 15% buffer (~2,700 tokens) with explicit entity tracking reserve

## Problem
Production error on dev GCP showed:
```
Context too large for model qwen-3-235b-a22b-instruct-2507: 
- Input: 97,923 tokens
- Max allowed: 94,372 tokens (80% of 117,964)
- Overage: ~3,500 tokens
```

Root cause: Entity tracking components (entity_preload_text, entity_specific_instructions, entity_tracking_instruction, timeline_log) are added AFTER truncation but were not budgeted.

## Test plan
- [x] Created `test_entity_tracking_budget.py` with 4 regression tests
- [x] All budget-related tests pass (15 tests)
- [x] Tests verify reserve covers maximum entity overhead

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Add a 10,500-token `ENTITY_TRACKING_TOKEN_RESERVE` to scaffold budgeting (replacing 15% buffer) and add tests to prevent post-truncation entity tracking from exceeding context limits.
> 
> - **LLM Service (context budgeting)**:
>   - Add `ENTITY_TRACKING_TOKEN_RESERVE` (10,500) and include it in `scaffold_tokens` to account for post-truncation entity tracking (`entity_preload_text`, `entity_specific_instructions`, `entity_tracking_instruction`, `timeline_log`).
>   - Replace prior 15% scaffold buffer; update budget logging to reflect raw scaffold + entity reserve.
> - **Tests**:
>   - Add unit tests `mvp_site/tests/test_entity_tracking_budget.py` validating reserve size against typical, production, and worst-case overheads.
>   - Add end-to-end tests `mvp_site/tests/test_end2end/test_entity_tracking_budget_end2end.py` ensuring large contexts with many NPCs (incl. stress test) no longer overflow and return 200 responses.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit
