# PR #5536: Fix living-world schema drift: include last_living_world_* fields

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-02-15
**Author:** jleechan2015
**Stats:** +76/-20 in 2 files

## Summary
This PR fixes living-world schema drift on `origin/main` by adding missing top-level fields to `mvp_site/schemas/game_state.schema.json`:
- `last_living_world_turn`
- `last_living_world_time`

It also adds a regression test in `mvp_site/tests/test_game_state.py` to ensure these schema fields remain present.

## Raw Body
## Summary
This PR fixes living-world schema drift on `origin/main` by adding missing top-level fields to `mvp_site/schemas/game_state.schema.json`:
- `last_living_world_turn`
- `last_living_world_time`

It also adds a regression test in `mvp_site/tests/test_game_state.py` to ensure these schema fields remain present.

## Root Cause / Break Origin
The regression was introduced when living-world tracking fields were added in PR **#4540** (`c5deeb8fd`) to `mvp_site/game_state.py`, but corresponding schema fields were not added.

## Why this is minimal
- No behavioral refactor
- No unrelated schema reshaping
- Only schema field backfill + one targeted test

## Validation
- `./vpython -m pytest -q mvp_site/tests/test_game_state.py -k \"schema_includes_living_world_tracking_fields or test_to_dict\"`
- Result: `3 passed`

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Schema-only additions plus a targeted regression test; no runtime logic changes, so risk is limited to consumers validating against the updated schema.
> 
> **Overview**
> Fixes **game state schema drift** by adding top-level living-world tracking fields to `game_state.schema.json`: `last_living_world_turn` (int) and `last_living_world_time` (nullable `WorldTime`).
> 
> Adds a regression test in `test_game_state.py` that loads the schema JSON and asserts these fields (and the `anyOf` shape for `last_living_world_time`) remain present; the rest of the test-file changes are formatting/import cleanup to support that test.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 9802d7cd73c2548fa81a6bf7ef0db429b6845fe6. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->

## Summary by CodeRabbit

## Release Notes

* **New Features**
  * Enhanced living world system with automatic tracking of when world events are
