# PR #5569: fix: Only trigger CLI fallback on rate limit if exit code is non-zero

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-02-16
**Author:** jleechan2015
**Stats:** +58/-1 in 3 files

## Summary
- Fix false positive rate limit detection in CLI fallback logic
- Rate limit now only triggers fallback when exit code is non-zero (actual failure)
- Prevents unnecessary fallback when informational messages mention "rate limit"

## Raw Body
## Summary
- Fix false positive rate limit detection in CLI fallback logic
- Rate limit now only triggers fallback when exit code is non-zero (actual failure)
- Prevents unnecessary fallback when informational messages mention "rate limit"

## Problem
The fallback detection triggered on ANY "rate limit" text in output, even when the agent completed successfully (exit code 0). This caused false positives from CodeRabbit comments and other informational messages.

## Solution
Modified `orchestration/task_dispatcher.py` line ~2414 to only set RATE_LIMITED=1 when:
1. Non-zero exit code AND
2. Rate limit pattern detected

This matches the recommended fix from bead: `rate-limit-false-positive-fix.md`

🤖 Generated with [Claude Code](https://claude.ai/code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Small, localized change to fallback heuristics that reduces spurious retries; primary risk is missing rare rate-limit cases that still exit 0.
> 
> **Overview**
> Prevents unnecessary CLI fallback by tightening rate-limit detection in the runtime attempt loop.
> 
> `orchestration/task_dispatcher.py` now only marks an attempt as rate-limited when **both** the attempt exit code is non-zero and the log segment matches the rate-limit pattern, avoiding false positives from informational text in successful runs.
> 
> Adds a new bead writeup (`.beads/rate-limit-false-positive-fix.md`) and updates `.beads/latest` to document the issue and recommended approach.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 00b8b673a7e9fe86c31656ec7e90847f6594bc4b. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
