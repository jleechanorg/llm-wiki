# PR #4641: test(automation): restore global runs rolling window

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-02-04
**Author:** jleechan2015
**Stats:** +243/-235 in 6 files

## Summary
(none)

## Raw Body
Restore the global runs rolling window tests (originally added in commit 24ec03b8, removed by the broad revert 90364511e).

This preserves coverage for rolling-window safety limits on automation runs.

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Changes core automation safety limiting and persistence semantics (daily reset to rolling window), which can affect when automation is allowed/blocked; behavior is covered by updated window-focused tests and remains configurable via env.
> 
> **Overview**
> Restores **rolling-window global run accounting** in `AutomationSafetyManager`: persisted state now tracks individual run timestamps (`runs`) instead of relying on a daily reset, trims counts to a configurable window via `AUTOMATION_GLOBAL_WINDOW_HOURS`, and normalizes legacy payloads (`total_runs`/`last_run`) into the new format.
> 
> Tightens timestamp parsing and UTC handling, adds a test helper (`_record_global_run_at_time`) for deterministic window tests, and updates `_clear_global_runs` accordingly. Tests are rewritten to validate rolling-window semantics (expiration, limit enforcement, and configurable window).
> 
> Separately improves PR comment tooling robustness: comment fetch now tolerates missing/null `user` fields, `_requires_response` skips AI responder and threaded bot replies, and `commentreply.validate_comment_data` no longer hard-fails when author metadata is missing (defaults to `"unknown"`). The PR monitor also standardizes test/CI preflight skipping by using `_parse_bool_env` for relevant env vars.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit a0f582034ec80806df621842d4f136578bffb1b1. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->
## Summary by CodeRabbit

* **New Features**
  * Global PR automation now enforces limits using a conf
