# PR #2207: Refine testing environment handling for model selection

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2025-12-01
**Author:** jleechan2015
**Stats:** +55/-20 in 3 files
**Labels:** codex

## Summary
- stop the TESTING environment flag from forcing the Gemini test model and instead rely on MOCK_SERVICES_MODE or an explicit FORCE_TEST_MODEL override
- add documentation for the new FORCE_TEST_MODEL flag so running servers with TESTING=true do not unexpectedly swap models
- expand centralized model selection tests to cover the updated override behavior and ensure TESTING alone uses normal model selection

## Raw Body
## Summary
- stop the TESTING environment flag from forcing the Gemini test model and instead rely on MOCK_SERVICES_MODE or an explicit FORCE_TEST_MODEL override
- add documentation for the new FORCE_TEST_MODEL flag so running servers with TESTING=true do not unexpectedly swap models
- expand centralized model selection tests to cover the updated override behavior and ensure TESTING alone uses normal model selection

## Testing
- python -m pytest mvp_site/tests/test_centralized_model_selection.py


------
[Codex Task](https://chatgpt.com/codex/tasks/task_e_692cffeaa9d8832f94cdb3f7bc2976e6)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Adds FORCE_TEST_MODEL env var and changes model selection to use mock/force overrides while TESTING no longer switches models; updates docs and tests.
> 
> - **Backend (model selection)**
>   - Update `mvp_site/llm_service.py::_select_model_for_user()` to prioritize `MOCK_SERVICES_MODE` or new `FORCE_TEST_MODEL` for using `TEST_MODEL`.
>   - `TESTING` no longer forces the test model; falls back to user preference or `DEFAULT_MODEL`.
> - **Tests**
>   - Expand `mvp_site/tests/test_centralized_model_selection.py` to cover:
>     - `FORCE_TEST_MODEL=true` returns `TEST_MODEL` and overrides user preference.
>     - Regression: `MOCK_SERVICES_MODE=true` still returns `TEST_MODEL`.
>     - `TESTING=true` alone does not force test model; valid/invalid user preferences handled.
>     - Verify `TEST_MODEL`/`DEFAULT_MODEL` values.
> - **Docs**
>   - Update `mvp_site/README.md` Environment Variables: clarify `TESTING` behavior and document new `FORCE_TEST_MODEL`.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 5f3f329cc61513f9d79d2ffbc6c25ad689e9ec68. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->

## Summary by CodeRabbit

* **Documentation**
  *
