# PR #1848: fix: ensure codex MCP servers register env blocks correctly

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2025-10-08
**Author:** jleechan2015
**Stats:** +17/-11 in 1 files

## Summary
- keep CLI args before env blocks when invoking `mcp add` to avoid swallowing `--stdio` and other arguments
- ensure all helper installs reuse cached env arrays instead of rebuilding order-specific lists
- fix memory/perplexity/worldarchitect setups to register their environment variables without leaking positional params

## Raw Body
## Summary

- keep CLI args before env blocks when invoking `mcp add` to avoid swallowing `--stdio` and other arguments
- ensure all helper installs reuse cached env arrays instead of rebuilding order-specific lists
- fix memory/perplexity/worldarchitect setups to register their environment variables without leaking positional params

## Testing

- Not run (script change)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Reorders mcp add arguments to preserve CLI flags and correctly register env vars; updates memory/perplexity/worldarchitect setups and uses NPX_PATH consistently.
> 
> - **scripts/mcp_common.sh**:
>   - **Argument Order**: Standardizes `mcp add` invocation to place `cli` flags before env flags and before positional args to avoid swallowing flags like `--stdio` and ensure env blocks apply.
>   - **Env Handling**:
>     - Builds and passes env arrays (e.g., `memory_env_flags`, `perplex_env_flags`, `world_env_flags`) instead of inline `--env ...` at the end.
>     - Adds `PYTHONPATH=$REPO_ROOT` for `worldarchitect` and passes `--stdio`.
>     - Ensures Grok uses combined `grok_env_flags` with reordered args.
>   - **NPX Consistency**: Replaces hardcoded `npx` with `"$NPX_PATH"` and adjusts wrapper script to exec `"$NPX_PATH"`.
>   - **Server Adds Updated**: Reorders/cleans `mcp add` calls for `react-mcp`, `ios-simulator-mcp`, `memory-server`, `perplexity-ask`, and `filesystem` to the standardized pattern.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit fba2c91f8b3e36edf6ae35d14f535b710303d019. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
