# PR #3874: Fix broken CI: remove obsolete tests and fix test env

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-01-20
**Author:** jleechan2015
**Stats:** +38/-143 in 9 files

## Summary
(none)

## Raw Body
Fix broken CI and complete Gemini 3 Pro cleanup

This PR addresses the CI breakage and incomplete cleanup following commit 6ad3ac77.

### CI & Environment Fixes
- **Fixed test failures:** Removed or updated tests in `mvp_site/tests/test_llm_provider_selection.py` and `test_llm_model_selection.py` that were still referencing the removed Gemini 3 Pro model.
- **Fixed test environment:** Updated `run_tests.sh` to export `WORLDAI_DEV_MODE=true` in CI simulation mode, preventing validation errors when GCP credentials are present.
- **Fixed deployment parsing:** Updated `deploy.sh` to:
    1. Use separate `--set-env-vars` flags for each environment variable.
    2. **Explicitly escape commas** in `RATE_LIMIT_EXEMPT_EMAILS` (e.g. `a,b` -> `a\,b`). This is required because `gcloud` uses commas as delimiters even within a single flag.

### Cleanup & Defaults
- **Frontend Cleanup:** Removed obsolete Gemini 3 Pro selection UI and allowlist logic from both v1 and v2 frontends.
- **Updated Defaults:** Updated default model references in `llm_response.py` and `fake_llm.py` from `gemini-3-pro-preview` to `gemini-3-flash-preview`.
- **Legacy Redirects:** Updated frontend model mapping to redirect any legacy `pro` or `2.5` selections to `gemini-3-flash-preview`.

### Documentation
- Updated `CLAUDE.md` to advise against running the full test suite locally due to execution time.

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->
## Summary by CodeRabbit

* **Documentation**
  * Added duplicated guidance on running tests (avoid running full suite locally; run specific test files; use CI) and a safe data-handling note.

* **Tests**
  * Removed three provider-selection tests and updated other tests to align mocks and expectations with new preferred/default models.

* **Chores**
  * CI simulation now exports a development-mode flag; deploy environment variable formatting adjusted.

* **Refactor**
  * Settings UI simplified: per-user Pro gating removed, model opti
