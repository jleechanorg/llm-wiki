# PR #4049: Spicy mode: stabilize toggle handling

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-01-25
**Author:** jleechan2015
**Stats:** +6/-3 in 3 files
**Labels:** codex

## Summary
(none)

## Raw Body
### Motivation
- Ensure spicy-mode routing and toggle commands work reliably by making user settings available on GameState and by matching UI-sent choice strings.
- Prevent UI choice clicks from failing to toggle spicy mode due to exact-text mismatches or missing user settings on the routing path.

### Description
- Add `user_settings` attribute to `GameState` and attach fetched user settings in `_prepare_game_state_with_user_settings` so agents (e.g., `SpicyModeAgent`) can read the current setting. (`mvp_site/game_state.py`, `mvp_site/world_logic.py`)
- Broaden and normalize the server-side spicy toggle handling to accept UI choice key formats and underscore variants, split off any `" - "` suffix added by the frontend, and simplify toggle assignment. (`mvp_site/world_logic.py`)
- Minor test tidy: remove stray blank lines/formatting in `TestSpicyModeInjection` and ensure tests assert the injection behavior consistently. (`mvp_site/tests/test_world_logic.py`)
- Commit includes automation marker in the message (`[codex-automation-commit]`).

### Testing
- Ran `pytest mvp_site/tests/test_world_logic.py::TestSpicyModeInjection -q` and observed all tests in that suite pass (`8 passed, 2 warnings`).
- Local pre-commit checks were evaluated during the workflow and a commit was made with the automation marker after validation steps.

------
[Codex Task](https://chatgpt.com/codex/tasks/task_e_697657ebb490832fb0fa0d892690c74e)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Strengthens spicy-mode reliability and state access.
> 
> - Add `user_settings` to `GameState` and set it in `_prepare_game_state_with_user_settings` so agents can read current settings
> - Broaden server-side toggle parsing: accept underscore key variants (e.g., `enable_spicy_mode`, `disable_spicy_mode`, `exit_spicy_mode`) and strip frontend `" - "` suffix; simplify toggle assignment (`new_spicy_state = is_enable_spicy`)
> - Small test tidy in `tests/test_world_logic.py`
> 
> <sup>Written by [Cursor Bugbot](ht
