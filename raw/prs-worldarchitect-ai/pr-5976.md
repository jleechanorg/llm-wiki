# PR #5976: feat(mem0): add Codex SessionStart+Stop hooks for automatic memory recall/save

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-03-15
**Author:** jleechan2015
**Stats:** +219/-0 in 3 files

## Summary
- Adds `.codex/hooks/mem0_recall.py` — SessionStart hook that searches the `openclaw_mem0` qdrant store and injects top-K memories as context before each Codex session
- Adds `.codex/hooks/mem0_save.py` — Stop hook that extracts facts from the last assistant turn and saves to qdrant via mem0 LLM extraction (`infer=True`)
- Updates `.codex/hooks.json` to wire both hooks alongside the existing git-header stop hook

## Raw Body
## Summary
- Adds `.codex/hooks/mem0_recall.py` — SessionStart hook that searches the `openclaw_mem0` qdrant store and injects top-K memories as context before each Codex session
- Adds `.codex/hooks/mem0_save.py` — Stop hook that extracts facts from the last assistant turn and saves to qdrant via mem0 LLM extraction (`infer=True`)
- Updates `.codex/hooks.json` to wire both hooks alongside the existing git-header stop hook

## Changes
| File | Change |
|---|---|
| `.codex/hooks/mem0_recall.py` | New — SessionStart mem0 recall (mirrors `.claude/hooks/mem0_recall.py`) |
| `.codex/hooks/mem0_save.py` | New — Stop mem0 save (mirrors `.claude/hooks/mem0_save.py`) |
| `.codex/hooks.json` | Updated — adds SessionStart entry, adds mem0_save as second Stop hook |

Mirrors the Claude hooks added in #5965, ported to the Codex hook system (v0.114.0). Both hooks fall back silently on any error and never block Codex.

## Testing
- Hooks are already running live at `~/.codex/hooks/` — validated that SessionStart injects memories and Stop saves facts across sessions
- Falls back to `{"continue": true}` on missing `OPENAI_API_KEY`, qdrant unavailable, or any exception

## Known Limitations
- Requires qdrant running locally on port 6333 and `OPENAI_API_KEY` set — gracefully skips if either is missing

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Medium risk because it introduces automatic hook-driven execution that sends session content to external services (OpenAI) and a local Qdrant store, which can affect privacy, reliability, and developer workflow if misconfigured.
> 
> **Overview**
> Adds a new Codex `SessionStart` hook that runs `mem0_recall.py` to query the `openclaw_mem0` Qdrant collection and inject top-scoring memories into the session prompt as `{"context": ...}`.
> 
> Extends the existing `Stop` hook chain to also run `mem0_save.py`, which extracts the last assistant message and persists inferr
