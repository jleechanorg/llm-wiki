# PR #5847: fix: avatar close button + double pip to 4x (176px)

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-03-04
**Author:** jleechan2015
**Stats:** +588/-37 in 5 files

## Summary
### Fix: Close Button Not Working
The `cloneNode(true)` approach for preventing listener accumulation was detaching the close button from the DOM. When `cloneNode(true)` was called on the overlay, the close button reference (`closeBtn`) was captured BEFORE the overlay replacement, leaving it orphaned. Replaced with simple `onclick` property assignment which naturally replaces previous handlers.

### Feature: Double Avatar Pip to 4x
- Desktop: 88px → 176px
- Mobile: 72px → 144px

## Raw Body
## Summary

### Fix: Close Button Not Working
The `cloneNode(true)` approach for preventing listener accumulation was detaching the close button from the DOM. When `cloneNode(true)` was called on the overlay, the close button reference (`closeBtn`) was captured BEFORE the overlay replacement, leaving it orphaned. Replaced with simple `onclick` property assignment which naturally replaces previous handlers.

### Feature: Double Avatar Pip to 4x
- Desktop: 88px → 176px
- Mobile: 72px → 144px

## Changes
- `mvp_site/frontend_v1/app.js` — Replace `cloneNode` with `onclick` for overlay + close button
- `mvp_site/frontend_v1/css/avatar.css` — Double avatar pip sizes

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Mostly UI/event-handling changes, but it also alters storage bucket resolution for avatar uploads/deletes; misconfiguration could route writes to an unexpected bucket.
> 
> **Overview**
> Improves the in-game avatar experience by moving the floating avatar into `#story-content` (text wraps around it), increasing pip size, and changing overlay/change/remove handlers to use `onclick`/`onchange` assignments to avoid listener accumulation and a broken close button.
> 
> Hardens backend avatar storage config by allowing an `AVATAR_STORAGE_BUCKET` override and rejecting Firebase URL-form bucket names (`*.firebasestorage.app`) by falling back to the default GCS bucket.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit dec555a017c89f4f3477f1b71c6bc57117c3f49f. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->
## Summary by CodeRabbit

* **Bug Fixes**
  * Prevented repeated event-handler stacking by switching to direct handlers; fixed avatar removal/cleanup and expanded-overlay close behavior.
  * Ensured avatar storage bucket resolution and validation are more 
