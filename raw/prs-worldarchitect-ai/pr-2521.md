# PR #2521: unify logging

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2025-12-22
**Author:** jleechan2015
**Stats:** +404/-94 in 5 files

## Summary
<!-- Brief description of changes -->

## Raw Body
- Add setup_unified_logging() in logging_util.py as single entry point
- Add get_repo_name() and get_branch_name() for /tmp/<repo>/<branch> paths
- Refactor setup_file_logging() in main.py to call unified setup
- Refactor setup_mcp_logging() in mcp_api.py to call unified setup
- Add initialization guard to prevent duplicate handlers
- All logs now go to both stdout/stderr (for GCP) and local file

## Summary
<!-- Brief description of changes -->

## Changes
<!-- Detailed list of what was changed -->
-
-
-

## Benefits
<!-- Why these changes improve the project -->
-
-

## Testing
<!-- How changes were tested -->
- [ ] Ran `./run_tests.sh` - all tests pass
- [ ] Tested core user workflows:
  - [ ] Campaign creation
  - [ ] Game interaction
  - [ ] Data persistence
- [ ] No regressions identified

## Screenshots
<!-- If UI changes, include before/after screenshots -->

## Checklist
- [ ] Code follows project conventions
- [ ] Tests added/updated as needed
- [ ] Documentation updated if required
- [ ] No hardcoded values or credentials
- [ ] Changes are backwards compatible

---
🤖 Generated with [Claude Code](https://claude.ai/code)
Co-Authored-By: Claude <noreply@anthropic.com>

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Introduce thread-safe, idempotent unified logging that auto-initializes and writes to both stdout and /tmp/<repo>/<branch>/<service>.log; refactor Flask/MCP to use it and add targeted tests.
> 
> - **Logging Utility (`mvp_site/logging_util.py`)**:
>   - Add unified logging with auto-initialization on import routing to both console and file under `/tmp/<repo>/<branch>/<service>.log`.
>   - Implement thread-safe, idempotent `setup_unified_logging()` with initialization guard and `is_logging_initialized()`.
>   - Add repo/branch detection via `get_repo_name()` and `get_branch_name()`; update `get_log_directory()` accordingly.
>   - Provide lazy init using `LOGGING_SERVICE_NAME` (default `"app"`).
> - **Entry Point
