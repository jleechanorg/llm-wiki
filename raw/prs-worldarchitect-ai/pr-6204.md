# PR #6204: fix(world-logic): hoist 5 fields out of rewards_box block

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-04-11
**Author:** jleechan2015
**Stats:** +76/-26 in 2 files

## Summary
5 fields were incorrectly nested inside `if hasattr(structured_response, "rewards_box"):` block in `world_logic.py:6707-6738`. They are now independent conditionals at the same level.

## Test Plan
- [ ] Level-up available on pure narrative turn → god_mode_response still fires
- [ ] Social HP challenge shows without rewards_box
- [ ] debug_info visible on narrative-only turns (debug mode)

🤖 Generated with [Claude Code](https://claude.ai/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Updates the `green-gate` workflow’s eligibility gating logic around PR review-thread pagination and author lookup; mistakes here could incorrectly block or allow the gate to run.
> 
>

## Raw Body
## Summary
5 fields were incorrectly nested inside `if hasattr(structured_response, "rewards_box"):` block in `world_logic.py:6707-6738`. They are now independent conditionals at the same level.

## Fields Fixed
| Field | Before | After |
|-------|--------|-------|
| `social_hp_challenge` | Inside rewards_box block | Independent conditional |
| `recommend_spicy_mode` | Inside rewards_box block | Independent conditional |
| `recommend_exit_spicy_mode` | Inside rewards_box block | Independent conditional |
| `debug_info` | Inside rewards_box block + debug_mode | debug_mode only |
| `god_mode_response` | Inside rewards_box block | Independent conditional |

## Impact
- `debug_info` now emitted on all turns — fixes system warnings and dice roll display data on narrative turns
- `god_mode_response` available on all turns
- `social_hp_challenge` and spicy mode fields available without rewards_box

## Root Cause
Checkpoint session fac29f6a4 (PR #2162) merged via PR #5782 placed `debug_info` inside the rewards_box block without noticing the existing fix at lines 6703-6705 for the same pattern on `action_resolution`.

## Test plan
- [ ] Level-up available on pure narrative turn → god_mode_response still fires
- [ ] Social HP challenge shows without rewards_box
- [ ] debug_info visible on narrative-only turns (debug mode)

🤖 Generated with [Claude Code](https://claude.ai/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Updates the `green-gate` workflow’s eligibility gating logic around PR review-thread pagination and author lookup; mistakes here could incorrectly block or allow the gate to run.
> 
> **Overview**
> Improves `Green Gate` eligibility Gate 5 by **failing closed when PR author lookup fails** and by **paginating GraphQL `reviewThreads`** (with a max-page guard) to avoid silent truncation when counting unresolved non-author, non-`nit:` comments.
> 
> Also tightens GraphQL error handling (detects `.errors`, missing pageInfo, and nested comment
