# PR #1755: Compose Tests: Exercise CLI and env fallbacks

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2025-09-29
**Author:** jleechan2015
**Stats:** +151/-30 in 4 files
**Labels:** codex

## Summary
- extend the compose command python tests to invoke the hook with CLI arguments and CLAUDE_COMPOSE_INPUT
- restore the shell-based compose command regression test to cover input precedence and SLASH_COMMAND_EXECUTE passthrough

## Raw Body
## Summary
- extend the compose command python tests to invoke the hook with CLI arguments and CLAUDE_COMPOSE_INPUT
- restore the shell-based compose command regression test to cover input precedence and SLASH_COMMAND_EXECUTE passthrough

## Testing
- python3 .claude/hooks/tests/test_multi_player_composition.py
- bash .claude/hooks/tests/test_compose_commands.sh

------
https://chatgpt.com/codex/tasks/task_e_68d666ee803c832f86f3de95d45105c7

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Prefer CLI args and CLAUDE_COMPOSE_INPUT over stdin, pass through SLASH_COMMAND_EXECUTE patterns, and add robust, TTY-aware stdin timeouts with expanded tests.
> 
> - **Hooks**:
>   - **compose-commands.sh**:
>     - Prefer CLI args, then `CLAUDE_COMPOSE_INPUT`, then timed `safe_read_stdin` for input.
>     - Pass through `SLASH_COMMAND_EXECUTE:*` unchanged (exact start match).
>     - Refine command counting (`valid_cmd_count`) and related filtering logic.
> - **Utilities**:
>   - **timeout-utils.sh**:
>     - Enhance `safe_read_stdin` with timeout validation, partial-data handling, and TTY-aware nonblocking read (Python-assisted) fallback to portable timeout.
> - **Tests**:
>   - **Shell tests**: Add passthrough test for `SLASH_COMMAND_EXECUTE` and minor renumbering.
>   - **Python tests**: Extend harness to pass CLI args/env and add tests for input precedence (CLI over env, env over stdin).
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit eb8d30997333706aab97856a5643ef1a5f05bd40. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->
## Summary by CodeRabbit

- New Features
  - Environment variable and CLI argument input now take precedence over stdin.
  - TTY-aware, non-blocking stdin reads with configurable timeouts and a fallback path to avoid interactive hangs.
  - External commands can 
