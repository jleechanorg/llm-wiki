# PR #1964: docs: Add Render API environment variable safety protocol

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2025-11-08
**Author:** jleechan2015
**Stats:** +24/-0 in 1 files

## Summary
Documents critical Render API PUT behavior that replaces ALL environment variables when updating a single variable. Includes complete safety pattern with backup/update/verify workflow to prevent accidental data loss.

## Raw Body
## Summary
Documents critical Render API PUT behavior that replaces ALL environment variables when updating a single variable. Includes complete safety pattern with backup/update/verify workflow to prevent accidental data loss.

## Context
While updating GEMINI_API_KEY on Render services, discovered PUT method replaces entire env var list instead of updating single variable. This caused temporary loss of all environment variables before recovery.

## Changes
- ✅ Add RENDER API ENVIRONMENT VARIABLE MANAGEMENT section to CLAUDE.md (line 387-409)
- ✅ Document dangerous PUT behavior with clear warnings
- ✅ Provide complete bash example for safe updates
- ✅ Include verification step to prevent data loss

## Benefits
- Prevents future incidents of accidental env var deletion
- Clear protocol for safe Render API updates  
- Reusable pattern for all Render deployments

## Test Results
Successfully used this protocol to:
1. Update GEMINI_API_KEY on 3 Render services
2. Restore all environment variables after accidental deletion
3. Verify stable site functionality with Playwright MCP testing

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Documents Render API PUT behavior that replaces all env vars and adds a safe backup/update/verify workflow in `CLAUDE.md`.
> 
> - **Docs**:
>   - **`CLAUDE.md`**: Add critical section on Render API env var management
>     - Warn that `PUT /services/{id}/env-vars` replaces the entire set
>     - Provide required protocol: GET all → modify target(s) → PUT full list
>     - Include bash example for backup (`backup.json`), update (`updated.json`), and apply steps
>     - Add verification step to confirm all env vars after PUT
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit afc86f6e580bbfbcf0a342d28cc0cd64f7d09fdd. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY --
