# PR #5706: Integrate: add hard-reset fallback in force-mode checkout recovery

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-02-22
**Author:** jleechan2015
**Stats:** +68/-7 in 3 files

## Summary
- Add an additional hard-reset/clean fallback when forced checkout to main still fails in --force recovery.
- This addresses tracked blockers like .beads/issues.jsonl that can still block checkout after initial forced retry.

## Raw Body
## Summary
- Add an additional hard-reset/clean fallback when forced checkout to main still fails in --force recovery.
- This addresses tracked blockers like .beads/issues.jsonl that can still block checkout after initial forced retry.

## Testing
- Not run (script-only change).

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Adds a destructive `reset --hard`/`clean -fd` recovery path in `integrate.sh --force`, which can permanently discard local changes if triggered; other changes are low-impact logging/test updates.
> 
> **Overview**
> Improves `integrate.sh` *FORCE MODE* checkout recovery: if normal and forced checkout to `main` still fails, it now clears tracked index flags for `.beads/issues.jsonl`, performs a `reset --hard` to `origin/main` (or `main`), runs a `git clean -fd` (with exclusions when the beads daemon is running), and then retries forced checkout, with consolidated success messaging.
> 
> Updates the Gemini consultant agent to capture and print CLI output for both primary and fallback model runs. Adjusts the export commands test to call `phase2_github_publish()` instead of invoking `_create_pull_request()` directly.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 641dbe7aa55ff0a06adce557baa08f588dfc2106. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->
## Summary by CodeRabbit

* **Chores**
  * Improved automatic recovery during main-branch switches: added a more robust fallback that attempts destructive recovery and cleanup when needed to restore a working state.
  * Clearer, consolidated user-facing messages during recovery so outcomes (success or fallback actions performed) are reported consistently.
<!-- end of auto-generated comment: release notes by coderabbit.ai -->
