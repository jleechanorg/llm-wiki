# PR #1936: feat: Update export configs to use unified MCP installer

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2025-11-02
**Author:** jleechan2015
**Stats:** +15/-43 in 2 files

## Summary
Updates export configuration files to reference the unified `install_mcp_servers.sh` installer (from PR #1908) instead of deprecated separate `claude_mcp.sh` and `codex_mcp.sh` launchers.

## Raw Body
## Summary

Updates export configuration files to reference the unified `install_mcp_servers.sh` installer (from PR #1908) instead of deprecated separate `claude_mcp.sh` and `codex_mcp.sh` launchers.

## Scope

**This PR only updates export configuration files - no code changes.**

### Files Changed (2 files, +15 -43)

✅ **Updated** `.claude/commands/exportcommands.py`:
  - Replaced `claude_mcp.sh` with `install_mcp_servers.sh` in script_patterns
  - Removed `codex_mcp.sh` from mcp_helper_scripts
  - Updated legacy_candidates to reference unified installer paths

✅ **Updated** `.claude/commands/localexportcommands.md`:
  - Removed old launcher references from "What Gets Exported" section
  - Added `install_mcp_servers.sh` to mcp_scripts consolidation array
  - Removed entire old launcher export section (28 lines)
  - Updated validation checklist and usage instructions

## What This Does

**Before:** Export commands referenced deprecated launchers
```bash
# Old deprecated launchers
~/.claude/claude_mcp.sh --dry-run
~/.claude/codex_mcp.sh --dry-run
```

**After:** Export commands reference unified installer
```bash
# New unified installer (from scripts/install_mcp_servers.sh)
~/.claude/scripts/install_mcp_servers.sh [claude|codex|both]
~/.claude/scripts/install_mcp_servers.sh --test-dir /tmp/test claude
```

## Benefits

✅ **No code duplication** - References existing unified installer from PR #1908
✅ **Simplified export** - One installer instead of two launchers
✅ **Focused scope** - Only configuration updates, no helper script modifications
✅ **Clean migration** - Export commands now reference correct installer location

## Unified Installer (Already Exists)

The unified installer lives at `scripts/install_mcp_servers.sh` (merged in PR #1908):
- Handles Claude, Codex, or both configurations
- Built-in `--test-dir` flag for safe testing
- Global scope installation for system-wide availability

## Export Process

During export (`/localexportcommands` or `/exportcomman
