# PR #473: Fix planning block choice buttons not working

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2025-07-10
**Author:** jleechan2015
**Stats:** +86/-61 in 2 files

## Summary
- Fixed planning block choice buttons that were no longer generating due to format mismatch
- Root cause: JavaScript regex expected `**Action:**` but AI generates `**Action** -` format  
- Replaced complex regex with maintainable line-by-line parsing
- Ensured structured `planning_block` field is properly parsed for button generation

## Test Plan
- [x] Verify regex parsing works with expected format
- [x] Test line-by-line parsing generates correct choice data
- [x] Confirm choice buttons are created with proper HTML structure
- [x] Validate click handlers are attached to generated buttons
- [ ] Manual test: Load campaign and verify planning blocks generate clickable buttons
- [ ] Manual test: Click choice buttons and verify they submit properly

🤖 Generated with [Claude Code](https://claude.ai/code)

## Raw Body
## Summary
- Fixed planning block choice buttons that were no longer generating due to format mismatch
- Root cause: JavaScript regex expected `**Action:**` but AI generates `**Action** -` format  
- Replaced complex regex with maintainable line-by-line parsing
- Ensured structured `planning_block` field is properly parsed for button generation

## Changes Made
1. **Format Fix**: Updated regex pattern to match dash format (`**Action** -`) instead of colon format
2. **Code Quality**: Replaced complex regex with readable string parsing using `split()`, `indexOf()`, `substring()`
3. **Integration Fix**: Applied parsing to structured JSON `planning_block` field in `generateStructuredFieldsHTML()`

## Technical Details
- **File**: `mvp_site/static/app.js`
- **Function**: `parsePlanningBlocks()` - completely refactored for maintainability
- **Root Issue**: Mismatch between expected format in commit 1bd1f938 and parsing logic
- **Solution**: Line-by-line parsing that handles both numbered and bracket choice formats

## Test Plan
- [x] Verify regex parsing works with expected format
- [x] Test line-by-line parsing generates correct choice data
- [x] Confirm choice buttons are created with proper HTML structure
- [x] Validate click handlers are attached to generated buttons
- [ ] Manual test: Load campaign and verify planning blocks generate clickable buttons
- [ ] Manual test: Click choice buttons and verify they submit properly

🤖 Generated with [Claude Code](https://claude.ai/code)
