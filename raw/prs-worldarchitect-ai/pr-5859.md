# PR #5859: Add /coderabbit and /cr commands for CodeRabbit review prompts

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-03-05
**Author:** jleechan2015
**Stats:** +58/-0 in 2 files

## Summary
Added two new Claude commands to streamline CodeRabbit integration: `/coderabbit` and its alias `/cr`. These commands automatically post "@coderabbit all good?" comments on the PR associated with the current branch, enabling quick re-review requests.

## Raw Body
## Summary
Added two new Claude commands to streamline CodeRabbit integration: `/coderabbit` and its alias `/cr`. These commands automatically post "@coderabbit all good?" comments on the PR associated with the current branch, enabling quick re-review requests.

## Changes
- Added `.claude/commands/coderabbit.md` - Main command that detects the current branch's PR and posts a CodeRabbit review prompt
- Added `.claude/commands/cr.md` - Alias command that delegates to `/coderabbit`

## Benefits
- Provides a convenient shorthand for triggering CodeRabbit reviews without manual PR comment creation
- Reduces friction in the code review workflow by automating the comment posting step
- Alias support (`/cr`) offers quick access for frequent users

## Testing
N/A - These are command definitions that integrate with existing `gh` CLI tooling. Functionality depends on GitHub CLI availability and PR context.

## Checklist
- [x] Code follows project conventions
- [x] No hardcoded values or credentials
- [x] Changes are backwards compatible

https://claude.ai/code/session_01FReiV2mCi5gXQRG3jifsyh

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Only adds local Claude command definitions that wrap `gh` CLI calls; no application/runtime code paths or data handling are modified.
> 
> **Overview**
> Adds a new Claude git command, `/coderabbit`, that uses the GitHub CLI to look up the PR for the current branch and post an `@coderabbit all good?` comment, then reports the PR URL and success.
> 
> Adds `/cr` as a simple alias that delegates to `/coderabbit`.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 638b1fba3e2f76257c04dd1808e9248414bc21c3. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
