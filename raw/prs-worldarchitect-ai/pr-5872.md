# PR #5872: fix: add missing check_github_requirements to scripts/mcp_common.sh

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-03-08
**Author:** jleechan2015
**Stats:** +26/-0 in 1 files

## Summary
- `check_github_requirements` was called on line 1249 of `scripts/mcp_common.sh` but never defined, causing the MCP installer to crash with exit code 127 on every run
- Synced the function definition from `.claude/scripts/mcp_common.sh` (the canonical version)

## Raw Body
## Summary
- `check_github_requirements` was called on line 1249 of `scripts/mcp_common.sh` but never defined, causing the MCP installer to crash with exit code 127 on every run
- Synced the function definition from `.claude/scripts/mcp_common.sh` (the canonical version)

## Production Code Changes
- **Before:** `scripts/mcp_common.sh` called `check_github_requirements` without defining it → installer always failed
- **Now:** Function defined before its call site, matching `.claude/scripts/mcp_common.sh`
- **Why:** The two files diverged; `.claude/scripts/` had the fix but `scripts/` did not

## Test Changes
None — this is a shell function definition fix

## Known Limitations
None

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Adds a small, self-contained shell function and does not change installation flow beyond preventing a runtime crash and printing diagnostics.
> 
> **Overview**
> Fixes `scripts/mcp_common.sh` crashing (exit code 127) by adding the previously missing `check_github_requirements` function before its call site.
> 
> The new helper reports GitHub token presence/validation status (optionally invoking `test_github_token` when available) and clarifies that the GitHub remote MCP server has full vs. public-only access depending on token availability.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit c8d023acf6dcc5a2cc07b2eb023b521e8f422c08. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
