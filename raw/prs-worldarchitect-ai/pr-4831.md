# PR #4831: fix(secondo): hardcode AI Universe Firebase API key for cross-machine usage

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-02-04
**Author:** jleechan2015
**Stats:** +15/-21 in 5 files

## Summary
- Fixed /secondo command failing on other machines with `auth/api-key-not-valid` error
- Hardcoded AI Universe Firebase API key with default syntax (`:-`) instead of strict (`?:`)
- Removed Firebase credential scrubbing from exportcommands.py that was preventing exported commands from working

**Key themes:**
- Cross-machine portability for /secondo command
- Correct implementation of PR #4291 (credential scrubbing removal)

## Raw Body
## Summary
- Fixed /secondo command failing on other machines with `auth/api-key-not-valid` error
- Hardcoded AI Universe Firebase API key with default syntax (`:-`) instead of strict (`?:`)
- Removed Firebase credential scrubbing from exportcommands.py that was preventing exported commands from working

**Key themes:**
- Cross-machine portability for /secondo command
- Correct implementation of PR #4291 (credential scrubbing removal)

## Production Code Changes

### 1. `.claude/commands/second_opinion.md` (+3/-3 lines)
**Purpose:** Main /secondo command definition

- **Changed API key syntax from strict to default** - Was `${VAR:?error}` (fails if unset) → Now `${VAR:-AIzaSyAff...}` (uses default if unset) → Allows command to work without env var setup
- **Updated error message** - Was `<your-ai-universe-firebase-api-key>` placeholder → Now shows actual API key → Clearer debugging for users

### 2. `.claude/commands/exportcommands.py` (+4/-6 lines)
**Purpose:** Export commands to claude-commands repo

- **Removed Firebase API key scrubbing** - Was replacing `AIzaSyAff...` with `<YOUR_FIREBASE_API_KEY>` → Now preserves actual key → Exported commands work out-of-the-box

### 3. `.gemini/scripts/exportcommands.py` (+4/-6 lines)
**Purpose:** Mirror of exportcommands for Gemini

- **Same change as above** - Removed Firebase credential scrubbing for consistency

## Rationale (per PR #4291)
- Firebase API keys are **safe to expose publicly** (protected by domain restrictions, not secrecy)
- AI Universe is a shared service - users should use actual credentials
- Exported commands should work without additional configuration

## Test Changes
- No test changes - this is a configuration fix

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Touches authentication configuration and intentionally embeds a Firebase API key in docs/scripts; while non-secret per Firebase’s model, misconfiguration or domain-re
