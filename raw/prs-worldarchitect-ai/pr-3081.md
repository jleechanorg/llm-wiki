# PR #3081: feat: Add DeferredRewardsAgent for parallel rewards processing every 10 scenes

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-02-01
**Author:** jleechan2015
**Stats:** +1369/-7 in 34 files

## Summary
Add **DeferredRewardsAgent** for parallel rewards processing that automatically catches missed XP/loot every 10 scenes.

### Key Features
- **Parallel Injection**: Runs IN PARALLEL with StoryModeAgent (same LLM call, not separate inference)
- **Automatic Triggering**: Every 10 scenes (configurable via `DEFERRED_REWARDS_SCENE_INTERVAL`)
- **Semantic Routing**: User can also explicitly request via natural language ("check for missed rewards")
- **Explicit API Mode**: Programmatic access via `mode=

## Raw Body
## Summary

Add **DeferredRewardsAgent** for parallel rewards processing that automatically catches missed XP/loot every 10 scenes.

### Key Features
- **Parallel Injection**: Runs IN PARALLEL with StoryModeAgent (same LLM call, not separate inference)
- **Automatic Triggering**: Every 10 scenes (configurable via `DEFERRED_REWARDS_SCENE_INTERVAL`)
- **Semantic Routing**: User can also explicitly request via natural language ("check for missed rewards")
- **Explicit API Mode**: Programmatic access via `mode="deferred_rewards"`

## Production Code Changes

### 1. `mvp_site/constants.py` (+4 lines)
**Purpose:** Add mode and prompt constants for DeferredRewardsAgent

- `MODE_DEFERRED_REWARDS = "deferred_rewards"` - Mode identifier
- `PROMPT_TYPE_DEFERRED_REWARDS = "deferred_rewards"` - Prompt type key
- `DEFERRED_REWARDS_INSTRUCTION_PATH` - Path to instruction file
- `DEFERRED_REWARDS_SCENE_INTERVAL = 10` - Configurable trigger interval

### 2. `mvp_site/intent_classifier.py` (+12 lines)
**Purpose:** Add semantic intent anchor phrases for DeferredRewardsAgent routing

### 3. `mvp_site/agent_prompts.py` (+50 lines)
**Purpose:** Add PromptBuilder support for deferred rewards instructions

- `should_include_deferred_rewards(turn_number)`: Check if deferred rewards should run
- `build_deferred_rewards_instruction(...)`: Build instruction with context

### 4. `mvp_site/agents.py` (+130 lines)
**Purpose:** DeferredRewardsAgent class and parallel injection mechanism

**DeferredRewardsAgent class:**
- `REQUIRED_PROMPT_ORDER`: master → game_state → planning_protocol → rewards → deferred_rewards → dnd_srd → mechanics
- `should_run_deferred_check(turn_number)`: Delegates to PromptBuilder

**StoryModeAgent parallel injection (NEW):**
- `_add_deferred_rewards_instruction()`: Adds instruction on turns 10, 20, 30, etc.
- Runs IN PARALLEL with story mode (same LLM call)

### 5. `mvp_site/prompts/deferred_rewards_instruction.md` (+80 lines)
**Purpose:** LLM instruction for deferred rewa
