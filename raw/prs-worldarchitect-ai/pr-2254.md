# PR #2254: Add per-campaign cost estimation to campaign_manager.py

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2025-12-02
**Author:** jleechan2015
**Stats:** +955/-3 in 10 files

## Summary
- Add `cost-report` command to estimate per-campaign costs sorted by most expensive
- Calculate token usage based on story entry count and context growth (43K base + 50K world + history tokens)
- Show short vs long context pricing breakdown (Gemini 3 Pro: $2/$4 per 1M input, $12/$18 per 1M output)
- Add Claude skill documentation for Firebase production queries

## Test Plan
- [x] Tested `cost-report` command with real production data
- [x] Verified token calculations match llm_service.py constants
- [x] Confirmed output formatting and JSON export

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Adds campaign analytics/cost reporting and integrates preventive guards into story processing, with tests and production Firebase docs.
> 
> - **Scripts (campaign_manager.py)**:
>   - **New commands**: `analytics` (mon

## Raw Body
## Summary
- Add `cost-report` command to estimate per-campaign costs sorted by most expensive
- Calculate token usage based on story entry count and context growth (43K base + 50K world + history tokens)
- Show short vs long context pricing breakdown (Gemini 3 Pro: $2/$4 per 1M input, $12/$18 per 1M output)
- Add Claude skill documentation for Firebase production queries

## Usage
```bash
# November 2025 costs
WORLDAI_DEV_MODE=true python scripts/campaign_manager.py cost-report jleechan@gmail.com --month 2025-11

# All time with top 30 campaigns
WORLDAI_DEV_MODE=true python scripts/campaign_manager.py cost-report jleechan@gmail.com --top 30
```

## Test plan
- [x] Tested `cost-report` command with real production data
- [x] Verified token calculations match llm_service.py constants
- [x] Confirmed output formatting and JSON export

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Adds campaign analytics/cost reporting and integrates preventive guards into story processing, with tests and production Firebase docs.
> 
> - **Scripts (campaign_manager.py)**:
>   - **New commands**: `analytics` (monthly activity + token estimates) and `cost-report` (per-campaign blended cost ranking).
>   - **Token/cost model**: Uses `~43k` base + `~50k` world + history tokens; short/long context pricing; JSON exports.
>   - **Firebase init**: Applies clock-skew patch and supports explicit credentials via `WORLDAI_GOOGLE_APPLICATION_CREDENTIALS`.
> - **Engine**:
>   - **Preventive Guards (`mvp_site/preventive_guards.py`)**: Auto-harden `state_changes` (time, core memories, location, resource checkpoint) and synthesize `god_mode_response` when needed.
>   - **Integration (`world_logic.process_action_unified`)**: Replaces direct `get_state_updates()` with guarded `state_changes`; persists `prevention_extras` into stored structured fields and unified response.
>   - **Packaging**: Exposes `preventive_guards` via `mvp_site/__init__.py`
