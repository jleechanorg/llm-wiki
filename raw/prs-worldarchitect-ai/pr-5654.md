# PR #5654: fix: harden automation mergeability and MiniMax CLI auth handoff

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-02-20
**Author:** jleechan2015
**Stats:** +1656/-1897 in 25 files

## Summary
This PR consolidates follow-up work since `origin/main` to cover both automation mergeability handling and MiniMax/CLI authentication hardening.

## Raw Body
## Summary
This PR consolidates follow-up work since `origin/main` to cover both automation mergeability handling and MiniMax/CLI authentication hardening.

## Scope (full change set)
- Fix mergeability and PR-processing signals across REST/GraphQL paths so `fixpr` and `fix-comment` can safely triage conflicts and unknown statuses.
- Improve automation tooling behavior for marker/commit detection and wrapper lifecycle (`automation` / monitoring flows).
- Harden MiniMax CLI auth handoff and environment cleanup, including anti-leak guards so `ANTHROPIC_AUTH_TOKEN` is not interpolated into non-MiniMax launcher scripts.
- Improve operational docs and agent skills around automation monitoring and MiniMax client behavior.
- Add and expand tests for mergeability edges, cleanup behavior, and MiniMax auth environment handling.

## Test impact
- Includes changes across 36 commits, spanning automation (`jleechanorg_pr_monitor`, publish/monitor scripts) and orchestration (`task_dispatcher`, MiniMax CLI env flow).

## Notes
- PR title was updated to reflect the broader, cross-cutting nature of the change set.

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Touches automation that posts comments/dispatches agents and modifies PR eligibility/skip logic; mistakes could cause missed processing or extra automation runs, though changes are largely defensive and gated by safety limits.
> 
> **Overview**
> Improves PR automation reliability by treating **unknown/failed GitHub status lookups conservatively**, normalizing mergeability signals across REST/GraphQL (`mergeable`, `mergeStateStatus`/`mergeableState`), and tightening skip/retry logic so `fixpr`/`fix-comment` reprocess when conflicts, failing checks, or unaddressed comments persist (including stale “queued” markers).
> 
> Hardens automation attribution and feedback-loop prevention by centralizing automation marker/commit-message detection (new `workflow:agent:sha` marker format plus legacy parsing) and adding cleanup
