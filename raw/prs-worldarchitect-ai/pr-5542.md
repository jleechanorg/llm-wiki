# PR #5542: Add Codex Web setup/maintenance scripts for git remote bootstrap

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-02-15
**Author:** jleechan2015
**Stats:** +132/-0 in 2 files

## Summary
- add `scripts/codex_web_setup.sh` to bootstrap git wiring in Codex Web containers
- add `scripts/codex_web_maintenance.sh` to re-apply the same wiring on cached container resume
- handle missing `origin` remote, branch/upstream detection, and print git diagnostics

## Raw Body
## Summary
- add `scripts/codex_web_setup.sh` to bootstrap git wiring in Codex Web containers
- add `scripts/codex_web_maintenance.sh` to re-apply the same wiring on cached container resume
- handle missing `origin` remote, branch/upstream detection, and print git diagnostics

## Why
Codex Web tasks sometimes fail to push when the runtime starts without a valid `origin` or upstream branch tracking. These scripts reduce manual recovery and allow non-interactive task runs to push reliably.

## Testing
- `bash -n scripts/codex_web_setup.sh`
- `bash -n scripts/codex_web_maintenance.sh`

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Adds new opt-in bash scripts that adjust git configuration and remotes; low impact on runtime behavior but could affect a developer/container git setup if invoked with unexpected env vars.
> 
> **Overview**
> Adds `scripts/codex_web_setup.sh` to make Codex Web environments more reliable for `git push` by ensuring global push defaults, creating a missing `origin` remote from common CI/Codex env vars (with URL redaction), fetching `origin`, and attempting to set the current branch upstream to `origin/<branch>`.
> 
> Adds `scripts/codex_web_maintenance.sh` to rerun the same bootstrap on container resume and print concise post-bootstrap branch/upstream diagnostics.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 81f374bf94b5d5ee8fbb253ba165b75475ec107b. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->

## Summary by CodeRabbit

* **Chores**
  * Added a Git repository maintenance script to display branch and upstream information.
  * Added a Git repository setup script to configure remote tracking and upstream branches automatically, with support for various Git environment variables and error handling for missing repositories or remote
