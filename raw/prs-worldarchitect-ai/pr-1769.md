# PR #1769: Fix: Build iOS Simulator MCP server before configuration

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2025-09-28
**Author:** jleechan2015
**Stats:** +67/-4 in 1 files
**Labels:** codex

## Summary
- run the iOS Simulator MCP npm build step during installation so the compiled entrypoint exists
- detect the built entrypoint when configuring Claude and fall back to index.js with clear logging

## Raw Body
## Summary
- run the iOS Simulator MCP npm build step during installation so the compiled entrypoint exists
- detect the built entrypoint when configuring Claude and fall back to index.js with clear logging

## Testing
- not run (script changes only)

------
https://chatgpt.com/codex/tasks/task_e_68d89e1f5eec832f8783d658a760830c

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Builds ios-simulator-mcp during install, detects and uses the compiled entrypoint with safer reinstall, clearer failures, and minor token/deps handling tweaks.
> 
> - **iOS Simulator MCP installation**:
>   - Builds TypeScript (`npm run build`) with optional timeout and logs; fails with `BUILD_FAILED` on error.
>   - Detects runtime entrypoint, preferring `build/index.js` and falling back to `index.js`; errors with `ENTRYPOINT_MISSING` if neither exists.
>   - Uses `npm ci` when `package-lock.json` is present; otherwise `npm install`.
>   - Backs up existing installation to a timestamped `.tgz` when a `.git` directory exists before removal.
>   - Configures Claude using the detected entrypoint and updates displayed path.
> - **Environment/token**:
>   - Ensures `GITHUB_TOKEN` is exported from `GITHUB_PERSONAL_ACCESS_TOKEN` if unset.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 1a16a19c923ff97fcd5bdef40670e49dbe1623b8. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->

## Summary by CodeRabbit

* **New Features**
  * Automatically builds TypeScript during ios-simulator-mcp installation and uses the generated runtime entrypoint when available.
  * Detects the correct runtime entrypoint after install and configures the integration accordingly.

* **Chores**
  * User-facing messages now display the detected entrypoint path instead of a fixed path.
  * Installation now fails fast with a clear BUILD_FAI
