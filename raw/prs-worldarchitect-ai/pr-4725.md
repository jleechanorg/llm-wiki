# PR #4725: Campaign upgrade choice injection + tier prompts

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-02-03
**Author:** jleechan2015
**Stats:** +507/-79 in 14 files

## Summary
- Inject `upgrade_campaign` choice server-side when CampaignUpgradeAgent is selected
- Require upgrade choice in divine/sovereign ascension prompts
- Apply divine/sovereign system prompts to all agents via centralized finalization
- Improve campaign copy script pagination to avoid Firestore timeouts

## Raw Body
## Summary
- Inject `upgrade_campaign` choice server-side when CampaignUpgradeAgent is selected
- Require upgrade choice in divine/sovereign ascension prompts
- Apply divine/sovereign system prompts to all agents via centralized finalization
- Improve campaign copy script pagination to avoid Firestore timeouts

## Testing
- python -m pytest mvp_site/tests/test_world_logic.py -k \"CampaignUpgradeChoiceInjection\"
- python -m pytest mvp_site/tests/test_agents.py -k \"tier_prompt\"

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Touches core response post-processing in `world_logic` and prompt construction, so mistakes could affect player choice rendering or system prompt composition across modes; changes are guarded by agent mode/tier checks and backed by new tests.
> 
> **Overview**
> Adds **server-side enforcement** for campaign ascension flows by injecting an `upgrade_campaign` choice into `planning_block` when `CampaignUpgradeAgent` is selected (handles string/invalid JSON, list-vs-dict choice formats, and de-duplication/normalization), and persists this injection in both stored structured fields and API responses.
> 
> Centralizes **tier prompt injection** in `PromptBuilder.finalize_instructions()` so divine/sovereign system prompts apply consistently to *all* agents (including fixed-prompt agents), updates tests accordingly, and tightens ascension ceremony prompt specs to require the `upgrade_campaign` choice.
> 
> Improves ops tooling by paginating Firestore subcollection copies in `scripts/copy_campaign.py` to avoid timeouts on large campaigns; remaining changes are mostly test/cleanup tweaks (unused imports, minor assertions).
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit d38d4160d198c900b8b60ef33f21de490d3fd385. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by 
