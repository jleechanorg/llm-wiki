# PR #5977: fix(living-world): streaming path extraction + backfill for world events

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-03-15
**Author:** jleechan2015
**Stats:** +117/-27 in 2 files

## Summary
- Streaming persist path now extracts world_events from state_updates (including custom_campaign_state nesting)
- Normalizes, annotates with turn/scene, and backfills when LLM omits events
- Filters to current scene to prevent cumulative history bleed
- Adds sequence_id and user_scene_number for non-streaming parity

## Raw Body
## Summary
- Streaming persist path now extracts world_events from state_updates (including custom_campaign_state nesting)
- Normalizes, annotates with turn/scene, and backfills when LLM omits events
- Filters to current scene to prevent cumulative history bleed
- Adds sequence_id and user_scene_number for non-streaming parity

## Production Code Changes
- **Before**: streaming_orchestrator persisted raw structured_fields dict directly — world_events stayed nested in state_updates, invisible to UI
- **Now**: `_enrich_streaming_structured_fields()` mirrors non-streaming path: extract, normalize, annotate, backfill, filter
- **Why**: Campaign b9LPKcLHEwvG4FGsQDpu (101 turns) showed only 2/25 recent entries with world_events; all others had events buried in state_updates

## Test Changes
- 3 unit tests updated: check key structured_fields values instead of exact dict match (enrichment adds sequence_id/user_scene_number)
- All 64 streaming orchestrator unit tests pass
- testing_mcp living_world_e2e: 2/2 scenarios pass
- testing_http living_world_ui_visibility: 1/1 pass (7/10 turns with events, all UI-visible)

## Known Limitations
- LLM-probabilistic: not every turn generates world_events (50-70% rate observed)
- Existing campaign data not retroactively fixed (only new turns benefit)

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Changes the streaming persistence payload shape by adding `sequence_id`/`user_scene_number` and moving/normalizing `world_events`, which could affect UI consumers or downstream data expectations. Uses private `world_logic` helpers and additional deep-copying, so correctness/perf should be verified on large campaigns.
> 
> **Overview**
> Streaming persistence now *enriches* the AI `structured_fields` before saving: it extracts `world_events` out of `state_updates` (including `custom_campaign_state` nesting), normalizes and annotates them with turn/scene metadata, backfi
