# PR #5739: fix(hooks): update bd shims for bd 0.56.1 compatibility

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-02-23
**Author:** jleechan2015
**Stats:** +6/-6 in 3 files

## Summary
- 3 of 5 `.husky/_/` shims called `bd hook <name>` (bd 0.55.4 API)
- bd 0.56.1 renamed the command to `bd hooks run <name>`
- Self-hosted runner `claude-drift-runner` (Mac) has bd 0.56.1, causing git checkout to fail

## Raw Body
## Summary
- 3 of 5 `.husky/_/` shims called `bd hook <name>` (bd 0.55.4 API)
- bd 0.56.1 renamed the command to `bd hooks run <name>`
- Self-hosted runner `claude-drift-runner` (Mac) has bd 0.56.1, causing git checkout to fail

## Production Code Changes
**Before:** `exec bd hook post-checkout "$@"` (same pattern for post-merge, pre-commit)
**Now:** `exec bd hooks run post-checkout "$@"`
**Why:** bd 0.56.1 breaking rename; `pre-push` and `prepare-commit-msg` were already correct

## Test Changes
None — hook shim files only

## Known Limitations
None

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Only updates Git hook shim invocation strings; low blast radius limited to local/dev tooling, with main risk being hooks not running if the bd CLI syntax differs.
> 
> **Overview**
> Updates three Husky shim scripts (`post-checkout`, `post-merge`, `pre-commit`) to be compatible with bd `0.56.1` by switching from `bd hook <name>` to `bd hooks run <name>` and bumping the recorded `bd-hooks-version`.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit cf76b43d7ec88ee760e6acd536a3fe7936f00bb1. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
