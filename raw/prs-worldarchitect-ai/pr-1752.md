# PR #1752: Simplify slash command script lookup

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2025-09-28
**Author:** jleechan2015
**Stats:** +13/-9 in 5 files
**Labels:** codex

## Summary
- allow Cerebras aliases to load `cerebras_direct.sh` from either `~/.claude` or the repository `.claude` directory
- update `/gstatus` to resolve `gstatus.py` from the same trusted locations before execution

## Raw Body
## Summary
- allow Cerebras aliases to load `cerebras_direct.sh` from either `~/.claude` or the repository `.claude` directory
- update `/gstatus` to resolve `gstatus.py` from the same trusted locations before execution

## Testing
- not run (not requested)


------
https://chatgpt.com/codex/tasks/task_e_68d72a7b8c9c832f87fd4fcdc8ef769f

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Update command scripts to resolve `cerebras_direct.sh` and `gstatus.py` from trusted locations with fallback and clear errors.
> 
> - **Commands**:
>   - **Cerebras aliases** (`.claude/commands/c.md`, `cereb.md`, `cerebras.md`, `qwen.md`): replace direct script call with trusted-location lookup for `cerebras_direct.sh` (check `~/.claude/commands/cerebras` then repo `.claude/commands/cerebras`), run with original args, and surface clear error if missing.
>   - **`/gstatus`** (`.claude/commands/gstatus.md`): resolve `gstatus.py` from trusted locations (`~/.claude/commands` then repo `.claude/commands`) before running via `python3`, with explicit error on lookup failure.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit aa0dd3a6161118a96db5930d646855a7558d32ff. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
