# PR #6138: Fix CharacterCreationAgent hijacking LevelUpAgent

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-04-07
**Author:** jleechan2015
**Stats:** +2733/-16 in 6 files

## Summary
When a user clicks "Level Up Now", the system was assigning `character_creation_in_progress=True` as a legacy artifact. Because `CharacterCreationAgent` holds a higher precedence over `LevelUpAgent` in our routing layer, the user request gets hijacked into Character Creation processing rather than properly opening the Level Up procedure. This left the user seeing the generic Level Up Rewards box mapped to a Finish button rather than offering their proper planning block with selection choices.

## Background
When a user clicks "Level Up Now", the system was assigning `character_creation_in_progress=True` as a legacy artifact. Because `CharacterCreationAgent` holds a higher precedence over `LevelUpAgent` in our routing layer, the user request gets hijacked into Character Creation processing rather than properly opening the Level Up procedure. This left the user seeing the generic Level Up Rewards box mapped to a Finish button rather than offering their proper planning block with selection choices.

## Raw Body
## Background
When a user clicks "Level Up Now", the system was assigning `character_creation_in_progress=True` as a legacy artifact. Because `CharacterCreationAgent` holds a higher precedence over `LevelUpAgent` in our routing layer, the user request gets hijacked into Character Creation processing rather than properly opening the Level Up procedure. This left the user seeing the generic Level Up Rewards box mapped to a Finish button rather than offering their proper planning block with selection choices.

## Goals
Ensure that `LevelUpAgent` is correctly invoked without being hijacked.

## High-level description
- Rename `_maybe_force_level_up_character_creation` to `_maybe_trigger_level_up_modal`
- Omit setting the `character_creation_in_progress` legacy tag. We only set `level_up_pending=True`.

## Testing
- E2E Test added: `test_level_up_agent_hijack_real_e2e.py`

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Changes state flags used for agent routing in the level-up flow; mistakes could misroute users between `LevelUpAgent` and `CharacterCreationAgent`. Scope is small and covered by updated unit coverage plus a new real E2E regression test.
> 
> **Overview**
> Fixes the level-up selection flow so clicking `level_up_now` **no longer sets legacy character-creation state** (which previously caused `CharacterCreationAgent` to hijack routing) and instead marks level-up via `level_up_in_progress`/`level_up_pending`.
> 
> `world_logic` now detects the modal choice directly from JSON user input (with a planning-block fallback) and adds guards to avoid overwriting explicit `False` state. Tests were updated to assert the character-creation keys are absent, and a new real E2E regression test validates routing stays in `level_up`.
> 
> Separately tightens packaging metadata in `pyproject.toml` and updates `run_tests.sh` MCP test discovery to ignore `.venv`/`venv` directories.
> 
> <sup>Reviewed by [Cursor Bugbot](https://cursor.com/bugbot) for commit 642d83bee
