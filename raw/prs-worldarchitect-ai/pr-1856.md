# PR #1856: fix: Remove unsafe-eval and tighten img-src CSP directives

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2025-10-13
**Author:** jleechan2015
**Stats:** +22/-10 in 1 files

## Summary
Security hardening follow-up to PR #1855 based on unanimous AI reviewer feedback.

### Changes
1. **Removed `unsafe-eval` from script-src** ❌→✅
   - Not required for Bootstrap or Firebase functionality
   - Significantly weakens XSS protection by allowing dynamic code execution
   - Flagged by all 4 AI reviewers as P1 security vulnerability

2. **Tightened img-src from wildcard `https:`** 🔓→🔒
   - **Before**: Allow images from ANY HTTPS domain
   - **After**: Only allow specific trusted domains:

## Raw Body
## Summary
Security hardening follow-up to PR #1855 based on unanimous AI reviewer feedback.

### Changes
1. **Removed `unsafe-eval` from script-src** ❌→✅
   - Not required for Bootstrap or Firebase functionality
   - Significantly weakens XSS protection by allowing dynamic code execution
   - Flagged by all 4 AI reviewers as P1 security vulnerability

2. **Tightened img-src from wildcard `https:`** 🔓→🔒
   - **Before**: Allow images from ANY HTTPS domain
   - **After**: Only allow specific trusted domains:
     - `self` (own domain)
     - `data:` (inline base64 images)
     - `https://cdn.jsdelivr.net` (Bootstrap/CDN)
     - `https://*.googleapis.com` (Google services)
     - `https://*.gstatic.com` (Google static content)

### Security Impact
- ✅ Hardens XSS protection (removes eval attack vector)
- ✅ Reduces CSRF/data exfiltration risk (limits image sources)
- ✅ Maintains all required functionality for app features
- ✅ No breaking changes - all current resources still load

### Reviewer Consensus
All AI reviewers flagged the same issues:
- @Copilot: "significantly weakens CSP protection"
- @cursor: "Bug: introduces security vulnerability"  
- @chatgpt-codex-connector: "P1: Drop unnecessary unsafe-eval"
- @coderabbitai: Concurred with other findings

Addresses: #1855 (review comments)

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Strengthens CSP in `mvp_site/main.py` by removing `unsafe-eval`, restricting `img-src` to specific domains, and adding stricter directives.
> 
> - **Security/CSP (`mvp_site/main.py`)**:
>   - Remove `unsafe-eval` from `script-src`.
>   - Restrict `img-src` from wildcard `https:` to specific domains: `self`, `data:`, `https://cdn.jsdelivr.net`, `https://*.googleapis.com`, `https://*.gstatic.com`, `https://images.unsplash.com`.
>   - Add stricter directives: `object-src 'none'`, `base-uri 'self'`, `form-action 'self'`, `frame-ancestors 'none'`.
>   - Whitelist `frame-src https://wo
