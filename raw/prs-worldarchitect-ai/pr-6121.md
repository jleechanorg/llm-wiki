# PR #6121: fix(streaming): persist server system_warnings to Firestore (BD-724 parity)

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-04-07
**Author:** jleechan2015
**Stats:** +102/-20 in 3 files

## Summary
- **Problem:** Non-streaming `process_action_unified` already writes combined server warnings to `structured_fields["system_warnings"]` before Firestore persist (BD-724). The **streaming** path (`stream_story_with_game_state` → `_enrich_streaming_structured_fields` → `add_story_entry`) did not, so `debug_info._server_system_warnings` (dice integrity, validation) never reached the persisted story entry’s top-level `system_warnings` — warnings could disappear on reload when using SSE streaming.
- 

## Test Plan
- `python -m pytest tests/ -x -q`
- `python -m pytest mvp_site/tests/test_streaming_orchestrator.py -q`

## Raw Body
## Summary

- **Problem:** Non-streaming `process_action_unified` already writes combined server warnings to `structured_fields["system_warnings"]` before Firestore persist (BD-724). The **streaming** path (`stream_story_with_game_state` → `_enrich_streaming_structured_fields` → `add_story_entry`) did not, so `debug_info._server_system_warnings` (dice integrity, validation) never reached the persisted story entry’s top-level `system_warnings` — warnings could disappear on reload when using SSE streaming.
- **Change:** After enriching streaming structured fields, copy `debug_info._server_system_warnings` into `structured_fields["system_warnings"]` (deduped, order preserved). Same security model as `world_logic`: only the server-controlled list is used.

## Beads

- `rev-752t` (this workstream; branch `feat/rev-bd7k`)
- Related: `rev-bd724` (parent BD-724 persistence story)

## Test plan

- `python -m pytest tests/ -x -q`
- `python -m pytest mvp_site/tests/test_streaming_orchestrator.py -q`

## Production code

- `/Users/jleechan/.worktrees/worldarchitect/wa-160/mvp_site/streaming_orchestrator.py` — `_merge_server_system_warnings_for_streaming_persistence`, called from `_enrich_streaming_structured_fields`
- `/Users/jleechan/.worktrees/worldarchitect/wa-160/mvp_site/tests/test_streaming_orchestrator.py` — `TestStreamingSystemWarningsPersistence`

Made with [Cursor](https://cursor.com)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Low risk: only augments streaming persistence to copy server-generated warnings into `system_warnings`, with straightforward unit coverage and no changes to core gameplay/state mutation logic.
> 
> **Overview**
> Ensures the **SSE streaming persistence path** saves server-generated warnings by copying `debug_info._server_system_warnings` into top-level `structured_fields.system_warnings` (deduped, order-preserving) before `add_story_entry`, matching non-streaming behavior.
> 
> Adds targeted tests around `_merge_server_system_warni
