# PR #6036: feat(statusline): add statusLine config and context caching to repo

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-03-21
**Author:** jleechan2015
**Stats:** +41/-29 in 3 files

## Summary
The Claude Code statusline was configured only in global `~/.claude/settings.json`, not in the repo. Contributors without that global config would not get the statusline. The global config uses `git-header.sh --status-only` to display branch, sync status, PR number, and context usage.

The repo's `git-header.sh` was also missing context percentage caching: when invoked from a terminal (no stdin), it showed `---%`. The global version caches ctx% to `/tmp/claude_ctx/<repo>/<branch>` via the Stop h

## Background
The Claude Code statusline was configured only in global `~/.claude/settings.json`, not in the repo. Contributors without that global config would not get the statusline. The global config uses `git-header.sh --status-only` to display branch, sync status, PR number, and context usage.

The repo's `git-header.sh` was also missing context percentage caching: when invoked from a terminal (no stdin), it showed `---%`. The global version caches ctx% to `/tmp/claude_ctx/<repo>/<branch>` via the Stop hook so subsequent invocations show the last known value.

## Raw Body
## Background
The Claude Code statusline was configured only in global `~/.claude/settings.json`, not in the repo. Contributors without that global config would not get the statusline. The global config uses `git-header.sh --status-only` to display branch, sync status, PR number, and context usage.

The repo's `git-header.sh` was also missing context percentage caching: when invoked from a terminal (no stdin), it showed `---%`. The global version caches ctx% to `/tmp/claude_ctx/<repo>/<branch>` via the Stop hook so subsequent invocations show the last known value.

## Goals
1. Make the statusline work out-of-the-box for all contributors via repo-level config
2. Sync the context caching improvement from `~/.claude/hooks/git-header.sh` to the repo version

## Tenets
- Minimal change: only add what is needed, no refactor of existing logic
- Safe for AO worktrees: `settings.json` contains only `statusLine` (presentation config, not permissions/modes)

## High-level description of changes
- `.claude/settings.json` (new): minimal settings file with statusLine command pointing to `git-header.sh --status-only`, falling back to global version
- `.claude/hooks/git-header.sh`: adds per-repo/per-branch ctx% cache at `/tmp/claude_ctx/<repo>/<branch>`
- `.gitignore`: removes `settings.json` from ignored list (presentation-only config is safe to commit); keeps `settings.local.json` ignored

## Testing
- Ran `bash .claude/hooks/git-header.sh --status-only` before and after
- Before: `ctx ---------- ---%` | After: `ctx ####------ 40%` (reads cached value)
- Verified `settings.json` committed cleanly and gitignore updated correctly

## Low-level details
- **.claude/settings.json**: new 8-line file with statusLine.command using robust git-repo-check pattern
- **.claude/hooks/git-header.sh lines 7-29**: adds ctx cache variables and read/write logic around existing JSON parsing
- **.gitignore**: removes `.claude/settings.json` entry, updates comment

🤖 Generated with [Claude Code](https
