# PR #4988: fix: Living world E2E test classifier routing and regression oracle

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-02-08
**Author:** jleechan2015
**Stats:** +162/-1 in 2 files

## Summary
- Fix turn 5 time-trigger test failure caused by semantic classifier routing to PlanningAgent
- Fix regression oracle false positives on turn numbering (user vs internal turns)

**Key themes:**
- Test input classifier sensitivity
- Regression oracle turn number alignment

## Raw Body
## Summary
- Fix turn 5 time-trigger test failure caused by semantic classifier routing to PlanningAgent
- Fix regression oracle false positives on turn numbering (user vs internal turns)

**Key themes:**
- Test input classifier sensitivity
- Regression oracle turn number alignment

## Production Code Changes

No production code changes. Test infrastructure only.

## Test Changes

### 1. `testing_mcp/test_living_world_real_e2e.py` (+2/-1 lines)
**Purpose:** Living world real E2E test scenarios

- **Changed turn 5 input text** - Old input ("ask a fruit vendor about overnight incidents") was classified as think mode (0.66-0.67 score) by semantic classifier → routed to PlanningAgent which excludes `PROMPT_TYPE_LIVING_WORLD` → Changed to dialog-oriented text ("stop at a fruit vendor's stall, 'Good morning!'") that routes to DialogAgent → Time-based trigger now fires correctly
- **Added `internal_turn_number` to turn_results** - Regression oracle was using user-facing turn numbers (1-10) for modulo checks but the actual system uses internal turns (user+1, since char creation = turn 1) → Added internal_turn_number field for correct LW turn determination

### 2. `testing_mcp/lib/regression_oracle.py` (+12/-4 lines)
**Purpose:** Regression validation oracle for multi-turn tests

- **Use `internal_turn_number` for LW checks** - Was checking `turn_number % 3 == 0` on user-facing turns (3, 6, 9) but actual LW turns are internal (3, 6, 9 = user turns 2, 5, 8) → Now reads `internal_turn_number` from turn results when available → Eliminates false positive `world_events_present` errors

## Test Results
- **2/2 scenarios pass (100%)**
- Evidence: `/tmp/worldarchitect.ai/dev1770538282/living_world_e2e/iteration_003`

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Test-only changes that adjust scenario text and add an assertion; low risk beyond possible test flakiness if routing metadata changes.
> 
> **Overview
