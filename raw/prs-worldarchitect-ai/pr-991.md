# PR #991: 🔧 Fix Claude bot server CLI syntax for GitHub Actions

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2025-07-26
**Author:** jleechan2015
**Stats:** +18/-17 in 1 files

## Summary
(none)

## Raw Body
## 🎯 Problem

The GitHub Claude bot system was failing with the error:
```
❌ Claude Code error: error: unknown option '--message'
```

This caused all `/claude` commands in PR comments to fail, making the recently merged bot system non-functional.

## 🔧 Solution

Updated `claude-bot-commands/server/claude-bot-server.py` to use the correct Claude CLI syntax:

**Before:**
```python
subprocess.run([claude_cmd, '--message', prompt], ...)
```

**After:**
```python  
subprocess.run([claude_cmd, '--print', prompt], ...)
```

## ✅ Changes

- ✅ **Fixed CLI command**: Replaced invalid `--message` flag with `--print` flag
- ✅ **Correct syntax**: Uses Claude CLI positional argument specification  
- ✅ **Non-interactive output**: `--print` ensures proper response capture
- ✅ **Maintains compatibility**: All other functionality unchanged

## 🧪 Testing

**Before fix:** GitHub Actions logs showed:
```
claude-command - completed failure
❌ Claude Code error: error: unknown option '--message'
```

**After fix:** Claude bot should respond normally to `/claude` commands in PR comments.

## 🚀 Impact

- **Resolves** GitHub Actions workflow failures
- **Enables** complex Claude commands via GitHub comments  
- **Completes** the ChatOps Claude bot system functionality
- **Zero breaking changes** to existing setup

## 🎭 Verification Steps

1. **Merge this PR**
2. **Comment `/claude Hello!` on any PR**  
3. **Verify bot responds** with Claude's actual reply
4. **Confirm no more** "unknown option" errors in Actions logs

## 📋 Files Changed

- `claude-bot-commands/server/claude-bot-server.py` - CLI syntax fix

This single-line change resolves the core issue preventing the Claude bot system from working properly.

🤖 Generated with [Claude Code](https://claude.ai/code)

Co-Authored-By: Claude <noreply@anthropic.com>
