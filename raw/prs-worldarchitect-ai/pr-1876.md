# PR #1876: Package .claude automation as Claude Code plugin

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2025-10-17
**Author:** jleechan2015
**Stats:** +203/-0 in 5 files
**Labels:** codex

## Summary
- add Claude Code plugin manifest and local marketplace metadata so the repo can be installed via /plugin
- expose a plugin-specific hooks configuration that resolves scripts through ${CLAUDE_PLUGIN_ROOT} while preserving project overrides
- document installation steps and include a README describing the packaged plugin components

## Raw Body
## Summary
- add Claude Code plugin manifest and local marketplace metadata so the repo can be installed via /plugin
- expose a plugin-specific hooks configuration that resolves scripts through ${CLAUDE_PLUGIN_ROOT} while preserving project overrides
- document installation steps and include a README describing the packaged plugin components

## Testing
- not run (documentation and configuration updates only)

------
https://chatgpt.com/codex/tasks/task_e_68ef3fead4b4832fab7c1307437bf2a0

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Packages `.claude` commands/agents/hooks as an installable Claude Code plugin with marketplace metadata and adds README docs for installation and usage.
> 
> - **Plugin packaging**:
>   - Add `/.claude-plugin/plugin.json` manifest exposing `../.claude/commands/`, `../.claude/agents/`, and `../.claude/hooks/hooks.plugin.json`.
>   - Add `/.claude-plugin/marketplace.json` to enable `/plugin marketplace add` and install via repo URL.
>   - Add `.claude/hooks/hooks.plugin.json` defining hooks for `PreToolUse`, `PostToolUse`, `UserPromptSubmit`, and `Stop`, resolving scripts via `${CLAUDE_PLUGIN_ROOT}` with project overrides.
> - **Documentation**:
>   - New `.claude-plugin/README.md` describing plugin contents and local development workflow.
>   - Update `README.md` with plugin distribution section and step-by-step local installation instructions.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit f383efbc6b858bb291de531d2c0a7adb6877fd3d. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->
## Summary by CodeRabbit

* **New Features**
  * Claude Code plugin for WorldArchitect.AI added — provides automation, governance, quality guardrails, declarative runtime hooks, and command/agent integration for workflow orchestration.

* **Documentation**
  * Adde
