# PR #4540: feat: Add living world dual-trigger system (turns + time)

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-02-03
**Author:** jleechan2015
**Stats:** +2072/-1124 in 16 files

## Summary
- Move living world tracking updates to post-response state handling to avoid advancing on failed LLM calls.
- Keep living world prompts out of combat mode and prevent stateless turn 0 triggers.
- Add focused tests for tracking updates and stateless/combat behavior.

## Raw Body
## Summary
- Move living world tracking updates to post-response state handling to avoid advancing on failed LLM calls.
- Keep living world prompts out of combat mode and prevent stateless turn 0 triggers.
- Add focused tests for tracking updates and stateless/combat behavior.

## Changes
- `mvp_site/agent_prompts.py`: stop mutating `last_living_world_*` during prompt build; guard stateless turn 0.
- `mvp_site/world_logic.py`: add `_maybe_update_living_world_tracking` and call it after state updates are applied.
- `mvp_site/agents.py`: remove living world prompt from `CombatAgent` prompt order.
- Tests updated/added in `mvp_site/tests/test_prompts.py`, `mvp_site/tests/test_agents.py`, `mvp_site/tests/test_world_time_progression.py`.

## Testing
- `python -m pytest mvp_site/tests/test_prompts.py -k "does_not_update_tracking_on_build or does_not_reset_tracking_on_build_without_time or stateless_turn_zero"`
- `python -m pytest mvp_site/tests/test_agents.py -k combat_prompt_order_excludes_living_world`
- `python -m pytest mvp_site/tests/test_world_time_progression.py -k maybe_update_living_world_tracking`
- `python -m pytest mvp_site/tests/test_prompts.py -k living_world`

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Touches prompt assembly and game-state mutation timing, so misfires could change when living-world events trigger or update tracking; guarded by mode checks and new unit tests but still affects core request/response flow.
> 
> **Overview**
> Implements a *dual-trigger living-world system* by making `PromptBuilder` conditionally inject `PROMPT_TYPE_LIVING_WORLD` based on `GameState.check_living_world_trigger(turn_number)` (with a stateless fallback interval) and ensuring turn 0 never triggers.
> 
> Moves living-world bookkeeping out of prompt construction: `world_logic._maybe_update_living_world_tracking` now updates `last_living_world_turn`/`last_living_world_time` **only after a successful response/state update**, and skips god/think/combat m
