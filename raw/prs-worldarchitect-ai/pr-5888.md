# PR #5888: fix(preview): restore Docker world assets and fail fast

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-03-09
**Author:** jleechan2015
**Stats:** +83/-14 in 5 files

## Summary
- extract only the preview server fix from #5879 onto a fresh branch from origin/main
- keep scope to preview runtime behavior only

## Raw Body
## Summary
- extract only the preview server fix from #5879 onto a fresh branch from origin/main
- keep scope to preview runtime behavior only

## Changes
- `.github/workflows/pr-preview.yml`: keep preview flow aligned with fail-fast behavior from extracted fix
- `mvp_site/Dockerfile`: restore Docker world-assets behavior from the preview fix commit

## Source
- extracted from commit `111491914` in https://github.com/jleechanorg/worldarchitect.ai/pull/5879/commits

## Testing
- not run (cherry-pick-only extraction)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Changes CI/CD preview build behavior and alters API response payloads for some execution paths, which may impact preview reliability and any clients/tests depending on prior response shape.
> 
> **Overview**
> Improves PR preview deployments by submitting Cloud Build with `--async` and actively polling `gcloud builds describe` for terminal statuses, failing fast on build errors/timeouts instead of relying on log streaming.
> 
> Fixes response consistency by always attaching `action_resolution`/`outcome_resolution` to `unified_response` (no longer gated behind `rewards_box`) and by backfilling `action_resolution` from legacy `outcome_resolution`; adds focused unit tests for this legacy behavior.
> 
> Removes the Docker image `COPY world/` step, changing how world assets are expected to be provided at runtime.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit b667778da112b5fbc851c0551bd869c4d9faf408. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->
## Summary by CodeRabbit

* **Chores**
  * PR preview deployment now uses asynchronous build submission with active polling and clearer progress/errors for more reliable previews.

* **Bug Fixes**
  * Responses now always include action and outcome resolu
