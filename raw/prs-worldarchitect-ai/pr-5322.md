# PR #5322: Follow-up: move Codex tracking tests into existing suite

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-02-12
**Author:** jleechan2015
**Stats:** +16/-20 in 2 files
**Labels:** codex

## Summary
(none)

## Raw Body
### Motivation

- Repository policy discourages adding new `test_*.py` files, so the Codex comment-tracking assertions needed to be moved into an existing test module to comply with test-file creation rules.

### Description

- Imported `build_default_comment` into `automation/jleechanorg_pr_automation/tests/test_automation_marker_functions.py` and added a `TestCodexDefaultCommentTracking` class containing two assertions that the default Codex comment mentions `html_url` and includes `FIXED vs CONSIDERED` and `[codex-automation-commit]` guidance.
- Removed the standalone test file `automation/jleechanorg_pr_automation/tests/test_codex_config_tracking.py` and consolidated its checks into the existing marker test suite.
- Committed the change (`b13a49f`) on branch `work` and created a follow-up PR summarizing the move and rationale.

### Testing

- Ran the modified test module with `python -m pytest -q -c /dev/null automation/jleechanorg_pr_automation/tests/test_automation_marker_functions.py` which completed successfully (`19 passed`).
- Verified the moved tests are present via `rg` and confirmed the old standalone test file was removed.

```
=== Git Status ===
On branch work
nothing to commit, working tree clean

[Dir: worldarchitect.ai | Local: work (no remote) | Remote: no upstream | PR: none]
```

------
[Codex Task](https://chatgpt.com/codex/tasks/task_e_698d22515134832fb668093ae91cc23e)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Test-only changes that reorganize existing assertions without altering production logic.
> 
> **Overview**
> Moves Codex default-comment tracking assertions into the existing `test_automation_marker_functions.py` suite by importing `build_default_comment` and adding a new `TestCodexDefaultCommentTracking` test case.
> 
> Removes the standalone `test_codex_config_tracking.py` file so tracking guidance checks (mentions of `html_url`, `FIXED vs CONSIDERED`, and `[codex-automation-commit]`) live alongside the automation marker
