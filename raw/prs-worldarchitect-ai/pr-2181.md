# PR #2181: Improve orchestration heartbeat tracking

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2025-11-29
**Author:** jleechan2015
**Stats:** +226/-64 in 5 files
**Labels:** codex

## Summary
- add structured heartbeat payload generation in AgentBase with configurable intervals
- persist health snapshots in the file-based broker registry via AgentRegistration
- add regression coverage for agent and broker heartbeat behavior

## Raw Body
## Summary
- add structured heartbeat payload generation in AgentBase with configurable intervals
- persist health snapshots in the file-based broker registry via AgentRegistration
- add regression coverage for agent and broker heartbeat behavior

## Testing
- python -m pytest orchestration/tests/test_message_broker.py orchestration/tests/test_agent_system_core.py

------
[Codex Task](https://chatgpt.com/codex/tasks/task_e_692a77fee2c8832fa30063b08da8374f)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Introduce structured, configurable heartbeats in `AgentBase` and persist health snapshots via `AgentRegistration` in the file-based `MessageBroker`, with accompanying tests.
> 
> - **Orchestration**:
>   - **AgentBase (`orchestration/agent_system.py`)**:
>     - Add structured heartbeat payload (`_collect_health_data`) and single-tick sender (`_heartbeat_tick`).
>     - Make heartbeat/error intervals configurable; refine loop with retry/backoff.
>     - Type hints tightened; A2A ping now returns response data; remove unused A2A message thread.
>   - **Broker (`orchestration/message_broker.py`)**:
>     - Use `AgentRegistration` for registry entries; `heartbeat(agent_id, health)` now updates registration, persists health, and returns `bool`.
>     - Stale cleanup uses `total_seconds`.
>   - **Core (`orchestration/core.py`)**:
>     - New `AgentRegistration` dataclass and `iso_now` helper.
> - **Tests**:
>   - Add coverage for heartbeat payload/tick (`orchestration/tests/test_agent_system_core.py`).
>   - Add broker heartbeat, persistence, and cleanup tests (`orchestration/tests/test_message_broker.py`).
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 1078c70c2611643ad0c80e971ce3fa2f49f30d53. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
