# PR #5282: fix: Level-up modal flow and test fixes

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-02-12
**Author:** jleechan2015
**Stats:** +3608/-458 in 34 files

## Summary
Fixes level-up modal reliability and test accuracy:
1. Server-side finish choice injection was being overwritten by raw LLM response
2. Race condition: modal injection used post-update state where LLM could clear level_up_in_progress in the same turn
3. Cross-modal flag interference: character-creation lock was forcing level_up_in_progress=True
4. Tests incorrectly checking for CharacterCreationAgent instead of LevelUpAgent

**Key themes:**
- Server-side injection preservation (planning_block ov

## Raw Body
## Summary
Fixes level-up modal reliability and test accuracy:
1. Server-side finish choice injection was being overwritten by raw LLM response
2. Race condition: modal injection used post-update state where LLM could clear level_up_in_progress in the same turn
3. Cross-modal flag interference: character-creation lock was forcing level_up_in_progress=True
4. Tests incorrectly checking for CharacterCreationAgent instead of LevelUpAgent

**Key themes:**
- Server-side injection preservation (planning_block overwrite fix)
- Modal lock scoping (character-creation vs level-up flag isolation)
- Stale flag guards for level-up reactivation
- Level-up mode freezes game time
- Code quality: REV- comment tag removal, exit choice centralization

## Production Code Changes

### 1. mvp_site/world_logic.py (+280/-194 lines)
**Purpose:** Core game state processing and response building

- **Use structured_fields planning_block instead of structured_response** - Line ~5414 copied raw LLM response planning_block -> Now uses structured_fields["planning_block"] with server injections -> Preserves server-side finish choice injection and reordering
- **Use pre-update state for modal injection** - _inject_modal_finish_choice_if_needed was called with updated_game_state_dict -> Now uses current_game_state_dict (pre-update) -> Prevents race condition where LLM sets level_up_in_progress=False in same turn, causing injection skip
- **Clear stale completion flags on new level-up** - Previous level_up_complete/level_up_cancelled flags could block future level-ups -> Now cleared when _check_and_set_level_up_pending detects a new level-up -> Allows multiple level-ups across a campaign
- **Scoped modal lock enforcement** - Character-creation lock was protecting ALL flags (including level-up) -> Now only protects flags for the currently active modal -> Fixes cross-modal interference where char-creation lock forced level_up_in_progress=True
- **Level-up mode freezes game time** - is_level_up_mode was
