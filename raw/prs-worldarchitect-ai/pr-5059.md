# PR #5059: Ignore versioned local virtualenv directories in gitignore

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-02-08
**Author:** jleechan2015
**Stats:** +1/-0 in 1 files

## Summary
- add `.venv*/` to `.gitignore`
- keep existing `.venv` and `venv` ignores unchanged
- prevent local versioned virtualenv folders like `.venv311/` from appearing as untracked

## Raw Body
## Summary
- add `.venv*/` to `.gitignore`
- keep existing `.venv` and `venv` ignores unchanged
- prevent local versioned virtualenv folders like `.venv311/` from appearing as untracked

## Testing
- `git status --short` no longer shows `.venv311/`

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Single `.gitignore` pattern addition; no runtime or production behavior changes.
> 
> **Overview**
> Updates `.gitignore` to also ignore *versioned* local virtualenv folders by adding the `.venv*/` pattern, preventing directories like `.venv311/` from showing up as untracked while keeping existing `venv`/`.venv` ignores unchanged.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit dc6a5e4e99a74316eb1c742c14f0ebdcf8df64de. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
