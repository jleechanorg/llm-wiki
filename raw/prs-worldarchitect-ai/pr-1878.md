# PR #1878: Handle Genesis CLI help path and update tests

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2025-10-17
**Author:** jleechan2015
**Stats:** +857/-79 in 7 files
**Labels:** codex

## Summary
- add a dedicated GenesisHelpRequested exception so -h/--help exits successfully while reusing shared usage output
- update the shared usage copy to avoid duplicate Codex notes and expose the standard help alias across runners
- extend CLI unit tests to cover help handling alongside the parser updates

## Raw Body
## Summary
- add a dedicated GenesisHelpRequested exception so -h/--help exits successfully while reusing shared usage output
- update the shared usage copy to avoid duplicate Codex notes and expose the standard help alias across runners
- extend CLI unit tests to cover help handling alongside the parser updates

## Testing
- pytest genesis/tests/test_common_cli_simple.py genesis/tests/test_genesis_simple_unit.py
- pytest genesis/tests/test_genesis_null_safety.py::TestGenesisModelPreference::test_is_codex_enabled_defaults_to_true genesis/tests/test_genesis_null_safety.py::TestGenesisModelPreference::test_is_codex_enabled_prefers_claude_flag


------
https://chatgpt.com/codex/tasks/task_e_68ef3e13b4dc832f96abe81e2c04c78a

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Adds a shared CLI parser with help/usage handling, migrates genesis to it, introduces a simplified runner, and expands tests for CLI, model selection, and IO/token safety.
> 
> - **CLI**:
>   - Introduces `genesis/common_cli.py` with `GenesisArguments`, `parse_genesis_cli`, `extract_model_preference`, `print_usage`, and custom exceptions; validates flags (`--refine`, `--iterate`, `--interactive`, `--codex`, `--claude`, `--pool-size`) and enforces positive integers.
>   - Updates `genesis/genesis.py` to use shared CLI (removes ad-hoc argv parsing), centralizes model selection, and applies pool size via `set_subagent_pool_size`.
> - **Runners**:
>   - Adds `genesis/genesis_simple.py`: headless loop runner reusing shared CLI, session persistence (`proto_genesis_session.json`), prompt building, completion detection, and model dispatch.
>   - Exposes package entry points via `genesis/__init__.py`.
> - **Refactors**:
>   - `is_codex_enabled` now uses `extract_model_preference` and rejects conflicting flags.
> - **Tests**:
>   - Adds `genesis/tests/test_common_cli_simple.py` covering parsing, help, validation, and model preference extraction.
>   - Expands `genesis/tests/test_genesis_null_safety.py` with model prefe
