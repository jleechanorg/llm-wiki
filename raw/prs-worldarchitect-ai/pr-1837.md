# PR #1837: Improve CLI detection and fallback for Codex support

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2025-10-07
**Author:** jleechan2015
**Stats:** +406/-52 in 4 files
**Labels:** codex

## Summary
- centralize CLI detection keywords and auto-select an installed CLI when tasks lack explicit preferences
- ensure agent creation falls back to an available CLI when the requested binary is missing and quote command templates safely
- extend Codex CLI tests to cover auto-selection and fallback execution scenarios

## Raw Body
## Summary
- centralize CLI detection keywords and auto-select an installed CLI when tasks lack explicit preferences
- ensure agent creation falls back to an available CLI when the requested binary is missing and quote command templates safely
- extend Codex CLI tests to cover auto-selection and fallback execution scenarios

## Testing
- pytest orchestration/tests/test_cli_support.py
- pytest orchestration/tests/test_collision_bug_fix.py orchestration/tests/test_unified_naming.py

------
https://chatgpt.com/codex/tasks/task_e_68e1ad6835d0832f86b12baa03f62f40

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Introduce multi-CLI agent execution (Claude or Codex) with automatic detection, fallback if preferred CLI is missing, safer command/quoting, and updated docs/tests.
> 
> - **Orchestration**:
>   - Update dependency checks to require `tmux`, `git`, `gh` and at least one LLM CLI (`claude` or `codex`).
>   - Improve messages and guidance when LLM CLI is missing.
> - **Task Dispatcher**:
>   - Add multi-CLI support via `CLI_PROFILES` for `claude` and `codex` with per-CLI command templates, attribution, and session handling.
>   - Detect CLI from `--agent-cli` flag or keywords; auto-select installed CLI; graceful fallback when requested CLI is unavailable.
>   - Safer execution: quote paths/args (`shlex`), sanitize agent tokens for filenames, structured stdin redirection, and robust logging/output handling.
>   - Store selected CLI in agent spec and propagate through prompts and run scripts.
> - **Docs**:
>   - Update `orchestration/README.md` to list LLM CLI options and generic creation instructions.
> - **Tests**:
>   - Add `orchestration/tests/test_cli_support.py` covering keyword detection, auto-selection, fallback, and Codex command/script generation.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 7cf688136cab7991f4bf5487a2420472a8f092d1. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard
