# PR #5554: Remove `prompt_archive` and switch prompt sources to `mvp_site/prompts/`

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-02-16
**Author:** jleechan2015
**Stats:** +13/-5152 in 21 files
**Labels:** codex

## Summary
(none)

## Raw Body
### Motivation
- Remove legacy archived prompt snapshots and simplify the canonical prompt source to a single active location for prompt delivery and RAG indexing.
- Keep documentation and constants in sync so runtime code and design docs reference the canonical `mvp_site/prompts/` location.
- Reduce maintenance surface and token-budget confusion caused by dual `prompt_archive/` vs `mvp_site/prompts/` references.

### Description
- Deleted the `prompt_archive/` directory and removed all archived prompt snapshot files (including `dec_2025/` and `older/`).
- Updated documentation and constants to stop pointing at `prompt_archive/` and instead refer to `mvp_site/prompts/` (files changed: `mvp_site/PROMPTS_README.md`, `mvp_site/constants.py`, `roadmap/rag_hybrid_prompt_design.md`, and `roadmap/rag_hybrid_impl_plan.md`).
- Adjusted roadmap and implementation-plan text to describe indexing and RAG retrieval over `mvp_site/prompts/` and to request a core header file under `mvp_site/prompts/`.
- Committed the removals on branch `work` and prepared PR metadata (`Remove prompt_archive folder and clean references`).

### Testing
- Verified the `prompt_archive` directory no longer exists using `find . -maxdepth 3 -type d -name 'prompt_archive'` and a Python `shutil.rmtree` check; result: `prompt_archive` removed.
- Searched the codebase for remaining `prompt_archive` references with `rg -n "prompt_archive" --glob '!docs/**/results_*/*' --glob '!docs/**/performance/**'` and confirmed active sources/docs were updated (remaining hits are historical performance artifacts under `docs/**/results_*` which are not live prompt paths).
- Inspected git state with `git status --short` and committed the deletions (`21 files changed, 5152 deletions`); branch `work` is clean locally but has no `origin` remote so `git push` was not performed in this environment.
- Created PR metadata via the repository helper (`make_pr`) which returned the PR title/body for review; CI/unit tests were not execu
