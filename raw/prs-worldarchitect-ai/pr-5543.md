# PR #5543: Add game state JSON export to campaign downloader

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-02-15
**Author:** jleechan2015
**Stats:** +53/-19 in 1 files

## Summary
- update `scripts/download_campaign.py` to export two files per campaign download:
  - story export in selected format (`txt`/`docx`/`pdf`)
  - game state export as `*_game_state.json`
- align filenames by introducing a shared filename prefix
- improve CLI output to report both saved files and sizes
- add a defensive fallback to basic story formatting when enhanced formatting crashes on malformed legacy `planning_block.choices`

## Raw Body
## Summary
- update `scripts/download_campaign.py` to export two files per campaign download:
  - story export in selected format (`txt`/`docx`/`pdf`)
  - game state export as `*_game_state.json`
- align filenames by introducing a shared filename prefix
- improve CLI output to report both saved files and sizes
- add a defensive fallback to basic story formatting when enhanced formatting crashes on malformed legacy `planning_block.choices`

## Why
The downloader previously exported only story text/doc output. This change adds game state JSON export for complete campaign snapshot downloads.

## Testing
- `python -m py_compile scripts/download_campaign.py`
- `python scripts/download_campaign.py --email "$EMAIL_USER" --campaign-id ARrfJ39LhNEi5rcGq1c7 --output-dir /tmp/campaign_download_test --format txt`
  - verified files created:
    - `Visenya v5_ARrfJ39L.txt`
    - `Visenya v5_ARrfJ39L_game_state.json`

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Script-only changes that add an additional JSON export and a guarded fallback path; main risk is altered export output/filenames and potential serialization edge cases for some Firestore types.
> 
> **Overview**
> Updates `scripts/download_campaign.py` to export **two artifacts per campaign**: the story in the requested format (`txt`/`docx`/`pdf`) plus a `*_game_state.json` snapshot, using a shared safe filename prefix.
> 
> Improves robustness by trying `get_story_text_from_context_enhanced(..., include_scenes=...)` and falling back to the legacy formatter on `AttributeError`/`TypeError` (while re-raising unexpected exceptions), and updates CLI output to report both saved files and their sizes.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 6c2116c7acee2d90bd2b1b005db7e21bbcf7f495. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release note
