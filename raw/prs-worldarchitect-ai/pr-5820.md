# PR #5820: chore: ignore .agentloop/ and .beads/.dolt/ local runtime directories

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-03-03
**Author:** jleechan2015
**Stats:** +4/-0 in 2 files

## Summary
- Add `.agentloop/` to root `.gitignore` — agent loop runtime state (config.yaml, history.json) is machine-local and should not be tracked
- Add `.dolt/` to `.beads/.gitignore` — existing `dolt/` pattern did not match the dotfile variant `.dolt/` that Dolt actually creates

## Raw Body
## Summary
- Add `.agentloop/` to root `.gitignore` — agent loop runtime state (config.yaml, history.json) is machine-local and should not be tracked
- Add `.dolt/` to `.beads/.gitignore` — existing `dolt/` pattern did not match the dotfile variant `.dolt/` that Dolt actually creates

## Production Code Changes
- **Before**: Both directories appeared as untracked files in `git status`
- **Now**: Both are properly gitignored
- **Why**: These are generated runtime directories with no value in version history; ignoring prevents accidental commits

## Test Changes
None — gitignore-only change, no tests affected.

## Known Limitations
None.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Gitignore-only changes; no production logic or data handling paths are modified.
> 
> **Overview**
> Prevents accidental commits of machine-local runtime state by adding `.agentloop/` to the root `.gitignore`.
> 
> Updates `.beads/.gitignore` to also ignore Dolt’s hidden `.dolt/` directory (in addition to `dolt/`).
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit d7b1418acfbc9dc0c259aba2d4aa5da3649cdba6. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->

## Summary by CodeRabbit

* **Chores**
  * Updated gitignore configuration files to exclude additional runtime and data directories from version control.

<!-- end of auto-generated comment: release notes by coderabbit.ai -->
