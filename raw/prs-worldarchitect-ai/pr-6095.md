# PR #6095: fix(testing): core test infrastructure fixes + skeptic gate/cron improvements

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-04-05
**Author:** jleechan2015
**Stats:** +1101/-546 in 8 files

## Summary
Replaces PR #6081 (which had unrelated git history causing permanent merge conflict). Identical code changes, clean ancestry from main.

## Background
Replaces PR #6081 (which had unrelated git history causing permanent merge conflict). Identical code changes, clean ancestry from main.

## Raw Body
## Background

Replaces PR #6081 (which had unrelated git history causing permanent merge conflict). Identical code changes, clean ancestry from main.

## Goals

1. Fix core testing infrastructure (browser_test_base.py gunicorn singleton, legacy subclass compatibility)
2. Fix skeptic-cron.yml Gate-4: use CI check-run conclusion instead of stale inline comment count
3. Fix skeptic-gate.yml Gate-4: same fix (aligns both workflows)
4. Fix `jq -rs` flag so BUGBOT_CONCLUSION shell comparison works correctly

## Tenets

- Gate-4 should be authoritative (CI check-run), not stale (inline comments persist forever)
- Browser test infrastructure must support legacy subclasses without breaking changes

## High-level description of changes

### `.github/workflows/skeptic-gate.yml` + `skeptic-cron.yml`
Gate-4 now uses `gh api .../commits/$SHA/check-runs | jq -rs '... | .conclusion'` instead of counting cursor[bot] inline comments. Inline comments are stale across commits — the old approach permanently blocked PRs that had any historical "error" Bugbot comment. Also fixed missing `-r` flag in `jq -s` (now `jq -rs`) so `BUGBOT_CONCLUSION` comparison to `"failure"` actually works.

### `testing_ui/lib/browser_test_base.py`
- Gunicorn singleton pattern (prevents port conflicts in parallel tests)
- String-port guard for legacy subclasses (passes test-name string as first arg)
- Restore `getattr(self, "test_user_id", None)` in video manifest (was hardcoded `None`)

### `testing_mcp/lib/evidence_utils.py`
- Fix ruff errors: remove `UTC = UTC` self-assign, use `capture_output=True`, fix PLR1714, F811 duplicate `save_request_responses`, RET504

### `testing_ui/core_tests/test_field_format_mismatch_red_green.py`
New red-green test for field format validation.

### `.beads/issues.jsonl`
Sync task tracking.

## Testing

CI: All checks passing on prior commits. Ruff, mypy, bandit all clean.

## Low-level details

- `.github/workflows/skeptic-gate.yml:101-112`: Gate-4 rewritten to use CI check
