# PR #1308: fix: Embed guidelines functionality directly into /plan and /execute commands

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2025-08-15
**Author:** jleechan2015
**Stats:** +115/-30 in 8 files

## Summary
Fixes reliability issue where `/guidelines` command doesn't consistently run during `/execute` workflow by embedding guidelines functionality directly into commands rather than relying on cross-command composition.

## Raw Body
## Summary
Fixes reliability issue where `/guidelines` command doesn't consistently run during `/execute` workflow by embedding guidelines functionality directly into commands rather than relying on cross-command composition.

## Root Cause Analysis
- `/copilot` works because it uses **direct orchestration** (all logic embedded)
- `/execute` failed because it used **command composition** (cross-command references that don't execute automatically)
- Claude Code commands work through prompt templates, not automatic cross-command execution

## Changes Made
- **Embedded guidelines logic** directly into `/plan` and `/execute` commands
- **Replaced documentation references** with actual implementation steps  
- **Added robust fallback strategies** for PR/branch context detection
- **Created branch-specific guidelines** when no PR context available

## Files Modified
- `.claude/commands/plan.md` - Embedded guidelines consultation implementation
- `.claude/commands/execute.md` - Updated to reference embedded implementation
- `docs/branch-guidelines_fix/guidelines.md` - Created (demonstrates working fallback)

## Testing Evidence
- ✅ Guidelines consultation executed successfully during `/execute` test
- ✅ Branch-specific guidelines file created automatically
- ✅ CLAUDE.md and base guidelines read successfully  
- ✅ Robust fallback when no PR context detected

## Impact
The `/guidelines` command will now run reliably during `/execute` because the logic is embedded directly rather than relying on cross-command composition that doesn't execute automatically.

🤖 Generated with [Claude Code](https://claude.ai/code)

Co-Authored-By: Claude <noreply@anthropic.com>
