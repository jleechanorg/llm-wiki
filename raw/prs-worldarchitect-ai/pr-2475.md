# PR #2475: Fix crontab automation to use CDP mode (v0.2.14)

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2025-12-18
**Author:** jleechan2015
**Stats:** +38/-2 in 2 files

## Summary
Fixes Cloudflare challenge appearing in crontab automation by ensuring CDP mode is used deterministically.

## Raw Body
## Summary

Fixes Cloudflare challenge appearing in crontab automation by ensuring CDP mode is used deterministically.

## Changes

1. **Add --use-existing-browser to --codex-update command** (automation/jleechanorg_pr_automation/jleechanorg_pr_monitor.py:1343)
   - The crontab automation was launching fresh browser instead of using CDP
   - This caused Cloudflare challenges to appear
   - Now deterministically uses CDP when called via `jleechanorg-pr-monitor --codex-update`

2. **Bump version to 0.2.14** (automation/pyproject.toml)
   - Published to PyPI with CDP fix

## Verification

- Tested from source: `python3 -m jleechanorg_pr_automation.openai_automation.codex_github_mentions --use-existing-browser --limit 50`
- Connected to Chrome via CDP successfully (no Cloudflare challenge)
- Processed 19 GitHub Mention tasks
- Clicked "Update branch" on 9 tasks
- Exit code 0 (successful completion)
- Published to PyPI as version 0.2.14
- Verified crontab command uses CDP mode

## Related

- Builds on PR #2372 which added CDP support
- Fixes the missing flag in the crontab automation path

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Enforces CDP mode for Codex automation with a Chrome CDP preflight check and bumps version to 0.2.16.
> 
> - **Automation/CLI**
>   - Add `check_chrome_cdp_accessible` to verify Chrome DevTools Protocol before `--codex-update` runs, with user guidance if unavailable.
>   - Update `--codex-update` to call `jleechanorg_pr_automation.openai_automation.codex_github_mentions` with `--use-existing-browser`.
>   - Improve handling: print outputs, fail fast on timeout/errors.
> - **Packaging**
>   - Bump version in `automation/pyproject.toml` to `0.2.16`.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit deba7b4b9bd04ce8dcf851faed6959423105b418. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</s
