# PR #5808: perf: defer startup imports, optimize first paint, and harden OpenClaw tunnel flow

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-03-02
**Author:** jleechan2015
**Stats:** +2031/-176 in 26 files

## Summary
This PR ships performance-focused startup/import/runtime improvements and related test hardening:

- Defer heavy Python imports in `mvp_site/main.py`, `mvp_site/streaming_orchestrator.py`, and `mvp_site/llm_providers/__init__.py` via lazy-loading (`LazyLoader` / `__getattr__`).
- Add compression and first-paint improvements in `mvp_site/frontend_v1/index.html` and `mvp_site/main.py` (`flask-compress`).
- Harden OpenClaw tunnel and settings path (`--doctor` preflight, stale cache/tunnel cleanup, 

## Raw Body
## Summary

This PR ships performance-focused startup/import/runtime improvements and related test hardening:

- Defer heavy Python imports in `mvp_site/main.py`, `mvp_site/streaming_orchestrator.py`, and `mvp_site/llm_providers/__init__.py` via lazy-loading (`LazyLoader` / `__getattr__`).
- Add compression and first-paint improvements in `mvp_site/frontend_v1/index.html` and `mvp_site/main.py` (`flask-compress`).
- Harden OpenClaw tunnel and settings path (`--doctor` preflight, stale cache/tunnel cleanup, strict fail behavior in E2E).
- Make visual-gate/Pillow optional in testing infrastructure and relax image-comparison skip semantics when not installed.
- Add startup import regression tests and cold-start import benchmark utility.
- Update runner image workflow to automatically update and commit lockfile ref.

## Evidence and Validation Notes

- Added scripts and tests are present, but latency outcome claims are not yet fully validated with post-deploy real-instance cold-start measurements in this PR.
- `B6-REV-mqs` intent and evidence work is still tied to the benchmark/validation steps documented in `roadmap/2026-03-01-cold-start-lazy-import-evidence-requirements.md`.

## Scope / Risk

- Low/Medium operational risk: startup import timing changes and deferred initialization.
- Medium operational risk: workflow now writes commits from CI and pushes to `main`.

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **High Risk**
> Touches production startup/import order and request handling (LazyLoader + provider lazy imports) and changes Cloud Run concurrency/compression behavior, which can cause runtime-only failures. CI workflow now commits and pushes from GitHub Actions, increasing operational risk if misconfigured or triggered unexpectedly.
> 
> **Overview**
> **Performance + cold-start:** Defers heavy modules from server startup using `importlib.util.LazyLoader` in `mvp_site/main.py` and `mvp_site/streaming_orchestrator.py`, and switches `mvp_site.llm_providers` to PEP-56
