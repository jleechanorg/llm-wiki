# PR #316: Fix TypeError in HealthStatus: Convert string HP values to integers

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2025-07-06
**Author:** jleechan2015
**Stats:** +372/-7 in 6 files

## Summary
This PR fixes a TypeError that occurs when Firestore returns HP values as strings instead of integers, causing the comparison `hp < 0` in HealthStatus to fail.

## Raw Body
## Summary
This PR fixes a TypeError that occurs when Firestore returns HP values as strings instead of integers, causing the comparison `hp < 0` in HealthStatus to fail.

## Error Fixed
```
TypeError: '<' not supported between instances of 'str' and 'int'
File "/app/mvp_site/schemas/entities_simple.py", line 75, in __init__
```

## Changes
- Added `int()` conversion for `hp_current` and `hp_max` values when creating PlayerCharacter entities
- Added `int()` conversion for `hp_current` and `hp_max` values when creating NPC entities
- Added comprehensive test coverage to ensure string HP values are properly converted

## Testing
- Created new test file `test_hp_type_conversion.py` with 4 test cases
- All entity tracking tests pass
- Tested with various HP formats (strings, negatives, old format)

## Benefits
- Prevents TypeError when Firestore stores HP values as strings
- Ensures consistent integer types for health calculations
- Maintains backward compatibility with existing data

🤖 Generated with [Claude Code](https://claude.ai/code)
