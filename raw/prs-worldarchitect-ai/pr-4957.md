# PR #4957: feat(automation): add --codex-update-api flag to pr-monitor

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-02-08
**Author:** jleechan2015
**Stats:** +1359/-24 in 9 files

## Summary
- Adds `--codex-update-api` flag to `jleechanorg-pr-monitor` CLI
- Lists and inspects Codex cloud tasks via `codex cloud` CLI API (no browser, no Cloudflare)
- Depends on PR #4956 (CodexCloudAPI wrapper)

**Key themes:**
- Cloudflare bypass via official API
- New CLI flag alongside existing `--codex-update`

## Raw Body
## Summary
- Adds `--codex-update-api` flag to `jleechanorg-pr-monitor` CLI
- Lists and inspects Codex cloud tasks via `codex cloud` CLI API (no browser, no Cloudflare)
- Depends on PR #4956 (CodexCloudAPI wrapper)

**Key themes:**
- Cloudflare bypass via official API
- New CLI flag alongside existing `--codex-update`

## Production Code Changes

### 1. `automation/jleechanorg_pr_automation/jleechanorg_pr_monitor.py` (+42/-1 lines)
**Purpose:** Main CLI entry point for PR automation workflows

- **Added `--codex-update-api` argument** - Had only `--codex-update` (browser) → Added `--codex-update-api` (CLI API) → Provides Cloudflare-free alternative
- **Added handler block** - No API-based handler existed → Added handler that calls `CodexCloudAPI.list_all_tasks()` with filtering → Lists ready GitHub Mention tasks with summary stats
- **Updated `--codex-task-limit` help text** - Referenced only `--codex-update` → Now references both flags → Shared limit parameter

## Test Changes
- Unit tests from PR #4956 cover the CodexCloudAPI wrapper
- Integration tested with: `codex cloud list --json --limit 5` (real API call, 5 tasks returned)

## Usage

```bash
# List ready GitHub Mention tasks via CLI API
jleechanorg-pr-monitor --codex-update-api --codex-task-limit 10

# Dry run
jleechanorg-pr-monitor --codex-update-api --no-act
```

## Depends On
- PR #4956 — `feat(automation): add CodexCloudAPI wrapper for codex cloud CLI`

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Adds a new workflow that can apply diffs, create commits, push branches, and create/update GitHub PRs via subprocess calls (`git`/`gh`/`codex`), so misconfiguration or edge cases could affect repos/branches despite being gated behind new flags.
> 
> **Overview**
> Adds a new `--codex-api` mode to `jleechanorg_pr_monitor.py` to list/filter Codex Cloud tasks via the `codex cloud` CLI (with `--codex-task-limit` shared with `--codex-updat
