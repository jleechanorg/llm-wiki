# PR #4528: feat(logging): add response body logging for OpenAI-compatible provider errors

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-02-02
**Author:** jleechan2015
**Stats:** +157/-2 in 2 files

## Summary
- Add response body logging when `choices[0]` is missing from OpenAI-compatible API responses
- Helps debug OpenRouter/Cerebras issues where API returns 200 OK with error body
- Truncates response to 500 chars to avoid log spam

## Raw Body
## Summary
- Add response body logging when `choices[0]` is missing from OpenAI-compatible API responses
- Helps debug OpenRouter/Cerebras issues where API returns 200 OK with error body
- Truncates response to 500 chars to avoid log spam

## Production Code Changes

### 1. `mvp_site/llm_providers/openai_compatible_provider_core.py` (+15/-2 lines)
**Purpose:** Log full API response body when parsing fails

- Wrap `extract_first_choice()` and `extract_first_choice_message()` in try/except
- On ValueError, log the full response body (truncated to 500 chars) before re-raising
- Uses existing `logger` and `error_log_prefix` parameters for consistent logging

## Test Changes
- Added `test_logs_response_body_when_choices_missing` TDD test
- Verifies response body is logged when API returns error body with 200 OK status
- 5/5 tests passing in `test_openai_compatible_provider_core.py`

## Context
This fix addresses the debugging issue from campaign `Rp7hvzhOnS3TlvxpwCy4` where OpenRouter with `x-ai/grok-4.1-fast` returned 200 OK but with an error body instead of `choices[0]`. Previously, we only saw "Invalid response JSON: missing choices[0]" without knowing what the API actually returned.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Adds error logging around response parsing and new tests; low functional risk, with minor risk of sensitive data exposure via logged response bodies (bounded to 500 chars).
> 
> **Overview**
> Adds defensive logging to `generate_openai_compatible_content` so that when parsing fails (notably missing `choices[0]`), it logs a truncated (500 chars) representation of the raw response body—falling back to `str()` if JSON serialization fails and ensuring logger failures never mask the original `ValueError`.
> 
> Extends `test_openai_compatible_provider_core.py` with coverage for the new behavior: logging on missing `choices`, logging fallback when `json.dumps` fails, and prese
