# PR #5791: Add pluggable AI agent support (claude, codex, amp)

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-02-28
**Author:** jleechan2015
**Stats:** +91/-23 in 2 files

## Summary
Refactor Ralph to support multiple AI agent backends (Claude Code, Codex, and Amp) via a `--tool` flag and `RALPH_TOOL` environment variable, replacing hardcoded Claude-only implementation.

## Raw Body
## Summary
Refactor Ralph to support multiple AI agent backends (Claude Code, Codex, and Amp) via a `--tool` flag and `RALPH_TOOL` environment variable, replacing hardcoded Claude-only implementation.

## Changes
- Added `--tool` command-line argument to `run` command supporting `claude`, `codex`, and `amp`
- Added `RALPH_TOOL` environment variable (defaults to `claude`) for persistent tool selection
- Extracted agent command construction into a case statement mapping tool names to their CLI invocations
- Updated agent execution to use dynamic `$AGENT_CMD` variable instead of hardcoded Claude command
- Updated status monitoring to detect and display all three agent types with dynamic agent name detection
- Updated help text and README with tool options and usage examples
- Maintained backwards compatibility: numeric arguments and `--tool` flag work without explicit `run` command

## Benefits
- Users can now choose their preferred AI agent backend without code modifications
- Enables testing and comparison across different agent implementations
- Cleaner separation of concerns with agent command abstraction
- Environment variable support allows default tool configuration per environment
- Improved status monitoring shows which agent is currently running

## Testing
- Backwards compatible: existing `./ralph.sh 20` and `./ralph.sh --tool claude 20` invocations work unchanged
- Help text validates all three tool options are documented
- Status monitoring regex updated to detect all three agent process patterns
- Error handling for invalid tool selection and missing `--tool` argument

## Checklist
- [x] Code follows project conventions
- [x] Changes are backwards compatible
- [x] Documentation updated (README and help text)
- [x] No hardcoded credentials or values
- [x] Error handling for invalid tool selection

---
🤖 Generated with Claude Code

https://claude.ai/code/session_015QXX3R46puvDYovJiZeccx

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Moderate ri
