# PR #6000: fix(hooks): address CodeRabbit review for git-header.sh

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-03-19
**Author:** jleechan2015
**Stats:** +335/-335 in 12 files

## Summary
- Add explicit Remote field to compact header showing which remote sync state is relative to
- Use shared parse_repo_from_url function for tracking_repo parsing instead of duplicating logic

## Raw Body
## Summary
- Add explicit Remote field to compact header showing which remote sync state is relative to
- Use shared parse_repo_from_url function for tracking_repo parsing instead of duplicating logic

## Related
- Fixes CodeRabbit review comments on PR #5999

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Medium risk because it changes the `git-header.sh` parsing/formatting and PR lookup logic (remote selection, upstream display), which can affect developer workflows and hook output across diverse git/remote setups.
> 
> **Overview**
> Updates Claude command docs to replace the Memory MCP workflow with **native memory** calls (`memory_search`, `memory_save`, plus `memory_read`/`memory_forget` in `/memory`) and adjusts related execution/learning/guidelines/goal guidance accordingly.
> 
> Improves `git-header.sh` by extracting a shared `parse_repo_from_url`, adding support for `ssh://` remotes, falling back to the tracking remote’s repo for `gh pr` lookups, and making the header line explicitly show **branch + upstream tracking** and the **remote name** while truncating the directory to keep key fields visible.
> 
> Updates hook tests to assert the new `Branch:` label/value and keeps the test runner changes to minor whitespace/format cleanup.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit e414786894ee32eb43e49b6ca9be4bd253e13971. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->
## Summary by CodeRabbit

* **Documentation**
  * Replaced "Memory MCP" with "Native Memory" across docs; simplified workflows to use native memory operations and streamlined learning/search flows.

* **Improvements**
  * Enhanced git header display with clearer branch/upstream/remote reporting, tracking-repo fallback, and improved status line formatting.

* **Tests**
  * Updated
