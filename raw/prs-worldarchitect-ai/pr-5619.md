# PR #5619: docs: align Gemini /copilot prompt with direct posting policy

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-02-18
**Author:** jleechan2015
**Stats:** +6/-4 in 1 files
**Labels:** codex

## Summary
(none)

## Raw Body
### Motivation

- Resolve contradictions between the Gemini `/copilot` manifest and the canonical `.claude/commands/copilot.md` so agents follow the direct LLM-posting workflow (avoid deprecated `/commentreply` usage and ambiguous "no external scripts" wording).

### Description

- File: `.gemini/commands/copilot.toml` — GOAL: make Gemini command docs mirror the canonical direct-execution `/copilot` flow; MODIFICATION: removed the blanket "No external scripts" phrase, added an explicit `Script policy` allowing only `/pair` via `python3 .claude/scripts/pair_execute.py`, replaced `/commentreply` references with wording that the LLM builds and posts the consolidated comment directly, and updated integration lines to recommend `gh pr comment --body-file`; NECESSITY: prevents agents from invoking deprecated orchestration paths or unsafe shell patterns and keeps documentation consistent across agent runtimes; INTEGRATION PROOF: changes align the Gemini manifest with `.claude/commands/copilot.md` and the ACTION_ACCOUNTABILITY `responses.json` flow. 

### Testing

- Validated TOML syntax by loading `.gemini/commands/copilot.toml` and `.gemini/commands/README.toml` with `tomllib` which succeeded. 
- Verified textual fixes with ripgrep checks for deprecated markers (`/commentreply`, "No external scripts") and confirmed the manifest now references direct posting (`gh pr comment --body-file`) and the explicit `/pair` exception. 
- Ran `python3 .claude/commands/commentreply.py --help` to confirm the tool exists (prints deprecation note pointing to the new LLM flow); note that full end-to-end posting and `git push` could not be exercised in this environment because the local branch has no upstream remote, so remote push and PR-update validation were not executed.

------
[Codex Task](https://chatgpt.com/codex/tasks/task_e_699571af8ed8832f975c8f519717ae1e)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Documentation-only changes that adjust workflow wording and recommend
