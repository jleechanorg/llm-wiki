# PR #5860: fix: classifier startup degrades gracefully on HuggingFace 429

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-03-05
**Author:** jleechan2015
**Stats:** +108/-140 in 4 files

## Summary
- Extract `check_classifier_startup()` from `create_app()` into `intent_classifier.py`
- Change `raise RuntimeError` to `logging_util.warning` when classifier model download fails
- Prevents Cloud Run crash loop when HuggingFace returns 429 (rate limiting)
- Semantic routing degrades to MODE_CHARACTER fallback automatically

## Raw Body
## Summary
- Extract `check_classifier_startup()` from `create_app()` into `intent_classifier.py`
- Change `raise RuntimeError` to `logging_util.warning` when classifier model download fails
- Prevents Cloud Run crash loop when HuggingFace returns 429 (rate limiting)
- Semantic routing degrades to MODE_CHARACTER fallback automatically

## Production Code Changes
- **`mvp_site/main.py`**: 35 lines of inline classifier startup logic → 4-line call to `check_classifier_startup()`
- **`mvp_site/intent_classifier.py`**: New `check_classifier_startup()` function that waits for initialization and handles failures gracefully (warning instead of crash)

## Test Changes
- **`mvp_site/tests/test_intent_classifier.py`**: New `test_classifier_startup_check_degrades_on_load_failure` — simulates exhausted retries with `_load_error` set, verifies no RuntimeError is raised

## Known Limitations
- None — this is a strict behavior change from crash to graceful degradation

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Changes production startup/model-loading behavior to rely on prebaked caches and disables previous fastembed monkey patches; if cache artifacts are missing or fastembed API differs, semantic routing may fall back unexpectedly.
> 
> **Overview**
> **Prevents HuggingFace network calls on cold start** by setting `HF_HUB_OFFLINE=1` in the Docker image and updating the intent classifier to prefer local-cache-only model initialization (using `local_files_only` when supported).
> 
> Removes the previous Gunicorn `on_starting` monkey patches that faked `huggingface_hub` responses for `fastembed`, replacing them with lightweight cache-artifact verification/logging and relying on the offline guardrail plus existing fallback behavior.
> 
> Adds a unit test to assert offline startup passes `local_files_only=True` to `TextEmbedding` when `HF_HUB_OFFLINE` is enabled.
> 
> <sup>Written by [Cursor Bugbot](https:
