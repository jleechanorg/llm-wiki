# PR #4451: fix(gemini): reduce dice tool exposure in prompts

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-02-01
**Author:** jleechan2015
**Stats:** +425/-15 in 7 files

## Summary
- Capture raw LLM request/response payloads (any provider) into evidence bundles
- Wire capture path into testing base class so all testing_mcp runs include it

## Test Plan
- `./vpython testing_mcp/test_smoke.py`

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Touches LLM request construction/logging and test evidence plumbing; main risk is inadvertent logging/capture volume or behavior differences in Gemini tool availability for code-execution models.
> 
> **Overview**
> **Reduces dice tool exposure for Gemini `code_execution` models** by filtering dice functions out of `function_declarations` and tightening the `CODE_EXECUTION_DICE_OVERRIDE` prompt wo

## Raw Body
## Summary
- Capture raw LLM request/response payloads (any provider) into evidence bundles
- Wire capture path into testing base class so all testing_mcp runs include it

## Changes
- Add provider-agnostic LLM capture in `mvp_site/llm_service.py`
- Update testing_mcp evidence bundling to include `llm_request_responses.jsonl`
- Remove gemini-specific capture plumbing in favor of generic capture

## Benefits
- Evidence bundles now include raw LLM payloads for any provider
- Better auditability for payload-level issues across providers

## Test Plan
- `./vpython testing_mcp/test_smoke.py`

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Touches LLM request construction/logging and test evidence plumbing; main risk is inadvertent logging/capture volume or behavior differences in Gemini tool availability for code-execution models.
> 
> **Overview**
> **Reduces dice tool exposure for Gemini `code_execution` models** by filtering dice functions out of `function_declarations` and tightening the `CODE_EXECUTION_DICE_OVERRIDE` prompt wording to avoid teaching dice tool names.
> 
> **Adds provider-agnostic LLM request/response capture for tests**: `llm_service` can append JSONL request/response entries (including the *post-stripped* system instruction for Gemini 3) when `LLM_REQUEST_RESPONSE_CAPTURE_PATH` is set, and `testing_mcp` wires this env var into local server startup and bundles `llm_request_responses.jsonl` into evidence output (plus server-warning extraction in `evidence.md`).
> 
> Expands test coverage to assert dice tools are not exposed in code-execution mode and enhances smoke test evidence to flag fallback dice tool usage and RNG verification status.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 43799992fbd23f637712d436bc91c476c55dda83. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comme
