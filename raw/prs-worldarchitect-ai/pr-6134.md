# PR #6134: refactor(testing_ui): remove dead path helpers from config

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-04-07
**Author:** jleechan2015
**Stats:** +42/-75 in 3 files

## Summary
- Remove unused `_resolve_branch_scoped_dir` and redundant `testing_utils.evidence` imports from `testing_ui/config.py`.
- `SCREENSHOT_DIR` / `VIDEO_DIR` are set directly from `testing_utils.browser.resolve_screenshot_dir` / `resolve_video_dir` (`worldarchitectai`).
- Add `__all__` to document the intended public API for browser tests.
- Tighten `test_testing_utils_centralization` to require imports from `testing_utils.browser`; fix Ruff PLR1714 on module checks in the same file.

## Raw Body
## Summary
- Remove unused `_resolve_branch_scoped_dir` and redundant `testing_utils.evidence` imports from `testing_ui/config.py`.
- `SCREENSHOT_DIR` / `VIDEO_DIR` are set directly from `testing_utils.browser.resolve_screenshot_dir` / `resolve_video_dir` (`worldarchitectai`).
- Add `__all__` to document the intended public API for browser tests.
- Tighten `test_testing_utils_centralization` to require imports from `testing_utils.browser`; fix Ruff PLR1714 on module checks in the same file.

## Tests
- `python -m pytest tests/ -x -q`
- `python3 -m pytest mvp_site/tests/test_testing_utils_centralization.py -x -q`

Closes #6128.

Made with [Cursor](https://cursor.com)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Low risk refactor limited to test configuration and assertions; main risk is changing how screenshot/video directories resolve if callers relied on the removed local path-munging behavior.
> 
> **Overview**
> **Refactors `testing_ui/config.py` to drop local branch/evidence path helper code** and instead set `SCREENSHOT_DIR`/`VIDEO_DIR` directly via `testing_utils.browser.resolve_screenshot_dir`/`resolve_video_dir`, while exporting an explicit public surface via `__all__`.
> 
> **Tightens centralization tests** in `test_testing_utils_centralization.py` to require imports from `testing_utils.browser` and cleans up a couple assertions (module checks / identity asserts). Also updates Ruff’s isort config (`combine-as-imports`) to match the repository’s `isort` settings.
> 
> <sup>Reviewed by [Cursor Bugbot](https://cursor.com/bugbot) for commit cb3d0a850ca05ba39728e3a23092f0a1f8614f01. Bugbot is set up for automated code reviews on this repo. Configure [here](https://www.cursor.com/dashboard/bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
