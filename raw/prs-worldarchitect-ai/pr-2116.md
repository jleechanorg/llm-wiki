# PR #2116: Fix parallel request handling with ThreadPoolExecutor

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2025-11-27
**Author:** jleechan2015
**Stats:** +2639/-132 in 28 files

## Summary
- Fixed blocking I/O serialization in asyncio routes by adding ThreadPoolExecutor
- Added `run_blocking_io()` helper to offload Firestore calls to thread pool
- Increased Cloud Run containerConcurrency from 10 to 80
- Executor threads set to 80 for maximum parallelism

## Test Plan
- [ ] Run `python3 scripts/test_parallel_concurrency.py --concurrent 20`
- [ ] Verify parallel execution in GCP logs

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Enable true parallel request handling via thread-pooled blocking I/O and switch default Gemini model to 3-pro-preview with 2.5 removed/redirected.
> 
> - **Backend/API (Flask)**:
>   - Add shared event loop + `ThreadPoolExecutor` and `run_blocking_io()` to offload Firestore ca

## Raw Body
## Summary
- Fixed blocking I/O serialization in asyncio routes by adding ThreadPoolExecutor
- Added `run_blocking_io()` helper to offload Firestore calls to thread pool
- Increased Cloud Run containerConcurrency from 10 to 80
- Executor threads set to 80 for maximum parallelism

## Test Results
- 20 concurrent: 100% success, 1.01x parallelism ratio (10.7x speedup)
- 60 concurrent: 100% success, 1.01x parallelism ratio
- 100 concurrent: 71% success (429 rate limiting), 1.00x parallelism ratio

## Test Plan
- [ ] Run `python3 scripts/test_parallel_concurrency.py --concurrent 20`
- [ ] Verify parallel execution in GCP logs

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Enable true parallel request handling via thread-pooled blocking I/O and switch default Gemini model to 3-pro-preview with 2.5 removed/redirected.
> 
> - **Backend/API (Flask)**:
>   - Add shared event loop + `ThreadPoolExecutor` and `run_blocking_io()` to offload Firestore calls; parallelize `/api/campaigns*` and settings routes.
>   - Make rate limits configurable via `CAMPAIGN_*`/`SETTINGS_RATE_LIMIT`; add CSP/security headers; improve auth bypass handling for tests.
> - **Models & UI**:
>   - Remove Gemini 2.5 models; default to `gemini-3-pro-preview` with legacy auto-redirects; allow `gemini-2.0-flash`.
>   - Update frontend v1/v2 settings UIs and templates to new model options; adjust tests and defaults; update `GeminiResponse` and selection logic.
> - **Testing & Tooling**:
>   - Add rigorous parallel test runner `scripts/test_parallel.py` and extensive concurrency/timing integration tests; include evidence JSON and summary docs.
> - **Auth Utilities**:
>   - Add configurable `.claude/scripts/auth-cli.mjs` with token refresh; add wrappers for WorldArchitect and AI Universe.
> - **Docs**:
>   - Overhaul README; add conflict resolution and performance evidence reports.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bu
