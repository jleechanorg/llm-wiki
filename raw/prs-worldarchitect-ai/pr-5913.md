# PR #5913: Finalize beads_rust cutover for .beads artifacts

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-03-09
**Author:** jleechan2015
**Stats:** +676/-927 in 3 files

## Summary
This PR finalizes migration from legacy bd/dolt-style beads usage to `beads_rust` (`br`) SQLite workflow for this repo's tracked artifacts.

## Raw Body
## Summary
This PR finalizes migration from legacy bd/dolt-style beads usage to `beads_rust` (`br`) SQLite workflow for this repo's tracked artifacts.

## What Changed
- Consolidated `.beads` migration artifacts into one PR.
- Cut over tracked metadata to SQLite-style usage:
  - `.beads/metadata.json` now points to `beads.db` + `issues.jsonl`.
  - `.beads/config.yaml` prefix comment updated to `rev`.
- Normalized `.beads/issues.jsonl` for `br` import compatibility.
- Added `dolt/` ignore rule in `.beads/.gitignore` to avoid tracking local Dolt runtime directory.

## Validation
Local validation executed on this branch:
- `br sync --import-only --db .beads/beads.db`
- `br sync --flush-only --db .beads/beads.db`
- `br stats --db .beads/beads.db --json`
- `br list --db .beads/beads.db --json --limit 3`
- `br ready --db .beads/beads.db --json --limit 3`

Observed stats snapshot:
- `total_issues`: 665
- `open_issues`: 345
- `closed_issues`: 310
- `ready_issues`: 345

## Backups (before/alongside cutover)
- Primary bundle: `/tmp/dolt_beads_backup_20260309_001859`
- Original `issues.jsonl` backups:
  - `/tmp/issues_jsonl_original_backup_20260309_003340`
  - `~/issues_jsonl_original_backup_20260309_003340`
- ID mapping artifact (old -> new IDs used for `br` compatibility):
  - `/tmp/id_map_bd_to_br_20260309_003140.csv`

## Notes
- Previous per-worktree PRs were closed as superseded by this consolidated PR.

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Config-only change affecting local Beads tooling defaults; no application runtime or security-sensitive logic is modified.
> 
> **Overview**
> Updates `.beads/config.yaml` to complete the cutover from the legacy Dolt-backed Beads setup to a local `beads_rust` SQLite workflow.
> 
> This simplifies the config, lowercases the `issue-prefix` (`REV` -> `rev`), and replaces the previous absolute Dolt `db` path with a portable relative SQLite DB path (`.beads/beads.db`) while retaining existing safety/collaboration flags (`
