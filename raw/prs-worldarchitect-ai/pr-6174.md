# PR #6174: [agento] fix(deploy): secure default for unknown environments and add preview case

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-04-10
**Author:** jleechan2015
**Stats:** +48/-21 in 5 files

## Summary
- Add `else` clause to set `PRODUCTION_MODE=true` for any `ENVIRONMENT` value not explicitly matching dev/preview/staging/stable, preventing silent auth bypass for unknown environments
- Explicitly add `preview` to the CI smoke test bypass case alongside `dev`

## Test Plan
- [ ] Verify deploy.sh passes syntax check
- [ ] CI smoke tests continue to work for dev/preview environments

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Touches deployment env-var wiring and MCP authentication bypass logic; misconfiguration could accidentally relax or over-tighten auth in dev/preview environments. Changes are localized but security-sensitive.
> 
> **Overview**
> **Deployment now recognizes `preview`

## Raw Body
## Summary
- Add `else` clause to set `PRODUCTION_MODE=true` for any `ENVIRONMENT` value not explicitly matching dev/preview/staging/stable, preventing silent auth bypass for unknown environments
- Explicitly add `preview` to the CI smoke test bypass case alongside `dev`

## Bugbot Issues Addressed
- **Medium**: Catch-all else replaced with elif loses secure default (9c297d8d-efa6-496d-a079-d5f57c40605e)
- **P1**: Dev deployments lose MCP authentication on public endpoints (6ad23a8c-2f7a-4b16-9ff9-c7d69499dd14)

## Changes
- `deploy.sh`: Added else clause with PRODUCTION_MODE=true for unknown environments; added preview to the dev/preview CI bypass case

## Test Plan
- [ ] Verify deploy.sh passes syntax check
- [ ] CI smoke tests continue to work for dev/preview environments

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Touches deployment env-var wiring and MCP authentication bypass logic; misconfiguration could accidentally relax or over-tighten auth in dev/preview environments. Changes are localized but security-sensitive.
> 
> **Overview**
> **Deployment now recognizes `preview` explicitly** and adjusts Cloud Run env var injection: `dev` enables `TESTING_AUTH_BYPASS`, `preview` sets `ENVIRONMENT=preview` (SMOKE_TOKEN auth), and **any unknown `ENVIRONMENT` now forces `PRODUCTION_MODE=true`** as a secure default.
> 
> **MCP auth bypass rules are tightened**: unauthenticated `/mcp` access in non-production is disabled when `TESTING_AUTH_BYPASS` is enabled (requiring `X-Test-Bypass-Auth` instead), preview can optionally accept `X-Test-Bypass-Auth` when `TESTING_AUTH_BYPASS_MODE` is set, and internal tracebacks are suppressed when running with `TESTING_AUTH_BYPASS`. Tests and the MCP tool contract version are updated accordingly.
> 
> <sup>Reviewed by [Cursor Bugbot](https://cursor.com/bugbot) for commit 98edb9604f9b84eca2277037e3e9cfc186094219. Bugbot is set up for automated code reviews on
