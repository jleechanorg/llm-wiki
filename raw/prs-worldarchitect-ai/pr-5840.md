# PR #5840: chore(ai_orch): default mission-control run model to sonnet

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-03-03
**Author:** jleechan2015
**Stats:** +4/-0 in 1 files

## Summary
- sets `ai_orch run` default model to `sonnet` in `orchestration/live_mode.py`
- keeps explicit `--model` override behavior unchanged

## Raw Body
## Summary
- sets `ai_orch run` default model to `sonnet` in `orchestration/live_mode.py`
- keeps explicit `--model` override behavior unchanged

## Validation
- `python3 -m pytest orchestration/tests/test_orchestrate_unified_args.py -q`
  - 30 passed, 12 skipped

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Small, localized default-parameter change affecting only `claude` command construction; main risk is unexpected model selection for users relying on previous implicit defaults.
> 
> **Overview**
> Updates `orchestration/live_mode.py` so `claude` commands launched via `ai_orch run` (both passthrough and async/tmux via `AsyncRunner._build_shell_cmd`) automatically add `--model sonnet` when no model is specified, while preserving explicit `--model` overrides.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 466f4184122a55d67963a77288e640415d1d6175. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->
## Summary by CodeRabbit

* **Chores**
  * CLI now defaults to the "sonnet" model for Claude when no model is specified, including live-mode and passthrough runs. This ensures a consistent model is always used by default.
<!-- end of auto-generated comment: release notes by coderabbit.ai -->
