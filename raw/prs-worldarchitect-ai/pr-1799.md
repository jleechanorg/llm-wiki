# PR #1799: Update /newbranch to base on origin/main and carry requested changes

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2025-09-30
**Author:** jleechan2015
**Stats:** +329/-87 in 2 files
**Labels:** codex

## Summary
- update the /newbranch command so it always checks out origin/main before creating the new branch
- carry forward uncommitted edits automatically and cherry-pick requested commits when the input asks to "bring in" changes
- refresh the /newbranch documentation with the new workflow and natural language examples

## Raw Body
## Summary
- update the /newbranch command so it always checks out origin/main before creating the new branch
- carry forward uncommitted edits automatically and cherry-pick requested commits when the input asks to "bring in" changes
- refresh the /newbranch documentation with the new workflow and natural language examples

## Testing
- python -m compileall .claude/commands/newbranch.py

------
https://chatgpt.com/codex/tasks/task_e_68dc27a8b4dc832f8719ba9245cfcc7e

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Updates `newbranch` to create branches from `origin/main`, stash/restore uncommitted edits, optionally cherry-pick requested commits parsed from input, and refreshes docs.
> 
> - **CLI script `/.claude/commands/newbranch.py`**:
>   - Build new branches from `origin/main` (`git checkout -b <name> origin/main`) after `fetch`/`pull`.
>   - Stash and restore uncommitted (including untracked) changes instead of auto-committing.
>   - Parse natural language to detect requests to include commits (e.g., "bring in changes"); resolve and cherry-pick selected or all local commits relative to `origin/main`.
>   - Sanitize branch names from free-form input; fallback to `dev{timestamp}`.
>   - Improved error handling and messages; attempt to restore stash on failures.
> - **Docs `/.claude/commands/newbranch.md`**:
>   - Updated behavior, usage examples, and error cases (including rebase conflicts).
>   - Clarifies always using latest `origin/main` and how commit inclusion works.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 4bde73221c230eab17ae8c69a8aea1c2d6aec04b. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
