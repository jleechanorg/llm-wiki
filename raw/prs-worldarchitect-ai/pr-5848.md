# PR #5848: Docs: make CLAUDE.md use git-header hook footer

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-03-04
**Author:** jleechan2015
**Stats:** +1/-1 in 1 files

## Summary
- Update `CLAUDE.md` mandatory greeting protocol footer to use the same `git-header.sh --with-api` script output pattern used in `AGENTS.md`.

## Raw Body
## Summary
- Update `CLAUDE.md` mandatory greeting protocol footer to use the same `git-header.sh --with-api` script output pattern used in `AGENTS.md`.

## Changes
- Before: `CLAUDE.md` required a static footer format `[Local: <branch> | Remote: <upstream> | PR: <number> <url>]`.
- After: `CLAUDE.md` requires ending responses with output of `$(git rev-parse --show-toplevel)/.claude/hooks/git-header.sh --with-api`.
- Why: Keeps Claude-side instructions aligned with AGENTS guidance and ensures consistent branch/remote/PR footer generation.

## Testing
- Manual doc verification:
  - Confirmed updated line exists in `/tmp/worldarchitect.ai/pr-claude-git-header/CLAUDE.md`.
  - Change scope is docs-only (single-line instruction update).

## Known Limitations
- This PR updates policy text only; it does not validate runtime behavior of external agents executing the hook.

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> <sup>[Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) is generating a summary for commit 793961eb94b0b334de2108bd2f410984d6d5c215. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
