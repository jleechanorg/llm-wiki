# PR #3236: Fix god_mode_directives dict-to-list type error

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-01-07
**Author:** jleechan2015
**Stats:** +95/-0 in 2 files

## Summary
- Fixed `'dict' object has no attribute 'append'` error occurring in God Mode interactions
- The `god_mode_directives` field in game state was sometimes stored as a dict instead of a list
- Added type check to convert dict to empty list before appending new directives
- Added test to verify the fix works when `god_mode_directives` starts as a dict

## Test Plan
- [x] Unit test added: `test_god_mode_directives_dict_to_list_conversion`
- [x] Quick validation test passed
- [ ] CI tests

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Ensures God Mode directive handling is robust against legacy/LLM-produced dict formats.
> 
> - In `world_logic.py`, convert `custom_campaign_state.god_mode_directives` to a list when not already a list; add a warning log before conversion
> - Adds unit test `test_god_mo

## Raw Body
## Summary

- Fixed `'dict' object has no attribute 'append'` error occurring in God Mode interactions
- The `god_mode_directives` field in game state was sometimes stored as a dict instead of a list
- Added type check to convert dict to empty list before appending new directives
- Added test to verify the fix works when `god_mode_directives` starts as a dict

## Root Cause

In `world_logic.py:2009`, the code used `.setdefault()` which returns the existing value if present. If `god_mode_directives` was already a dict (possibly from LLM responses or legacy saves), the subsequent `.append()` call failed.

## Test plan

- [x] Unit test added: `test_god_mode_directives_dict_to_list_conversion`
- [x] Quick validation test passed
- [ ] CI tests

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Ensures God Mode directive handling is robust against legacy/LLM-produced dict formats.
> 
> - In `world_logic.py`, convert `custom_campaign_state.god_mode_directives` to a list when not already a list; add a warning log before conversion
> - Adds unit test `test_god_mode_directives_dict_to_list_conversion` verifying successful append behavior when starting from a dict
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit c024696dfff6110b26adf0df1d57c4a5f0523830. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->

## Summary by CodeRabbit

* **Bug Fixes**
  * Improved handling of legacy god mode directive data to ensure compatibility and graceful degradation when data is in unexpected formats.

* **Tests**
  * Added test coverage for legacy data format conversion scenarios.

<sub>✏️ Tip: You can customize this high-level summary in your review settings.</sub>

<!-- end of auto-generated comment: release notes by coderabbit.ai -->
