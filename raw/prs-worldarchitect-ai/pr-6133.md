# PR #6133: feat(ci): broaden MCP smoke workflow path filters (wa-6129)

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-04-07
**Author:** jleechan2015
**Stats:** +120/-32 in 4 files

## Summary
- Expand `pull_request` and `push` path filters in `.github/workflows/mcp-smoke-tests.yml` so edits to MCP smoke comment generation, mode-contract scripts, `scripts/lib/**`, or `tests/test_mcp_global_installation.py` trigger the MCP Smoke Tests workflow.
- Add a regression test that locks in those path entries.
- Align `isolation_info` docstring example in `testing_mcp/lib/evidence_utils.py` with the `shared_campaigns` key used at runtime.

## Test Plan
- `python -m pytest tests/ -x -q` (48 passed, 4 skipped)

## Raw Body
## Summary
- Expand `pull_request` and `push` path filters in `.github/workflows/mcp-smoke-tests.yml` so edits to MCP smoke comment generation, mode-contract scripts, `scripts/lib/**`, or `tests/test_mcp_global_installation.py` trigger the MCP Smoke Tests workflow.
- Add a regression test that locks in those path entries.
- Align `isolation_info` docstring example in `testing_mcp/lib/evidence_utils.py` with the `shared_campaigns` key used at runtime.

## Test plan
- `python -m pytest tests/ -x -q` (48 passed, 4 skipped)

## Tracker
Internal / branch ref: **wa-6129-mcp-smoke** (GitHub issue `#6129` not found in this repo — link the correct issue if one exists elsewhere).

## Known limitations
- Does not change smoke test logic or server behavior; CI trigger coverage only.

Made with [Cursor](https://cursor.com)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> CI trigger/test-only changes with minimal blast radius; main risk is unintended extra or missing workflow runs due to path filter adjustments.
> 
> **Overview**
> Broadens `.github/workflows/mcp-smoke-tests.yml` `pull_request`/`push` path filters so edits to MCP smoke tooling (`scripts/mcp_smoke_comment.js`, smoke mode/contract scripts, `scripts/lib/**`) and the smoke workflow regression test itself will re-run MCP smoke CI.
> 
> Adds a new regression test in `tests/test_mcp_global_installation.py` that parses the workflow’s `on.*.paths` blocks and asserts the new entries are present (avoiding false positives from strings elsewhere in the YAML). Also tweaks `skeptic-cron.yml` evidence-request comment generation to a safer multi-arg `printf`, and fixes a docstring example key in `testing_mcp/lib/evidence_utils.py` (`shared_campaigns`).
> 
> <sup>Reviewed by [Cursor Bugbot](https://cursor.com/bugbot) for commit 94fbcb5a9fac0b588d97016ea8a4caa0c85e2361. Bugbot is set up for automated code reviews on this repo. Configure [here](https://www.cursor.com/dashboard/bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
