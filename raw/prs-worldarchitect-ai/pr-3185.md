# PR #3185: Add LLM detection for spicy mode in game state

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-02-02
**Author:** jleechan2015
**Stats:** +3435/-766 in 31 files

## Summary
Implements Spicy Mode - a mature content handling system with LLM detection, semantic routing, and specialized literary prompts.

**Key themes:**
- Semantic intent classification for intimate/romantic content
- Server-side spicy mode toggle handling with provider preservation
- SpicyModeAgent with specialized literary prompt stack
- Frontend/backend parity for manual provider change detection

## Raw Body
## Summary

Implements Spicy Mode - a mature content handling system with LLM detection, semantic routing, and specialized literary prompts.

**Key themes:**
- Semantic intent classification for intimate/romantic content
- Server-side spicy mode toggle handling with provider preservation
- SpicyModeAgent with specialized literary prompt stack
- Frontend/backend parity for manual provider change detection

## Production Code Changes

### 1. `mvp_site/agents.py` (+170/-1 lines)
**Purpose:** Agent routing and SpicyModeAgent implementation

| Change | Before | After | Why |
|--------|--------|-------|-----|
| **Added `_is_spicy_mode_enabled()` helper** | Duplicate 8-line pattern in 2 locations | Single helper function | DRY - Cursor Bugbot fix |
| **Added `SpicyModeAgent` class** | No agent for intimate content | New agent extending DialogAgent with spicy_mode instruction | Specialized literary prompt for mature content |
| **Added `SPICY_TOGGLE_*_PHRASES` constants** | Toggle commands processed as narrative | Explicit phrase sets for detection | Enables early return without LLM |
| **Added `_normalize_spicy_toggle_input()`** | UI prefixes not stripped | Strips prefixes, punctuation | Reliable toggle matching |
| **Added `spicy_bias` to classifier** | Classifier unaware of current mode | Passes spicy_bias when enabled | Slight prior boost (0.04) toward spicy |
| **Added MODE_SPICY to dialog bypass** | Only MODE_FACTION bypassed dialog | MODE_SPICY >= 0.7 also bypasses | Prevents dialog state trapping |
| **Added spicy intent routing** | Spicy intent fell to StoryMode | Routes to SpicyModeAgent | Semantic routing regardless of toggle |
| **Added `raw_classifier_*` metadata** | No original classifier data | Preserved in metadata | Debug visibility |

### 2. `mvp_site/world_logic.py` (+309/-0 lines)
**Purpose:** Server-side spicy mode handling and choice injection

| Change | Before | After | Why |
|--------|--------|-------|-----|
| **Added `_inject_spicy_mode_choice_if_n
