# PR #5965: feat(mem0): UserPromptSubmit+Stop hooks for automatic memory recall/save

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-03-15
**Author:** jleechan2015
**Stats:** +229/-0 in 3 files

## Summary
- `mem0_recall.py` — fires on `UserPromptSubmit`, searches `openclaw_mem0` qdrant, injects top-6 memories as `additionalContext` before every prompt
- `mem0_save.py` — fires on `Stop`, extracts facts from last response via mem0 LLM, saves to qdrant

## Raw Body
## Summary
- `mem0_recall.py` — fires on `UserPromptSubmit`, searches `openclaw_mem0` qdrant, injects top-6 memories as `additionalContext` before every prompt
- `mem0_save.py` — fires on `Stop`, extracts facts from last response via mem0 LLM, saves to qdrant

## Cross-session recall test (3/3 PASS)
All three hard cross-session questions answered correctly from memory alone:
- worldarchitect.ai PR #5955 goal recalled ✓
- qdrant docker container name + mount path recalled ✓
- mctrl Slack channel ID `C0AKALZ4CKW` recalled ✓

## Shared store
Both hooks use the same `openclaw_mem0` qdrant collection as the openclaw agent and Codex — one unified memory for all agents.

## Requirements
- `OPENAI_API_KEY` in env (for `text-embedding-3-small` embedder)
- qdrant running at `127.0.0.1:6333`
- `mem0ai` Python package installed
- Register in `~/.claude/settings.json` hooks section

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Medium risk because it introduces new automation that calls external services (OpenAI + local Qdrant) and can affect prompt context and data persisted across sessions, though it is gated by env checks and fails open (exits 0) on errors.
> 
> **Overview**
> Adds two new Claude hook scripts: `mem0_recall.py` injects top-scoring matches from the `openclaw_mem0` Qdrant collection as `additionalContext` on `UserPromptSubmit`, and `mem0_save.py` reads the transcript on `Stop` and saves inferred facts from the last assistant message back to mem0.
> 
> Updates `scripts/validate_imports.py` to allow `mem0` as an optional/conditional dependency so the new hooks’ inline import doesn’t trip import-validation.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit b582186fc5633ae52ab68ec9cac28f3acb471af5. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an au
