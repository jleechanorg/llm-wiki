# PR #2013: Fix auth script path in secondo-cli.sh

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2025-11-13
**Author:** jleechan2015
**Stats:** +22/-22 in 6 files

## Summary
Fix auth script path references in `secondo-cli.sh` to align with AI Universe authentication patterns.

## Raw Body
## Summary
Fix auth script path references in `secondo-cli.sh` to align with AI Universe authentication patterns.

## Changes
- Updated `check_auth()` to use `~/.claude/scripts/auth-cli.mjs` (instead of `scripts/auth-cli.mjs`)
- Updated `get_token()` to use `~/.claude/scripts/auth-cli.mjs`
- Fixed error message to show correct authentication path

## Context
Identified during MCP Agent Mail coordination with NovaDev. The auth script is deployed to `~/.claude/scripts/auth-cli.mjs` via `/localexportcommands`, but `secondo-cli.sh` was referencing the wrong path.

## Testing
- Verified auth script exists at `~/.claude/scripts/auth-cli.mjs`
- Confirmed alignment with `.claude/skills/ai-universe-auth.md` documentation
- Path consistency across auth workflow

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Switches secondo CLI, docs, and tests to use ~/.claude paths and fixes auth-cli.mjs references.
> 
> - **CLI**
>   - Update `~/.claude/scripts/secondo-cli.sh` usage string and switch auth calls to `~/.claude/scripts/auth-cli.mjs` for `status` and `token`.
> - **Docs**
>   - Replace references from `scripts/secondo-cli.sh` to `~/.claude/scripts/secondo-cli.sh` in `skills/ai-universe-httpie.md`, `skills/ai-universe-second-opinion-workflow.md`, `skills/secondo-dependencies.md`, and `mcp_servers/secondo/README.md`.
> - **Tests**
>   - Modify `scripts/test_secondo_pr.sh` to validate `.claude/scripts/secondo-cli.sh` path and related checks.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 09ac326d13265617c3253402479c78a979c7b73f. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->

## Summary by CodeRabbit

* **Chores**
  * Updated authentication configuration file location to the user home directory. Users referencing auth
