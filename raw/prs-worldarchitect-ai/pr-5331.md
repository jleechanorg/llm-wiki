# PR #5331: Repository URL path validation

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-02-12
**Author:** app/cursor
**Stats:** +3/-0 in 1 files

## Summary
Fixes a bug where repository URLs with `.git` suffixes or trailing slashes would cause GitHub API calls to fail.

## Raw Body
## Summary
Fixes a bug where repository URLs with `.git` suffixes or trailing slashes would cause GitHub API calls to fail.

## Changes
- Added two lines to normalize the `REPO_OWNER_AND_NAME` variable by removing `.git` suffixes and trailing slashes after initial parsing.

## Benefits
- Improves the robustness of the runner setup script by correctly handling common repository URL formats.
- Prevents GitHub API call failures when users provide URLs copied directly from git remotes or documentation.

## Testing
- [ ] Ran `./run_tests.sh` - all tests pass
- [ ] Tested core user workflows:
  - [ ] Campaign creation
  - [ ] Game interaction
  - [ ] Data persistence
- [ ] No regressions identified
- Verified the fix by ensuring repository URLs like `https://github.com/owner/repo.git` or `https://github.com/owner/repo/` are correctly processed for API calls.

## Screenshots
<!-- If UI changes, include before/after screenshots -->

## Checklist
- [x] Code follows project conventions
- [ ] Tests added/updated as needed
- [ ] Documentation updated if required
- [x] No hardcoded values or credentials
- [x] Changes are backwards compatible

---
🤖 Generated with [Claude Code](https://claude.ai/code)
Co-Authored-By: Claude <noreply@anthropic.com>

---

<!-- CURSOR_SUMMARY -->
> [!NOTE]
> **Low Risk**
> Low risk: a small, localized change to URL parsing in a setup script; primary impact is on GitHub API path construction for runner registration.
> 
> **Overview**
> Improves `scripts/self-hosted/setup-github-runner-redundant.sh` repository URL handling by normalizing the parsed `REPO_OWNER_AND_NAME` (strips a trailing `.git` suffix and trailing `/`). This prevents `gh api repos/$REPO_OWNER_AND_NAME/...` calls from failing when users paste common remote URL variants.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 679989221f20d00668368497568a878ab927d052. This will update automatically on new commits. Configure [here](https://cursor.com/dash
