# PR #5581: Revert "Allow configurable OpenClaw gateway port in Settings and use it at runtime"

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-02-16
**Author:** jleechan2015
**Stats:** +29/-993 in 16 files

## Summary
(none)

## Raw Body
Reverts jleechanorg/worldarchitect.ai#5580

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Primarily deletes an optional provider path and related UI/validation/tests, reducing surface area rather than changing core gameplay logic. Risk is mostly limited to any leftover user settings referencing `openclaw` now being unsupported.
> 
> **Overview**
> This PR **reverts the OpenClaw gateway integration** by removing the `openclaw` LLM provider from the backend provider list and selection logic, deleting `openclaw_provider.py`, and stripping OpenClaw-specific settings/model inference and BYOK wiring from `llm_service`/`constants`.
> 
> The settings UI/API contract is rolled back to only `gemini`/`openrouter`/`cerebras`: the OpenClaw radio option and model/port controls are removed from `settings.html` and `settings.js`, validation and default settings entries are dropped, and OpenClaw tests/docs/dependencies (including `websocket-client`) are deleted. Streaming is now restricted to **Gemini only**.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 79658f879e49ef9968cdeb8de24a6352e831e10f. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
