# PR #5577: Relocate SDUI design to roadmap and expand with system plan

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-02-16
**Author:** jleechan2015
**Stats:** +627/-0 in 1 files
**Labels:** codex

## Summary
(none)

## Raw Body
### Motivation

- Centralize the Server-Driven UI (SDUI) design in `roadmap/` and turn the previous design note into a roadmap-grade implementation plan with explicit next steps.  
- Provide a clear backend-first migration path by proposing a dedicated `mvp_site/sdui` module, optional MCP envelope return (`return_sdui`), and a repo ownership split for client renderers.  
- Ensure the plan directly maps current `frontend_v1` streaming and structured-field behavior to a stable SDUI contract so no v1 capabilities are lost during migration.

### Description

- Moved the original design file out of `docs/design/ai-universe-sdui-architecture.md` and added the expanded spec as `roadmap/ai-universe-sdui-architecture.md` containing a TOC, mermaid system diagram, JSON input/output schemas, and extraction mapping.  
- Proposed a new backend package boundary `mvp_site/sdui` and enumerated concrete files to implement (e.g., `envelope.py`, `component_mapper.py`, `schema.py`, `mcp_adapter.py`) along with accompanying unit test files and explicit file-justification entries per repo protocol.  
- Documented optional MCP delivery (`return_sdui=true`), a feature/compatibility matrix tied to `frontend_v1` behavior, phased implementation plan, and validation gates; this change is documentation-only and does not modify runtime code paths.

### Testing

- Ran `git diff --check` and the whitespace/check passed.  
- Verified presence and key sections of the new roadmap file with `rg -n "^## Table of Contents|^## 3\) System Diagram|^## 8\) Proposed Files|^## 9\) v1 Compatibility Matrix|mvp_site/sdui|return_sdui" roadmap/ai-universe-sdui-architecture.md` and the expected sections were found.  
- Committed the changes with `git add -A && git commit -m "Move SDUI design to roadmap with diagram and file plan"` successfully, but attempts to push (`git push`) were not completed in this environment due to no remote/upstream configured (local branch `work` has no remote).

------
[Codex Task](https:
