# PR #3144: feat(api): Implement story pagination to avoid loading all entries into memory

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-01-06
**Author:** jleechan2015
**Stats:** +1894/-133 in 15 files

## Summary
- **Root cause**: Campaign kuXKa6vrYY6P99MfhWBn had 1620 story entries = 34.7MB response, exceeding Cloud Run 32MB limit
- Implements query-level pagination using Firestore DESCENDING + LIMIT to avoid loading all entries into memory
- Adds Load older entries UI button for accessing full story history
- Default fetches last 300 entries (configurable via story_limit param, clamped 10-500)

## Test Plan
- [x] Unit tests for pagination logic in test_api_routes.py
- [ ] Manual test: Load campaign with many entries
- [ ] Manual test: Click Load older entries button

## Raw Body
## Summary
- **Root cause**: Campaign kuXKa6vrYY6P99MfhWBn had 1620 story entries = 34.7MB response, exceeding Cloud Run 32MB limit
- Implements query-level pagination using Firestore DESCENDING + LIMIT to avoid loading all entries into memory
- Adds Load older entries UI button for accessing full story history
- Default fetches last 300 entries (configurable via story_limit param, clamped 10-500)

## Changes
### Backend (firestore_service.py)
- get_campaign_metadata() - lightweight campaign fetch without story entries
- get_story_count() - efficient count using Firestore aggregation query
- get_story_paginated() - fetches only last N entries at query level

### API (main.py)
- Updated get_campaign endpoint to use paginated story fetch
- Added story_pagination metadata in response
- New endpoint GET /api/campaigns/<id>/story for loading older entries

### Frontend (app.js)
- Added pagination state management
- Added loadOlderStoryEntries() function for cursor-based pagination
- Added Load older entries button

## Test plan
- [x] Unit tests for pagination logic in test_api_routes.py
- [ ] Manual test: Load campaign with many entries
- [ ] Manual test: Click Load older entries button

## Related
- Follows up on PR #3143 (quick fix with in-memory truncation)
- Bead: WA-935

Generated with Claude Code

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Introduces query-level pagination for campaign stories to prevent oversized responses and reduce memory/latency.
> 
> - New `GET /api/campaigns/<id>/story` endpoint with cursor params (`limit`, `before`, `before_id`, `newer_count`, `newer_gemini_count`) and pagination metadata (`total_count`, `fetched_count`, `has_older`, `oldest_timestamp`, `oldest_id`)
> - `GET /api/campaigns/<id>` now fetches paginated story entries; adds `story_limit` and `slim=true` options; returns `story_pagination` block; improved size diagnostics logging
> - Firestore service: `get_campaign_metadata()`, `get_story_count()` (aggregation with fallback), `get
