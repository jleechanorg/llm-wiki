# PR #4529: fix(god-mode): set dice_roll_strategy=None for entire god mode flow

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-02-02
**Author:** jleechan2015
**Stats:** +149/-6 in 5 files

## Summary
**BUG:** God mode still allowed code execution/dice rolls because `dice_roll_strategy` was set at line 4150 based on model, but we only passed `None` to `build_system_instructions`. The local variable still had `code_execution` value.

**FIX:** Set `dice_roll_strategy = None` when `is_god_mode_command` is True, BEFORE any code that uses it for API calls or validation.

**Key themes:**
- God mode dice strategy regression fix
- TDD-driven fix with failing test first

## Raw Body
## Summary

**BUG:** God mode still allowed code execution/dice rolls because `dice_roll_strategy` was set at line 4150 based on model, but we only passed `None` to `build_system_instructions`. The local variable still had `code_execution` value.

**FIX:** Set `dice_roll_strategy = None` when `is_god_mode_command` is True, BEFORE any code that uses it for API calls or validation.

**Key themes:**
- God mode dice strategy regression fix
- TDD-driven fix with failing test first

## Production Code Changes

### 1. `mvp_site/llm_service.py` (+5/-3 lines)
**Purpose:** Core LLM service handling story continuation

- **Added `dice_roll_strategy = None` assignment in god mode block** - Previously only passed `None` to `build_system_instructions` → Now set the local variable to `None` before ANY code uses it → Prevents:
  1. `[SYSTEM ENFORCEMENT:...]` being added to user input (line 4654)
  2. Code execution fabrication checks triggering (line 4793)
  3. Dice metadata being populated in structured response (line 4742)

## Test Changes

### 1. `mvp_site/tests/test_end2end/test_god_mode_end2end.py` (+90 lines)
- Added `test_god_mode_does_not_use_code_execution_strategy` to `TestGodModeEnd2End`
- **RED phase verified:** Test failed before fix (found `SYSTEM ENFORCEMENT` in prompt)
- **GREEN phase verified:** Test passes after fix
- All 21 god mode tests pass

## Test Results
- All 21 god mode E2E tests pass
- All 14 world_logic god mode tests pass

## Known Limitations
| Issue | Severity | Notes |
|-------|----------|-------|
| None | - | This is a targeted fix for the dice_roll_strategy regression |

## Evidence
- Bug discovered via bead REV-e0o
- Production campaign `Rp7hvzhOnS3TlvxpwCy4` showed dice rolls in god mode

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Touches core LLM request orchestration and prompt construction logic; mistakes could affect how all god-mode interactions are routed or va
