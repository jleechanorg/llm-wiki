# PR #2444: feat(orchestration): Add -f flag to cursor agent for non-interactive execution

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2025-12-17
**Author:** jleechan2015
**Stats:** +73/-36 in 6 files

## Summary
- Add `-f` (force) flag to cursor-agent command template for non-interactive execution
- Update test to verify `-f` flag is present in command template
- Bump orchestration version to 0.1.12 and fix package-dir configuration

## Test Plan
- [x] Run `pytest orchestration/tests/test_cli_support.py -v`
- [x] Verify cursor agent via orchestration framework
- [x] Fresh install from PyPI confirms `-f` flag present

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Enable non-interactive `cursor-agent` (-f), improve PR comment fetching (inline/issue + optional review bodies) with idempotent replies, and update copilot-lite verification; bump to 0.1.12.
> 
> - **Comment processing (/

## Raw Body
## Summary
- Add `-f` (force) flag to cursor-agent command template for non-interactive execution
- Update test to verify `-f` flag is present in command template
- Bump orchestration version to 0.1.12 and fix package-dir configuration

## Changes
| File | Change |
|------|--------|
| `orchestration/task_dispatcher.py` | Add `-f` flag to cursor command template |
| `orchestration/tests/test_cli_support.py` | Add test assertion for `-f` flag |
| `orchestration/pyproject.toml` | Version bump + package-dir fix |

## Why
The `-f` flag is required for `cursor-agent` to run without prompting for user confirmation, which is essential for automated orchestration workflows.

## Testing
- ✅ All 35 CLI support tests pass
- ✅ Verified cursor agent works through orchestration framework (2+2=4 test)
- ✅ Published to PyPI v0.1.12 and verified fresh install has `-f` flag

## Test plan
- [x] Run `pytest orchestration/tests/test_cli_support.py -v`
- [x] Verify cursor agent via orchestration framework
- [x] Fresh install from PyPI confirms `-f` flag present

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Enable non-interactive `cursor-agent` (-f), improve PR comment fetching (inline/issue + optional review bodies) with idempotent replies, and update copilot-lite verification; bump to 0.1.12.
> 
> - **Comment processing (/commentreply)**:
>   - Fetch and merge `inline`, `issue`, and optional `review` body comments; tag each with `type`.
>   - Graceful degradation when review bodies fail; require core inline/issue.
>   - Idempotency for review-body replies by detecting prior issue-comment references.
>   - Never post placeholders when no Claude response found.
> - **/copilot-lite workflow**:
>   - Safer branch sanitization; write fetched comments to `{comments: [...]}`; add `type` in fallback.
>   - Update jq queries to handle `.comments` structure and per-type counts; stricter verification gate and missing-ID listing.
> - **Orch
