# PR #5889: Fix settings listener re-init after SPA navigation

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-03-14
**Author:** jleechan2015
**Stats:** +149/-4 in 2 files

## Summary
This is a clean extraction of the true settings navigation fix from #5511 onto current `main`.

When the SPA loads `/settings`, it replaces `settingsView.innerHTML` with fresh DOM. On repeat visits, [`initializeSettings()`](mvp_site/frontend_v1/app.js) skipped `setupSettingsEventListeners()` because `window.settingsListenersAttached` was already set from the first visit. That left the replacement settings DOM without model/BYOK listeners.

This PR fixes only that re-initialization bug.

## Raw Body
## Summary
This is a clean extraction of the true settings navigation fix from #5511 onto current `main`.

When the SPA loads `/settings`, it replaces `settingsView.innerHTML` with fresh DOM. On repeat visits, [`initializeSettings()`](mvp_site/frontend_v1/app.js) skipped `setupSettingsEventListeners()` because `window.settingsListenersAttached` was already set from the first visit. That left the replacement settings DOM without model/BYOK listeners.

This PR fixes only that re-initialization bug.

## Scope
- Re-run settings initialization after each SPA settings page load once `settings.js` is already loaded
- Add a focused regression test proving listeners attach again after simulated settings DOM replacement

## Explicitly Excluded
- No BYOK backend changes
- No provider or settings API changes
- No unrelated UI, auth, or OpenClaw changes from #5511's branch history

## Relation to #5511
- Extracted from: #5511
- This PR carries only the repeat settings navigation / BYOK re-attachment fix needed on top of current `main`

## Validation
- `node` CommonJS harness: repeated settings initialization attaches listeners to replacement DOM
- `npx eslint mvp_site/frontend_v1/app.js mvp_site/frontend_v1/tests/settings_listeners.test.js`
- Evidence bundle saved under `/tmp/pr-5511-settings-nav-evidence`

## Notes
The existing `mvp_site/frontend_v1/tests/settings_listeners.test.js` file still has unrelated baseline failures in this repo around OpenClaw/auth expectations when run wholesale under a CommonJS compile path. The new repeat-navigation regression test added here passes.

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Low risk: changes are limited to frontend settings-page initialization and add/adjust unit tests, with no API or backend behavior changes.
> 
> **Overview**
> Fixes repeat navigation to `/settings` in the SPA by **always re-running** `setupSettingsEventListeners()` once `settings.js` is already loaded, ensuring freshly replaced settings DOM gets 
