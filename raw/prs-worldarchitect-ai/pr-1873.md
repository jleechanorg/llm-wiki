# PR #1873: Load all Codex MCP API keys from interactive shell

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2025-10-14
**Author:** jleechan2015
**Stats:** +86/-0 in 2 files
**Labels:** codex

## Summary
- load every Codex MCP API key (Grok, xAI, Render, Perplexity) from an interactive shell so ~/.bashrc exports are respected

## Raw Body
## Summary
- load every Codex MCP API key (Grok, xAI, Render, Perplexity) from an interactive shell so ~/.bashrc exports are respected

## Testing
- bash -n codex_mcp.sh
- bash -n scripts/mcp_common.sh

------
https://chatgpt.com/codex/tasks/task_e_68ee936dd6dc832fab66130b3c422983

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Loads missing API keys from ~/.bashrc in non-interactive runs and sets/propagates default Grok/XAI model env vars for grok-mcp.
> 
> - **Launcher (`codex_mcp.sh`)**:
>   - Loads missing API keys from interactive shell (`~/.bashrc`) for `XAI_API_KEY`, `GROK_API_KEY`, `RENDER_API_KEY`, `PERPLEXITY_API_KEY` via `load_interactive_env_var`.
>   - Sets defaults: `GROK_DEFAULT_MODEL="grok-3"`; `XAI_DEFAULT_CHAT_MODEL` falls back to `GROK_DEFAULT_MODEL`.
>   - Cleans up temporary variables after loading.
> - **MCP setup (`scripts/mcp_common.sh`)**:
>   - For `grok-mcp`, derives a `grok_default_model` from `GROK_DEFAULT_MODEL` or `XAI_DEFAULT_CHAT_MODEL` and passes env flags: `GROK_DEFAULT_MODEL`, `GROK_MODEL`, `XAI_DEFAULT_CHAT_MODEL`, `XAI_MODEL` alongside `XAI_API_KEY`/`GROK_API_KEY`.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 5b7ffea1472c6952312fb1812df2205aebe3740f. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->
## Summary by CodeRabbit

- New Features
  - Automatically loads missing API keys from your shell profile when running non-interactively, reducing setup friction.
  - Applies sensible default model selections for Grok/XAI when not explicitly configured.
  - Ensures the chosen default model is propagated to the Grok MCP server setup for consistent runtime behavior.

- Chores
  - Cleans up temporary environment variables after initialization to avoid leaking transient values.
<!-- end of auto-generated comment: release notes by coderabbit
