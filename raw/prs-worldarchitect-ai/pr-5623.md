# PR #5623: fix(ci): add missing permissions to auto-deploy-dev workflow

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-02-18
**Author:** jleechan2015
**Stats:** +3/-0 in 1 files

## Summary
- **Fixed startup_failure** on `auto-deploy-dev.yml` that has been broken since at least Feb 16 (20+ consecutive failures)
- **Root cause**: Top-level `permissions` only granted `contents: read`, but the reusable workflow job passed `issues: write` and needed `pull-requests: read` — GitHub Actions rejects permission escalation beyond the workflow-level grant
- **Fix**: Added `issues: write` and `pull-requests: read` to the top-level permissions block

## Test Plan
- [x] YAML validates locally
- [ ] CI checks pass on PR
- [ ] Trigger auto-deploy-dev manually to verify no more startup_failure

## Raw Body
## Summary
- **Fixed startup_failure** on `auto-deploy-dev.yml` that has been broken since at least Feb 16 (20+ consecutive failures)
- **Root cause**: Top-level `permissions` only granted `contents: read`, but the reusable workflow job passed `issues: write` and needed `pull-requests: read` — GitHub Actions rejects permission escalation beyond the workflow-level grant
- **Fix**: Added `issues: write` and `pull-requests: read` to the top-level permissions block

## Production Code Changes
**`.github/workflows/auto-deploy-dev.yml`** - before: top-level `permissions: { contents: read }` only → now: `permissions: { contents: read, issues: write, pull-requests: read }` → why: reusable workflow call job cannot escalate permissions beyond what the calling workflow declares at top level

## Test Plan
- [x] YAML validates locally
- [ ] CI checks pass on PR
- [ ] Trigger auto-deploy-dev manually to verify no more startup_failure

## Known Limitations
- The previous PR #5620 fixed the `fetch-pr-details/action.yml` YAML parse error but this separate permissions issue was the actual blocker for dev auto-deploy

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> CI-only change that adjusts GitHub Actions permission scopes; behavior change is limited to allowing the workflow to run successfully.
> 
> **Overview**
> Fixes `auto-deploy-dev.yml` startup failures by expanding the workflow’s top-level GitHub Actions `permissions` to include `issues: write` and `pull-requests: read`, matching the permissions requested by the reusable `deploy-dev.yml` job.
> 
> Also adds `pull-requests: read` to the `deploy` job’s explicit permissions to keep job-level permissions aligned with the workflow-level grant.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit c428e4602aa3825e1d16804b43d9732ca30e0d67. This will update automatically on new commits. Configure [here](https://cursor.com/dashbo
