# PR #3053: refactor(export): Unify campaign export logic between web UI and CLI

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-01-04
**Author:** jleechan2015
**Stats:** +217/-182 in 3 files

## Summary
- Centralizes enhanced story formatting in `document_generator.py`
- Web UI export button and CLI download script now share identical logic
- Removes ~130 lines of duplicated code from the download script

## Test Plan
- [x] Verified document_generator tests pass
- [x] Verified script syntax compiles
- [x] Tested enhanced formatting function directly
- [ ] CI passes

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Centralizes enhanced story formatting and unifies export behavior across web UI and CLI.
> 
> - Adds shared `get_story_text_from_context_enhanced()` in `document_generator.py` with scene headers, session headers, resources, dice rolls, and choi

## Raw Body
## Summary
- Centralizes enhanced story formatting in `document_generator.py`
- Web UI export button and CLI download script now share identical logic
- Removes ~130 lines of duplicated code from the download script

## Changes
| File | Change |
|------|--------|
| `mvp_site/document_generator.py` | Add enhanced formatting functions |
| `mvp_site/world_logic.py` | Use shared `get_story_text_from_context_enhanced()` |
| `scripts/download_campaign.py` | Import from document_generator instead of duplicating |

## Features Now Unified
Both export paths now produce identical output with:
- Scene numbers and headers (`SCENE 1`, `SCENE 2`, etc.)
- Session headers (timestamps, location, status)
- Resources display
- Dice rolls display
- Choice detection (`Player (choice: A)` vs `Player (freeform)`)

## Example Output
```
============================================================
SCENE 1
============================================================
[Day 1, Morning]

Game Master:
The adventure begins...

Player (choice: A):
Attack

Player (freeform):
I search the room
```

## Test plan
- [x] Verified document_generator tests pass
- [x] Verified script syntax compiles
- [x] Tested enhanced formatting function directly
- [ ] CI passes

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Centralizes enhanced story formatting and unifies export behavior across web UI and CLI.
> 
> - Adds shared `get_story_text_from_context_enhanced()` in `document_generator.py` with scene headers, session headers, resources, dice rolls, and choice detection
> - Updates `world_logic.export_campaign_unified` to use the shared formatter
> - Refactors `scripts/download_campaign.py` to import the shared formatter and remove ~130 lines of duplicated logic
> 
> Outputs from both export paths are now consistent and richer (e.g., `SCENE N`, `Player (choice: X)`), reducing maintenance risk.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboa
