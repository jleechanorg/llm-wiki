# PR #1248: fix: Restore Claude Code hooks functionality - UserPromptSubmit and speculation detection

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2025-08-10
**Author:** jleechan2015
**Stats:** +13/-3 in 1 files

## Summary
Fixes critical Claude Code hooks configuration issues that broke slash command composition and hook execution timing:

- **Restore UserPromptSubmit hooks**: Re-enables `compose-commands.sh` for slash command combinations like `/think /debug /analyze`
- **Fix speculation detection hook timing**: Move from PostResponse to PostToolUse for proper execution
- **Remove incorrect PreToolUse usage**: UserPromptSubmit is the correct hook for user input processing

## Raw Body
## Summary

Fixes critical Claude Code hooks configuration issues that broke slash command composition and hook execution timing:

- **Restore UserPromptSubmit hooks**: Re-enables `compose-commands.sh` for slash command combinations like `/think /debug /analyze`
- **Fix speculation detection hook timing**: Move from PostResponse to PostToolUse for proper execution
- **Remove incorrect PreToolUse usage**: UserPromptSubmit is the correct hook for user input processing

## Root Cause Analysis

**The Problem**: Commit `83daf1ee0` ("Revert settings.json to main version to remove hooks changes") accidentally removed the `UserPromptSubmit` hooks section that powered slash command composition.

**The Impact**: 
- `compose-commands.sh` script existed but couldn't execute (no hook integration)
- Users lost ability to combine commands like `/think /debug /analyze`
- Hook execution timing was suboptimal

## Technical Details

### UserPromptSubmit Hook Restoration
```json
"UserPromptSubmit": [
  {
    "matcher": "*",
    "hooks": [
      {
        "type": "command",
        "command": "bash -c 'if git rev-parse --is-inside-work-tree >/dev/null 2>&1; then ROOT=$(git rev-parse --show-toplevel); [ -x \"$ROOT/.claude/hooks/compose-commands.sh\" ] && exec \"$ROOT/.claude/hooks/compose-commands.sh\" \"$@\"; fi; echo \"$@\"' --",
        "description": "Universal command composition for slash command combinations"
      }
    ]
  }
]
```

### Hook Timing Optimization
- **UserPromptSubmit**: Processes user input before Claude (perfect for command composition)
- **PostToolUse**: Runs after tool execution (better for speculation detection than PostResponse)

## Test Results

✅ **Command Composition Works**:
```bash
Input: "/think /debug analyze this problem"
Output: "🔍 Use these approaches in combination: /think /debug. Apply this to: analyze this problem"
```

✅ **JSON Validation**: `jq . .claude/settings.json` passes
✅ **Script Executable**: `.claude/hooks/compose-commands.sh` confirmed
