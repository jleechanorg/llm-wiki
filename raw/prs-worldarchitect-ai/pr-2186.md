# PR #2186: Enforce fixpr agent selection and align commit prefixes

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2025-11-30
**Author:** jleechan2015
**Stats:** +222/-54 in 8 files

## Summary
- force the requested CLI for fixpr (no keyword-based overrides) and pass it through dispatcher
- align commit prefixes with the actual CLI used; avoid Claude/Gemini mix-ups
- update tests to reflect forced-CLI behavior

## Raw Body
## Summary
- force the requested CLI for fixpr (no keyword-based overrides) and pass it through dispatcher
- align commit prefixes with the actual CLI used; avoid Claude/Gemini mix-ups
- update tests to reflect forced-CLI behavior

## Testing
- not run (logic-only orchestration change)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Forces the requested CLI for fixpr end-to-end, adds a dry‑run listing of conflict/failing‑check PRs, aligns commit markers, updates tests, and bumps versions.
> 
> - **Orchestration**:
>   - Enforce CLI selection via `forced_cli` in `TaskDispatcher._detect_agent_cli` and `analyze_task_and_create_agents` with validation and precedence.
>   - `orchestrated_pr_runner.dispatch_agent_for_pr` passes `forced_cli`, updates task description with explicit fallback steps, and aligns commit message prefix to `[<agent_cli>-automation-commit]`.
> - **PR Monitor**:
>   - Add `list_actionable_prs()` to surface PRs with merge conflicts or failing checks; new CLI flag `--list-eligible`.
>   - Treat comment "skipped" as success for safety metrics; only increment processed count on actual posts; improved logging.
>   - Import and use `has_failing_checks`.
> - **Tests**:
>   - Update dispatcher interface usages to accept `forced_cli`; expand CLI-selection tests (forced, keywords, fallbacks, errors).
>   - Add `test_pr_monitor_eligibility.py` for listing behavior; adjust existing runner tests accordingly.
> - **Versions/Deps**:
>   - Bump `jleechanorg-pr-automation` to `0.2.5` and `jleechanorg-orchestration` to `0.1.10`; update dependency constraint.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit c3bbb41dd912e46bc77fe268eb65321a95089b6b. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->
## Summary by CodeRabbit

* **New Features**
  - Optional forced-CLI override 
