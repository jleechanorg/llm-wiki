# PR #4273: Prompts: add code_execution variants and loader

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-01-31
**Author:** jleechan2015
**Stats:** +18/-14 in 4 files

## Summary
- Replace inline code_execution prompt injection with prompt variants
- Add prompt loader helper and tests for prompt variants

## Raw Body
## Summary
- Replace inline code_execution prompt injection with prompt variants
- Add prompt loader helper and tests for prompt variants

## Original PR
- https://github.com/jleechanorg/worldarchitect.ai/pull/4226

## Beads
- DICE-kow

## Testing
- Not run (split-only change)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Changes affect prompt composition for Gemini `code_execution` (core runtime behavior) and could alter dice/tool instruction precedence if marker extraction or prompt contents are incorrect; mitigated by explicit markers, warnings, and new tests.
> 
> **Overview**
> Gemini `code_execution` dice instructions are now sourced from a version-controlled prompt variant (`mechanics_system_instruction_code_execution.md`) via `load_prompt_variant()`, instead of being built via inline string injection.
> 
> The provider now extracts only the dice section using explicit `BEGIN/END_CODE_EXECUTION_DICE_SECTION` markers and logs a warning + falls back to the whole file if markers are missing; the prompt loader also hardens error messaging when variant files aren’t found.
> 
> Adds/updates the code-execution prompt file (including correct section marker placement) and expands tests to verify variant loading, required/forbidden content, and that `gemini_provider.py` no longer contains the string-injection pattern.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 8153922914714f103ca4345c90b828563f67fd5f. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->
## Summary by CodeRabbit

* **New Features**
  * Added external, version-controlled prompt variant loading and a comprehensive game mechanics protocol for dice/code execution scenarios.

* **Refactor**
  * Replaced embedded code-execution guidance with dynamic prompt-based guidance and deterministic fall
