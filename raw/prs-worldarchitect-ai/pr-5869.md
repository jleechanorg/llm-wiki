# PR #5869: Fix /localexportcommands Ralph export and make /ralph repo-agnostic

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-03-07
**Author:** jleechan2015
**Stats:** +164/-40 in 4 files

## Summary
- export root `ralph/` toolkit to `~/ralph` in `/localexportcommands`
- update `/ralph` command guidance to run Ralph against any repository via `--workspace`
- keep existing `.claude`/`.codex` export behavior unchanged

## Raw Body
## Summary
- export root `ralph/` toolkit to `~/ralph` in `/localexportcommands`
- update `/ralph` command guidance to run Ralph against any repository via `--workspace`
- keep existing `.claude`/`.codex` export behavior unchanged

## Changes
- `.claude/commands/localexportcommands.md`
  - added `Ralph Toolkit (ralph/) → ~/ralph/` to exported components
  - added implementation block to copy `ralph/` to `~/ralph/` (rsync preferred, `cp -a` fallback)
  - added executable permission normalization for exported `*.sh`
  - added post-export checklist entry for `~/ralph`
- `.claude/commands/ralph.md`
  - removed worldarchitect-specific framing and `scripts/ralph/...` assumptions
  - documented portable invocation using `~/ralph/ralph.sh run --workspace /path/to/target-repo`
  - kept optional in-repo invocation example for current repo

## Testing
- `bash -n` on extracted implementation shell block in `.claude/commands/localexportcommands.md` (pass)
- `git diff --check` (pass)

## Known limitations
- This PR updates command docs/embedded scripts only; it does not alter runtime behavior of `ralph/ralph.sh` itself.

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Changes affect developer automation that writes to `~/ralph` and alters `ralph.sh` run startup behavior (auto-creating `prd.json` and validating git workspaces), which could disrupt existing local workflows if assumptions differ.
> 
> **Overview**
> Makes Ralph portable across repositories by updating `/localexportcommands` to export/sync the root `ralph/` toolkit to `~/ralph` (with backup, `rsync --delete` support, runtime/state-file preservation, and executable normalization), and updating the `/ralph` command docs to use `~/ralph/ralph.sh ... --workspace`.
> 
> Enhances `ralph.sh` `run` to resolve workspace earlier and to *fail fast* by creating a minimal `prd.json` template (or erroring if `--workspace` isn’t a git repo) instead of running without a PRD; adds/updates `ralph/tests/test_ralph.sh` covera
