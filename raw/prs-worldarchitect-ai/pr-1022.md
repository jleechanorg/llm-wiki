# PR #1022: fix: Resolve Claude CLI EBADF errors in bot server

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2025-07-26
**Author:** jleechan2015
**Stats:** +252/-29 in 3 files

## Summary
Fixes Claude CLI "EBADF: bad file descriptor" errors in the bot server that were preventing GitHub Actions slash commands from working.

## Raw Body
## Summary
Fixes Claude CLI "EBADF: bad file descriptor" errors in the bot server that were preventing GitHub Actions slash commands from working.

## Changes Made
- **Environment cleanup**: Remove `NODE_CHANNEL_FD` environment variable that causes EBADF errors
- **Subprocess fixes**: Add `stdin=subprocess.DEVNULL` to prevent file descriptor issues
- **Path portability**: Use `os.path.expanduser('~/.claude/local/claude')` instead of hardcoded paths
- **Error handling**: Comprehensive timeout and error response handling
- **Anti-pattern enforcement**: Maintain prohibition of hardcoded command parsing

## Testing Results
- ✅ First test prompt (hello) returns actual Claude response without EBADF errors
- ✅ Claude CLI integration now functional 
- ✅ Maintains CLI usage (not API) to avoid cost concerns
- ⚠️ Some complex prompts may timeout after 30s but core functionality restored

## Test Instructions
```bash
# Terminal 1: Start the server
cd claude-bot-commands/server/
python3 claude-bot-server.py

# Terminal 2: Test the endpoint
curl -X POST -d "prompt=hello" http://127.0.0.1:5001/claude
```

🤖 Generated with [Claude Code](https://claude.ai/code)
