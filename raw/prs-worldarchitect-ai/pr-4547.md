# PR #4547: fix(frontend): display dice roll label when purpose field is missing

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-02-02
**Author:** jleechan2015
**Stats:** +61/-1 in 2 files

## Summary
- Backend now normalizes `label` → `purpose` in action_resolution rolls for frontend compatibility

**Key themes:**
- Single source of truth (backend handles field mapping, not frontend)
- Cleaner architecture (frontend stays simple, backend normalizes data)

## Raw Body
## Summary
- Backend now normalizes `label` → `purpose` in action_resolution rolls for frontend compatibility

**Key themes:**
- Single source of truth (backend handles field mapping, not frontend)
- Cleaner architecture (frontend stays simple, backend normalizes data)

## Production Code Changes

### 1. `mvp_site/action_resolution_utils.py` (+10/-1 lines)
**Purpose:** Normalize action_resolution data before sending to frontend

- **Added label→purpose normalization** - Function only populated missing `total` → Now also copies `label` to `purpose` when `purpose` is missing → Frontend expects `purpose` for roll labels, but code execution produces `label`

## Test Changes
- Added 2 tests in `test_action_resolution_utils.py`:
  - `test_add_action_resolution_normalizes_label_to_purpose` - Tests HP roll scenario  
  - `test_add_action_resolution_preserves_existing_purpose` - Tests existing purpose is not overwritten
- All 25 tests pass

## Evidence

**Before (what user saw):**
```
🎲 Dice Rolls:
1d6-1 = 2
```

**After (what user will see):**
```
🎲 Dice Rolls:
1d6-1 = 2 (Level 9 HP Increase)
```

**Root cause:** The action_resolution.mechanics.rolls data has `label` but frontend expected `purpose`.

**Why backend fix is better:**
- Frontend stays simple (only checks one field)
- Normalization happens once in central location
- No duplicate logic in frontend_v1 and frontend_v2

🤖 Generated with [Claude Code](https://claude.ai/code)

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->

## Summary by CodeRabbit

* **Bug Fixes**
  * Enhanced data normalization for action resolution records to automatically map alternative fields and populate missing values, ensuring more consistent data structure and handling.

* **Tests**
  * Added comprehensive unit tests verifying normalization behavior, including proper field mapping, preservation of existing data, and handling of missing data fields.

<!-- end of auto-generated comment: release notes by coderabbit.a
