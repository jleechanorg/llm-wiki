# PR #127: Fix combat bug

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2025-06-27
**Author:** jleechan2015
**Stats:** +329/-5 in 3 files

## Summary
Fixes critical combat cleanup bugs where defeated enemies were not being properly removed from the game state, leading to inconsistent combat state and gameplay issues.

## Raw Body
# Fix Combat Cleanup Bug - RED/GREEN Testing Implementation

## Summary

Fixes critical combat cleanup bugs where defeated enemies were not being properly removed from the game state, leading to inconsistent combat state and gameplay issues.

## Changes

### 🐛 **Bug Fixes**
- **Fixed null pointer vulnerability** in `apply_automatic_combat_cleanup()` when `GameState.from_dict()` returns `None`
- **Enhanced cleanup logic** to handle all defeat scenarios robustly
- **Improved defensive programming** for edge cases with malformed data

### 🧪 **Testing**
- Implemented **RED/GREEN testing methodology** to ensure comprehensive bug coverage
- Created failing tests first (RED phase) to expose real issues
- Applied fixes to make tests pass (GREEN phase)
- Added comprehensive test suite covering edge cases

### 🔧 **Implementation Details**

#### Core Fix in `main.py`
```python
def apply_automatic_combat_cleanup(updated_state_dict: dict, proposed_changes: dict) -> dict:
    # CRITICAL BUG FIX: Handle case where GameState.from_dict returns None
    temp_game_state = GameState.from_dict(updated_state_dict)
    if temp_game_state is None:
        logging.error("COMBAT CLEANUP ERROR: GameState.from_dict returned None, returning original state")
        return updated_state_dict
    
    # Enhanced cleanup logic for all scenarios...
```

#### Key Improvements
- **Null safety**: Added explicit check for `GameState.from_dict()` returning `None`
- **Universal cleanup**: Always attempt cleanup when combatants exist, regardless of change type
- **Better logging**: Enhanced debugging information for cleanup operations
- **Edge case handling**: Robust handling of malformed combat state data

### 📁 **Files Modified**
- `mvp_site/main.py` - Core combat cleanup function fixes
- `mvp_site/test_combat_cleanup_red_green.py` - Comprehensive RED/GREEN test suite
- `.gitignore` - Added user data and uploads exclusions

### 🎯 **Issues Resolved**

**Before Fix:**
- Defeated enemies remained in `comba
