# PR #5721: Main PR: Centralize automation marker workflows and harden Codex/fix-comment preflight (E2E validation)

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-02-23
**Author:** jleechan2015
**Stats:** +378/-97 in 15 files

## Summary
(none)

## Raw Body
## Goals

- Make automation commit/marker handling consistent across Codex and fix-comment/fixpr workflows.
- Ensure marker parsing is centralized and backward-compatible with existing `fixcomment`/legacy marker shapes.
- Harden remote automation preflight validation and make failures diagnosable.
- Preserve safe dispatch semantics for fix-comment vs fixpr job modes (no implicit flow override).
- Track and document PR scope and impacted areas clearly against `origin/main`.

## Full set of changes vs `origin/main`

### 1) Centralized marker configuration (`automation/jleechanorg_pr_automation/codex_config.py`)
- Added `codex-api` to workflow names used for central marker detection.
- Added/used centralized `build_automation_commit_marker(workflow, actor)` helper.
- Extended marker docs and allowed workflow set for automated detection/parsing.

### 2) PR monitor marker handling (`automation/jleechanorg_pr_automation/jleechanorg_pr_monitor.py`)
- Switched PR monitor commit marker constant to `build_automation_commit_marker("codex-api")`.
- Updated fix-comment marker generation to use helper-based centralized format.
- Kept actor-aware marker support and helper-based message matching for all workflows.
- Updated internal guidance used by automation for commit message markers in responses.

### 3) Codex API/Cloud task automation (`automation/jleechanorg_pr_automation/openai_automation/codex_cli_tasks.py`)
- Updated remote apply/create commit messages to emit centralized `codex-api` marker.
- Ensures codex-api remote commit/PR-update messages are in the repository-wide marker format.

### 4) Fixpr orchestration flow (`automation/jleechanorg_pr_automation/orchestrated_pr_runner.py`)
- Uses centralized marker builder for conflict/final fixpr commit markers.
- Keeps slash-command dispatch confined to fixpr mode; preserves custom task description behavior in fix-comment mode.

### 5) Validation hardening (`orchestration/cli_validation.py`, `orchestration/task_dispatcher.py`)

