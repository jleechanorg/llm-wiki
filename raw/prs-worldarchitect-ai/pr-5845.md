# PR #5845: fix(codex-update): use headed off-screen Chrome to bypass Cloudflare bot detection

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-03-04
**Author:** jleechan2015
**Stats:** +139/-5 in 5 files

## Summary
- Headless Playwright is blocked by Cloudflare even with `playwright-stealth` — title stays "Just a moment..." indefinitely
- Switch to headed Chrome with `--window-position=-32000,-32000` (off-screen, invisible to user) which passes Cloudflare checks
- Apply `playwright_stealth` patches for additional bot evasion hardening

## Raw Body
## Summary

- Headless Playwright is blocked by Cloudflare even with `playwright-stealth` — title stays "Just a moment..." indefinitely
- Switch to headed Chrome with `--window-position=-32000,-32000` (off-screen, invisible to user) which passes Cloudflare checks
- Apply `playwright_stealth` patches for additional bot evasion hardening

## Production Code Changes

**Before**: `--headless` flag passed to `codex_github_mentions.py` subprocess; no launch args
**Now**: No `--headless` flag; headed Chrome launched with `--window-position=-32000,-32000` + stealth patches
**Why**: Cloudflare bot detection blocks headless Playwright regardless of stealth; headed mode with off-screen positioning is functionally invisible to users but passes Cloudflare

## Test Changes

Manually verified: title shows "Codex" (not "Just a moment..."), "✅ Already logged in to OpenAI", "✅ Update complete! Processed 0 task(s)"

## Known Limitations

- Off-screen window opens a real Chrome process (minor resource overhead vs headless)
- Requires valid `~/.chatgpt_codex_auth_state.json` (refresh periodically)

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Changes the Codex browser automation runtime mode (headed vs headless) and upgrades `playwright-stealth`, which could affect reliability/compatibility across different environments and CI runners.
> 
> **Overview**
> Codex update automation now launches Chromium **headed** (instead of `--headless`) and pushes the window off-screen via `--window-position=-32000,-32000` to avoid Cloudflare blocking.
> 
> It also applies `playwright_stealth` page patches during setup and bumps dependency versions (`playwright-stealth` to `>=2.0.0`, `jleechanorg-pr-automation`/`jleechanorg-orchestration` patch versions).
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 3b57cd2b68142f7d5a82c156b14f2dfbe942889f. This will update automatically on new commi
