# PR #5632: Testing UI: narrow OpenClaw settings exception handling

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-02-19
**Author:** jleechan2015
**Stats:** +1/-4 in 1 files
**Labels:** codex

## Summary
(none)

## Raw Body
### Motivation
- Prevent the test harness from swallowing unexpected failures during `tools_call("update_user_settings", ...)` by avoiding a broad `except Exception` that masks real errors.

### Description
- Tightened `configure_openclaw_settings()` in `testing_ui/test_openclaw_settings_browser.py` to catch only `RuntimeError`, `ValueError`, and `KeyError` and removed the catch-all `except Exception` so unexpected errors surface.

### Testing
- Compiled the modified test file with `python -m compileall -q testing_ui/test_openclaw_settings_browser.py` which succeeded.
- Attempted to run the focused pytest selection (`pytest -q mvp_site/tests/test_openclaw_provider.py mvp_site/tests/test_llm_provider_selection.py mvp_site/tests/test_settings_validation.py`) but it failed early due to missing environment dependency `flask` (`ModuleNotFoundError: No module named 'flask'`).
- Changes were committed on branch `work` and a follow-up PR titled `Follow-up: tighten OpenClaw settings browser error handling` was created.

------
[Codex Task](https://chatgpt.com/codex/tasks/task_e_69963878b918832fb167ed5f722bd4bc)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Test-only change that adjusts exception handling; main risk is making previously-hidden test failures surface and fail runs.
> 
> **Overview**
> Tightens error handling in `OpenClawBrowserTest.configure_openclaw_settings()` by expanding the handled failures to include `KeyError` and **removing the catch-all `except Exception`**, so unexpected exceptions during `tools_call("update_user_settings", ...)` surface instead of being swallowed.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit eb4d13ec29f5a4dc3711a7927e3eda6f0c574350. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
