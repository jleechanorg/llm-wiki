# PR #2687: feat(memory): Add token budget and deduplication for core memories

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2025-12-28
**Author:** jleechan2015
**Stats:** +971/-11 in 5 files

## Summary
Implements token budget and deduplication for core memories to prevent context overflow in long-running campaigns.

### Problem
- Core memories grow unbounded (800+ observed in production campaigns)
- ALL memories sent to LLM every turn, consuming entire context window
- Near-duplicate memories waste tokens
- Previous 180-char truncation was too aggressive

### Solution

| Feature | Implementation | Benefit |
|---------|----------------|---------|
| Token budget | `select_memories_by_budget()` -

## Test Plan
- [x] All 23 new memory_utils tests pass
- [x] All 6 existing preventive_guards tests pass
- [ ] Run full test suite
- [ ] Manual test with large campaign

## Raw Body
## Summary

Implements token budget and deduplication for core memories to prevent context overflow in long-running campaigns.

### Problem
- Core memories grow unbounded (800+ observed in production campaigns)
- ALL memories sent to LLM every turn, consuming entire context window
- Near-duplicate memories waste tokens
- Previous 180-char truncation was too aggressive

### Solution

| Feature | Implementation | Benefit |
|---------|----------------|---------|
| Token budget | `select_memories_by_budget()` - prioritizes recent memories | Caps memory tokens at 20K |
| Deduplication | `is_duplicate_memory()` - 0.85 similarity threshold | Prevents near-duplicates |
| Truncation fix | 180 → 500 chars | Preserves plot details |

### Architecture

New centralized `memory_utils.py` module containing:
- `select_memories_by_budget()` - Token-aware memory selection
- `is_duplicate_memory()` / `is_similar_memory()` - Fuzzy matching
- `format_memories_for_prompt()` - Consistent formatting
- All memory-related constants

### Files Changed

- `mvp_site/memory_utils.py` - New centralized module
- `mvp_site/preventive_guards.py` - Uses centralized functions
- `mvp_site/llm_service.py` - Applies token budget
- `mvp_site/tests/test_memory_utils.py` - 23 comprehensive tests

## Test plan

- [x] All 23 new memory_utils tests pass
- [x] All 6 existing preventive_guards tests pass
- [ ] Run full test suite
- [ ] Manual test with large campaign

## Related

- PR #2686: Original 180→500 char fix (can be closed after this merges)
- Bead `worktree_worker3-1tg`: Future RAG implementation

Generated with Claude Code

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Core-memory management centralized and bounded**
> 
> - New `mvp_site/memory_utils.py`: token-budgeted selection (`select_memories_by_budget`), fuzzy duplicate detection (`is_duplicate_memory`/`is_similar_memory`), prompt formatting, and constants (e.g., 500-char `[auto]` cap)
> - `llm_service.py`: applies budgeted selection to `core_memor
