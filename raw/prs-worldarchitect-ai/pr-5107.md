# PR #5107: Hotfix: restore raw_limit variable for raw LLM logging

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-02-09
**Author:** jleechan2015
**Stats:** +2/-0 in 1 files

## Summary
Fix regression introduced by merged PR #5106 where `raw_limit` was removed but still referenced in `_log_raw_llm_data(...)` calls.

## Raw Body
## Summary
Fix regression introduced by merged PR #5106 where `raw_limit` was removed but still referenced in `_log_raw_llm_data(...)` calls.

## Changes
- Restore `raw_limit = _get_raw_limit()` in both LLM response processing flows before calling `_log_raw_llm_data`.

## Why
Without this, `create_campaign`/`process_action` can fail with:
- `name 'raw_limit' is not defined`

## Validation
- `python3 -m py_compile mvp_site/llm_service.py`

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Small hotfix that restores a missing local variable used only for debug/raw logging; minimal behavior change beyond preventing runtime errors when raw capture is enabled.
> 
> **Overview**
> Restores `raw_limit = _get_raw_limit()` in both the initial story and continuation response processing flows so `_log_raw_llm_data(...)` has the required limit value.
> 
> Prevents `NameError: raw_limit is not defined` regressions that could break `get_initial_story`/`continue_story` when raw LLM capture logging is enabled.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 7ebc54e773456e448216c489cad3672db099ec94. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
