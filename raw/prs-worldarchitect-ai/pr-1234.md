# PR #1234: feat: Add --continue flag and branch-specific commits to schedule_branch_work.sh

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2025-08-09
**Author:** jleechan2015
**Stats:** +100/-54 in 3 files

## Summary
- Add `--continue` flag support to resume previous Claude conversations instead of starting fresh
- Fix commit history to show branch-specific commits (`origin/main..branch`) instead of all commits  
- Update argument parsing to handle the optional `--continue` flag
- Update usage documentation to reflect new option

## Raw Body
## Summary
- Add `--continue` flag support to resume previous Claude conversations instead of starting fresh
- Fix commit history to show branch-specific commits (`origin/main..branch`) instead of all commits  
- Update argument parsing to handle the optional `--continue` flag
- Update usage documentation to reflect new option

## Changes Made
1. **--continue Flag Support**: Added logic to detect `--continue` flag and pass it to Claude command
2. **Improved Argument Parsing**: Enhanced parsing to handle optional branch name and `--continue` flag in any order
3. **Branch-Specific Commit History**: Changed from `git log --oneline -3 --first-parent` to `git log --oneline -3 origin/main.."$REMOTE_BRANCH"` to show only commits on the current branch
4. **Updated Usage**: Enhanced help text to document the new `--continue` option

## Usage Examples
```bash
# Schedule fresh conversation at 2:00 PM
./schedule_branch_work.sh 14:00

# Schedule resumed conversation at 2:00 PM  
./schedule_branch_work.sh 14:00 --continue

# Schedule resumed conversation for specific branch
./schedule_branch_work.sh 14:00 my-feature-branch --continue
```

## Benefits
- **Conversation Continuity**: Users can now resume previous conversations instead of losing context
- **Better Branch Context**: Commit history now shows relevant branch work instead of potentially unrelated commits
- **Flexible Usage**: `--continue` flag can be used with or without explicit branch specification

## Testing
- ✅ Syntax validation passed
- ✅ Usage message updated correctly  
- ✅ Argument parsing handles all combinations
- ✅ Commit history shows branch-specific commits only

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->
## Summary by CodeRabbit

* **New Features**
  * Enhanced script to support flexible argument order and optional continuation of previous conversations.
  * Improved detection and handling of the default branch and recent commits for more accurate context.

* **Bug Fixes**
 
