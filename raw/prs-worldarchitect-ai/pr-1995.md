# PR #1995: fix: map 'prod' and 'production' to 'stable' environment in deploy script

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2025-11-11
**Author:** jleechan2015
**Stats:** +27/-10 in 2 files

## Summary
Fixed the deploy script to map both 'prod' and 'production' arguments to the 'stable' environment, ensuring consistent production deployments.

## Raw Body
## Summary

Fixed the deploy script to map both 'prod' and 'production' arguments to the 'stable' environment, ensuring consistent production deployments.

## Changes

- Updated argument parsing in three locations to accept 'prod', 'production', and 'stable' as synonyms
- All three now deploy to the 'stable' environment
- Updated comments to clarify the mapping

## Testing

✅ Successfully deployed mvp-site-app-stable to production using `../deploy.sh prod`
- Service URL: https://mvp-site-app-stable-114133832173.us-central1.run.app
- Build completed successfully
- Deployment verified

## Usage

Now you can use any of these commands interchangeably:
- `./deploy.sh stable`
- `./deploy.sh prod`
- `./deploy.sh production`

All will deploy to the 'stable' environment.

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Implements route-specific API rate limits with static/frontend exemptions and updates deploy.sh to treat 'prod'/'production' as 'stable'.
> 
> - **Backend/API (Flask)**:
>   - **Rate Limiting**:
>     - Remove default limits; configure `Limiter` with no `default_limits`.
>     - Add per-route limits to `GET/POST/PATCH /api/campaigns*`, `POST /api/campaigns/<id>/interaction`, `GET /api/campaigns/<id>/export`, `GET /api/time`, `GET/POST /api/settings`, and legacy `/handle_interaction`.
>     - Exempt static/frontend routes: `/frontend_v1/*`, `/static/*` redirect, and SPA fallback (`/`, `/<path>`).
> - **DevOps**:
>   - **deploy.sh**: Accept `prod`/`production` as synonyms for `stable` in all entry paths (current dir, explicit dir, interactive selection).
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 395466d65e171b06d7f481437b75075f5a60f327. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->

## Summary by CodeRabbit

* **Chores**
  * Expanded environment detection to 
