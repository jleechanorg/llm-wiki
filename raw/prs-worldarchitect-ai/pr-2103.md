# PR #2103: Address service DI and validation feedback

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2025-11-24
**Author:** jleechan2015
**Stats:** +208/-90 in 6 files
**Labels:** codex

## Summary
- use injected modules across services, add thread-safe/resettable singletons, and guard async runners against pydantic validation errors
- align user settings validation with world_logic schema and translate ai_model/debug_mode to expected payloads
- expand tests for injected world_logic usage, singleton reset, and updated async helper docs/imports

## Raw Body
## Summary
- use injected modules across services, add thread-safe/resettable singletons, and guard async runners against pydantic validation errors
- align user settings validation with world_logic schema and translate ai_model/debug_mode to expected payloads
- expand tests for injected world_logic usage, singleton reset, and updated async helper docs/imports

## Testing
- pytest mvp_site/tests/test_user_service.py mvp_site/tests/test_worldai_service.py

------
[Codex Task](https://chatgpt.com/codex/tasks/task_e_6924b2ad7dc0832fbec4b8573548c6d9)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Switch services to injected modules with thread-safe/resettable singletons, harden async error handling, and align user settings schema; update tests accordingly.
> 
> - **Services**:
>   - Use `self._run_async(lambda: self.world_logic.*)` instead of direct calls; add `world_logic_module` DI and propagate injected deps to `world_logic`.
>   - Add thread-safe singleton pattern with `Lock`, `get_service(**kwargs)`, and `reset_service()` in `worldai_service` and `user_service`.
>   - Catch `PydanticValidationError` in `_run_async` and map to `ValidationError` (applied across methods, e.g., `create_campaign`).
> - **User Settings**:
>   - Validate only `ai_model`, `gemini_model`, `debug_mode`; reject unknown keys.
>   - Normalize to `{'gemini_model': ..., 'debug_mode': ...}` before calling `world_logic`.
> - **Package**:
>   - Fix relative import in `services/__init__.py`.
> - **Tests**:
>   - Replace with `AsyncMock` where appropriate; verify injected `world_logic` usage and settings normalization.
>   - Add tests for singleton reset and DI; minor assertions updated to new schema.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 50cb471b370009f9aca137b0cce63c53e52a2e14. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
