# PR #5048: Fix: Self-hosted runner compatibility (skip Linux-specific setup on macOS)

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-02-08
**Author:** jleechan2015
**Stats:** +13/-8 in 3 files

## Summary
Fix self-hosted macOS runner compatibility by conditionally skipping Linux-specific setup steps (Python installation and apt-get commands).

**Key themes:**
- OS-aware workflow steps for cross-platform runner support
- Self-hosted runner cost optimization (macOS runners are free, use existing pyenv Python)
- Proper fallback logic when self-hosted unavailable

## Raw Body
## Summary

Fix self-hosted macOS runner compatibility by conditionally skipping Linux-specific setup steps (Python installation and apt-get commands).

**Key themes:**
- OS-aware workflow steps for cross-platform runner support
- Self-hosted runner cost optimization (macOS runners are free, use existing pyenv Python)
- Proper fallback logic when self-hosted unavailable

## Production Code Changes

### 1. `.github/workflows/mcp-smoke-tests.yml` (+1/-0 lines)
**Purpose:** MCP smoke test workflow with self-hosted runner fallback

- **Added Python setup condition** - Previously installed Python unconditionally → Now checks `runner.os == 'Linux'` → Self-hosted macOS runner has Python 3.11 via pyenv, GitHub-hosted Linux needs installation
  - Line 63: `if: runner.os == 'Linux'` added to `Set up Python` step

### 2. `.github/workflows/hook-tests.yml` (+3/-1 lines)
**Purpose:** Claude Code hooks test workflow with self-hosted runner fallback

- **Added Python setup condition** - Previously installed Python unconditionally → Now checks `runner.os == 'Linux'` → Self-hosted macOS runner has Python 3.11 via pyenv, GitHub-hosted Linux needs installation
  - Line 43: `if: runner.os == 'Linux'` added to `Set up Python 3.11` step

- **Added jq installation condition** - Previously ran `sudo apt-get install -y jq` unconditionally → Now checks `runner.os == 'Linux'` → Prevents sudo password error and apt-get unavailability on macOS
  - Line 49: `if: runner.os == 'Linux'` added to `Install jq (for hook tests)` step

### 3. `.github/workflows/doc-size-check.yml` (+1/-0 lines)
**Purpose:** Documentation size validation workflow with self-hosted runner fallback

- **Added Python setup condition** - Previously installed Python unconditionally → Now checks `runner.os == 'Linux'` → Self-hosted macOS runner has Python 3.11 via pyenv, GitHub-hosted Linux needs installation
  - Line 46: `if: runner.os == 'Linux'` added to `Set up Python` step

## Test Changes

No test files modified. Validate
