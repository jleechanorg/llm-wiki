# PR #5749: fix(ci): validate cached venv pip wrapper before reuse

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-02-24
**Author:** jleechan2015
**Stats:** +0/-0 in 0 files

## Summary
- Fix CI venv cache validation to properly check pip executable before reuse
- Uses direct pip path instead of python -m pip to avoid module import issues
- Adds timeout to prevent hanging on broken cached venv

## Raw Body
## Summary
- Fix CI venv cache validation to properly check pip executable before reuse
- Uses direct pip path instead of python -m pip to avoid module import issues
- Adds timeout to prevent hanging on broken cached venv

## Production Code Changes
- **Before**: Used `venv/bin/python -m pip --version` which could fail if pip module has issues
- **After**: Uses `venv/bin/pip --version` with explicit executable check and timeout

## Test Changes
- No test changes needed - this is a CI infrastructure fix

## Known Limitations
- None

🤖 Generated with [Claude Code](https://claude.ai/code) via MiniMax API

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> CI-only workflow tweak that tightens cached venv validation; low risk beyond potentially increasing cache misses or job time if checks are too strict.
> 
> **Overview**
> Improves GitHub Actions venv cache reuse by validating both `venv/bin/python` and the `venv/bin/pip` executable before reusing a restored environment.
> 
> The workflow now runs `pip --version` (via the wrapper, not `python -m pip`) under a short `timeout`, and falls back to deleting and recreating the venv when validation fails to avoid hangs or broken cached environments.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 8cbb3819a5873db89df3a495013522af146c6192. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->

## Summary by CodeRabbit

* **Chores**
  * Enhanced CI/CD pipeline reliability through improved virtual environment validation. Now includes executable binary verification and timeout-protected version confirmation checks to ensure cached environments are properly configured and usable before reuse, with automatic fallback to creating fresh environments if validation fails.

<!-- end of auto-generated comment: release notes by coderabbit
