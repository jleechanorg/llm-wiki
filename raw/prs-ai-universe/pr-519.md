# PR #519: fix: prefer port 2000 when running local server

**Repo:** jleechanorg/ai_universe
**Merged:** 2025-11-03
**Author:** jleechan2015
**Stats:** +210/-52 in 2 files
**Labels:** codex

## Summary
- update run_local_server.sh to probe port 2000 before launching and set environment hints when it is unavailable
- allow the run-local-server TypeScript helper to honor preferred or minimum port overrides when selecting a port

## Raw Body
## Summary
- update run_local_server.sh to probe port 2000 before launching and set environment hints when it is unavailable
- allow the run-local-server TypeScript helper to honor preferred or minimum port overrides when selecting a port

## Testing
- npm run test:run-local-server
- ./run_local_server.sh (terminated after verifying startup)

------
https://chatgpt.com/codex/tasks/task_e_6907b6a1db40832f9b0c8d45cc22a0c8

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Prefer port 2000 with env-based overrides and conflict retries for the local server, plus graceful shutdown handling in the backend server.
> 
> - **Local Dev Runner (`scripts/run-local-server.ts`)**:
>   - **Port selection**: Prefer `2000` by default; support `RUN_LOCAL_SERVER_PREFERRED_PORT`, `RUN_LOCAL_SERVER_MIN_PORT`, `RUN_LOCAL_SERVER_MAX_PORT`; improved fallback range; `isPortAvailable` now binds to `0.0.0.0`.
>   - **Startup robustness**: Detect `EADDRINUSE` via piped output and retry up to 3 times with new ports; enhanced startup logs; maintains watch-mode behavior.
> - **Backend Server (`backend/src/server.ts`)**:
>   - **Graceful shutdown**: Handle `SIGINT`/`SIGTERM`, close Express server cleanly, force-exit after 5s timeout if needed.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit cc61be7128aab590405c0dd6a594e4b990f3caf3. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->
## Summary by CodeRabbit

* **New Features**
  * Clearer startup output showing server bind address and retry attempts.
* **Bug Fixes**
  * Server now performs graceful shutdown on termination signals to avoid abrupt exits.
  * Automatic retry logic for port conflicts during startup to improve reliability.
  * Improved port validation and a more sensible default port selection.
<!-- end of auto-generated comment: release notes by
