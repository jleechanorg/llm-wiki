# PR #224: feat(python-mcp-server-utils): align utilities with ts parity

**Repo:** jleechanorg/ai_universe
**Merged:** 2025-10-07
**Author:** jleechan2015
**Stats:** +418/-4 in 5 files
**Labels:** codex

## Summary
- allow the FastMCP HTTP proxy helper to accept mapping-style CORS configuration and tolerate partial loggers while keeping parity with the TypeScript implementation
- ensure the server factory relies on inspect for awaitable tool registration to mirror runtime expectations
- add pytest coverage for the HTTP proxy, server factory, and tool helper utilities to lock in behaviour

## Raw Body
## Summary
- allow the FastMCP HTTP proxy helper to accept mapping-style CORS configuration and tolerate partial loggers while keeping parity with the TypeScript implementation
- ensure the server factory relies on inspect for awaitable tool registration to mirror runtime expectations
- add pytest coverage for the HTTP proxy, server factory, and tool helper utilities to lock in behaviour

## Testing
- pytest packages/python-mcp-server-utils/tests -q

------
https://chatgpt.com/codex/tasks/task_e_68e4dd335d08832fae0e7b8ed289d110

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Expand HTTP proxy CORS config and resilient logger handling; add comprehensive tests and minor typing cleanup.
> 
> - **HTTP proxy (`mcp_server_utils/http_proxy.py`)**:
>   - Accept `cors_options` as `WildcardCORSOptions` or mapping; pass to `CORSMiddleware` via dict.
>   - Make `_call_logger` robust using `inspect` to handle positional/keyword variants (`error`, `exc_info`), format extras, and not mask logger `TypeError`s.
>   - Minor: add `LoggerLike` docstring.
> - **Server factory (`mcp_server_utils/server_factory.py`)**:
>   - Typing cleanup (remove unused `Iterable`).
> - **Tests**:
>   - Add pytest coverage for HTTP proxy (CORS mapping, wildcard, logger behaviors), server factory (tool/agent/on_initialize registration, health-check tool), and tool helpers (echo/status/version, error handling, logging).
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 08de62b1c0ffabf58229a46379f93e82a0d0455f. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->

## Summary by CodeRabbit

- New Features
  - HTTP proxy now accepts flexible CORS configuration, supporting both wildcard options and generic mappings.
- Bug Fixes
  - Improved logging behavior to handle various logger signatures and exception reporting more
