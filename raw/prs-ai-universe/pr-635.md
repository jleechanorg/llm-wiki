# PR #635: Add email notifications for auto-deploy failures

**Repo:** jleechanorg/ai_universe
**Merged:** 2025-11-11
**Author:** jleechan2015
**Stats:** +66/-0 in 1 files

## Summary
Adds email notifications for auto-deploy failures by creating GitHub issues when deployments fail.

## Raw Body
## Summary

Adds email notifications for auto-deploy failures by creating GitHub issues when deployments fail.

## Changes

- Added `issues: write` permission to workflow
- Added failure notification step that creates GitHub issue
- Issue includes workflow URL, commit details, and troubleshooting steps
- Uses GitHub's built-in notification system (no SMTP required)

## How It Works

1. **On Failure**: Workflow creates a GitHub issue with failure details
2. **Email Trigger**: GitHub sends email notification if:
   - Email notifications are enabled in GitHub settings
   - You're watching the repository or issues are assigned to you
3. **Issue Content**: Includes workflow URL, commit details, and next steps

## Setup Required

To receive emails, ensure GitHub email notifications are enabled:
- Go to: https://github.com/settings/notifications
- Under "Email notifications", ensure "Issues" is checked
- Verify your email address is correct

## Testing

This will be tested automatically on the next deployment failure.

## Related

- Frontend PR: #185 (already merged)
- Uses same pattern as frontend workflow

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Adds a failure handler to the backend auto-deploy workflow that opens a GitHub issue with run/commit details and enables required permissions.
> 
> - **CI/CD (GitHub Actions)**: `/.github/workflows/auto-deploy.yml`
>   - **Permissions**: Add `permissions` block with `contents: read` and `issues: write`.
>   - **Failure Notifications**: New `Notify on failure` step that:
>     - Appends a concise failure summary to `GITHUB_STEP_SUMMARY`.
>     - Creates a GitHub issue containing workflow/commit metadata and next steps, with safe-escaped commit message.
>     - Avoids duplicates by checking for an existing open issue containing the commit SHA in the title.
>     - Uses `GH_TOKEN` for `gh` CLI authentication.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 9f48fd23cd85823a238ace0556efd9ca9b8
