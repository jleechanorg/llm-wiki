# PR #369: fix: guard auto resolver gh args expansion

**Repo:** jleechanorg/ai_universe
**Merged:** 2025-10-16
**Author:** jleechan2015
**Stats:** +6/-2 in 1 files
**Labels:** codex

## Summary
- guard the GitHub CLI metadata lookups against empty repo argument expansion so the auto resolver stays compatible with `set -u`
- preserve the existing repository-aware behavior when `GITHUB_REPOSITORY` is available

## Raw Body
## Summary
- guard the GitHub CLI metadata lookups against empty repo argument expansion so the auto resolver stays compatible with `set -u`
- preserve the existing repository-aware behavior when `GITHUB_REPOSITORY` is available

## Testing
- bash -n scripts/auto_resolve_conflicts.sh

------
https://chatgpt.com/codex/tasks/task_e_68f0922571f8832fa456eac007a7df07

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Guard empty --repo arg expansion and fetch head/base branches in a single gh call in `scripts/auto_resolve_conflicts.sh`.
> 
> - **Scripts**:
>   - **`scripts/auto_resolve_conflicts.sh`**:
>     - Guard GitHub CLI `--repo` arg expansion by introducing `GH_VIEW_ARGS` to avoid empty-array issues.
>     - Consolidate PR metadata retrieval into one `gh pr view` call (fetching `headRefName` and `baseRefName`) and parse via TSV/IFS.
>     - Preserves repository-aware behavior when `GITHUB_REPOSITORY` is set and remains compatible with `set -u`.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 121db5a3b37715f53b14924db50da54a01dd376f. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->
## Summary by CodeRabbit

* **Chores**
  * Streamlined background conflict-resolution automation by consolidating branch metadata retrieval into a single step.
  * Centralized parsing for branch names reduces redundant external calls and improves reliability.
  * No visible behavior changes; outcomes remain the same with more efficient and robust background processing.
<!-- end of auto-generated comment: release notes by coderabbit.ai -->
