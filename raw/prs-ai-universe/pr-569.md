# PR #569: Create systematic fix without hardcoding

**Repo:** jleechanorg/ai_universe
**Merged:** 2025-11-06
**Author:** jleechan2015
**Stats:** +19/-98 in 2 files

## Summary
(none)

## Raw Body
Ensures shared-libs packages are always built and up-to-date by running `npm run prepare:shared-libs` before:
- Starting the local development server (run_local_server.sh)
- Deploying to Cloud Run (deploy.sh)

Benefits:
- Prevents "export not found" errors from stale or missing builds
- Ensures consistent behavior across development and deployment
- Replaces manual package iteration in deploy.sh with systematic approach
- Eliminates need to manually rebuild shared-libs after pulling changes

This fix addresses the root cause of missing exports like APPROX_CHARS_PER_TOKEN by ensuring the build infrastructure runs automatically.

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Replaces per-package shared-libs build logic with a single `backend/npm run prepare:shared-libs` call in deploy and local server scripts for consistent, prebuilt packages.
> 
> - **Build tooling**
>   - `deploy.sh`: Replace manual shared-libs iteration with `cd backend && npm run prepare:shared-libs`; keep subsequent backend build and Cloud Build staging as-is.
>   - `scripts/run-local-server.ts`: Refactor `buildSharedLibs` to invoke `npm run prepare:shared-libs` in `backend/`; remove per-package install/build logic and add error/status handling.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 15f438b4573f6bd21cc12cb44990b88500b64aad. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->

## Summary by CodeRabbit

* **Chores**
  * Consolidated shared library build process from multi-package approach to a centralized step
  * Added automatic library preparation before local server startup across all execution paths
  * Streamlined deployment workflow by simplifying the build process

<!-- end of auto-generated comment: release notes by coderabbit.ai -->
