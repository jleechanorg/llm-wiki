# PR #691: ci: share backend dev workflow

**Repo:** jleechanorg/ai_universe
**Merged:** 2025-11-13
**Author:** jleechan2015
**Stats:** +209/-143 in 2 files

## Summary
- move the dev deployment job into deploy-dev.yml so manual dispatches and auto-deploy reuse the same steps
- capture commit metadata inside the reusable workflow and keep health checks + failure issue logic intact

## Raw Body
## Summary
- move the dev deployment job into deploy-dev.yml so manual dispatches and auto-deploy reuse the same steps
- capture commit metadata inside the reusable workflow and keep health checks + failure issue logic intact

## Testing
- not run (workflow-only change)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Refactors backend dev deployment into a reusable workflow and updates auto-deploy to call it with inputs and inherited secrets.
> 
> - **CI/CD Workflows**:
>   - **New reusable workflow** `/.github/workflows/deploy-dev.yml`:
>     - Supports `workflow_dispatch` and `workflow_call` with `reason` and `ref_name` inputs.
>     - Deploys to Cloud Run using env-configured `GCP_PROJECT`, `GCP_REGION`, `SERVICE_NAME`.
>     - Captures commit metadata, performs health check, writes deployment summary, and opens a failure issue on errors.
>   - **Auto-deploy update** `/.github/workflows/auto-deploy.yml`:
>     - Replaced inline steps with `uses: ./.github/workflows/deploy-dev.yml`.
>     - Passes `reason` and `ref_name`; `secrets: inherit`; retains necessary permissions.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 12b8d83dafa084cad1668456b11726ec0df4baab. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
