# PR #293: fix: checkout PR head commit for claude fixpr workflow

**Repo:** jleechanorg/ai_universe
**Merged:** 2025-10-11
**Author:** jleechan2015
**Stats:** +1/-1 in 1 files
**Labels:** codex

## Summary
- update the FixPR workflow checkout step to pull the pull request head commit directly
- ensure the workflow still runs when GitHub cannot create the merge ref due to conflicts
- allow manual FixPR runs to honor the requested branch when checking out code

## Raw Body
## Summary
- update the FixPR workflow checkout step to pull the pull request head commit directly
- ensure the workflow still runs when GitHub cannot create the merge ref due to conflicts
- allow manual FixPR runs to honor the requested branch when checking out code

## Testing
- not run (workflow change only)

------
https://chatgpt.com/codex/tasks/task_e_68ead19b9f10832fbc52003719edaa99

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Update checkout ref to prefer PR head, support workflow input target-ref, and add fallbacks to head_ref/ref_name for reliability.
> 
> - **Workflow (.github/workflows/claude-copilot.yml)**:
>   - **Checkout step**: Expand `ref` resolution to `${{ github.event.pull_request.head.sha || github.event.inputs['target-ref'] || github.head_ref || github.ref_name || github.sha }}`.
>     - Supports manual `workflow_dispatch` via `target-ref`.
>     - Adds fallbacks (`head_ref`, `ref_name`) to handle missing merge refs and ensure reliable checkout.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 7afaaff509d9d911a25e490a65d48d4f470983f5. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
