# PR #887: refactor: rename usedFallback to hasError for clarity

**Repo:** jleechanorg/ai_universe
**Merged:** 2025-12-03
**Author:** jleechan2015
**Stats:** +203/-80 in 18 files

## Summary
- Renamed `usedFallback` to `hasError` as the primary field for LLM failure detection
- `usedFallback` is maintained as a deprecated alias for backward compatibility
- Fixed API response to actually return `hasError` at top level (was missing from return statements)
- Updated all test assertions to expect the new `hasError` field

## Test Plan
- [x] All 55 unit test suites pass (520 tests)
- [x] Live API test confirms `hasError: false` at top level
- [x] Type checking passes
- [x] ESLint validation passes

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Propagates a new hasError flag across services and libraries (deprecating usedFallback), enhances HTTP clients/metrics, tightens CI smoke tests, and streamlines secret mounting in deployments.
> 
> - **Backend**:
>   - **Conversa

## Raw Body
## Summary
- Renamed `usedFallback` to `hasError` as the primary field for LLM failure detection
- `usedFallback` is maintained as a deprecated alias for backward compatibility
- Fixed API response to actually return `hasError` at top level (was missing from return statements)
- Updated all test assertions to expect the new `hasError` field

## Changes
- `SingleModelChatService.ts`: Internal variable renamed, `normalizeAssistantContent` returns `hasError`
- `ConversationAgent.ts`: Added `hasError` to response type and all return statements
- `normalizeContent.ts` (shared-libs): Returns both `hasError` and `usedFallback`
- Multiple test files updated with `hasError` field expectations

## Test plan
- [x] All 55 unit test suites pass (520 tests)
- [x] Live API test confirms `hasError: false` at top level
- [x] Type checking passes
- [x] ESLint validation passes

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Propagates a new hasError flag across services and libraries (deprecating usedFallback), enhances HTTP clients/metrics, tightens CI smoke tests, and streamlines secret mounting in deployments.
> 
> - **Backend**:
>   - **Conversation flow**: Propagate `hasError` in `ConversationAgent.handleUserSendMessage` and `generateAssistantResponseSafe`; include diagnostics in fallback assistant metadata.
>   - **Chat service**: `SingleModelChatService` switches to `ERROR_ASSISTANT_MESSAGE`, normalizes via `hasError`, sets assistant metadata `{ hasError, usedFallback: hasError }` and web search diagnostics accordingly.
>   - **HTTP Client**: Metrics/logging distinguish success vs error; add `http_outbound_request_errors` counter and warn on non-2xx responses.
> - **Shared libs (convo-agent)**:
>   - **API change**: `normalizeContent` returns `{ content, hasError, usedFallback }`; introduce `DEFAULT_ERROR_MESSAGE` (deprecate fallback names).
>   - **ChatCompletionService**: adds `hasError` in results (keeps `usedFallbac
