# PR #589: Review AGENTS.md documentation and plan next steps

**Repo:** jleechanorg/ai_universe
**Merged:** 2025-11-09
**Author:** jleechan2015
**Stats:** +43/-155 in 2 files

## Summary
(none)

## Raw Body
CRITICAL ADDITIONS from official beads/AGENTS.md:

🚨 Session-End Protocol (MOST CRITICAL):
- Added mandatory `mcp__beads__sync()` before ending sessions
- Prevents data loss by forcing export, commit, and push
- Updated agent workflow to emphasize this requirement

📋 Issue Organization:
- Added complete issue type definitions (bug, feature, task, epic, chore)
- Added explicit priority scale (0=Critical to 4=Backlog)
- Previously only mentioned without definitions

🔍 Discovery & Management:
- Added stale work detection (`mcp__beads__stale()`)
- Added duplicate detection and auto-merge capabilities
- Expanded Core MCP Functions with complete command set

🧪 Testing Best Practices:
- Added BEADS_DB=/tmp/test.db pattern to avoid production pollution
- Added proactive duplicate detection workflow
- Added CLI duplicate auto-merge command

Files updated:
- CLAUDE.md: Added all sections to Beads tracking section
- AGENTS.md: Added matching sections with CLI examples

Reference: https://github.com/steveyegge/beads/blob/main/AGENTS.md

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Condenses Beads docs, adds mandatory `mcp__beads__sync()` step, quick refs (types/priorities), stale/duplicate checks, and testing/CLI notes across AGENTS.md and CLAUDE.md.
> 
> - **Documentation (Beads usage)**
>   - **AGENTS.md**:
>     - Add mandatory session-end `mcp__beads__sync()` to agent workflow and core functions.
>     - Add quick references: issue types/priorities; cross-workspace `workspace_root` override.
>     - Expand core MCP functions: `stale`, `duplicates`, `update/close`, `sync`; add testing pattern `BEADS_DB=/tmp/test.db` and CLI worktree notes.
>     - Streamline automatic discovery guidance and emphasize dependency linking via `discovered-from`.
>   - **CLAUDE.md**:
>     - Mirror Beads quick refs and automation guidance; add concise core MCP function block including `stale`, `duplicates`, and mandatory `sync`.
>     - Introduce explicit session-end protoco
