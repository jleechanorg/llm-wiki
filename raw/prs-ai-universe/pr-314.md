# PR #314: feat: run web search before second opinion calls

**Repo:** jleechanorg/ai_universe
**Merged:** 2025-10-13
**Author:** jleechan2015
**Stats:** +66/-10 in 3 files
**Labels:** codex

## Summary
- add a shared helper in mcp-server-utils to run web search and build prompt context for second opinion flows
- have SecondOpinionAgent call the helper before dispatching to the primary and secondary models, recording telemetry about search results
- cover the helper with unit tests and export it through the shared package entry point
- tighten web search sanitization and align streaming telemetry to avoid duplicate context blocks while capturing stage latency

## Raw Body
## Summary
- add a shared helper in mcp-server-utils to run web search and build prompt context for second opinion flows
- have SecondOpinionAgent call the helper before dispatching to the primary and secondary models, recording telemetry about search results
- cover the helper with unit tests and export it through the shared package entry point
- tighten web search sanitization and align streaming telemetry to avoid duplicate context blocks while capturing stage latency

## Testing
- npm --prefix shared-libs/packages/mcp-server-utils run test -- SecondOpinionSearchContext
- npm --prefix backend run test -- SecondOpinionAgent

------
https://chatgpt.com/codex/tasks/task_e_68ed452d286c832f91d81db8e1649bf8

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Tightens web search result sanitization and maxResults clamping, updates streaming calls to record latency with stage labels, and adjusts prompt generation to avoid duplicating context with updated tests.
> 
> - **Backend (`backend/src/agents/SecondOpinionAgent.ts`)**:
>   - Add `LatencyTracker` integration to streaming `callWithTimeout` calls with stage labels (`primary_response`, `prompt_generation`, `parallel_secondary_responses`, `synthesis_final`).
>   - Update secondary prompt generation to reference an appended web search context block instead of inlining it.
>   - Propagate tracker and stage label through `executeStaggeredRequests` and synthesis calls.
> - **Shared Utils (`shared-libs/.../SecondOpinionSearchContext.ts`)**:
>   - Clamp `maxResults` to `[0,10]` with integer coercion; pass bounded value to `runSearch` and sanitization.
>   - Strengthen result filtering and normalization (require non-empty string URLs; trim fields) before formatting.
> - **Tests (`__tests__/SecondOpinionSearchContext.test.ts`)**:
>   - Update to handle trimmed URLs and add test for `maxResults` clamping and ignoring empty URLs.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit cf538fadb16fefeb3fa523
