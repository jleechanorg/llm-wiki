# PR #378: Enable OpenRouter web search for Cerebras primary calls

**Repo:** jleechanorg/ai_universe
**Merged:** 2025-10-17
**Author:** jleechan2015
**Stats:** +391/-133 in 6 files
**Labels:** codex

## Summary
- eliminate redundant spreads when capturing Cerebras web search diagnostics
- use Node's structuredClone to serialize Cerebras web search diagnostics metadata safely

## Raw Body
## Summary
- eliminate redundant spreads when capturing Cerebras web search diagnostics
- use Node's structuredClone to serialize Cerebras web search diagnostics metadata safely

## Testing
- node --check scripts/mcp-ci-tests.mjs

------
https://chatgpt.com/codex/tasks/task_e_68f09367ede4832fa7d0b45cd428753c

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Adds OpenRouter web search to Cerebras with configurable plugin/options, request payload wiring, and metadata/diagnostics; updates config defaults and tests/CI validations.
> 
> - **Backend**
>   - **`CerebrasLLMTool`**: Adds OpenRouter-only web search integration with configurable `plugin` (`engine`, `maxResults`, `searchPrompt`) and `options` (`searchContextSize`); builds `plugins` and `web_search_options` into requests (streaming and non-streaming); caps results; enriches `metadata` with provider, usage, annotations, and `webSearchDiagnostics`; refactors payload building and initialization.
>   - **Types**: Replace local `AppConfig` with re-export from `@ai-universe/config-utils`; keep `LLMResponse` supporting `metadata`.
>   - **Tests**: Add E2E test verifying Cerebras via OpenRouter sends `plugins`/`web_search_options` and returns diagnostics; minor expectation tweaks.
> - **Shared Config (config-utils)**
>   - **Schema**: Extend `models.cerebras` with `webSearch` (enabled, plugin, options).
>   - **Defaults**: Enable Cerebras web search by default (`engine: exa`, `maxResults: 6`, `searchContextSize: high`).
>   - **Tests**: Update to assert new Cerebras web search defaults.
> - **CI Script**
>   - Enhance MCP smoke tests to validate OpenRouter web search diagnostics and summarize engine/maxResults/context.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit a84b32080835c87c0537aec9b32cb5d3adc0038f. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: relea
