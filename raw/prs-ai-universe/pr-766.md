# PR #766: Relax UUID v4 validation in client library

**Repo:** jleechanorg/ai_universe
**Merged:** 2025-11-20
**Author:** jleechan2015
**Stats:** +522/-110 in 13 files

## Summary
(none)

## Raw Body
<!-- CURSOR_SUMMARY -->
> [!NOTE]
> Client now accepts any non-empty conversationId (trimmed) and uses normalized options when calling the API; adds default secondary-model selection export/mocks, loosens parallel timing tests, and bumps client to 2.1.0.
> 
> - **Client**:
>   - Relax `conversationId` validation to accept any non-empty string; trim and return normalized options from `validateRequestOptions`.
>   - `SecondOpinionClient.ask` now uses the normalized options and auto-fills fingerprint/userAgent on them.
>   - Version bump to `2.1.0`.
> - **Config Utils**:
>   - Export `selectDefaultSecondaryModels` for centralized secondary-model selection.
> - **Backend Tests**:
>   - Mock `selectDefaultSecondaryModels` across agent tests; adjust parallel timing test to allow 20% overhead.
>   - Extend validation and web search diagnostics coverage (Cerebras primary behavior preserved).
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 00c37fca6ccfe82cf73a296ca925ddf0fbf69203. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->
## Summary by CodeRabbit

* **Bug Fixes**
  * Relaxed conversationId validation to accept non-UUID non-empty strings (rejects empty/whitespace-only).

* **Tests**
  * Expanded test coverage for conversationId normalization.
  * Improved test mocks and stability (added deterministic default-secondary-model helpers; parallel test tolerance increased).

* **Chores**
  * validateRequestOptions now returns a normalized options object.
  * Package version bumped to 2.1.0.

<sub>✏️ Tip: You can customize this high-level summary in your review settings.</sub>
<!-- end of auto-generated comment: release notes by coderabbit.ai -->
