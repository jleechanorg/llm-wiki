# PR #329: chore: hash shared libs builds and drop dist artifacts

**Repo:** jleechanorg/ai_universe
**Merged:** 2025-10-14
**Author:** jleechan2015
**Stats:** +421/-96 in 19 files
**Labels:** codex

## Summary
- add content-hash helpers for shared libs (compute script, staging verifier, run-build-if-ready) and switch prepare logic to hash comparison + persisted .src-hash files
- copy shared build scripts into Docker images/deploy contexts so file: dependencies install correctly with prebuilt dist assets
- update backend coverage workflow to rebuild shared libs with fresh npm cache, eliminating stale dist exports behind the Node 22 backend validation failure
- drop previously committed dist artifacts a

## Raw Body
## Summary
- add content-hash helpers for shared libs (compute script, staging verifier, run-build-if-ready) and switch prepare logic to hash comparison + persisted .src-hash files
- copy shared build scripts into Docker images/deploy contexts so file: dependencies install correctly with prebuilt dist assets
- update backend coverage workflow to rebuild shared libs with fresh npm cache, eliminating stale dist exports behind the Node 22 backend validation failure
- drop previously committed dist artifacts and document the new prepare flow/hash requirements

## Testing
- node backend/scripts/prepare-shared-libs.mjs
- docker build --no-cache backend

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Move shared-libs from mtime to content-hash builds with .src-hash, add helper scripts, and update CI, Dockerfile, deploy staging, and docs accordingly.
> 
> - **Shared-libs build system**:
>   - Add `scripts/compute-directory-hash.mjs` and use hash-based staleness checks with persisted `dist/.src-hash`.
>   - Update `backend/scripts/prepare-shared-libs.mjs` to compare content hashes and write `.src-hash`.
>   - Add `shared-libs/scripts/run-build-if-ready.mjs`; wire into packages via `"prepare"` scripts in `auth-utils`, `config-utils`, `firestore-utils`, `mcp-server-utils`.
>   - Add `scripts/shared-build-functions.sh` hash implementation replacing mtime logic.
>   - Add `scripts/verify-shared-libs-staging.mjs` to validate staged shared-libs.
> - **CI/CD**:
>   - Update `.github/workflows/backend-coverage.yml`: clear npm cache, rebuild shared libs; robust base worktree steps with conditionals.
> - **Docker/Deploy**:
>   - Backend `Dockerfile`: copy `/scripts` and shared-libs helpers into images for `file:` deps; include scripts in prod stage.
>   - `deploy.sh`: stage shared-libs build assets and helper scripts; verify staging before Cloud Build.
> - **Repo config & docs**:
>   - `.gitignore`: ignore all `**/dist/`.
>   - Update `CLAUDE.md`, `shared-libs/README.md`, and docs to docume
