# PR #150: test: validate MCP smoke tests return all model responses

**Repo:** jleechanorg/ai_universe
**Merged:** 2025-10-04
**Author:** jleechan2015
**Stats:** +132/-5 in 1 files
**Labels:** codex

## Summary
- parse the MCP smoke test response JSON to inspect primary and secondary model outputs
- enforce configurable expectations for one primary and three secondary model responses before passing the smoketest

## Raw Body
## Summary
- parse the MCP smoke test response JSON to inspect primary and secondary model outputs
- enforce configurable expectations for one primary and three secondary model responses before passing the smoketest

## Testing
- not run (requires deployed MCP service)

------
https://chatgpt.com/codex/tasks/task_e_68e041ae5644832fbb0cc9ff3884cd61

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Parse and validate structured second-opinion JSON, enforcing configurable primary/secondary counts and consistency with summary fields.
> 
> - **scripts/mcp-ci-tests.mjs**:
>   - **Second-opinion validation**:
>     - Parse response JSON; validate presence and count of `primary` entries, non-empty `response`, and no `error` flags.
>     - Validate `secondaryOpinions` contain non-empty responses, count meets expectations, and models are unique.
>     - Cross-check `summary.totalModels` and `summary.successfulResponses` against observed successes.
>   - **Configurability**:
>     - Add `MCP_EXPECTED_PRIMARY_COUNT` (default 1) and `MCP_EXPECTED_SECONDARY_COUNT` (default 3 via non-negative parsing).
>   - **Utilities/return**:
>     - Add `parseNonNegativeInt` helper.
>     - Enhanced logging of model sources and counts; return now includes `parsed` payload.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 85d723e2f628031bc39f0e32de26713762ce30f5. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->
## Summary by CodeRabbit

* New Features
  * Added environment variables to configure expected counts: MCP_EXPECTED_PRIMARY_COUNT (default 1) and MCP_EXPECTED_SECONDARY_COUNT (default 3).
  * Stricter validation of second-opinion responses, including JSON structure checks, primary/secondary content validation, and model/count consistency.
  * Enhanced diagnostics with clearer error messages, JS
