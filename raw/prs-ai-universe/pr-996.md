# PR #996: Set secondary model timeout default to 3.5 minutes

**Repo:** jleechanorg/ai_universe
**Merged:** 2026-02-14
**Author:** jleechan2015
**Stats:** +86/-13 in 8 files

## Summary
- Lowered backend default secondary-model timeout from 10 minutes to 3.5 minutes while keeping primary/prompt/execution timeouts at 10 minutes.
- Updated runtime config comment and endpoint documentation to document secondary timeout behavior.

## Raw Body
## Summary
- Lowered backend default secondary-model timeout from 10 minutes to 3.5 minutes while keeping primary/prompt/execution timeouts at 10 minutes.
- Updated runtime config comment and endpoint documentation to document secondary timeout behavior.

## Files changed
- backend/src/config/llmDefaults.ts
- backend/src/services/RuntimeConfigService.ts
- docs/endpoint-documentation.md

## Validation
- Not run in this session (requested change is configuration-only).

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Behavioral change affects request timeout handling for all secondary model calls and could increase timeout-induced fallbacks in production; otherwise changes are localized to configuration defaults and tests/docs.
> 
> **Overview**
> Updates shared LLM timeout defaults so **secondary models default to 3.5 minutes** while primary/prompt/overall execution remain at 10 minutes, and threads the new `SECONDARY_MODEL_TIMEOUT_MS` through backend `DEFAULT_LLM_TIMEOUTS` fallbacks.
> 
> Adds Jest coverage to ensure backend `llmDefaults` respects shared timeout config and falls back safely when invalid, updates runtime-config expectations, and refreshes endpoint docs/commentary to reflect the new secondary timeout behavior.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 20d7d7512b7c76abb503d66f26e6ee2369f13774. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->
## Summary by CodeRabbit

* **New Features**
  * Added configurable secondary model timeout with 3.5-minute default (previously 10 minutes).

* **Documentation**
  * Updated endpoint documentation for differentiated timeout handling between primary and secondary models.

* **Tests**
  * Added test coverage for LLM timeout defaults and configuration fallback scenarios.
<!-- end of auto-generated co
