# PR #213: fix: enforce conversation MCP auth context

**Repo:** jleechanorg/ai_universe
**Merged:** 2025-10-07
**Author:** jleechan2015
**Stats:** +51/-8 in 3 files
**Labels:** codex

## Summary
- remove the in-memory conversation storage fallback and require a configured remote conversation MCP service
- document the mandatory conversation MCP environment variables and deployment steps for Cloud Run
- add configuration guardrails and tests to ensure the server fails fast when required settings are missing
- ensure ConversationAgent builds MCP auth contexts so MCP tool executions forward required headers
- validate trimmed MCP service user IDs and extract the token estimation constant f

## Raw Body
## Summary
- remove the in-memory conversation storage fallback and require a configured remote conversation MCP service
- document the mandatory conversation MCP environment variables and deployment steps for Cloud Run
- add configuration guardrails and tests to ensure the server fails fast when required settings are missing
- ensure ConversationAgent builds MCP auth contexts so MCP tool executions forward required headers
- validate trimmed MCP service user IDs and extract the token estimation constant for readability

## Testing
- npm --prefix backend run test -- ConversationMCPTool.test.ts

------
https://chatgpt.com/codex/tasks/task_e_68e4c682ba30832fb56468785ee93220

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Propagates MCP auth context from `ConversationAgent`, tightens `ConversationMCPTool` config validation, and extracts a token-estimation constant with corresponding tests.
> 
> - **Backend**:
>   - **`ConversationAgent`**:
>     - Add `createAgentAuthContext` and forward auth context to `conversation.start`, `conversation.add-message`, `conversation.get-history`, and `conversation.list` tool executions.
>   - **`ConversationMCPTool`**:
>     - Validate that trimmed `CONVERSATION_MCP_SERVICE_USER_ID` is non-empty; use `parseInt` for `CONVERSATION_MCP_TIMEOUT_MS`.
>     - Extract `CHARS_PER_TOKEN` and use it in `trimConversationMessages`.
>   - **Tests**:
>     - Add test asserting empty-after-trim `CONVERSATION_MCP_SERVICE_USER_ID` throws.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit ce578bbbfed3b5d73c664f4bc2697848b9fd45e2. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
