# PR #100: fix: Require FIREBASE_PROJECT_ID in all environments

**Repo:** jleechanorg/ai_universe
**Merged:** 2025-09-30
**Author:** jleechan2015
**Stats:** +24/-26 in 4 files

## Summary
(none)

## Raw Body
Production deployment was failing because FIREBASE_PROJECT_ID was missing. Removed demo-project fallback and added FIREBASE_PROJECT_ID to deployment env vars. Both dev and prod now use consistent Firebase configuration.

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Enforces required FIREBASE_PROJECT_ID, sets it in CI/tests and deploys via YAML env file; updates prod CORS domain, consolidates CORS env, and adjusts deploy timeouts.
> 
> - **Backend**:
>   - **Auth**: `FirebaseAuthTool` now requires `FIREBASE_PROJECT_ID` in all environments (no fallback); clearer error message.
> - **CI/Tests**:
>   - Integration workflow sets `FIREBASE_PROJECT_ID: demo-project`.
>   - Test setup provides default `FIREBASE_PROJECT_ID` for CI.
> - **Deployment**:
>   - Use YAML `--env-vars-file` to set envs, including `FIREBASE_PROJECT_ID`, `CORS_ALLOWED_ORIGINS`, and optional `REDIS_URL`; remove separate post-deploy CORS update.
>   - Update prod `CORS_ORIGINS` to `https://ai-universe-frontend*.us-central1.run.app`.
>   - Adjust timeouts: deploy 10m, traffic update 10m.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 8cc3400bbead3a9bfead0bc443daf4be8b4e2f0a. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->
## Summary by CodeRabbit

* **Chores**
  * Require FIREBASE_PROJECT_ID from environment (no fallback); clearer error message when missing.
  * Deployment now injects FIREBASE_PROJECT_ID and CORS_ALLOWED_ORIGINS into environment variables for consistent configuration.
  * CORS configuration is applied during deployment via ENV_VARS (no separate post-deploy CORS update step).

* **Bug Fixes**
  * Prevented unintended defaults for Firebase project selection, reducing risk of misconfigured authentication.
<!-- end of auto-generated comment: release notes by coderabbit.ai -->
