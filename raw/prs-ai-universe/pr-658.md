# PR #658: Fix monitoring instrumentation and proxy startup regressions

**Repo:** jleechanorg/ai_universe
**Merged:** 2025-11-12
**Author:** jleechan2015
**Stats:** +211/-102 in 18 files
**Labels:** codex

## Summary
- align monitoring helpers with unified status labels, add optional tool labels, and treat token usage as cumulative metrics
- harden Cerebras streaming metrics, CommentReply tool error propagation, and health check success/error counters
- improve proxy startup and PR tooling scripts, restore required secrets, and update documentation/examples

## Raw Body
## Summary
- align monitoring helpers with unified status labels, add optional tool labels, and treat token usage as cumulative metrics
- harden Cerebras streaming metrics, CommentReply tool error propagation, and health check success/error counters
- improve proxy startup and PR tooling scripts, restore required secrets, and update documentation/examples

## Testing
- npm test -- src/__tests__/MonitoringService.test.ts

------
[Codex Task](https://chatgpt.com/codex/tasks/task_e_6914de28fbdc832f8240928d37b48116)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Unifies monitoring to status-based labels with options, strengthens Cerebras/CommentReply instrumentation and proxy startup flow, and improves deployment/metrics scripts and configs.
> 
> - **Observability/Monitoring**:
>   - Switch metrics to `status` label (replacing `success`), add optional `labels` and `logger` to `withToolMonitoring` and propagate from helpers.
>   - Treat `token_usage` as `CUMULATIVE`; add backoff wait during initialization retries.
>   - Update HTTP/tool latency and counters to use unified labels; expose types via `index.ts`.
> - **LLM/Tools**:
>   - Cerebras streaming: validate early, report `status` and `error_type`, include `model_name`, and record `streaming_chunks` robustly.
>   - CommentReply MCP: validate args with thrown errors, wrap in `withToolMonitoring({ logger })`, and return structured failures.
>   - Conversation MCP: pass `{ logger }` to monitored operations.
> - **Server/Proxy**:
>   - Health check records success counter after responding.
>   - `startFastMcpHttpProxy`: serve 502 until MCP ready, improved Express listening/error handling, and smarter port selection/fallback.
> - **DevOps/Scripts**:
>   - Cloud Run deploy mounts `FIREBASE_AUTH_PROJECT_ID` secret; PR test detection honors `DISABLE_SECRET_MANAGER`.
>   - Monitoring dashboard script: temp file usage, error handling, cross-platform open.
>   - Metrics query script: better auth errors and project selection.
> - **Te
