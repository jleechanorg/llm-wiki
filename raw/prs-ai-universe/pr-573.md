# PR #573: feat: Add Beads MCP integration for git-backed issue tracking

**Repo:** jleechanorg/ai_universe
**Merged:** 2025-11-07
**Author:** jleechan2015
**Stats:** +442/-1719 in 12 files

## Summary
Adds Beads issue tracker integration to AI Universe with automatic workspace detection for multi-repo/worktree environments.

**✅ Follows File Justification Protocol** - All documentation integrated into existing CLAUDE.md and AGENTS.md files (no standalone markdown files created).

## Raw Body
## Summary

Adds Beads issue tracker integration to AI Universe with automatic workspace detection for multi-repo/worktree environments.

**✅ Follows File Justification Protocol** - All documentation integrated into existing CLAUDE.md and AGENTS.md files (no standalone markdown files created).

## Changes

### 🔧 MCP Installer Integration
- **scripts/mcp_common.sh** (+70 lines)
  - Added beads MCP server installation (lines 1746-1814)
  - Automatic workspace detection via per-project daemons
  - Environment variables: `BEADS_USE_DAEMON=1`, `BEADS_PATH`
  - Checks for `beads-mcp` command and `bd` CLI availability

### 📚 Documentation (Integrated into Existing Files)
- **AGENTS.md** (+193 lines)
  - Multi-repo/worktree support with automatic workspace detection
  - Automatic issue discovery protocol
  - MCP function reference and examples
  - Agent workflow patterns

- **CLAUDE.md** (+77 lines)
  - Beads tracking section (lines 50-126)
  - Automatic discovery rules (bugs, TODOs, test failures, missing deps)
  - Git worktree handling
  - Session workflow

### 📦 Beads Database
- **.beads/** (initialized)
  - `.gitignore` - Properly configured (ignores DB, tracks JSONL)
  - `beads.left.jsonl` - Git-backed issue storage
  - `issues.jsonl` - Alternative storage format
  - `config.yaml` - Project configuration
  - `metadata.json` - Project metadata

### 🧹 Cleanup
- **scripts/claude_mcp.sh** (-1,719 lines)
  - Removed deprecated duplicate (keep root `./claude_mcp.sh`)

### ⚙️ Git Configuration
- **.gitattributes** (new)
  - Merge strategy for JSONL files (union merge)

## Features

### 🎯 Automatic Workspace Detection
```bash
# Works seamlessly across repos/worktrees
cd ~/project_ai_universe/worktree_beads
mcp__beads__ready()  # → Shows worktree_beads issues

cd ~/other-project
mcp__beads__ready()  # → Shows other-project issues (auto-detected!)
```

### 🤖 Automatic Issue Discovery
- **Bugs encountered (P0)** → Auto-filed with `discovered-from` dependency
- **Explicit TODOs (P
