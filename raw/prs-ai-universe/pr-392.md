# PR #392: fix: open run_local_server in new mac terminal

**Repo:** jleechanorg/ai_universe
**Merged:** 2025-10-19
**Author:** jleechan2015
**Stats:** +70/-0 in 1 files
**Labels:** codex

## Summary
- detect macOS environments and relaunch run_local_server in a fresh Terminal window
- re-enter the script with a guard environment variable so the npm task only runs once

## Raw Body
## Summary
- detect macOS environments and relaunch run_local_server in a fresh Terminal window
- re-enter the script with a guard environment variable so the npm task only runs once

## Testing
- not run (not requested)


------
https://chatgpt.com/codex/tasks/task_e_68f3fdb4e2a4832f9e63a0bfbc695fba

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> On macOS, relaunches `run_local_server.sh` in a new Terminal window when not already in a terminal, forwarding env/args with a guard flag and sensible fallbacks; otherwise runs the npm task directly.
> 
> - **Scripts**:
>   - **macOS Terminal relaunch** in `run_local_server.sh`:
>     - Detects non-interactive sessions and opens a new Terminal window via `osascript`.
>     - Forwards exported environment variables and CLI args; filters problematic vars and newlines.
>     - Uses `RUN_LOCAL_SERVER_CHILD=1` to prevent relaunch loops.
>     - Preserves working directory; falls back to in-process execution if Terminal is already attached or `osascript` is unavailable.
>   - Default behavior unchanged: runs `npm run run-local-server` when appropriate.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit c7ef2e974bc140ff4deb64052022d9b29d58dafa. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->
## Summary by CodeRabbit

* **Chores**
  * Improved macOS local server startup: when possible the server now opens in a native Terminal window for a cleaner interactive session. Terminal detection and robust fallbacks ensure it still starts reliably in non-interactive contexts (GUI launches, CI-like environments), preserving existing behavior while providing a more consistent developer experience.
<!-- end of auto-generated comment: release notes by coderabbit.ai -->
