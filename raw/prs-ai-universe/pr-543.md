# PR #543: fix: remove conversation start tool

**Repo:** jleechanorg/ai_universe
**Merged:** 2025-11-04
**Author:** jleechan2015
**Stats:** +218/-326 in 8 files
**Labels:** codex

## Summary
- remove the `conversation.start` MCP tool from the backend and rely on `conversation.send-message` for creation
- update backend README, CI smoke tests, and integration utilities to reflect the revised toolset
- adjust unit and integration tests plus the MCP simulator to exercise the new creation flow

## Raw Body
## Summary
- remove the `conversation.start` MCP tool from the backend and rely on `conversation.send-message` for creation
- update backend README, CI smoke tests, and integration utilities to reflect the revised toolset
- adjust unit and integration tests plus the MCP simulator to exercise the new creation flow

## Testing
- npm test -- ConversationAgent

------
https://chatgpt.com/codex/tasks/task_e_690909983a54832f9fd18bcf51a6b6c6

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Removes `conversation.start` and standardizes conversation creation via `conversation.send-message`, updating README, CI/integration tests, unit tests, and the MCP simulator; docs also switch to `agent.second_opinion`.
> 
> - **Backend**:
>   - Remove `conversation.start` tool and schema from `ConversationAgent`.
>   - Add `conversation.send-message` schema/handler; keep `conversation.add-message` (routes assistant messages directly, user via `SingleModelChatService`).
> - **Docs**:
>   - Update README tool list: `agent_second_opinion` → `agent.second_opinion`; list `conversation.send-message` and remove `conversation.start`.
> - **Tests**:
>   - Update `ConversationAgent.test.ts` to drop `conversation.start` cases; add coverage for auto-create via `conversation.send-message` and assistant routing.
>   - Integration tests (`testing_integration/*`) migrate start flows to `conversation.send-message` and adjust ID extraction.
> - **CI/Tooling**:
>   - `scripts/mcp-ci-tests.mjs` required tools updated (remove `conversation.start`), smoke test now seeds via `conversation.send-message`.
> - **Simulator**:
>   - Replace `conversation.start` handler with `conversation.send-message`; support optional `conversationId`, `role`, and `metadata`; return `conversation`, `message`, and IDs consistently.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 5eb2dd4a7b7c400e80a640d7953d54aff72e216d. This will update automatically on new commits. Configure [here](https://cursor.
