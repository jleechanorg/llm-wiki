# PR #225: fix: resolve MCP smoke workflow merge conflicts

**Repo:** jleechanorg/ai_universe
**Merged:** 2025-10-09
**Author:** jleechan2015
**Stats:** +500/-34 in 9 files
**Labels:** codex

## Summary
- allow the smoke test runner and CLI to accept a pre-resolved preview service URL
- update the PR preview workflow to discover the service URL before invoking the shared CLI
- document the optional override flag and add regression coverage for the direct service URL path

## Raw Body
## Summary
- allow the smoke test runner and CLI to accept a pre-resolved preview service URL
- update the PR preview workflow to discover the service URL before invoking the shared CLI
- document the optional override flag and add regression coverage for the direct service URL path

## Testing
- pytest packages/github-actions-deploy/tests/test_smoke_scaffolding.py

------
https://chatgpt.com/codex/tasks/task_e_68e4d37c0588832f8b71e5900e1ac07d

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Introduces a shared MCP smoke test CLI and integrates it into PR preview workflows with service URL discovery, GH_TOKEN standardization, job URL resolution, and minor CI tweaks.
> 
> - **CI/Workflows**:
>   - **PR Preview (`.github/workflows/pr-dev-preview.yml`)**:
>     - Standardize `GH_TOKEN` to `${{ github.token }}` and set at job level.
>     - Discover service URL post-deploy and wait for health before commenting.
>     - Replace inline smoke test logic with `gha-mcp-smoke` CLI; add job ID resolution for accurate log links.
>     - Add separate Python MCP utils smoke test job with improved result commenting.
>   - **CI (`.github/workflows/ci.yml`)**:
>     - Set `continue-on-error: true` for Codecov upload.
> - **Package `github-actions-deploy`**:
>   - Add MCP smoke testing scaffolding: `github_actions_deploy/smoke.py` and CLI `smoke_cli.py`.
>   - Export `smoke` in `__init__.py`; add script entry `gha-mcp-smoke` in `pyproject.toml`.
>   - README: document MCP smoke test CLI usage and options.
> - **Tests**:
>   - Add `tests/test_smoke_scaffolding.py` with fixtures and coverage for runner behavior and comment formatting.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 41b2c280869df0a4c1984ff406d723b5b38a7c1a. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->
## Summary by CodeRa
