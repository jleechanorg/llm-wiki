# PR #208: feat: expose config-utils public api re-exports

**Repo:** jleechanorg/ai_universe
**Merged:** 2025-10-07
**Author:** jleechan2015
**Stats:** +134/-59 in 12 files
**Labels:** codex

## Summary
- expose the config manager exports and second opinion constants from the package entry point
- add package export metadata plus a regression script to confirm the public API matches the source
- align tests and docs to cover the top-level imports and updated runtime defaults

## Raw Body
## Summary
- expose the config manager exports and second opinion constants from the package entry point
- add package export metadata plus a regression script to confirm the public API matches the source
- align tests and docs to cover the top-level imports and updated runtime defaults

## Testing
- npm run build
- npm run check:exports
- npm run test

------
https://chatgpt.com/codex/tasks/task_e_68e4b5e5f3d8832f800c84b798808988

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Expose key re-exports at the package entry point with explicit exports map and add a script to verify published API; update tests, configs, and docs accordingly.
> 
> - **Public API exposure**:
>   - Re-export `ConfigManager`, `configManager`, `RuntimeConfigService`, `runtimeConfigService`, types, and all `SecondOpinionConfig` constants from `src/index.ts`.
>   - Add `exports` map in `package.json` for `.`/`dist/*`/`package.json`.
> - **Verification & tooling**:
>   - Add `scripts/check-exports.mjs` and `npm run check:exports` to print sorted export keys.
>   - Update `jest.config.js` and `tsconfig.json` to map `@ai-universe/config-utils` to the source and include Node types.
> - **Tests**:
>   - Switch imports to `@ai-universe/config-utils`; add test asserting public export surface.
>   - Adjust expectations: prefer secrets over env vars, rename `CEREBRAS_API_KEY` to `OPENROUTER_API_KEY`, update model IDs (`cerebras` to `qwen/qwen3-235b-a22b-thinking-2507`, `perplexity` to `sonar`), add `openai` model, and increase default/max timeouts to 600000/900000ms.
>   - Strengthen Firestore mocks with explicit types.
> - **Logging**:
>   - Improve logger printf typing/formatting and timestamp handling.
> - **Docs**:
>   - Rename to "Constants & conversation defaults" and note entry-point exports; add instructions to verify the public API.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit b20c46fc413ca1879b9db8be4466920fdf505e50. This will update automatically on new c
