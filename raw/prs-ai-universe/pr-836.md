# PR #836: Extract shared code into libraries

**Repo:** jleechanorg/ai_universe
**Merged:** 2025-11-27
**Author:** jleechan2015
**Stats:** +1714/-1259 in 36 files

## Summary
(none)

## Raw Body
Extract RateLimitErrors.ts (72 LOC) from backend/src/utils/ to the shared mcp-server-utils package, with comprehensive tests (29 tests).

Changes:
- Move RateLimitErrors.ts to shared-libs/packages/mcp-server-utils/src/
- Add RateLimitErrors.test.ts with 29 comprehensive tests
- Update backend imports to use @ai-universe/mcp-server-utils
- Export RateLimitExceededError and formatCustomWindowDescriptor

Files affected:
- backend/src/agents/SecondOpinionAgent.ts (import update)
- backend/src/agents/ConversationAgent.ts (import update)
- backend/src/test/SecondOpinionAgent.rateLimit.test.ts (import update)
- shared-libs/packages/mcp-server-utils/src/index.ts (new export)

Test results:
- mcp-server-utils: 257 tests pass (including 29 new RateLimitErrors tests)
- backend: 505 tests pass

This extraction consolidates rate limit error handling in the shared library where RateLimitTool and RateLimitWindowType are already defined.

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Unifies model/config imports, extracts conversation MCP client and rate-limit/sanitization utilities into shared libs with new schemas and tests, and refactors backend to use them plus a monitored HTTP client.
> 
> - **Shared Libraries (mcp-server-utils, config-utils)**:
>   - **Config**: Replace `SecondOpinionConfig` with unified `ModelConfig` exports across packages.
>   - **New Utilities**: Add `ConversationMCPClientWithTimeout`, `RateLimitErrors`, and `sanitizeAuthenticatedArguments`; export via shared index.
>   - **Schemas**: Introduce Zod conversation schemas and publish canonical JSON schemas; remove `agent.second-opinion` schema and second-opinion validators.
>   - **Tests**: Add comprehensive tests for conversation client, rate-limit errors, auth sanitization, schemas, and ESM imports.
> - **Backend**:
>   - **Agents**: Update imports to shared utils; remove local rate-limit errors; adjust SecondOpinionAgent to rely on shared errors; minor schema fields added (e.g., `_authenticat
