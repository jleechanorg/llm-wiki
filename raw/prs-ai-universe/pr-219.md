# PR #219: feat: extract MCP smoke test scaffolding into shared CLI

**Repo:** jleechanorg/ai_universe
**Merged:** 2026-03-16
**Author:** jleechan2015
**Stats:** +48/-332 in 5 files
**Labels:** codex

## Summary
- add reusable MCP smoke test runner and CLI to the github-actions-deploy package
- document new gha-mcp-smoke command and expose it via package exports
- update the preview workflow to install the package and invoke the CLI for smoke validation

## Raw Body
## Summary
- add reusable MCP smoke test runner and CLI to the github-actions-deploy package
- document new gha-mcp-smoke command and expose it via package exports
- update the preview workflow to install the package and invoke the CLI for smoke validation

## Testing
- pytest

------
https://chatgpt.com/codex/tasks/task_e_68e4d37c0588832f8b71e5900e1ac07d

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Updates the PR preview workflow and shared smoke-test tooling; failures could break CI smoke validation and PR commenting, but changes are confined to CI/developer tooling (no runtime app logic).
> 
> **Overview**
> The PR refactors PR-preview MCP smoke testing to run through the `github-actions-deploy` package’s `gha-mcp-smoke` CLI, removing the workflow’s explicit Cloud Run URL lookup, Firebase token generation, shared-libs build step, and log-artifact upload.
> 
> In `github-actions-deploy`, the smoke runner/CLI API is simplified by dropping the `--service-url` override and test-mode labeling, switching `MCP_SERVER_URL` injection to `setdefault`, and trimming success/failure log output and comment content; tests and README examples are updated accordingly.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit abf0debd92559905c73ad2a17386bba00bd8ed9a. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->
## Summary by CodeRabbit

- **New Features**
  - Added a CLI command for running MCP smoke tests and exposed smoke testing in the deployment toolkit.
- **Documentation**
  - Updated README/quick-start with a “Run MCP smoke tests” step and usage examples.
- **Chores**
  - Simplified PR preview workflow to invoke the new CLI for smoke tests and reporting.
- **Tests**
  - Added tests covering smoke scaffolding, environment handling, error paths, and comment generatio
