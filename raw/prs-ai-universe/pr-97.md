# PR #97: fix: Use local workspace package for mcp-server-utils

**Repo:** jleechanorg/ai_universe
**Merged:** 2025-09-30
**Author:** jleechan2015
**Stats:** +68/-17 in 8 files

## Summary
- Fixed deployment build failure by switching `@ai-universe/mcp-server-utils` from npm registry reference to local workspace package
- Excluded test files from TypeScript build to prevent compilation errors
- Ensures consistency with other `@ai-universe/*` shared-libs packages

## Test Plan
- [x] TypeScript compilation succeeds
- [x] Unit tests pass
- [x] Pre-commit hooks pass
- [x] Ready for deployment

## Raw Body
## Summary
- Fixed deployment build failure by switching `@ai-universe/mcp-server-utils` from npm registry reference to local workspace package
- Excluded test files from TypeScript build to prevent compilation errors
- Ensures consistency with other `@ai-universe/*` shared-libs packages

## Changes
- `backend/package.json`: Changed dependency from `^1.0.0` to `file:../shared-libs/packages/mcp-server-utils`
- `shared-libs/packages/mcp-server-utils/tsconfig.json`: Excluded `**/*.test.ts` and `**/__tests__` from build

## Test Plan
- [x] TypeScript compilation succeeds
- [x] Unit tests pass
- [x] Pre-commit hooks pass
- [x] Ready for deployment

## Root Cause
Package was referencing npm registry instead of local workspace, causing TypeScript module resolution failure during deployment build.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Use local workspace `@ai-universe/mcp-server-utils` and integrate its build into CI, deploy, and Docker scripts, while excluding its tests from TS build.
> 
> - **Build/Deps**:
>   - Switch `backend` dependency `@ai-universe/mcp-server-utils` to local workspace (`file:../shared-libs/packages/mcp-server-utils`); lockfile updated to link local package.
> - **CI/CD**:
>   - Workflows (`.github/workflows/ci.yml`, `integration.yml`) now install/build `shared-libs/packages/mcp-server-utils`.
>   - `deploy.sh`: builds all shared libraries first, then builds backend.
>   - `backend/docker/install_shared_libs.sh`: includes `mcp-server-utils` in `SHARED_LIBS_PACKAGES`.
> - **Build config**:
>   - `shared-libs/packages/mcp-server-utils/tsconfig.json`: exclude `**/*.test.ts` and `**/__tests__` from compilation.
> - **Repo hygiene**:
>   - `.gitignore`: ignore `.serena/memories/` and `backend/shared-libs/`.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 6565508d428b08fb18036989f1042b1f39afba59. This will update automatically on new commits. Configu
