# PR #701: Fix admin fallback rate limiting

**Repo:** jleechanorg/ai_universe
**Merged:** 2025-11-13
**Author:** jleechan2015
**Stats:** +137/-4 in 3 files
**Labels:** codex

## Summary
- make the rate-limit contact email configurable via RATE_LIMIT_CONTACT_EMAIL so the backend and shared limiter stay in sync
- update the RateLimitTool to always include the contact/admin fallback emails (and optional overrides) when building the admin allowlist
- add regression coverage proving fallback admins do not inherit the 10/hr non-VIP throttle

## Raw Body
## Summary
- make the rate-limit contact email configurable via RATE_LIMIT_CONTACT_EMAIL so the backend and shared limiter stay in sync
- update the RateLimitTool to always include the contact/admin fallback emails (and optional overrides) when building the admin allowlist
- add regression coverage proving fallback admins do not inherit the 10/hr non-VIP throttle

## Testing
- `cd backend && npm run test -- RateLimitTool.test.ts`

------
[Codex Task](https://chatgpt.com/codex/tasks/task_e_69158db3a284832fb31189d5c654ebdb)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Configures rate-limit contact email via env with validation and updates RateLimitTool to merge validated fallback admin emails into the admin allowlist, with regression tests.
> 
> - **Backend**
>   - SecondOpinionAgent: make `RATE_LIMIT_CONTACT_EMAIL` configurable with validation and default fallback; used in rate-limit error messaging.
> - **Shared Library (`RateLimitTool`)**
>   - Add builtin fallback admin emails and merge with env overrides (`RATE_LIMIT_CONTACT_EMAIL`, `RATE_LIMIT_FALLBACK_ADMIN_EMAILS`) using validation and caching.
>   - Update admin allowlist assembly to include fallbacks, cache via env signature, and log when falling back; return fallback set when no env config.
>   - Add email validation utility.
> - **Tests**
>   - Regression: verify fallback contact admins are treated as admins (not limited to 10/hr).
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit a9f57d17b1d5c33b654833b7bf33b559d8f9be4d. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->
## Summary by CodeRabbit

* **New Features**
  * Rate-limit contact email is now configurable via environment with validation and a safe default.

* **Improvements**
  * Rate limiting now merges configured admin emails with a validated fallback
