# PR #590: refactor: Extract rate limiting to shared-libs and integrate Secret Manager

**Repo:** jleechanorg/ai_universe
**Merged:** 2025-11-09
**Author:** jleechan2015
**Stats:** +981/-797 in 17 files

## Summary
(none)

## Raw Body
# Rate Limiting & Secret Manager Refactor

**Split from combined/pr547-549 for easier review**

## Changes

### Infrastructure Improvements
- Extracted `RateLimitTool` from backend to `@ai-universe/mcp-server-utils` shared library
- Integrated Google Cloud Secret Manager for dynamic rate limit configuration
- Removed backend-specific RateLimitTool implementation
- Updated all imports to use shared package

### Files Changed (17 files)
- **Deleted:** `backend/src/tools/RateLimitTool.ts` 
- **Enhanced:** `shared-libs/packages/mcp-server-utils/src/RateLimitTool.ts` with Secret Manager
- **Updated:** Package dependencies with `@google-cloud/secret-manager`
- **Fixed:** Import paths in 11 test files + `createFastMCPServer.ts`

## Testing
- ✅ All 41 rate limiting tests pass
- ✅ Build successful
- ✅ Type checking passed
- ✅ Linting passed

## Benefits
- Centralizes rate limiting logic in shared library
- Enables dynamic rate limit configuration via Secret Manager
- Improves code reusability across services
- Cleaner separation of concerns

**Reference:** Split from combined/pr547-549 (conversation stack changes)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Moves rate limiting into the shared utils with Secret Manager–backed config, updates imports/usages across backend, and adds caching/cleanup improvements with new tests.
> 
> - **Shared Utils (`@ai-universe/mcp-server-utils`)**:
>   - **`RateLimitTool`**: Adds Google Secret Manager–backed config with caching (5m success TTL, 30s fallback TTL), increases dev limits (`10000`/min), stable `auth-fallback` identifiers, async `dispose()`, and simplified distributed detection (env override).
>   - **Logging**: Exposes `setRateLimitLogger` and uses shared logger interface.
> - **Backend**:
>   - Replaces local `tools/RateLimitTool.ts` (deleted) with imports from `@ai-universe/mcp-server-utils`.
>   - `createFastMCPServer`: initializes shared `RateLimitTool`; removes `setRuntimeConfig` call; wires shared loggers.
> - **Tests**:
>   
