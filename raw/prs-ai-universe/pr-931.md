# PR #931: Switch to OpenRouter Cerebras model for synthesis

**Repo:** jleechanorg/ai_universe
**Merged:** 2025-12-18
**Author:** jleechan2015
**Stats:** +79/-11 in 2 files
**Labels:** codex

## Summary
- resolve Cerebras mode selection to use OpenRouter SMART mode when an OpenRouter key is present and fall back to FAST/direct when only the Cerebras key is configured
- align Cerebras primary and synthesis error metadata with the active endpoint, including web search diagnostics when routed through OpenRouter
- add test coverage for OpenRouter-key-missing fallback and set mock environment defaults for deterministic Cerebras web search assertions

## Raw Body
## Summary
- resolve Cerebras mode selection to use OpenRouter SMART mode when an OpenRouter key is present and fall back to FAST/direct when only the Cerebras key is configured
- align Cerebras primary and synthesis error metadata with the active endpoint, including web search diagnostics when routed through OpenRouter
- add test coverage for OpenRouter-key-missing fallback and set mock environment defaults for deterministic Cerebras web search assertions

## Testing
- npm run test -- --runTestsByPath src/agents/__tests__/SecondOpinionAgent.cerebrasWebSearch.test.ts


------
[Codex Task](https://chatgpt.com/codex/tasks/task_e_69435dd820f8832fa4452285bcd9c485)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Dynamically chooses Cerebras SMART (OpenRouter) vs FAST (direct) based on config/env, threads mode through callers/synthesis, and aligns error metadata; adds tests for fallback behavior and web-search diagnostics.
> 
> - **Agents (`backend/src/agents/SecondOpinionAgent.ts`)**:
>   - **Cerebras mode resolution**: Add `resolvePrimaryCerebrasMode()` to pick `SMART` (OpenRouter) when an OpenRouter key is present, else `FAST` (direct) if only a Cerebras key exists, with env var fallbacks.
>   - **Error metadata**: Add `buildCerebrasErrorMetadata()` to set provider and `webSearchDiagnostics` based on resolved mode; applied to primary and synthesis error paths.
>   - **Model callers**: Extend `createModelCallers(...)` to accept `cerebrasMode`; propagate to `toolRegistry.getLLMCaller('cerebras', { cerebrasMode })` for primary and synthesis.
>   - **Execution flow**: Resolve and pass `cerebrasMode` for primary and synthesis; ensure primary model availability and include mode-aware diagnostics.
>   - **Config import**: Import `config` alongside `getConfig`.
> - **Tests (`backend/src/agents/__tests__/SecondOpinionAgent.cerebrasWebSearch.test.ts`)**:
>   - Set/restore `OPENROUTER_API_KEY` and `CEREBRAS_API_KEY` per test for deterministic assertions.
>   - Verify Cerebras uses Open
