# PR #287: fix: restore Python 3.8 typing compatibility

**Repo:** jleechanorg/ai_universe
**Merged:** 2025-10-11
**Author:** jleechan2015
**Stats:** +44/-28 in 4 files
**Labels:** codex

## Summary
- replace Python 3.9+ generic syntax in MCP server utilities with typing equivalents for Python 3.8
- update the FastMCP HTTP proxy helper to accept union-typed CORS options without PEP 604 syntax

## Raw Body
## Summary
- replace Python 3.9+ generic syntax in MCP server utilities with typing equivalents for Python 3.8
- update the FastMCP HTTP proxy helper to accept union-typed CORS options without PEP 604 syntax

## Testing
- pytest tests/test_python_mcp_server_utils.py -q *(fails: ModuleNotFoundError: No module named 'fastmcp')*

------
https://chatgpt.com/codex/tasks/task_e_68ea18d660e8832f8b2c2fb93763305f

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Replaces 3.9+ generic/union syntax with typing equivalents and minor isinstance/Task annotations to support Python 3.8 across MCP server utilities.
> 
> - **Typing compatibility (Python 3.8)**:
>   - `mcp_server_utils/cors.py`:
>     - Switch `dict[...]` to `Dict[...]` for `_wildcard_patterns`, `to_starlette_kwargs` return and `kwargs` types.
>   - `mcp_server_utils/http_proxy.py`:
>     - Replace `|` unions with `Union[...]` for `cors_options`.
>     - Use `MappingABC` for `isinstance` checks; import `Mapping` from `typing`.
>     - Relax `asyncio.Task[None]` to `asyncio.Task`.
>   - `mcp_server_utils/server_factory.py`:
>     - Use `Union`/`Dict` for `RegisterableTool`, `create_mcp_server` `options`, and health-check tool types; add explicit `Dict[str, Any]` payload annotations.
>   - `mcp_server_utils/tool_helpers.py`:
>     - Replace `dict[...]` with `Dict[...]`; use `Union` in `status_provider` and related annotations; update logger param to `Optional[Callable[[str, Optional[Dict[str, Any]]], None]]`; type `call_params` as `Dict[str, Any]`.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit b065cd6775994d20dc0595be2b5b3127ba7df145. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
