# PR #345: feat: add pr preview workflow installer

**Repo:** jleechanorg/ai_universe
**Merged:** 2025-10-14
**Author:** jleechan2015
**Stats:** +830/-4 in 5 files
**Labels:** codex

## Summary
- add `scripts/install-pr-preview-workflow.sh` to copy the PR preview workflow, deploy script, and smoke tests into downstream repositories
- introduce reusable template assets under `docs/workflows/templates/pr-preview/` with placeholder substitution for project, region, and service naming
- document the installer in `docs/workflows/README.md`, updating the quickstart, setup guidance, and reference snippets to match the new templates

## Raw Body
## Summary
- add `scripts/install-pr-preview-workflow.sh` to copy the PR preview workflow, deploy script, and smoke tests into downstream repositories
- introduce reusable template assets under `docs/workflows/templates/pr-preview/` with placeholder substitution for project, region, and service naming
- document the installer in `docs/workflows/README.md`, updating the quickstart, setup guidance, and reference snippets to match the new templates

## Testing
- ./scripts/install-pr-preview-workflow.sh --project-id demo-project --region europe-west1 --service-prefix demo-service /tmp/test-pr-preview


------
https://chatgpt.com/codex/tasks/task_e_68eded19ffa8832f945980ad6164ba9d

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Adds an installer and reusable templates to set up a GCP PR preview workflow with MCP smoke tests, and updates docs with quickstart and reference snippets.
> 
> - **Templates (new)**:
>   - `docs/workflows/templates/pr-preview/.github/workflows/pr-dev-preview.yml.tmpl` – GitHub Actions workflow for deploying Cloud Run PR previews and running MCP smoke tests.
>   - `docs/workflows/templates/pr-preview/deploy.sh.tmpl` – Deploy script with placeholder-based project/region/service naming.
>   - `docs/workflows/templates/pr-preview/scripts/mcp-ci-tests.mjs` – Node.js MCP smoke tests (health check, tools/list, health-check, agent.second_opinion).
> - **Installer (new)**:
>   - `scripts/install-pr-preview-workflow.sh` – Copies templates into a target repo, substitutes `__GCP_PROJECT__`, `__GCP_REGION__`, `__SERVICE_NAME_PREFIX__`, sets executable bits, and updates/creates `package.json` with `test:mcp` (supports `--force`, `--skip-package-json`).
> - **Docs**:
>   - `docs/workflows/README.md` – Adds Quickstart Checklist, Bootstrap Script usage, reference implementations, expanded config/env guidance, and file manifest updates (including lockfile and optional helpers).
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 68dc
