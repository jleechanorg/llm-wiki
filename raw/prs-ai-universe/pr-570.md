# PR #570: Verify add-message tool removal

**Repo:** jleechanorg/ai_universe
**Merged:** 2025-11-08
**Author:** jleechan2015
**Stats:** +102/-165 in 5 files

## Summary
(none)

## Raw Body
Following up on PR #565 which consolidated conversation persistence on sendMessage, this removes the deprecated conversation.add-message tool:

- Removed conversation.add-message tool registration from ConversationAgent
- Removed addMessageSchema which is no longer needed
- Removed tests for add-message tool
- Updated test expectations to not include add-message in tool list

All functionality is now handled by conversation.send-message which supports both creating new conversations and adding messages to existing ones.

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Removes the deprecated `conversation.add-message` tool, consolidates message persistence through `conversation.send-message` (with auto-creation and ChatService handling), and updates tests/CI accordingly.
> 
> - **ConversationAgent**:
>   - Remove `addMessageSchema` and `conversation.add-message` tool.
>   - Update `conversation.send-message` to route `user` messages via `chatService.processUserMessage` (including `title`/`metadata`) and non-user roles directly to `conversationTool.sendMessage`.
> - **SingleModelChatService**:
>   - `processUserMessage` now accepts optional `conversationId`, trims inputs, forwards `title`, and derives/returns persisted `conversationId` from persistence response.
> - **Tests**:
>   - Update `ConversationAgent.test` to only use `conversation.send-message`; adjust mocks/expectations; remove add-message scenarios.
>   - Add coverage for deriving `conversationId` when not provided; ensure `title` propagated; keep fallback behaviors validated.
> - **CI/Smoke Tests**:
>   - Remove `conversation.add-message` from required tools in `scripts/mcp-ci-tests.mjs` and use `conversation.send-message` for follow-up messages.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit f62653b54e87a13d95cacc4d83156b30dd82913e. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY 
