# PR #706: Force Cerebras OpenRouter calls to stay on BYOK

**Repo:** jleechanorg/ai_universe
**Merged:** 2025-11-17
**Author:** jleechan2015
**Stats:** +64/-7 in 2 files
**Labels:** codex

## Summary
- add a dedicated helper to detect OpenRouter endpoints and attach the `provider.only` guard when calling Cerebras through OpenRouter so BYOK traffic never falls back to other providers
- keep the web search diagnostics logic aligned with the new helper and update the end-to-end payload test to assert the `provider` block

## Raw Body
## Summary
- add a dedicated helper to detect OpenRouter endpoints and attach the `provider.only` guard when calling Cerebras through OpenRouter so BYOK traffic never falls back to other providers
- keep the web search diagnostics logic aligned with the new helper and update the end-to-end payload test to assert the `provider` block

## Testing
- `cd backend && npm test -- TokenCentralization`
- `cd backend && npm run test:end2end -- webSearchPayload` *(fails: server not running for end-to-end harness)*

------
[Codex Task](https://chatgpt.com/codex/tasks/task_e_6915a0f6cf7c832fb515e04f1022f5ff)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Adds an OpenRouter endpoint helper and forces `provider.only: ['cerebras']` on OpenRouter requests (omitted for direct Cerebras), aligning web search payload/diagnostics and updating end-to-end tests.
> 
> - **Backend**:
>   - **CerebrasLLMTool** (`backend/src/tools/CerebrasLLMTool.ts`):
>     - Add `isOpenRouterEndpoint()` helper and use it across payload/metadata logic.
>     - When on OpenRouter, add `provider: { only: ['cerebras'] }` to request payload; omit for direct Cerebras endpoints.
>     - Gate web search `plugins`/`web_search_options` to OpenRouter endpoints; update `webSearchDiagnostics` accordingly.
>     - Normalize payload to omit `stream` when false; set metadata `provider` based on endpoint.
> - **Tests**:
>   - **End-to-end** (`backend/src/test/end2end/webSearchPayload.test.ts`):
>     - Assert `provider: { only: ['cerebras'] }` and web search plugin/options on OpenRouter requests; expect `stream` to be `undefined`.
>     - Add test ensuring direct Cerebras endpoint omits `provider`, retains model, and leaves `stream` undefined.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 1395e977d8f32b0b4ff4d459115fce8ec2a98031. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-g
