# PR #583: Refactor conversation setup concurrency into shared utils

**Repo:** jleechanorg/ai_universe
**Merged:** 2025-11-09
**Author:** jleechan2015
**Stats:** +361/-105 in 5 files
**Labels:** codex

## Summary
- add a shared runConversationSetupConcurrency helper in mcp-server-utils with full test coverage
- refactor SecondOpinionAgent to use the shared helper and keep persistence/error telemetry in the agent
- re-export the helper from the shared utils index for downstream consumers

## Raw Body
## Summary
- add a shared runConversationSetupConcurrency helper in mcp-server-utils with full test coverage
- refactor SecondOpinionAgent to use the shared helper and keep persistence/error telemetry in the agent
- re-export the helper from the shared utils index for downstream consumers

## Testing
- npm --prefix shared-libs/packages/mcp-server-utils test -- --runTestsByPath src/__tests__/ConversationSetupConcurrency.test.ts
- npm --prefix backend test -- SecondOpinionAgent.latency.test.ts

------
[Codex Task](https://chatgpt.com/codex/tasks/task_e_690f0b04e438832f9b1405c4a7a5dcfd)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Adds a shared conversation-setup concurrency helper and integrates it into SecondOpinionAgent, improves citation extraction by merging inline and metadata refs with deduping, and replaces mutex-based synthesis slots with a simple queue limiter, with tests.
> 
> - **Backend (`backend/src/agents/SecondOpinionAgent.ts`)**:
>   - Integrates `runConversationSetupConcurrency` to load history and (optionally) persist the user message concurrently; surfaces errors without throwing and passes `preloadedHistory` to `prepareConversationPrompt`.
>   - Replaces mutex-based synthesis slot control with queue/counter limiter (`acquireSynthesisSlot`/`releaseSynthesisSlot`).
>   - Adds `extractSourcesFromResponse` to merge inline text citations with metadata annotations (deduplicated); updates source aggregation in result compilation.
> - **Shared Utils (`shared-libs/packages/mcp-server-utils`)**:
>   - Adds `conversation/runConversationSetupConcurrency` with error normalization and returns `{ history, persistedMessage, historyError, persistenceError }`.
>   - Re-exports from `src/index.ts`.
> - **Tests**:
>   - New tests for conversation-setup concurrency and for metadata/inline citation extraction and deduplication.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit fc43b7d77572e5f9840cb24374f4181f3408fcbf. This will update
