# PR #105: fix: keep SecretManager JSON credential support across deploy copies

**Repo:** jleechanorg/ai_universe
**Merged:** 2025-10-01
**Author:** jleechan2015
**Stats:** +171/-65 in 2 files
**Labels:** codex

## Summary
- document that `deploy.sh` copies the shared library sources into `backend/shared-libs`, which overwrites direct edits to the backend copy of `SecretManager.ts`
- move the GOOGLE_APPLICATION_CREDENTIALS_JSON handling into the canonical shared library version so deploy-time sync keeps the JSON credential flow
- add regression tests that cover successful JSON credential parsing and the fallback path when parsing fails

## Raw Body
## Summary
- document that `deploy.sh` copies the shared library sources into `backend/shared-libs`, which overwrites direct edits to the backend copy of `SecretManager.ts`
- move the GOOGLE_APPLICATION_CREDENTIALS_JSON handling into the canonical shared library version so deploy-time sync keeps the JSON credential flow
- add regression tests that cover successful JSON credential parsing and the fallback path when parsing fails

## Testing
- npm test *(fails: existing config-utils suite has TypeScript typing errors and outdated expectations)*

------
https://chatgpt.com/codex/tasks/task_e_68dc8a970ddc832fa9f91446d078f3e1

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Adds explicit JSON credential init with ADC fallback to SecretManager and updates typings/mocks and tests accordingly, including healthcheck via accessSecretVersion.
> 
> - **Config Utils (`SecretManager`)**:
>   - Add `GOOGLE_APPLICATION_CREDENTIALS_JSON` parsing and initialize client with explicit credentials; fall back to Application Default Credentials with improved logging.
>   - Introduce explicit `SecretManagerClient` interface and tuple result types for `accessSecretVersion` and `listSecrets` using `@google-cloud/secret-manager` protos.
> - **Tests**:
>   - Refactor mocks to typed helpers (`createAccessSecretVersionResult`, etc.) and adjust implementations.
>   - Add tests for explicit JSON credential path and ADC fallback on parse failure.
>   - Update `testConnection` to probe via `accessSecretVersion` (healthcheck secret), handle timeouts/jitter, and refine CI metadata checks.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 9e790fced8908b011b0ac72c670a6f3ecd4a273d. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
