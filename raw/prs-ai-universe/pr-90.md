# PR #90: refactor: share FastMCP server factory

**Repo:** jleechanorg/ai_universe
**Merged:** 2025-09-29
**Author:** jleechan2015
**Stats:** +8457/-1181 in 23 files
**Labels:** codex

## Summary
- extract the FastMCP server bootstrap logic into `backend/src/createFastMCPServer.ts` for reuse
- update both the HTTP and stdio entrypoints to import the shared factory and remove duplicated tool/agent setup

## Raw Body
## Summary
- extract the FastMCP server bootstrap logic into `backend/src/createFastMCPServer.ts` for reuse
- update both the HTTP and stdio entrypoints to import the shared factory and remove duplicated tool/agent setup

## Testing
- npm run build

------
https://chatgpt.com/codex/tasks/task_e_68da58a0d454832f8faffd4dcb68d1f4

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Introduces a shared MCP server utils package (HTTP proxy, wildcard CORS, port discovery) and refactors backend to a shared FastMCP server factory with updated HTTP/stdio entrypoints and tests.
> 
> - **Backend**:
>   - **Server Factory**: Extract FastMCP bootstrap into `backend/src/createFastMCPServer.ts`; update `server.ts` and `stdio-server.ts` to use it.
>   - **HTTP Proxy Startup**: Replace inline proxy/CORS/port logic with utilities; keep health root endpoints and conversation API.
>   - **Jest**: Fix ESM transforms and module mapping; add CI discrepancy red/green test.
> - **Shared Library (`@ai-universe/mcp-server-utils`)**:
>   - **New Utilities**: `startFastMcpHttpProxy`, `createWildcardCorsOptions`, `wildcardToSafeRegex` (ReDoS-safe), `findAvailablePort`.
>   - **Docs/Config**: README, Jest config, TS config.
>   - **Tests**: Unit tests for CORS, wildcard regex, and port discovery.
> - **Dependencies**:
>   - Add `@ai-universe/mcp-server-utils`; bump `fastmcp` and various transitive deps; update lockfiles.
> - **Docs**:
>   - Add REAL MCP test results and PR guidelines.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 8719e437a281591653d8d7abb3a0850263b810fe. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->
## Summary by CodeRabbit

* New Features
  * Introduced a reusable MCP server utilities package with HTTP proxy, wildcard CORS, and dynamic port selection.
  * Added Cloud Run–friendly hea
