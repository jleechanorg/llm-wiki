# PR #233: fix: align model token limits with shared defaults

**Repo:** jleechanorg/ai_universe
**Merged:** 2025-10-08
**Author:** jleechan2015
**Stats:** +151/-101 in 4 files
**Labels:** codex

## Summary
- expose shared LLM token limit constants and helpers from the config-utils package
- update ConfigManager to emit maxInputTokens/maxOutputTokens across providers and keep legacy maxTokens aliases
- reuse the shared exports inside backend llmDefaults and refresh the ConfigManager tests

## Raw Body
## Summary
- expose shared LLM token limit constants and helpers from the config-utils package
- update ConfigManager to emit maxInputTokens/maxOutputTokens across providers and keep legacy maxTokens aliases
- reuse the shared exports inside backend llmDefaults and refresh the ConfigManager tests

## Testing
- npm test -- ConfigManager *(fails: jest binary unavailable in container)*
- npx tsc -p shared-libs/packages/config-utils/tsconfig.json *(fails: missing @types/node type definitions in environment)*

------
https://chatgpt.com/codex/tasks/task_e_68e6a3f0c72c832fa5fae075150cdd99

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Centralizes LLM token limit constants/utilities in config-utils, updates ConfigManager and backend to use them with maxInputTokens/maxOutputTokens (keeping legacy maxTokens), and refreshes tests.
> 
> - **Shared Library (`config-utils`)**:
>   - **Token Limit APIs**: Add and export `DEFAULT_MAX_INPUT_TOKENS`, `DEFAULT_MAX_OUTPUT_TOKENS`, `GLOBAL_MAX_OUTPUT_TOKENS`, `CLAUDE_SAFE_MAX_OUTPUT_TOKENS`, `PROVIDER_SAFE_OUTPUT_LIMITS`, `resolveSafeOutputLimit`, and `ModelDefaults` from `ConfigManager` and `index`.
>   - **Config Schema**: Update `AppConfig.models.*` to include `maxInputTokens` and `maxOutputTokens`; keep `maxTokens` as optional legacy alias.
>   - **Model Defaults**: Set per-provider limits via `resolveSafeOutputLimit`; change `perplexity.model` to `sonar-pro`; set `cerebras` legacy `maxTokens` to `GLOBAL_MAX_OUTPUT_TOKENS`.
> - **Backend**:
>   - **llmDefaults**: Reuse shared token limit exports/types; remove local implementations; add `promptGeneration` to `DEFAULT_LLM_TIMEOUTS`; re-export shared constants/helpers.
> - **Tests**:
>   - Update `ConfigManager.test.ts` to validate new fields (`maxInputTokens`, `maxOutputTokens`), legacy `maxTokens` behavior, and `perplexity` model name; adjust imports to include new exports.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit fd71ac40476abe2c2ba3365830
