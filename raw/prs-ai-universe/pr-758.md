# PR #758: fix: reconcile Gemini token counts with provider totals

**Repo:** jleechanorg/ai_universe
**Merged:** 2025-11-17
**Author:** jleechan2015
**Stats:** +14/-5 in 1 files
**Labels:** codex

## Summary
- reconcile Gemini prompt and completion token counts against provider totals before logging and cost calculation, deriving any missing counts directly from total usage
- retain defensive total token fallback when provider totals are absent or invalid

## Raw Body
## Summary
- reconcile Gemini prompt and completion token counts against provider totals before logging and cost calculation, deriving any missing counts directly from total usage
- retain defensive total token fallback when provider totals are absent or invalid

## Testing
- `cd backend && npm run lint -- --max-warnings=0`
- `cd backend && npm run test -- GeminiLLMTool.test.ts`


------
[Codex Task](https://chatgpt.com/codex/tasks/task_e_691aa5afa66c832f8b58aa139608fa12)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Reworks Gemini token accounting to reconcile prompt/completion with provider totals and adds safer total-token fallback for logging/costs.
> 
> - **Backend**
>   - **Gemini token accounting (`backend/src/tools/GeminiLLMTool.ts`)**:
>     - Derives `promptTokens`/`completionTokens` from `usageMetadata.totalTokenCount` when individual counts are missing; uses estimates only as last resort.
>     - Computes `totalTokens` via provider total when available, with guarded fallback to `promptTokens + completionTokens` if invalid.
>     - Keeps cost estimation and logging but now based on reconciled counts; attaches raw provider `usageMetadata` when present.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit e909d6731dba3d93e57a8a5eb7ce7adfe586f730. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
