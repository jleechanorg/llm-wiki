# PR #697: fix: audit token tracking across llm tools

**Repo:** jleechanorg/ai_universe
**Merged:** 2025-11-17
**Author:** jleechan2015
**Stats:** +279/-33 in 11 files
**Labels:** codex

## Summary
- update the Gemini tool to reuse the grounded prompt text for token estimation, honor the provider usage metadata, and log prompt/completion tokens into the summary so the cost calculator receives real counts
- switch the Perplexity tool to report the configured model id and feed its total token usage through the centralized cost calculator for accurate tier-based pricing
- recognize `step2-final-synthesis` as a synthesis identifier across both cost calculator implementations and document the s

## Raw Body
## Summary
- update the Gemini tool to reuse the grounded prompt text for token estimation, honor the provider usage metadata, and log prompt/completion tokens into the summary so the cost calculator receives real counts
- switch the Perplexity tool to report the configured model id and feed its total token usage through the centralized cost calculator for accurate tier-based pricing
- recognize `step2-final-synthesis` as a synthesis identifier across both cost calculator implementations and document the supported identifiers, with new tests protecting all of the updated behaviors

## Testing
- `cd backend && npm run test -- CostCalculator.test.ts GeminiLLMTool.test.ts PerplexityLLMTool.test.ts`


------
[Codex Task](https://chatgpt.com/codex/tasks/task_e_691578a7452c832fbfe3a9948eeac79c)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Improves token accounting and cost derivation by honoring provider usage, deriving costs from token totals, and treating synthesis model identifiers consistently across tools.
> 
> - **Backend**:
>   - **GeminiLLMTool** (`backend/src/tools/GeminiLLMTool.ts`):
>     - Honors `usageMetadata` (prompt/completion/total tokens); estimates when missing; reuses grounded prompt for estimation.
>     - Computes cost via `CostCalculator.calculateCost(model, promptTokens, completionTokens, totalTokens)` and returns detailed `metadata.usage`.
>     - Continues web search enforcement; avoids sending `toolConfig`; improved logging.
>   - **PerplexityLLMTool** (`backend/src/tools/PerplexityLLMTool.ts`):
>     - Reports configured `model` id; parses `total_tokens` robustly; routes totals through `CostCalculator` for pricing; improved logging; preserves web search overrides.
>   - **CostCalculator** (backend and shared `mcp-server-utils`):
>     - Adds unified synthesis IDs set including `step2-final-synthesis`.
>     - `calculateTotalCost` uses primary-model pricing for synthesis (by total tokens) and derives missing costs from token counts; normalization s
