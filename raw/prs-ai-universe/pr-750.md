# PR #750: Only return retryAt payloads vs ratelimit full info

**Repo:** jleechanorg/ai_universe
**Merged:** 2025-11-16
**Author:** jleechan2015
**Stats:** +227/-301 in 7 files
**Labels:** codex

## Summary
- sanitize ConversationAgent and SecondOpinionAgent so both streaming and non-streaming flows now emit only the retryAt timestamp when their rate limit trips
- document the minimal rate limit response contract and add dedicated regression tests to cover both agents
- align the standalone rate-limit response validation script with the new policy by logging only the retryAt payload

## Raw Body
## Summary
- sanitize ConversationAgent and SecondOpinionAgent so both streaming and non-streaming flows now emit only the retryAt timestamp when their rate limit trips
- document the minimal rate limit response contract and add dedicated regression tests to cover both agents
- align the standalone rate-limit response validation script with the new policy by logging only the retryAt payload

## Testing
- `cd backend && npm run test -- ConversationAgent.rateLimit SecondOpinionAgent.rateLimit`

------
[Codex Task](https://chatgpt.com/codex/tasks/task_e_69159c5e98e4832fa96c13d34be36ad9)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Standardizes rate-limit handling to return only a retryAt timestamp in both Conversation and SecondOpinion agents (streaming and non-streaming), with tests, docs, and scripts updated accordingly.
> 
> - **Backend**:
>   - **ConversationAgent**: FastMCP tool wrapper now catches `RateLimitExceededError` and returns `createMCPToolResult({ retryAt })`.
>   - **SecondOpinionAgent**:
>     - Removes contact/email-based rate-limit messaging; deletes related constants.
>     - Non-streaming `handleSecondOpinion` returns only `{ retryAt }` on rate limit.
>     - Streaming path enqueues an `error` event with `message: retryAt` and `metadata: { retryAt }`.
> - **Tests**:
>   - Update `ConversationAgent.rateLimit.test` to expect `{ retryAt }` and add case for downstream `RateLimitExceededError`.
>   - Add `SecondOpinionAgent.rateLimit.test` covering non-streaming and streaming `{ retryAt }` behavior.
>   - Adjust `SecondOpinionAgent.authRefactor.test` expectations to parse and assert `retryAt`.
> - **Docs/Scripts**:
>   - Rewrite `docs/rate-limit-response-format.md` to document the retryAt-only contract.
>   - Update `docs/test-rate-limit-response.mjs` to output and validate only `{ retryAt }`.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 6843570f4fa781653ba9aa0ce742f85e4a261b7f. This will update automatically on 
