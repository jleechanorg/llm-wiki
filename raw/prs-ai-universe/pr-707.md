# PR #707: fix: harden in-memory rate limit retention

**Repo:** jleechanorg/ai_universe
**Merged:** 2025-11-13
**Author:** jleechan2015
**Stats:** +74/-53 in 7 files
**Labels:** codex

## Summary
- track per-identifier windows so the in-memory limiter keeps the full hourly/daily history and cleanly removes entries when limits reset
- extend the shared rate-limit tests to cover cleanup behaviour, align backend tests with the current dev/test defaults, and drop the unused RATE_LIMIT_FORCE_NON_DISTRIBUTED env hook
- update the public rate-limit response doc to reflect the real authenticated tiers and call out that production loads its values from Secret Manager

## Raw Body
## Summary
- track per-identifier windows so the in-memory limiter keeps the full hourly/daily history and cleanly removes entries when limits reset
- extend the shared rate-limit tests to cover cleanup behaviour, align backend tests with the current dev/test defaults, and drop the unused RATE_LIMIT_FORCE_NON_DISTRIBUTED env hook
- update the public rate-limit response doc to reflect the real authenticated tiers and call out that production loads its values from Secret Manager

## Testing
- npm run test -- RateLimitTool.test.ts

------
[Codex Task](https://chatgpt.com/codex/tasks/task_e_6915a1a634e8832face3129f52ea61b5)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Harden in-memory rate limit retention by tracking per-identifier windows, switch dev to test defaults, update docs/tests, and remove an unused deploy flag.
> 
> - **Core (shared-libs `RateLimitTool`)**:
>   - Track per-identifier windows via `identifierWindows`; persist on writes and honor during `cleanupExpiredEntries` for accurate hourly/daily retention.
>   - Centralize state removal with `deleteIdentifierState` (used by cleanup, eviction, and `resetRateLimit`).
>   - Development now reuses test defaults (`getTestDefaults()`); removed generous `DEV_ADMIN_RATE_LIMIT` fallback.
> - **Tests**:
>   - Add cleanup retention window tests ensuring entries expire per configured window.
>   - Align expectations with dev/test defaults (e.g., anonymous=5/hr, auth=10/hr); simplify env handling and drop `RATE_LIMIT_FORCE_NON_DISTRIBUTED` references.
>   - Strengthen concurrency and production-default assertions.
> - **Docs**:
>   - Update `RATE_LIMIT_FIX.md` and `rate-limit-response-format.md` to reflect authenticated limits (10/hr, 20/day) and note Secret Manager as config source.
> - **Tooling**:
>   - Remove unused `--dev-admin-rate-limits` flag from `deploy.sh` and related help/arg parsing.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit c8f7ecd864fe70db3b320627303b4eec667c6d
