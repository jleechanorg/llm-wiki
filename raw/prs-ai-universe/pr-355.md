# PR #355: feat: enforce live web search across llm stack

**Repo:** jleechanorg/ai_universe
**Merged:** 2025-10-16
**Author:** jleechan2015
**Stats:** +2859/-344 in 25 files
**Labels:** codex

## Summary
- attach provider-specific `webSearchDiagnostics`, source counts, and totals to `agent.second_opinion` responses so smoke and integration suites can assert live search enforcement end-to-end
- extend the shared OpenAI config typing plus every LLM tool to normalize and expose enforced web-search payloads (tool choice, search options, and retrieved citations) via response metadata/getters
- exercise the updated pipeline against the local MCP server to confirm the JSON payload now carries enforced 

## Raw Body
## Summary
- attach provider-specific `webSearchDiagnostics`, source counts, and totals to `agent.second_opinion` responses so smoke and integration suites can assert live search enforcement end-to-end
- extend the shared OpenAI config typing plus every LLM tool to normalize and expose enforced web-search payloads (tool choice, search options, and retrieved citations) via response metadata/getters
- exercise the updated pipeline against the local MCP server to confirm the JSON payload now carries enforced search parameters, citations, and usage metadata for OpenAI, Perplexity, Gemini, and Grok

## Testing
- npm run test -- OpenAILLMTool
- npm run test -- PerplexityLLMTool
- npm run test -- GeminiLLMTool
- CI=false CI_SIMULATION=false CEREBRAS_API_KEY=live-cerebras-key CLAUDE_API_KEY=live-claude-key GEMINI_API_KEY=live-gemini-key PERPLEXITY_API_KEY=live-perplexity-key GROK_API_KEY=xai-live-key-abcdef OPENAI_API_KEY=live-openai-key NODE_ENV=test npx jest grok-api
- SKIP_E2E_HEALTHCHECK=true npm run test:end2end -- webSearchPayload
- MCP_SERVER_URL=http://localhost:2000/mcp CI_SIMULATION=false pytest testing_integration/test_web_search_requirements.py -s
- MCP_SERVER_URL=http://localhost:2000/mcp CI_SIMULATION=false python - <<'PY'\nfrom testing_integration.mcp_client import call_tool\nfrom testing_integration.mcp_utils import extract_json_content\nresp = call_tool('agent.second_opinion', {'question': 'Demonstrate grounded reasoning with citations.', 'userId': 'diagnostics-check', 'maxOpinions': 2})\ncontent = extract_json_content(resp)\nprint(content['metadata']['webSearchDiagnostics'])\nprint('primary sources:', content['metadata']['sourceCounts']['primary'])\nprint('totalSources:', content['metadata']['totalSources'])\nPY

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Enforces web search across providers and surfaces diagnostics/citations in second-opinion responses, with config/type support and comprehensive tests.
> 
> - **Backend Agent (`backend/src/agents/SecondOpini
