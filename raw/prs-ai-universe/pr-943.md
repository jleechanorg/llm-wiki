# PR #943: Pass user email to conversation backend

**Repo:** jleechanorg/ai_universe
**Merged:** 2025-12-25
**Author:** jleechan2015
**Stats:** +57/-9 in 5 files

## Summary
(none)

## Raw Body
Pass the authenticated user's email through the conversation message flow so it can be stored in Firestore alongside conversations:

- Add userEmail parameter to handleUserSendMessage in ConversationAgent
- Forward userEmail when calling conversationTool.sendMessage
- Update ConversationMCPTool.sendMessage and addMessage to accept and forward userEmail to the convo-backend MCP server
- Update query-user-activity.mjs to read email from the new userEmail field on conversation documents (with fallback to Firebase Auth lookup)

The email is now captured from the authenticated user context and passed through to the backend for storage. The convo-backend will need to be updated to accept and store this field in Firestore.

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Adds email propagation to conversation flow and updates analytics to consume it.**
> 
> - Conversation flow (`ConversationAgent` → `ConversationMCPTool`) now accepts `userEmail` and forwards it to the convo-backend as `email` in both `sendMessage` and `addMessage`; seed message persistence updated accordingly
> - `query-user-activity.mjs` reads `userEmail` from conversation documents (with metadata fallback), stores it in per-user stats, and prioritizes it when matching emails over Firebase Auth lookups
> - Transport tests add `Origin` headers to better simulate browser requests for Accept-header/CORS behavior
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 1b0d42d09492ec173719d2f2cea17a0da3a7a236. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->
## Summary by CodeRabbit

* **New Features**
  * Conversations now carry an authenticated user email with messages, improving identity association and ordering.
  * User activity reports and matching prioritize conversation-provided email for more accurate attr
