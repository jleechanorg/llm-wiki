# PR #981: fix: Migrate Grok API from deprecated Live Search to Agent Tools API

**Repo:** jleechanorg/ai_universe
**Merged:** 2026-02-05
**Author:** jleechan2015
**Stats:** +238/-247 in 9 files

## Summary
- Fixes Grok API HTTP 410 error caused by xAI deprecating their Live Search API
- Migrates from deprecated `search_parameters` field to new Agent Tools API `tools` array
- Grok was failing while Gemini and Perplexity succeeded, causing only 2/3 secondary models to return results

## Test Plan
- [x] Type check passes
- [x] Build passes  
- [ ] Deploy to dev and verify Grok API calls succeed
- [ ] Verify all 3 secondary models return results in multi-model consultation

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Changes the Grok provider request/response contract and web-search enablement logic, which could break production Grok calls or diagnostics if the Responses API assumptions are wrong; workflow/depen

## Raw Body
## Summary
- Fixes Grok API HTTP 410 error caused by xAI deprecating their Live Search API
- Migrates from deprecated `search_parameters` field to new Agent Tools API `tools` array
- Grok was failing while Gemini and Perplexity succeeded, causing only 2/3 secondary models to return results

## Changes
- Replace `search_parameters` and `return_citations` with `tools` array containing `web_search` and `x_search`
- Map legacy `sources` config to new tool types
- Update tests to verify new API format

## Test plan
- [x] Type check passes
- [x] Build passes  
- [ ] Deploy to dev and verify Grok API calls succeed
- [ ] Verify all 3 secondary models return results in multi-model consultation

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Changes the Grok provider request/response contract and web-search enablement logic, which could break production Grok calls or diagnostics if the Responses API assumptions are wrong; workflow/dependency tweaks are low risk but may affect CI coverage.
> 
> **Overview**
> Fixes Grok requests failing due to xAI deprecating `/chat/completions` live search by migrating `GrokLLMTool` to `/responses`, sending `tools: [{type:'web_search'}]` (and omitting deprecated `search_parameters`/`return_citations`). The tool now parses Responses API outputs (`output_text`/`output[]`) with a fallback to legacy `choices`, and honors `searchParameters.mode=off` by omitting tools entirely while updating `webSearchDiagnostics` accordingly.
> 
> Updates Grok-focused unit/e2e tests and the post-deploy `scripts/mcp-ci-tests.mjs` assertions to validate the new tool-based diagnostics, and adjusts CI to run on Node 20 + 22, removes the PR preview node-compatibility gate, and drops `@types/redis` from backend deps.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit b6b8ed499fb6d0a9871a410b66e62b5f2c5e49bd. This will update automatically on new commits. Con
