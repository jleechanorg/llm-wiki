# PR #619: Fix message persistence and ordering issue

**Repo:** jleechanorg/ai_universe
**Merged:** 2025-11-11
**Author:** jleechan2015
**Stats:** +759/-84 in 6 files

## Summary
(none)

## Raw Body
<!-- CURSOR_SUMMARY -->
> [!NOTE]
> Preserves receive-time (`_receivedAt`) timestamps across user/assistant messages to enforce chronological ordering (including fallbacks) and adds tests to verify capture, propagation, and resolution.
> 
> - **Backend**:
>   - **ConversationAgent**: Captures user message receive time; passes `userMessageTimestamp` to assistant generation; adds `_receivedAt` to metadata for non-user sends and fallback assistant; ensures assistant timestamp is after user (by +1ms).
>   - **SingleModelChatService**: Records `_receivedAt` on incoming messages; propagates timestamp to assistant generation; persists assistant with `_receivedAt` set to user timestamp + 1ms.
>   - **ConversationMCPTool**: Normalizes message `createdAt` by prioritizing `_receivedAt` via `resolveMessageTimestamp`; preserves ordering in `sendMessage` responses and `getConversationHistory` mapping.
> - **Tests**:
>   - Updates existing tests for new metadata; adds `MessageTimestampOrdering.test.ts` to validate capture, propagation, +1ms ordering, fallback behavior, and history timestamp preservation.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 25cdddb1a3e0f12d375796e050a089bb7db695e9. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->
## Summary by CodeRabbit

* **Bug Fixes**
  * Ensure user and assistant messages carry preserved receive timestamps so conversations display in correct chronological order (assistant timestamps follow user timestamps, including fallback/error cases).

* **Tests**
  * Added/updated comprehensive tests validating timestamp capture, propagation, ordering, and metadata preservation across normal and fallback flows.

* **Documentation**
  * Clarified timestamp resolution and preservation behavior in internal docs/comments.
<!-- end of auto-generate
