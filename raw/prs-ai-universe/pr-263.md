# PR #263: chore: log MCP smoke success metrics

**Repo:** jleechanorg/ai_universe
**Merged:** 2025-10-11
**Author:** jleechan2015
**Stats:** +368/-31 in 4 files

## Summary
- emit a success summary in gha-mcp-smoke so Actions logs show pass details
- truncate long stdout while still including service/workflow references
- cover success logging and truncation with unit tests

## Raw Body
## Summary
- emit a success summary in gha-mcp-smoke so Actions logs show pass details
- truncate long stdout while still including service/workflow references
- cover success logging and truncation with unit tests

## Testing
- python -m pytest packages/github-actions-deploy/tests/test_smoke_scaffolding.py

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Enhances MCP smoke tests with concise success summaries, structured JSON logging/truncation, workflow integration, and unit tests.
> 
> - **CI/Workflow (`.github/workflows/pr-dev-preview.yml`)**:
>   - Trigger on `packages/github-actions-deploy/**` changes.
>   - Run smoke tests with `MCP_LOG_BODY_MAX_CHARS="0"`, capture JSON log via `MCP_LOG_JSON` to `$RUNNER_TEMP/mcp-smoke-log.json`.
>   - Preserve exit code, export `MCP_SMOKE_LOG_PATH` env, and upload log as artifact (`actions/upload-artifact@v4`).
> - **Python smoke runner (`packages/github-actions-deploy/github_actions_deploy/smoke.py`)**:
>   - Add success summary emission with service/workflow/job URLs and trimmed output (`MAX_SUCCESS_OUTPUT_CHARS`).
> - **Node smoke script (`scripts/mcp-ci-tests.mjs`)**:
>   - Add detailed structured logging (info/warn/error), HTTP request/response capture (headers, base64 bodies), RPC/tool/conversation logs, and body truncation (`MCP_LOG_BODY_MAX_CHARS`).
>   - Improve JSON/error handling and retry messages; write structured log to `MCP_LOG_JSON` on success/failure.
> - **Tests (`packages/github-actions-deploy/tests/test_smoke_scaffolding.py`)**:
>   - Add tests for success summary output and truncation behavior.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 059400ec4593e2b310435190a7fa10af96028fa0. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->
## Summary by CodeRabbit

* **New Features**
  * Smoke test logs now include a c
