# PR #243: feat: enable claude-copilot workflow for self-hosted runner

**Repo:** jleechanorg/ai_universe
**Merged:** 2025-10-11
**Author:** jleechan2015
**Stats:** +381/-82 in 8 files

## Summary
(none)

## Raw Body
Enables the claude-copilot workflow that was previously disabled. All prerequisites verified: ~/.claude exists, OAuth token configured, Docker available.

### Goal
- Ensure the Claude Copilot runner can autonomously execute `/copilot`, commit any fixes it produces, and push those updates back to the PR with no manual intervention.

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Replaces the disabled copilot job with a containerized FixPR workflow that can commit/push changes, plus new Docker image, more robust CLI wrapper/SDK loading, and tolerant health/test utilities.
> 
> - **CI/Workflow (`.github/workflows/claude-copilot.yml`)**:
>   - Replace disabled `claude-copilot` with enabled `fixpr` job; add `workflow_dispatch` input, `permissions: write-all`, full checkout, git identity config, and initial HEAD capture.
>   - Run `/fixpr` via SDK container (custom Dockerfile), validate Claude auth, and determine safe push target (skip protected refs) with conditional commit/push logic.
> - **Container Runtime**:
>   - Add `docker/claude-runner.Dockerfile` installing `@anthropic-ai/claude-code` and `@modelcontextprotocol/sdk`.
> - **CLI Wrapper (`scripts/claude-cli-wrapper.sh`)**:
>   - Support external Dockerfile, rebuild args, CI simulation flags, forwarded git identity, and container env markers.
>   - Allow token-based auth when no local creds; gate container commits; improved Docker build path and env wiring.
> - **SDK Runner (`scripts/claude-sdk-runner.mjs`)**:
>   - Broaden module resolution to include `sdk.mjs` from workspace/global paths.
> - **Health/Test Utilities**:
>   - `testing_llm/server-health.js`: default port to `2000`; include `serverRunning` in critical checks; refined status evaluation.
>   - `testing_llm/server-manager.js`: register Claude MCP server; accept DEGRADED as ready; inject multiple API keys from env/defaults.
>   - `testing_llm/test-runner.js`: treat DEGRADED as pass, add skipped-tests/CI-simulation handling, improved final report metrics.
> - 
