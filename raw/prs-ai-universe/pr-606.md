# PR #606: Fix API schema limit and re-enable token tests

**Repo:** jleechanorg/ai_universe
**Merged:** 2025-11-09
**Author:** jleechan2015
**Stats:** +20/-22 in 3 files

## Summary
(none)

## Raw Body
This commit addresses two high-priority issues:

1. **Restore conversation.get-history limit from 100 to 1000**
   - Fix: backend/src/agents/ConversationAgent.ts:11,18
   - Updated listConversationsSchema and historySchema max limit from 100 to 1000
   - Restores compatibility with downstream consumers (Convo UI, integration tests) that request ≥200 messages for long-running conversations
   - Updated test: ConversationAgent.test.ts to validate new 1000 limit

2. **Re-enable TokenCentralization test suite**
   - Fix: backend/src/test/TokenCentralization.test.ts:10
   - Changed describe.skip to describe to execute 30+ token tests
   - Ensures regressions in resolveSafeOutputLimit and provider configs are caught before reaching CI/production
   - All 27 tests now passing

Breaking change from PR #603 has been reverted to maintain backward compatibility with existing consumers.

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Increases conversation list/history max limit to 1000 and re-enables the TokenCentralization test suite.
> 
> - **Backend/API**:
>   - Increase pagination `limit` max from `100` to `1000` in `ConversationAgent` schemas (`listConversationsSchema`, `historySchema` in `backend/src/agents/ConversationAgent.ts`).
> - **Tests**:
>   - Update `backend/src/test/ConversationAgent.test.ts` to validate new 1000 limit (reject `1001`, accept `1000`, `100`).
>   - Re-enable `backend/src/test/TokenCentralization.test.ts` by removing `describe.skip`.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 185316ffeb7133fedc969301c28f541ea91948f4. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->

## Summary by CodeRabbit

* **Improvements**
  * Increased maximum results limit to 1,000 for conversation lists and history requests (previously 100), allowing retrieval
