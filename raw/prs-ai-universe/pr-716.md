# PR #716: feat: align model context windows

**Repo:** jleechanorg/ai_universe
**Merged:** 2025-11-13
**Author:** jleechan2015
**Stats:** +26/-11 in 2 files

## Summary
- add provider-specific context token limits for every configured model so we no longer cap requests at 50k tokens
- plumb the new limits through the ConfigManager output (including OpenAI) so backend tools inherit the larger contexts automatically
- update the ConfigManager test suite to lock in the new limits

## Raw Body
## Summary
- add provider-specific context token limits for every configured model so we no longer cap requests at 50k tokens
- plumb the new limits through the ConfigManager output (including OpenAI) so backend tools inherit the larger contexts automatically
- update the ConfigManager test suite to lock in the new limits

## Testing
- cd shared-libs/packages/config-utils && npm run test

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Introduces provider-specific context token limits and wires them into all model configs (including OpenAI), updating types and tests accordingly.
> 
> - **Config (config-utils/src/ConfigManager.ts)**:
>   - Add `PROVIDER_CONTEXT_TOKEN_LIMITS` and `getProviderContextLimit()`.
>   - Apply per-provider `maxInputTokens` for `cerebras`, `claude`, `gemini`, `grok`, `perplexity`, and `openai`.
>   - Extend `AppConfig.models.openai` to include `maxInputTokens`.
> - **Tests (config-utils/tests/ConfigManager.test.ts)**:
>   - Update assertions to expect provider-specific `maxInputTokens` values and presence of `openai` model.
>   - Keep existing checks for `maxOutputTokens`, legacy `maxTokens`, and web/search parameters.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 5e61e883e99f1e4a689b392a6c9c254db7f0c585. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
