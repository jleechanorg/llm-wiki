# PR #194: test: expand coverage for mcp server utils

**Repo:** jleechanorg/ai_universe
**Merged:** 2025-10-07
**Author:** jleechan2015
**Stats:** +556/-18 in 7 files
**Labels:** codex

## Summary
- add coverage for the server factory, lifecycle manager, tool helpers, and HTTP proxy utilities
- adjust the port discovery test to simulate persistent failures through the error event
- bump the package version to 1.0.1 for publication

## Raw Body
## Summary
- add coverage for the server factory, lifecycle manager, tool helpers, and HTTP proxy utilities
- adjust the port discovery test to simulate persistent failures through the error event
- bump the package version to 1.0.1 for publication

## Testing
- npm test -- --runInBand
- npm run build

------
https://chatgpt.com/codex/tasks/task_e_68e4216783d8832f9f253e4fd8abe3f8

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Adds extensive tests across MCP server utils (factory, lifecycle, tools, HTTP proxy, port discovery) and bumps the package to 1.0.1.
> 
> - **Tests**:
>   - `ServerFactory` and `createHealthCheckTool`: server init with default/custom instructions; tool/agent registration; health tool payload.
>   - `ServerLifecycleManager` and `setupGracefulShutdown`: start/stop state, error hooks, signal handlers, exit codes.
>   - `ToolHelpers`: `createEchoTool`, `createVersionTool`, `createStatusTool`, `withErrorHandling`, `withLogging` behaviors.
>   - `startFastMcpHttpProxy`: Express setup, CORS support, proxy wiring, error handling/logging, return shape.
>   - `findAvailablePort`: simulate persistent failures via `error` events, bounds, timing, consecutive calls.
> - **Version**: bump `@ai-universe/mcp-server-utils` to `1.0.1` (`package.json`, `package-lock.json`).
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit c4d250bdf928e5accda5b3496ecb725af7090f55. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->

## Summary by CodeRabbit

* **Tests**
  * Added comprehensive coverage for MCP server creation, tool utilities (echo, version, status, error handling, logging), server lifecycle and graceful shutdown, HTTP proxy startup with CORS and error handling, and port availability error paths to improve reliability.
* **Chores**
  * Bumped package version from 1.0.0 to 1
