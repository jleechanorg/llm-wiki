# PR #678: chore: document backend auto deploy skipping ci

**Repo:** jleechanorg/ai_universe
**Merged:** 2025-11-13
**Author:** jleechan2015
**Stats:** +18/-6 in 2 files

## Summary
- add explicit workflow permissions and summary note so it is clear dev auto deploy runs without CI gating

## Raw Body
## Summary
- add explicit workflow permissions and summary note so it is clear dev auto deploy runs without CI gating

## Testing
- not run (workflow yaml change only)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Explicitly sets workflow permissions, adds a note that dev auto-deploy skips CI gating, and makes production deploy reason optional with consistent handling across steps.
> 
> - **Workflows**:
>   - `.github/workflows/auto-deploy.yml`:
>     - Add top-level `permissions: contents: read`; remove redundant `contents` under job, keep `issues: write`.
>     - Deployment summary now includes "CI Gate: skipped (auto-deploy triggers immediately on push)".
>   - `.github/workflows/deploy-production.yml`:
>     - `workflow_dispatch.inputs.reason` made optional with default `''`.
>     - Use `DEPLOY_REASON` env and fallback `REASON` for consistent display in summary, complete, and failure steps.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 34cf75c26772bdc9d04db782452001f9d863a59e. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->
## Summary by CodeRabbit

* **Chores**
  * Adjusted workflow permissions: moved repository read permission to top-level and removed redundant job-level read permission.
  * Added a CI gate "skipped" line to deployment summaries.
  * Made deployment reason optional with a default; introduced a normalized reason value and environment variable to show a consistent "(not provided)" message across summary and failure notifications.
<!-- end of auto-generated comment: release notes by coderabbit.ai -->
