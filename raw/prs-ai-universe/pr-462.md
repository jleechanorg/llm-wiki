# PR #462: fix: remove synthetic 'Conversation started' messages and add auto-dependency installation

**Repo:** jleechanorg/ai_universe
**Merged:** 2025-10-31
**Author:** jleechan2015
**Stats:** +298/-135 in 10 files

## Summary
- Removes synthetic "Conversation started: {title}" messages that were causing duplicate conversation entries in the UI
- Makes `initialMessage` required for all new conversations
- Deprecates `startConversation()` method to prevent future synthetic message creation
- Adds automatic dependency check and installation to `run-local-server.ts`

## Raw Body
## Summary
- Removes synthetic "Conversation started: {title}" messages that were causing duplicate conversation entries in the UI
- Makes `initialMessage` required for all new conversations
- Deprecates `startConversation()` method to prevent future synthetic message creation
- Adds automatic dependency check and installation to `run-local-server.ts`

## Changes
### Backend
- **ConversationAgent.ts**: Made `initialMessage` required in schema, throws error if not provided
- **ConversationMCPTool.ts**: Deprecated `startConversation()` method with clear error message
- **Tests**: Updated all tests to provide `initialMessage` and verify deprecation behavior

### Development Tools
- **run-local-server.ts**: Added automatic `npm install` check based on `node_modules` existence and `package.json` modification time

## Problem Fixed
Users were seeing duplicate conversation entries in the sidebar:
1. "Conversation started: test"
2. "test"

This was caused by the backend creating a synthetic message when conversations were initialized.

## Solution
- Removed synthetic message creation entirely
- All conversations must now start with an actual user or assistant message
- Frontend no longer needs to filter synthetic messages

## Testing
- ✅ All unit tests pass
- ✅ ConversationAgent tests updated for new behavior
- ✅ ConversationMCPTool tests verify deprecation
- ✅ Linting and type checking pass

## Frontend Action
No changes needed! The duplicate conversations will disappear once this is deployed.

## Next Steps
Need to delete existing synthetic messages from the database (separate task).

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Removes synthetic conversation-start messages by deprecating `startConversation`, requires an explicit `initialMessage`, switches creation flows to `sendMessage`, and auto-installs backend deps when running locally.
> 
> - **Backend**
>   - **Conversations**: Require `initialMessage` in `
