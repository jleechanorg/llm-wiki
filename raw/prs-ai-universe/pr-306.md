# PR #306: fix: skip optional npm deps to prevent bazelisk cleanup errors

**Repo:** jleechanorg/ai_universe
**Merged:** 2025-10-13
**Author:** jleechan2015
**Stats:** +1367/-121 in 10 files
**Labels:** codex

## Summary
- disable optional npm dependencies in the repo and backend packages so Bazelisk binaries are never installed during CI cleanup
- copy the backend .npmrc into Docker build contexts and document the behavior for developers

## Raw Body
## Summary
- disable optional npm dependencies in the repo and backend packages so Bazelisk binaries are never installed during CI cleanup
- copy the backend .npmrc into Docker build contexts and document the behavior for developers

## Testing
- npm ci
- npm ci --prefix backend

------
https://chatgpt.com/codex/tasks/task_e_68ec43e113f8832fbf17f738a7cd8d72

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Disable optional npm deps and adjust CI/Docker to prevent bazelisk EACCES failures; refine config-utils public exports and add targeted tests and docs.
> 
> - **CI/Build**:
>   - **NPM behavior**: Add repo-wide `.npmrc` and `backend/.npmrc` with `omit=optional` to skip `@bazel/bazelisk`.
>   - **Workflow hardening**: In `/.github/workflows/pr-dev-preview.yml`, clear `node_modules` before `npm ci` in smoke tests.
>   - **Script**: Add `scripts/test-ci-environment.sh` to reproduce CI install scenarios and EACCES errors in Docker.
> - **Docker**:
>   - Copy `.npmrc` into both build and production stages in `backend/Dockerfile` to ensure optional deps remain skipped during image builds.
> - **Shared Libraries (`@ai-universe/config-utils`)**:
>   - Restructure `src/index.ts` exports to explicitly forward constants/types from `SecondOpinionConfig` and avoid `export *` evaluation issues.
>   - Add tests `tests/indexExports.test.ts` validating token-limit constants and `resolveSafeOutputLimit` behavior.
> - **Docs**:
>   - Add `.github/REPLICATING_CI.md` with local CI replication steps; update `README.md` to note optional deps are disabled by default and how to include them.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 886b1a9a0e25e640f67a62813542117f1bdfac10. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->
## Summary by CodeRabbit

* **Chores**
  * Standardized npm config t
