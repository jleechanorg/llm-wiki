# PR #365: chore: hash shared-libs builds and drop dist artifacts

**Repo:** jleechanorg/ai_universe
**Merged:** 2025-10-16
**Author:** jleechan2015
**Stats:** +51/-44 in 5 files

## Summary
- Implement content-hash-based build detection for shared-libs (replaces mtime checking)
- Remove dist/ artifacts from git (added to .gitignore)
- Add automatic rebuild triggers via npm prepare scripts
- Centralize hash computation in shared utility script

## Test Plan
- [x] Pre-push CI checks passed (type-check, lint, build, unit tests)
- [x] Build system validates shared-libs before backend compilation
- [x] Docker deployment verified with staged artifacts

🤖 Generated with [Claude Code](https://claude.com/claude-code)

Co-Authored-By: Claude <noreply@anthropic.com>

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Forces clean shared-lib rebuilds in CI and scripts (removing dist, honoring FORCE_REBUILD), makes hashing conditional on src presence, and updates rate-l

## Raw Body
## Summary
- Implement content-hash-based build detection for shared-libs (replaces mtime checking)
- Remove dist/ artifacts from git (added to .gitignore)
- Add automatic rebuild triggers via npm prepare scripts
- Centralize hash computation in shared utility script

## Changes
- **Build System**: Content-hash-based staleness detection using SHA256
- **Git Hygiene**: Remove dist/ from version control (prevents merge conflicts)
- **CI/CD**: Force rebuild in CI environment, use pre-built packages in Docker
- **Testing**: Update rate limit tests to use dynamic limit discovery

## Technical Details
- Hash computation matches prepare-shared-libs.mjs algorithm
- Shared helper script: `scripts/compute-directory-hash.mjs`
- Docker build uses pre-built packages (dist-only, no src/)
- Verification script ensures staging integrity

## Test Plan
- [x] Pre-push CI checks passed (type-check, lint, build, unit tests)
- [x] Build system validates shared-libs before backend compilation
- [x] Docker deployment verified with staged artifacts

🤖 Generated with [Claude Code](https://claude.com/claude-code)

Co-Authored-By: Claude <noreply@anthropic.com>

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Forces clean shared-lib rebuilds in CI and scripts (removing dist, honoring FORCE_REBUILD), makes hashing conditional on src presence, and updates rate-limit tests to discover limits dynamically.
> 
> - **CI**:
>   - Force rebuild shared libraries by setting `FORCE_REBUILD=true` in `backend-coverage.yml` for prepare/rebuild steps on both base and PR.
> - **Build Scripts**:
>   - `backend/scripts/prepare-shared-libs.mjs`: clean rebuild by removing `dist/` when `needsBuild`; add `rmSync`.
>   - `shared-libs/scripts/run-build-if-ready.mjs`: write `.src-hash` only when `src/` exists (skip when missing).
>   - `scripts/compute-directory-hash.mjs`: simplify entry sort comparator.
> - **Tests**:
>   - `backend/src/test/RateLimitTools.test.ts`: derive limits from runtime responses, use a shared `context
