# PR #705: ci: add manual backend deploy workflow

**Repo:** jleechanorg/ai_universe
**Merged:** 2025-11-13
**Author:** jleechan2015
**Stats:** +21/-0 in 1 files

## Summary
- add a thin workflow_dispatch wrapper so we can manually trigger backend dev deploys without depending on the reusable workflow metadata cache
- wrapper just forwards inputs to the existing deploy-dev.yml so auto-deploy keeps using the same steps

## Raw Body
## Summary
- add a thin workflow_dispatch wrapper so we can manually trigger backend dev deploys without depending on the reusable workflow metadata cache
- wrapper just forwards inputs to the existing deploy-dev.yml so auto-deploy keeps using the same steps

## Testing
- not run (workflow-only change)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Add a manual `workflow_dispatch` wrapper to trigger backend dev deploy via the existing reusable workflow.
> 
> - **CI/GitHub Actions**:
>   - **New manual deploy workflow**: Adds `/.github/workflows/deploy-dev-manual.yml` using `workflow_dispatch` to trigger backend dev deploys.
>     - Delegates to reusable `./.github/workflows/deploy-dev.yml`.
>     - Forwards `reason` and `ref_name`; inherits secrets; sets `contents: read` permissions.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 65622adce62410c0790752f68285fd1e686ce07c. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
