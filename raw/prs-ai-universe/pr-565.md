# PR #565: refactor: consolidate conversation persistence on sendMessage

**Repo:** jleechanorg/ai_universe
**Merged:** 2025-11-05
**Author:** jleechan2015
**Stats:** +355/-232 in 9 files
**Labels:** codex

## Summary
- route all conversation persistence through ConversationMCPTool.sendMessage and remove the redundant addMessage helper
- update ConversationAgent, SecondOpinionAgent, and SingleModelChatService to send both user and assistant messages via sendMessage
- refresh mocks and Jest suites to reflect the unified API surface

## Raw Body
## Summary
- route all conversation persistence through ConversationMCPTool.sendMessage and remove the redundant addMessage helper
- update ConversationAgent, SecondOpinionAgent, and SingleModelChatService to send both user and assistant messages via sendMessage
- refresh mocks and Jest suites to reflect the unified API surface

## Testing
- npm run test -- --runTestsByPath backend/src/test/SingleModelChatService.test.ts backend/src/test/ConversationAgent.test.ts backend/src/test/ConversationMCPTool.test.ts backend/src/agents/__tests__/SecondOpinionAgent.conversation.test.ts *(fails: Missing script "test")*

------
https://chatgpt.com/codex/tasks/task_e_690b0f7e2240832fa56182a92ec277b7

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Route all conversation writes through `ConversationMCPTool.sendMessage`, removing `addMessage`, and update agents/services/tests to the unified API with improved response/metadata handling.
> 
> - **Backend**:
>   - **ConversationMCPTool**: Remove `addMessage`; expand `sendMessage` to accept `{ userId, content, role, conversationId?, title?, metadata? }` and return normalized `ConversationMessageResponse` with `conversationId`, `messageId`, `sequence`, `title`.
>   - **Agents**:
>     - `ConversationAgent`: `conversation.add-message` now uses `sendMessage` for non-user roles; existing user flow still delegates to `SingleModelChatService`.
>     - `SecondOpinionAgent`: Use `sendMessage` for persisting user and assistant messages (including synthesis metadata); keep `assistantMessage` in final payload.
>   - **Services**:
>     - `SingleModelChatService`: Persist both user and assistant messages via `sendMessage`; unchanged prompt/history logic.
> - **Tests/Mocks**:
>   - Update all mocks and expectations to `sendMessage` (remove `addMessage` paths).
>   - Refresh fixtures to include new fields (`conversationId`, `messageId`, `sequence`, `title`) and metadata assertions.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbo
