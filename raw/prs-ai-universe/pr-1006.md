# PR #1006: fix: default OpenAI secondary to gpt-5-nano

**Repo:** jleechanorg/ai_universe
**Merged:** 2026-03-09
**Author:** jleechan2015
**Stats:** +77/-124 in 17 files

## Summary
- switch the default OpenAI secondary model from `gpt-5-mini` to `gpt-5-nano`
- update cost/display aliases so OpenAI defaults stay internally consistent
- keep non-PR Cloud Run deployments warm with `minScale=1`
- rotate and validate the Gemini API key in GCP Secret Manager

## Raw Body
## Summary
- switch the default OpenAI secondary model from `gpt-5-mini` to `gpt-5-nano`
- update cost/display aliases so OpenAI defaults stay internally consistent
- keep non-PR Cloud Run deployments warm with `minScale=1`
- rotate and validate the Gemini API key in GCP Secret Manager

## Why
- direct dev MCP test of `compare grok llm vs chatgpt` with default params showed `gpt-5-mini` as the slowest model leg at `41.914s`
- the same trace completed in `45.374s` end-to-end because `parallel_secondary_responses` waited on that OpenAI call
- Cloud Run dev/prod also showed cold-start penalties before the `minScale=1` change

## Evidence
- Cloud Run investigation: `/tmp/ai_universe/worktree_latency2/evidence/latency_logs_20260308_181944`
- Direct dev MCP timing: `/tmp/ai_universe/worktree_latency2/evidence/direct_dev_query_20260308_195559`
- Gemini key validation + Secret Manager rotation: `/tmp/ai_universe/worktree_latency2/evidence/gemini_key_rotation_20260308_200240`
- Local server latency run: `/tmp/ai_universe/worktree_latency2/evidence/local_dev_query_fixed_20260308_200808`

## Testing
- `cd backend && npm test -- --runInBand --runTestsByPath src/test/CostCalculator.test.ts src/agents/__tests__/SecondOpinionAgent.variableScope.test.ts`
- `cd backend && npm run type-check`
- `./run_local_server.sh --force-default-port` plus authenticated MCP request using default params for `compare grok llm vs chatgpt`
- `git push -u origin codex/gpt5-nano-openai-default` (pre-push hook ran lint/build/unit checks)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Behavior changes in model selection/validation (OpenAI no longer allowed as secondary) and Cloud Run scaling could affect request routing and operational cost/performance; core logic changes are otherwise small and covered by updated tests.
> 
> **Overview**
> Updates OpenAI defaults to use **GPT-5 Nano** (config default, cost pricing, and display-name aliases), and adds explicit pricing/normalization coverage 
