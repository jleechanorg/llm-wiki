# PR #917: Add abort-aware streaming retries for Cerebras

**Repo:** jleechanorg/ai_universe
**Merged:** 2025-12-06
**Author:** jleechan2015
**Stats:** +8/-3 in 1 files
**Labels:** codex

## Summary
- propagate AbortSignal from Cerebras streaming options into the retry loop so rate-limit/network backoff can be cancelled
- pass the signal to httpClient.fetch and delayWithAbort for streaming calls to align behavior with non-streaming requests

## Raw Body
## Summary
- propagate AbortSignal from Cerebras streaming options into the retry loop so rate-limit/network backoff can be cancelled
- pass the signal to httpClient.fetch and delayWithAbort for streaming calls to align behavior with non-streaming requests

## Testing
- npm --prefix backend test -- CerebrasLLMTool.retry.test.ts *(fails: npm not available in container)*

------
[Codex Task](https://chatgpt.com/codex/tasks/task_e_693337833d88832f84611f30c7f23d45)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Propagates AbortSignal through Cerebras streaming to cancel fetch and exponential backoff during retries.
> 
> - **Backend - Cerebras streaming**:
>   - Accepts `options.signal` in `CerebrasCallOptions` and threads it through `callStream` → `internalCallStream`.
>   - Passes `signal` to `httpClient.fetch` and `delayWithAbort(...)` to make retries/backoff abortable.
>   - Keeps web search behavior unchanged; only streaming retry flow is now abort-aware.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit dfcfb18ea0207161ca800410008414a99b9b7d9a. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
