# PR #168: fix: stage shared libs in temp build context

**Repo:** jleechanorg/ai_universe
**Merged:** 2025-10-04
**Author:** jleechan2015
**Stats:** +49/-35 in 2 files
**Labels:** codex

## Summary
- stage shared library build outputs inside a temporary directory instead of copying them into backend/shared-libs
- revert repo ignore rules to match the cleaner workflow and document the temp-context behavior
- ensure the deployment script always cleans up the ephemeral build context after Cloud Build finishes

## Raw Body
## Summary
- stage shared library build outputs inside a temporary directory instead of copying them into backend/shared-libs
- revert repo ignore rules to match the cleaner workflow and document the temp-context behavior
- ensure the deployment script always cleans up the ephemeral build context after Cloud Build finishes

## Testing
- not run (not requested)

------
https://chatgpt.com/codex/tasks/task_e_68e17467379c832f9b3220511e4d6508

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Stages built shared libraries in a temporary build context for Cloud Build (with automatic cleanup) and updates docs to reflect docs-only `backend/shared-libs/`.
> 
> - **Deployment**:
>   - Uses a temporary build context (`mktemp`) for Docker/Cloud Build, rsyncs `backend/` into it, and submits builds from that path.
>   - Stages pre-built shared libs into `BUILD_CONTEXT_DIR/shared-libs/packages/*` (copy `package.json`, `README.md`, `dist/`; never `src/`).
>   - Adds cleanup trap to always remove the temporary context; updates logs and paths; adjusts async/sync `gcloud builds submit` to use the temp dir.
> - **Docs**:
>   - Update `backend/shared-libs/README.md` to “Docs Only,” explaining that artifacts are staged in a temporary context during deploy and not written to the repo.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 98817233f2a78fbcf954baa94e4a7f6f8465ecb5. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
