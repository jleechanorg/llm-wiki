# PR #891: fix: add fallbackMessage alias and stabilize second opinion test

**Repo:** jleechanorg/ai_universe
**Merged:** 2025-12-10
**Author:** jleechan2015
**Stats:** +74/-3 in 3 files
**Labels:** codex

## Summary
- add deprecated `fallbackMessage` alias to `ChatCompletionConfig` and honor it when deriving error messages
- restore deterministic second-opinion CI question to avoid time-sensitive responses

## Raw Body
## Summary
- add deprecated `fallbackMessage` alias to `ChatCompletionConfig` and honor it when deriving error messages
- restore deterministic second-opinion CI question to avoid time-sensitive responses

## Testing
- `npm test -- --runTestsByPath shared-libs/packages/convo-agent/tests/ChatCompletionService.test.ts` *(fails: repository has no "test" script)*

------
[Codex Task](https://chatgpt.com/codex/tasks/task_e_692ed2a03bcc832fbf697f611b636d32)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Adds tests for deprecated fallbackMessage and stub fallback diagnostics, and updates MCP CI to validate Grok mode via an allowlist (on/auto).
> 
> - **Tests**:
>   - `backend/src/test/SingleModelChatService.test.ts`: Add test ensuring stub mode fallback sets `metadata.webSearchDiagnostics.stubFallback=true`, plus `usedFallback` and `hasError` flags.
>   - `shared-libs/packages/convo-agent/tests/ChatCompletionService.test.ts`: Add test that uses deprecated `fallbackMessage` when `errorMessage` is absent.
> - **CI/Diagnostics**:
>   - `scripts/mcp-ci-tests.mjs`: Simplify Grok `search_parameters.mode` validation using an allowlist `['on','auto']` (still enforcing both as valid).
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit d5d5460bfc80d966e46f590ae2f29bcb7a947c83. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
