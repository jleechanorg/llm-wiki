# PR #154: Remove CORS config from deploy.sh, rely on code wildcard

**Repo:** jleechanorg/ai_universe
**Merged:** 2025-10-04
**Author:** jleechan2015
**Stats:** +18/-19 in 2 files

## Summary
- Removed redundant CORS_ALLOWED_ORIGINS environment variable from deploy.sh
- Code already has wildcard CORS configuration in server.ts that handles all deployed frontends

## Raw Body
## Summary
- Removed redundant CORS_ALLOWED_ORIGINS environment variable from deploy.sh
- Code already has wildcard CORS configuration in server.ts that handles all deployed frontends

## Details
The backend code (server.ts:79-83) already has a wildcard CORS configuration that matches `https://ai-universe-frontend*.run.app`, which works for all deployments (dev/staging/prod). The deploy.sh script was duplicating this configuration unnecessarily.

## Changes
- Removed CORS_ORIGINS variable assignments for all environments (dev, staging, prod, pr)
- Removed CORS_ORIGINS_OVERRIDE check
- Removed CORS_ALLOWED_ORIGINS from environment variables YAML

## Testing
- All presubmit checks passed (type-check, lint, build, tests)
- CORS configuration now relies solely on code-level wildcard patterns

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Centralizes CORS handling in code using a wildcard run.app pattern and updates deploy.sh to pass optional CORS_EXTRA_ORIGINS (e.g., localhost), removing prior per-environment origins and overrides.
> 
> - **CORS Configuration**:
>   - **Server (`backend/src/server.ts`)**:
>     - Default allowed origins now start with wildcard `https://ai-universe-frontend*.*.run.app`.
>     - Reads additional origins from `CORS_EXTRA_ORIGINS`; falls back to adding `http://localhost:3000` if unspecified.
>   - **Deployment (`deploy.sh`)**:
>     - Replaces per-environment `CORS_ORIGINS` with `CORS_EXTRA_ORIGINS` (mainly localhost entries).
>     - Removes `CORS_ORIGINS_OVERRIDE` handling.
>     - Updates env vars file to set `CORS_EXTRA_ORIGINS` instead of `CORS_ALLOWED_ORIGINS`.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit d33ecc065e7b4d36b196f65ca5ef6c7e47879b47. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release note
