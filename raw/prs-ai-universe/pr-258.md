# PR #258: fix: type express handlers for cloud deploy

**Repo:** jleechanorg/ai_universe
**Merged:** 2025-10-10
**Author:** jleechan2015
**Stats:** +21/-6 in 1 files

## Summary
- import Express request/response/next types and annotate middleware
- type the health and root endpoints to satisfy strict TS for Cloud Build

## Raw Body
## Summary
- import Express request/response/next types and annotate middleware
- type the health and root endpoints to satisfy strict TS for Cloud Build

## Testing
- npm run build

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Types Express handlers, introduces APP_VERSION-based version reporting, enriches health/root responses, and refines /mcp Accept-header middleware.
> 
> - **Server (`backend/src/server.ts`)**
>   - **Versioning/Metadata**:
>     - Read `APP_VERSION` env with fallback and return in `GET /health` and `GET /`.
>     - `GET /health`: add `Cache-Control: no-store`, content type `application/health+json`, include `epochMs`.
>   - **Middleware (`/mcp`)**:
>     - Preserve original `Accept` as `x-original-accept` in non-production and log at debug with metadata.
>     - Continue appending `text/event-stream` when missing.
>   - **Type Safety**:
>     - Import and apply Express `Request`, `Response`, `NextFunction` types to middleware and route handlers.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 5a759b6ecf9550efb4ed8aa529e394507dd6ff6b. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->
## Summary by CodeRabbit

- New Features
  - Health and root endpoints now expose service version, timestamp, environment, transport, and set caching/no-store for more reliable diagnostics.
  - Adds debug-level logging of request Accept headers in non-production to aid troubleshooting.

- Refactor
  - Improved HTTP handler typings and middleware handling to increase reliability without changing user-facing behavior.
<!-- end of auto-generated comment: release notes by coderabbit.ai -->
