# PR #730: fix: guard auth bypass logic locally

**Repo:** jleechanorg/ai_universe
**Merged:** 2025-11-13
**Author:** jleechan2015
**Stats:** +15/-7 in 1 files
**Labels:** codex

## Summary
- ensure the RUN_LOCAL_SERVER_DISABLE_AUTH flag is only honored for non-production, non-Cloud Run environments and lazily construct the FirebaseAuthTool so local runs without Firebase credentials succeed
- keep injectAuthContext consistent by returning null when there are no arguments to trust during a bypass

## Raw Body
## Summary
- ensure the RUN_LOCAL_SERVER_DISABLE_AUTH flag is only honored for non-production, non-Cloud Run environments and lazily construct the FirebaseAuthTool so local runs without Firebase credentials succeed
- keep injectAuthContext consistent by returning null when there are no arguments to trust during a bypass

## Testing
- `cd backend && npm run build`


------
[Codex Task](https://chatgpt.com/codex/tasks/task_e_6916671ea3d4832fa9d72503891688ad)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Restricts auth bypass to true-local dev, lazily constructs/reuses FirebaseAuthTool, and refines injectAuthContext behavior when no arguments.
> 
> - **Backend (`backend/src/server.ts`)**:
>   - **Auth Bypass Guard**:
>     - Compute `isLocalAuthBypassEnabled` only when `RUN_LOCAL_SERVER_DISABLE_AUTH` is set and environment is not Cloud Run or production.
>   - **Firebase Auth Tool**:
>     - Add lazy initializer `ensureFirebaseAuthTool()` to instantiate and reuse `FirebaseAuthTool` on demand.
>     - Switch token verification to use the ensured instance.
>   - **Auth Context Injection**:
>     - In `injectAuthContext`, when bypass is enabled and no `params.arguments`, return `null` instead of echoing the body; otherwise trust supplied context.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 3a071ac01388adaf5fe5a3fe481446f356a8f4f6. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
