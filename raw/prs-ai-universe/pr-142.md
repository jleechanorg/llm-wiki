# PR #142: fix: allow Cloud Run preview domains for CORS

**Repo:** jleechanorg/ai_universe
**Merged:** 2025-10-02
**Author:** jleechan2015
**Stats:** +66/-2 in 4 files
**Labels:** codex

## Summary
- ensure the backend always whitelists Cloud Run PR preview frontends in its CORS configuration
- cover the preview domains in the CORS utility tests and improve null-origin handling
- fix the Jest configuration so ES module path mappings work during the tests
- add regression coverage for literal `"null"` origins when toggling `allowNullOrigin`

## Raw Body
## Summary
- ensure the backend always whitelists Cloud Run PR preview frontends in its CORS configuration
- cover the preview domains in the CORS utility tests and improve null-origin handling
- fix the Jest configuration so ES module path mappings work during the tests
- add regression coverage for literal `"null"` origins when toggling `allowNullOrigin`

## Testing
- NODE_OPTIONS=--experimental-vm-modules npm --prefix shared-libs/packages/mcp-server-utils test -- --runTestsByPath src/__tests__/createWildcardCorsOptions.test.ts

------
https://chatgpt.com/codex/tasks/task_e_68ded544b990832f83d30d27a5b64843

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Ensures CORS allows Cloud Run PR preview frontends and improves null-origin handling, with tests updated and Jest config fixed.
> 
> - **Backend**:
>   - Extend CORS `allowedOrigins` in `backend/src/server.ts` to always include Cloud Run PR preview patterns: `https://ai-universe-frontend-s*-*-*.a.run.app`.
> - **Utils**:
>   - Update `createWildcardCorsOptions` to treat `null` (in addition to `undefined` and literal `'null'`) as allowed when `allowNullOrigin` is true.
> - **Tests**:
>   - Add cases for literal `'null'` origin allow/deny toggling and for Cloud Run PR preview domains in `src/__tests__/createWildcardCorsOptions.test.ts`.
> - **Config**:
>   - Fix Jest ESM mapping key: rename `moduleNameMapping` to `moduleNameMapper` in `jest.config.js`.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit ae8bab6a877c094ca3ff7fbf87ee58b265acf898. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
