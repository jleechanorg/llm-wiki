# PR #130: fix(second-opinion): remove redundant secondary runs

**Repo:** jleechanorg/ai_universe
**Merged:** 2025-10-02
**Author:** jleechan2015
**Stats:** +165/-34 in 2 files
**Labels:** codex

## Summary
- avoid the initial secondary opinion fetch and reuse synthesis candidates so each request only triggers one set of secondary calls
- add a regression test that stubs config-utils and asserts executeStaggeredRequests runs exactly once

## Raw Body
## Summary
- avoid the initial secondary opinion fetch and reuse synthesis candidates so each request only triggers one set of secondary calls
- add a regression test that stubs config-utils and asserts executeStaggeredRequests runs exactly once

## Testing
- npm run test -- SecondOpinionAgent.latency.test.ts

------
https://chatgpt.com/codex/tasks/task_e_68de4e04f050832f856ed920ecec611f

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Defers secondary opinion requests to the synthesis phase with de-duplicated candidates and adds a regression test ensuring only one secondary run.
> 
> - **Backend (SecondOpinionAgent)**:
>   - Defer secondary opinion fetching to synthesis; remove initial secondary calls and record latency reason when skipped.
>   - Add `resolveCandidateSecondaryModels` to pick defaults or requested models, de-duplicate, and exclude the primary.
>   - Use candidate models for step2 (`synthesis_secondary_responses`) and set `secondaryResponses` to step2 successes.
>   - Enhance latency tracking with structured reasons (`deferred_to_synthesis`, `secondary_limit_zero`, `secondary_disabled`, `no_candidate_models`).
> - **Tests**:
>   - Add `SecondOpinionAgent.latency.test.ts` with tool stubs and config-utils stub to assert `executeStaggeredRequests` is called exactly once and validates per-model calls.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit a7804eadc57189d0bd1e1e90208f28fb2a13cd31. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->

## Summary by CodeRabbit

- New Features
  - Smarter selection of secondary models that avoids duplicates.
  - Secondary opinions are now gathered during synthesis for more consistent results.
  - Clearer status when secondary opinions are skipped or unavailable.

- Performance
  - Reduced redundant requests and more predicta
