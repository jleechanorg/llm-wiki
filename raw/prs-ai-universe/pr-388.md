# PR #388: refactor: centralize latency summary logging

**Repo:** jleechanorg/ai_universe
**Merged:** 2025-10-18
**Author:** jleechan2015
**Stats:** +216/-101 in 5 files
**Labels:** codex

## Summary
- centralize latency summary logging in the shared LatencyTracker with structured summaries and context allowlisting
- update SecondOpinionAgent to record cost/token metrics consistently and delegate summary logging to the shared helper
- document the latency artifact path and new summary helper usage in backend and library READMEs

## Raw Body
## Summary
- centralize latency summary logging in the shared LatencyTracker with structured summaries and context allowlisting
- update SecondOpinionAgent to record cost/token metrics consistently and delegate summary logging to the shared helper
- document the latency artifact path and new summary helper usage in backend and library READMEs

## Testing
- npm test -- --runTestsByPath src/__tests__/LatencyTracker.test.ts

------
https://chatgpt.com/codex/tasks/task_e_68f2a8650b50832f8ab9fb144dfa5599

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Centralizes latency summary logging in shared LatencyTracker (with context allowlisting) and updates SecondOpinionAgent to use it and consistently normalize cost/token metrics; docs/tests updated.
> 
> - **Shared libs (`@ai-universe/mcp-server-utils`)**:
>   - **LatencyTracker**: Add `buildSummaryLog`/`logSummary(status, { includeContextKeys, totals, ... })` with allowlisted context; introduce `LatencySummaryTotals`; default `tokens`/`cost` to `null` in logs/snapshots; keep `/tmp/<repo>/<branch>/<flow>-<timestamp>.json` persistence.
>   - **Tests**: New summary logging test validating totals, redacted context, and slowest/longest metrics.
>   - **README**: Document new summary helper and updated persistence path.
> - **Backend**:
>   - **SecondOpinionAgent**: Replace manual latency summary assembly with `latencyTracker.logSummary`; add `LATENCY_SUMMARY_CONTEXT_KEYS`; normalize cost via `normalizeCost` and use throughout (model calls, stages, outputs).
>   - **README**: Update latency artifact path to `/tmp/<repo>/<branch>/<flow>-<timestamp>.json`.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 2413f63e168d0d91b06cf1bf96262902be6b0e6b. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->

## Summary by CodeRabbit

* **New Features**
