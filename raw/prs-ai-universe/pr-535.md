# PR #535: fix: enforce ai universe firebase auth

**Repo:** jleechanorg/ai_universe
**Merged:** 2025-11-03
**Author:** jleechan2015
**Stats:** +109/-14 in 4 files

## Summary
- hardcode AI Universe Firebase project/domain in auth CLI and reject mismatched JWTs during login/refresh/token reads
- default setup helper inputs to AI Universe values and update docs with concrete exports and new guardrail note

## Raw Body
## Summary
- hardcode AI Universe Firebase project/domain in auth CLI and reject mismatched JWTs during login/refresh/token reads
- default setup helper inputs to AI Universe values and update docs with concrete exports and new guardrail note

## Testing
- ./run_tests.sh --unit-only

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Hardcodes AI Universe Firebase config in the auth CLI, validates JWT project/issuer on login/refresh/status, adds project metadata to saved tokens, and updates docs/setup script with AI Universe defaults.
> 
> - **Auth CLI (`scripts/auth-cli.mjs`)**:
>   - Enforces `ai-universe-b3551` via `EXPECTED_FIREBASE_PROJECT_ID`/`EXPECTED_FIREBASE_AUTH_DOMAIN` and config validation.
>   - Adds JWT parsing and project/issuer checks on `login`, `token` read, and `refresh` (rejects mismatches).
>   - Persists `firebaseProjectId` in saved token; shows project in `status` output.
>   - Auto-refresh now validates refreshed tokens before saving.
> - **Setup Script (`scripts/setup-firebase-config.sh`)**:
>   - Adds AI Universe default prompts for `FIREBASE_API_KEY`, `FIREBASE_AUTH_DOMAIN`, `FIREBASE_PROJECT_ID`.
> - **Docs (`AGENTS.md`, `CLAUDE.md`)**:
>   - Updates environment variable examples to AI Universe values.
>   - Notes project guardrail rejecting non-`ai-universe-b3551` tokens; minor formatting fix.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 32a7d1a2682a615a041204de9aa9ae90bbf75f67. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->

## Summary by CodeRabbit

* **New Features**
  * Added strict Firebase project validation to prevent token mismatches and provide clear error guidance.
  * Enhanced setup experience with pre-filled configuration defaults and validation for API keys and project settings.

* **Documentation**
  * Updated environment varia
