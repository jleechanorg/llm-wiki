# PR #241: fix: recover repo slug when metadata is missing

**Repo:** jleechanorg/ai_universe
**Merged:** 2025-10-09
**Author:** jleechan2015
**Stats:** +193/-0 in 1 files
**Labels:** codex

## Summary
- capture the repository root/dirname so the Codex setup script can infer the slug contextually
- add a GitHub API fallback that looks up repositories accessible to the token when no remote or metadata is available

## Raw Body
## Summary
- capture the repository root/dirname so the Codex setup script can infer the slug contextually
- add a GitHub API fallback that looks up repositories accessible to the token when no remote or metadata is available

## Testing
- bash -n scripts/setup_codex_git.sh
- GITHUB_REPOSITORY='' scripts/setup_codex_git.sh


------
https://chatgpt.com/codex/tasks/task_e_68e5f269ac34832fb028baeeac4834f6

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Adds `scripts/setup_codex_git.sh` to configure a token-authenticated GitHub remote, with repo slug inferred from remote, metadata, or GitHub API.
> 
> - **Scripts**:
>   - Add `scripts/setup_codex_git.sh` to configure a GitHub remote using a personal access token (`--token`, `--repo`, `--remote-name`, `--store-credentials`).
>   - Sets/creates `git` remote URL as `https://oauth2:TOKEN@github.com/OWNER/REPO.git`, optionally configures `credential.helper store`.
> - **Repo slug inference**:
>   - From existing remote URL (supports `git@github.com:` and `https://...` formats via `sanitize_url`).
>   - From `.pr-metadata.json` via a Python snippet (`repository_full_name`/`repo_full_name`/`repository`).
>   - From GitHub API: lists accessible repos with the token and matches by repo dirname.
> - **CLI/UX**:
>   - `--help` usage, argument validation, clear errors when token/slug is missing; idempotent remote update and masked output of token.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit e896d5d7857ca8c5a2f2f973aa01ad8927fc535e. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
