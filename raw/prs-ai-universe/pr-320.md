# PR #320: fix: refine GitHub remote sanitization

**Repo:** jleechanorg/ai_universe
**Merged:** 2025-10-13
**Author:** jleechan2015
**Stats:** +18/-2 in 1 files
**Labels:** codex

## Summary
- strip query and fragment parts from git@ GitHub remotes before building the sanitized path
- ensure fallback sanitization enforces github.com host and rejects empty or malformed paths

## Raw Body
## Summary
- strip query and fragment parts from git@ GitHub remotes before building the sanitized path
- ensure fallback sanitization enforces github.com host and rejects empty or malformed paths

## Testing
- bash -n scripts/setup_codex_git.sh

------
https://chatgpt.com/codex/tasks/task_e_68ed5a836680832f85100f89c84a7cd6

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Tightens `sanitize_url` to strip query/fragment from SSH remotes and enforce `github.com` host with valid paths in fallback handling.
> 
> - **scripts/setup_codex_git.sh**:
>   - **SSH (`git@github.com:`) handling**: Strip query (`?`) and fragment (`#`) and trailing slash before forming `github.com/<path>`.
>   - **Fallback normalization**: Require `github.com` host, reject empty/malformed paths, and emit normalized `github.com/<remainder>`.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 7a77cf58b215c14ae146c3d5ee49fa586fede208. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
