# PR #95: fix: ensure PR previews deploy to unique Cloud Run services

**Repo:** jleechanorg/ai_universe
**Merged:** 2025-10-01
**Author:** jleechan2015
**Stats:** +250/-32 in 3 files
**Labels:** codex

## Summary
- normalize the GitHub Actions preview service name so each PR deploys to a dedicated Cloud Run instance
- sanitize incoming service names to meet Cloud Run requirements while preserving the PR-specific suffix

## Raw Body
## Summary
- normalize the GitHub Actions preview service name so each PR deploys to a dedicated Cloud Run instance
- sanitize incoming service names to meet Cloud Run requirements while preserving the PR-specific suffix

## Testing
- python -m compileall packages/github-actions-deploy/github_actions_deploy/cli.py

------
https://chatgpt.com/codex/tasks/task_e_68db47940834832fb37b2d5c09fe807b

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Adds lifecycle labels and idle/expiry controls for PR previews, normalizes per-PR Cloud Run service names, and updates the cleanup script to hour-based logic using preview-expiry labels.
> 
> - **Deployments (`deploy.sh`)**:
>   - PR previews: set `--max-instances=1`, keep `--min-instances=0`, reduce deploy timeout to 10m.
>   - Add lifecycle controls via envs: `PREVIEW_CLEANUP_HOURS` (default 6) and `PREVIEW_IDLE_SHUTOFF_HOURS` (default 1) with validation.
>   - Apply labels `preview=true` and `preview-expiry=<UTC yyyymmddHHMMSS>` to preview services and print lifecycle info.
> - **GitHub Actions Deploy CLI (`packages/github-actions-deploy/github_actions_deploy/cli.py`)**:
>   - Ensure unique per-PR Cloud Run service names with sanitization (`ensure_preview_service_name`).
>   - Preview-friendly deploy defaults: `--min-instances 0`, `--max-instances 1`.
>   - Annotate preview services with cleanup labels and include lifecycle details (idle/expiry) in PR success comments.
>   - Robust env parsing for lifecycle settings; include `--project` when fetching service URL.
> - **Cleanup (`scripts/cleanup-gcp-stale-services.sh`)**:
>   - Switch to hour-based thresholds (`--hours`, `--warning-hours`); deprecate `--days` with conversion.
>   - Validate thresholds and ensure warning < cleanup.
>   - Read `metadata.labels.preview-expiry`; prioritize label-based expiry, fallback to age; update summaries accordingly.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit bc7644d3f6c78265c3511946bbbbd26c993aa340. This
