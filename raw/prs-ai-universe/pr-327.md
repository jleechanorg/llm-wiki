# PR #327: Address shared-libs automation feedback

**Repo:** jleechanorg/ai_universe
**Merged:** 2025-10-13
**Author:** jleechan2015
**Stats:** +31/-24 in 4 files
**Labels:** codex

## Summary
- ensure the shared-libs preparation script flags every missing dependency before installs
- switch backend config imports back to the package root instead of reaching into dist artifacts
- harden CI utility scripts by capturing docker exit codes/output and adding timeouts to runtime validation

## Raw Body
## Summary
- ensure the shared-libs preparation script flags every missing dependency before installs
- switch backend config imports back to the package root instead of reaching into dist artifacts
- harden CI utility scripts by capturing docker exit codes/output and adding timeouts to runtime validation

## Testing
- node backend/scripts/prepare-shared-libs.mjs
- npm test -- sharedLibRuntimeValidation.test.ts

------
https://chatgpt.com/codex/tasks/task_e_68ed74312e54832f913bf9a18277fcd3

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Collect and report all missing dependencies during shared-libs prep, switch backend config imports to package root, and harden CI/test scripts (cwd, timeout, Docker output handling).
> 
> - **Shared-libs preparation (`backend/scripts/prepare-shared-libs.mjs`)**:
>   - Collects all missing dependencies and reports them together instead of stopping at the first missing package.
> - **Backend config (`backend/src/config/llmDefaults.ts`)**:
>   - Switches imports from deep `dist` path to package root `@ai-universe/config-utils`, separating type imports.
> - **Tests/CI**:
>   - `backend/src/test/sharedLibRuntimeValidation.test.ts`: Resolve backend root via `process.cwd()` and add a `timeout` to `spawnSync`.
>   - `scripts/test-ci-environment.sh`: Use safer shell quoting; capture `npm install` output to reliably detect `EACCES` errors.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 6cae57a83a11cbf3628c61f623aa59c053359c22. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
