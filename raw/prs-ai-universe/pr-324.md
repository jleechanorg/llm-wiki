# PR #324: fix: align anonymous rate limit windows

**Repo:** jleechanorg/ai_universe
**Merged:** 2025-10-13
**Author:** jleechan2015
**Stats:** +9/-9 in 3 files
**Labels:** codex

## Summary
- align the default rate limit configuration to 5 anonymous and 50 authenticated requests per hour to match documented policy
- update runtime config defaults and regression expectations so multi-model usage enforces the hourly limits consistently

## Raw Body
## Summary
- align the default rate limit configuration to 5 anonymous and 50 authenticated requests per hour to match documented policy
- update runtime config defaults and regression expectations so multi-model usage enforces the hourly limits consistently

## Testing
- npm run test -- RateLimitTool

------
https://chatgpt.com/codex/tasks/task_e_68ed6cf69fc8832fad53d3835efaa069

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Aligns rate limit defaults to 5 anonymous and 50 authenticated requests per 1-hour window; updates tests accordingly.
> 
> - **Rate Limiting**:
>   - **Defaults**: Update production/test defaults to hourly windows (`60*60*1000`).
>     - `anonymous`: `5` req/hour (was `10` per 5 min in prod; `5` per 1 min in tests).
>     - `authenticated`: `50` req/hour (was `60` per 5 min in prod; `20` per 1 min in tests).
>     - `admin`: `1000` req/hour for defaults; dev env still uses admin limits per minute.
>   - **RuntimeConfigService**: Set `rateLimit.anonymous` to `{ requests: 5, windowMs: 1h }` and `authenticated.windowMs` to `1h`.
> - **Tests**:
>   - Adjust `RateLimitTool` regression to expect production anonymous `requests = 5`.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 44acb385b4be103b015b38dcf41b30f0df1b9a59. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->

## Summary by CodeRabbit

* **Chores**
  * Updated rate limiting defaults: anonymous users are now limited to 5 requests per hour (was 10 per 5 minutes). Authenticated users are now measured over a 1-hour window with a 50 requests/hour cap.
  * Overall rate limit windows standardized to 1 hour for consistency.
* **Tests**
  * Adjusted tests to reflect the new rate limit thresholds and windows.

<!-- end of auto-generated comment: release notes by coderabbit.ai -->
