# PR #335: fix: harden shared-lib prepare automation for CI

**Repo:** jleechanorg/ai_universe
**Merged:** 2025-10-13
**Author:** jleechan2015
**Stats:** +139/-5 in 6 files
**Labels:** codex

## Summary
- add a guard script that only rebuilds shared libraries when dependencies are installed and persists the source hash
- point each shared library package's prepare hook at the guarded helper to avoid failing npm ci lifecycles
- restore the test-environment authenticated rate limit default to 20 requests so CI expectations remain accurate

## Raw Body
## Summary
- add a guard script that only rebuilds shared libraries when dependencies are installed and persists the source hash
- point each shared library package's prepare hook at the guarded helper to avoid failing npm ci lifecycles
- restore the test-environment authenticated rate limit default to 20 requests so CI expectations remain accurate

## Testing
- ./run_tests.sh --unit-only

------
https://chatgpt.com/codex/tasks/task_e_68ed8106713c832fbfe3ca01a7e0925c

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Adds a guarded prepare script for shared libs and updates test authenticated rate limit to 20 requests/hour.
> 
> - **Shared Libraries**:
>   - **Prepare Hook Hardening**: Replace `prepare` scripts in `@ai-universe/{auth-utils,config-utils,firestore-utils,mcp-server-utils}` with `node ../../scripts/run-build-if-ready.mjs` to avoid running builds before deps are installed.
>   - **New Script**: Add `shared-libs/scripts/run-build-if-ready.mjs` to conditionally run `build` and write `dist/.src-hash`.
> - **Backend**:
>   - **Rate Limits (test defaults)**: In `backend/src/tools/RateLimitTool.ts`, change `authenticated` default from `50` to `20` requests per hour.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit d97cf2b03f4965a11be9e1dc8b6c2bcb21eb474e. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
