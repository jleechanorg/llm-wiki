# PR #226: fix: harden runner workflows and unblock multi-model synthesis tests

**Repo:** jleechanorg/ai_universe
**Merged:** 2025-10-09
**Author:** jleechan2015
**Stats:** +270/-40 in 15 files

## Summary
This PR strengthens the Claude CLI Docker wrapper and self-hosted runner infrastructure while fixing critical test failures in the multi-model opinion synthesis tool.

### Key Changes
1. **Claude Runner Infrastructure** - Hardened authentication, environment forwarding, and Docker integration
2. **Multi-Model Test Fixes** - Added missing `isOpenAIConfigured` mock and OpenAI/Grok tool support
3. **LLM Tool Improvements** - Enhanced error handling and token limit validation
4. **CI/CD Enhancements

## Raw Body
## Summary
This PR strengthens the Claude CLI Docker wrapper and self-hosted runner infrastructure while fixing critical test failures in the multi-model opinion synthesis tool.

### Key Changes
1. **Claude Runner Infrastructure** - Hardened authentication, environment forwarding, and Docker integration
2. **Multi-Model Test Fixes** - Added missing `isOpenAIConfigured` mock and OpenAI/Grok tool support
3. **LLM Tool Improvements** - Enhanced error handling and token limit validation
4. **CI/CD Enhancements** - Added runner validation workflows and improved auth checks
5. **Code Quality** - Removed unused imports and dependencies

---

## 🔧 Infrastructure & CI/CD (5 files)

### `.github/workflows/claude-copilot.yml`
- Added `workflow_dispatch` trigger for manual runs
- Enhanced runner environment normalization (HOME, CLAUDE_AUTH_PATHS, CLAUDE_COMMANDS_DIR)
- Implemented OAuth token fallback from runner config
- Added auth presence validation before execution
- Export PR and GitHub context variables
- Run wrapper with `--skip-tests` flag

### `.github/workflows/runner-validation.yml` ✨ NEW
- Workflow dispatch and push triggers on `claude_runner` branch
- Validates runner availability and environment
- Tests checkout and file listing capabilities

### `test-runner-job.yml` ✨ NEW
- Manual trigger workflow for runner testing
- Supports `claude-code`, `mac`, and `arm64` labels
- Echoes runner info and verifies repository access

### `scripts/claude-cli-wrapper.sh`
- **Authentication**: Replace `ANTHROPIC_API_KEY` with `CLAUDE_CODE_OAUTH_TOKEN`
- **Environment Forwarding**: Added `FORWARDED_ENV_VARS` with runtime notices
- **Auth Paths**: Support `~/.claude` and `CLAUDE_AUTH_PATHS`
- **Early Exit**: Fail fast when no reusable auth found
- **Docker Improvements**: Enhanced TTY handling, arg forwarding, path mapping
- **Container Setup**: Symlinks for `backend` and `shared-libs`, proper node_modules
- **Validation**: Warn if `COMMANDS_DIR` missing

### `scripts/shared-build-
