# PR #322: Fix stale shared-libs detection and rebuild automation

**Repo:** jleechanorg/ai_universe
**Merged:** 2025-10-13
**Author:** jleechan2015
**Stats:** +17/-9 in 3 files
**Labels:** codex

## Summary
- Guard the shared-libs preparation script against missing dependency maps while preserving recursive dependency resolution to keep installs reliable. 
- Clear `node_modules` before every `npm ci` invocation in the backend coverage workflow so cached file: tarballs cannot serve stale builds.
- Run the CI environment replication script’s apt installs in noninteractive mode and check specifically for the Bazelisk package directory to avoid false positives.

## Raw Body
## Summary
- Guard the shared-libs preparation script against missing dependency maps while preserving recursive dependency resolution to keep installs reliable. 
- Clear `node_modules` before every `npm ci` invocation in the backend coverage workflow so cached file: tarballs cannot serve stale builds.
- Run the CI environment replication script’s apt installs in noninteractive mode and check specifically for the Bazelisk package directory to avoid false positives.

## Testing
- node backend/scripts/prepare-shared-libs.mjs

------
https://chatgpt.com/codex/tasks/task_e_68ed661b4fe8832fb880e1f6e40f3558

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Handle missing dependency maps in shared-libs script, clear node_modules before npm ci in coverage workflow, and make CI test script noninteractive with precise Bazelisk check.
> 
> - **CI Workflows** (`.github/workflows/backend-coverage.yml`):
>   - Clear `node_modules` before `npm ci` for backend installs in base and PR runs to avoid stale artifacts.
> - **Shared Libraries Prep** (`backend/scripts/prepare-shared-libs.mjs`):
>   - Guard dependency aggregation by defaulting `dependencies`, `devDependencies`, and `peerDependencies` to `{}` to handle missing maps.
> - **CI Environment Test Script** (`scripts/test-ci-environment.sh`):
>   - Run apt in noninteractive mode and check specifically for `node_modules/@bazel/bazelisk` to avoid false positives.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit e7c7b778225d947706dc0cda470eac98be8d05f8. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
