# PR #84: fix: test VPC-SC async build solution for GitHub Actions

**Repo:** jleechanorg/ai_universe
**Merged:** 2025-09-29
**Author:** jleechan2015
**Stats:** +180/-83 in 2 files

## Summary
Testing VPC-SC async build solution to resolve GitHub Actions deployment failures.

### Problem
GitHub Actions shows "failure" even when deployments succeed due to VPC Service Controls preventing log streaming from `gcloud builds submit`.

### Solution
- Detects CI environment (`CI=true` or `GITHUB_ACTIONS=true`) 
- Uses async build with status polling instead of log streaming
- Avoids VPC-SC restrictions while maintaining deployment verification

### Key Changes
- **deploy.sh**: Added async bui

## Raw Body
## Summary
Testing VPC-SC async build solution to resolve GitHub Actions deployment failures.

### Problem
GitHub Actions shows "failure" even when deployments succeed due to VPC Service Controls preventing log streaming from `gcloud builds submit`.

### Solution
- Detects CI environment (`CI=true` or `GITHUB_ACTIONS=true`) 
- Uses async build with status polling instead of log streaming
- Avoids VPC-SC restrictions while maintaining deployment verification

### Key Changes
- **deploy.sh**: Added async build detection and status polling for CI environments
- **Workflow**: Re-enabled PR preview deployment workflow 
- **Timeout**: Added 15-minute build timeout for CI robustness

### Testing
This PR tests the async build solution in a real GitHub Actions environment.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Re-enables PR preview deploys, prebuilds shared libs, posts PR status comments, and adds CI-aware async Cloud Build with status polling in deploy.sh.
> 
> - **CI Workflow (`.github/workflows/pr-dev-preview.yml`)**:
>   - Re-enabled PR preview job for non-draft PRs (`if: github.event.pull_request.draft == false`).
>   - Added Node.js 20 setup with npm cache; prebuilds `shared-libs` packages.
>   - Switched deployment to `./deploy.sh pr <PR_NUMBER>` with image tag and increased timeout.
>   - Added PR comments: build started, deployment success (with service URL/health/MCP), and failure.
> - **Deployment Script (`deploy.sh`)**:
>   - Added CI-aware async `gcloud builds submit` with operation polling and 15m timeout to avoid VPC-SC log streaming issues; keeps sync build for local use.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit a45c0907c6b89b9fa5a7e27b51c68784269ada55. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
