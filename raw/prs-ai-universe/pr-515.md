# PR #515: feat: implement refresh token authentication and local MCP Inspector

**Repo:** jleechanorg/ai_universe
**Merged:** 2025-11-02
**Author:** jleechan2015
**Stats:** +507/-40 in 6 files

## Summary
This PR implements refresh token support for extended authentication sessions and creates a local MCP Inspector server with automatic token injection.

### Key Changes

**1. Refresh Token Authentication**
- ✅ Extended sessions (60+ days without re-authentication)
- ✅ Automatic token refresh using Firebase REST API
- ✅ New `refresh` command for manual token renewal
- ✅ Auto-refresh on `token` and `test` commands

**2. Local MCP Inspector Server**
- ✅ Automatic token injection from `~/.ai-universe

## Test Plan
- [x] Tested refresh token authentication flow
- [x] Verified automatic token refresh
- [x] Tested local MCP Inspector server
- [x] Verified token auto-injection
- [x] All pre-push checks passed (TypeScript, ESLint, Build, Unit Tests)

## Raw Body
## Summary

This PR implements refresh token support for extended authentication sessions and creates a local MCP Inspector server with automatic token injection.

### Key Changes

**1. Refresh Token Authentication**
- ✅ Extended sessions (60+ days without re-authentication)
- ✅ Automatic token refresh using Firebase REST API
- ✅ New `refresh` command for manual token renewal
- ✅ Auto-refresh on `token` and `test` commands

**2. Local MCP Inspector Server**
- ✅ Automatic token injection from `~/.ai-universe/auth-token.json`
- ✅ No manual copy-paste required
- ✅ Simple one-command startup: `npm run mcp-inspector`
- ✅ Serves on http://localhost:9000

**3. Documentation Updates**
- ✅ CLAUDE.md: Added refresh token and local MCP Inspector sections
- ✅ AGENTS.md: Updated with new authentication workflows
- ✅ Removed outdated MCP Inspector URL references (port 6274)

**4. Package Updates**
- ✅ Added `mcp-inspector` npm script
- ✅ All dependencies already installed

## Usage

```bash
# Authenticate once (lasts 60+ days)
node scripts/auth-cli.mjs login

# Start local MCP Inspector with auto-token injection
npm run mcp-inspector

# Open http://localhost:9000 - token automatically injected!
```

## Test Plan

- [x] Tested refresh token authentication flow
- [x] Verified automatic token refresh
- [x] Tested local MCP Inspector server
- [x] Verified token auto-injection
- [x] All pre-push checks passed (TypeScript, ESLint, Build, Unit Tests)

## Breaking Changes

None - this is purely additive functionality.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Adds refresh-token based auth with auto/manual refresh and a local MCP Inspector that auto-injects tokens, plus docs and npm script updates.
> 
> - **Auth CLI (`scripts/auth-cli.mjs`)**:
>   - Capture/store `refreshToken`; add `refreshIdToken` via Firebase REST.
>   - Auto-refresh on `token`/`test` commands; new `refresh` command.
>   - Callback now validates and saves 
