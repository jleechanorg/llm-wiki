# PR #146: feat: two step second opinion flow

**Repo:** jleechanorg/ai_universe
**Merged:** 2025-10-03
**Author:** jleechan2015
**Stats:** +377/-219 in 10 files
**Labels:** codex

## Summary
- add a reusable prepare-shared-libs script so backend commands install and build local packages on demand
- point backend TypeScript path aliases at the built shared libraries and skip latency persistence while running under Jest
- update the secondary prompt test expectations to reflect the streamlined two-step second-opinion flow

## Raw Body
## Summary
- add a reusable prepare-shared-libs script so backend commands install and build local packages on demand
- point backend TypeScript path aliases at the built shared libraries and skip latency persistence while running under Jest
- update the secondary prompt test expectations to reflect the streamlined two-step second-opinion flow

## Testing
- npm run build
- npm run test

------
https://chatgpt.com/codex/tasks/task_e_68dfa14c9230832fa05597067195fc22

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Refactors second-opinion to run secondary models in parallel and simplify synthesis, adds shared-libs prep script with path aliases, improves error/latency handling, and updates tests/deploy tooling.
> 
> - **Backend – Second Opinion Flow**:
>   - Run secondary model requests in parallel with the primary; collect via `Promise.allSettled` and validate responses.
>   - Simplify parsing by stripping legacy markers in `parseCombinedResponse` and remove embedded secondary-prompt generation from primary prompts.
>   - Generate final synthesis from the original question + primary + successful secondary responses; add skip reasons and robust error handling.
>   - Add `error?: boolean` to `LLMResponse` and include `secondaryFailureReason` in result metadata.
> - **Shared Libraries & Build**:
>   - Add `scripts/prepare-shared-libs.mjs` and wire it into all npm scripts (`dev`, `build`, `test`, etc.).
>   - Add TS path aliases to built outputs in `tsconfig.json`.
> - **Infra/Tooling**:
>   - `LatencyTracker.persist` no-op under Jest.
>   - Deployment: ensure only built `dist/` for shared-libs is copied; guard against copying `src/`.
>   - Docker/Dockerignore comments and minor adjustments.
> - **Tests**:
>   - Update tests to expect sourcing requirements only (no secondary prompt markers), validate exclusion of errored secondary responses, and primary-only behavior on secondary failure.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit f9
