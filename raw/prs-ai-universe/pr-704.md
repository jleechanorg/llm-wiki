# PR #704: Debug in-memory rate limit logic

**Repo:** jleechanorg/ai_universe
**Merged:** 2025-11-13
**Author:** jleechan2015
**Stats:** +407/-333 in 5 files

## Summary
(none)

## Raw Body
Bug: User made 2 requests and hit rate limit with error showing "10 requests per hour"
Root Cause: Cloud Run detection triggered "distributed mode" with ultra-strict 1 req/min limit

Changes:
- Removed isDistributedDeployment() and handleDistributedRisk() methods
- Removed RATE_LIMIT_RENDER_ASSUME_SINGLE environment variable logic
- Updated render.yaml to remove obsolete env var
- Removed distributed deployment tests
- All rate limiting now uses configured limits (10/day anon, 30/day auth, etc.)

The distributed protection was too aggressive (1 request per minute) and conflicted with actual configured limits. Rate limiting now works as expected using Secret Manager config or hardcoded fallbacks.

Tests: All 26 RateLimitTool tests passing

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Eliminates distributed-deployment 1 req/min override, simplifying `RateLimitTool` to use configured limits and updating tests, docs, and Render config accordingly.
> 
> - **Rate limiting (library)**:
>   - Removes distributed-protection logic from `shared-libs/packages/mcp-server-utils/src/RateLimitTool.ts`:
>     - Deletes `isDistributedDeployment()` and `handleDistributedRisk()` and related logging flags.
>     - Simplifies `checkRateLimit()` to always apply configured limits (no Cloud Run override).
> - **Tests**:
>   - Backend: adds regression suite in `backend/src/test/RateLimitTool.test.ts` reproducing Cloud Run issue and verifying fix (no 1 req/min).
>   - Library: replaces distributed-detection logging tests with config logging tests in `shared-libs/.../__tests__/RateLimitTool.test.ts`.
> - **Docs**:
>   - Adds `docs/BUG_REPRODUCTION_RATE_LIMIT_PROOF.md` with reproduction, failure evidence, and fix validation.
> - **Config**:
>   - Updates `render.yaml` to remove `RATE_LIMIT_RENDER_ASSUME_SINGLE` and clarify rate-limit config via Secret Manager/defaults.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 2560d3901c37d787b2bee6af25b1bd
