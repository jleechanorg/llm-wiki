# PR #374: fix: guard Cerebras web search payloads for native endpoints

**Repo:** jleechanorg/ai_universe
**Merged:** 2025-10-16
**Author:** jleechan2015
**Stats:** +22/-3 in 1 files
**Labels:** codex

## Summary
- disable Cerebras web search when the configured endpoint is not the OpenRouter gateway and log the reason
- reuse the shared DEFAULT_PRIMARY_MODEL fallback and derive response metadata provider from the endpoint domain

## Raw Body
## Summary
- disable Cerebras web search when the configured endpoint is not the OpenRouter gateway and log the reason
- reuse the shared DEFAULT_PRIMARY_MODEL fallback and derive response metadata provider from the endpoint domain

## Testing
- npm run lint *(fails: ESLint 9 requires eslint.config.js instead of .eslintrc)*

------
https://chatgpt.com/codex/tasks/task_e_68f0a3d19c1c832fb1c3f757422c5461

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Disables Cerebras web search when not using OpenRouter, derives response metadata provider from endpoint, and uses DEFAULT_PRIMARY_MODEL as the fallback model.
> 
> - **Cerebras tool**:
>   - **Web search**: Disable when `endpoint` is not OpenRouter; add diagnostics with `reason` and `endpoint`, and log the condition; only enable plugins/options when supported.
>   - **Initialization**: Set `endpoint` before applying `webSearch` config; fallback to `DEFAULT_PRIMARY_MODEL` on init failure.
>   - **Responses**: Set metadata `provider` based on endpoint domain (`openrouter` vs `cerebras`).
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 38cfe89d14b4c33d26249ad61e4947c61a76fca4. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
