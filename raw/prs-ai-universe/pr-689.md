# PR #689: Fix deployment docs and config handling for Redis follow-up

**Repo:** jleechanorg/ai_universe
**Merged:** 2025-11-13
**Author:** jleechan2015
**Stats:** +30/-27 in 5 files
**Labels:** codex

## Summary
- Hoist the environment detection logic inside `ConversationMCPTool` so production/PR rules are applied consistently
- Stop copying `node_modules` into the Cloud Build context, document the exact `--set-secrets` contract, and clarify how `REDIS_URL` is injected
- Enumerate the API-key secrets in `ConfigManager`, continue preferring Secret Manager values when present, and tidy the Redis enforcement test suite name

## Raw Body
## Summary
- Hoist the environment detection logic inside `ConversationMCPTool` so production/PR rules are applied consistently
- Stop copying `node_modules` into the Cloud Build context, document the exact `--set-secrets` contract, and clarify how `REDIS_URL` is injected
- Enumerate the API-key secrets in `ConfigManager`, continue preferring Secret Manager values when present, and tidy the Redis enforcement test suite name

## Testing
- `npm test -- RateLimitTool`
- `npm test -- ConfigManager`


------
[Codex Task](https://chatgpt.com/codex/tasks/task_e_691583e43358832f8975ef186ccae92c)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Unifies environment/secret resolution for Conversation MCP, makes Secret Manager the authoritative source for API keys, trims Cloud Build context, and updates deployment docs (Redis/env and --set-secrets).
> 
> - **Backend**:
>   - **ConversationMCPTool**: Hoists env detection; treats PR previews differently; requires explicit config in production-like envs; skips Secret Manager in PR; normalized URL handling and logging.
>   - **ConfigManager**: Enumerates API-key secrets and makes Secret Manager values override env vars when non-empty; adds secret name mappings (e.g., `ANTHROPIC_API_KEY→claude-api-key`).
> - **Infrastructure**:
>   - **deploy.sh**: Stops copying `node_modules` for shared-libs into Cloud Build context (built artifacts only).
> - **Docs**:
>   - **CLAUDE.md**: Updates `--set-secrets` contract (adds `OPENROUTER_API_KEY`, `FIREBASE_AUTH_PROJECT_ID`, `CONVERSATION_MCP_SERVER_URL`); clarifies Redis via env (`REDIS_URL`) with in-memory fallback when unavailable.
> - **Tests**:
>   - **RateLimitTool**: Renames Redis enforcement suite title (no behavior change).
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit dfc81ebdd9ce518b8a978d7ca08f889c3c2deec1. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
