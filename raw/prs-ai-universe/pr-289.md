# PR #289: feat: share second opinion latency instrumentation

**Repo:** jleechanorg/ai_universe
**Merged:** 2025-10-13
**Author:** jleechan2015
**Stats:** +469/-38 in 7 files
**Labels:** codex

## Summary
- add per-stage and per-model latency instrumentation to the second-opinion flow with structured logging and persistence
- relocate the latency tracker implementation and tests into @ai-universe/mcp-server-utils with a shared verbose logging flag
- document the MCP_VERBOSE_LATENCY_LOGGING toggle for backend consumers and ensure logger wiring flows through the shared utility

## Raw Body
## Summary
- add per-stage and per-model latency instrumentation to the second-opinion flow with structured logging and persistence
- relocate the latency tracker implementation and tests into @ai-universe/mcp-server-utils with a shared verbose logging flag
- document the MCP_VERBOSE_LATENCY_LOGGING toggle for backend consumers and ensure logger wiring flows through the shared utility

## Testing
- npm run test -- LatencyTracker

------
https://chatgpt.com/codex/tasks/task_e_68ea81645250832fa43eae0259a4c517

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Moves LatencyTracker to shared utils with a global verbose flag and integrates it into SecondOpinionAgent to emit per-stage/model latency (tokens/cost) and final summaries, wiring backend logging and updating docs/tests.
> 
> - **Shared utils (`@ai-universe/mcp-server-utils`)**:
>   - **New `LatencyTracker`**: Pluggable logger, env‑controlled verbosity (`MCP_VERBOSE_LATENCY_LOGGING`, legacy fallback), per‑stage/model metrics with tokens/cost, snapshots, and `/tmp` persistence.
>   - **Exports/Docs/Tests**: Export `LatencyTracker`; README documents latency toggle and usage; adds unit tests for logging behavior and flags.
> - **Backend**:
>   - **SecondOpinionAgent**: Switch to shared `LatencyTracker`; record tokens/cost for model calls and stages; add latency summary log (slowest stage/model, totals); include `latencySummary` in persist meta.
>   - **Logging wiring**: Backend `logger` passed to `LatencyTracker` via `setLatencyTrackerLogger`.
>   - **Docs**: Backend README adds latency logging section and new env vars (`MCP_VERBOSE_LATENCY_LOGGING`, legacy flag).
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 7d8430ba91b8771bcbce80f3fb9aab8b3e4b5804. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->
## Summary by CodeRabbi
