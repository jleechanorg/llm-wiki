# PR #924: Switch to OpenRouter Cerebras model for synthesis

**Repo:** jleechanorg/ai_universe
**Merged:** 2025-12-27
**Author:** jleechan2015
**Stats:** +226/-74 in 9 files

## Summary
(none)

## Raw Body
<!-- CURSOR_SUMMARY -->
> [!NOTE]
> **Cerebras routing and web search behavior**
> 
> - Centralizes Cerebras routing through `OpenRouter` when available, with per-call control: primary responses have `web search` disabled; synthesis keeps it enabled
> - Adds env/config-based auto-detection of `CerebrasMode` and propagates `cerebrasDisableWebSearch` to `getLLMCaller('cerebras')`; enriches error/timeout metadata via `buildCerebrasErrorMetadata`
> 
> **Agent/Registry changes**
> 
> - `SecondOpinionAgent`: resolves Cerebras mode, disables web search for primary calls, uses resolved mode for synthesis, and records diagnostics in results
> - `ToolRegistry.getLLMCaller`: supports `{ cerebrasMode, cerebrasDisableWebSearch }`, auto-detects OpenRouter vs direct, and passes options to tool calls
> 
> **Tests and CI**
> 
> - New/expanded tests validate OpenRouter vs direct fallback, per-call web-search flags, and synthesis behavior; integration test now runs unskipped and includes authenticated user IDs
> - Workflow runs backend with `--disable-auth` for simulation; adjusts CI script assertions to top-level `webSearchDiagnostics`
> 
> **Docs/Deps**
> 
> - Updates `CLAUDE.md` Cerebras routing guidance; adds `redis` dependency in backend lockfile
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 138931ddaf8be29e529be870527a42a46db7066f. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->
## Summary by CodeRabbit

* **New Features**
  * Per-call control to disable web search for primary responses while keeping final syntheses web-enabled.

* **Refactor**
  * SMART routing unified to consistently use the OpenRouter path; routing and diagnostics now apply across primary, secondary, and synthesis flows.

* **Bug Fixes**
  * Resolved inconsistencies between primary and synthesis web-searc
