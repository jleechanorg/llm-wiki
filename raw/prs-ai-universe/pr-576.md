# PR #576: feat: harden conversation MCP, rate limits, and citation guarantees

**Repo:** jleechanorg/ai_universe
**Merged:** 2025-11-08
**Author:** jleechan2015
**Stats:** +102/-11 in 3 files

## Summary
- Rebuilt the conversation MCP stack so `conversation.send-message`, `conversation.add-message`, `conversation.get-history`, and `conversation.list` all preserve conversation ids, assistant replies, and metadata even under Firestore eventual consistency. Added convo delete wiring, streaming persistence fixes, and noserver-parity harnesses under `testing_integration/convo/` with evidence capture scripts.
- Hardened rate limiting by moving `RateLimitTool` into `shared-libs`, adding Secret Manager 

## Raw Body
## Summary
- Rebuilt the conversation MCP stack so `conversation.send-message`, `conversation.add-message`, `conversation.get-history`, and `conversation.list` all preserve conversation ids, assistant replies, and metadata even under Firestore eventual consistency. Added convo delete wiring, streaming persistence fixes, and noserver-parity harnesses under `testing_integration/convo/` with evidence capture scripts.
- Hardened rate limiting by moving `RateLimitTool` into `shared-libs`, adding Secret Manager backed config (dev + prod), cache invalidation, and FastMCP guardrails (10 MB limit, convo tool filters, request parser) with new Jest coverage and a `scripts/test-fastmcp-guardrails.mjs` helper.
- Dramatically improved `SecondOpinionAgent`: expanded citation requirements/prompts, upgraded HTTP→HTTPS normalization, added numbered-reference parsing, stricter per-model source validation in `scripts/mcp-ci-tests.mjs`, and refreshed docs/runbooks/CLAUDE skills plus automation (presubmit typecheck workflow, run-local-server improvements, CLAUDE CLI helper scripts).

## Key Changes
- **Conversation MCP + Persistence**
  - Enforced send-message contract, assistant reply persistence, conversation.delete surface, retry-on-empty handling, JSON-RPC error shaping, and pagination metadata fixes.
  - Added deterministic convo tests (`test_send_message_auto_reply`, `test_conversation_list`, `test_conversation_id_regression`, `test_conversation_sequences`, `test_conversation_history`) plus noserver parity docs and evidence captures under `/tmp/testllm_evidence_*`.
  - Expanded documentation (`roadmap/convo/*`, `docs/convo/*`, `README`/`AGENTS.md`) and added tooling (conversation MCP timeout client, helper scripts, CLI guidance).
- **Rate Limiting + FastMCP Guardrails**
  - Secret Manager integration with cached config, dev defaults, increased 10k req/min limit, shared RateLimitTool adoption across backend + shared-libs, and new unit coverage.
  - StartFastMcpHttpProxy request pars
