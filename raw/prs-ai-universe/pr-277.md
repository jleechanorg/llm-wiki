# PR #277: Fix backend coverage workflow regressions

**Repo:** jleechanorg/ai_universe
**Merged:** 2025-10-13
**Author:** jleechan2015
**Stats:** +566/-5 in 5 files
**Labels:** codex

## Summary
- add a validation job that runs lint, build, unit, and integration checks with a live backend so the workflow matches CI expectations
- harden the coverage diff job with base-branch fallbacks, consistent reporters, and artifact paths that surface HTML and JSON coverage data
- normalize the coverage diff script to align file keys across worktrees and improve regression reporting clarity

## Raw Body
## Summary
- add a validation job that runs lint, build, unit, and integration checks with a live backend so the workflow matches CI expectations
- harden the coverage diff job with base-branch fallbacks, consistent reporters, and artifact paths that surface HTML and JSON coverage data
- normalize the coverage diff script to align file keys across worktrees and improve regression reporting clarity

## Testing
- npm run test:coverage -- --coverageReporters=json-summary --coverageReporters=lcov --coverageReporters=html


------
https://chatgpt.com/codex/tasks/task_e_68e9d4c09ac8832fbb892cbe74cc9a50

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Adds a backend CI workflow that validates and compares coverage vs base, uploads reports, and introduces env-configurable Jest coverage plus a test:coverage script.
> 
> - **CI/Workflows**:
>   - Add `backend-coverage.yml` with validation (lint, build, unit, integration against live server with Redis) across Node 20/22.
>   - Add coverage job that runs base vs PR, generates reports, compares results, and uploads artifacts (HTML, JSON, summary).
> - **Testing/Coverage**:
>   - Add `test:coverage` script to run Jest with coverage in CI.
>   - Update `jest.config.js` to support `COVERAGE_DIR` and `COVERAGE_REPORTERS` env vars and include `json-summary`.
> - **Tooling**:
>   - Introduce `scripts/coverage-diff.mjs` to normalize file keys and output a Markdown summary with per-metric regressions.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit e751cfee7c3f6e81c2532a3bf55b5cea79450ff0. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->
## Summary by CodeRabbit

* Chores
  * Added a CI workflow that validates changes across multiple Node.js versions, runs unit and integration tests with a backing service, performs a security audit, and publi
