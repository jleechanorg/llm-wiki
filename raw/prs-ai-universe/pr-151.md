# PR #151: fix: restrict CORS wildcard to Cloud Run domains

**Repo:** jleechanorg/ai_universe
**Merged:** 2025-10-03
**Author:** jleechan2015
**Stats:** +17/-3 in 3 files
**Labels:** codex

## Summary
- tighten the default backend CORS wildcard so that only Cloud Run run.app hosts are accepted
- align the production deploy configuration and unit tests with the safer origin pattern and ensure lookalike domains are rejected

## Raw Body
## Summary
- tighten the default backend CORS wildcard so that only Cloud Run run.app hosts are accepted
- align the production deploy configuration and unit tests with the safer origin pattern and ensure lookalike domains are rejected

## Testing
- npm test -- --runTestsByPath ../shared-libs/packages/mcp-server-utils/src/__tests__/createWildcardCorsOptions.test.ts *(fails: Jest cannot find tests outside the backend directory)*

------
https://chatgpt.com/codex/tasks/task_e_68e047e5fb8c832f8637fbef00c6e575

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Restricts CORS wildcard to Cloud Run run.app domains and updates prod deploy config and tests.
> 
> - **Backend**:
>   - Tighten default CORS origins in `backend/src/server.ts` to `https://ai-universe-frontend*.run.app`.
> - **Deploy**:
>   - Update prod `CORS_ORIGINS` in `deploy.sh` to `https://ai-universe-frontend*.run.app`.
> - **Tests**:
>   - Adjust allowed origin pattern in `shared-libs/packages/mcp-server-utils/src/__tests__/createWildcardCorsOptions.test.ts` to `.run.app`.
>   - Add test rejecting `ai-universe-frontend` lookalike domains outside Cloud Run.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit ee2b0d8dbd42b813f1bceceb92356c299387b01a. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
