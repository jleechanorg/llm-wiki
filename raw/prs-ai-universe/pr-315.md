# PR #315: fix: shared-libs prep handles dependency freshness and rebase hooks

**Repo:** jleechanorg/ai_universe
**Merged:** 2025-10-13
**Author:** jleechan2015
**Stats:** +214/-18 in 4 files
**Labels:** codex

## Summary
- restore dependency freshness detection in `prepare-shared-libs.mjs`, including npm install/ci triggers and dependency-order builds
- propagate the Node script exit code to shell callers, document the Node 20 runtime prerequisite, and harden git diff handling
- extend git hook automation with a post-rewrite rebase hook that forces rebuilds when histories are rewritten

## Raw Body
## Summary
- restore dependency freshness detection in `prepare-shared-libs.mjs`, including npm install/ci triggers and dependency-order builds
- propagate the Node script exit code to shell callers, document the Node 20 runtime prerequisite, and harden git diff handling
- extend git hook automation with a post-rewrite rebase hook that forces rebuilds when histories are rewritten

## Testing
- node backend/scripts/prepare-shared-libs.mjs

------
https://chatgpt.com/codex/tasks/task_e_68ed49f90328832fb955d35ad4a1393f

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Improves shared-libs preparation with smarter install/build detection, topological build ordering, CI-aware installs, and adds a post-rewrite git hook for rebase-triggered rebuilds.
> 
> - **Shared-libs preparation (`backend/scripts/prepare-shared-libs.mjs`)**:
>   - Topologically sorts packages based on internal deps to determine build order.
>   - Restores dependency freshness checks (mtime of `node_modules` vs `package-lock.json`/`package.json`, missing deps via `createRequire`).
>   - Detects build outputs from `package.json` (`main`, `types`/`typings`) instead of hardcoded `dist/index.*`.
>   - Smarter rebuild logic (separate JS/types checks, FORCE_REBUILD/CI via boolean parsing) and clearer reasons in logs.
>   - Uses `npm ci` on CI, `npm install` locally.
> - **Git hooks (`scripts/setup-git-hooks.sh`)**:
>   - Hardened git invocations with `git -C "$PROJECT_ROOT"`.
>   - New `post-rewrite` (rebase) hook to force shared-libs refresh after history rewrites; updates post-merge messaging.
> - **Build scripts**:
>   - `scripts/shared-build-functions.sh`: propagate Node script exit code and improve error reporting.
> - **Docs**:
>   - `scripts/README.md`: note Node.js 20+ requirement.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 746e0559ec4de0ee6c3618a0cea27081464811a2. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bug
