# PR #212: Containerize Claude CLI tooling with SDK runner, auth reuse, and self-hosted CI

**Repo:** jleechanorg/ai_universe
**Merged:** 2025-10-07
**Author:** jleechan2015
**Stats:** +1107/-8 in 7 files
**Labels:** codex

## Summary
- replace the legacy claude-cli wrapper with a Docker-driven SDK image that mounts host auth state, forwards slash commands, and orchestrates `/testllm`
- add a Node-based Claude Code SDK runner plus pinned dependency versions shared by both local installs and the container build
- introduce a self-hosted "Claude Copilot Verification" workflow that runs the Docker wrapper, along with README documentation covering automation, runner setup, and PR guidance

## Raw Body
## Summary
- replace the legacy claude-cli wrapper with a Docker-driven SDK image that mounts host auth state, forwards slash commands, and orchestrates `/testllm`
- add a Node-based Claude Code SDK runner plus pinned dependency versions shared by both local installs and the container build
- introduce a self-hosted "Claude Copilot Verification" workflow that runs the Docker wrapper, along with README documentation covering automation, runner setup, and PR guidance

## Testing
- ./run_tests.sh --unit-only

------
https://chatgpt.com/codex/tasks/task_e_68d4f7fb8644832f8d338303dd8b04ac

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Adds a Dockerized Claude Code CLI wrapper and Node SDK runner, introduces a self-hosted GitHub Actions copilot workflow, updates docs, and pins SDK dependencies.
> 
> - **Tooling**:
>   - **Dockerized Claude CLI wrapper**: Replaces legacy `scripts/claude-cli-wrapper.sh` with containerized runner that mounts host auth, executes default `/copilot`, and can run `/testllm` with configurable paths and flags.
>   - **Node SDK runner**: Adds `scripts/claude-sdk-runner.mjs` to invoke `@anthropic-ai/claude-code` with streaming output and env-based configuration.
> - **CI**:
>   - **Self-hosted workflow**: Adds `.github/workflows/claude-copilot.yml` to run the wrapper on PR events using a self-hosted runner and Docker.
> - **Docs**:
>   - **README**: Documents the wrapper, workflow usage, and self-hosted runner provisioning.
>   - **PR guidelines**: Adds `docs/pr-guidelines/codex-create-script-for-docker-slash-commands-guidelines.md` with title/description/testing template.
> - **Dependencies**:
>   - Pin `@modelcontextprotocol/sdk` to `1.18.0` and add dev dependency `@anthropic-ai/claude-code@1.0.126`.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit d970addc26dc87adab86f2bf9b9f9b068db5e80b. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_
