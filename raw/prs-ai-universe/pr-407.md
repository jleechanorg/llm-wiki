# PR #407: Revert PR #406 - render.yaml was in wrong repo

**Repo:** jleechanorg/ai_universe
**Merged:** 2025-10-21
**Author:** jleechan2015
**Stats:** +26/-16 in 2 files

## Summary
(none)

## Raw Body
This PR reverts #406 which was mistakenly merged to ai_universe instead of ai_universe_convo_mcp.

The render.yaml changes will be recreated in the correct repository.

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Renames the Render web service, switches to a build script, adds a health check, and revises env vars (API keys, Firebase config, and CORS).
> 
> - **Render deployment (`render.yaml`)**:
>   - **Service**: Rename `name` to `ai-universe-backend-server`; add `healthCheckPath: /health`; switch `buildCommand` to `./build-render.sh`.
>   - **Env vars**:
>     - API keys: ensure `sync: false`; add `OPENAI_API_KEY`; include `GROK_API_KEY`, `PERPLEXITY_API_KEY`, `OPENROUTER_API_KEY`, `GEMINI_API_KEY`.
>     - Firebase: replace admin/project entries with full web config (`FIREBASE_API_KEY`, `FIREBASE_AUTH_DOMAIN`, `FIREBASE_PROJECT_ID`, `FIREBASE_STORAGE_BUCKET`, `FIREBASE_MESSAGING_SENDER_ID`, `FIREBASE_APP_ID`, `FIREBASE_MEASUREMENT_ID`).
>     - CORS: replace `CORS_ALLOWED_ORIGINS` with `CORS_ORIGIN: "*"`.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 99fd26597c747bd65ecd6a09198f0298b9aa40bb. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
