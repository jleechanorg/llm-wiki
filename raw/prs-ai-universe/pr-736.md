# PR #736: Fix redirect auth flow and user-agent tests

**Repo:** jleechanorg/ai_universe
**Merged:** 2025-11-14
**Author:** jleechan2015
**Stats:** +348/-608 in 10 files
**Labels:** codex

## Summary
- deduplicate Firebase auth error handling, expose checkRedirectResult on SecondOpinionClient, and update docs/examples for the redirect flow
- harden the CLI redirect page with safer error reporting and add real unit tests for AuthHelper user-agent compliance
- replace the backend integration test with mocked coverage and user-agent classification utilities that work in Node

## Raw Body
## Summary
- deduplicate Firebase auth error handling, expose checkRedirectResult on SecondOpinionClient, and update docs/examples for the redirect flow
- harden the CLI redirect page with safer error reporting and add real unit tests for AuthHelper user-agent compliance
- replace the backend integration test with mocked coverage and user-agent classification utilities that work in Node

## Testing
- `cd shared-libs/packages/second-opinion-client && npx jest auth-user-agent.test.ts`
- `cd backend && NODE_ENV=test CI_SIMULATION=true ALLOW_E2E_SERVER_REQUESTS=true SKIP_E2E_HEALTHCHECK=true npx jest --config=jest.end2end.config.cjs --runTestsByPath src/test/end2end/google-oauth-user-agent.test.ts`


------
[Codex Task](https://chatgpt.com/codex/tasks/task_e_69168fcfeac8832fba20cf34f5102aa8)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Switches Firebase auth to a redirect-first flow with unified user‑agent error handling, exposes checkRedirectResult(), updates CLI/docs/examples, and replaces E2E with mocked UA compliance tests.
> 
> - **Auth (client library)**:
>   - Add `checkRedirectResult()` in `src/SecondOpinionClient.ts`; expose redirect completion API.
>   - Change `AuthHelper.signInWithGoogle()` to redirect flow and return `{ isAuthenticated: false }`; factor shared error handling into `handleAuthError()` detecting `disallowed_useragent`.
>   - Configure `GoogleAuthProvider` with `prompt: 'select_account'`; rely on default scopes.
> - **CLI (`scripts/auth-cli.mjs`)**:
>   - Harden in-browser auth page with `setStatus()` and `handleAuthError()`; clearer UA-block messages; consistent redirect flow status updates.
> - **Tests**:
>   - Replace backend integration with mocked tests in `backend/src/test/end2end/google-oauth-user-agent.test.ts` including UA classification and redirect result handling.
>   - Add unit tests `src/__tests__/auth-user-agent.test.ts` covering provider params, redirect flow, error translation, and auth state listeners.
> - **Docs**:
>   - Update 
