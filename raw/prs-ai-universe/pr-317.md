# PR #317: Harden conflict auto-resolve workflow

**Repo:** jleechanorg/ai_universe
**Merged:** 2025-10-13
**Author:** jleechan2015
**Stats:** +277/-14 in 6 files
**Labels:** codex

## Summary
- ensure the conflict resolution scripts operate from the repo root, guard against dirty trees, and work in CI contexts
- make the auto-resolver fail fast for forked PRs and restore the previous branch when run locally
- document how to trigger and replicate the auto resolve workflow inside `.github/workflows/README.md`

## Raw Body
## Summary
- ensure the conflict resolution scripts operate from the repo root, guard against dirty trees, and work in CI contexts
- make the auto-resolver fail fast for forked PRs and restore the previous branch when run locally
- document how to trigger and replicate the auto resolve workflow inside `.github/workflows/README.md`

## Testing
- not run (documentation and shell scripts only)


------
https://chatgpt.com/codex/tasks/task_e_68ed53f41b24832f9294d50cdc507e75

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Adds a GH Actions workflow to auto-merge base into PR branches and strengthens conflict-resolution scripts; minor backend typing tweaks.
> 
> - **CI/Workflows**:
>   - **New** `/.github/workflows/resolve-conflicts.yml`: auto-resolves PR conflicts on PR events and manual dispatch; guarded to same-repo branches; sets concurrency and bot author; runs `scripts/resolve_conflicts.sh`.
>   - **Docs** `/.github/workflows/README.md`: usage, manual dispatch, local replication, and limitations.
> - **Scripts**:
>   - **New** `scripts/auto_resolve_conflicts.sh`: fetches PR metadata via `gh`, verifies clean repo, detects forks, merges base into head, pushes, and restores branch locally; explicit exit codes.
>   - **Updated** `scripts/resolve_conflicts.sh`: runs from repo root, accepts optional PR number, validates `gh`, derives PR when omitted, checks clean tree, and delegates to helper script.
> - **Backend**:
>   - `backend/src/server.ts`: add explicit Express `Request/Response/NextFunction` types to middleware and handlers.
> - **Config**:
>   - `backend/src/config/llmDefaults.ts`: adjust `ModelDefaults` type import/re-export syntax (no behavior change).
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 97ea2aae75744fd55537aaeae9611d2957e264dc. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release not
