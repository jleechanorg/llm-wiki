# PR #360: build: enforce merge-conflict guardrails

**Repo:** jleechanorg/ai_universe
**Merged:** 2025-10-15
**Author:** jleechan2015
**Stats:** +281/-23 in 7 files
**Labels:** codex

## Summary
- document the merge-conflict guardrails in AGENTS.md for quick reference
- add repository-managed pre-commit and pre-push hooks that block commits or pushes while MERGE_HEAD is present
- require explicit modes in the conflict resolution helpers, enforce merge commit prefixes, and make run_tests.sh abort on dirty or conflicted trees

## Raw Body
## Summary
- document the merge-conflict guardrails in AGENTS.md for quick reference
- add repository-managed pre-commit and pre-push hooks that block commits or pushes while MERGE_HEAD is present
- require explicit modes in the conflict resolution helpers, enforce merge commit prefixes, and make run_tests.sh abort on dirty or conflicted trees

## Testing
- hooks/pre-commit *(fails with MERGE_HEAD present as expected)*
- ./scripts/resolve_conflicts.sh --manual "demo merge"
- ./run_tests.sh --unit-only *(fails when conflicts are present as expected)*

------
https://chatgpt.com/codex/tasks/task_e_68ede5951d98832f8a65535b433d9d71

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Add repo-managed hooks and conflict-resolution workflow (explicit modes, standardized merge commit prefixes) and make tests abort on dirty or conflicted trees, with docs updated.
> 
> - **Scripts/Tooling**:
>   - **Conflict workflow**: Overhaul `scripts/resolve_conflicts.sh` to require `--auto` or `--manual <reason>`; supports `--pr`, CI defaults to auto; validates clean state; manual path finalizes merge with prefixed message `[MERGE MANUAL: <reason>] …`.
>   - **Auto-merge**: Enhance `scripts/auto_resolve_conflicts.sh` to rewrite merge commit subject with `[MERGE RESOLVED] …` for true merge commits; skip on fast-forward; sources `scripts/git_message_utils.sh`.
>   - **Commit message utils**: Add `scripts/git_message_utils.sh` with `extract_commit_subject` helper.
>   - **Git hooks**: Add `hooks/pre-commit` and `hooks/pre-push` to block actions when `.git/MERGE_HEAD` exists, guiding to `./scripts/resolve_conflicts.sh`.
>   - **Test runner**: Update `scripts/run_tests.sh` to abort when unresolved conflicts or dirty working tree are detected.
> - **Documentation**:
>   - Add **Merge Conflict Guardrails** to `AGENTS.md` detailing hooks, resolution scripts/modes, and test expectations; reiterate PR-diff evaluation guidance.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for
