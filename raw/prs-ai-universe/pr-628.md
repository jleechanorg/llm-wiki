# PR #628: Fix smoke command workflow trigger

**Repo:** jleechanorg/ai_universe
**Merged:** 2025-11-12
**Author:** jleechan2015
**Stats:** +818/-211 in 22 files

## Summary
(none)

## Raw Body
- Add workflow_dispatch trigger for manual testing
- Support both PR comment and manual triggers
- Update comment titles to explicitly say 'Real Mode'
- Only post comments on issue_comment events (not manual runs)

Changes:
- Ack comment: 'Real API Smoke Tests Started (Real Mode)'
- Success: 'Smoke Tests Passed (Real Mode)'
- Failure: 'Smoke Tests Failed (Real Mode)'

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Adds a manual-triggered smoke test flow with real/mock mode labeling, fixes conversation history ordering/timestamp handling, and centralizes canonical examples with updated mocks/tests.
> 
> - **CI/Workflows**:
>   - Add `workflow_dispatch` to `manual-smoke-tests.yml` with `pr_number` input; support PR comments or manual runs.
>   - Post PR comments only on `issue_comment` events; update comment copy to include "(Real Mode)".
>   - Build shared-libs before tests; set `MCP_TEST_MODE=real`; pass job URLs to smoke runner.
> - **Smoke Test Runner (Python)**:
>   - Accept `test_mode` and include mode label in PR comments; determine from env when not provided.
>   - Emit concise success logs; tests updated to cover mode labels and env merging.
> - **Backend (ConversationMCPTool)**:
>   - Global chronological sort across paginated history; keep sentinel timestamps for sorting then replace for clients.
>   - Expanded tests for pagination ordering, invalid/sentinel timestamps, and Firestore timestamps.
> - **Smoke Tests (Node)**:
>   - Import canonical examples from shared lib; remove JSON fixtures in `docs/backend/schema/*`.
>   - Broaden second-opinion diagnostics to handle Cerebras via Cerebras/OpenRouter; stricter validations.
>   - Update mocks to match canonical shapes.
> - **Shared Libs**:
>   - Export new test fixtures: `canonicalExamples` and `llmResponseExamples`.
>   - Minor typing fixes in `RateLimitTool` (VIP allowlist parsing).
> - **Repo**:
>   - Add `.egg-info/` to `.gitignore`.
>   - Add smoke test evidence files under `smoke_tests/`.
> 
> <sup>
