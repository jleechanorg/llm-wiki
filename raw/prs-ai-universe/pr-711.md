# PR #711: ci: add manual trigger to backend auto deploy

**Repo:** jleechanorg/ai_universe
**Merged:** 2025-11-13
**Author:** jleechan2015
**Stats:** +7/-22 in 2 files

## Summary
- add workflow_dispatch with optional reason input to auto-deploy
- forward reason/ref_name to deploy-dev as in frontend PR #240
- remove redundant deploy-dev-manual wrapper (auto workflow now exposes Run button)

## Raw Body
## Summary
- add workflow_dispatch with optional reason input to auto-deploy
- forward reason/ref_name to deploy-dev as in frontend PR #240
- remove redundant deploy-dev-manual wrapper (auto workflow now exposes Run button)

## Testing
- not run (workflow-only change)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Adds a manual trigger with optional reason to backend auto-deploy and removes the separate manual deploy workflow.
> 
> - **CI/Workflows**:
>   - **`auto-deploy.yml`**:
>     - Add `workflow_dispatch` with optional `inputs.reason`.
>     - Compute `with.reason` based on event (`workflow_dispatch` vs `push`) and pass `ref_name` through.
>   - **Removed**: `./.github/workflows/deploy-dev-manual.yml` (redundant manual wrapper).
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 796fcacf6ed0639aa88191c39ef2282ff173f169. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
