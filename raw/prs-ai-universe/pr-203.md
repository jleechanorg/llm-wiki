# PR #203: feat: python mcp server utils server

**Repo:** jleechanorg/ai_universe
**Merged:** 2025-10-07
**Author:** jleechan2015
**Stats:** +1393/-6 in 15 files
**Labels:** codex

## Summary
- require Python 3.10+ and ship pytest-anyio in the testing extras so async smoke tests install their plugins
- tighten the MCP port discovery routine with validated search bounds and consistent range reporting
- add a regression test that exercises downward port searches to guard the new behaviour

## Raw Body
## Summary
- require Python 3.10+ and ship pytest-anyio in the testing extras so async smoke tests install their plugins
- tighten the MCP port discovery routine with validated search bounds and consistent range reporting
- add a regression test that exercises downward port searches to guard the new behaviour

## Testing
- pytest tests/test_python_mcp_server_utils.py -q
- pytest tests/workflows/test_pr_dev_preview_workflow.py -q

------
https://chatgpt.com/codex/tasks/task_e_68de82b2c948832f81ea12133165ac7c

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Introduces a new Python FastMCP utilities package with CORS/HTTP proxy/server helpers, adds end-to-end tests and a GitHub Actions smoke-test job, and updates PR preview workflow triggers.
> 
> - **Python package: `packages/python-mcp-server-utils/`**
>   - Adds FastMCP utilities: `cors.py`, `http_proxy.py`, `ports.py`, `server_factory.py`, `server_lifecycle.py`, `tool_helpers.py`, `wildcard.py` with `__init__.py` exports.
>   - Packaging: `pyproject.toml` (Python 3.10+, testing extras), `README.md`, `LICENSE`.
> - **Tests**
>   - Adds `tests/test_python_mcp_server_utils.py`: end-to-end FastMCP proxy test, wildcard CORS, and port-discovery coverage.
>   - Adds `tests/workflows/test_pr_dev_preview_workflow.py`: asserts Python smoke-test job and pytest flags.
> - **CI/Workflow (`.github/workflows/pr-dev-preview.yml`)**
>   - Replaces `paths-ignore` with explicit `paths` filters.
>   - Adds `python-mcp-utils-smoke-tests` job (Python 3.11, installs package with `[testing]`, runs `pytest` target, comments results).
> - **Docs**
>   - Expands `README.md` with architecture, development setup, testing, deployment, and security sections, including Python utils mention.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit c9eca24b7bfc263f29f6ca09e6352ca7f2f64fb7. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
