---
name: levelup-session-verified-migration-state-2026-06-13
description: "Verified origin/main state of the level-up-session migration; corrects 3 false carried-forward findings (file not deleted, rev-74a8m wrong, 3 PRs already resolved)"
metadata: 
  node_type: memory
  type: project
  originSessionId: 9bef64e0-720e-43e4-87a7-77be34ec8775
---

Verified code audit of the level-up-session migration on `origin/main` @ `18aadc3c` (2026-06-13). Corrects errors that propagated through compacted evidence lanes and beads.

**FALSE findings now corrected:**
- ❌ "`level_up_session.py` deleted in #7447" → it EXISTS (916 lines), imported by god_mode_level_up.py:30, rewards_engine.py:38, world_logic.py:112.
- ❌ rev-74a8m "level_up_session.status has zero production callers" → FALSE. `.status` drives live routing: rewards_engine.py:292-310 `_is_session_active`, read at :1681, agents.py:1514. The "zero callers" was a symbol-grep artifact (`level_up_session.is_session_active(` → 0 hits because the check was inlined as `_is_session_active`).
- ❌ "3 open PRs to align (#7502/#7508/#7452)" → RESOLVED: #7502 MERGED (5f610303c1); #7508 CLOSED; #7452 CLOSED.

**True migration state vs 06-08 plan (PR1→PR6):** PR1 LANDED-partial (reducer exists, only 2/8 writers wired: apply_model_level_up_signal @ rewards_engine.py:3454, apply_god_mode_admin_commit @ god_mode_level_up.py:187/304; 6/8 lifecycle fns dead test-only). PR2 NOT landed (finish→status=complete @ world_logic.py:3003-3007, no level≥target gate; clamp deliberately removed per comment 2944-2964). PR3 NOT landed (no shared streaming/non-streaming preflight; streaming never builds complete session; assert_level_up_invariants never called in prod). PR4 LANDED (god-mode split god_mode_level_up.py:171/268/290). PR5 PARTIAL (status read but legacy flags still primary 1726/1730/1687/1668 + explicit_modal_clear veto 1682). PR6 NOT landed (project_legacy_level_up_fields dead 0 callers; no grep gates; rewards_box.new_level written directly world_logic.py:312-326 = LIVE divergence source; field_ownership_registry.json exists but inert).

**Divergence bug root cause (verified):** reducer runs only when model emits `level_up_signal` (canonical_signal from level_up_signal only, rewards_engine.py:2963/3057, never rewards_box). Model writes future level into rewards_box.new_level but no level_up_signal → reducer skipped → no session → block_unauthorized_level_mutation_deltas reverts → split brain. Fix = prompt-first (model always emits level_up_signal on advance) + PR6 rewards_box demotion. #7508 read WRONG field (rewards_box.current_level == pre-level per rewards_system_instruction.md:42-44; future level is new_level).

The original design is fully specified in roadmap/level-up-session-state-machine-design-2026-06-08.md + level-up-session-implementation-roadmap-2026-06-08.md. Lane reports: /tmp/worldarchitect.ai/northstar-audit/lane{A,B,C,D,E,F}-*.md.

**DESIGN DECISION 2026-06-13 (v2 — supersedes the pending state machine):** Reworked northstar (roadmap/level-up-session-northstar-2026-06-11.md, on branch docs-levelup-northstar-refresh) to **immediate-commit + session-as-record**. User's three decisions: (1) immediate-commit — model grants level → full sheet (level+HP+features+spells) written to player_character_data canonical in ONE atomic Firestore write, NO pending/staging, NO finish-commit move step (= the path apply_god_mode_admin_commit already uses; organic now matches god-mode); (2) recommended-applied + editable-in-place before finish/exit, editing later = normal turn, no deferral; (3) one combined review modal for multi-level. KEY: keep level_up_session but as the consolidated TRANSACTION RECORD (from→to, applied changes, offered_choices, selections, source, review_open — NO 6-status machine), created FROM the llm response and the ONLY level-up INPUT to the llm request. Inviolable rule that kills divergence: player_character_data + level_up_session written in the SAME atomic op from the SAME response (never separately); player_character_data.level is the SOLE read-authority. The 13 scattered fields + 4-homed XP fold into level_up_session/player_character_data or become one-way display projections. Frontend never reads the session (Lane F: app.js reads planning_block.choices + rewards_box banner only). The MOVE STEP (pending→canonical) was the bug; deleting it eliminates finish-limbo/stuck-modal/divergence/atomic-pair at root. ZFC correction: availability is model-granted, NEVER level_from_xp(xp)>level (T2). Migration M-A prompt(full-sheet)→M-B atomic co-write→M-C session sole request input→M-D route on review_open→M-E delete pending machine+scattered fields+synthesis→M-F grep gates. Net LOC strongly negative.

**COLD REVIEW (codex via /fs dark-factory spec_gen, 2026-06-13):** verdict FAIL on spec testability (referenced missing test_god_mode_level_up.py) but design validated SOUND. KEY substantive finding (verified): level_up_session has a SECOND writer — world_logic._build_level_up_session_update (world_logic.py:2409, called :2815/:3003) stamps source="server" (world_logic.py:2421; schema allows it at game_state.schema.json:1528 but T5 doesn't). This two-writer split (reducer vs world_logic), not the abstract "move step," is the mechanical origin of divergence. My M-B had an ERROR ("_build_level_up_session_update must run on both paths" = extend the 2nd writer to streaming = spreads divergence); CORRECTED to DELETE it + fold into reducer. dark-factory fix_spec node crashed (TypeError: replace() arg 2 must be str not dict — tooling bug). PR #7514 HEAD 2fde2b2da8. Impl decomposition (foundation-first + file-disjoint parallel + merge_train + domain-lock area-lock): PR-1 level_up_session.py (reducer API, sole writer) lands first → PR-2 agents.py / PR-3 rewards_engine.py / PR-4 world_logic.py (deletes _build_level_up_session_update) / PR-5 llm_parser.py+game_state.py fan out file-disjoint; PR-A prompts + PR-F gates parallel from start. Sealed holdouts: ~/projects/dark-factory-holdouts/holdouts/level-up-session/scenarios.yaml (12 scenarios). Validation gates (minimal_feature.dot): review=code-reviewer, gate_es+gate_er=evidence, holdout_eval=sealed, gate_cs=/code_standards.
