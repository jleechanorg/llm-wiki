# PR #6158: fix(ci): strictly enforce mp4 and cast video evidence

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-04-09
**Author:** jleechan2015
**Stats:** +82/-6 in 2 files

## Summary
(none)

## Raw Body
### Background
Previous lack of video recording requirements caused agents to slip by skeptic-cron checks with only text logs. This aligns the harness gate with human evaluation standards.

### Goals
- Enforce video recording requirement at the CI / `skeptic-cron` level automatically.
- Formalize terminal/UI video proofs.

### High-level description of changes
- Checks PR body and comments for `.mp4`, `.cast`, `.webm`, `.gif`, `loom.com` and `asciinema.org` before allowing Gate 6 (evidence) to pass.
- Included shell safety workflow fixes from prior agent loops.

### Testing
- Manual logic validation. Verified GitHub API calls and grep logic for gate validation.

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Tightens the merge gate in `skeptic-cron` by adding content-based evidence checks, which can block previously mergeable PRs if the regex/API aggregation misses valid evidence links or produces false negatives.
> 
> **Overview**
> **Skeptic-cron evidence gating is tightened**: Gate 6 now scans the PR body, issue comments, and review bodies for video/terminal-recording links (`.mp4`, `.cast`, `.webm`, `.gif`, Loom, Asciinema, GitHub attachments) and only passes if *both* the existing evidence approval/checks succeed *and* a video link is found.
> 
> **Shell safety guidance is updated** in `.agent/workflows/shell-safety.md` to strictly ban heredocs/inline multiline bodies, and to standardize a timeout + `GH_PROMPT_DISABLED=1`/`GH_NO_UPDATE_NOTIFIER=1`/`GH_PAGER=""` pattern (plus throttling) for reliable `gh` write operations.
> 
> <sup>Reviewed by [Cursor Bugbot](https://cursor.com/bugbot) for commit 2e2485f2b29fe055906399fe81353b509fa93877. Bugbot is set up for automated code reviews on this repo. Configure [here](https://www.cursor.com/dashboard/bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

## Evidence
- Implemented and verified strict API URL regex for video parsing.
- Triggered PR checks using the newly enforced CI tests.
- Captured dummy test video proo
