# PR #6087: [agento] fix(skeptic-cron): convert null mergeable to string for GHA output

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-04-04
**Author:** jleechan2015
**Stats:** +2/-2 in 1 files

## Summary
- **Root cause**: `mergeable: null` for PRs with `mergeable_state=unknown` caused GitHub Actions to reject the workflow output (GHA only accepts strings/numbers, not bare `null`)
- **Symptom**: "Unable to process file command output" failure + "Invalid format" annotation on `skeptic-cron` run #23969907460
- **Fix**: Pipe `mergeable` through `jq`'s `tostring` so `null` becomes "null" (valid JSON string)

## Test Plan
- [ ] Trigger `skeptic-cron` manually and confirm no GHA output errors
- [ ] Verify cron still processes PRs with known mergeable state correctly

🤖 Generated with [Claude Code](https://claude.ai/code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Low risk: workflow-only change that coerces `mergeable` to a string to avoid GitHub Actions output parsing failures when the API returns `null`. No application/runtime code paths are affected.
> 
> **Overview**
> Prevents `skeptic-cron` work

## Raw Body
## Summary

- **Root cause**: `mergeable: null` for PRs with `mergeable_state=unknown` caused GitHub Actions to reject the workflow output (GHA only accepts strings/numbers, not bare `null`)
- **Symptom**: "Unable to process file command output" failure + "Invalid format" annotation on `skeptic-cron` run #23969907460
- **Fix**: Pipe `mergeable` through `jq`'s `tostring` so `null` becomes "null" (valid JSON string)

## Test plan

- [ ] Trigger `skeptic-cron` manually and confirm no GHA output errors
- [ ] Verify cron still processes PRs with known mergeable state correctly

🤖 Generated with [Claude Code](https://claude.ai/code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Low risk: workflow-only change that coerces `mergeable` to a string to avoid GitHub Actions output parsing failures when the API returns `null`. No application/runtime code paths are affected.
> 
> **Overview**
> Prevents `skeptic-cron` workflow runs from failing when a PR’s `mergeable` value is `null`.
> 
> In the "List open PRs" step, the PR list JSON now stringifies `mergeable` via `jq` (`.mergeable | tostring`) for both the paginated call and the retry path, ensuring the `pr_numbers` job output never contains a bare `null`.
> 
> <sup>Reviewed by [Cursor Bugbot](https://cursor.com/bugbot) for commit c7b03afbce5d2147cfd97d2e58c4bc9d0a0edd0f. Bugbot is set up for automated code reviews on this repo. Configure [here](https://www.cursor.com/dashboard/bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->

## Summary by CodeRabbit

## Release Notes

* **Chores**
  * Improved reliability of pull request processing in automated workflows to ensure more consistent data handling.

<!-- end of auto-generated comment: release notes by coderabbit.ai -->
