# PR #6201: fix: un-nest social_hp_challenge/spicy/god_mode from rewards_box gate

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-04-11
**Author:** jleechan2015
**Stats:** +245/-31 in 2 files

## Summary
- **Bug**: `social_hp_challenge`, `recommend_spicy_mode`, `recommend_exit_spicy_mode`, and `god_mode_response` were gated inside `if hasattr(structured_response, "rewards_box")`, silently dropped on turns with no rewards_box (pure narrative/combat without XP)
- **Fix**: Moved all four fields to independent if-blocks at the same level as the rewards_box check, matching the pattern for `debug_info` after PR #6197
- **Test**: Added 4 new tests covering each formerly-gated field

## Raw Body
## Summary
- **Bug**: `social_hp_challenge`, `recommend_spicy_mode`, `recommend_exit_spicy_mode`, and `god_mode_response` were gated inside `if hasattr(structured_response, "rewards_box")`, silently dropped on turns with no rewards_box (pure narrative/combat without XP)
- **Fix**: Moved all four fields to independent if-blocks at the same level as the rewards_box check, matching the pattern for `debug_info` after PR #6197
- **Test**: Added 4 new tests covering each formerly-gated field

## Production Code Changes

`mvp_site/world_logic.py` — `process_action_unified`

Before: 4 fields nested inside `if hasattr(structured_response, "rewards_box")` block
After: Each field is an independent `if hasattr` check at top level alongside `rewards_box`

Also removed dead `debug_mode` parameter from `_make_request` test helper (Bugbot issue from PR #6197).

## Test Changes
- `_make_request()` — removed unused `debug_mode` param
- `_make_llm_response_with_fields(fields: dict)` — new flexible fixture for arbitrary structured_response fields
- `test_social_hp_challenge_emitted_without_rewards_box`
- `test_recommend_spicy_mode_emitted_without_rewards_box`
- `test_god_mode_response_emitted_without_rewards_box`
- `test_dice_rolls_emitted_without_rewards_box`

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Changes response-shaping logic in `process_action_unified`, which could alter client-visible payloads on turns without `rewards_box`. Scope is small and covered by new unit tests, but affects multiple structured fields returned to the UI.
> 
> **Overview**
> Fixes `process_action_unified` to no longer gate `social_hp_challenge`, `recommend_spicy_mode`, `recommend_exit_spicy_mode`, and `god_mode_response` on the presence of `structured_response.rewards_box`, ensuring these fields are emitted on narrative-only turns.
> 
> Adds targeted tests that construct mock LLM responses without `rewards_box` and assert each field is still surfaced; also removes an unused `debug_mode` 
