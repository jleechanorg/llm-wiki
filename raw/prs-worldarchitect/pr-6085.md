# PR #6085: chore(repo): ignore .beads/.beads/ SQLite files + add MCP installer script

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-04-04
**Author:** jleechan2015
**Stats:** +123/-0 in 2 files

## Summary
Two cleanup items found as untracked files in the worktree:
1. `.beads/.beads/` directory created by `br` (beads_rust) when running in worktrees — contains SQLite DB and WAL files that must not be committed
2. `scripts/install_worldai_mcp.sh` was authored on `feat/update-beads-skill-docs` but never merged to main

## Background
Two cleanup items found as untracked files in the worktree:
1. `.beads/.beads/` directory created by `br` (beads_rust) when running in worktrees — contains SQLite DB and WAL files that must not be committed
2. `scripts/install_worldai_mcp.sh` was authored on `feat/update-beads-skill-docs` but never merged to main

## Raw Body
## Background
Two cleanup items found as untracked files in the worktree:
1. `.beads/.beads/` directory created by `br` (beads_rust) when running in worktrees — contains SQLite DB and WAL files that must not be committed
2. `scripts/install_worldai_mcp.sh` was authored on `feat/update-beads-skill-docs` but never merged to main

## Goals
- Prevent accidental commits of beads SQLite binary files
- Land the MCP installer script onto main

## Tenets
- Minimal scope: no code changes, only gitignore + script file

## High-level description of changes
- `.gitignore`: add `.beads/.beads/` entry alongside existing `.beads/.br_history/` exclusion
- `scripts/install_worldai_mcp.sh`: installs worldai MCP servers into `~/.local` using `uv`, carried from `feat/update-beads-skill-docs`

## Testing
- `git check-ignore -v .beads/.beads/beads.db` confirms the new gitignore entry fires
- Script is a standalone bash installer, no CI tests required

## Low-level details
- **`.gitignore:267`**: added `.beads/.beads/` — the `br` CLI creates a nested `.beads/.beads/` when run from a worktree; the outer `.beads/` is tracked but inner SQLite DB files should not be
- **`scripts/install_worldai_mcp.sh`**: creates `~/.local/venvs/worldai-mcp`, installs MCP servers via `uv`, links binaries to `~/.local/bin`

## Verification
- `git check-ignore -v .beads/.beads/beads.db` → `.gitignore:267:.beads/.beads/`

🤖 Generated with [Claude Code](https://claude.ai/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Low risk: only updates `.gitignore` and adds a standalone local-dev install script; no runtime code paths are modified.
> 
> **Overview**
> Prevents accidental commits of `br`/beads worktree-generated SQLite state by adding `.beads/.beads/` to `.gitignore`.
> 
> Adds `scripts/install_worldai_mcp.sh`, a local installer that uses `uv` to create a dedicated venv under `~/.local/venvs/worldai-mcp`, install `mvp_site` requirements, and generate helper launch scripts (`worldai-mcp-act
