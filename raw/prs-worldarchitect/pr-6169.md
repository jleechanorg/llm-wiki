# PR #6169: [agento] fix(test_cerebras): avoid KeyError in planning_block assertion

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-04-09
**Author:** jleechan2015
**Stats:** +7/-3 in 1 files

## Summary
- Fix KeyError risk in `test_cerebras_provider.py` by using safe `.get()` chain instead of direct dict access on `properties["planning_block"]`
- Follows issue #3462 guidance: \"avoid KeyError in assertions (assert key presence first)\"

## Raw Body
## Summary
- Fix KeyError risk in `test_cerebras_provider.py` by using safe `.get()` chain instead of direct dict access on `properties["planning_block"]`
- Follows issue #3462 guidance: \"avoid KeyError in assertions (assert key presence first)\"

## Test Changes
- `mvp_site/tests/test_cerebras_provider.py`: Changed `properties["planning_block"].get("type")` to `properties.get("planning_block", {}).get("type")` — no behavioral change, just safer access

## Known Limitations
- Pre-existing bandit SHA1 warnings in `game_state.py` (not addressed here)

Closes #3462

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Test-only changes that make assertions and stubs more defensive without affecting runtime code paths.
> 
> **Overview**
> Tightens `test_cerebras_provider.py` to avoid a potential `KeyError` when asserting the `planning_block` schema type by using a safe `.get()` chain after confirming the key exists.
> 
> Also applies small, no-behavior-change formatting tweaks to mocked `generate_openai_compatible_content` return values in two tests.
> 
> <sup>Reviewed by [Cursor Bugbot](https://cursor.com/bugbot) for commit bec9dd725f8250301635250b185e2f58a4cbb040. Bugbot is set up for automated code reviews on this repo. Configure [here](https://www.cursor.com/dashboard/bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->

## Summary by CodeRabbit

* **Tests**
  * Improved robustness of JSON schema response handling in test assertions.
  * Updated mock payloads for better test reliability while maintaining existing behavior and error handling.

<!-- end of auto-generated comment: release notes by coderabbit.ai -->
