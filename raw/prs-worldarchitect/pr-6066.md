# PR #6066: fix(avatar-crop): correct drag bounds — both axes independently draggable

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-04-01
**Author:** jleechan2015
**Stats:** +2/-2 in 1 files

## Summary
- Fix avatar drag-to-reposition so both horizontal and vertical axes are independently draggable regardless of image aspect ratio
- Root cause: `Math.min(0, ...)` as upper bound locked one axis to zero

## Raw Body
## Summary
- Fix avatar drag-to-reposition so both horizontal and vertical axes are independently draggable regardless of image aspect ratio
- Root cause: `Math.min(0, ...)` as upper bound locked one axis to zero

## Changes
- `mvp_site/frontend_v1/js/avatar-crop.js:89-90`: swap `Math.min(0, Math.max(...))` → `Math.max(0, Math.min(...))` for both offsetX and offsetY

## Testing
- 39 avatar unit tests pass (pytest)
- Pre-commit checks pass (ESLint, mypy, bandit — no new issues)

Fixes cursor:worldai-6065-fix

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Small, localized UI math change affecting only avatar drag behavior; low chance of broader regressions beyond crop positioning edge cases.
> 
> **Overview**
> Fixes avatar crop dragging bounds by correcting the clamp logic for `offsetX`/`offsetY`, ensuring the image can be repositioned independently on both axes (especially for non-square aspect ratios) while still preventing dragging beyond the crop zone.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 318a033926a6330dfd931695643482b474d48de1. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->

## Summary by CodeRabbit

* **Bug Fixes**
  * Fixed image repositioning in the avatar crop tool. The drag-to-reposition functionality now correctly constrains image movement within the crop zone boundaries.

<!-- end of auto-generated comment: release notes by coderabbit.ai -->
