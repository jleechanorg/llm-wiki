# PR #6157: feat: add local venv bootstrap for testing_mcp tests

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-04-09
**Author:** jleechan2015
**Stats:** +171/-2 in 3 files

## Summary
- Add `_ensure_local_venv()` function that creates/validates local venv on-demand
- Add `WORLDARCH_LOCAL_VENV` path pointing to project-root venv
- Enables test isolation for `testing_mcp/` without relying on global venv

## Raw Body
## Summary
- Add `_ensure_local_venv()` function that creates/validates local venv on-demand
- Add `WORLDARCH_LOCAL_VENV` path pointing to project-root venv
- Enables test isolation for `testing_mcp/` without relying on global venv

## Changes
- `testing_mcp/lib/server_utils.py`: Added local venv bootstrap logic

## Testing
- [ ] Local venv created on first testing_mcp test run
- [ ] Dependencies installed from requirements.txt
- [ ] Tests run with isolated venv

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Introduces automatic venv creation/upgrade and dependency installation during `testing_mcp` server startup, which can affect local developer environments and adds new subprocess/locking failure modes. Runtime behavior in CI is mostly unchanged due to an explicit CI skip and interpreter fallback path.
> 
> **Overview**
> Adds on-demand local virtualenv bootstrapping to `testing_mcp` server startup: `start_local_mcp_server` now attempts `_ensure_local_venv()` (with a simple filesystem lock), installs `mvp_site/requirements.txt` when needed, and prefers `WORLDARCH_LOCAL_VENV` when ready while falling back to the existing shared venv or system Python.
> 
> Also updates tracking/docs by appending a few new beads entries in `.beads/issues.jsonl` and adding a roadmap note about the PR rescue pass.
> 
> <sup>Reviewed by [Cursor Bugbot](https://cursor.com/bugbot) for commit a1ad8122495754fa15b52324d9d6da63ffb8cbba. Bugbot is set up for automated code reviews on this repo. Configure [here](https://www.cursor.com/dashboard/bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->
## Summary by CodeRabbit

* **Bug Fixes & Improvements**
  * Added automatic local virtual environment bootstrap and readiness detection at server startup, with safer fallback to existing interpreters if bootstrap fails; startup logs a warning on bootstrap errors.

