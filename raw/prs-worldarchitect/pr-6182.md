# PR #6182: fix(deploy): add PRODUCTION_MODE=true for preview environment

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-04-10
**Author:** jleechan2015
**Stats:** +10/-3 in 2 files

## Summary
- Add `PRODUCTION_MODE=true` to the preview environment case in `deploy.sh`
- Preview deployments now enforce production auth like staging/stable
- Fixes bug rev-w1dt from PR #6174 where preview had weaker auth enforcement

## Test Plan
- [ ] Verify preview Cloud Run deployment includes `PRODUCTION_MODE=true` env var
- [ ] Confirm staging and stable cases remain unchanged

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Changes preview deployments to run with `PRODUCTION_MODE=true`, which can impact authentication/CI behavior in that environment if callers relied on the previous lax config. Workflow trigger adjustments are low risk but may change when th

## Raw Body
## Summary
- Add `PRODUCTION_MODE=true` to the preview environment case in `deploy.sh`
- Preview deployments now enforce production auth like staging/stable
- Fixes bug rev-w1dt from PR #6174 where preview had weaker auth enforcement

## Test plan
- [ ] Verify preview Cloud Run deployment includes `PRODUCTION_MODE=true` env var
- [ ] Confirm staging and stable cases remain unchanged

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Changes preview deployments to run with `PRODUCTION_MODE=true`, which can impact authentication/CI behavior in that environment if callers relied on the previous lax config. Workflow trigger adjustments are low risk but may change when the evidence gate runs.
> 
> **Overview**
> Preview deployments now set `PRODUCTION_MODE=true` in `deploy.sh`, aligning preview auth enforcement with staging/stable and updating the associated log message.
> 
> The `evidence-gate` GitHub workflow is expanded to include `deploy.sh` changes, renamed to **Evidence Gate**, and can now be run via `workflow_dispatch` (with an optional `pr_sha` input) in addition to PR path-based triggering.
> 
> <sup>Reviewed by [Cursor Bugbot](https://cursor.com/bugbot) for commit 43cebe69ee41434de351a86a941bd7ec5fa975e2. Bugbot is set up for automated code reviews on this repo. Configure [here](https://www.cursor.com/dashboard/bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->

## Summary by CodeRabbit

* **Chores**
  * Preview environment deployment configuration now enables production mode with updated logging.

<!-- end of auto-generated comment: release notes by coderabbit.ai -->
