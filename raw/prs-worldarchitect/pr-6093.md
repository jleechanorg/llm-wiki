# PR #6093: [agento] fix(security): remove unsigned JWT fallback on expired tokens

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-04-04
**Author:** jleechan2015
**Stats:** +138/-55 in 2 files

## Summary
- Removes the vulnerable unsigned JWT fallback at main.py:1610-1645
- Attackers could forge any user_id by crafting JWTs with valid structure but garbage signatures
- The check 'exp' in error_str.lower() was too broad (matched 'exception', 'expected', etc.)
- Added 3 security tests to prevent regression

## Raw Body
## Summary
- Removes the vulnerable unsigned JWT fallback at main.py:1610-1645
- Attackers could forge any user_id by crafting JWTs with valid structure but garbage signatures
- The check 'exp' in error_str.lower() was too broad (matched 'exception', 'expected', etc.)
- Added 3 security tests to prevent regression

## Changes
- mvp_site/main.py: Removed unsigned JWT fallback + unused base64 import
- mvp_site/tests/test_jwt_signature_bypass.py: New test file with 3 security tests

## Test Evidence
### JWT Security Tests (test_jwt_signature_bypass.py)


Fixes rev-i4wz.

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **High Risk**
> Touches authentication token verification logic; mistakes could lock out valid users or reopen a signature-bypass vulnerability. Added regression tests reduce risk but auth changes remain security-critical.
> 
> **Overview**
> Removes the previous *unsigned JWT payload fallback* that accepted tokens when `verify_id_token` raised expiry-related errors, ensuring all requests require successful Firebase signature verification.
> 
> Simplifies clock-skew handling to only adjust verification behavior when skew is high (`UseActualTime` + `check_revoked=False`) and adds security regression tests that attempt forged, garbage-signature JWTs and assert protected endpoints return `401` even when mocked Firebase errors mention expiry/`exp` substrings.
> 
> <sup>Reviewed by [Cursor Bugbot](https://cursor.com/bugbot) for commit 72af7af12e1da55d2aebb3c5952608e74a433f8e. Bugbot is set up for automated code reviews on this repo. Configure [here](https://www.cursor.com/dashboard/bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->
## Summary by CodeRabbit

* **Bug Fixes**
  * Enforced a consistent ID-token verification behavior by removing a lenient tolerance path, tightening authentication checks for protected endpoints.

* **Tests**
  * Added security tests to verify that forged or failing tokens are
