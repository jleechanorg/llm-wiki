# PR #6197: fix(debug_info): emit debug_info on all turns, not just when rewards_box present

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-04-11
**Author:** jleechan2015
**Stats:** +152/-3 in 2 files

## Summary
- **Bug**: `debug_info` was gated inside `if hasattr(structured_response, "rewards_box")`, so it was silently absent on turns with no combat/XP rewards (most turns), even when `debug_mode=True`
- **Fix**: Moved to the outer `if structured_response:` block alongside `dice_rolls` and other per-turn fields
- **TDD**: Red to Green via `TestDebugInfoIndependentOfRewardsBox`

## Raw Body
## Summary

- **Bug**: `debug_info` was gated inside `if hasattr(structured_response, "rewards_box")`, so it was silently absent on turns with no combat/XP rewards (most turns), even when `debug_mode=True`
- **Fix**: Moved to the outer `if structured_response:` block alongside `dice_rolls` and other per-turn fields
- **TDD**: Red to Green via `TestDebugInfoIndependentOfRewardsBox`

## Production Code Changes

`mvp_site/world_logic.py`

Before: `debug_info` emitted only inside rewards_box gate (lines ~6971-6973)

Now: emitted in `if structured_response:` block unconditionally when `debug_mode=True`

Why: The rewards_box is only present on combat/XP turns; all other turns would silently drop debug_info, making debug mode unreliable for most gameplay.

## Test Changes

Added `TestDebugInfoIndependentOfRewardsBox` in `test_world_logic.py`:
- `test_debug_info_emitted_when_no_rewards_box` - confirms debug_info present even when LLM omits rewards_box
- `test_debug_info_absent_when_debug_mode_off` - confirms no leakage when debug mode disabled

## Known Limitations

Does not address the separate dice_rolls/code_execution investigation (tracked separately, acknowledged in PR #6193).

Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Low risk: small change to response assembly that only affects optional `debug_info` output when `debug_mode=True`, with focused tests covering the regression and ensuring no leakage when debug mode is off.
> 
> **Overview**
> Fixes unified action responses to include `structured_response.debug_info` whenever `debug_mode=True`, even on turns where the LLM omits `rewards_box`.
> 
> Adds regression tests ensuring `debug_info` is emitted without `rewards_box` and remains absent when debug mode is disabled.
> 
> <sup>Reviewed by [Cursor Bugbot](https://cursor.com/bugbot) for commit bb3ba8342931822beef1cc0817e488b0f5ae2a30. Bugbot is set up for automated code reviews on this repo. Conf
