# PR #6171: [agento] ci(cleanup): delete PR preview Docker images on close + self-hosted runner enforcement

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-04-10
**Author:** jleechan2015
**Stats:** +639/-322 in 27 files

## Summary
- Adds Docker preview image deletion workflow (pr-cleanup.yml) to prevent Artifact Registry bloat
- Enforces self-hosted runners for private workflows across all workflow files
- Adds preview-image-janitor scheduled workflow to prune stale image digests
- Improves FastEmbed download retry/backoff with new unit tests

## Raw Body
## Summary
- Adds Docker preview image deletion workflow (pr-cleanup.yml) to prevent Artifact Registry bloat
- Enforces self-hosted runners for private workflows across all workflow files
- Adds preview-image-janitor scheduled workflow to prune stale image digests
- Improves FastEmbed download retry/backoff with new unit tests

## Production Code Changes
- `.github/workflows/pr-cleanup.yml`: +102 lines — Docker image deletion on PR close, image prune before label removal
- `.github/workflows/preview-image-janitor.yml`: +49 lines — scheduled cleanup of stale preview image digests
- 19+ workflow files: runner label updates to use `vars.SELF_HOSTED_RUNNER_LABELS`
- `scripts/preflight_model_docker.py`: retry/backoff improvements for FastEmbed downloads
- `mvp_site/tests/test_preflight_model_docker.py`: new unit tests for retry logic

## Test Changes
- Added test coverage for `preflight_model_docker.py` retry/backoff

## Known Limitations
- CI-only changes, no production application logic modified

Closes #6155 (merge conflicts resolved)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Medium risk because it changes CI/deploy infrastructure: runner selection is tightened to self-hosted and new workflows/scripts delete GCR image tags/digests, which could disrupt CI or remove needed images if misconfigured.
> 
> **Overview**
> **Previews/CI cleanup:** Adds automated cleanup for PR preview container artifacts, including `pr-cleanup.yml` deleting the PR’s `gcr.io/...:${commit-sha}` image on close and a new scheduled `preview-image-janitor.yml` that prunes stale preview digests via `.github/scripts/prune-preview-image-digests.sh`.
> 
> **Runner enforcement & reliability:** Standardizes many workflows to use the shared self-hosted runner selector (`vars.SELF_HOSTED_RUNNER_LABELS`) and updates checkouts to pin the PR head SHA with `persist-credentials: false`. Also hardens `scripts/preflight_model_docker.py` with configurable download retry/backoff and safer cache cl
