# PR #6195: fix(rewards): restore has_visible_content gate with progress_percent support

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-04-11
**Author:** jleechan2015
**Stats:** +131/-1 in 2 files

## Summary
- PR #6193 removed the `has_visible_content` gate from `normalize_rewards_box_for_ui` entirely, breaking the `None`-sentinel contract used by `_process_rewards_followup` (world_logic.py:1767-1774)
- Without the gate, `normalize_rewards_box_for_ui({})` returns non-None, causing `rewards_already_in_response=True` for any primary response without rewards, silently skipping the rewards followup
- Restores the gate but adds `progress_percent > 0` (the actual missing condition from the original regres

## Raw Body
[copilot] Addresses Codex P1 review on PR #6193.

## Summary

- PR #6193 removed the `has_visible_content` gate from `normalize_rewards_box_for_ui` entirely, breaking the `None`-sentinel contract used by `_process_rewards_followup` (world_logic.py:1767-1774)
- Without the gate, `normalize_rewards_box_for_ui({})` returns non-None, causing `rewards_already_in_response=True` for any primary response without rewards, silently skipping the rewards followup
- Restores the gate but adds `progress_percent > 0` (the actual missing condition from the original regression)

## Root Cause

`NarrativeResponse._validate_rewards_box(None)` always returns `{}`, so `structured_response.rewards_box` is always a dict. The check at world_logic.py:1767 relied on `normalize_rewards_box_for_ui({}) is None`. PR #6193 broke this sentinel.

## Fix

Restores gate in `mvp_site/rewards/builder.py` with `progress_percent > 0` added.

## Testing

- 157 reward tests pass
- Addresses: https://github.com/jleechanorg/worldarchitect.ai/pull/6193#discussion_r3067808529

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Changes the `normalize_rewards_box_for_ui` sentinel behavior that downstream response logic relies on, which can affect whether rewards followups are sent and displayed. Surface area is small and covered by new targeted unit tests, but it touches user-facing reward/progression payloads.
> 
> **Overview**
> Restores the `has_visible_content` gate in `normalize_rewards_box_for_ui` so inputs with no visible rewards return `None` again (re-establishing the downstream *"no rewards present"* sentinel behavior).
> 
> Extends visibility detection to treat `progress_percent > 0` as displayable content, and adds a new unit test suite covering the sentinel/visibility cases (empty dict, zero values, truthy `level_up_available`, loot normalization, and `progress_percent` behavior).
> 
> <sup>Reviewed by [Cursor Bugbot](https://cu
