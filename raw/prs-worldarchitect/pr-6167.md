# PR #6167: test: E2E test for rewards_box/planning_block atomicity

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-04-09
**Author:** jleechan2015
**Stats:** +645/-3 in 6 files

## Summary
- Add E2E test that verifies rewards_box and planning_block atomicity in both:
  - Immediate response path (process_action)
  - Polling path (get_campaign_state)

## Raw Body
## Summary
- Add E2E test that verifies rewards_box and planning_block atomicity in both:
  - Immediate response path (process_action)
  - Polling path (get_campaign_state)

## Testing
- Run against real MCP server at localhost:8080
- Uses real Firestore and Gemini LLM
- Pass rate: 1/1 (100%)

## Changes
- New test file: `testing_mcp/test_rewards_box_planning_block_e2e.py`

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Touches `world_logic` response shaping for both action processing and campaign-state polling, so regressions could affect level-up UX and client expectations for `rewards_box`/`planning_block` fields. Changes are bounded and covered by new unit and real E2E tests, but still impact a high-traffic gameplay path.
> 
> **Overview**
> Fixes a *level-up “badge without buttons”* inconsistency by enforcing atomicity between `rewards_box` and `planning_block`: when `rewards_box.level_up_available=true`, the server now ensures the planning block contains the required level-up choices (or suppresses both if it can’t safely inject).
> 
> Extends the polling path (`get_campaign_state_unified`) to return `rewards_box` and `planning_block` extracted from the latest Gemini story entry and applies the same stale-signal suppression + atomicity enforcement as the immediate action response.
> 
> Adds `FIELD_REWARDS_BOX` to structured field extraction, expands unit tests around atomicity/injection failure handling and JSON-serializable campaign state output, and introduces a real MCP+Firestore E2E test validating atomicity across immediate vs polling responses.
> 
> <sup>Reviewed by [Cursor Bugbot](https://cursor.com/bugbot) for commit e3f2b8d21ef8159f7c9d6942caf0c2f05147ab3c. Bugbot is set up for automated code reviews on this repo. Configure [here](https://www.cursor.com/dashboard/bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->
