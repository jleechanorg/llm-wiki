# PR #6114: fix(world_logic): gate level-up narrative on _resolve_level_up_signal

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-04-06
**Author:** jleechan2015
**Stats:** +743/-3 in 4 files

## Summary
- **Level-up narrative injection** now uses `_resolve_level_up_signal()` (same as choice injection) instead of only checking `rewards_pending.level_up_available`.
- Stops **stale** `rewards_pending` from injecting **LEVEL UP** banners when modal flags (e.g. `level_up_in_progress=False`) say level-up is inactive.
- Passes `rewards_box` into `_inject_levelup_narrative_if_needed` from both unified response paths.

## Raw Body
## Summary
- **Level-up narrative injection** now uses `_resolve_level_up_signal()` (same as choice injection) instead of only checking `rewards_pending.level_up_available`.
- Stops **stale** `rewards_pending` from injecting **LEVEL UP** banners when modal flags (e.g. `level_up_in_progress=False`) say level-up is inactive.
- Passes `rewards_box` into `_inject_levelup_narrative_if_needed` from both unified response paths.

## Production code
- `mvp_site/world_logic.py`: optional `rewards_box` param; gate + `resolved_new_level`; two call sites updated.

## Tests
- `mvp_site/tests/test_world_logic.py`: regression `test_inject_levelup_narrative_skipped_when_stale_modal_flags_suppress_signal`.
- Local: `pytest mvp_site/tests/test_world_logic.py::TestLevelUpInjection` → 9 passed.

## Known limitations
- Does not include unrelated wa-139 / Firestore `_DELETED` work; scoped to narrative/level-up signal parity only.


Made with [Cursor](https://cursor.com)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Touches level-up UX injection logic in `world_logic.py`; risk is limited to when/where level-up banners and choices appear, with a regression test added to cover stale-flag suppression.
> 
> **Overview**
> Fixes **stale level-up UI injections** by gating level-up narrative/banner insertion on `_resolve_level_up_signal()` (the same canonical signal used for choice injection), instead of relying on `rewards_pending.level_up_available` alone.
> 
> Updates the level-up injection helpers to accept/propagate an optional `rewards_box` and to derive `resolved_new_level` consistently, preventing incorrect **LEVEL UP** banners when modal flags (e.g. `level_up_in_progress=False`) explicitly suppress level-up. Adds a regression test in `test_world_logic.py` covering the stale-modal-flag suppression path.
> 
> <sup>Reviewed by [Cursor Bugbot](https://cursor.com/bugbot) for commit 9386a60eebfdeef5d3ab59bcb1741889033641af. Bugbot is set up for automated code reviews on this repo. C
