# PR #6132: feat(wizard): sync Dragon Knight default with canonical world module (wa-6123)

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-04-09
**Author:** jleechan2015
**Stats:** +478/-47 in 3 files

## Summary
- Align `CampaignWizard.DEFAULT_DRAGON_KNIGHT_DESCRIPTION` with the canonical `world_reference/campaign_module_dragon_knight.md` so the campaign wizard pre-fills the full Dragon Knight module (expanded Lumiel lore, full Dragon's Favor mechanics, class-adaptive Champion's Inheritance tables).
- Add an inline note that the JS default should stay mirrored when the markdown source changes.

## Raw Body
## Summary
- Align `CampaignWizard.DEFAULT_DRAGON_KNIGHT_DESCRIPTION` with the canonical `world_reference/campaign_module_dragon_knight.md` so the campaign wizard pre-fills the full Dragon Knight module (expanded Lumiel lore, full Dragon's Favor mechanics, class-adaptive Champion's Inheritance tables).
- Add an inline note that the JS default should stay mirrored when the markdown source changes.

## Production code
- **Before:** Abbreviated Dragon Knight copy embedded in `campaign-wizard.js` (condensed Dragon's Favor / Champion's Inheritance vs `world_reference`).
- **Now:** Full markdown-equivalent text in `DEFAULT_DRAGON_KNIGHT_DESCRIPTION` for parity with the world module file.
- **Why:** Single source of truth for published lore and GM mechanics; avoids drift between reference doc and first-run wizard text.

## Test changes
- None (existing JS tests reference the constant by symbol).

## Known limitations
- `app.js` `DRAGON_KNIGHT_NARRATIVE` remains the short legacy blurb for non-wizard flows; only the modern wizard default was expanded to match `world_reference`.

## Tracker
Related: **wa-6123-dragon-knight** (branch naming / internal tracker).

## Verification
- `node --check mvp_site/frontend_v1/js/campaign-wizard.js`
- `python3 -m pytest tests/ -x -q` (47 passed, 4 skipped)


Made with [Cursor](https://cursor.com)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Primarily content/copy expansion plus a new regression test and minor CI trigger tweaks; low risk beyond potential UX/performance impact from the larger default string.
> 
> **Overview**
> Updates the campaign wizard’s `DEFAULT_DRAGON_KNIGHT_DESCRIPTION` to match the full canonical `world_reference/campaign_module_dragon_knight.md`, so Dragon Knight campaigns prefill the expanded lore/mechanics rather than a condensed blurb.
> 
> Adds a Node unit test that exercises `handleCampaignTypeChange('dragon-knight')` and asserts the wizard description textarea exactly matches the canonical markdown,
