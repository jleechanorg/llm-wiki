# PR #6111: sync: update claude commands and hooks from user-scope

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-04-06
**Author:** jleechan2015
**Stats:** +1311/-59 in 5 files

## Summary
Syncs 5 files from user-scope config backup to worldarchitect.ai `.claude/`:

- **`compose-commands.sh`**: idle hook guard, worktree NO-PR sentinel, CR review staleness detection, silent passthrough (no UI banner when prompt is unmodified)
- **`UserPromptSubmit.sh`**: suppression sentinel, `find_conversation_file`, PR merge-state guard, session health thresholds (500/1000/2000 messages)
- **`claw.md`**: rewritten for OpenClaw native WebSocket dispatch via `openclaw agent` CLI
- **`exportcommands

## Test Plan
- [ ] Verify `compose-commands.sh` passes through plain prompts silently (no UI banner)
- [ ] Verify slash command composition still works
- [ ] Verify `UserPromptSubmit.sh` session warnings fire at thresholds

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Modifies Claude CLI hooks and task-dispatch routing logic, which can affect agent automation behavior and potentially suppress or misroute prompts if classification/g

## Raw Body
## Summary

Syncs 5 files from user-scope config backup to worldarchitect.ai `.claude/`:

- **`compose-commands.sh`**: idle hook guard, worktree NO-PR sentinel, CR review staleness detection, silent passthrough (no UI banner when prompt is unmodified)
- **`UserPromptSubmit.sh`**: suppression sentinel, `find_conversation_file`, PR merge-state guard, session health thresholds (500/1000/2000 messages)
- **`claw.md`**: rewritten for OpenClaw native WebSocket dispatch via `openclaw agent` CLI
- **`exportcommands.sh`**: rewritten with rsync behavior docs and content filters
- **`benchg-ts.md`**: fix hardcoded project path → `worldarchitect.ai`

## Test plan
- [ ] Verify `compose-commands.sh` passes through plain prompts silently (no UI banner)
- [ ] Verify slash command composition still works
- [ ] Verify `UserPromptSubmit.sh` session warnings fire at thresholds

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Modifies Claude CLI hooks and task-dispatch routing logic, which can affect agent automation behavior and potentially suppress or misroute prompts if classification/guards are wrong.
> 
> **Overview**
> Updates the `.claude` automation stack to reduce hook-induced loops and improve task dispatching.
> 
> Reworks `/claw` to *route coding tasks* through parallel `ao spawn`/`ao send` sessions (with bead creation, session detection, slash-command inlining, and delivery tracking) while keeping a gateway HTTP path for read-only tasks.
> 
> Hardens hooks to avoid noisy or stale injections: `compose-commands.sh` adds NO-PR/idle suppression, `{{pr_number}}` substitution, and a CodeRabbit review-SHA staleness check that strips outdated CR instruction blocks; `UserPromptSubmit.sh` adds suppression sentinels plus merged/closed PR guards before emitting session-size warnings.
> 
> Replaces `exportcommands.sh` with a self-contained exporter that union-merges global/project `.claude` dirs, applies de-ident
