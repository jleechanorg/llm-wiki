# PR #6109: feat(runners): zero-config start-runner.sh — no .env required

**Repo:** jleechanorg/worldarchitect.ai
**Merged:** 2026-04-07
**Author:** jleechan2015
**Stats:** +221/-39 in 4 files

## Summary
Managing GitHub Actions self-hosted runners required manually copying `.env.example` → `.env` and setting an `ACCESS_TOKEN`. This made setup on a new machine a multi-step manual process.

## Background
Managing GitHub Actions self-hosted runners required manually copying `.env.example` → `.env` and setting an `ACCESS_TOKEN`. This made setup on a new machine a multi-step manual process.

## Raw Body
## Background
Managing GitHub Actions self-hosted runners required manually copying `.env.example` → `.env` and setting an `ACCESS_TOKEN`. This made setup on a new machine a multi-step manual process.

## Goals
- `bash self-hosted-oss/start-runner.sh` works on any machine with `gh auth login` done — no other setup
- Automatically clean up stale runners and containers before starting fresh ones

## Tenets
- No new dependencies — uses `gh` CLI which is already required
- `.env` still works as an override for custom configs

## High-level description of changes
- If no `.env` / `ACCESS_TOKEN`, falls back to `gh auth token`
- Defaults `RUNNER_SCOPE=org` and `ORG_NAME=jleechanorg` so bare checkouts work
- Deregisters all offline GitHub runners for the org before starting
- Removes stale Docker containers matching the runner name prefix

## Testing
- Ran on this machine after wiping Docker: deregistered 3 stale runners, started 2 fresh org runners, picked up a CI job within 15s

## Low-level details
- **self-hosted-oss/start-runner.sh**: Sources `.env` if present, otherwise uses `gh auth token`. Added `deregister_offline_runners()` and `cleanup_stale_containers()` called before docker pull.

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Medium risk because `start-runner.sh` now performs automated cleanup (deregistering offline runners and removing containers) and changes auth/defaulting behavior, which could affect existing self-hosted runner setups if misconfigured.
> 
> **Overview**
> Enables **zero-config** self-hosted runner startup: `self-hosted-oss/start-runner.sh` now optionally loads `.env`, otherwise derives/uses defaults and pulls a token from `gh auth token`, with a `RUNNER_DRY_RUN` mode.
> 
> Before starting new containers it now **deregisters offline GitHub Actions runners** (prefix-matched, via `gh api`) and **removes stale indexed runner containers**, then proceeds to pull the runner image and start the requested runner count.
> 
> Separately, 
