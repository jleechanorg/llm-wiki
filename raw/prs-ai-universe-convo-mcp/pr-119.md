# PR #119: chore: reconcile Firestore indexes with deployed state

**Repo:** jleechanorg/ai_universe_convo_mcp
**Merged:** 2025-11-23
**Author:** jleechan2015
**Stats:** +7/-7 in 1 files

## Summary
- Synced firestore.indexes.json with what is actually deployed in production
- Added userId + createdAt DESC index (was deployed but missing from JSON)
- Removed unused userId + archived index (covered by 3-field archived index)

## Test Plan
- [x] Verified JSON is valid with jq
- [x] Deployed indexes with firebase deploy --only firestore:indexes
- [x] Verified deployed indexes match JSON via firebase firestore:indexes

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Syncs `firestore.indexes.json` to match production: adds `userId+createdAt DESC`, adds `userId+sessionId`, and moves archived filter to a 3-field index; retains message ordering indexes.
> 
> - **Firestore indexes 

## Raw Body
## Summary
- Synced firestore.indexes.json with what is actually deployed in production
- Added userId + createdAt DESC index (was deployed but missing from JSON)
- Removed unused userId + archived index (covered by 3-field archived index)

## Changes
The JSON file now accurately reflects the 6 indexes deployed to Firestore:
1. conversations: userId + updatedAt DESC - main list query
2. conversations: userId + createdAt DESC - sort by creation date
3. conversations: userId + sessionId - session lookup
4. conversations: userId + archived + updatedAt DESC - filter archived
5. messages: conversationId + timestamp - message ordering
6. messages: conversationId + sequence - message pagination

## Test plan
- [x] Verified JSON is valid with jq
- [x] Deployed indexes with firebase deploy --only firestore:indexes
- [x] Verified deployed indexes match JSON via firebase firestore:indexes

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Syncs `firestore.indexes.json` to match production: adds `userId+createdAt DESC`, adds `userId+sessionId`, and moves archived filter to a 3-field index; retains message ordering indexes.
> 
> - **Firestore indexes (`backend/firestore.indexes.json`)**:
>   - **conversations**:
>     - Add `userId ASC + createdAt DESC` index.
>     - Add `userId ASC + sessionId ASC` index (replaces 2-field archived index).
>     - Keep `userId ASC + updatedAt DESC`.
>     - Add `userId ASC + archived ASC + updatedAt DESC` for archived filtering.
>   - **messages**:
>     - Keep `conversationId ASC + timestamp ASC`.
>     - Keep `conversationId ASC + sequence ASC`.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 8bf852ad5df33fbf461ec41e1e3f3d6ad1cf8de6. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
