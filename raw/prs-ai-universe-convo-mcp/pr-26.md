# PR #26: fix: support convo_id alias for send-message auto-create

**Repo:** jleechanorg/ai_universe_convo_mcp
**Merged:** 2025-10-07
**Author:** jleechan2015
**Stats:** +180/-8 in 2 files
**Labels:** codex

## Summary
- normalize the send-message tool so it accepts the `convo_id` alias and auto-creates conversations when no id is provided
- expand logging for troubleshooting identifier mismatches and add unit coverage for the alias behaviour

## Raw Body
## Summary
- normalize the send-message tool so it accepts the `convo_id` alias and auto-creates conversations when no id is provided
- expand logging for troubleshooting identifier mismatches and add unit coverage for the alias behaviour

## Testing
- npm run test

------
https://chatgpt.com/codex/tasks/task_e_68e4e264e4d8832f946ace883043cb71

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Adds `convo_id` alias with normalization/precedence in `sendMessage`, expands logging, and adds unit tests for auto-create and alias behavior.
> 
> - **Backend Tools**:
>   - **`sendMessage` (`backend/src/tools/unified-mcp-tools.ts`)**:
>     - Accepts `convo_id` alias; trims IDs and normalizes to `conversationId` with `conversationId` taking precedence over `convo_id`.
>     - Introduces `normalizeConversationId` helper and `SendMessageParams` type; narrows `metadata` to `Record<string, unknown>`.
>     - Expanded logging includes provided identifiers on create/add and error paths.
> - **Tests**:
>   - **`backend/src/tools/unified-mcp-tools.test.ts`**: Adds unit tests covering auto-creation without IDs, alias `convo_id` usage for existing conversations, and fallback when `conversationId` is empty/whitespace.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit eb3c128adee8f96d4b3a086a29cb819d912d2358. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->

## Summary by CodeRabbit

- New Features
  - Added support for a convo_id alias to target conversations, alongside conversationId. The system selects the valid identifier and auto-creates a conversation when none is provided.
- Bug Fixes
  - Improved handling of empty or whitespace conversation identifiers, ensuring correct fallback to a valid ID and consistent message routing.

<!-- end of auto-generated comment: release notes by coderabbit.
