# PR #18: feat: make start convo take an initial message

**Repo:** jleechanorg/ai_universe_convo_mcp
**Merged:** 2025-10-07
**Author:** jleechan2015
**Stats:** +1193/-157 in 14 files
**Labels:** codex

## Summary
- allow `start-conversation` to accept an initial message and seed the legacy flow
- refresh the smoke tests/docs to cover legacy + unified APIs and resolve the base URL from CI (SERVICE_URL fallback + gcloud)
- make the conversation server boot logic Jest-friendly and add shared global mocks so unit tests run under CommonJS

## Raw Body
## Summary
- allow `start-conversation` to accept an initial message and seed the legacy flow
- refresh the smoke tests/docs to cover legacy + unified APIs and resolve the base URL from CI (SERVICE_URL fallback + gcloud)
- make the conversation server boot logic Jest-friendly and add shared global mocks so unit tests run under CommonJS

## Testing
- npm run test
- SERVICE_URL=http://localhost:3000 node testing_llm/smoke-test.js --base-url=http://localhost:3000 (requires local server)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Adds `message` alias to `start-conversation`, serves MCP metadata on GET `/mcp`, introduces PR preview health+smoke tests, and makes server/test setup Jest-friendly with new scripts and docs.
> 
> - **Backend (MCP Server)**
>   - `start-conversation`: accept `initialMessage` or `message` alias; seed first user message.
>   - New GET `/mcp` metadata endpoint with example payload.
>   - Optional unified tools via `createExpressApp({ includeUnifiedTools })`; tool list conditioned accordingly.
>   - Export test helpers: `resetConversationsForTest`, `stopRateLimitCleanupForTest`; hold interval handle for cleanup.
>   - Refined direct-execution detection for server start.
> - **Types**
>   - `StartConversationParams`: add optional `message` alias.
> - **CI/CD**
>   - New workflow `.github/workflows/pr-preview-smoke.yml` to run MCP smoke tests against preview URL.
>   - Enhanced `.github/workflows/pr-preview.yml`: capture PR metadata, fetch service URL, wait for health, comment links, then trigger smoke tests.
> - **Tests & Scripts**
>   - Add `backend/src/conversation-server.test.ts` and shared `backend/src/test/mocks/globalMocks.ts`.
>   - New Node smoke runner `scripts/mcp-ci-tests.mjs` and `npm run test:mcp`.
>   - New `testing_llm/smoke-test.js` and `test-start-conversation-message.js`; strengthen `test-start-conversation.js` assertions.
> - **Docs**
>   - Update READMEs to document `message` alias and smoke-test usage.
> 
> <sup>Written by [Curso
