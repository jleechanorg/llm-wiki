# PR #98: ci: share convo dev workflow

**Repo:** jleechanorg/ai_universe_convo_mcp
**Merged:** 2025-11-13
**Author:** jleechan2015
**Stats:** +230/-177 in 2 files

## Summary
- copy the dev deployment job into deploy-dev.yml so workflow_dispatch and auto-deploy share identical logic
- capture commit metadata via git, feed it into the Python failure issue builder, and add branch/reason context to summaries

## Raw Body
## Summary
- copy the dev deployment job into deploy-dev.yml so workflow_dispatch and auto-deploy share identical logic
- capture commit metadata via git, feed it into the Python failure issue builder, and add branch/reason context to summaries

## Testing
- not run (workflow-only change)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Create a reusable dev deployment workflow and update auto-deploy to invoke it with branch/reason context and commit metadata.
> 
> - **Workflows**:
>   - **New reusable `deploy-dev.yml`**:
>     - Supports `workflow_dispatch` and `workflow_call` with `reason` and `ref_name` inputs; sets GCP env vars.
>     - Captures commit message via git; deploys to Cloud Run; fetches service URL; performs health check.
>     - Writes rich deployment summary (branch, reason, commit, links); files GitHub issue on failure.
>   - **Refactor `auto-deploy-dev.yml`**:
>     - Simplifies job to call `deploy-dev.yml` on `push` to `main`, passing `reason`/`ref_name`, inheriting secrets, and setting minimal permissions.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit a74453103117accddad50dd943402134fb242a9c. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
