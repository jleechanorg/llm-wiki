# PR #31: fix: restore MAX_CONTENT_LENGTH to 200,000 characters

**Repo:** jleechanorg/ai_universe_convo_mcp
**Merged:** 2025-10-10
**Author:** jleechan2015
**Stats:** +41/-4 in 4 files

## Summary
Fixes critical regression in MAX_CONTENT_LENGTH that was dropped from 200,000 to 10,000 characters, breaking existing workflows that send longer content (LLM transcripts, attachments).

## Raw Body
## Summary
Fixes critical regression in MAX_CONTENT_LENGTH that was dropped from 200,000 to 10,000 characters, breaking existing workflows that send longer content (LLM transcripts, attachments).

## Bug Fixes

### High Priority: MAX_CONTENT_LENGTH Regression
- ✅ Restored `MAX_CONTENT_LENGTH = 200000` in all 3 files:
  - `backend/src/utils/validation.ts`
  - `backend/src/conversation-server.ts`
  - `backend/src/tools/unified-mcp-tools.ts`
- **Impact**: Clients can now send longer prompts without getting "content must be 10000 characters or less" errors
- **Safety**: Firestore supports ~1MB per field, so 200k is well within safe limits

### Medium Priority: Hardcoded Substring Value
- ✅ Fixed `backend/src/conversation-server.ts:256` to use `MAX_CONTENT_LENGTH` constant instead of hardcoded `10000`
- **Impact**: Future changes to MAX_CONTENT_LENGTH will be automatically reflected in sanitization logic

## Test Changes
- ✅ Updated `unified-conversation-storage.test.ts` to test updatedAt timestamp behavior

## Verification
- ✅ Type check passed
- ✅ Lint passed
- ✅ All MAX_CONTENT_LENGTH usages now consistent at 200,000

🤖 Generated with [Claude Code](https://claude.com/claude-code)
