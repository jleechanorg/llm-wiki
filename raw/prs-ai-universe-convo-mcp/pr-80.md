# PR #80: Ensure metadata consistency across conversation API endpoints

**Repo:** jleechanorg/ai_universe_convo_mcp
**Merged:** 2025-11-09
**Author:** jleechan2015
**Stats:** +228/-28 in 7 files

## Summary
(none)

## Raw Body
Audit and fix all conversation API endpoints to consistently return metadata:
- convo.get-history: Already returns metadata for each message ✓
- convo.send-message: Already returns metadata in response ✓
- convo.get-conversation: Already returns conversation metadata ✓
- convo.get-message: Already returns message metadata ✓
- convo.list-conversations: FIXED to include conversation metadata

Changes:
- Updated ConversationSummary type to include optional metadata field
- Modified storage layer to include metadata when listing conversations
- Updated tools layer to include metadata in list response
- Added comprehensive Matrix 6 tests for list-conversations metadata
- Generated evidence samples in /tmp/backend/metadata_regression/

Key guarantees:
- No manual JSON serialization - return structured objects directly
- No field stripping - Firestore reads are complete
- Nested structures like metadata.secondOpinion fully preserved
- Consistent behavior across all endpoints

All tests pass, type-check passes, lint passes.

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Adds conversation-level metadata to `convo.list-conversations` and ensures metadata is preserved/passed through across storage, tools, and client SDK.
> 
> - **Backend/Storage (`backend/src/firestore/unified-conversation-storage.ts`)**:
>   - Extend `ConversationSummary` with optional `metadata` and include it when listing conversations.
>   - Preserve and return message `metadata` immediately in create/add transactions; avoid extra reads except for timestamps.
> - **Tools/API (`backend/src/tools/unified-mcp-tools.ts`)**:
>   - `convo.list-conversations`: return `metadata` for each conversation.
>   - `convo.get-history` and `convo.get-message`: include `metadata` on messages when present.
>   - `convo.send-message`: propagate immediate `metadata` and `timestamp` in responses.
> - **Client SDK (`packages/conversation-client`)**:
>   - Types updated to include `metadata` in `SendMessageResponse
