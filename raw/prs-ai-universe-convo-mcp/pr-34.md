# PR #34: fix: add Firebase configuration to render.yaml

**Repo:** jleechanorg/ai_universe_convo_mcp
**Merged:** 2025-10-21
**Author:** jleechan2015
**Stats:** +18/-0 in 1 files

## Summary
Fixes conversation service failures on Render by adding missing Firebase configuration and API keys.

## Raw Body
## Summary
Fixes conversation service failures on Render by adding missing Firebase configuration and API keys.

## Changes
- ✅ Add  (correct Firebase project)
- ✅ Add  (real admin email, not test placeholder)
- ✅ Add  with  (backend code reads this variable)
- ✅ Add  as alias for compatibility  
- ✅ Add all model API keys: OpenRouter, Gemini, Perplexity, Grok

## Problem Fixed
Conversation MCP service was failing with:
```
[error]: Failed to create conversation with message
  "error": {},
```

Root cause: Missing  environment variable prevented Firestore initialization.

## Testing
- Render will auto-deploy on merge (autoDeploy: true)
- Monitor logs: `mcp__render__list_logs` for conversation creation success
- Verify no more "Failed to create conversation" errors

## Related
- Reverts incorrect PR jleechanorg/ai_universe#406 (was in wrong repo)
- Addresses P1 bugs identified by AI reviewers

🤖 Generated with Claude Code

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Add Firebase config and multiple model API key environment variables to `render.yaml` for the conversation MCP server.
> 
> - **Render infrastructure config (`render.yaml`)**:
>   - **Firebase**: add `FIREBASE_PROJECT_ID` (`ai-universe-b3551`) and `FIREBASE_ADMIN_EMAILS` (`jleechan@gmail.com`).
>   - **API keys**: add `CLAUDE_API_KEY` (alias `ANTHROPIC_API_KEY`), `OPENROUTER_API_KEY`, `GEMINI_API_KEY`, `PERPLEXITY_API_KEY`, `GROK_API_KEY` with `sync: false`.
>   - **Unchanged**: existing MCP and rate limiting env vars remain.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 1d9b98d57059a02e479fff165a4c97e851e5c197. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
