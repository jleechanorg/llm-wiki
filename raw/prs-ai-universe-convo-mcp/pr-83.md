# PR #83: Add JSON schemas to MCP conversation utilities

**Repo:** jleechanorg/ai_universe_convo_mcp
**Merged:** 2025-11-09
**Author:** jleechan2015
**Stats:** +2450/-15 in 12 files

## Summary
(none)

## Raw Body
- Add schemas.ts with JSON Schema Draft 7 compliant schemas for all tools
- Export schemas via @ai-universe/mcp-conversation-client/schemas
- Include request and response schemas for:
  - health-check
  - convo.send-message
  - convo.get-conversation
  - convo.get-history
  - convo.get-message
  - convo.list-conversations
- Update package.json to v2.1.0 with proper ES module exports
- Update tsconfig.json to use ES2020 modules
- Update README with comprehensive schema documentation and examples
- All tests passing, type-check and lint successful

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Adds Draft-07 JSON schemas for all MCP tools with new subpath exports, migrates package to ESM, updates docs, and extends conversation metadata with lastMessage.
> 
> - **Schemas**:
>   - Add Draft-07 JSON schemas (`src/schemas.ts`) for all tools (`health-check`, `convo.send-message`, `convo.get-conversation`, `convo.get-history`, `convo.get-message`, `convo.list-conversations`).
>   - Export schemas via `@ai-universe/mcp-conversation-client/schemas`; bundle `dist/schemas.{js,d.ts}`.
> - **Client API**:
>   - Export schemas from `src/index.ts` and `dist/index.{js,d.ts}`.
>   - Extend `ConversationMetadata` with `lastMessage`.
> - **Build/Packaging**:
>   - Migrate to ESM: set `type: module`, switch TS `module` to `ES2020`, and add exports map (including `./schemas`).
>   - Bump version to `v2.1.0`.
> - **Docs**:
>   - Update README with schema usage, validation examples, and version history.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 869c52fcbf405e47a872351546bd0780c6a9ae05. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
