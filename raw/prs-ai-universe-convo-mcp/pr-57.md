# PR #57: feat: enforce idempotent conversation persistence contract

**Repo:** jleechanorg/ai_universe_convo_mcp
**Merged:** 2025-11-04
**Author:** jleechan2015
**Stats:** +2836/-115 in 63 files

## Summary
- enforce server-side persist-or-error semantics with retries + telemetry and stricter ID validation per conversation persistence handoff
- add client guard/docs + bump @ai-universe/mcp-conversation-client to 2.0.4 with changelog entry
- capture shared contract doc so agents can drop synthetic fallbacks once upgraded

## Raw Body
## Summary
- enforce server-side persist-or-error semantics with retries + telemetry and stricter ID validation per conversation persistence handoff
- add client guard/docs + bump @ai-universe/mcp-conversation-client to 2.0.4 with changelog entry
- capture shared contract doc so agents can drop synthetic fallbacks once upgraded

## Testing
- npm run test (see /tmp/convo_mcp_test_evidence/npm-test.log)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Adds server-side persist-or-error with pre-generated IDs, retry/telemetry and stricter ID validation; updates the TS client to v2.0.6 with guards and docs; and expands tests.
> 
> - **Backend**:
>   - **Persistence & Idempotency**: Pre-generate and accept `conversationId`/`messageId` in `createConversationWithMessage` and `addMessageToConversation`; handle existing writes idempotently; preserve/convert timestamps.
>   - **Retries & Telemetry**: Add transient error detection, single retry wrapper, and emit `conversation_persistence` metrics with context; structured error logging.
>   - **Validation**: Reject whitespace IDs; normalize alias `convo_id`; enforce identifier presence after writes.
>   - **API Outputs**: `get-history` returns `conversationId`; `list-conversations` includes `totalCount` and `updatedAt`.
> - **Tools**:
>   - Update `sendMessage` flow to pre-reserve IDs, wrap storage calls with retry/metrics, confirm ownership post-add, and ensure returned IDs.
> - **Tests**:
>   - Add client guard tests for persist-or-error handling and server error surfacing.
>   - Expand tool tests for ID reservation, alias handling, whitespace rejection, retry vs. permanent errors, and missing IDs.
> - **Client (v2.0.6)**:
>   - Enforce persist-or-error in `sendMessage`; parse wrapped results; expose auth handler, circuit breaker, executor, and forwarders; rebuild dist.
> - **Docs**:
>   - Add persistence contract README; update package README/CHANGELOG; minor guideline note in `AGENTS.md`.
> 
> <sup>Written by [Cursor Bugbot](https:
