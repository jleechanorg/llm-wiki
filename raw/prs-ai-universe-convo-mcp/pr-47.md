# PR #47: feat: add get message tool

**Repo:** jleechanorg/ai_universe_convo_mcp
**Merged:** 2025-11-02
**Author:** jleechan2015
**Stats:** +284/-3 in 5 files
**Labels:** codex

## Summary
- add Firestore helper to fetch individual conversation messages with ownership validation
- expose a new `getMessage` MCP tool that formats message payloads for clients
- cover message retrieval with targeted unit tests including validation failures

## Raw Body
## Summary
- add Firestore helper to fetch individual conversation messages with ownership validation
- expose a new `getMessage` MCP tool that formats message payloads for clients
- cover message retrieval with targeted unit tests including validation failures

## Testing
- npm run test

------
https://chatgpt.com/codex/tasks/task_e_6906f630d7c4832fa2b79116d16cb694

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Adds Firestore-backed single-message retrieval and exposes it via `convo.get-message`, with server wiring and tests.
> 
> - **MCP Tools/Server**:
>   - Add `convo.get-message` handler in `backend/src/conversation-server.ts` with user validation and rate limiting; include in `tools/list` output.
> - **Unified Tools**:
>   - Implement `getMessage` in `backend/src/tools/unified-mcp-tools.ts` to fetch and format a single message (ISO timestamp, optional metadata) via storage.
> - **Firestore Storage**:
>   - Add `getConversationMessage` in `backend/src/firestore/unified-conversation-storage.ts` to fetch a message by `messageId` with ownership check.
> - **Tests**:
>   - Expand `backend/src/tools/unified-mcp-tools.test.ts` with unit tests for `getMessage` (trimming, not-found, validation).
>   - Update `testing_llm/smoke-test.js` to expect `convo.get-message` in tools list and verify single-message retrieval end-to-end.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 7508cdba4cc8744df99222eca6938e9a1c3ad727. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->
## Summary by CodeRabbit

* **New Features**
  * Added ability to fetch a single conversation message by ID (with validation and per-user rate limiting), exposed via the public tools surface.

* **Tests**
  * Expanded unit and end-to-end tests to cover message retrieval, validation, normalization, and missing-messa
