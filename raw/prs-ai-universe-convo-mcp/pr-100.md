# PR #100: ci: add manual convo deploy workflow

**Repo:** jleechanorg/ai_universe_convo_mcp
**Merged:** 2025-11-13
**Author:** jleechan2015
**Stats:** +21/-0 in 1 files

## Summary
- add a workflow_dispatch wrapper that forwards to deploy-dev.yml so we can manually trigger convo dev deploys without waiting on GitHub to refresh the reusable workflow metadata
- the wrapper exposes the same reason input and inherits secrets so behavior matches the auto-deploy path

## Raw Body
## Summary
- add a workflow_dispatch wrapper that forwards to deploy-dev.yml so we can manually trigger convo dev deploys without waiting on GitHub to refresh the reusable workflow metadata
- the wrapper exposes the same reason input and inherits secrets so behavior matches the auto-deploy path

## Testing
- not run (workflow-only change)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Add a manual GitHub Actions workflow that invokes `deploy-dev.yml` via `workflow_dispatch`, forwarding `reason` and `ref_name` and inheriting secrets.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit f1b6cb7247cf35f54eae8cc9d98a1f8c22bc8dfe. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
