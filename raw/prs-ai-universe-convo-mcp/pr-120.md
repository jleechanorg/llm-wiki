# PR #120: Store userid and email in Firestore

**Repo:** jleechanorg/ai_universe_convo_mcp
**Merged:** 2025-11-27
**Author:** jleechan2015
**Stats:** +329/-21 in 6 files

## Summary
(none)

## Raw Body
- Add userEmail field to Conversation type and ConversationMetadata
- Add UserProfile type for users collection
- Add email validation function with RFC 5322 compliant regex
- Create users collection reference in Firestore client
- Implement upsertUserProfile and getUserProfile functions
- Update createConversationWithMessage to accept and store email
- Update createEmptyConversation to accept and store email
- Add userEmail to conversation documents (denormalized)
- Update tool handlers to accept email parameter
- Add Firestore index for email-based queries on users collection

Email is stored both in the users collection (as user profile) and denormalized in conversation documents for efficient queries.

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Adds optional email to conversations, introduces Firestore `users` collection with upsert/get APIs, and updates tools/storage to validate, store, and expose user email.
> 
> - **Firestore storage**:
>   - Add `userEmail` to conversation documents and `ConversationMetadata`; store on create (`createConversationWithMessage`, `createEmptyConversation`).
>   - Introduce `users` collection via `usersRef()` and user profile types (`UserProfileData`).
>   - Implement user profile APIs: `upsertUserProfile`, `getUserProfile`, `getUserProfileByEmail` (email-normalized lookups).
>   - Background upsert of user profile when creating conversations.
> - **Validation**:
>   - New email utilities in `utils/email.ts` (`validateEmail`, regex, max length) used across storage and tools.
> - **Tools (MCP)**:
>   - `sendMessage` and `createEmptyConversation` accept optional `email`; validate/normalize and pass to storage.
>   - `getConversationMetadata` now returns `userEmail` when present.
> - **Types**:
>   - Update `Conversation` to include `userEmail`; add `UserProfile` interface.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 3cf01bf9fd68a32e67f9a9f45ce92a62ee641c53. This will update auto
