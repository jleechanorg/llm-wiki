---
name: mfm8tfz-stuck-levelup-stale-flag-openrouter-verdict-7262
description: MfM8TFz stuck LevelUpAgent = stale top-level level_up_pending modal-lock (prod GEMINI 3); #7251/#7261 both REFUTED on twin replay; #7262 router fix VERIFIED PASS on twin replay forced Gemini 3 (flag cleared, streak reset, 0 MODAL_LOCK)
metadata:
  node_type: memory
  type: project
  originSessionId: 15c06163-394b-4d82-97e5-d551f8fa1350
---

Twin-clone repro (original `MfM8TFz73DUeFFmt0mDb` untouched at turn 102; replay writes to twin under test account) gave a HIGH-confidence verdict on the stuck LevelUpAgent.

**State:** level 8 (Sylphina, Bladesinger), `experience.current` 36870–41820 / 48000 (NOT eligible for L9), `level_up_pending=True`, `_agent_selection_tracker.count` climbing 13→14→15 (LevelUpAgent streak).

**PROVIDER CORRECTION (owner-confirmed 2026-06-04): production is GEMINI 3, not OpenRouter.** The repro logs' `PROVIDER_SELECTION_FINAL: provider=openrouter, model=x-ai/grok-4.3` was a LOCAL-ENV FIDELITY BUG, not production truth: those lines are for TWIN campaign IDs (`UDvlDoi5zjXL8NpkmtA3`, `LwXd01wjDl4GtNyZqDhf`) under the test account (`0wf6sCRE…`, `TESTING_AUTH_BYPASS=true`); the test-bypass server saw owner `vnLp2G3m…`'s STORED OpenRouter key and set `has_byok=True`, routing to OpenRouter. Live Firestore (verified): `users/vnLp2G3m…/campaigns/MfM8TFz…` pins NO model/provider (all null); account doc has `openrouter_api_key` PRESENT but `openrouter_enabled=null` + `preferred_model`/`provider`/`selected_model` all null → production resolves to DEFAULT Gemini 3. So my earlier "provider is OpenRouter NOT Gemini" claim was WRONG.

**Bucket refutations:**
- **A — Gemini PROHIBITED_CONTENT: NOT the cause** (owner-confirmed + behavioral signature). My original refutation rationale ("impossible because OpenRouter") was based on the wrong provider and is VOID. Correct reasoning: a content block fails the WHOLE turn (empty stream + fallback); this campaign instead LOOPS on one agent with the flag stuck while the GM keeps narrating coherent 2000-char combat entries (`actor:"gemini"`, through turn 102) — that is the stale-flag signature, not a content-block signature. The merged [#7251](https://github.com/jleechanorg/worldarchitect.ai/pull/7251) no-cap prompt PR (built for the Gemini-blocked `mXhtOcc`) still does NOT apply to `MfM8TFz`.
- **B — level-20 cap (#7251): REFUTED.** Level 8, XP below L9. #7251 verified NOT to fix (twin streak climbed 13→14→15 on merged-#7251 tree).
- **C — XP canonicalization ([#7261](https://github.com/jleechanorg/worldarchitect.ai/pull/7261)): REFUTED as the fix.** Replayed a real turn on the #7261 branch (`731e06c8`): loop persisted identically — `level_up_pending` stayed True, streak 14→15, `MODAL_LOCK: Clearing stale` logged 0 times. #7261 only edits XP threshold fields (`needed_for_next_level`/`to_next_level`), never `level_up_pending`/MODAL_LOCK/`is_level_up_active()`.

**Root cause (confirmed on BOTH #7251 and #7261 trees):** stale `level_up_pending=True` with no actionable transition (`rewards_pending=None`, `rewards_box.level_up_available=false` → `is_level_up_active()=False`). The **streaming router locks MODAL_LOCK to LevelUpAgent on the raw flag** and never reaches the `world_logic.py:2501` stale-clear guard, so the flag never clears. Also: on origin/main `is_stale_level_up_pending` (`rewards_engine.py:1432`) reads `level_up_pending` from `custom_campaign_state` ONLY, so a top-level orphan flag is invisible to the detector (agents.py:1288 escape hatch never fires).

**Prescribed fix = gate MODAL_LOCK on `is_level_up_active()` not the raw flag** — which is exactly what open PR [#7262](https://github.com/jleechanorg/worldarchitect.ai/pull/7262) (`fix/stale-top-level-levelup-pending`, bead rev-x7pug) does to the router (`agents.py` +24/-15 + detector + game_state). [#7247](https://github.com/jleechanorg/worldarchitect.ai/pull/7247) is detector-only (necessary-but-not-sufficient; router may lock before the detector gate) and a duplicate subset → consolidate into #7262 (PR-proliferation). #7262 is the ONLY open PR matching the proven cause → arbiter twin replay of MfM8TFz on the #7262 branch **FORCED to Gemini 3** = **VERDICT PASS** (2026-06-04, branch `fix/stale-top-level-levelup-pending` @ `4d83bc0e20a869bcd17e40dec53b6c70f520c1ab`, real Firebase + real `gemini-3-flash-preview` via `FORCE_PROVIDER=gemini`, streaming `/interaction/stream` 56-event turn). Twin `dsveziSZFLOHHpjjFhu6` seeded with the EXACT orphan shape (`level_up_pending=true` TOP-LEVEL while `custom.level_up_pending=null`, streak `count=14 last_agent=LevelUpAgent priority=3_modal_level_up`); original `MfM8TFz` untouched (0 log refs). After the probe turn: `🔓 CHARACTER_CREATION_CHECK: Ignoring stale level_up_pending (XP below next-level threshold and no rewards_pending signal)` fired on the top-level flag → routed StoryModeAgent → `level_up_pending` cleared `true→false`, streak reset `14→1`, **`MODAL_LOCK: LevelUpAgent locked` = 0 occurrences** in the whole log. All 4 PASS criteria met. Contrast: #7261 replay streak 14→15, flag stayed True. Recommendation: merge #7262 (human-gated; needs CodeRabbit APPROVED + /green), consolidate detector-only #7247 into it. Provider-independent root cause held: the gate is upstream of the LLM call. Evidence: `/tmp/worldarchitect.ai/repro-MfM8TFz/classification_7262.md`, `twin_7262_v2_{pre,post}_replay_state.json`, `twin_7262_v2_replay_turn1.sse`, `decisive_log_lines_7262.txt`, `local_mcp_63279.log`. LESSON: local test-bypass server's BYOK resolution treats a stored-but-disabled `openrouter_api_key` as `has_byok=True` → routes to OpenRouter when production (enabled=null) uses default Gemini 3; never trust repro `PROVIDER_SELECTION_FINAL` as production provider — verify the live account/campaign Firestore doc.

Evidence: `/tmp/worldarchitect.ai/repro-MfM8TFz/` — `classification_7261.md`, `twin_7261_replay_turn1.sse` (592 chunks), `twin_7261_post_replay_state.json`, `decisive_log_lines_7261.txt`, plus prior #7251 bundle. See [[stuck-levelupagent-root-cause-no-cap-prompt-fix-pr]] and [[canonical-level-up-target-lives-in-result-rewards-box-not-the-stream-done-payload-2026-06-04]].
