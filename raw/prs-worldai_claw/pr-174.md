# PR #174: [worldai_claw] feat(parties): POST /parties endpoint with name field and auth scoping

**Repo:** jleechanorg/worldai_claw
**Merged:** 2026-04-03
**Author:** jleechan2015
**Stats:** +237/-127 in 7 files

## Summary
Supersedes closed PR #163. Implements POST /parties (wc-p1r) with additional rounds of CR feedback addressed, plus schema enhancements from continued Antigravity iteration.

## Background
Supersedes closed PR #163. Implements POST /parties (wc-p1r) with additional rounds of CR feedback addressed, plus schema enhancements from continued Antigravity iteration.

## Raw Body
## Background
Supersedes closed PR #163. Implements POST /parties (wc-p1r) with additional rounds of CR feedback addressed, plus schema enhancements from continued Antigravity iteration.

## Goals
- Implement POST /parties endpoint for party creation
- GET /sessions with proper isolation and repository wiring
- Auth scoping for party ownership
- Schema-level name and owner_id fields with idempotent migrations

## Tenets
- No breaking changes to existing session/turn API
- Idempotent migrations (ALTER TABLE guards)
- Zod validation at API boundary

## High-level description of changes
12 commits implementing the party creation endpoint across multiple CR review rounds:
1. Initial party routes + GET /sessions implementation
2. CR round 2: SQLite PRAGMA bootstrap, atomic join, test isolation
3. CR round 3+4: auth scoping, dead migration removal, SessionRow types
4. POST /sessions accountId alignment
5. Parties schema: name/owner_id columns + Zod request validation on POST /parties

## Testing
- Unit tests added for party creation and session isolation
- CI triggered on each push

## Low-level details
- **packages/backend/src/app.ts**: POST /parties with Zod-validated name field, auth scoping, correct INSERT/response shape
- **packages/backend/src/storage/sqlite_repository.ts**: parties table schema with name/owner_id columns, idempotent ALTER TABLE migrations

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Changes authentication and authorization paths for multiple HTTP endpoints and adds new DB-backed party metadata, which could affect access control and data isolation if misapplied. Also changes the response contract for `/entities/:id/verify`, requiring client/test updates.
> 
> **Overview**
> Tightens API auth/account scoping by resolving a stable `accountId` from **agent tokens, the dev token, or allowlisted Google ID tokens** and reusing it across routes (including `/sessions`, party join/leave, and registration-code rotation).
> 
> Extends HTTP multi
