# PR #171: [worldai_claw] feat(rpg-engine): WorldClaw 8-bit pixel-art RPG prototype

**Repo:** jleechanorg/worldai_claw
**Merged:** 2026-03-31
**Author:** jleechan2015
**Stats:** +235/-74 in 10 files

## Summary
Building the WorldClaw RPG Engine browser-based HTML5 prototype over Phaser 3.

## Background
Building the WorldClaw RPG Engine browser-based HTML5 prototype over Phaser 3.

## Raw Body
[claimed][done]\n[antig]

## Background
Building the WorldClaw RPG Engine browser-based HTML5 prototype over Phaser 3.

## Goals
Implement a prompt-to-JSON generation pipeline using the Claude API, rendering a playable 8-bit RPG pixel-art map.

## Tenets
Keep dependencies light (only phaser and vite). Do not hardcode assets, generate them procedurally or fetch them dynamically to ensure maximum flexibility and 8-bit aesthetics.

## High-level description
- Integrated Vite dev server config
- Developed UI for theme selection and prompt input
- Added procedural sprite generation with Phaser 3 Graphics
- Rendered tilemap and mapped dynamic objects
- Integrated Anthropic LLM JSON schema enforcing strict layout

## Testing
- Verified development server startup completely without errors.

## Details
Adds RPG engine in worldai_claw/rpg-engine/

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Updates LLM integration and schema validation paths used to generate worlds, which could break generation or reject previously accepted responses. Also changes spawn handling and input/ARIA behavior, with moderate risk of runtime UX regressions.
> 
> **Overview**
> Tightens the world-generation pipeline by **strengthening JSON extraction and schema enforcement** (including walkability checks for `spawn_point`, NPCs, and items) and updating the model/tooling used to request structured `generate_world` output.
> 
> Improves runtime robustness in `worldai-rpg` by validating `worldData` on scene init, using safer spawn fallbacks, and skipping invalid NPC/item coordinates with clearer warnings. Separately, makes small UI/accessibility tweaks (ARIA roles/pressed state, input sizing) and clarifies docs/architecture guidance around keeping Anthropic API keys server-side.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit e7dcb0e75c3891e2a6e4532fbbce9bcde3dccea7. This will update automatically on new commits. Configure [here](https://cursor.com/d
