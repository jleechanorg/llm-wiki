# PR #157: [agento] feat(testing): M5c 4-layer chat streaming flow integration test

**Repo:** jleechanorg/worldai_claw
**Merged:** 2026-03-31
**Author:** jleechan2015
**Stats:** +244/-0 in 2 files

## Summary
- Add `testing_mcp/test_streaming_flow.py` — a real integration test for the 4-layer SSE streaming chain
- Exercises: client POST `/sessions/:id/turn` → server turn endpoint → OpenClaw gateway → SSE event emission back to client
- Verifies: (1) SSE stream returns HTTP 200, (2) at least one delta/chunk event has text content, (3) stream closes with a done/complete event
- Uses `testing_mcp` style: real local server via `running_backend()`, no mocks

## Test Plan
- [x] `pytest test_streaming_flow.py` — PASS in 13.93s

Closes #d1z

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Medium risk because it introduces a real-backend/LLM integration test that may be slow or flaky in CI due to network/gateway dependencies and long timeouts.
> 
> **Overview**
> Adds a new `@pytest.mark.integration` test (`test_streaming_flow.py`) that boots a real backend, creates a session, then calls `POS

## Raw Body
## Summary
- Add `testing_mcp/test_streaming_flow.py` — a real integration test for the 4-layer SSE streaming chain
- Exercises: client POST `/sessions/:id/turn` → server turn endpoint → OpenClaw gateway → SSE event emission back to client
- Verifies: (1) SSE stream returns HTTP 200, (2) at least one delta/chunk event has text content, (3) stream closes with a done/complete event
- Uses `testing_mcp` style: real local server via `running_backend()`, no mocks

## Evidence
- Test run: 16 SSE events, 13 delta chunks, 1 done event — all assertions passed

## Test plan
- [x] `pytest test_streaming_flow.py` — PASS in 13.93s

Closes #d1z

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Medium risk because it introduces a real-backend/LLM integration test that may be slow or flaky in CI due to network/gateway dependencies and long timeouts.
> 
> **Overview**
> Adds a new `@pytest.mark.integration` test (`test_streaming_flow.py`) that boots a real backend, creates a session, then calls `POST /sessions/:id/turn` with SSE enabled and asserts **SSE content-type**, presence of at least one `chunk` event with non-empty text, and a terminating `done` event (failing on any `error` events and capturing evidence artifacts).
> 
> Updates `pytest.ini` to register an `integration` marker for these real backend/LLM tests.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 679c3ed2cc34c68822cfeaa7ad7bfbdda819d80f. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->
## Summary by CodeRabbit

* **Tests**
  * Added an end-to-end integration test for SSE streaming that validates session creation and streaming responses. It captures evidence on errors, parses line-based server-sent events, ensures no error events, requires a
