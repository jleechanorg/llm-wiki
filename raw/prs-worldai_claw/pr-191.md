# PR #191: [antig] fix: complete consolidation of browser test base files (wc-0ou)

**Repo:** jleechanorg/worldai_claw
**Merged:** 2026-04-03
**Author:** jleechan2015
**Stats:** +9/-9 in 9 files

## Summary
(none)

## Raw Body
### Background
There were two browser test base files: `testing_ui/browser_test_base.py` (older, previously reduced to a one-line re-export via PR #187) and `testing_ui/lib/browser_test_base.py` (comprehensive, real base class). The remaining importers were holding up the removal of the legacy file.

### Goals
- Migrate remaining importers of `testing_ui.browser_test_base` across all test files and markdown docs.
- Fully remove the legacy `testing_ui/browser_test_base.py` file.
- Evaluate whether the inline `UnifiedServerManager` legacy server bootrapper can be replaced with `testing_ui.conftest._ensure_service`.

### Tenets
- Keep test infrastructure completely unified.
- Minimize obsolete files.

### High-level description
- Updated 6 Python test files and 1 markdown file to correctly import from `testing_ui.lib.browser_test_base`.
- Deleted `testing_ui/browser_test_base.py`.
- Evaluated `UnifiedServerManager`: manual Python regression script runs (`if __name__ == '__main__':`) without Pytest depend directly on it for boot-strapping. It cannot be fully removed until those scripts are refactored under `pytest` format natively using `ensure_test_servers`.

### Testing
- Validated `python -m py_compile` across all modified scripts.
- Scripts still instantiate and utilize `LegacyBrowserTestBase` cleanly through the new import points.

### Low-level details
- `scripts/ralph/CLAUDE-video.md`: Updated documentation paths.
- `testing_ui/lib/browser_test_base.py`: Ensured `BrowserTestBase` class alias is actively exported.
- `testing_ui/test_companion_panel_browser.py`, etc.: Updated imports.

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Low risk refactor confined to test infrastructure and documentation, mainly changing import paths and deleting a redundant module. Risk is limited to potential breakage of ad-hoc test runners that still referenced the removed `testing_ui/browser_test_base.py` path.
> 
> **Overview**
> Completes consolidation of the UI test harne
