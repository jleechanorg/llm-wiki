# PR #142: [agento] fix: stabilize web integration tests to reflect current MVP behavior

**Repo:** jleechanorg/worldai_claw
**Merged:** 2026-03-29
**Author:** jleechan2015
**Stats:** +35/-18 in 5 files

## Summary
Stabilizes 12 failing web integration tests in `packages/web/tests/` by aligning test expectations with the current MVP implementation.

## Raw Body
## Summary

Stabilizes 12 failing web integration tests in `packages/web/tests/` by aligning test expectations with the current MVP implementation.

## Changes

### `packages/web/src/App.tsx`
- Updated game screen API path from `/api/campaigns/${id}` to `/api/worlds/${id}` to match backend routing
- Updated error message to "Failed to fetch world"

### `packages/web/src/screens/Dashboard.tsx`
- Updated API path from `/api/campaigns` to `/api/worlds`
- Updated error message to "Failed to fetch worlds"

### `packages/web/tests/gameScreen.test.tsx`
- Updated button name assertions from `View World`/`View Characters` to `Show World Info`/`Show Characters`
- Fixed `toggles world info section` test: "World Info" appears in both the section heading and the toggle button, so use `getAllByText` and check the heading specifically after toggle
- Fixed initial state: `showWorldInfo=true` → button shows "Hide World Info" not "Show World Info"

### `packages/web/tests/app.test.tsx`
- Fixed "Skip setup" flow: requires two clicks — first shows confirm dialog, second calls `skipToDashboard`
- Fixed `skip setup persists auth token`: added second confirm click

### `packages/web/tests/4layer/routeIntegration.test.tsx`
- Updated button name assertions to match component initial state ("Hide World Info", "Hide Characters")

## Testing

All 115 tests in `packages/web/tests/` pass:

```
Test Files  10 passed (10)
     Tests  115 passed (115)
```

Closes: worldai_claw integration test stabilization

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Low risk: primarily updates API route strings and adjusts test expectations/flows to match current UI behavior, with minimal logic changes.
> 
> **Overview**
> Updates the web app to fetch world data from the `worlds` API routes (switching `/api/campaigns` → `/api/worlds` in the game bootstrap and dashboard) and adjusts corresponding error messages.
> 
> Stabilizes failing tes
