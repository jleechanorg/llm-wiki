# PR #121: [agento] [P2] feat(worldai-claw): agent-first setup — registration codes, MCP tools, skill.md

**Repo:** jleechanorg/worldai_claw
**Merged:** 2026-03-29
**Author:** jleechan2015
**Stats:** +1018/-14 in 8 files

## Summary
- Implements **Phase 1** of `worldai_claw_agent_setup_design.md`: agent-first onboarding with registration codes and MCP-first integration
- Backend: SQLite registration codes + agent credentials, HTTP auth endpoints, `register`/`get_status` MCP tools, `/skill.md` agent instruction contract
- Frontend: `/agent-setup` 3-step screen (OpenClaw / Claude Code / Claude Desktop tabs) with copy-ready snippets and starter prompt

## Test Plan
- [ ] `curl http://localhost:3000/skill.md` returns the agent instruction contract
- [ ] `POST /api/agent/registration-code` returns a code
- [ ] `POST /api/agent/register` with valid code returns `{ account_id, credential_id, secret }`
- [ ] `POST /api/agent/login` with valid credentials returns `{ auth_token }`
- [ ] `POST /api/agent/registration-code/rotate` invalidates prior code
- [ ] MCP `tools/list` includes `register` and `get_status`
- [ ] Frontend: navigate to `/agent-setup`, generate 

## Raw Body
## Summary
- Implements **Phase 1** of `worldai_claw_agent_setup_design.md`: agent-first onboarding with registration codes and MCP-first integration
- Backend: SQLite registration codes + agent credentials, HTTP auth endpoints, `register`/`get_status` MCP tools, `/skill.md` agent instruction contract
- Frontend: `/agent-setup` 3-step screen (OpenClaw / Claude Code / Claude Desktop tabs) with copy-ready snippets and starter prompt

## Test plan
- [ ] `curl http://localhost:3000/skill.md` returns the agent instruction contract
- [ ] `POST /api/agent/registration-code` returns a code
- [ ] `POST /api/agent/register` with valid code returns `{ account_id, credential_id, secret }`
- [ ] `POST /api/agent/login` with valid credentials returns `{ auth_token }`
- [ ] `POST /api/agent/registration-code/rotate` invalidates prior code
- [ ] MCP `tools/list` includes `register` and `get_status`
- [ ] Frontend: navigate to `/agent-setup`, generate code, copy snippet

## Testing
Server not running in this environment — real smoke test deferred to CI or local run.

🤖 Generated with [Claude Code](https://claude.ai/code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Adds a new agent authentication/onboarding flow (registration codes, credentials, bearer tokens) plus new unauthenticated endpoints and SQLite schema changes, which could impact security and persistence if misconfigured. MCP now conditionally authorizes `get_status` based on bearer tokens, so incorrect header handling could break agent clients.
> 
> **Overview**
> Enables **agent-first onboarding** by adding backend support for registration codes, one-time agent credentials, and bearer auth tokens, plus a public `/skill.md` instruction contract.
> 
> Introduces new HTTP endpoints under `/api/agent/*` for generating/rotating registration codes, registering, and logging in, and extends the SQLite schema to persist `registration_codes`, `agent_credentials`, and `agent_tokens` (with hashing) as well as `campaig
