# PR #123: [agento] test(worldai_claw-a6f): add context blocks non-empty assertion

**Repo:** jleechanorg/worldai_claw
**Merged:** 2026-03-29
**Author:** jleechan2015
**Stats:** +87/-24 in 2 files

## Summary
The worldai_claw-a6f bead requires wiring ContextBuilder into the turn route for per-turn game state context injection. ContextBuilder was already wired into app.ts handleTurnRoute in worldai_claw-n3o. This PR adds the remaining test assertion from the bead.

## Background
The worldai_claw-a6f bead requires wiring ContextBuilder into the turn route for per-turn game state context injection. ContextBuilder was already wired into app.ts handleTurnRoute in worldai_claw-n3o. This PR adds the remaining test assertion from the bead.

## Raw Body
## Background
The worldai_claw-a6f bead requires wiring ContextBuilder into the turn route for per-turn game state context injection. ContextBuilder was already wired into app.ts handleTurnRoute in worldai_claw-n3o. This PR adds the remaining test assertion from the bead.

## Goals
- Add assertion that context blocks are non-empty after session has accumulated history

## Tenets
- Prefer targeted integration tests over mocking
- No changes to production code (implementation already complete)

## High-level description
Adds should return non-empty context blocks when session has accumulated history to context_wiring.test.ts. Simulates a session with player data, location, party, quest objectives, and accumulated memory entries, then asserts buildContextInjection produces all expected block types.

## Testing
- Single new test added to packages/backend/src/__tests__/context_wiring.test.ts
- All 4 context block types asserted: compacted_history, pinned_critical_memory, rolling_memory, recent_transcript, game_state

Closes worldai_claw-a6f.

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Low risk: changes are limited to test coverage (plus minor JSONL text normalization) and do not alter production behavior.
> 
> **Overview**
> Adds a new `context_wiring` test that simulates a session with accumulated memory/history and asserts `buildContextInjection` emits **non-empty**, correctly scoped blocks for `compacted_history`, `pinned_critical_memory`, `rolling_memory`, `recent_transcript`, and `game_state`.
> 
> Also tightens an existing `normalizeSessionMemory` assertion to require identity (`toBe`) for pass-through objects, and normalizes punctuation/encoding in `.beads/issues.jsonl` descriptions.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 8e0ab21157dffd6593c960ae790ae61cd2961940. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
