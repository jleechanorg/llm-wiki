# PR #119: [agento] feat: enforce dice mandate in narrative (worldai_claw-4f8)

**Repo:** jleechanorg/worldai_claw
**Merged:** 2026-03-29
**Author:** jleechan2015
**Stats:** +162/-0 in 2 files

## Summary
(none)

## Raw Body
### Background
Enforce server-side dice rolling by flagging dice notation and results in LLM-generated narrative.

### Goals
- Prevent LLM from fabricating roll results.
- Keep narrative clean of dice notation.
- Ensure all rolls are handled server-side.

### Testing
- Added unit tests in packages/backend/tests/dice_mandate.test.ts.
- Verified detection of 1d20, 2d6+5, and [18] patterns.
- Verified case-insensitivity.

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Adds new narrative-validation logic that may flag/deny LLM responses based on regex keyword matching, which risks false positives/negatives if enforced in production. Scope is limited to a new helper plus unit tests, with no other call sites found in the backend.
> 
> **Overview**
> Adds `validateDiceMandate()` to `system_instruction.ts` to enforce a server-side dice mandate by detecting dice notation (e.g. `1d20`, `2d6+5`), bracketed roll results (e.g. `[18]`), and roll-related keywords without accompanying `mechanic_requests`.
> 
> Introduces unit tests (`dice_mandate.test.ts`) covering detection/allowlist cases for dice patterns, fabricated results, and the presence/absence of `mechanic_requests`.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 8eed3027fdc075242b0cb36731097d6122687de4. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
