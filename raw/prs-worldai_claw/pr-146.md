# PR #146: [agento] feat(web): enforce backend verification before setup can be skipped

**Repo:** jleechanorg/worldai_claw
**Merged:** 2026-03-29
**Author:** jleechan2015
**Stats:** +222/-14 in 3 files

## Summary
Enforces that users must successfully run the OpenClaw backend connection test before the "skip" option appears in the setup wizard. Prevents users from landing in the dashboard with broken runtime config.

## Test Plan
- [x] `npx vitest run` in `packages/web` — 158 tests pass
- [x] Setup wizard blocks skip when backend never tested
- [x] Setup wizard allows skip after successful connection test
- [x] Setup wizard shows error and stays on page when `skipToDashboard` network call fails

## Raw Body
## Summary

Enforces that users must successfully run the OpenClaw backend connection test before the "skip" option appears in the setup wizard. Prevents users from landing in the dashboard with broken runtime config.

## Changes

### `packages/web/src/context/AppContext.tsx`
- Add `backendVerified: boolean` to context state + setter
- Add `seed` prop to `AppProvider` for test injection of initial state

### `packages/web/src/App.tsx`
- `SetupWizard`: reads `backendVerified` from context; skip button is hidden when `backendVerified=false` (connection never tested). After successful test, `backendVerified` is set to true
- Skip button remains available via two-click confirm after test succeeds (for offline/degraded scenarios)
- `App`: accepts optional `seed` prop to pass through to `AppProvider`

### `packages/web/tests/app.test.tsx`
- 4 new tests: blocks nav when never tested, allows nav after successful test, persistent error on network failure, dashboard reachable with openclaw_url in localStorage
- All 10 app tests pass (158 total)

## Test plan

- [x] `npx vitest run` in `packages/web` — 158 tests pass
- [x] Setup wizard blocks skip when backend never tested
- [x] Setup wizard allows skip after successful connection test
- [x] Setup wizard shows error and stays on page when `skipToDashboard` network call fails

## Related

Closes wc-b3s

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Changes setup wizard gating and stores a new verification key in global context, which can alter onboarding/navigation behavior if the key is miscomputed or not persisted as intended. Test coverage is expanded, but the new state introduces subtle UI conditions around URL/token trimming and step back/forward flows.
> 
> **Overview**
> Enforces that the setup wizard’s **“Skip setup”** path is only available after a successful OpenClaw `test-connection` for the *current* URL/token pair, by tracking a `verifiedB
