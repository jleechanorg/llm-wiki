# PR #223: [agento] Fix faction sim concurrency bug (#80)

**Repo:** jleechanorg/worldai_claw
**Merged:** 2026-04-05
**Author:** jleechan2015
**Stats:** +160/-21 in 2 files

## Summary
(none)

## Raw Body
Fixes #80

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Touches turn-time living-world simulation gating and campaign/session bootstrap inputs, which can affect gameplay flow and prompt generation if misconfigured. Concurrency protection is in-memory only, so behavior differs in multi-replica deployments.
> 
> **Overview**
> Prevents **concurrent living-world faction simulations** from running for the same `campaignId` by adding a per-campaign in-flight lock alongside the existing cooldown, ensuring `processInPlayTriggers` executes at most once at a time per campaign.
> 
> Extends campaign/session bootstrap to accept and persist a `mechanics` setting (defaulting to `dnd5e`), uses `buildSystemInstruction` more broadly when creating campaigns/sessions, and tightens `WorldBootstrapConfig.universeType` typing.
> 
> Adds a regression test that fires two simultaneous turns from different sessions sharing a campaign and asserts only one simulation runs.
> 
> <sup>Reviewed by [Cursor Bugbot](https://cursor.com/bugbot) for commit 6fea6117f1d8a728815fc9e59dccf66d1ff0c144. Bugbot is set up for automated code reviews on this repo. Configure [here](https://www.cursor.com/dashboard/bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->
## Summary by CodeRabbit

* **New Features**
  * Campaigns and sessions now persist and propagate a selectable "mechanics" setting (default: "dnd5e"), which influences session initialization and system prompt behavior.

* **Bug Fixes**
  * Prevented concurrent faction simulations for the same campaign so only one runs at a time, avoiding duplicate processing across sessions.

* **Tests**
  * Added integration test validating campaign-scoped concurrency control for faction simulations.
<!-- end of auto-generated comment: release notes by coderabbit.ai -->
