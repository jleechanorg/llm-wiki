# PR #159: [agento] feat(api): GET /entities/:id/verify returns {entityId, chainLength, lastHash, verified}

**Repo:** jleechanorg/worldai_claw
**Merged:** 2026-03-31
**Author:** jleechan2015
**Stats:** +120/-38 in 3 files

## Summary
- Updates GET /entities/:id/verify route to use getEntityChain() factory and return the required shape {entityId, chainLength, lastHash, verified: boolean}
- Exports hashBlock() from entity_chain.ts for computing the last block hash
- Updates existing integration test to assert the new response shape

## Test Plan
- [x] Integration test: packages/backend/tests/entity_verify_endpoint.test.ts — asserts correct shape, verified=true for valid/tampered chains
- [ ] CI tests pass

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Medium risk because it changes the public response schema/semantics of `/entities/:id/verify` and rewrites verification logic to bypass the existing `verifyEntityChain`/IPFS path, which could affect consumers and 

## Raw Body
## Summary
- Updates GET /entities/:id/verify route to use getEntityChain() factory and return the required shape {entityId, chainLength, lastHash, verified: boolean}
- Exports hashBlock() from entity_chain.ts for computing the last block hash
- Updates existing integration test to assert the new response shape

## Test plan
- [x] Integration test: packages/backend/tests/entity_verify_endpoint.test.ts — asserts correct shape, verified=true for valid/tampered chains
- [ ] CI tests pass

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Medium risk because it changes the public response schema/semantics of `/entities/:id/verify` and rewrites verification logic to bypass the existing `verifyEntityChain`/IPFS path, which could affect consumers and edge-case correctness.
> 
> **Overview**
> **Updates `/entities/:id/verify` to a new response contract**: it now always returns `{ entityId, chainLength, lastHash, verified }`, with missing entities returning `verified: false` and empty fields.
> 
> **Reworks verification to be DB-only and consistent**: the handler loads `entity_chains` + all `entity_blocks`, recomputes hashes via `hashBlock`, treats JSON parse failures as `verified: false` (not 500), and drops the previous `verifyEntityChain`/`ipfs_bridge`-based report.
> 
> **Exports `hashBlock` and updates tests**: `hashBlock` is now exported from `entity_chain.ts`, and the integration test suite is updated to assert the new shape plus improved DB setup/teardown using `resetDatabase`/path restore.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit a9d7d0ebad9b7b29da2b48cf3e1a757b38e469ca. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->
## Summary by CodeRabbit

* **Refactor**
  * Simplified the entity v
