# PR #205: feat(test): remove hardcoded dev-token from browser tests

**Repo:** jleechanorg/worldai_claw
**Merged:** 2026-04-04
**Author:** jleechan2015
**Stats:** +2/-3 in 2 files

## Summary
Issue: wc-xzm (Remove hardcoded dev-token fallback in apiClient.ts and browser_test_base.py)

## Background
Issue: wc-xzm (Remove hardcoded dev-token fallback in apiClient.ts and browser_test_base.py)

## Raw Body
## Background
Issue: wc-xzm (Remove hardcoded dev-token fallback in apiClient.ts and browser_test_base.py)

## Description
Removed the `dev-token` fallback in `browser_test_base.py`, matching the already removed fallback in `apiClient.ts`. This prevents sending hardcoded dev-tokens by default which masks real authentication configurations.

## Testing
CI will run to verify UI tests adapt.

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Low risk: test-only change that stops injecting a bogus Authorization header when no token is configured, which may require CI/local runs to explicitly provide auth where previously masked.
> 
> **Overview**
> Stops browser E2E tests from defaulting to a hardcoded `dev-token` when `WORLDCLAW_TOKEN`/`TEST_WORLDCLAW_TOKEN` are unset, returning an empty token instead so the test harness omits the `Authorization` header by default.
> 
> Marks the tracking bead `wc-xzm` as closed in `.beads/issues.jsonl`.
> 
> <sup>Reviewed by [Cursor Bugbot](https://cursor.com/bugbot) for commit ccbf1f394acfdb50e2bb97cbf3c866928a1c6b1e. Bugbot is set up for automated code reviews on this repo. Configure [here](https://www.cursor.com/dashboard/bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->

## Summary by CodeRabbit

* **Chores**
  * Updated internal metadata and test infrastructure to refine record management and authentication token handling in the testing harness.

<!-- end of auto-generated comment: release notes by coderabbit.ai -->
