# PR #134: fix: correct PATCH endpoint for skeptic gate trigger comments

**Repo:** jleechanorg/worldai_claw
**Merged:** 2026-03-28
**Author:** jleechan2015
**Stats:** +1/-1 in 1 files

## Summary
- Fix HTTP 404 error when Skeptic Gate workflow tries to update existing trigger comments
- Root cause: `issues/{pr_num}/comments/{id}` is not a valid GET/PATCH endpoint; correct endpoint is `issues/comments/{id}`
- This caused all Skeptic Gate runs to fail immediately whenever a concurrent cancelled run had already posted a trigger comment

## Test Plan
- [ ] Skeptic Gate workflows should now successfully PATCH existing trigger comments instead of failing with 404
- [ ] Previously failing workflows on PRs #127-131 should now run correctly

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Low risk workflow-only change that fixes a 404 when updating an existing PR comment; behavior change is limited to the `gh api` PATCH URL.
> 
> **Overview**
> Fixes the Skeptic Gate workflow

## Raw Body
## Summary
- Fix HTTP 404 error when Skeptic Gate workflow tries to update existing trigger comments
- Root cause: `issues/{pr_num}/comments/{id}` is not a valid GET/PATCH endpoint; correct endpoint is `issues/comments/{id}`
- This caused all Skeptic Gate runs to fail immediately whenever a concurrent cancelled run had already posted a trigger comment

## Test plan
- [ ] Skeptic Gate workflows should now successfully PATCH existing trigger comments instead of failing with 404
- [ ] Previously failing workflows on PRs #127-131 should now run correctly

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Low risk workflow-only change that fixes a 404 when updating an existing PR comment; behavior change is limited to the `gh api` PATCH URL.
> 
> **Overview**
> Fixes the Skeptic Gate workflow’s trigger-comment update path by switching the `gh api` PATCH call to the correct comment endpoint (`issues/comments/{id}`) instead of the invalid per-PR URL.
> 
> This prevents Skeptic Gate runs from failing with 404 when a prior run already created the trigger comment and the workflow attempts to update it.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 7bf3847964b1dfac541fc84d0b1cfab8147aedf0. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
