# PR #196: [antig] fix: restrict arbitrary action_type in faction_simulator and fix lint

**Repo:** jleechanorg/worldai_claw
**Merged:** 2026-04-04
**Author:** jleechan2015
**Stats:** +52/-31 in 7 files

## Summary
(none)

## Raw Body
Background: We had several open issues (76, 79, 80) and bugs regarding UI styling, missing action_type enums, and lint errors.
Goals:
- Close issue 76 by preventing unrecognized LLM action types from causing side-effects.
- Address lint errors in app.ts and tests.
Tenets: Maintain strict validation, evidence fidelity.
Testing: Unit tests pass, eslint passes.

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Medium risk because it changes backend API response/request shapes for party/session endpoints and adds stricter validation that can break existing clients, plus it gates faction simulation side-effects based on LLM output.
> 
> **Overview**
> Prevents the faction simulator from applying stateful side-effects for unrecognised LLM `action_type` values by introducing an allowlist (`KNOWN_ACTION_TYPES`) and skipping unknown actions.
> 
> Tightens backend API contracts around parties/sessions: party creation now requires a non-empty trimmed `name`, party responses are reshaped to use `id`/`accountId` (vs `partyId`/`ownerId`), join-party now expects/returns `party_id`, and `/sessions` list returns `party_id`.
> 
> Hardens OpenClaw model selection by validating `openclaw_endpoint_ref` (and `WORLDCLAW_DEFAULT_MODEL`) as safe identifiers in both request schema and client resolution, and includes small test/lint fixes (event listener timeout scoping, longer MCP timeout).
> 
> <sup>Reviewed by [Cursor Bugbot](https://cursor.com/bugbot) for commit 71c2f02dac774183dc5259200aaefcbbf0a49a80. Bugbot is set up for automated code reviews on this repo. Configure [here](https://www.cursor.com/dashboard/bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->

## Summary by CodeRabbit

* **Bug Fixes**
  * Fixed party endpoint response schema: `partyId` renamed to `id`, `ownerId` to `accountId`; removed `sessionIds` from responses
  * Enhanced party name validation to require non-empty trimmed values
  * Restricted 
