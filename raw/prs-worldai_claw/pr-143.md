# PR #143: [agento] feat: wire game action buttons to real backend endpoints

**Repo:** jleechanorg/worldai_claw
**Merged:** 2026-03-29
**Author:** jleechan2015
**Stats:** +331/-31 in 4 files

## Summary
Game action buttons (Roll Dice, View World, View Characters) in GameScreen.tsx had placeholder `alert()` behavior or only toggled local state. They needed to be wired to the real backend endpoints.

## Background
Game action buttons (Roll Dice, View World, View Characters) in GameScreen.tsx had placeholder `alert()` behavior or only toggled local state. They needed to be wired to the real backend endpoints.

## Raw Body
## Background
Game action buttons (Roll Dice, View World, View Characters) in GameScreen.tsx had placeholder `alert()` behavior or only toggled local state. They needed to be wired to the real backend endpoints.

## Goals
- Roll Dice button → POST /api/sessions/:id/turn with `mechanic_request` field
- View World Info → GET /api/worlds/:id on show
- View Characters → GET /api/sessions/:id on show
- Loading indicators during API fetches

## Tenets
- TDD: write tests first, verify RED, then implement GREEN
- Minimal change: reuse existing API client (apiFetch)
- Backward compatible: buttons still toggle visibility when callbacks not provided

## Testing
- 23 unit tests pass (8 new TDD tests added in `packages/web/tests/gameScreen.test.tsx`)
- All existing tests pass; 2 pre-existing failures in app.test.tsx and 6 in routeIntegration.test.tsx (unrelated — those tests check for button text "View World" that was never in the component)

## Low-level details

### packages/web/src/screens/GameScreen.tsx
- Added `onViewWorld?: (show: boolean) => void`, `onViewCharacters?: (show: boolean) => void`, `isLoadingWorld?: boolean`, `isLoadingCharacters?: boolean`, `showWorldInfo?: boolean`, `showParty?: boolean` to `GameScreenProps`
- Toggle buttons now call callbacks when provided, fall back to internal state
- Added `data-testid` and loading `<p role="status">` to World Info and Party sections
- Added missing `<h2>World Info</h2>` heading to World Info section

### packages/web/src/App.tsx
- `GameScreenWrapper`: added state for loading flags and controlled visibility
- `handleViewWorld`: calls GET /api/worlds/:id when toggled open, shows loading indicator, updates `campaign.worldInfo`
- `handleViewCharacters`: calls GET /api/sessions/:id when toggled open, shows loading indicator, updates `campaign.party`
- `handleSendMessage`: adds `mechanic_request` field to POST /api/sessions/:id/turn when player_input starts with `/roll `

### packages/web/tests/gameScreen.test.tsx
- 8 new tes
