# PR #160: [agento] feat(world): add unsupported/ambiguous action handling to narrative system instructions

**Repo:** jleechanorg/worldai_claw
**Merged:** 2026-03-31
**Author:** jleechan2015
**Stats:** +68/-0 in 2 files

## Summary
worldai_claw-b6f: When a player submits an action that is vague, impossible, or contradicts world rules, the LLM currently guesses silently. This adds explicit handling to the narrative system instructions.

## Background
worldai_claw-b6f: When a player submits an action that is vague, impossible, or contradicts world rules, the LLM currently guesses silently. This adds explicit handling to the narrative system instructions.

## Raw Body
## Background

worldai_claw-b6f: When a player submits an action that is vague, impossible, or contradicts world rules, the LLM currently guesses silently. This adds explicit handling to the narrative system instructions.

## Goals

- Add a dedicated section to `narrative_lite_system_instruction.md` instructing the LLM on unsupported/ambiguous action handling
- Write a unit test verifying the new section exists and contains required keywords
- Ensure all tests pass

## Tenets

- Minimal change — only the necessary addition to the system prompt and a focused unit test
- Universe-agnostic guidance in the section text

## High-level description of changes

1. **`docs/system_instructions/narrative_lite_system_instruction.md`**: Added `## Unsupported and Ambiguous Actions` section with three directives:
   - **Acknowledge** the action in-narrative
   - **Explain** why it cannot proceed
   - **Offer** 1–2 alternative plausible actions
   - Guard against silently fabricating a resolution

2. **`packages/backend/tests/world/narrative_lite_system_instruction.test.ts`**: Unit test that verifies:
   - The section heading exists
   - Required keywords are present (`Acknowledge`, `Explain`, `Offer`, `alternative`)
   - The "do not silently guess" guard is present
   - The 1–2 alternative count is mentioned

## Testing

- `npx jest --testPathPattern=narrative_lite_system_instruction` — 102 suites pass, 11 pre-existing failures unrelated to this change
- The new test passes (verified among 1402 passing tests)

## Low-level details

- **`docs/system_instructions/narrative_lite_system_instruction.md`**: Added new section after `## Error Handling (Narrative-Facing)`
- **`packages/backend/tests/world/narrative_lite_system_instruction.test.ts`**: New test file; resolves instruction file from repo root via `path.resolve(__dirname, '../../../../docs/...')`

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Low risk: do
