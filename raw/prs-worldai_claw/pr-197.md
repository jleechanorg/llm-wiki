# PR #197: WIP: Verify Entity Endpoint

**Repo:** jleechanorg/worldai_claw
**Merged:** 2026-04-04
**Author:** jleechan2015
**Stats:** +5/-0 in 1 files

## Summary
(none)

## Raw Body
Draft for Verify Entity Endpoint

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Adds gating around faction action side effects based on a known-action allowlist; small change but it touches core simulation flow and currently references `knownActionTypes` (vs `KNOWN_ACTION_TYPES`), which could cause a runtime/build failure if not defined.
> 
> **Overview**
> Adds an additional allowlist check in `runSimulation` to **skip history/effects/diplomacy/world-event side effects** when an LLM-produced faction `action_type` is not recognised, logging a warning instead.
> 
> This change is intended to prevent unknown/creative action types from mutating world state, but the new guard uses `knownActionTypes` rather than the existing `KNOWN_ACTION_TYPES` constant.
> 
> <sup>Reviewed by [Cursor Bugbot](https://cursor.com/bugbot) for commit b202fd9773d5ba9bd933e9f4fd0f94969810249c. Bugbot is set up for automated code reviews on this repo. Configure [here](https://www.cursor.com/dashboard/bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
