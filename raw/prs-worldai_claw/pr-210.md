# PR #210: feat(web): add staggered entrance and ambient glow pulse animations

**Repo:** jleechanorg/worldai_claw
**Merged:** 2026-04-04
**Author:** jleechan2015
**Stats:** +31/-2 in 2 files

## Summary
Issues: wc-alo (Add staggered entrance animation for choice cards) and wc-btj (Add ambient glow pulse on choice cards and active elements)

## Background
Issues: wc-alo (Add staggered entrance animation for choice cards) and wc-btj (Add ambient glow pulse on choice cards and active elements)

## Raw Body
## Background
Issues: wc-alo (Add staggered entrance animation for choice cards) and wc-btj (Add ambient glow pulse on choice cards and active elements)

## Description
- Implemented `@keyframes slideUpFade` and applied an animated stagger delay on `.choice-card:nth-child(...)` to create a cascading reveal effect (offsetting the delay by 50ms per card).
- Created `@keyframes ambient-pulse` with oscillating box-shadow between 0.1 and 0.4 opacity of the primary accent color.
- Applied `ambient-pulse` to `.btn--primary`, `.choice-card`, `.tab-pill--active`, and `.bottom-tab--active` to give active elements a subtle breathing glow. Removed the pulse animation on hover for `.choice-card` so it doesn't conflict with its primary hover box shadow.

## Testing
CI tests will verify build passes.

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Pure CSS animation changes affecting only web UI presentation; main risk is minor UX/perf/regression for motion-sensitive users if not handled elsewhere.
> 
> **Overview**
> Adds `ambient-pulse` box-shadow animation and applies it to **primary buttons** plus active navigation elements (`.bottom-tab--active`, `.tab-pill--active`) to create a subtle breathing glow.
> 
> Introduces `slideUpFade` and wires **staggered entrance** animation delays for `.choice-card` items, while pausing the pulse animation on hover to avoid conflicting with the hover shadow.
> 
> Updates `.beads/issues.jsonl` to mark the related UI polish issues as closed.
> 
> <sup>Reviewed by [Cursor Bugbot](https://cursor.com/bugbot) for commit 1091d50fec4cd675611c435c62b0d43849467ed3. Bugbot is set up for automated code reviews on this repo. Configure [here](https://www.cursor.com/dashboard/bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
