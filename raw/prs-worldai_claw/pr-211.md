# PR #211: fix(connection): reduce local connection latency timeout

**Repo:** jleechanorg/worldai_claw
**Merged:** 2026-04-04
**Author:** jleechan2015
**Stats:** +84/-42 in 4 files

## Summary
Issue: wc-81u (OpenClaw local connection check latency is too slow)

## Background
Issue: wc-81u (OpenClaw local connection check latency is too slow)

## Raw Body
## Background
Issue: wc-81u (OpenClaw local connection check latency is too slow)

## Description
- Shortened the `probeTimeoutMs` for localhost URLs from 10000ms to 500ms in `app.ts` to fail fast when local gateways are unresponsive.
- Removed the `2000ms` visual dwell delay in `App.tsx` so success auto-navigates immediately.
- Added a performance test covering local connection expectation under 500ms.

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Tightens connection probe timing and abort behavior, which could increase false negatives for slow-but-valid local gateways and change setup navigation timing. Scope is limited to the OpenClaw test-connection route, its tests, and the web setup wizard redirect delay.
> 
> **Overview**
> **Speeds up OpenClaw local connection checks** by switching `/openclaw/test-connection` to a *single shared* `AbortController` budget (500ms for localhost, 5s remote) across both the `/v1/models` and `/v1/chat/completions` probes, replacing the prior per-call 10s local timeout.
> 
> Adds a regression/performance test that verifies slow local gateways are aborted within the expected window, and updates the web setup flow to remove the 2s success dwell (now a short 100ms defer) before auto-navigating to the dashboard.
> 
> <sup>Reviewed by [Cursor Bugbot](https://cursor.com/bugbot) for commit bd8ddffb383cdb1cad7b35f3da848111d0d541e1. Bugbot is set up for automated code reviews on this repo. Configure [here](https://www.cursor.com/dashboard/bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->
## Summary by CodeRabbit

* **Performance Improvements**
  * Local connection probe now aborts slow local gateways faster for snappier validation.
  * Setup wizard immediately transitions to the dashboard when online, removing the previous delay.

* **Tests**
  * Added an end-to-end test verifying the local probe aborts within the expected short window.

* **Chores**
  * Bulk up
