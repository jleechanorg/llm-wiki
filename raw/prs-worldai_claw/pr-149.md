# PR #149: [agento] fix: use PATCH for settings update test (worldai_claw-tes1)

**Repo:** jleechanorg/worldai_claw
**Merged:** 2026-03-30
**Author:** jleechan2015
**Stats:** +8/-2 in 1 files

## Summary
- Add `_patch_json` helper to `testing_mcp/test_worldai_claw_api.py` (same pattern as `_post_json` but uses `method='PATCH'`)
- Change `test_settings_update` to call `_patch_json` instead of `_post_json` for the `/settings` endpoint

## Background
The test `test_settings_update` was sending `POST /settings` but the backend route at `packages/backend/src/app.ts:1533` only implements `PATCH /settings`. This caused the test to fail with a 405 Method Not Allowed.

## Raw Body
## Summary

- Add `_patch_json` helper to `testing_mcp/test_worldai_claw_api.py` (same pattern as `_post_json` but uses `method='PATCH'`)
- Change `test_settings_update` to call `_patch_json` instead of `_post_json` for the `/settings` endpoint

## Background

The test `test_settings_update` was sending `POST /settings` but the backend route at `packages/backend/src/app.ts:1533` only implements `PATCH /settings`. This caused the test to fail with a 405 Method Not Allowed.

## Testing

- Code review: confirmed `app.patch('/settings', ...)` exists at line 1533 of `packages/backend/src/app.ts`
- Test execution: infra issue (`npm rebuild better-sqlite3` times out at 60s) prevents running the test in this environment — this is a pre-existing harness gap unrelated to this change

Closes #1

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Low risk test-only change that aligns the settings update integration test with the backend’s expected HTTP method; minimal chance of affecting runtime behavior.
> 
> **Overview**
> Fixes the `/settings` integration test to **update settings via `PATCH` instead of `POST`**, matching the backend route and avoiding 405 failures.
> 
> Adds a `_patch_json` helper and updates `test_settings_update` to fetch current settings, toggle `theme`, and assert the response reflects the new value.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 494184a8159c2fa6382b52b7db57c8a2e67a5e25. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->
## Summary by CodeRabbit

* **Tests**
  * Updated the settings-update test to first retrieve current settings, toggle the theme, and then apply the change.
  * Strengthened assertions to confirm the response reflects the toggled theme value.
  * Adjust
