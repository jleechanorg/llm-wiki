# PR #136: chore: route all CI test runs to self-hosted runners

**Repo:** jleechanorg/worldai_claw
**Merged:** 2026-03-29
**Author:** jleechan2015
**Stats:** +3/-3 in 3 files

## Summary
All test runs should use self-hosted runners. The previous conditional routed PR events to ubuntu-latest as a cost optimization, but the desired behavior is all events on self-hosted.

## Background
All test runs should use self-hosted runners. The previous conditional routed PR events to ubuntu-latest as a cost optimization, but the desired behavior is all events on self-hosted.

## Raw Body
## Background
All test runs should use self-hosted runners. The previous conditional routed PR events to ubuntu-latest as a cost optimization, but the desired behavior is all events on self-hosted.

## Changes
- Removed `github.event_name == 'pull_request' && 'ubuntu-latest'` conditional from `test.yml`
- All runs now use `fromJson(vars.SELF_HOSTED_RUNNER_LABELS || '["self-hosted","worldai-claw"]`)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> CI execution now depends entirely on the availability and correct labeling of self-hosted runners; misconfiguration or runner downtime could block PR checks and automation.
> 
> **Overview**
> Routes GitHub Actions jobs to self-hosted runners by switching `runs-on` to `fromJson(vars.SELF_HOSTED_RUNNER_LABELS || '["self-hosted","worldai-claw"]')`.
> 
> This removes the prior `pull_request`-specific fallback to `ubuntu-latest` in `test.yml` and also moves `coderabbit-ping-on-push` and `skeptic-gate` off GitHub-hosted runners, making all these workflows consistently run on the configured self-hosted pool.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit f0bed6ab70075ed655cc8bac9df043abd9b2ceee. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->
## Summary by CodeRabbit

* **Chores**
  * CI workflows updated to select runners from dynamic self-hosted labels (with a default fallback) instead of the fixed hosted image.
  * Applied to build, test, and gating workflows.
  * No functional or user-facing changes—no impact on features or behavior.
<!-- end of auto-generated comment: release notes by coderabbit.ai -->
