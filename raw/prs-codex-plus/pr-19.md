# PR #19: feat(commands): add codex history slash command

**Repo:** jleechanorg/codex_plus
**Merged:** 2025-10-06
**Author:** jleechan2015
**Stats:** +375/-0 in 1 files
**Labels:** codex

## Summary
- add a /history slash command definition for Codex session archives
- document robust permission-handling workflow for reading ~/.codex/sessions JSONL history
- mirror filtering, formatting, and fallback behaviors tailored to Codex session metadata

## Raw Body
## Summary
- add a /history slash command definition for Codex session archives
- document robust permission-handling workflow for reading ~/.codex/sessions JSONL history
- mirror filtering, formatting, and fallback behaviors tailored to Codex session metadata

## Testing
- not run (not requested)


------
https://chatgpt.com/codex/tasks/task_e_68e35a20a760832fbba28969d934ee1b

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Adds a comprehensive spec/doc for the `/history` command, detailing Python-based permission fallback, search/filtering, `/execute` integration, examples, outputs, and error handling.
> 
> - **Command Spec: `/history`** in `/.codexplus/commands/history.md`
>   - **Execution Workflow**: Immediate steps, multi-phase flow, and resilient permission handling with Python subprocess fallback.
>   - **Integration**: Delegates execution to `/execute` with strategy optimization and parallel processing.
>   - **Search Features**:
>     - Text strategies: exact, case-insensitive, tokenized, fuzzy (`--fuzzy`).
>     - Filters: time (`--date`, `--after`, `--before`, `--recent`), project/context (`--project`, `--cwd`, `--branch`), message types (`--type`, `--has-tools`, `--has-errors`).
>     - Output controls: `--limit`, `--context`, `--format`, `--files-only`.
>   - **Implementation Details**: Streaming JSONL parsing, pre-filtering pipeline, parallel file processing, smart discovery under `~/.codex/sessions`.
>   - **Python Fallback Commands**: Directory listing, file discovery, robust JSONL reading with error handling.
>   - **Output Formats**: Text (default), JSON, and table with sample structures.
>   - **Error Handling**: Missing directory, corrupted JSONL, permission issues (automatic Python fallback), large result sets, no matches.
>   - **Examples & Advanced**: Usage examples, composition with other commands, future semantic search and query intelligence.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 9eb373fe89d160b2
