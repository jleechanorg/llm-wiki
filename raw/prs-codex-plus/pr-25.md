# PR #25: fix: harden proxy resilience around upstream failures

**Repo:** jleechanorg/codex_plus
**Merged:** 2025-10-08
**Author:** jleechan2015
**Stats:** +386/-48 in 6 files

## Summary
(none)

## Raw Body
## Goal
- Harden the Codex proxy against upstream instability while debugging recurring 401 reports.

## Modifications
- Add retry scheduling for the initial upstream request and emit structured SSE errors when streaming fails mid-flight.
- Ensure status line hooks clean up subprocesses on timeout or broken pipes and add an explicit shutdown path for the background updater.
- Invoke the new shutdown hook from the app lifespan and cover the scenarios with regression tests.
- Document `gh` CLI availability in `AGENTS.md` for future maintenance.

## Necessity
- Upstream curl_cffi request failures were bubbling to clients and leaving background tasks hanging, making 401 incidents harder to diagnose and recover from.

## Integration Proof
- `pytest tests/test_proxy_resilience.py -q`

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Adds upstream retry/backoff and structured SSE errors, improves hook subprocess robustness and background status task shutdown, integrates shutdown in app lifespan, and adds regression tests.
> 
> - **Proxy Resilience (LLMExecutionMiddleware)**:
>   - Add upstream request retry/backoff and classify stream errors; emit structured SSE error events on failures.
>   - Improve streaming cleanup with explicit close handling.
> - **Hooks (status line)**:
>   - Treat `"hook failed"` like timeouts for fallback.
>   - Ensure subprocess termination on timeout/broken pipe; safer cancellation path.
> - **Status Line Middleware/Lifecycle**:
>   - Serialize start/stop with locks; add `stop_background_status_update()` and clean shutdown; wire stop into app lifespan.
> - **Tests**:
>   - Add `tests/test_proxy_resilience.py` covering retries, SSE error emission, broken pipe handling, and background task cancellation.
> - **Docs**:
>   - Note authenticated `gh` CLI availability in `AGENTS.md`.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit b84a0bdb20dcb50fa9b6a344ae605f0d073781f0. This will update automatically on new commits. C
