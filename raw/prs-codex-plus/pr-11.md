# PR #11: chore: add install helper script

**Repo:** jleechanorg/codex_plus
**Merged:** 2025-09-28
**Author:** jleechan2015
**Stats:** +207/-127 in 3 files
**Labels:** codex

## Summary
(none)

## Raw Body
## Goal
- provide a first-class installation helper that configures Codex-Plus shell integration while documenting the current prototype state.

## Modifications
- add an `install.sh` script that detects the active shell profile, appends the Codex-Plus helper snippet, and exposes aliases for managing `proxy.sh`.
- refresh `README.md` with prototype warnings, updated quick-start instructions, and detailed descriptions of the proxy architecture, repository layout, and guardrails from `AGENTS.md`/`CLAUDE.md`.

## Necessity
- simplifies onboarding by codifying the shell configuration previously done manually.
- keeps documentation synchronized with the latest middleware, hook, and logging capabilities so contributors understand the constraints before making changes.

## Integration Proof
- `SHELL=/bin/bash HOME=$(mktemp -d) ./install.sh` (verifies the installer appends the expected snippet).

------
https://chatgpt.com/codex/tasks/task_e_68d869840098832f934911ce8d448b39

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Introduces `install.sh` for automatic shell integration (with tests) and overhauls `README.md` with prototype notice, updated setup, and operational guidance.
> 
> - **Installer**:
>   - Adds `install.sh` that detects a shell RC file, appends a guarded snippet, and exports `CODEX_PLUS_REPO`/`CODEX_PLUS_PROXY_PORT`.
>   - Defines `codex_plus_proxy()` helper, a `codex()` wrapper that routes via `http://localhost:10000` when the proxy PID is active, and aliases `codexd`/`codex-plus-proxy`.
> - **Tests**:
>   - Adds `tests/test_install_script.py` to verify single-shot snippet insertion, expected aliases/exports, and correct RC selection.
> - **Docs**:
>   - Rewrites `README.md` with prototype warning, streamlined Quick Start using `./install.sh`, shell integration details, feature highlights, repo tour, development/testing steps, guardrails, troubleshooting, and licensing/contribution notes.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot
