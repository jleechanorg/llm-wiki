# PR #224: Export Claude Commands - 2026-01-15

**Repo:** jleechanorg/claude-commands
**Merged:** 2026-01-16
**Author:** jleechan2015
**Stats:** +11438/-1870 in 92 files

## Summary
(none)

## Raw Body
Automated export of command composition system. Includes commands, hooks, agents, skills, scripts, and orchestration. See README for quick start and adaptation guide.

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Strengthens automation, review handling, and test/evidence workflows while adding token-aware hooks and richer docs.
> 
> - **Hooks & settings**: New `PreToolUse.sh` (adds `Read` offsets, `git diff --minimal --unified=0`) and `UserPromptSubmit.sh` (session-size warnings), plus `metrics_summary.sh`; wired via `.claude/settings.json`.
> - **PR comment handling**: `commentreply.py` adds pending-review cleanup, secure threaded replies with 422 fallback to issue comments, idempotency, skip placeholders, coverage validation, and improved logging; supports mixed `author`/`user.login` formats.
> - **Git/evidence tooling**: `savetmp.py` adds `--pr-mode`, better base-ref/diff logic, logging integration, safer checksum cleanup; `generatetest.md` mandates shared `testing_mcp/lib/*` utilities and iteration-based evidence bundles; evidence standards updated.
> - **Commands & docs**: New `/automation-publish`, overhauled `/pair` (dual-agent with background monitor, MCP Mail/Beads), `commentfetch` repo override, safer `localexportcommands` sync (no `--delete`), `superpowers-*` stubs.
> - **CI/tests**: New tests for compose-commands (CI-safe), PR formatter/commentreply (including `dismiss_pending_reviews`), and `savetmp`.
> - **Infra docs**: GCP deployment guide adds PR preview discovery/health checks; minor sync/worktree and git-header parsing fixes.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit ce33480e471c7e8d1dd2214f33bfc981b348037a. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->
## Summary by CodeRabbit

* **New Features**
  * Session health monitoring and token-optim
