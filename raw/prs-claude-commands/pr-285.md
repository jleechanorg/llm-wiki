# PR #285: Claude Commands Export 2026-03-24

**Repo:** jleechanorg/claude-commands
**Merged:** 2026-03-24
**Author:** jleechan2015
**Stats:** +10778/-3558 in 146 files

## Summary
(none)

## Raw Body
Automated export. Source files overwrite target; target-only files preserved.

Changed:  146 files changed, 10778 insertions(+), 3558 deletions(-)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Medium risk because it replaces the export implementation with a new `exportcommands.sh` that performs git/rsync/filtering and could accidentally publish or mutate files if misconfigured. Most other changes are additive command definitions and portability tweaks.
> 
> **Overview**
> Introduces several new Claude slash commands for autonomous workflows and diagnostics, including `/converge`, `/gene` (and aliases), PR-comment convergence commands (`/copilotc`, `/fixprc`), AO autonomy diagnostics (`/auton`), and reporting (`/agento_report`).
> 
> Replaces the previous Python-based exporter by deleting `exportcommands.py` and adding a new `exportcommands.sh` that union-merges `~/.claude` + repo `.claude`, syncs selected root dirs, applies portable content filters, removes sensitive settings files, and optionally updates `README.md` via `claude` before pushing/creating a PR.
> 
> Improves portability by switching hardcoded repo/path defaults to environment-driven values (e.g., `$GITHUB_REPOSITORY`), updating `/claw` to run via `bash --login` for env availability, and adjusting benchmark scripts and examples to avoid project-specific hardcoding.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit da689e60f9e6e787142caa6546a51bd84f658887. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
