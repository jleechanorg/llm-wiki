# PR #299: Claude Commands Export 2026-04-04

**Repo:** jleechanorg/claude-commands
**Merged:** 2026-04-05
**Author:** jleechan2015
**Stats:** +44054/-5485 in 305 files

## Summary
(none)

## Raw Body
Automated export. Source files overwrite target; target-only files preserved.

Changed:  305 files changed, 44058 insertions(+), 5483 deletions(-)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Updates several execution-critical slash commands (notably `/claw` routing and gateway token handling, plus `/auton` diagnostics), so misconfiguration could break task dispatch or PR automation even though most changes are instructional/docs.
> 
> **Overview**
> Large refresh of the `.claude/commands` system to push more autonomy into day-to-day workflows. Adds new operational commands (`/eloop` evolve loop, `/evidence-check`) and extends existing ones to measure PR automation health (stalled PR detection, merge-quality/zero-touch audits, zombie worker detection) and to write durable insights back into file-based Claude memory from `/checkpoint` and `/debug`.
> 
> Significantly revises `/claw` dispatch: simplifies/duplicates slash-command resolution in both AO and gateway paths, changes the learning-loop gate heuristics, adds delivery tracking metadata, and reworks gateway auth handling to read the token up front and pass it via a temp file to the async `curl` process (plus simpler gateway health check + SSE parsing/error reporting). Also includes several smaller command tweaks (e.g., `/coderabbit` execution steps, `CopilotCommandBase` default repo fallback to `$GITHUB_REPOSITORY`, path hardcoding in `/benchg-ts`, and trimming `/exportcommands` docs to point at the canonical script).
> 
> <sup>Reviewed by [Cursor Bugbot](https://cursor.com/bugbot) for commit ec99574aac701c022c0e4aecbf7872770b319aed. Bugbot is set up for automated code reviews on this repo. Configure [here](https://www.cursor.com/dashboard/bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->
## Summary by CodeRabbit

* **New Commands**
  - /eloop (evolve_loop), /evidence-check, /pr-media, /nextsteps, /up, /r (immediate execution)

* **
