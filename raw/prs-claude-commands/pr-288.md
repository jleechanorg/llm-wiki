# PR #288: fix: GitHub Actions GCP vars use ${{ vars.GCP_PROJECT_ID }} syntax

**Repo:** jleechanorg/claude-commands
**Merged:** 2026-03-26
**Author:** jleechan2015
**Stats:** +12/-12 in 8 files

## Summary
- Fixes 4 Critical CodeRabbit findings: `$GCP_PROJECT_ID` used as literal string in YAML `env:` and `with:` blocks instead of proper `${{ vars.GCP_PROJECT_ID }}` syntax
- 8 files, 12 changes total
- Shell `run:` blocks (gcloud commands) are unaffected — shell variables are correct there

## Raw Body
## Summary
- Fixes 4 Critical CodeRabbit findings: `$GCP_PROJECT_ID` used as literal string in YAML `env:` and `with:` blocks instead of proper `${{ vars.GCP_PROJECT_ID }}` syntax
- 8 files, 12 changes total
- Shell `run:` blocks (gcloud commands) are unaffected — shell variables are correct there

## Files changed
| File | Fix |
|------|-----|
| `workflows/mcp-smoke-tests.yml` | env GCP_PROJECT |
| `workflows/auto-deploy-dev.yml` | env GCP_PROJECT |
| `workflows/pr-cleanup.yml` | env GCP_PROJECT (2 jobs) |
| `workflows/deploy-dev.yml` | env GCP_PROJECT |
| `workflows/pr-preview.yml` | env GCP_PROJECT |
| `workflows/mcp-manual-smoke-tests.yml` | env GCP_PROJECT |
| `workflows/test-email-notification.yml` | with project (2×) |
| `workflows/deploy-production.yml` | with project (2×) |

Closes #287 (follow-up fix)

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Touches multiple GitHub Actions workflows used for deploy/smoke/cleanup and production email notifications; while the change is a small syntax fix, mistakes here could break CI/CD execution or reporting.
> 
> **Overview**
> Updates GitHub Actions workflows to stop treating `GCP_PROJECT_ID` as a literal shell string and instead resolve it via GitHub Actions variables (`${{ vars.GCP_PROJECT_ID }}`).
> 
> This fixes `GCP_PROJECT` values in dev deploy/preview/smoke/cleanup workflows and ensures the `project` passed to `send-deploy-notification` in production/test workflows uses the resolved project ID.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit ec79ebee79060ddb2c3bbc4da79662a903889df8. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
