# PR #234: Add tsx fallback for CLI startup scripts

**Repo:** jleechanorg/ai_universe_frontend
**Merged:** 2025-11-13
**Author:** jleechan2015
**Stats:** +75/-11 in 4 files
**Labels:** codex

## Summary
- update the Claude CLI wrapper and MCP stdio wrapper so they no longer depend on pre-built `dist/` files and instead fall back to running the TypeScript entrypoints with `tsx` when needed
- add the `tsx` dev dependency so the repository can execute backend scripts without committing build artifacts

## Raw Body
## Summary
- update the Claude CLI wrapper and MCP stdio wrapper so they no longer depend on pre-built `dist/` files and instead fall back to running the TypeScript entrypoints with `tsx` when needed
- add the `tsx` dev dependency so the repository can execute backend scripts without committing build artifacts

## Testing
- npm run lint
- npm run type-check

------
[Codex Task](https://chatgpt.com/codex/tasks/task_e_69158c75a9a8832f9148825cb769a0e9)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> CLI and MCP stdio startup scripts now fall back to running TypeScript via tsx when compiled outputs are missing; adds tsx as a devDependency.
> 
> - **Scripts**:
>   - `scripts/claude-cli-wrapper.sh`:
>     - Adds fallback to run `src/server.ts` via `tsx` (prefers local binary, then global, then `npx`).
>     - Validates backend paths and improves error handling; preserves env setup.
>   - `scripts/mcp_stdio_wrapper.js`:
>     - Runs compiled `dist/stdio-server.js` if present, else falls back to `src/stdio-server.ts` via `tsx` (local or `npx`).
>     - Refactors process spawning into `runProcess`, adds existence checks and signal forwarding.
> - **Dev Tooling**:
>   - Adds `tsx` to `devDependencies` in `package.json` (lockfile updated).
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit b7aaacb96043b6cc672106302b893dfd4554a24c. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
