# PR #185: Fix tests and skip tests for all environments

**Repo:** jleechanorg/ai_universe_frontend
**Merged:** 2025-11-11
**Author:** jleechan2015
**Stats:** +936/-371 in 13 files

## Summary
- Merged main into PR branch and resolved merge conflicts
- Fixed JSON schema base URI issue in test environment
- Updated deploy.sh to skip tests for all environments (dev, staging, prod)

## Raw Body
## Summary

- Merged main into PR branch and resolved merge conflicts
- Fixed JSON schema base URI issue in test environment
- Updated deploy.sh to skip tests for all environments (dev, staging, prod)

## Changes

- `src/services/canonicalValidators.ts` - Resolved merge conflict
- `tests/setup.ts` - Fixed JSON schema base URI for Node.js test environment
- `deploy.sh` - Skip tests for all environments to speed up deployment

## Testing

✅ All tests pass locally (39 files, 215 tests)
✅ Type checking passes
✅ Linting passes

## Impact

- Dev auto-deploy will work (deploy.sh dev skips tests)
- Staging deployments will be faster (no test timeouts)
- Tests still run in CI workflow separately

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Gates auto-deploy on passing CI, updates deploy.sh to use --suppress-logs and skip tests for all envs, adds deployment and URL-navigation tests, and documents Cypress setup/secrets.
> 
> - **CI/CD**:
>   - Gate auto-deploy with a `wait-for-ci` job running `type-check`, `lint`, and `test:ci`; `deploy` now `needs` it in `.github/workflows/auto-deploy.yml`.
>   - Deployment job authenticates to GCP, enables required APIs, runs `deploy.sh`, fetches URL, and performs health checks.
> - **deploy.sh**:
>   - Use `gcloud builds submit --suppress-logs` (replaces `--no-stream-logs`) in CI.
>   - Skip tests for all environments; update help text and messaging.
> - **Tests**:
>   - Add `tests/deployment/deploy.test.ts` validating bash syntax, flags (`--suppress-logs`), substitutions, and structure.
>   - Add Playwright E2E `tests/e2e/conversation-url-navigation.spec.ts` for direct `?conversationId=` navigation and invalid IDs.
>   - Add unit tests `tests/useConversations.url-navigation.test.tsx` for preserving/clearing URL conversationId on auth/init vs user switch.
>   - Test setup updates: fix `@cfworker/json-schema` base URI and enhance MSW/fetch logging in `tests/setup.ts`; minor `src/test-setup.ts` cleanup.
> - **Docs**:
>   - New `docs/CYPRESS_EXEC
