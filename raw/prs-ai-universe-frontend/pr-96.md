# PR #96: fix: sync render.yaml with existing service for Blueprint integration

**Repo:** jleechanorg/ai_universe_frontend
**Merged:** 2025-10-30
**Author:** jleechan2015
**Stats:** +158/-1 in 2 files

## Summary
Fixes the Render build error by aligning `render.yaml` Blueprint configuration with the existing Render dashboard service name.

## Raw Body
## Summary
Fixes the Render build error by aligning `render.yaml` Blueprint configuration with the existing Render dashboard service name.

## Problem
- Render service name: `ai-universe-frontend` (srv-d3d3pj24d50c73d1qv7g)
- Blueprint service name: `ai-universe-frontend-server` ❌
- **Result**: Service ignores Blueprint, uses broken dashboard config
- **Error**: `sh: 1: vite: not found` (missing `--include=dev` flag)

## Solution
- Update `render.yaml` service name: `ai-universe-frontend-server` → `ai-universe-frontend` ✅
- Names now match, enabling Blueprint sync
- Blueprint includes critical fix: `npm install --include=dev && npm run build:prod`

## Changes
- **render.yaml**: Update service name to match existing dashboard service
- **RENDER_BLUEPRINT_SYNC.md**: Complete guide for applying Blueprint to existing service

## Testing
```bash
npm run test:render-config  # ✅ Passes
```

## Next Steps After Merge
1. Go to Render Dashboard → New → Blueprint
2. Select this repo (main branch after merge)
3. Choose "Update existing service: ai-universe-frontend"
4. Blueprint will sync correct build command with `--include=dev`

## Related
- Reproduces Render build error: `./reproduce-render-error.sh`
- Automated test: `tests/deployment/renderConfig.test.ts`
- Alternative fix: PR #92 (moves Vite to dependencies - workaround)

🤖 Generated with [Claude Code](https://claude.com/claude-code)

Co-Authored-By: Claude <noreply@anthropic.com>

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Aligns Render Blueprint service name with the existing service and adds a concise sync guide to apply the configuration.
> 
> - **Config**:
>   - Update `render.yaml` service name from `ai-universe-frontend-server` to `ai-universe-frontend` to match the existing Render service for Blueprint linking.
> - **Docs**:
>   - Add `RENDER_BLUEPRINT_SYNC.md` with steps to apply the Blueprint via dashboard/CLI, verification procedures, and rollback guidance.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com
