# PR #150: Fix second multi-model message disappearing bug

**Repo:** jleechanorg/ai_universe_frontend
**Merged:** 2025-11-10
**Author:** jleechan2015
**Stats:** +34/-2 in 2 files
**Labels:** codex

## Summary
- merge pending optimistic messages directly in the messages query result while active to avoid refetch overwrites
- skip backend fetches for temp conversation IDs by returning cached optimistic history
- extend regression tests to ensure no temp conversation fetches are issued during new conversation sends

## Raw Body
## Summary
- merge pending optimistic messages directly in the messages query result while active to avoid refetch overwrites
- skip backend fetches for temp conversation IDs by returning cached optimistic history
- extend regression tests to ensure no temp conversation fetches are issued during new conversation sends

## Testing
- npm test -- tests/useConversations.test.tsx

------
[Codex Task](https://chatgpt.com/codex/tasks/task_e_6911b7b37244832f9178174d459c1dfe)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Merges pending optimistic messages directly in the messages query and avoids backend fetches for temp conversation IDs, with tests ensuring no temp-ID requests or payloads.
> 
> - **Hooks (`src/hooks/useConversations.ts`)**:
>   - Merge pending optimistic messages in `messages` query via `applyPendingOptimisticMessages` to prevent refetch overwrites.
>   - Handle no active conversation by returning empty history (`createEmptyHistoryResponse`).
>   - Detect `temp-conversation-*` IDs and use cached history instead of calling `conversationApi.getMessages`.
> - **Tests (`tests/useConversations.test.tsx`)**:
>   - Add regression assertions ensuring `getMessages` is never called with temp conversation IDs.
>   - Verify MCP calls (`agent.second_opinion`, `conversation.send-message`) omit temp `conversationId` for new conversations (multi-model and single-model).
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit d9b8fc9ddd3881f9073f98e6aa3162ee68693b01. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
