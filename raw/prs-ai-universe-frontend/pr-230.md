# PR #230: ci: reuse deploy-dev workflow

**Repo:** jleechanorg/ai_universe_frontend
**Merged:** 2025-11-13
**Author:** jleechan2015
**Stats:** +215/-172 in 2 files

## Summary
- extract the dev deployment logic into a reusable workflow so auto-deploy and manual runs share the exact same steps
- keep the full Cloud Run deploy, health check, and failure issue path in deploy-dev.yml, then call it from the push-triggered workflow

## Raw Body
## Summary
- extract the dev deployment logic into a reusable workflow so auto-deploy and manual runs share the exact same steps
- keep the full Cloud Run deploy, health check, and failure issue path in deploy-dev.yml, then call it from the push-triggered workflow

## Testing
- not run (workflow-only change)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Creates a reusable dev deployment workflow and updates auto-deploy to call it with inputs and inherited secrets.
> 
> - **CI/CD Workflows**:
>   - **New reusable `deploy-dev.yml`**:
>     - Supports `workflow_dispatch` and `workflow_call` with `reason` and `ref_name` inputs.
>     - Sets GCP env, authenticates, builds, deploys via `deploy.sh`, fetches Cloud Run URL, performs health checks.
>     - Adds deployment summary and failure issue creation with commit metadata.
>   - **Simplified `auto-deploy.yml`**:
>     - Replaces inline steps with `uses: ./.github/workflows/deploy-dev.yml`.
>     - Passes `reason` and `ref_name`; `secrets: inherit`; retains needed permissions.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 3e0ee52153e46ab437a00144dab08131e1b60f0b. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
