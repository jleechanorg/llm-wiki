# PR #174: Follow-up: Fix review feedback for conversation optimistic cache

**Repo:** jleechanorg/ai_universe_frontend
**Merged:** 2025-11-11
**Author:** jleechan2015
**Stats:** +40/-12 in 7 files
**Labels:** codex

## Summary
- preserve optimistic cache snapshots without duplicating resolved temp messages by cross-checking pending optimistic IDs
- regenerate canonical schemas and update mock/sample data so both `tokens` and `tokensUsed` remain available during the transition
- clarify the MCP token field comment and extend mocks/tests to emit `tokensUsed` alongside `tokens`

## Raw Body
## Summary
- preserve optimistic cache snapshots without duplicating resolved temp messages by cross-checking pending optimistic IDs
- regenerate canonical schemas and update mock/sample data so both `tokens` and `tokensUsed` remain available during the transition
- clarify the MCP token field comment and extend mocks/tests to emit `tokensUsed` alongside `tokens`

## Testing
- `npm test -- tests/useConversations.test.tsx`


------
[Codex Task](https://chatgpt.com/codex/tasks/task_e_69127a017d54832fbbd44308ac83b101)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Prevents duplicated optimistic temp messages when merging cache with server data and updates schemas, mocks, and tests to use tokensUsed (while emitting tokens for compatibility).
> 
> - **Conversations (useConversations)**:
>   - Refines cache merge: preserves only temp messages that are still pending by cross-checking `pendingOptimisticMessagesRef` and filtering snapshot messages, preventing duplicates.
> - **Schemas and Data**:
>   - Canonical schemas: rename `tokens` → `tokensUsed` in `modelOpinion` and `synthesis` across definitions.
>   - Mock/sample data and mocks (`canonicalSamples`, `mockMcpServer`, test fixtures/tests): emit `tokensUsed` alongside existing `tokens` for transition.
> - **Types**:
>   - `ModelOpinion` comment clarified for `tokens`/`tokensUsed` consistency.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 5e4af8facd134ef243b0b91883412db617b09a7c. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
