# PR #310: Fix scrolling issues on mobile UI

**Repo:** jleechanorg/ai_universe_frontend
**Merged:** 2025-11-24
**Author:** jleechan2015
**Stats:** +175/-20 in 4 files

## Summary
(none)

## Raw Body
- Add touch-action CSS utilities for better scroll handling on mobile
- Change SecondOpinionCards clickable area from entire card to header only
- Convert clickable divs to semantic buttons with proper ARIA attributes
- Add touch-passthrough class to content areas that should allow scrolling
- Main scroll container now uses touch-scroll class for pan-y priority

This fixes the issue where trying to scroll on mobile would accidentally focus on UI elements instead of scrolling the main content area.

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Improves mobile scrolling and touch interactions, converts opinion cards to accessible header buttons, and closes the conversation sidebar after selection on mobile.
> 
> - **UI/Components**:
>   - `src/components/ChatInterface.tsx`:
>     - Auto-close sidebar on mobile when selecting the active or a new conversation (`handleSelectConversation`).
>     - Main messages container adds `touch-scroll` for smoother mobile scrolling.
>   - `src/components/SecondOpinionCards.tsx`:
>     - Make only the header clickable by converting headers to `<button>` elements with `aria-expanded`/`aria-label` and `disabled` where applicable.
>     - Add `touch-passthrough` to non-clickable content and previews; use `touch-target` on interactive controls; stop event propagation on copy buttons.
> - **Styles** (`src/index.css`):
>   - Add touch utilities: `touch-scroll`, `touch-passthrough`, `touch-target`.
>   - Reset styles for `button.card-header` and adjust card header layout.
> - **Tests**:
>   - `tests/components/ChatInterface.sidebarMobile.test.tsx`: verifies sidebar closes after selecting a conversation on mobile.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit a3eef31fe3b9fb3953b8411d8b02fe4ca67ea642. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coder
