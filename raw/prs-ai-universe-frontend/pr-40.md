# PR #40: Refine second opinion integration coverage

**Repo:** jleechanorg/ai_universe_frontend
**Merged:** 2025-09-28
**Author:** jleechan2015
**Stats:** +139/-9 in 4 files
**Labels:** codex

## Summary
- reuse a shared MCP base URL constant across the second opinion integration tests
- verify both empty and whitespace-only questions are rejected without suppressing type errors
- reuse the derived endpoint constant when asserting backend validation failures

## Raw Body
## Summary
- reuse a shared MCP base URL constant across the second opinion integration tests
- verify both empty and whitespace-only questions are rejected without suppressing type errors
- reuse the derived endpoint constant when asserting backend validation failures

## Testing
- npm run test:integration

------
https://chatgpt.com/codex/tasks/task_e_68d87b62d1b0832fac1cdd653cb94eda

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Adds integration tests and updates mocks to validate empty/missing questions, and updates unit tests to handle 413 and dynamic SSE request IDs.
> 
> - **Tests**:
>   - **Integration**: New `testing_integration/secondOpinion.integration.test.ts` verifying client rejects empty/whitespace questions and backend returns JSON-RPC error when arguments are omitted.
>   - **Unit**: Update `src/services/__tests__/mcpClient.test.ts` to simulate HTTP 413 for long questions and assert POST call; make SSE test use the next dynamic `requestId`.
> - **Mocks**:
>   - **Request typing**: Introduce `McpRequestBody` and remove `any` casts in `src/test/mocks/mcpHandlers.ts` and `tests/mocks/mcpServer.ts`.
>   - **Validation**: Add server-side checks for missing/empty `question` returning HTTP 400 with JSON-RPC `-32602` error.
>   - Keep existing tool listing and streaming handlers while aligning responses with backend behavior.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit fc4fbe0ebf1ed3318633d044112265cb4e928818. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->
## Summary by CodeRabbit

- New Features
  - Clearer error messaging for the Second Opinion tool when the question is missing or empty.
- Bug Fixes
  - Requests lacking a valid question now return a 400 with a descriptive error; oversized questions surface appropriate HTTP errors.
- Tests
  - Added and 
