# PR #144: Set up pre-commit hooks for Git

**Repo:** jleechanorg/ai_universe_frontend
**Merged:** 2025-11-11
**Author:** jleechan2015
**Stats:** +6158/-8704 in 36 files

## Summary
(none)

## Raw Body
This commit adds comprehensive pre-commit validation to catch issues before they're committed, addressing the gap discovered when the "unused variable" error wasn't caught locally.

Changes:
1. Installed Husky (v9.1.7) and lint-staged (v16.2.6) for git hook management
2. Created .husky/pre-commit hook that runs:
   - npm run type-check (TypeScript validation)
   - npm run lint (ESLint code quality)
   - npm run build (Production build verification with ROLLUP_SKIP_NODE_NATIVE workaround)
3. Added lint-staged configuration for targeted file checks (ESLint + Prettier)
4. Fixed TypeScript configuration issues:
   - Removed bundled types from tsconfig.json types array (vitest/globals, @testing-library/jest-dom)
   - Added explicit beforeEach import in src/test-setup.ts
5. Fixed rollup native/WASM fallback by running ensure-rollup-native.mjs
6. Fixed 3 synthesis test failures by correcting field names (tokens → tokensUsed):
   - tests/synthesis/layer2-normalization.test.ts
   - tests/synthesis/layer3-adapter.test.ts
   - tests/useConversations.test.tsx (createSecondOpinionResponse helper)
7. Updated CLAUDE.md with pre-commit hook documentation

Why This Matters:
- Prevents broken code from being committed
- Catches type errors, lint issues, and build errors locally
- Provides faster feedback than waiting for CI
- Follows CLAUDE.md Pre-Commit Workflow protocol automatically
- Fixed 3 pre-existing test failures (6 failures → 3 failures)

Technical Details:
- Husky v9.1.7 with v10-compatible format (no husky.sh sourcing)
- Lint-staged v16.2.6 for efficient staged file checks
- Pre-commit hook runs type-check, lint, and build sequentially
- Tests skipped in pre-commit (3 pre-existing failures unrelated to this feature)
- Commit blocked if type-check, lint, or build fails

Impact:
- This would have caught the unused isSendingMessage variable before commit
- All core validation (TypeScript, ESLint, build) must pass to commit
- Test failures 
