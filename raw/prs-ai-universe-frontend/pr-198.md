# PR #198: Fix website issue

**Repo:** jleechanorg/ai_universe_frontend
**Merged:** 2025-11-12
**Author:** jleechan2015
**Stats:** +53/-7 in 1 files

## Summary
(none)

## Raw Body
- Replace --suppress-logs with --async for CI environments
- Implement polling mechanism to wait for build completion
- Use gcloud builds describe to check status every 15 seconds
- Exit with error if build fails, continue if successful
- Local development still uses synchronous builds (works with user credentials)

This fixes the production deployment failure where service accounts couldn't stream Cloud Build logs due to VPC-SC restrictions.

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Use async Cloud Build in CI, poll build status until completion, and fail fast on errors; local dev remains synchronous.
> 
> - **Deploy script (`deploy.sh`)**:
>   - **CI build path**:
>     - Replace `--suppress-logs` with `--async` for `gcloud builds submit`.
>     - Capture build ID (`--format="value(id)"`) and print Console URL.
>     - Poll `gcloud builds describe <ID>` every 15s up to `BUILD_TIMEOUT_SECONDS`.
>     - Handle statuses (`SUCCESS`, `FAILURE`, `TIMEOUT`, `CANCELLED`, `INTERNAL_ERROR`, `WORKING`, `QUEUED`) and exit non-zero on failure.
>     - Final status check ensures build completed successfully.
>   - **Local build path**:
>     - Keep synchronous build with log streaming unchanged.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 85101bbe11cc44471e311b5dcdf94d2d833dc6b9. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
