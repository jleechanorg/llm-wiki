# PR #197: Fix incomplete rate limit error message

**Repo:** jleechanorg/ai_universe_frontend
**Merged:** 2025-11-12
**Author:** jleechan2015
**Stats:** +397/-12 in 8 files

## Summary
(none)

## Raw Body
<!-- CURSOR_SUMMARY -->
> [!NOTE]
> Adds detailed rate‑limit errors (with contact email and reset info) across MCP client/shared calls and prevents duplicate toasts in chat UIs; includes comprehensive tests.
> 
> - **Services**:
>   - **`src/services/mcpClient.ts`**: Convert client `RateLimitError (429)` to app `RateLimitError` with `contactEmail ("jleechan@gmail.com")`, limit defaults, and computed `resetTime` for `getSecondOpinion`, `startConversation`, and `addMessageToConversation`.
>   - **`src/services/mcpShared.ts`**: Detect rate-limit responses and throw detailed `RateLimitError` immediately (no retries); add builder for defaulted rate‑limit payload.
> - **UI**:
>   - **`src/components/ChatInterface.tsx` & `ChatInterfaceLegacy.tsx`**: In catch blocks, skip generic toasts when `err instanceof RateLimitError` to avoid duplicates; preserve existing success/error flows.
>   - **`src/hooks/useConversations.ts`**: On mutation error, display detailed rate‑limit toast via `displayRateLimitError`; exclude `RateLimitError` from surfaced `error` state.
> - **Tests**:
>   - **New** `tests/mcpClient.rateLimit.test.ts`: Verifies contact email, reset-time calculation, and handling across conversation methods and UI formatting.
>   - **Updated** `src/services/__tests__/mcpClient.test.ts`: Mock exports `RateLimitError`.
> - **Docs**:
>   - `docs/pr-description-rate-limit-fix.md`: PR overview and instructions.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 135fcd9be52a763c9fc18d9e0c219f5fee639ae0. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->
## Summary by CodeRabbit

* **Bug Fixes**
  * Improved rate-limit handling to avoid duplicate notifications, preserve user messages on failed send attempts, and surface clearer reset timing and support contact info in UI messages.


