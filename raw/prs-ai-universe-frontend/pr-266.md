# PR #266: Suppress redundant conversation error toasts when MCP offline

**Repo:** jleechanorg/ai_universe_frontend
**Merged:** 2025-11-17
**Author:** jleechan2015
**Stats:** +35/-7 in 2 files
**Labels:** codex

## Summary
- treat conversation errors as secondary signals and suppress them whenever the MCP transport is already reporting an outage so we only surface the underlying connection failure once
- keep the existing toast dedupe helper but make placeholders/error badges derive from the filtered conversation error state so users no longer see duplicate "Load failed" messaging

## Raw Body
## Summary
- treat conversation errors as secondary signals and suppress them whenever the MCP transport is already reporting an outage so we only surface the underlying connection failure once
- keep the existing toast dedupe helper but make placeholders/error badges derive from the filtered conversation error state so users no longer see duplicate "Load failed" messaging

## Testing
- npm run lint
- npm run type-check

------
[Codex Task](https://chatgpt.com/codex/tasks/task_e_6918eea2df60832fa6d1216e87fdf6b7)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Deduplicates and gates error toasts (respecting MCP outage vs. rate limits), bases UI state on filtered `effectiveConversationError`, and displays the actual error text in connection status.
> 
> - **Chat UX**:
>   - Add toast deduping via `lastToastMessageRef` and `showErrorToast()` in `src/components/ChatInterface.tsx`.
>   - Treat non-rate-limit MCP errors as hard outages; derive `effectiveConversationError` to suppress redundant conversation-error toasts.
>   - Update effects to use `showErrorToast` for MCP and conversation errors; reset dedupe state when errors clear.
>   - Input placeholder/error state now keyed off `effectiveConversationError`.
> - **Status Display**:
>   - `src/components/ConnectionStatus.tsx`: show actual `error` string instead of a generic label.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 77671f6632f84a5ae0b3e757ada26852a496a76d. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
