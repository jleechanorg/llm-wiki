# PR #265: Fix auth redirect heuristics and mocks

**Repo:** jleechanorg/ai_universe_frontend
**Merged:** 2025-11-16
**Author:** jleechan2015
**Stats:** +52/-63 in 4 files
**Labels:** codex

## Summary
- add touch-capable fallback to the redirect heuristic and harden the vitest suite with proper global cleanup
- guard redirect result handling in `AuthProvider` and relax MSW handlers so requests without `userId` follow the new payload contract
- keep the auth strategy tests deterministic by restoring navigator/window state after each case

## Raw Body
## Summary
- add touch-capable fallback to the redirect heuristic and harden the vitest suite with proper global cleanup
- guard redirect result handling in `AuthProvider` and relax MSW handlers so requests without `userId` follow the new payload contract
- keep the auth strategy tests deterministic by restoring navigator/window state after each case

## Testing
- `npm run test -- tests/utils/authStrategy.test.ts`
- `npm run lint`
- `npm run type-check`


------
[Codex Task](https://chatgpt.com/codex/tasks/task_e_6918f8486bcc832f9f33145d25f7e6e6)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Broaden redirect-auth detection to touch-capable devices, relax mock MCP handlers to default missing userId, and guard redirect result error handling in AuthProvider with improved tests.
> 
> - **Auth Strategy (`src/utils/authStrategy.ts`)**:
>   - Expand `shouldUseRedirectAuth` to use `navigator.maxTouchPoints` (touch-capable) and simplify logic to `isMobileUA || hasTouchScreen`.
> - **Auth Provider (`src/contexts/AuthProvider.tsx`)**:
>   - Guard `setAuthError` in redirect result catch to only run when component is mounted.
> - **Mocks/MSW (`src/test/mocks/mcpHandlers.ts`)**:
>   - Introduce `DEFAULT_TEST_USER_ID` and `resolveUserId` to default missing `userId`.
>   - Remove strict `userId` validation across conversation handlers; keep resolution for contract parity.
> - **Tests (`tests/utils/authStrategy.test.ts`)**:
>   - Add helpers and teardown to mock/restore `navigator.userAgent` and `navigator.maxTouchPoints`.
>   - Update tests to pass UA explicitly and use touch-point mocking for desktop-like iPad cases.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit c7a18ac7b7d6a9cbff2e4adda4b727133ce21257. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
