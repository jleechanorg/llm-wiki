# PR #141: Integrate with external service

**Repo:** jleechanorg/ai_universe_frontend
**Merged:** 2025-11-11
**Author:** jleechan2015
**Stats:** +4272/-3267 in 17 files

## Summary
(none)

## Raw Body
Replace custom JSON-RPC 2.0 MCP client implementation with official @ai-universe/second-opinion-client package while maintaining backwards compatibility with existing frontend code.

Changes:
- Install @ai-universe/second-opinion-client@1.0.0
- Rewrite AiUniverseMcpClient as wrapper around SecondOpinionClient
- Maintain same API interface for seamless integration
- Preserve all existing methods (getSecondOpinion, startConversation, etc)
- Add proper TypeScript type conversions
- Pass type-check and lint validation

Benefits:
- Leverage official maintained client library
- Reduce custom code maintenance burden
- Get automatic updates and bug fixes
- Consistent API across all AI Universe services

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Replaces the custom MCP HTTP client with a wrapper around @ai-universe/second-opinion-client, updates build config and tests, and fixes validation/health checks and summary handling (including synthesis).
> 
> - **MCP Client**:
>   - Replace custom JSON-RPC client with wrapper over `@ai-universe/second-opinion-client` while preserving API (`getSecondOpinion`, `startConversation`, `addMessageToConversation`, `listAvailableTools`, `healthCheck`).
>   - Add strict input validation, real `testConnection()` and `checkHealth()` usage, and robust response conversion (tokens/cost preservation; synthesis included in summary; fallback for `successfulResponses`).
>   - Implement tools listing via `POST` to `'/mcp'`; normalize base URL handling.
> - **Build/Config**:
>   - Add dependency `@ai-universe/second-opinion-client`; set `overrides.firebase` to `^12.3.0`; add optional `@esbuild/linux-x64`.
>   - Vite: exclude server-only deps from optimizeDeps; externalize Node built-ins/server packages in Rollup; keep proxy setup.
> - **Tests**:
>   - Rewrite unit tests to mock the new client; add synthesis/summary aggregation cases; remove legacy fetch-based suites.
> - **UI/Types**:
>   - Minor `ConversationSidebar` polish (keyboard scope
