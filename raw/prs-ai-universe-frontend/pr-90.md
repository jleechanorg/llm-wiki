# PR #90: Fix streaming response hook URL resolution

**Repo:** jleechanorg/ai_universe_frontend
**Merged:** 2025-10-30
**Author:** jleechan2015
**Stats:** +28/-11 in 2 files
**Labels:** codex

## Summary
- resolve the streaming hook's server URL using normalized environment values
- fall back to the local /api/mcp proxy when no MCP URL is configured so streaming tests run

## Raw Body
## Summary
- resolve the streaming hook's server URL using normalized environment values
- fall back to the local /api/mcp proxy when no MCP URL is configured so streaming tests run

## Testing
- npm test -- --run

------
https://chatgpt.com/codex/tasks/task_e_6902c5b095f0832f84c6e1b4fe460494

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Normalizes streaming client server URL with a default fallback and makes unit tests mandatory in CI.
> 
> - **Hooks**:
>   - **`src/hooks/useMcp.ts` (streaming)**: Normalize `serverUrl` resolution for `createStreamingClient` using env var with trimming, trailing-slash handling, and automatic `/mcp` suffix; default to `/api/mcp` when unset.
> - **CI**:
>   - Update workflow to run unit tests without `continue-on-error`, making failures block CI.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 942395f25a2ae0b3fc37da28fc2e2fd8bb18a304. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
