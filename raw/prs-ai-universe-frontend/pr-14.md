# PR #14: refactor: remove hardcoded model names from frontend

**Repo:** jleechanorg/ai_universe_frontend
**Merged:** 2025-09-21
**Author:** jleechan2015
**Stats:** +59/-74 in 10 files

## Summary
- Remove hardcoded model names and display actual model names from backend response
- Update TypeScript types to use string instead of hardcoded literal types  
- Simplify component logic by removing model-specific display functions
- Clean up CSS by removing model-specific badge styles

## Test Plan
- [ ] Verify UI displays actual model names from backend instead of hardcoded labels
- [ ] Test with different model responses to ensure proper display
- [ ] Confirm no TypeScript errors after type changes

🤖 Generated with [Claude Code](https://claude.ai/code)

## Raw Body
## Summary
- Remove hardcoded model names and display actual model names from backend response
- Update TypeScript types to use string instead of hardcoded literal types  
- Simplify component logic by removing model-specific display functions
- Clean up CSS by removing model-specific badge styles

## Changes
- **Types**: Updated `SecondOpinionResponse` interface to use `string` for all model fields
- **Components**: Modified `SynthesisView` and `SecondOpinionCards` to display dynamic model names
- **Styles**: Removed model-specific CSS classes (claude, gemini, cerebras, perplexity)

## Test plan
- [ ] Verify UI displays actual model names from backend instead of hardcoded labels
- [ ] Test with different model responses to ensure proper display
- [ ] Confirm no TypeScript errors after type changes

🤖 Generated with [Claude Code](https://claude.ai/code)
