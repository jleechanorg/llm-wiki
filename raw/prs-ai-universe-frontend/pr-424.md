# PR #424: chore: dedupe CLI prereqs in setup script

**Repo:** jleechanorg/ai_universe_frontend
**Merged:** 2026-02-01
**Author:** jleechan2015
**Stats:** +11/-3 in 1 files
**Labels:** codex

## Summary
(none)

## Raw Body
### Motivation

- Avoid repeated `apt-get update` / prerequisite installs when both `gcloud` and `gh` are missing to reduce redundant work and speed up fresh-environment runs.

### Description

- Consolidated CLI prerequisite installation by moving `apt-transport-https`, `ca-certificates`, `curl`, and `gnupg` checks into a single conditional so the packages are ensured once when either `gcloud` or `gh` is required.
- Deferred `setup_gcloud_repo` and `setup_github_repo` to run only when their respective CLIs are actually missing, keeping repo source setup conditional and clearer.
- Updated `scripts/setup-dev-tools.sh` and committed the change with the automation marker in the commit message (`[codex-automation-commit]`).

### Testing

- Ran pre-commit hooks which executed `npm run lint` and `npm run type-check`, and both checks passed locally.
- Created a commit (`chore: dedupe cli prereqs in setup script [codex-automation-commit]`), but `git push` failed in this environment because no `origin` remote is configured. Please push the branch from a networked environment with repo access to update the PR.

------
[Codex Task](https://chatgpt.com/codex/tasks/task_e_697ecdc1e9a4832f80f62acd2404a5b2)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Small control-flow change in a dev setup script; main risk is altering install ordering/conditions for `gcloud`/`gh` on fresh machines.
> 
> **Overview**
> Avoids redundant prerequisite setup in `scripts/setup-dev-tools.sh` when installing `gcloud` and/or `gh`.
> 
> The script now detects missing CLIs first, ensures shared apt prereqs (`apt-transport-https`, `ca-certificates`, `curl`, `gnupg`) only once if either is needed, and runs `setup_gcloud_repo`/`setup_github_repo` only for the missing tool before updating apt and installing the corresponding package.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 85335e7f4d37346c8f7bbc8cd4ec8a97f5f01321. This will update automatically on n
