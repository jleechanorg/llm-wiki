# PR #235: feat: add dismissible rate limit toast

**Repo:** jleechanorg/ai_universe_frontend
**Merged:** 2025-11-13
**Author:** jleechan2015
**Stats:** +62/-37 in 2 files
**Labels:** codex

## Summary
- replace the plain text rate limit toast with a custom JSX notification that surfaces the limit, reset time, and contact link in a structured layout
- add a dedicated close button so the user can explicitly dismiss the persistent rate limit dialog

## Raw Body
## Summary
- replace the plain text rate limit toast with a custom JSX notification that surfaces the limit, reset time, and contact link in a structured layout
- add a dedicated close button so the user can explicitly dismiss the persistent rate limit dialog

## Testing
- npm run type-check


------
[Codex Task](https://chatgpt.com/codex/tasks/task_e_691596507a78832faf1f8b9e0e3facd3)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Replaces the plain text rate-limit toast with a styled, accessible, dismissible JSX toast component.
> 
> - **UI**:
>   - Replace plain text toast with a custom `toast.custom` JSX component in `src/utils/rateLimitDisplay.tsx` (migrated from `.ts`).
>     - Structured content: title, limit details, reset time, and a `mailto:` contact link.
>     - Adds explicit dismiss button; persistent with `id: 'rate-limit-notification'` and `position: 'top-right'`.
>     - Accessibility improvements: `role="alert"`, ARIA labels.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit cba3f72efb424bfa549bb87a865973da5c86a980. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
