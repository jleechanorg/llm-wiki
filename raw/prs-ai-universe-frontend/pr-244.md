# PR #244: feat: allow disabling auth for local dev

**Repo:** jleechanorg/ai_universe_frontend
**Merged:** 2025-11-16
**Author:** jleechan2015
**Stats:** +1271/-51 in 19 files
**Labels:** codex

## Summary
- add a `--disable-auth` option to `run_local_server.sh` so the dev server can export `VITE_AUTH_DISABLED=true`, document the flag, and always bind the Vite server to `0.0.0.0`
- plumb a new `isAuthDisabled` flag through the auth context so the UI, Firebase helpers, and conversation hooks can bypass Google Sign-In when the flag is active
- add the new env typing and docs to clarify the workflow

## Raw Body
## Summary
- add a `--disable-auth` option to `run_local_server.sh` so the dev server can export `VITE_AUTH_DISABLED=true`, document the flag, and always bind the Vite server to `0.0.0.0`
- plumb a new `isAuthDisabled` flag through the auth context so the UI, Firebase helpers, and conversation hooks can bypass Google Sign-In when the flag is active
- add the new env typing and docs to clarify the workflow

## Testing
- `npm run test -- tests/useConversations.test.tsx`


------
[Codex Task](https://chatgpt.com/codex/tasks/task_e_6916244bd484832fa38bb139e4c33def)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Adds a dev flag to disable Firebase auth and plumbs VITE_AUTH_DISABLED through auth, conversations, and UI to run with a local mock backend; updates docs, typings, and tests.
> 
> - **Dev Scripts**:
>   - `run_local_server.sh`: add `--disable-auth` (sets `VITE_AUTH_DISABLED=true` and `VITE_ENABLE_MCP_MOCKS=true`), parse flags, validate usage; start Vite with `--host 0.0.0.0 --port $VITE_PORT`.
> - **Auth**:
>   - Context/Provider: expose `isAuthDisabled`; when true, create `local-dev-user`, bypass Firebase flows, make `signIn/signOut` no-ops; call `setMockMcpEnabled(true/false)`.
>   - `App.tsx`: treat `isAuthDisabled` as authenticated; hide Sign Out when disabled.
>   - `WelcomePage`: hide Google sign-in when disabled.
>   - `ChatInterface` (+ Legacy): skip token subscription; pass `authToken: undefined` when disabled.
>   - `ConversationProvider`: set `userId` to `mock-user` (when mocks) or `local-dev-user` (when disabled) else Firebase `uid`.
>   - `utils/auth.ts`: return empty string for ID token when disabled.
> - **Types/Env**:
>   - Add `VITE_AUTH_DISABLED` to typings.
> - **Docs**:
>   - `docs/SCRIPTS.md`: document `--disable-auth` and examples.
> - **Tests**:
>   - Add comprehensive test matrix and unit tests covering auth-disabled behavior across App, WelcomePage, ChatInterface, ConversationProvider, and token util.
> 
> <sup>Written by [Cursor Bugbot](https
