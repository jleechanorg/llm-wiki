# PR #240: ci: allow manual auto deploy

**Repo:** jleechanorg/ai_universe_frontend
**Merged:** 2025-11-13
**Author:** jleechan2015
**Stats:** +7/-1 in 1 files

## Summary
- add workflow_dispatch to auto-deploy.yml so we can trigger the same path manually
- pass the optional reason input down to the reusable deploy workflow (with the existing auto string as fallback)

## Raw Body
## Summary
- add workflow_dispatch to auto-deploy.yml so we can trigger the same path manually
- pass the optional reason input down to the reusable deploy workflow (with the existing auto string as fallback)

## Testing
- not run (workflow-only change)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Adds a manual trigger to the auto-deploy workflow and passes an optional reason to the reusable deploy workflow with a fallback message.
> 
> - **CI / Workflows**:
>   - Add `workflow_dispatch` to `.github/workflows/auto-deploy.yml` with optional `inputs.reason`.
>   - Pass `reason` to `deploy-dev.yml`, defaulting to `format('Auto-deploy from push to {0}', github.ref_name)` when no input is provided.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 36fd1bb7420b44119ae62b62481ff0a1a1225410. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
