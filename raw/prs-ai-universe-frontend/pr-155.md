# PR #155: Fix multi-model message sending issue

**Repo:** jleechanorg/ai_universe_frontend
**Merged:** 2025-11-11
**Author:** jleechan2015
**Stats:** +371/-172 in 7 files

## Summary
(none)

## Raw Body
<!-- CURSOR_SUMMARY -->
> [!NOTE]
> Shows a sparkle icon on user messages sent in multi-model mode by propagating `requestedMode` metadata end-to-end, and adds e2e tests for visibility and persistence.
> 
> - **UI**:
>   - **User Message Indicator**: Display sparkle icon on user messages when `metadata.requestedMode` is `"multi"` in `components/MessageItem.tsx`.
>   - **Controls/Testing Hooks**: Add `data-testid` to `chat-mode-toggle`, input, and submit in `components/ChatInterface.tsx` and `components/ChatInput.tsx`.
> - **Data/Adapters**:
>   - Extend `UIMessage` with `metadata` (supports `requestedMode`).
>   - Normalize and carry `secondOpinion` via `adaptMessagesForUI`; strip raw `secondOpinion` from metadata to avoid duplication in `adapters/messageAdapter.ts`.
> - **Hooks/API**:
>   - Send `requestedMode` in message metadata for both multi-model (`"multi"`) and normal (`"normal"`) flows; include in optimistic messages and merge logic in `hooks/useConversations.ts`.
>   - Add `requestedMode` to `MessageMetadata` in `types/conversation.ts`.
> - **Tests**:
>   - Add Playwright e2e `tests/e2e/multimodel-sparkle-icon.spec.ts` validating immediate display, absence in normal mode, persistence after refresh, and faster timing vs assistant response.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit c487a098a8b09322240dc2dfabc384cc680202a8. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->

## Summary by CodeRabbit

* **New Features**
  * Added a sparkle icon to user messages in multi-model mode, displaying immediately and persisting across page refreshes.

* **Tests**
  * Added end-to-end tests for the multi-model sparkle icon feature, validating visibility, mode detection, and persistence behavior.

<!-- end of auto-generated comment: release notes by coderabbit.ai -->
