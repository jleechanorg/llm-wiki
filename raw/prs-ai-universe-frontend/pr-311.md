# PR #311: Address touch scroll utility redundancy

**Repo:** jleechanorg/ai_universe_frontend
**Merged:** 2025-11-27
**Author:** jleechan2015
**Stats:** +2508/-553 in 16 files
**Labels:** codex

## Summary
- remove the redundant touch-passthrough utility and rely on a single touch-scroll class
- update SecondOpinionCards to use touch-scroll on opinion cards and previews for consistent mobile scrolling behavior

## Raw Body
## Summary
- remove the redundant touch-passthrough utility and rely on a single touch-scroll class
- update SecondOpinionCards to use touch-scroll on opinion cards and previews for consistent mobile scrolling behavior

## Testing
- npm run lint
- npm run type-check

------
[Codex Task](https://chatgpt.com/codex/tasks/task_e_6924b2d8c4fc832fb4fe5b5903e93b88)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Introduces a TypeScript-based local server launcher, optimizes conversation message handling to avoid history fetches, removes MCP connection delay, and improves mobile sidebar/touch interactions with accompanying tests and docs.
> 
> - **Tooling/Dev**:
>   - **Local Launcher**: Add `scripts/run-local.ts` (TypeScript) and delegate `run_local_server.sh` to it; new npm scripts `local*`; install `@ai-universe/typescript-server-launcher`.
> - **Performance**:
>   - **Assistant Extraction**: In `src/services/conversationApi.ts`, prefer `assistantMessage` from API responses in `createConversation`/`createMessage`, avoiding history fetch fallback.
>   - **MCP Connect**: In `src/hooks/useMcp.ts`, remove `setTimeout(0)` deferral; connect immediately.
> - **UI/UX (Mobile)**:
>   - **Sidebar**: `ChatInterface` closes sidebar on conversation select in mobile view.
>   - **Touch Handling**: Add `touch-target`/`touch-passthrough` usage in `MessageItem` and `SynthesisView`; enhance `.touch-passthrough` CSS with `overscroll-behavior: contain`.
> - **Docs**:
>   - Add `docs/async-optimization-review.md` summarizing async/parallelization findings and fixes.
> - **Tests**:
>   - Add tests for assistant extraction, MCP connection timing, mobile sidebar behavior, Firebase init race handling, and the local launcher script.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 886c154cb405e05ef1e55e2049aebfcd634ded88. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
