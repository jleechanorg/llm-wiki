# PR #329: Redesign multi model toggle to be compact

**Repo:** jleechanorg/ai_universe_frontend
**Merged:** 2025-12-01
**Author:** jleechan2015
**Stats:** +37/-33 in 1 files

## Summary
(none)

## Raw Body
Replace single toggle button with pill button group showing both modes:
- "Single" mode with target icon (red when active)
- "Multi AI" mode with sparkles icon (purple/amber when active)
- Compact design with subtle backgrounds and borders
- Active state uses white background with shadow

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Replaces the single toggle with a compact two-button pill for switching between Single and Multi AI modes using Target and Sparkles icons.
> 
> - **UI (ChatInterface)**:
>   - **Mode Switch Redesign**: Replace single toggle with a compact segmented pill showing both modes (`Single`, `Multi AI`).
>     - Adds `Target` icon for `Single` and reuses `Sparkles` for `Multi AI` with active-state colors and subtle borders/shadows.
>     - New buttons `data-testid`: `chat-mode-single` (Single) and retains `chat-mode-toggle` (Multi AI).
>   - Removes `toggleChatMode` handler in favor of explicit `setChatMode` per button.
>   - Minor import update to include `Target`.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 1e28fc101c0c432ddfca6a3127f7bd9f92ad1d1e. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
