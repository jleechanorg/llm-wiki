# PR #282: Add local server logging to temp directory

**Repo:** jleechanorg/ai_universe_frontend
**Merged:** 2025-11-20
**Author:** jleechan2015
**Stats:** +172/-18 in 2 files

## Summary
(none)

## Raw Body
…ries

- Add get_log_dir() function to generate structured log paths: /tmp/<repo>/<branch>/
- Update all server startup functions to redirect output to structured log files:
  - dev-server.log: Development server logs
  - proxy-server.log: Production proxy server logs
  - preview-server.log: Preview server logs
  - test-run.log: Test execution logs
- Add 'logs' command to show log directory and list available log files
- Update Cypress parallel test runner to use the same structured log directory
- Display log file paths during server startup for easy monitoring
- Auto-create log directories on startup
- Sanitize repo/branch names for filesystem compatibility

Benefits:
- Easier log organization across multiple branches and repos
- Prevents log file conflicts between different projects
- Simplifies debugging by keeping logs organized per branch
- Consistent with git workflow (branch-specific logs)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Add structured per-repo/branch logging for local servers and Cypress, piping output to /tmp/<repo>/<branch> with a new logs command and surfaced log paths.
> 
> - **Scripts (`run_local_server.sh`)**:
>   - Add `get_log_dir`/`ensure_log_dir` to create structured logs at `/tmp/<repo>/<branch>`; sanitize names.
>   - Pipe output of `dev`, `proxy`, `preview`, and `test` to log files: `dev-server.log`, `proxy-server.log`, `preview-server.log`, `test-run.log` (via `tee`).
>   - Display log file locations and tail commands on startup; add `logs` command to list log files and sizes.
>   - Enable `set -o pipefail` for safer piping.
> - **Testing (`testing_integration/ui/run_cypress_parallel.mjs`)**:
>   - Write per-chunk Cypress logs to the same structured log dir (`cypress-*.parallel.log`).
>   - Auto-create log directory; print its path; update messages to reflect new location.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit c2b9b6373103f8b36daa8d696abbf344bf6d01e2. This will upda
