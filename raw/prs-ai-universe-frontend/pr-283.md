# PR #283: feat: auth reliability, conversation ID validation, and backend logging

**Repo:** jleechanorg/ai_universe_frontend
**Merged:** 2025-11-21
**Author:** jleechan2015
**Stats:** +1260/-373 in 27 files

## Summary
This PR improves authentication reliability, conversation ID validation, and adds comprehensive backend logging infrastructure.

### Authentication & Conversation Reliability
- Rebuild authenticated MCP client per call with 401 retry and `forceRefresh` tokens
- Prevent infinite retry loops and token refresh storms with precise error pattern matching
- Fix conversation ID validation to accept both UUID v4 and Firestore 20-char alphanumeric IDs
- Handle nested `{history:{messages:[]}}` response fo

## Test Plan
- [x] `npm run type-check` - TypeScript passes
- [x] `npm run lint` - ESLint passes with zero warnings
- [x] `npm test` - 318 tests pass (42 skipped)
- [x] Pre-push hooks pass

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->

## Raw Body
## Summary

This PR improves authentication reliability, conversation ID validation, and adds comprehensive backend logging infrastructure.

### Authentication & Conversation Reliability
- Rebuild authenticated MCP client per call with 401 retry and `forceRefresh` tokens
- Prevent infinite retry loops and token refresh storms with precise error pattern matching
- Fix conversation ID validation to accept both UUID v4 and Firestore 20-char alphanumeric IDs
- Handle nested `{history:{messages:[]}}` response format from backend

### Conversation/Message Normalization
- Ensure conversationIds preserved during normalization
- Enforce `createdAt` as ISO 8601 string throughout message lifecycle
- Tighten message normalization helpers and mock data to match types
- Extract helpers to `conversationHelpers.ts` and `messageNormalizers.ts` with unit tests

### Backend Logging Infrastructure
- Add browser fetch logger with unique IDs, recursion guard, and body capture
- Add Vite middleware (`backendLoggerMiddleware.ts`) to write JSON logs to `/tmp/ai-universe-frontend-logs/`
- Add optional GCP Cloud Logging support (`@google-cloud/logging`)
- Fix backendLogger test failures with guards for test environments

### Developer Tooling
- Add `--force-default-port` flag to `run_local_server.sh` for port reclaim
- Default noserver tests to use `localhost:2000` MCP URL
- Add `testing_integration/run_tests.sh` wrapper script
- Update mock data to use ISO `createdAt` timestamps

## Files Changed (26 files, +1188/-373 lines)
- `src/services/mcpClient.ts` - Client rebuild per call, validation fix
- `src/services/conversationApi.ts` - Nested response handling, auth retry
- `src/hooks/useConversations.ts` - Conversation ID validation, ISO timestamps
- `src/utils/backendLogger.ts` - Enhanced logging with file/cloud support
- `vite-plugins/backendLoggerMiddleware.ts` - New middleware for dev logging
- `testing_integration/run_all_tests.mjs` - Env var passthrough for noserver tests
- And more...


