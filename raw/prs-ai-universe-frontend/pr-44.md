# PR #44: feat: Add GitHub Actions PR Preview Deployment with VPC-SC Support

**Repo:** jleechanorg/ai_universe_frontend
**Merged:** 2025-09-29
**Author:** jleechan2015
**Stats:** +1632/-1282 in 12 files

## Summary
• Implements automated PR preview deployments using the published `github-actions-deploy` PyPI package
• Adds VPC-SC compatible async build support to prevent CI failures from log streaming restrictions
• Provides comprehensive PR commenting with build status and deployment URLs

## Test Plan
- [x] Package published to PyPI successfully
- [x] Workflow configuration validated
- [x] Deploy script updated with async build support
- [ ] Test PR preview deployment in GitHub Actions
- [ ] Verify PR commenting functionality
- [ ] Confirm VPC-SC compatibility

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Adds GCP deploy and PR-preview workflows, makes Cloud Build async in CI, standardizes backend config, hardens Firebase/MCP client,

## Raw Body
## Summary
• Implements automated PR preview deployments using the published `github-actions-deploy` PyPI package
• Adds VPC-SC compatible async build support to prevent CI failures from log streaming restrictions
• Provides comprehensive PR commenting with build status and deployment URLs

## Changes Made
• **New Workflow**: `.github/workflows/pr-preview.yml` - Automated PR preview deployments
• **Enhanced Script**: `deploy.sh` - Added CI environment detection and async build mode
• **Integration**: Uses published `github-actions-deploy-0.1.0` PyPI package for deployment automation

## Technical Implementation
• **VPC-SC Solution**: Async builds with status polling instead of log streaming
• **Cloud Build**: Frontend-specific configuration with Firebase environment variables
• **PR Comments**: Professional build status updates with service URLs and health checks
• **Environment Detection**: Automatic CI mode for GitHub Actions, sync mode for local development

## Test Plan
- [x] Package published to PyPI successfully
- [x] Workflow configuration validated
- [x] Deploy script updated with async build support
- [ ] Test PR preview deployment in GitHub Actions
- [ ] Verify PR commenting functionality
- [ ] Confirm VPC-SC compatibility

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Adds GCP deploy and PR-preview workflows, makes Cloud Build async in CI, standardizes backend config, hardens Firebase/MCP client, tightens types, and upgrades ai/lighthouse.
> 
> - **CI/CD**:
>   - **Workflows**: Add `/.github/workflows/deploy.yml` and `/.github/workflows/pr-preview.yml` for GCP Cloud Run deploys and PR previews (auth via `GCP_SA_KEY`, health checks, summary output).
>   - **Docs**: Add `DEPLOYMENT_FIX.md` noting secrets and `secretmanager.secretAccessor` permission.
> - **Deployment**:
>   - **Script**: Update `deploy.sh` to support async Cloud Build in CI (status polling) to avoid VPC‑SC log streaming; environment
