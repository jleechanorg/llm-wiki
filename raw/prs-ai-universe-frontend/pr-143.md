# PR #143: Fix 6 test failures in conversation system

**Repo:** jleechanorg/ai_universe_frontend
**Merged:** 2025-11-11
**Author:** jleechan2015
**Stats:** +755/-45 in 17 files

## Summary
(none)

## Raw Body
<!-- CURSOR_SUMMARY -->
> [!NOTE]
> Preserves optimistic messages across refetches, switches synthesis token field to tokens, surfaces mutation errors, updates UI/rendering and tests, and tweaks CI for integration tests.
> 
> - **Conversations (core hook)**:
>   - Add `cacheSnapshotRef` and `messagesQuery.select` merge to retain optimistic/user and assistant messages across refetches; snapshot after mutation success.
>   - Include `sendMessageMutation.error` in returned `error` state.
>   - Ensure temp conversation IDs aren’t sent to backend; keep queries enabled without auto-refetch for existing conversations.
> - **UI**:
>   - `ChatInterface`: disable input when disconnected; respect selected mode on first send.
>   - Update token displays to use `tokens` (not `tokensUsed`) in `MessageItem`, `SecondOpinionCards`, `SynthesisView`, and `conversations/MultiModelDetails`.
> - **Types/Utils**:
>   - `ModelOpinion` adds `tokens`; `normalizeSecondOpinion` accepts `tokens|tokensUsed`, sets both, and computes totals from `tokens`.
> - **Tests**:
>   - Expand/adjust `useConversations` optimistic persistence, error propagation, and temp-ID behavior; update `mcpClient` and `ChatInterface` tests; all passing.
> - **Mocks/Samples**:
>   - Propagate `tokens` field in mocks and canonical samples.
> - **CI/Docs**:
>   - CI: skip Cypress binary for integration tests.
>   - Add `BEAD_RESOLUTION_EVIDENCE.md` and `PR_DESCRIPTION.md` with verification evidence.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 0f0ad2d2a5ba880de671f14e7235b5408558086b. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->
## Summary by CodeRabbit

* **Bug Fixes**
  * Preserve optimistic messages during conversation history refreshes
  * Surface send-message errors in the overall error state
  * Fix token coun
