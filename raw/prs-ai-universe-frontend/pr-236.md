# PR #236: fix: suppress duplicate rate limit toast

**Repo:** jleechanorg/ai_universe_frontend
**Merged:** 2025-11-13
**Author:** jleechan2015
**Stats:** +8/-1 in 1 files
**Labels:** codex

## Summary
- ensure the conversations hook stores the standard `RATE_LIMIT_ERROR_MESSAGE` when rate limit errors occur so the UI recognizes them as informational notifications
- keep non-rate-limit errors unchanged, preventing duplicate toasts while preserving existing error handling

## Raw Body
## Summary
- ensure the conversations hook stores the standard `RATE_LIMIT_ERROR_MESSAGE` when rate limit errors occur so the UI recognizes them as informational notifications
- keep non-rate-limit errors unchanged, preventing duplicate toasts while preserving existing error handling

## Testing
- npm run lint
- npm run type-check

------
[Codex Task](https://chatgpt.com/codex/tasks/task_e_691598e21324832fb874a3f2504155ce)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Map RateLimitError to the shared RATE_LIMIT_ERROR_MESSAGE in `useConversations` to prevent duplicate toasts while leaving other errors unchanged.
> 
> - **Hooks**:
>   - `src/hooks/useConversations.ts`
>     - Error handling: when `RateLimitError` occurs, set error to `RATE_LIMIT_ERROR_MESSAGE` instead of the raw message to avoid duplicate toasts; preserve original messages for non-rate-limit errors.
>     - Adds import of `RATE_LIMIT_ERROR_MESSAGE`.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 8b1d0d203616ccab69f1659d2fd93041349e6179. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->

## Summary by CodeRabbit

* **Bug Fixes**
  * Enhanced error messaging when conversations reach rate limits, providing clearer feedback about service constraints.

<!-- end of auto-generated comment: release notes by coderabbit.ai -->
