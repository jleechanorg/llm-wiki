# PR #383: chore: add dev tooling setup script

**Repo:** jleechanorg/ai_universe_frontend
**Merged:** 2026-02-01
**Author:** jleechan2015
**Stats:** +113/-180 in 4 files
**Labels:** codex

## Summary
(none)

## Raw Body
### Motivation

- Provide a single script to install and verify common CLI/tools referenced by `.claude/skills` so contributors have a reproducible dev environment.

### Description

- Add `scripts/setup-dev-tools.sh` which verifies or installs `python3`, `pip3`, `node`/`npm`, `rg` (ripgrep), `curl`, `tesseract-ocr`, and the Google Cloud SDK (`gcloud`).
- The script also installs Python OCR runtime deps via `python3 -m pip install --user --upgrade pillow pytesseract` and uses apt-based installs and the official GCloud apt repo for `gcloud`.
- The script requires root (or `sudo`) to install system packages and prints status messages for each check.

### Testing

- Ran pre-commit checks which include `npm run lint` and `npm run type-check`, and both checks passed.
- The created script was made executable and committed as `scripts/setup-dev-tools.sh`.

------
[Codex Task](https://chatgpt.com/codex/tasks/task_e_697b103ecff0832fbf70bcc58d5c66fa)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Mostly documentation cleanup plus small control-flow and keyring-writing tweaks in a dev setup script; low impact beyond developer environments, with minor risk of affecting fresh apt-based installs.
> 
> **Overview**
> **Improves dev-environment tooling setup and its verification docs.** The `setup-dev-tools.sh` flow is adjusted to only install apt prerequisites and add the Google Cloud/GitHub CLI apt repos when `gcloud`/`gh` are actually missing, and keyring setup now logs when keyrings already exist and writes the gcloud keyring via `tee`.
> 
> Docs (`codex-container-install-test.md`, `dev-tools-setup-run.md`, `gh-comment-check.md`) are rewritten from pasted session logs into short, repeatable checklists with expected outcomes and troubleshooting guidance, explicitly discouraging embedding raw command output.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 5b4c52ccf4a5eef8b39f14268beacf13a395ed76. This will update automatically on
