# PR #173: fix: guard Husky prepare script during Docker builds

**Repo:** jleechanorg/ai_universe_frontend
**Merged:** 2025-11-10
**Author:** jleechan2015
**Stats:** +1/-1 in 1 files
**Labels:** codex

## Summary
- guard the Husky prepare script so Docker/CI builds exit cleanly when devDependencies are omitted
- fail fast with an explicit error message if Husky is installed but the install step errors for another reason

## Raw Body
## Summary
- guard the Husky prepare script so Docker/CI builds exit cleanly when devDependencies are omitted
- fail fast with an explicit error message if Husky is installed but the install step errors for another reason

## Testing
- npm run type-check
- npm exec lint-staged -- --allow-empty *(fails: lint-staged devDependency unavailable in this environment)*

------
[Codex Task](https://chatgpt.com/codex/tasks/task_e_691224eb0e0c832fbcb22d4c4d61b7d0)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Update `package.json` `prepare` script to skip Husky in CI/when `SKIP_HUSKY` is set, safely resolve Husky, and fail with a clear error if installation runs but errors.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit e6e50a67b67a819070e492d39effd49613a5b416. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
