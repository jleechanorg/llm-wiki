---
name: project_2026-06-11_pr7441_level_up_signal_prompt_fix
description: PR #7441 prompt-first level_up_signal current_turn_exp fix — driven to green, contradiction spanned 8 prompts not 5
metadata:
  type: project
---

PR [#7441](https://github.com/jleechanorg/worldarchitect.ai/pull/7441) (rev-0zlyk, first M1 unfreeze candidate, precondition for M2). Goal: prompts must emit `current_turn_exp` INSIDE `level_up_signal` because the backend gate `rewards_engine.canonicalize_level_up_signal` → `_reject_malformed_normalized_lus` drops any signal lacking it (`MISSING_EVIDENCE: current_turn_exp is required`).

**Key finding**: the prompt/backend contradiction was NOT in 5 prompts (as the original PR claimed) but in **8**: the PR's original narrative/rewards/level_up/god_mode + I added `game_state_instruction.md` (3 spots), `combat_system_instruction.md` (2 spots), `narrative_lite_system_instruction.md` (1), the 2 cursor-flagged modal-flow bullets in `level_up_instruction.md`, AND the token-constrained ESSENTIALS block in `rewards_system_instruction.md` (lines 31-35, "with only" — cursor Bugbot caught this; the main-body fix missed it). Kept the ban on *derived* thresholds/deltas (`xp_to_next`, percentages); only the post-award total `current_turn_exp` belongs in the signal.

**Gotchas hit**:
- `mvp_site/schemas/prompt_tool_contracts.json` tracks sha256 of `game_state_instruction.md` — editing it fails "Prompt / Tool Contract Hash Validation" AND "Harness autonomy checks" (same root cause). Fix: `python scripts/validate_prompt_tool_contracts.py --update` + manually bump the `version` field. M0 merge bumped it to 1.1.32 concurrently → merge conflict in the manifest; resolved to 1.1.33 + regenerate.
- Real-mode testing_mcp needs `WORLDAI_DEV_MODE=true` (NOT TESTING_AUTH_BYPASS which would bypass auth) when `WORLDAI_GOOGLE_APPLICATION_CREDENTIALS` is set in bashrc — else `clock_skew_credentials.validate_deployment_config()` raises. Run with worktree venv `/Users/jleechan/projects/worktree_lvl_clean_flags/venv/bin/python` pointed at the /tmp clone (imports resolve fine).
- Regression test `test_level_up_signal_prompts_require_current_turn_exp_field` (test_prompts.py::TestLevelUpSignalPromptCoverage): use `" ".join(content.split())` whitespace-normalize before the forbidden-wording assert — a fixed `\n  ` pattern missed the differently-indented ESSENTIALS block (cursor Low-Sev).

**Real-LLM evidence** (commit 420bb6d69e, head later e1c8988927): raw model emitted `{current_level:4,target_level:5,current_turn_exp:6780,previous_turn_exp:6400,total_exp_for_next_level:6500}`; gate ACCEPTS it, DROPS the old 2-field shape. Scenario 3/4 — the one fail is the documented `public_level_up_signal_contract` (rewards_engine.py:708) response-strip propagation gap, deliberately OUT OF SCOPE (single-owner file, M0 freeze). Gist b8af287d968b73b547feddd172973af7 (.cast + console). Bundle iteration_003.

Commits: 420bb6d69e (8-prompt fix + test), 870a07cf13 (hash bump), d8b8ba6c67 (merge main), e1c8988927 (ESSENTIALS + test harden). NEVER merge (human-gated); never force-push.

**UPDATE (cursor re-review loop)**: the contradiction was even wider than 8 prompts. Cursor Bugbot, on each push, surfaced one more bundled prompt at a time. Final scope = **10 level_up_signal-emitting prompts** all fixed: narrative_system, rewards_system (BOTH main body, ESSENTIALS block, AND the "1a. Raw Level-Up Signal" block — three separate spots in one file), level_up, god_mode, game_state, combat, narrative_lite, planning_protocol, deferred_rewards (StoryMode every-10th-turn injection), level_up_modal_override — PLUS the duplicate "Pending Level XP Denominator" string inline in `agent_prompts.py:2480` (build_continuation_reminder). Lesson: when fixing a prompt-contract contradiction, grep the WHOLE prompts/ dir + agent_prompts.py inline strings for every phrasing variant ("not inside", "out of", "never put them in", "keep XP totals ... in rewards_box") in ONE pass up front — don't fix file-by-file as a bot finds them (cost 6 extra review cycles). Also found a second real requirement: the gate needs XP-delta evidence (previous_turn_exp OR xp_earned/... OR tangible rewards), so on non-award modal turns previous_turn_exp is REQUIRED not optional. Commit chain: 420bb6d69e, 870a07cf13, d8b8ba6c67(merge), e1c8988927, 06cdeacfaa, f303e7495a, 83a4c63eb9, a2af4e7be9. Regression test grew to 10 subtests; uses whitespace-normalized assertion so indentation variants don't fool it.

**OUTCOME (2026-06-11)**: GREEN. Head f417e4821711f116f90d1696010b9165df0f7054, mergeStateStatus=CLEAN. Skeptic self-verify (run 27317942480) verdict PASS at exact head — all 8 gates PASS (CI/conflicts/CodeRabbit-APPROVED/Bugbot/inline-comments/evidence/self-verify/smoke). Green Gate run 27317950977 = success; every check-run on the head is `success`. NOT merged (human-gated). Final commit chain (regular pushes only): 420bb6d69e → 870a07cf13 → d8b8ba6c67(merge main) → e1c8988927 → 06cdeacfaa → f303e7495a → 83a4c63eb9 → f417e48217.
