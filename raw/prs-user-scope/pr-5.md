# PR #5: backup: exclude codex sessions_archive from Dropbox sync

**Repo:** jleechanorg/user_scope
**Merged:** 2026-03-09
**Author:** jleechan2015
**Stats:** +30/-1 in 13 files

## Summary
- exclude `~/.codex/sessions/sessions_archive/` from the Dropbox sync path in `scripts/backup-home.sh`
- keep exclusion configurable via `CODEX_SESSIONS_EXCLUDE_PATTERNS` (default `sessions_archive/`)
- add regression test to ensure archived sessions are not copied while active sessions are still copied

## Raw Body
## Summary
- exclude `~/.codex/sessions/sessions_archive/` from the Dropbox sync path in `scripts/backup-home.sh`
- keep exclusion configurable via `CODEX_SESSIONS_EXCLUDE_PATTERNS` (default `sessions_archive/`)
- add regression test to ensure archived sessions are not copied while active sessions are still copied

## Validation
- `pytest -q tests/test_backup_home_dryrun.py -k "codex_sessions_sync_excludes_sessions_archive_subdir or dry_run_reports_success or dry_run_dropbox_receives_private_data"`
- `pytest -q tests/test_install_script.py -k "backup_home_script_does_not_send_private_data_to_git"`

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Changes backup inclusion rules for Dropbox; a misconfigured exclude pattern could unintentionally omit session data from backups.
> 
> **Overview**
> Prevents `~/.codex/sessions/sessions_archive/` from being copied into the Dropbox backup by adding a configurable `CODEX_SESSIONS_EXCLUDE_PATTERNS` (default `sessions_archive/`) and wiring it into the Codex sessions rsync entry in `scripts/backup-home.sh`.
> 
> Adds a live rsync regression test (`test_live_codex_sessions_sync_excludes_sessions_archive_subdir`) to verify active sessions are still backed up while the archive subdirectory is excluded.
> 
> Also includes new `.beads` issue history artifacts generated by an automated smoke test.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit f81a6d15d805e79b5db57001a03d05a1158a86a7. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
