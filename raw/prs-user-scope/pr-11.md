# PR #11: chore: remove broken bd hooks shims

**Repo:** jleechanorg/user_scope
**Merged:** 2026-03-14
**Author:** jleechan2015
**Stats:** +14/-96 in 5 files

## Summary
(none)

## Raw Body
bd hooks subcommand no longer exists in br v0.1.24. Removes post-checkout, post-merge, pre-push, prepare-commit-msg shims. Simplifies pre-commit to gitleaks-only scan.

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Hook-only changes that affect local developer workflow; main risk is commits no longer being checked by `bd` hooks and differing behavior depending on whether `gitleaks` is installed.
> 
> **Overview**
> Removes the `.beads` git hook shims (`post-checkout`, `post-merge`, `pre-push`, `prepare-commit-msg`) that delegated to the now-missing `bd hooks run ...` command.
> 
> Rewrites `.beads/hooks/pre-commit` to run `gitleaks protect --staged` (optionally using `.gitleaks.toml`) and block commits on detected secrets, otherwise warning if `gitleaks` is not installed.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 9687e97f34108d726810a218519fe45d14dd943f. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
