# PR #13: chore: clean up stale beads br_history snapshots and sync issues

**Repo:** jleechanorg/user_scope
**Merged:** 2026-04-10
**Author:** jleechan2015
**Stats:** +2/-8 in 9 files

## Summary
- Remove 8 orphaned `.beads/.br_history/` snapshot files from March 9 beads migration runs
- Sync `.beads/issues.jsonl` to latest state

## Test Plan
- [ ] CI passes (hygiene, hook validity, backup dry-run tests)

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Low risk housekeeping change that only deletes obsolete `.beads/.br_history` snapshot artifacts and updates the `.beads/issues.jsonl` data file; no runtime code paths are modified.
> 
> **Overview**
> Cleans up Beads history by removing several orphaned `.beads/.br_history/` snapshot `.jsonl` and `.meta.json` files

## Raw Body
## Summary
- Remove 8 orphaned `.beads/.br_history/` snapshot files from March 9 beads migration runs
- Sync `.beads/issues.jsonl` to latest state

## Test plan
- [ ] CI passes (hygiene, hook validity, backup dry-run tests)

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Low risk housekeeping change that only deletes obsolete `.beads/.br_history` snapshot artifacts and updates the `.beads/issues.jsonl` data file; no runtime code paths are modified.
> 
> **Overview**
> Cleans up Beads history by removing several orphaned `.beads/.br_history/` snapshot `.jsonl` and `.meta.json` files from prior migration/smoke-test runs.
> 
> Updates `.beads/issues.jsonl` to reflect the latest issue state, including adding two newly closed tasks.
> 
> <sup>Reviewed by [Cursor Bugbot](https://cursor.com/bugbot) for commit dd185578c5514e635d6bb3d5180065d80ae954a7. Bugbot is set up for automated code reviews on this repo. Configure [here](https://www.cursor.com/dashboard/bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
