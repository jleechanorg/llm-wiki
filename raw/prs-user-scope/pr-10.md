# PR #10: backup: daily 9am email report for GitHub + Dropbox backup runs

**Repo:** jleechanorg/user_scope
**Merged:** 2026-03-14
**Author:** jleechan2015
**Stats:** +283/-33 in 82 files

## Summary
- add backup run report capture to scripts/backup-home.sh so each run writes a latest report with file-level rsync itemized lines and final git/dropbox status
- add scripts/send_backup_report_email.sh to email the latest backup report (or missing-report/failure state)
- extend scripts/install_conversation_backup_launchd.sh to also install a second LaunchAgent that sends the backup report email daily at 9:00 AM local time
- update README with new daily-email behavior and script

## Raw Body
## Summary
- add backup run report capture to scripts/backup-home.sh so each run writes a latest report with file-level rsync itemized lines and final git/dropbox status
- add scripts/send_backup_report_email.sh to email the latest backup report (or missing-report/failure state)
- extend scripts/install_conversation_backup_launchd.sh to also install a second LaunchAgent that sends the backup report email daily at 9:00 AM local time
- update README with new daily-email behavior and script

## Notes
- report email recipient uses BACKUP_EMAIL (or BACKUP_REPORT_EMAIL) from ~/Library/Application Support/user-scope/config/home-backup.env
- installer now deploys two launchd labels:
  - com.user-scope.home-backup (every 4h)
  - com.user-scope.home-backup-email (daily 09:00)

## Validation
- bash -n scripts/backup-home.sh scripts/send_backup_report_email.sh scripts/install_conversation_backup_launchd.sh

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Medium Risk**
> Touches automated backup + git commit/push behavior and introduces an email-sending path (credentials/env-driven), so failures could affect scheduled backups or leak operational details if misconfigured.
> 
> **Overview**
> `backup-home.sh` now writes an atomic “latest” run report (itemized `rsync` lines plus final `git`/Dropbox status), switches `rsync` to `--inplace` for large-file reliability, and writes Dropbox metadata via tmp+`mv` to avoid lock errors. It also changes git snapshotting to commit/push to `main`/`master` even when invoked from another branch by using a temporary `git worktree`.
> 
> Adds `send_backup_report_email.sh` to email the latest report (or `MISSING_REPORT`/`FAILED`/`INCOMPLETE` status) and extends `install_conversation_backup_launchd.sh` to optionally install a second LaunchAgent that runs the email script daily at 09:00 when `BACKUP_EMAIL`/`BACKUP_REPORT_EMAIL` is configured. README and checked-in backup artifacts are updated accordingly.
> 
> <sup>Written by [Cursor Bugbot](https://curso
