# PR #12: [agento] fix: compute disk usage percentage from same basis as used_gb

**Repo:** jleechanorg/user_scope
**Merged:** 2026-04-02
**Author:** jleechan2015
**Stats:** +3/-1 in 1 files

## Summary
The disk usage alert script was reporting inconsistent metrics: `Used: 796G of 926G (12%)`. The percentage shown (12%) did not match the actual used/total ratio (796/926 ≈ 86%).

Root cause: script computes `used_gb` correctly as `total - available` (APFS-aware) but printed percentage from raw `df $5` field, which under-reports used space on APFS because it only counts non-purgeable blocks.

## Background
The disk usage alert script was reporting inconsistent metrics: `Used: 796G of 926G (12%)`. The percentage shown (12%) did not match the actual used/total ratio (796/926 ≈ 86%).

Root cause: script computes `used_gb` correctly as `total - available` (APFS-aware) but printed percentage from raw `df $5` field, which under-reports used space on APFS because it only counts non-purgeable blocks.

## Raw Body
## Background

The disk usage alert script was reporting inconsistent metrics: `Used: 796G of 926G (12%)`. The percentage shown (12%) did not match the actual used/total ratio (796/926 ≈ 86%).

Root cause: script computes `used_gb` correctly as `total - available` (APFS-aware) but printed percentage from raw `df $5` field, which under-reports used space on APFS because it only counts non-purgeable blocks.

## Goals

- Display percentage computed from the same used/total basis used for `used_gb` calculation
- Keep all other behavior unchanged

## Testing

Ran corrected script with `--dry-run` (threshold 50G) and high threshold (900G):

```
$ DISK_ALERT_THRESHOLD_GB=50 ./scripts/disk_usage_alert.sh --dry-run
DRY_RUN: alert would be sent
[disk-alert] Mac disk usage 799G (threshold 50G)

Disk usage threshold exceeded on Mac.
Used: 799G of 926G (86)

$ DISK_ALERT_THRESHOLD_GB=900 ./scripts/disk_usage_alert.sh
OK: 799G used of 926G (86), threshold=900G
```

Percentage now consistent: 799G of 926G (86%) calculated from same basis.

Verified production install location also works:

```
$ ~/Library/Application\ Support/user-scope/bin/disk_usage_alert.sh
OK: 799G used of 926G (86), threshold=900G
```

## Verification

- [x] Script runs without errors
- [x] Percentage (86%) now matches used/total ratio
- [x] Production launchd job reinstalled to use fixed script

Fixes: jleechanorg/user_scope#orch-d5m

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> **Low Risk**
> Low risk: changes only the displayed `used_pct` calculation in a shell alert script; main threshold logic and alert delivery paths are unchanged.
> 
> **Overview**
> Fixes misleading disk-usage reporting in `scripts/disk_usage_alert.sh` by computing `used_pct` from the same APFS-aware basis as `used_gb` (`(total - available) / total`) instead of using `df`’s raw percent field.
> 
> This makes the OK/alert output percentage consistent with the used/total GB values, especially on macOS APFS where `df` can under-report used sp
