=== ET Run for PR #6261 ===
Run ID: ET_6261_20260417T150827Z
Session: 58c72f24
Timestamp: 20260417T150827Z


=== Prompt Sent to MiniMax ===
## Task: Fix PR #6261

### Original PR Title: [antig] refactor(rewards): migrate to centralized robust numeric extraction & simplify chaining

### Original PR Description:
## Background
The previous PR introduced a `_get_raw(keys)` helper function in `world_logic.py` to extract reward values with fallback keys (e.g. `xp_gained`, `xp`, `xpAmount`) from the LLM payload, and another local function `_extract_reward_value` to parse out strings like "500 XP" into `500`. 
These localized UI-layer hacks were fragile, bypassed central type constraints in the Pydantic schemas, and the `_get_raw` helper itself was redundant. We observed a regression where the rewards box was not visibly rendering for campaign `WQEl4sJb7RqWLndJK4GU` due to LLM units ("1,000 gp") tricking the coercer into returning `0`.

## Goals
1. Implement a globally robust, regex-based numeric extraction mechanism within `DefensiveNumericConverter` to natively strip units, commas, and handle LLM hallucinations across all numeric fields.
2. Remove redundant and over-engineered helper methods (like `_extract_reward_value` and `_get_raw`) from `world_logic.py`.
3. Simplify dictionary default chaining securely.

## Tenets
- **Backend Owns Integrity**: Ensure robust numeric extraction occurs at the schema conversion level, protecting all fields globally (HP, AC, Gold, XP).
- **Clean Code / Less Code**: Eliminate unnecessary helper functions where standard Python short-circuit logic naturally addresses the requirement.

## High-level description
This PR centralizes robust numeric parsing. In `DefensiveNumericConverter`, `convert_value` now includes an initial regex pass using `re.search(r"[-+]?[0-9]*\.?[0-9]+", value.replace(",", ""))` so that strings like `"850 XP"`, `"10/20"`, or `"1,000"` resolve safely to integers.
In `world_logic.py`, we removed the entire `_extract_reward_value` block and the newer `_get_raw` list-iteration helper. We instead leverage Python's native `normalized.get() or normalized.get()` sh
...


=== MiniMax Response ===
[Thinking: Looking at this PR, I need to understand what it's trying to fix:

1. The PR adds a robust numeric extraction mechanism in `DefensiveNumericConverter` using regex to handle LLM outputs like "500 XP", "1,000", etc.
2. It removes redundant helper functions from `world_logic.py`
3. It simplifies dictionary default chaining using Python's `or` logic

Let me analyze what needs to be done:

1. **In `defensive_numeric_converter.py`**: Add regex-based numeric extraction that strips units, commas, and ha...]
