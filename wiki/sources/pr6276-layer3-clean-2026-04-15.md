---
title: "PR #6276 Layer 3 CLEAN Status 2026-04-15"
type: source
tags: ["pull-request", "rewards-engine", "layer3-clean", "world-logic", "v4"]
source_file: "worldarchitect/.beads/issues.jsonl"
date: 2026-04-15
last_updated: 2026-04-15
---

## Summary
PR #6276 (feat/world-logic-clean-layer3) is ~75% implemented. Core v4 architecture is complete and CI-green. Layer 3 CLEAN stripping work has 3 of 4 beads closed, with the main work (rev-v4ci01) blocked on a new finding: the design doc's function equivalence mappings don't hold — world_logic deprecated functions have different behavior than their rewards_engine counterparts.

## Key Status Changes
- rev-v4ci04: **done** — CI line-count gate added (world_logic.py ≤ 9200)
- rev-v4ci02: **done** — integration tests already exist in test_rewards_engine_wiring.py
- rev-v4ci03: **done** — agents.py already refactored
- rev-v4ci05: **new** — behavioral equivalence audit required before redirecting
- rev-v4ci01: **open** — strip world_logic.py 8896→1500 lines (gated on rev-v4ci05)

## CI Gate Added
File: `.github/workflows/design-doc-gate.yml`
```
check_gate "world_logic.py line count" "9200" \
  "wc -l < mvp_site/world_logic.py"
```
Target is ~1500 lines; starting at 9200 allows PR to pass while tracking progress.

## Design Doc Equivalence Gap (Critical Finding)
Design doc maps world_logic deprecated functions to rewards_engine equivalents claiming behavioral equivalence. Audit shows:

| world_logic function | design doc says → | actual rewards_engine | divergence |
|---|---|---|---|
| `_should_emit_level_up_rewards_box` | `should_show_rewards_box` | `should_show_rewards_box(rb)` | world_logic takes `(game_state, rewards_box)` and calls `_resolve_level_up_signal` for cross-check; rewards_engine version only checks `rewards_box` alone |
| `_enforce_rewards_box_planning_atomicity` | `_enforce_atomicity` | `_enforce_atomicity(rb, pb)` | world_logic does choice injection + stale scrubbing + suppression; rewards_engine is simple null-check (if either None, both None) |
| `_project_level_up_ui_from_game_state` | `project_level_up_ui` | `project_level_up_ui(gs)` | world_logic returns `rewards_box` only; rewards_engine returns `(rewards_box, planning_block)` tuple |

## What Stays in world_logic.py
Per design doc line 805, `_maybe_trigger_level_up_modal()` stays (modal injection, called by llm_parser.py:636).
`process_action_unified()` and `get_campaign_state_unified()` stay (MCP API entry points at mcp_api.py:316/:333).

## Functions That Can Be Deleted (after equivalence audit)
1. `_should_emit_level_up_rewards_box()` (line 1718)
2. `_project_level_up_ui_from_game_state()` (line 4235)
3. `_enforce_primary_rewards_box_postcondition()` (line 1291)
4. `_enforce_rewards_box_planning_atomicity()` (line 2886)

## Integration Tests
4 tests already exist in `test_rewards_engine_wiring.py`:
- `test_llm_parser_streaming_calls_canonicalize_rewards_once`
- `test_llm_parser_streaming_does_not_double_canonicalize`
- `test_llm_parser_nonstreaming_calls_canonicalize_rewards_once`
- `test_no_double_canonicalization_in_any_path`

## PR Status
PR #6276: OPEN on `feat/world-logic-clean-layer3`, all 6 CI grep gates green + new line-count gate.
