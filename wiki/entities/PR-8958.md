---
title: "PR #8958 — worldarchitect.ai dice-roll maxLength bounds"
type: entity
tags: [pr, worldarchitect.ai, dice-roll, truncation, max-tokens, human-held]
date: 2026-08-17
last_updated: 2026-08-17
sources:
  - /Users/jleechan/llm_wiki/raw/feedback_2026-08-17_factory_gh_token_and_poll_cadence.md
---

## Summary
PR #8958 in `jleechanorg/worldarchitect.ai` fixes `mechanics.rolls[].label` unbounded string growth that was hitting `MAX_TOKENS` truncation during streaming. The PR was stuck at `HUMAN_HELD · CONFLICTING · PENDING` because the Linux auto-factory daemon on jeff-ubuntu couldn't probe it (`gh pr view` returning HTTP 403).

## URL
https://github.com/jleechanorg/worldarchitect.ai/pull/8958

## Branch
`fix/dice-roll-label-maxlength-truncation-repro`

## Status (as of 2026-08-17)
- **Pre-fix**: `HUMAN_HELD · CONFLICTING · PENDING` — remediated in `c057f1ff7`; awaiting manual rebase across history rewrite. Daemon probe stuck at `INTAKE → SKIPPED_INELIGIBLE` because of HTTP 403 rate-limit errors.
- **Post-fix (PR #651 deployed)**: `EXISTING_PR_ADOPTED × 2` within 90 seconds of daemon restart. Factory back to actively processing.

## Beads
- `rev-rewt0` (P1 bug) — REPRO+FIXED — the remediation is committed at `c057f1ff7`
- `rev-z046a` (P2 task) — TRACK: PR #8958 — reviewed APPROVE at `2c8e4a118d`; needs `/green` + `/es` evidence

## Unblocking cause
The PR was collateral damage from the Linux auto-factory daemon's over-polling. [[PR #651]] (dark-factory) fixed the underlying cause by slowing `fast_tick_secs` and adding an explicit `GH_TOKEN` drop-in. Once the daemon's GitHub API budget recovered, the factory resumed normal probing and adopted PR #8958.

## Related
- [[PR #651]] — the unblocking fix
- [[worldarchitect.ai]] — the repo
- `rev-rewt0` — bug bead